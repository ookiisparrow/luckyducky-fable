# 数据库 / 云存储 权限期望表（根因账本 #9，固化验收单 X）

> 「敏感写一律走云函数」的前提是**客户端不能直接写库**。这道闸在云开发控制台设置、**代码看不到**（CLI 也读不到安全规则）——故是 git 外资产，须人工核对。本表是期望档位的正册副本（生产仓验收单 X 固化），上线前/迁移后逐项核一次。
>
> 怎么核：微信开发者工具 → 云开发 → 数据库 → 逐集合 → 右上「权限设置」看当前档位。**绝对不能有集合是「所有人可读写」**。

## 37 个集合期望档位

| 集合 | 期望档位 | 写入口 |
|---|---|---|
| `users` | 仅创建者可读写 / 仅后台 | login / updateProfile |
| `orders` | 仅创建者 / 仅后台 | createOrder / shipOrder / confirmReceive / confirmEnter |
| `qrcodes` | 仅后台（客户端禁写） | genQrcodes / activateCourse |
| `activations` | 仅创建者 / 仅后台 | activateCourse / confirmEnter |
| `progress` | 仅创建者 / 仅后台 | trackEvent |
| `events` | 仅创建者 / 仅后台 | trackEvent |
| `reviews` | 仅创建者 / 仅后台 | submitReview |
| `afterSales` | 仅创建者 / 仅后台 | applyRefund / refundCallback / adminApi |
| `adminConfig` | 仅后台（口令/设置） | adminApi |
| `productsDraft` | 仅后台 | adminApi.saveDraft |
| `coursesDraft` | 仅后台 | adminApi.saveCourseDraft |
| `cards` | 仅后台 | adminApi.saveCard |
| `uploadChunks` | 仅后台（临时中转） | adminApi.uploadChunk |
| `config` | 仅后台（支付开关/flowId） | 控制台/CLI |
| `inventory` | **仅后台（客户端禁读写）** | adminApi.saveStock / createOrder（经 kit/inventory·库存#1） |
| `auditLog` | **仅后台（客户端禁读写）** | adminApi 分发处（经 kit/audit recordAudit·操作审计#4） |
| `products` | **仅后台（客户端禁读写）** | adminApi.publishProduct |
| `courses` | **仅后台（客户端禁读写）** | adminApi.publishCourse / seedCourses |
| `content` | **仅后台（客户端禁读写）** | adminApi.saveHomeContent |
| `rateLimit` | 仅后台（内部频控，客户端禁读写） | kit.throttle（adminApi 认证失败计数，根因#13；**throttle 动态建，部署后须同锁仅管理端**） |
| `kfState` | 仅后台（微信客服内部状态，客户端禁读写） | kfCallback / kit.wecom（access_token 缓存·sync cursor·msgid 去重；**CS 域动态建，部署后须同锁仅管理端**） |
| `kfIdentity` | 仅后台（身份桥接映射，客户端禁读写） | kfBind（external_userid→openid·根因#3 不信前端；**动态建，部署后须同锁仅管理端**） |
| `feedback` | 仅后台（意见反馈·客户端只写不读·仅管理端可读） | submitFeedback（运营钩子①·待办#23·withOpenId+withRateLimit；**动态建，部署后须同锁仅管理端**） |
| `wxBills` | 仅后台（微信账单缓存，客户端禁读写） | adminApi.downloadBill（S16 外部对账·经 kit/wxpay 拉微信交易账单；**动态建，部署后须同锁仅管理端**） |
| `checkpoints` | **仅后台（客户端禁读写）** | adminApi.saveCheckpoints（def 节点定义）/ submitCheckpointPhoto（sub 用户拍照·imgSecCheck 过后·节点诊断 B2.2；**动态建，部署后须同锁仅管理端**） |
| `conversations` | **仅后台（客户端禁读写）** | cs/kfCallback（入站/出站会话归档·B5.1 外包管控底座）/ adminApi.searchConversations 读（含 PII 会话全文·customer:view 闸；**动态建，部署后须同锁仅管理端**） |
| `kb` | **仅后台（客户端禁读写）** | adminApi.saveKb（知识库整体覆盖式策展·FAQ 答案单源·后台360工作站 B4.1；客服 bot dispatch 只读·**动态建，部署后须同锁仅管理端**） |
| `csat` | **仅后台（客户端禁读写）** | cs/kfCallback（recordCsat·会话后评分 1-5+备注·后台360工作站 B4.3）；adminApi.getCsatReport 只读·**动态建，部署后须同锁仅管理端** |
| `csSession` | **仅后台（客户端禁读写）** | 承面C 坐席台 actions（B6·会话态 pending→active→closed·后台360工作站；阶段0地基先声明·B6.1 起写·**动态建，部署后须同锁仅管理端**） |
| `agentState` | **仅后台（客户端禁读写）** | 承面C 坐席台 actions（B6·坐席在线态 online/busy/offline·后台360工作站；阶段0地基先声明·**动态建，部署后须同锁仅管理端**） |
| `materials` | **仅后台（客户端禁读写）** | adminApi.saveMaterial / adjustStock（经 kit/scmStock 门1·进销存 SCM-0·守卫 material-stock-single-seam；**动态建，部署后须同锁仅管理端**） |
| `suppliers` | **仅后台（客户端禁读写）** | adminApi.saveSupplier（厂家/织女主档·SCM-0；**动态建，部署后须同锁仅管理端**） |
| `purchaseOrders` | **仅后台（客户端禁读写）** | adminApi scmPurchase actions（车道 A·SCM-0 先登记；**动态建，部署后须同锁仅管理端**） |
| `outworkOrders` | **仅后台（客户端禁读写）** | adminApi scmOutwork actions（车道 B·SCM-0 先登记；**动态建，部署后须同锁仅管理端**） |
| `assemblyOrders` | **仅后台（客户端禁读写）** | adminApi scmAssembly actions（车道 C·SCM-0 先登记；**动态建，部署后须同锁仅管理端**） |
| `stockLedger` | **仅后台（客户端禁读写）** | 仅 kit/scmStock（applyStockMoves 确定性流水·SCM-0·守卫 scm-ledger-idempotent；**动态建，部署后须同锁仅管理端**） |
| `bomProfiles` | **仅后台（客户端禁读写）** | adminApi scmBom actions（车道 C·SCM-0 先登记；**动态建，部署后须同锁仅管理端**） |

> ⚠️ **更正（2026-06-14 验收单 X 实测·计数随 COLLECTIONS 同步=37）：上表 37 个集合一律「所有用户不可读写」(仅管理端·`inventory` 库存#1/`auditLog` 审计#4/`feedback` 反馈#23/`wxBills` 外部对账#S16/`checkpoints` 节点诊断#B2.2/`conversations` 会话归档#B5.1/`kb` 知识库#B4.1/`csat` 满意度#B4.3/`csSession`·`agentState` 承面C#B6 同档)**——小程序**零直接读库**（全走云函数 getProducts/getCourses/getContent），故 products/courses/content **不需要公开读**；原标「所有人可读」是错的，且让 `courses` 里的 `videoFileId` 被客户端直查泄露（付费视频漏口）。商品图/头像显示走的是**存储**的 `cloud://` 公开读，与集合读权限无关。
> **核对法（行为实测）**：客户端直接 `add`/`get` 各集合 → **一律被拒**（-502003）才算过。切勿设「所有人可读写」或「仅创建者可读写」（后者客户端仍能写/读自己那条，曾是 13 集合可写的真洞）。
> **快速核验/批量锁（CLI·2026-06-25 实证）**：`tcb permission get collection -e <env>` 查全部档位（`adminonly`=仅管理端✓·`private`=仅创建者✗·`readonly`=所有人可读✗）；`echo y | tcb permission set collection:<名> --level adminonly -e <env>` 批量锁（交互确认须管道喂 y）。**2026-06-25 全 24 集合核+锁=adminonly**（新增 `checkpoints` 第 25·B2.2 动态建·上线前同锁），堵了 `rateLimit`/`wxBills` 两处 private 真洞（动态新建默认 private）；未建集合(reviews/inventory/auditLog/feedback)已预设 adminonly 规则、首写即锁。**新动态建集合上线前都用 `permission get` 复查、有 private 即 set adminonly**。

## 重写线净新增集合（rewrite/shared/src/collections.ts·上表 37 之外）

> 旧线 37 集合之外，重写线净新增下列集合。守卫 `rw-collection-perm-registered` 按名核对本表全覆盖（上表标题计数仍随旧线 37 走·守卫 `collection-count-synced`）。**动态建默认 private=真洞**（rateLimit/wxBills 前科）：每个集合首写后（或预建时）须 `echo y | tcb permission set collection:<名> --level adminonly -e <env>` 锁仅管理端。

| 集合 | 期望档位 | 写入口 |
|---|---|---|
| `anomalies` | **仅后台（客户端禁读写）** | recordAnomaly 单口写入（bug 收集器账本·指纹去重；**动态建，部署后须同锁仅管理端**） |
| `inspectRuns` | **仅后台（客户端禁读写）** | runInspection 单口写入（巡检机体检报告·红绿灯；**动态建，部署后须同锁仅管理端**） |
| `secureConfig` | **仅后台（客户端禁读写·凭证承重：企微 Secret/Token/AESKey/CorpId + 微信支付商户 API 私钥/证书序列号）** | adminApi.saveSecureConfig 单口写入（kit/secureConfig 读库优先 env 兜底·零回显；**动态建——admin 首次保存即建档，建档后立即 `permission set adminonly`；未锁前客户端虽读不到服务端写入的 wxkf/wxpay doc（无 _openid），但可 add 垃圾档，污染面真存在**） |
| `orderIdempotency` | **仅后台（客户端禁读写）** | createOrder 单口写入（批E·P1 防网络超时重试双建单·claim _id=(openid+客户端幂等键) 确定性哈希；**动态建，部署后须同锁仅管理端**——未锁前客户端可直接 `add` 猜测/占位 claim 文档，虽因需知晓受害者 openid+随机幂等键而难精确撞车，但仍是可避免的写入面） |

## 云存储

- `products/` `avatars/`（商品图/头像，公开展示）：**仅创建者可写、所有人可读**。
- `videos/`（**付费课视频**，审计 P1）：客户端经云函数 `getPlaybackUrl` 服务端鉴权后拿短时效 URL，不直接 `getTempFileURL`。
  - **保护靠「fileID 不可得」而非存储路径私有**（2026-06-14 验收单 X 实测更正：云开发存储规则只认创建者、不认路径，`resource.path` 写法报 InvalidParameter，做不到「products 公开读 + videos 私有读」）。
  - 实际保护链（四条合起来 = 客户端永远拿不到 videoFileId）：① `getCourses` 剥 videoFileId（只返 `hasVideo` 布尔）② `getPlaybackUrl` 只发短时效 URL、不发 fileID ③ `getMyCourses` 不带 ④ **`courses` 集合 仅管理端**（客户端直查也拿不到）。⇒ videos/ 存储即便公开读也无人能取。
  - 核对（验收单 X 扩展，已实测）：客户端 `db.collection('courses').get()` 被拒（-502003）= videoFileId 不可得；未购课账号付费段 `getPlaybackUrl` 返 `NOT_ENTITLED`。

## 备注：debt #14 与确定性 _id

- 写侧确定性 _id（submitReview `orderId__productId`、afterSales `orderId__productId`、qrcodes/code、seed `_id`=业务 id）让「仅创建者可读写」即够（撞 _id 抛错=天然唯一）。
- users/progress 已接确定性 _id（login/updateProfile/trackEvent 经 `ensureDoc`，**债#14 已根治**：并发首写撞号即幂等、不重复建档）；仍先 `where` 命中老随机 _id 档作惰性迁移，权限模型不变（仍仅创建者/仅后台）。
