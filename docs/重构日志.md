# 重构日志（样板房专属）

> 本文件记重构线批次结论与证据（Phase A / B0–B9 = 第一次重构；下新增「## 第二次重构（演进）」段续记）；生产线工作日志已归 `archive/工作日志-生产线-至20260612.md`（冻结于 0612）。三支柱：`根因账本.md`（病根）+ `现状与路线.md`（live 地图；第一次重构总计划 v5 见 `archive/`）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`archive/项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-07-03 · 🧹 控制台定向体检批3：P3 卫生五项（mock 拆包/路径消毒/流水补痕/分累加/operator 溯源）
**来源**：定向体检 P3 顺手项五条。**做**：① agent mock 懒加载（静态→动态 import·Vite 拆独立 chunk·产物实证主包 0 演示数据串；残留：chunk 文件仍在托管目录但永不加载、内容全虚构样本·可接受）；② 上传路径消毒（storeImage pid / getVideoUploadMeta courseId 补 `[^\w-]` 白名单·同 name 口径·防 '../' 入对象键）；③ runAssembly 第④步 fg 流水写失败补 error 痕（同 ship ledger 口径·库存本身③已正确）；④ customer360 画像 totalSpent 改分累加回元（0.1×3=0.3 精确）；⑤ 发货核销流水 operator 贯认证账号（曾写死 'admin'·与 SCM 各线 agentId 口径一致）。
**守卫**：repoCheck `agent-mock-lazy-only`（禁静态 import mock）+ 行为守卫 `upload-path-sanitized`；④⑤ 各加用例进既有 suite（customer360/scmShipLedger）。反向自检×4（静态 import/去消毒/回写死 admin/回浮点直加→各红→还原绿）。
**验证**：check 全绿（守卫 125+5·测试 1026）·build:cloud(38)/admin/agent 过·agent 主包 grep 实证演示数据 0 命中。

## 2026-07-03 · 🧰 控制台定向体检批2：401 引导重登 + 库存有界读 + 对账截断标注（深审 P2×3）
**来源**：定向体检 P2 三条——② 前端无 401 处理（令牌过期/停号卡死报错页）+ 丢弃 caps；③ listInventory 裸 .get() 真 SDK 默认 100 条静默截断（SKU 破百少显示）；④ getBillMatch 触 CAP(2000) 无标注（老窗口误报「微信有我方无·最危险」）。
**做**：② admin/agent 请求层统一 401→logout+回登录页、403→提示不登出；登录门禁（无 '*'/customer:view 拒进控制台、指去 /agent；菜单级 caps 隐藏**刻意不做**——现网仅两角色、别为不存在的中间角色建机制·决策§22）。③ getInventory 分页取齐（100/页·封顶 1000）+truncated 如实报（页/接口层贯穿提示）；**桩对齐真 SDK：裸 .get() 默认 100 封顶**（根因#8 桩≠真藏了这 bug·对齐后 1016 测试零破＝无同类漏网，且成常驻绊网）。④ getBillMatch 返 approx（触顶即标）+ 对账页红字提示「差异可能是截断假象」。
**守卫**：`inventory-reads-bounded`（灌150条锁不被100截断）+ `billmatch-approx-flag`（灌满CAP锁标注）+ repoCheck `admin-auth-expiry-handled`（请求层必认401）。反向自检×3（回退单页读/写死approx/去掉logout→各红→还原绿）。
**验证**：check 全绿（守卫 124+5·测试 1021）·build:cloud(38)/admin/agent 过。

## 2026-07-03 · 🔐 控制台定向体检批1：登录凭证不落盘（深审 P1·根因#3）
**来源**：用户「针对性扫描控制台端服务」定向体检 P1——admin/agent 前端把口令原文存 localStorage 且每请求回传（外包机器上商户超管口令＝失守；企微免登早有安全形状、口令路径没跟上）。
**做**：口令登录改「登录成功签发高熵会话令牌」——`issueSession`/`resolveSession` 单源（lib.ts·企微免登同款收编，wecomSessionHash 单槽退役）：账号 doc `sessions` 数组容多设备（控制台+坐席台不互踢·cap 8 剪最旧·12h·sha 入库不存明文）；checkKey 口令未命中 fallback 会话解析（fail-closed：过期 SESSION_EXPIRED/停号 ACCOUNT_DISABLED 拒）；前端只存令牌（无令牌拒登·mock 同形）；**口令作 key 兼容不破**（旧已部署前端）。
**守卫**：新 repoCheck `admin-session-token-not-password`（持久化点窗口扫描无口令 + 后端必经 issueSession·登录请求一次性上送放行）+ 行为守卫 `admin-session-issued`（adminSession.test.js 7 例：签发/令牌鉴权非提权/过期/停号/多设备/超容剪最旧/口令兼容）。反向自检×3：前端回存口令→结构红；后端去签发→结构+6 测试红；解析器无视过期→行为红；各还原绿。
**验证**：`npm run check` 全绿（守卫 123 repoCheck+5 fileRule·测试 1016）·build:cloud(38)/admin/agent 过。**部署语义**：adminApi 重部署后旧企微会话（≤12h）失效一次（企微内静默重签无感）；旧控制台前端存的口令继续可用，换新前端起不再落盘。

## 2026-07-03 · 📦 发货工作台三步向导（R32·主流程同上新形态·零后端）
**来源**：用户从「出库/发货想用扫码枪提效」逐轮收敛拍板（流程 7 步逐项确认 + 「主要流程·参照上新」+ 拣货范围=全部待发货）——现实约束：无电子面单 API，运单号在快递员上门逐箱贴条码那刻才产生 → 打自家内部标签贴箱（QR=订单id），快递员贴完码后「扫箱码选单+扫快递码发货」两段扫码，全程不打字。
**做（零后端·纯 admin 前端）**：① 纯逻辑单源 `utils/fulfill.js`（pickSummary 拣货聚合 / labelData 标签派生[短编号 MMdd-随机段·收件人·产品名×数量·总件数·**不含金额**] / **classifyScan 扫码分类安全判序**[id 形状但不在队列→order-not-in-queue·挡「已发货箱旧码被当运单号发出去」] / buildLabelHtml 打印页[@page 预设 100×180/76×130·每单一页·HTML 转义·>6 行 dense]）；② `Fulfill.vue` 向导壳（`/fulfill/step/1-3`·数据归壳：游标拉全量 paid+Set 去重+25 页保险丝·三步同一队列数字互相咬合·sessionLog 切步不丢）+ `pages/fulfill/` 三步组件（拣货表 / 打印[QRCode.toDataURL→window.open 新窗 w.print()·「已打印」localStorage 记忆+交集 GC·纸张预设记本机] / 扫码[粘性物流公司·失焦暂停横幅·核对卡与纸标签同 labelData 口径·shipOrder 错误码人话映射·失败保持选中重扫]）；③ Sidebar trade 组特例首项（同「商品与上新」形态·三步子菜单）+ **顺修既有 bug**：两向导共用 `:n` 参数 stepActive 互相误高亮——各限定势力范围；④ COMPANIES 从 Orders.vue 抬升 utils/fulfill.js 单源。
**守卫**：零新增 id（`admin-nav-route-synced` 罩注册面[反向①假孤儿路由→红]；分类器安全判序由测试锁[反向②抽掉 order-not-in-queue 判序→关键断言红]；出纸/扫码枪物理层归根因#8 靠人）。
**测试**：新 `tests/admin/fulfill.test.js` 20 例（聚合/短编号/标签派生不含 amount/分类器边界[CR 后缀·月=88·随机段 0 头→tracking]/打印 HTML[转义·无￥·dense]）·988→**1008**。
**验证**：`npm run check` 全绿 · `build:admin` 过 · **headless Edge（CDP+playwright-core·假 API 路由拦截喂样本）26 项断言全过**：侧边栏/三步/聚合数字/打印 HTML 内容/扫码全链[选单→发货→队列递减→旧码拦→feeMismatch 挡]/上新高亮回归/console 零报错 + 1440 宽截图核视觉（顺手修窄窗流水面板挤扁核对卡→flex-wrap）。**剩靠人（真纸真枪·根因#8）**：100×180 实纸打一张验 QR 可扫/分页不串；扫码枪扫纸标签出完整 16 位+回车；真快递面单条码形态核一次；真快递员场景走通一单全链。登记：需求清单 R32（33/33·31✅·顺修 R31 stale「未开工」→全 LIVE）；系统事实零改动（零新 action/集合）。

## 2026-06-15 · 🏷️ v0.9.2 里程碑（代码里程碑·新会话先读这段）

**v0.9.2 = 代码里程碑**（tag 在 main `d182075`），收束 v0.9.1 tag 以来三条线；**不是新部署**（部署状态见末段）。
- **① v0.9.1 验收收尾**：验收单 X/Y/Z 真机真钱过——X 库权限客户端实测抓真 P0（13 集合曾可写 + `courses` 公开读漏 `videoFileId`）→ 16 集合锁仅管理端；本仓接管 tcb 部署闸（deny-all → 生产仓模型，敏感函数 `ask`）。
- **② 产品打磨**（前端 miniapp·用户可见）：微信登录半屏弹窗 + 启动加载页 + 登录流程完善（退出/一键登录/默认昵称 `Lucky friend`+随机鸭头像）；首页/购物车 UI 批（品牌 logo/卡片等高/购买滚动居中/顶栏吸顶/结算条对齐）；「我」页未激活课程空态；**返回重构**（Tab 页「再按一次退出」= scroll-view + page-container）；新 skill `/frontend-check`。
- **③ 治理脊柱**（第二次重构·演进；纯文档/守卫/工具，**不碰运行时**）：P0 知识地基（立 `演进地图`）→ 文档精简+架构校准（活文档瘦 ~40%）→ P1 需求显形（`需求清单` 31 条单源 + 逐条复核，修 R22 跑偏）→ 文档架构重设（19→13 份，客观/主观分治 + 反膨胀守卫 `docs-budget`）→ **P2 需求→守卫闭环**（`requirement-trace` 守卫 + `npm run trace` 爆炸半径）→ **P2b 须守卫覆盖**（钱/状态/安全类需求须有对应类守卫）。

**当时态（v0.9.2·2026-06-15 快照·勿读成当前数；当前计数见 `现状与路线`§3 + `自动化验证系统`）**：测试 **278** 全过；守卫 **15 repoCheck + 5 fileRule**；活文档 **13**；skills **7**；CLAUDE 144/180。生产仓 `luckyducky-miniprogram` 零改动（HEAD `2dd5a72`，回滚基线）。
**部署状态（honest，勿读成"已全上线"）**：后端云函数仍是 v0.9.1 部署的 **28 个**（治理 work 不碰运行时、无需重部署，后端已是生产随时服务）；前端登录/UI/返回 等产品改动**已合 main、未发布生产**——发布属「全客户上线」动作（`build:mp-weixin` + `urlCheck`→true + 微信后台合法域名 + 审核），仍待。
**容量评估（2026-06-15 看码评估·非压测）**：4 位数/天 架构可承——并发正确性稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）。上量前必清 4 件（详 `待办与债` §上量就绪）：① 数据库套餐档位（控制台查·最大未知）② 视频 CDN/VOD（R21 搁置·裸云存储吐视频带宽=头号成本/风险）③ 看板内存聚合 1000 条/表封顶（4 位数当天 GMV 即少算）④ 索引 + **从未压测**（根因#8 同理：单人能用 ≠ 千人同时）。

## → 已退役批次（卷档索引 `archive/README.md`）
- **2026-07-01 ~ 07-02**（进销存 ERP 五车道全线 / M⑦ 推送·免登·移动化 / 承面C 接真接口+三车道整合 / 深度审核批 / 深审客服批 / admin 侧边栏重组 / 07-01 P0 链）→ `archive/重构日志-202607上.md`
- **2026-06-28~30**（客服自建线：选型→拆批→B0/B1.1→三波并行→M⑤ RBAC + 品牌字体真机修 + dev:h5）→ `archive/重构日志-202606客服自建.md`
- 更早：`archive/重构日志-202606上半.md` / `-202606下.md` / `-第一次重构.md`
