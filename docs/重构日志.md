# 重构日志（样板房专属）

> 本文件记重构线批次结论与证据（Phase A / B0–B9 = 第一次重构；下新增「## 第二次重构（演进）」段续记）；生产线工作日志已归 `archive/工作日志-生产线-至20260612.md`（冻结于 0612）。三支柱：`根因账本.md`（病根）+ `现状与路线.md`（live 地图；第一次重构总计划 v5 见 `archive/`）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`archive/项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-06-15 · 🏷️ v0.9.2 里程碑（代码里程碑·新会话先读这段）

**v0.9.2 = 代码里程碑**（tag 在 main `d182075`），收束 v0.9.1 tag 以来三条线；**不是新部署**（部署状态见末段）。
- **① v0.9.1 验收收尾**：验收单 X/Y/Z 真机真钱过——X 库权限客户端实测抓真 P0（13 集合曾可写 + `courses` 公开读漏 `videoFileId`）→ 16 集合锁仅管理端；本仓接管 tcb 部署闸（deny-all → 生产仓模型，敏感函数 `ask`）。
- **② 产品打磨**（前端 miniapp·用户可见）：微信登录半屏弹窗 + 启动加载页 + 登录流程完善（退出/一键登录/默认昵称 `Lucky friend`+随机鸭头像）；首页/购物车 UI 批（品牌 logo/卡片等高/购买滚动居中/顶栏吸顶/结算条对齐）；「我」页未激活课程空态；**返回重构**（Tab 页「再按一次退出」= scroll-view + page-container）；新 skill `/frontend-check`。
- **③ 治理脊柱**（第二次重构·演进；纯文档/守卫/工具，**不碰运行时**）：P0 知识地基（立 `演进地图`）→ 文档精简+架构校准（活文档瘦 ~40%）→ P1 需求显形（`需求清单` 31 条单源 + 逐条复核，修 R22 跑偏）→ 文档架构重设（19→13 份，客观/主观分治 + 反膨胀守卫 `docs-budget`）→ **P2 需求→守卫闭环**（`requirement-trace` 守卫 + `npm run trace` 爆炸半径）→ **P2b 须守卫覆盖**（钱/状态/安全类需求须有对应类守卫）。

**当时态（v0.9.2·2026-06-15 快照·勿读成当前数；当前计数见 `现状与路线`§3 + `自动化验证系统`）**：测试 **278** 全过；守卫 **15 repoCheck + 5 fileRule**；活文档 **13**；skills **7**；CLAUDE 144/180。生产仓 `luckyducky-miniprogram` 零改动（HEAD `2dd5a72`，回滚基线）。
**部署状态（honest，勿读成"已全上线"）**：后端云函数仍是 v0.9.1 部署的 **28 个**（治理 work 不碰运行时、无需重部署，后端已是生产随时服务）；前端登录/UI/返回 等产品改动**已合 main、未发布生产**——发布属「全客户上线」动作（`build:mp-weixin` + `urlCheck`→true + 微信后台合法域名 + 审核），仍待。
**容量评估（2026-06-15 看码评估·非压测）**：4 位数/天 架构可承——并发正确性稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）。上量前必清 4 件（详 `待办与债` §上量就绪）：① 数据库套餐档位（控制台查·最大未知）② 视频 CDN/VOD（R21 搁置·裸云存储吐视频带宽=头号成本/风险）③ 看板内存聚合 1000 条/表封顶（4 位数当天 GMV 即少算）④ 索引 + **从未压测**（根因#8 同理：单人能用 ≠ 千人同时）。

## 2026-07-01 · 🅲 承面C 阶段0地基 Batch 2（坐席台契约扇出闸·M0.a 契约 + M0.c 入队·cap 留车道A）
**来源**：`docs/后台360工作站/承面C工单.md` §2——开 3 车道并行前 master 串行立齐「契约 + 会话写入者」，否则各造一套 API＝整合灾难。
**做**：**M0.a** 坐席台 8 action（listQueue/claim/release/sendAgentMessage/getThread/setAgentStatus/escalate/close）I/O 契约单源 `packages/shared/src/csAgentDesk.ts`（纯类型·引 `CsSessionStatus`·车道 A 实现 + 车道 B 对 mock 共同遵·经 `@luckyducky/shared` 引·index 同步导出 `./cs`+`./csAgentDesk`）；**M0.c** `dispatch.ts` 加 `enqueueSession` + `kfCallback` 转人工分支调用——转人工即 upsert `csSession` `pending`（确定性 _id=`wxkf:<openKfId>:<euid>`·`.add` 撞 id 天然幂等·**不 clobber 已认领的 active**·根因#1），让 csSession 有真实 writer（listQueue 有数据）。
**cap/守卫的刻意不做（防过度工程·§7）**：cap `agent:handle` 运行时 wire（`ROLES.outsourced`+`ACTION_CAPS`）+ `order-transitions-declared` 扩扫 `functions/cs/` **归车道 A·与 action 同批落**——现网 ACTION_CAPS 只登记有实现的 action、未 gate 者默认拒 `admin:write`（守卫 `agent-rbac-gated` 在），空标 8 个不存在的 action = 空守（违 `lib.ts`「不立无 action 消费者的空 cap」+ Batch 1 提交明示）；守卫扫 `.collection('字面量')`/`transition('csSession',…)`，本批入队用 `COLLECTIONS.csSession` 常量 + 非 transition，扩扫要等车道 A 的字面量流转才真发挥。契约头注已文档化 cap 要求·车道 A 引同一 cap 串防漂移（根因#5）。
**守卫（反向自检×2）**：M0.c 行为测试（转人工入队 pending / active 不 clobber / 缺 euid 不写）——反向自检 `.add`→clobber 式 `.doc().set`→幂等测试红→还原绿；M0.a 契约在 typecheck 范围——反向自检引不存在类型→`build:shared` 报错→还原绿。**未加新结构守卫**（契约靠 typecheck + 消费侧类型约束·入队靠行为测试·为单条常量写加结构守卫＝过度工程）。
**指标**：守卫 **112 不变**·测试 783→**787**（+4）·集合 30 / 云函数 37 **不变**·`npm run check` 全绿·`build:cloud(37)` DONE。**剩**：master 起并行（预铺 csA/csB/csC worktree + `npm install`）→ 用户开 3 CC 贴工单 §6 → master 跑 `/loop` 整合。生产仓零改动。

## 2026-07-01 · 🔎 查订单身份桥接改走平台原生 batchget（idconvert 撞 48002 墙后·真机逼出）
**来源**：查订单真机验 → login idconvert 恒 `48002`（诊断布尔日志 pinpoint：hasUnionid/hasToken=true、hasExt=false·idconvert 是客户联系接口·Lucky Ducky 授权进客户联系可调用应用后**仍** 48002=更深权限墙）。用户拍「方案优先平台原生」→ 改用微信客服自己的顾客接口。**做**：① login 从 idconvert 改为**只存 unionid 到 users**（不调外部 API·不持密钥）；② kfCallback 收消息时 `ensureKfIdentity`——经**微信客服 `kf/customer/batchget`**（新 wecom helper）反查 external_userid→unionid→users.openid→建 kfIdentity（`resolveOpenid` 不动·读它查订单）·**绕客户联系 idconvert 48002**；已建映射即跳·best-effort 不反噬消息处理。**守卫**：`login-kf-identity-bridge` 改焊两端（login 存 unionid + kfCallback batchget 写 kfIdentity·反向自检过）。**测试**：login.test 重写（存 unionid）+ kfWecom 加 kfCustomerBatchget 单测。**指标**：守卫 111 不变·测试 774 不变·云函数 37·`npm run check` 全绿·`build:cloud(37)`·dry-run login+kfCallback。**剩**：部署 + 真机（顾客登录存 unionid → 发客服消息 batchget 建映射 → 查订单；batchget 是否受「顾客须授权/绑开放平台」限属根因#8 真机验）。

## 2026-07-01 · 📡 微信客服活体探针 kfHealthProbe（上线后静默故障告警·补调试日志 AA 隐患）
**来源**：独立端→企业微信内迁移中反复踩静默故障（FORGED/60020/40001·断了只在日志、没人盯就不知道·根因#8/调试日志 AA「上线后活体探针缺失」）→ 用户拍板补探针。**做**：新 `cs/kfHealthProbe`（30 分定时 `kfProbeTimer`·`isServerCall` 防客户端刷告警）→ `getAccessToken`(探 Secret) + `listKfAccounts`(新 wecom helper·真调 `kf/account/list` 探可信IP/权限·gettoken 抓不到 60020 须真调)→ 静默故障经 `notifyAlert` **唯一 botpush 接缝**推企微告警（webhook 已配·守 bot-push-single-seam）。**诚实边界**：回调验签漂移(FORGED)那类纯自动测不了（需合成真顾客消息）·靠「变更后手动发一条验」另兜。**守卫**：新 `kf-health-probe-wired`(roots #12·焊 ① isServerCall ② 真调 getAccessToken+listKfAccounts ③ 经 notifyAlert 接缝)·反向自检过。**指标**：守卫 110→**111**·测试 768→**774**(+5 探针 + 元测)·云函数 36→**37**·`npm run check` 全绿·`build:cloud(37)`·dry-run 仅 kfHealthProbe（wecom/kit 改经 tree-shaking 不波及）。**剩**：部署 + 定时真跑（故障时手机收告警）。

## 2026-07-01 · 🔗 P0 链② login 端客服身份桥接（开放平台认证通过后·查订单地基·不 republish·login 不持密钥）
**来源**：用户「1，开放平台通过」——① 绑开放平台认证通过 → 小程序登录能拿 unionid。补 login 端桥接（原「前端剩 login 存 unionid」）让客服会话「查你的订单」可用。**设计**：桥接放 login 云函数（backend-only·不 republish 发版），且 **login 不持微信客服密钥**——加 `getCachedKfToken`(kit) 复用 kfCallback 维护的缓存令牌做 idconvert（尊重刚做的密钥收敛·根因#3 密钥不扩散到 login）。
**做（cloud·best-effort 不反噬登录）**：① kit `currentUnionId`（经 kit 取 UNIONID·守 T2 禁裸 getWXContext·`as any` 绕 SDK 类型滞后）+ `getCachedKfToken`（只读 kfState 缓存令牌·不 gettoken 不需 secret）；② login 首次（有 unionid + 未 kfBound + 有缓存令牌 + idconvert 得 ext）建 `ext→openid` 映射写 kfIdentity + 标 `users.kfBound`（幂等·省 idconvert 限频）；取不到令牌/48h 会话身份就跳过下次再试。
**守卫（反向自检·109→110）**：新 `login-kf-identity-bridge`(roots #3) 焊 login 三链（currentUnionId 取 + unionidToExternalUserid 转 + 写 kfIdentity）·防桥接被摘致查订单静默失效。反向自检：去 currentUnionId→红→还原绿。
**测试**：桩加 `setUnionId` + getWXContext 返 UNIONID；新 `login.test.js` 4 例（桥接建映射+标 kfBound / 无 unionid 跳 / 无缓存令牌跳不崩 / 已绑不再 idconvert）。
**指标**：守卫 109→**110**·测试 763→**768**(+4 login + 元测)·云函数/集合不变（36/28）·`npm run check` 全绿·`build:cloud(36)`·**dry-run 仅 login 变**（kit 改经 tree-shaking 不波及他函数）。**剩**：部署 login + 真机验（测试顾客登录小程序→查 kfIdentity 有 ext→openid 映射→客服会话查订单）。生产仓零改动。

## 2026-07-01 · 📨 P0 ④ 客服主动发消息验证工具 cs/kfSend（send_msg 接待窗口真机验·承面C sendAgentMessage 雏形）
**来源**：P0 推进中——① 绑开放平台用户已提交等认证（查订单待认证通过 + 真机验 unionid·根因#8 不盲写）；② 客户联系后台核出小程序已绑 + 1 自建应用可调接口（大半就位）；转做**不卡认证、可今天验**的 ④「send_msg 人工接待窗口真机验」——承面C 收发命门（施工蓝图「没验不开建」）。机器人即时自动回复的 send_msg 已证能发，缺的是「非即时·坐席主动发」这一真机证据 + 一个能主动触发 send_msg 的入口（现无）。
**做（cloud·新最小函数）**：新 `cs/kfSend`——给定 external_userid + open_kfid + text，48h 窗口内经 `kit/wecom.sendMsg` 主动发文字。**服务端专用闸 `isServerCall`**（越权发送面·带 openid 客户端调用拒·根因#3）；errcode 原样带回便于真机联调看结果。**定位**＝承面C 坐席回复 `sendAgentMessage` 的验证版雏形（承面C 落地升级为坐席 RBAC agent:handle + 会话归属校验）——非开建承面C，是其买/建决策的前置命门验证。
**守卫（反向自检·108→109）**：新 `kf-send-server-gated`（roots #3）焊 ① isServerCall fail-closed 拒客户端 ② 真调 sendMsg。反向自检：篡改闸 `!isServerCall()`→`false`→红→还原绿。新错误码 `SERVER_ONLY`（登记 errors 册·known-error-codes 过）。
**登记**：cloudbaserc（36）+ 系统事实接口册 `cs.kf-send` 行 + 云函数计数 35→36（function-count-synced）+ guard-deploy SENSITIVE_FNS 加 kfSend（主动发消息敏感）。
**测试**：`kfSend.test.js` 4 例（服务端调用发出·客户端 SERVER_ONLY 拒·缺参 BAD_ARGS·未配置 KF_NOT_CONFIGURED）·桩 fetch 同 kfBind 法。
**指标**：守卫 108→**109**·测试 758→**763**（+4 kfSend + 守卫元测）·云函数 35→**36**·集合不变·`npm run check` 全绿·`build:cloud(36)` DONE。**剩靠人**：① 用户在 kfSend 函数设 `WXKF_CORPID`/`WXKF_SECRET` env（copy kfCallback·config手册 §3）② 非企业成员测试微信 ③ 真机联调（顾客发消息开窗→抓 external_userid→invoke kfSend 延时主动发→顾客确认收到=命门通过·根因#8）。生产仓零改动。

## 2026-07-01 · 🔏 B5.4 审计扩真实操作者身份（agent_id·§1.5 越权剩口·承面C 前置·不卡 P0）
**来源**：「继续客服系统」→ 核出 M①–M⑤ 全部署、M③/M⑥ 卡 P0（靠人运营前置）；不卡 P0 的真实缺口＝施工蓝图 §1.5 越权剩口「审计 agent_id 未做·多账号已上·审计现仍 `operator:'admin'`」。亲核（根因#8 看码非声称）：`kit/audit.ts` recordAudit 硬编码 `operator:'admin'`、注释自陈「多用户后扩为真实身份、痕迹结构已就位」——M⑤ 多账号 RBAC 既上线，正是补它之时。治 **根因#3 信任边界可追溯**。
**做（cloud·三处贯通·向后兼容）**：① `lib.ts` checkKey 三返回点加 `operator`（超管 doc=`name||'admin'`·外包账号=`name||_id`·bootstrap=`'admin'`）——身份随 caps 一起解析返回；② `index.ts` 分发处从 `auth` 取 `operator` 贯到审计两调用点（try/catch 各一）；③ `audit.ts` recordAudit 接 `operator?` 入参、以 `String(entry.operator||'admin').slice(0,60)` 落库（缺省回退 admin 保 fail-soft/向后兼容）。谁查/改了谁留真痕、不再全糊 admin。
**守卫（先红后绿·反向自检·107→108）**：新 `audit-operator-threaded`（roots #3）焊三链——① audit.ts operator 取自 entry.operator（非硬编码）② index.ts 读 auth.operator + recordAudit 调用传 operator ③ lib.ts checkKey 返回 operator。先红：4 子断言全红；后绿：转绿。反向自检：post-green 篡改 audit.ts arm①（退回 `operator:'admin'`）→红→还原→绿。**踩坑**：lib.ts 注释初写「recordAudit」字样触发 `admin-actions-audited` 单源守卫（禁 audit.ts/index.ts 外提该词）→改措辞「操作审计」避开（守卫正确工作·非误报）。
**测试**：audit.test.js 加「operator 贯入→记真实身份(agent-lin)」；原「未传→回退 admin」断言补注释保留。
**指标**：守卫 107→**108**（+audit-operator-threaded）·测试 756→**758**（+operator 贯入 + 守卫元测）·集合/云函数不变（35）·`npm run check` 全绿·`build:cloud(35)` DONE·计数同步 `自动化验证系统`(108)。**✅ 已部署 adminApi 生产**（`3bf03da` 后·preflight 全过·dry-run 挑 1 函数·smoke ping 200·operator 落地生效·三处均在 adminApi 打包单元内）。真机端到端（真账号登录→动状态→查 auditLog 见真 operator）属根因#8 靠人。生产仓零改动。

## → 已退役：2026-06-28~30 批次
2026-06-28~30 的客服自建建设线（选型→拆批→B0/B1.1→三波并行→M⑤ RBAC）+ 品牌字体真机修 + dev:h5 等**已关账**批次已卷档 → `archive/重构日志-202606客服自建.md`（索引 `archive/README.md`）。更早：`archive/重构日志-202606上半.md` / `-202606下.md` / `-第一次重构.md`。
