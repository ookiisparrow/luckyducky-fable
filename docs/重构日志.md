# 重构日志（样板房专属）

> 本文件记重构线批次结论与证据（Phase A / B0–B9 = 第一次重构；下新增「## 第二次重构（演进）」段续记）；生产线工作日志已归 `archive/工作日志-生产线-至20260612.md`（冻结于 0612）。三支柱：`根因账本.md`（病根）+ `现状与路线.md`（live 地图；第一次重构总计划 v5 见 `archive/`）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`archive/项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-07-02 · 🎭 产销统计页加本地演示数据（`69bdfc5`·非新开关）
**来源**：用户问「演示数据+开关会不会有问题」→本仓对此有前科（根因#6 演示回退双路径泄漏）→复用 admin 包既有 `cloudMode` 本地模式范式（构建期 `VITE_ADMIN_API` 定死、非运行时可翻开关）落地，非新机制；`DEMO_ROWS` 收在 `ScmSummary.vue` 单文件不碰 `listInventory()` 等共用函数；模板把「需云端」提示从阻断表格的分支独立出来，改成与演示表格并存的「非真实统计」提示条，防误认真数据。**诚实边界**：生产构建验证 `cloudMode` 恒真、演示分支逻辑不可达，但字符串仍以死代码形式存在于产物里（同既有 local 模式函数现状一致，非 uni-app `#ifdef` 那种物理裁切）。测试 988 不变（纯 UI 分支改动）·`npm run check` 全绿·headless 浏览器验过提示条+3 行演示表同时渲染、零 console 报错·**无需重新部署**（只影响本地 dev、生产行为零变化）。生产仓零改动。

## 2026-07-02 · ✅ 供应链联动开单+产销统计 部署记账（CC 直部署 [[cc-deploy-authorized]]）
**部署**：`deploy-fns` dry-run 确认仅 `adminApi` 1 个函数变更（`kit/scmStock` 改动未波及其余 37 函数·tree-shaking 精确）→ 真部署成功·manifest 已更新；preflight 钱链漂移清零；smoke：ping 200 ok + `getFgSummary` 坏口令 401 `BAD_KEY` fail-closed。`build:admin` fresh（产物与部署前验证构建同哈希 `index-DHI3dFvf.js`，证构建可复现非偶然）+ `tcb hosting deploy packages/admin/dist /admin`（5 文件）→ curl 核验根(官网)/`/q/`/`/admin/` 三路径 200 且根仍官网未被覆盖、`/admin/` 引用新包；**线上 JS 包内容里直接核出 `scm-summary`/`产销统计` 字样字面量**，证新代码真在生产环境（非本地假绿·根因#8）。**联动开单+产销统计整线 LIVE**。剩：真机登录走一遍「算缺口→去开单→预填」+ 打包/发货几笔后核对统计数字（靠人）。

## 2026-07-02 · 🔗 供应链 admin 联动开单 + 产销统计两页（用户「统计+流程割裂」反馈两连修）
**来源**：用户核实进销存全链路后确认扣库存自动化已全通（采购入库/打包/下单/发货各自同步扣账，不是「先统计再扣」），唯独缺「打包/发货累计」统计层；追问「流程怎么穿插」→ 核出备货计算器算完缺口后要手抄数字去开单的断点。两件事一批修，都不碰扣库存逻辑本身。
**做**：① **联动开单**：新模块单例 `store/scmHandoff.js`（consume 即清·非 Pinia——一次性跳转传值不需要响应式，用不着的机器不装）——ScmPlanner 算完缺口，采购/外协缺口各加「去开单」按钮带供应商+物料+数量跳转，ScmPurchase/ScmOutwork 收到即预填草稿（单价/织女缺口算不出来、留空待人工补）。② **产销统计新页**：`kit/scmStock.ts` 新增门1 出口 `sumLedgerByItemKey(docType)`（stockLedger 按 itemKey 分组求和）+ `scmPlanner.ts` 新只读 action `getFgSummary`（assembly_in=打包累计、ship=发货累计·delta 负转正）+ 新页 `ScmSummary.vue`（打包/发货/现存三列对照·join listInventory）+ SCM_FLOW 单源追加第 7 站「产销统计」（Sidebar/Tabs 自动带出、不用分别改两处）。
**桩修（根因#8 验证样本失真）**：测试内存桩 `Aggregate.group()` 此前把 `_id:'$field'` 当单桶处理——GMV/评分聚合都只用 `_id:null` 从未暴露；`getFgSummary` 是本仓第一个真分组消费者，逼出桩改真按字段值分桶（`_id:null` 单桶行为不变·既有聚合测试零回归）。
**守卫**：无新增 id；`getFgSummary` 初版直碰 `stockLedger` 一度触发既有 `material-stock-single-seam` 红——改经门1 `sumLedgerByItemKey` 收口即绿，守卫抓得准；`interface-catalog-sync`/`admin-nav-route-synced` 覆盖新 action/新页无需额外改。
**测试**：+7（getFgSummary 3 例含「同产品两次打包求和不塌成一桶／不同产品不串账」断言 + scmHandoff 4 例）。
**指标**：`npm run check` 全绿（结构 122 条不变）·测试 981→988·集合/云函数不变（纯只读聚合+前端联动，无新写入面）。**剩**：部署 adminApi（用户终端·`getFgSummary` 走 admin:write 仅超管）+ admin 重新构建部署 + 真机走一遍联动开单/统计数字核对。

## 2026-07-02 · 🧭 供应链 admin 按工作流程重排 + 顶部流程标签（用户「使用流程散乱」拍板）
**来源**：C+D 部署上线后用户反馈供应链 6 页顺序散乱（原：物料→采购→外协→配方→打包→备货，设置项与操作项混排）、要求按实际工作流程排序并加顶部标签快速切换。
**做**：① 顺序改为「建档 → 算缺口 → 补料 → 产出」——物料与供应商 → 配方模板 → 备货计算 → 采购管理 → 外协加工 → 打包组装（此前备货计算排最后，但逻辑上它是每轮备货的决策起点，采购/外协是照着它的缺口去补，顺序对不上「先算后补」的实际动线）；② 新增单源 `utils/scmFlow.js`（`SCM_FLOW` 顺序表：路径+文案+图标），Sidebar「供应链」nav 组与新组件 `components/ScmFlowTabs.vue`（编号 1-6 的顶部标签条·当前页高亮·可直接互跳）共用同一份数据、不各写一份；③ 6 个 SCM 页面顶部各插一条 `<ScmFlowTabs />`。
**守卫（扩面·非新增）**：`admin-nav-route-synced` 原本纯正则扫 `Sidebar.vue` 文本找 `to:` 字面量，Sidebar 把 SCM 条目改成 `items: SCM_FLOW`（导入引用）后该守卫扫不到路径、假报 6 处「孤儿页」——**根因是 Sidebar 把导航数据外包给了单源模块，守卫的静态扫描没跟着看**。扩守卫：跟随 Sidebar.vue 的本地 `@/` import 一层，把被引文件内容并入扫描面（同上次「加页忘加导航」根因#5 的样板复制思路——现在复制变成了引用，守卫要能看穿引用）。反向自检：在 router.js 加一条假路由 `/scm-fake-orphan`（不进 Sidebar/SCM_FLOW）→ 守卫准确抓出孤儿页 → 移除后绿。
**验证**：`npm run check` 全绿（结构 122 条不变·仅扩了 `admin-nav-route-synced` 内部实现，未增守卫计数）· `build:admin` 过 · **真机验（根因#8「构建过≠真机能用」，非声称）**：起 admin dev server + Playwright headless 驱动——登录→逐页核对顶部标签顺序/当前页高亮索引与侧边栏顺序完全一致（截图见证）→ 点击标签栏「打包组装」从「物料与供应商」页真实跳转到 `/scm-assembly`（非仅 DOM class 变化）→ console 无报错。
**指标**：改动 9 文件 + 新增 2 文件（`utils/scmFlow.js` 数据单源 + `components/ScmFlowTabs.vue` 组件）；测试计数不变（981，纯前端导航重排无新业务逻辑）。生产仓零改动。

## 2026-06-15 · 🏷️ v0.9.2 里程碑（代码里程碑·新会话先读这段）

**v0.9.2 = 代码里程碑**（tag 在 main `d182075`），收束 v0.9.1 tag 以来三条线；**不是新部署**（部署状态见末段）。
- **① v0.9.1 验收收尾**：验收单 X/Y/Z 真机真钱过——X 库权限客户端实测抓真 P0（13 集合曾可写 + `courses` 公开读漏 `videoFileId`）→ 16 集合锁仅管理端；本仓接管 tcb 部署闸（deny-all → 生产仓模型，敏感函数 `ask`）。
- **② 产品打磨**（前端 miniapp·用户可见）：微信登录半屏弹窗 + 启动加载页 + 登录流程完善（退出/一键登录/默认昵称 `Lucky friend`+随机鸭头像）；首页/购物车 UI 批（品牌 logo/卡片等高/购买滚动居中/顶栏吸顶/结算条对齐）；「我」页未激活课程空态；**返回重构**（Tab 页「再按一次退出」= scroll-view + page-container）；新 skill `/frontend-check`。
- **③ 治理脊柱**（第二次重构·演进；纯文档/守卫/工具，**不碰运行时**）：P0 知识地基（立 `演进地图`）→ 文档精简+架构校准（活文档瘦 ~40%）→ P1 需求显形（`需求清单` 31 条单源 + 逐条复核，修 R22 跑偏）→ 文档架构重设（19→13 份，客观/主观分治 + 反膨胀守卫 `docs-budget`）→ **P2 需求→守卫闭环**（`requirement-trace` 守卫 + `npm run trace` 爆炸半径）→ **P2b 须守卫覆盖**（钱/状态/安全类需求须有对应类守卫）。

**当时态（v0.9.2·2026-06-15 快照·勿读成当前数；当前计数见 `现状与路线`§3 + `自动化验证系统`）**：测试 **278** 全过；守卫 **15 repoCheck + 5 fileRule**；活文档 **13**；skills **7**；CLAUDE 144/180。生产仓 `luckyducky-miniprogram` 零改动（HEAD `2dd5a72`，回滚基线）。
**部署状态（honest，勿读成"已全上线"）**：后端云函数仍是 v0.9.1 部署的 **28 个**（治理 work 不碰运行时、无需重部署，后端已是生产随时服务）；前端登录/UI/返回 等产品改动**已合 main、未发布生产**——发布属「全客户上线」动作（`build:mp-weixin` + `urlCheck`→true + 微信后台合法域名 + 审核），仍待。
**容量评估（2026-06-15 看码评估·非压测）**：4 位数/天 架构可承——并发正确性稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）。上量前必清 4 件（详 `待办与债` §上量就绪）：① 数据库套餐档位（控制台查·最大未知）② 视频 CDN/VOD（R21 搁置·裸云存储吐视频带宽=头号成本/风险）③ 看板内存聚合 1000 条/表封顶（4 位数当天 GMV 即少算）④ 索引 + **从未压测**（根因#8 同理：单人能用 ≠ 千人同时）。

## 2026-07-02 · 🩺 深审客服批：6 逻辑修（推送键错位 + CSAT 关单闭环 + bot 不抢话 ×2 + 假转接 + 上下文窗·先测试红后绿）
**来源**：用户「审核客服系统看逻辑错误」→ /deep-audit 逐行审 cs 全链逮 2 P1 + 4 P2 → 用户拍板「都修」。先写 7 失败测试（红）再实现（TDD）。
**修**：**F1(P1)** 推送键错位——超管坐席身份 'admin' 但账号 doc 'auth'，notifyOnlineAgents 查不到其 wecomUserId＝只有超管值班时推送恒空 → docId 映射 admin→auth；**F2(P1)** CSAT 关单评分链断——closeConversation 提示「回复 1-5 分」但 dispatch 无自由文本数字处理（注释声称≠实现）→ 关单发提示成功即立 `csatask:<euid>` 标记(48h)，dispatch 收纯数字 1-5 且有标记才 recordCsat+消费+回原因菜单（副作用绑提示·防随口数字误记）；**F3(P2)** enter_session 直发欢迎菜单不查 held＝排队中顾客重开聊天窗被 bot 抢话且点菜单又被静默 → 抽单源 `heldStatus()`（handleMessage 同口径）enter_session 过它；**F4(P2)** enqueueSession 基建错被当撞号吞＝假「已转接」无人接 → 三态返回 queued/already/failed（同 firstSeen 款·回查 doc 确认）+ transfer 分支 failed 时如实话术「稍后再点一次」+ alert ENQUEUE_FAILED；**F5(P2)** getThread since=createdAt-1＝坐席看不到顾客点转人工**之前**的问题描述 → 首拉上下文窗放宽 10 分钟（cursor 轮询不变·超窗历史仍不回溯·AD 语义保留）；**F6(P2)** 关单 CSAT 提示不落归档（kfSend 无归档）＝质检见顾客答不见我们问 → closeConversation 发送成功后 inline 归档 conversations（direction out·agentId 可溯）。
**守卫（扩 2 既有·不新增 id·反向自检×3）**：`csat-score-bounded` 扩「关单评分闭环」双端断言（dispatch 须消费 csatask + agentDesk 发 CSAT_PROMPT 须立标记·反向去消费分支→红）；`agent-channel-stays-assistant` 扩臂③（index.ts enter_session 须过 heldStatus·反向去调用→红）；F1 由 agentPush 行为测试锁（反向去映射→红）。
**测试**：+7（超管收推送/关单标记+归档/数字评分记账+无标记不误记+过期不记/heldStatus 三态/入队三态+假转接话术/上下文窗）+ 1 旧测试按 F5 新语义更新（超窗历史仍排除·意图保留）。`npm run check` 全绿·`build:cloud(38)`。
**审核全景（好的说好）**：转人工不动平台态/状态机声明对账/防吞单三态+时间预算/外包双闸/免登五路 fail-closed/fail-soft 纪律——逐行验过全稳；by-design 核过不算错：外包可 claim escalated（spec 声明）、closed 重开（AD）。P3 留观：免登令牌保温依赖探针 env（靠人）、listQueue 同毫秒游标边缘。**✅ 已部署生产**（CC 直部署·dry-run diff=2 恰 kfCallback+adminApi·smoke ping 200/坏 key fail-closed·钱链无漂移）——注：adminApi 包同车 main 上并行 ERP 线已合码（check 绿·默认拒闸）。

## 2026-07-02 · 📐 SCM-D 计划核销线：备货计算器 + 发货核销流水（车道 D·`feat/scm-d` 接续 C·守卫先红后绿）
**来源**：蓝图 §4 第二波车道 D（用户「开始c、d工作」后半）。
**做**：① 新 `actions/scmPlanner.ts` 1 action `getRestockPlan`（**只读**·targets ≤50 正整数校验）：Σ各产品 resolveBom（门3 与组装同一解析·配方语义单源）聚合需求 → 对比主档库存（经 kit/scmStock 只读出口）→ **外协缺口**（带结不外购·业务定稿带结=织女产出：缺口=应发同色 L 原团数·派生原团需求叠回采购口径——发出去的原团也是要备的料）+ **采购缺口按供应商分列**（够扣不出行·未建档料点名+归「未挂供应商」组）；fail-closed：NO_TEMPLATE / NO_PROFILE 带 productId / BAD_TARGETS；② `orders.ts` shipOne 加核销留痕：**首次 paid→shipped**（cur==='paid' 且条件更新成功）逐订单行落 ship 流水（`fg:<pid>__<spec>` 只留痕不动账——成品扣账在下单预留；确定性 `_id=ship:<orderId>:fg:…` 并发/重试天然幂等）；改单号 shipped→shipped 不重记；旧单无 items 静默跳过不落假流水（根因#8）；fail-soft 留痕失败不反噬发货；③ admin `ScmPlanner.vue` 备货计算页（目标行编辑→两张缺口清单·外协「应发原团」/采购按供应商分组·未建档警示）+ api/cloud.js getRestockPlan 封装 + router `/scm-planner` + Sidebar「备货计算」。**激活码对账（蓝图可选项）诚实后置**：卡片账↔qrcodes unused 对账值一个只读页，但当前无印刷单流程消费它——待真用到随 SCM-Z 补（§7 别为不存在的需求建机制）。
**守卫（+1 test 型·反向自检咬红还原绿）**：新 `ship-verify-ledger`（roots #2·「如实核销」蓝图定稿）——锁「首次发货必落 ship 流水（确定性 id·delta=−qty）+ 改单号/批量不双记账 + fg 行不碰 materials/inventory」。反向：注释掉 shipOne 留痕块 → 3 测试红 → 还原绿。planner 无新守卫（只读·无不变量可焊·resolveBom 行为已有 scmBom.test 锁）。
**测试**：新 `tests/cloud/scmShipLedger.test.js` 4 例（首次发货逐行流水/改单号不双记/批量 shipOrders 同源留痕/旧单无 items 不落假流水不崩）+ 新 `tests/cloud/scmPlanner.test.js` 5 例（外协缺口+派生原团进采购·供应商分列+够扣不出行+未建档点名·全够三空·fail-closed 三种·RBAC 403）·962→**971**。既有 shipOrder.test.js（wx 上报 fail-soft）零回归。
**验证**：`npm run check` 全绿 · `build:cloud(38)`+`build:admin` 过 · 登记：系统事实 getRestockPlan 行 + shipOrder 行补 stockLedger 写面 + 计数 93→94（ACTIONS 90→91）。不新增函数/集合。**车道 C+D 全落**：`feat/scm-c`（tag `scm/c/ready-1`）→ `feat/scm-d`（tag `scm/d/ready-1`·含 C）；主树 M⑦ CC 活跃期间全程 worktree 隔离施工、主树零污染。**✅ 已整合上 main**（同日·主树核清（干净 + main 未动）后 ff `d07efb3`＝worktree 已全绿同一提交、组合态免重验·tag `scm/c/merged-1`+`scm/d/merged-1`·生产仓零改动 HEAD `2dd5a72`）。**✅ 已部署 LIVE**（同日·用户终端两步·CC 核验：adminApi manifest `86d850b812ef` dry-run 归零 / `/admin` 新包 `index-DvmXhYRu.js` / 四路径 200 根仍官网 / smoke ping ok+错误口令 BAD_KEY fail-closed；用户在 worktree 跑的 deploy·manifest 记账已同步回主树）。**剩**：真单真机验 + 期初盘点录入随 SCM-Z。

## 2026-07-02 · 🧵 SCM-C 配方组装线：全局模板×差异位 → 打包扣料入成品（车道 C·`feat/scm-c`·守卫先红后绿）
**来源**：蓝图 §4 第二波车道 C（用户「开始c、d工作」·单 CC 顺做 C→D）。主树 M⑦ CC 仍活跃（17:56 尚在提交）→ 按并行工作流纪律复用 `luckyducky-next-scm` worktree 隔离施工、主树恢复零污染。
**做**：① **门4 落地**（SCM-0 纪律修正兑现）：`kit/inventory.ts` 新 `produceStock(productId, spec, qty)`——本文件唯一新增出口（守卫 stock-atomic-conditional 收口下）·CAS 增·无文档则建（新 SKU 首产可入）·**stock=null 不限量保持不动**（不隐式翻限量）·qty 正整数 fail-closed；② 新 `actions/scmBom.ts` 3 action——getBomSetup / saveBomTemplate（全局一张模板存 config 确定性 `_id='scmBomTemplate'`·白名单清洗镜像门3 校验口径：qtyPerSet 正整数·tier/form 枚举·**带结槽仅 L 档**）/ saveBomProfile（bomProfiles `_id=productId`·三色 slug+专属包装/卡片料号全必填）；③ 新 `actions/scmAssembly.ts` 3 action——**runAssembly（单步执行无状态机·蓝图定稿）：门3 resolveBom 解析 → bomSnapshot/consumedLines 快照冻结存单 → 门1 applyStockMoves assembly_out 扣原料（all-or-nothing·不足撤 claim 整单回滚）→ 门4 produceStock 入成品 → assembly_in fg 流水留痕；`_id=assemblyId` 确定性 claim（前端一次生成重试复用）重放 409 DUPLICATE 不双扣** / previewAssembly（只读预演·用料×库存对照）/ listAssemblies（bounded）；④ admin `ScmBom.vue`（模板行编辑+每产品差异位·专属件按命名契约预选 pkg:/card:<productId>）+ `ScmAssembly.vue`（预演缺口标红→确认执行→组装单历史含快照展开）+ api/cloud.js 6 封装（BOM_ERR 独立块）+ router×2 + Sidebar「配方模板」「打包组装」。
**守卫（+1 test 型·反向自检×2 各咬红还原绿）**：新 `bom-snapshot-frozen`（roots #2·同订单快照原则）——锁「改模板后历史组装单 bomSnapshot/consumedLines 不追新」+「同 assemblyId 重放 409 不双扣」+「料不足全单回滚原料/成品/单据全不动」。反向①：claim `_id` 改随机 → 4 测试红（幂等/快照/累加全崩）；反向②：快照存 null → 2 测试红。其余白捡（transitions 对账扫 scm*·单点收口·审计·RBAC 默认拒·interface-catalog-sync·admin-nav-route-synced）。
**测试**：新 `tests/cloud/scmAssembly.test.js` 12 例（模板/差异位白名单与契约·执行全链账目断言[原料 6 行扣准/inventory 建档/流水 6+1/快照存单]·冻结·幂等·回滚·累加+不限量不动·NO_TEMPLATE/NO_PROFILE/BAD_SETS fail-closed·预演只读·RBAC 外包 6 action 403）·950→**962**。
**验证**：`npm run check` 全绿 · `build:cloud(38)`+`build:admin` 过 · 登记：系统事实 6 action 行 + 计数 87→93（ACTIONS 84→90）。不新增函数/集合（模板复用 config·集合 SCM-0 预登记）。

## 2026-07-02 · 🛒 SCM-A 采购线：采购单状态机 + 到货入库（车道 A·并行 worktree·零新守卫）
**来源**：蓝图 §4 第一波并行车道 A（SCM-0 地基就绪后开工·worktree 隔离施工·只动本车道文件）。
**做**：① 新 `actions/scmPurchase.ts` 5 action——listPurchases（bounded ≤200·状态过滤·createdAt 倒序）/ savePurchase（建/改草稿·仅 draft 可改[where 条件原子锁草稿态]·供应商须 factory·qty 正整数/unitPriceFen 非负整数分/料号读主档校验[经 listMaterialDocs 不碰 materials]/**同料重复行拒 DUP_LINE**[两行同料撞入库幂等键会漏记后行]·totalFen 服务端 Σqty×价 不信前端）/ markOrdered（transition draft→ordered）/ **receivePurchase（transition ordered→received·moved===true 才调门1 applyStockMoves purchase_in 入库·重放 moved=false 直接返已入库不双记账·目标态重放返 200 幂等）** / cancelPurchase（draft/ordered→cancelled·received 后声明表无边天然拒）；② index.ts 登记 5 action（不登 ACTION_CAPS=默认拒仅超管·写类自动审计）；③ admin `ScmPurchase.vue`（列表状态 chip+建/改草稿[厂家下拉·行编辑]+行内 下单/入库/取消·金额展示元/传输存储分）+ api/cloud.js 5 封装（PURCHASE_ERR 独立块不改共享行）+ router `/scm-purchase` + Sidebar「供应链」组「采购管理」。
**守卫**：**零新守卫（按蓝图）**——`order-transitions-declared` 按 scm* 前缀自动纳管本文件（transition 边全在 scm.spec 声明表内）+ material-stock-single-seam/scm-ledger-idempotent/known-collections-only/admin-actions-audited/agent-rbac-gated/interface-catalog-sync/admin-nav-route-synced 白捡覆盖。
**测试**：新 `tests/cloud/scmPurchase.test.js` 12 例（校验拒 qty 浮点/零/负·负价/浮点价·空行·幽灵料号·重复行·供应商不存在/织女；totalFen 服务端算无视伪造；draft→ordered→received 正流转+确定性流水；received 重放幂等不双入库；draft 直收 409；received 后 cancel 409；draft/ordered 可取消；列表过滤；RBAC 外包 5 action 全 403/超管过）。
**验证**：`npm run check` 全绿 + `build:cloud`+`build:admin` 过。登记：系统事实 5 action 行 + admin action 计数**按真实数字校正** 62→81（旧行 stale：真值 HEAD 已是 ACTIONS 73[62 漏计 承面C agentDesk 10+agents 4]·+5 后 ACTIONS 78+pre-auth 3=81）。
## 2026-07-02 · 🧶 SCM-B 外协线（第一波并行·车道 B·独立 worktree·零新守卫）
**来源**：蓝图 §4 车道 B（README 需求定稿：织女把起手结做在**最大团**毛线上——发原团收带结·损耗可见·计件工钱）；SCM-0 五门就位后第一波并行开工。
**做**：新 `actions/scmOutwork.ts` 6 action——listOutworks（bounded ≤200·status/workerId 过滤倒序）/ saveOutwork（仅 draft 可改·workerId 须织女档·发料行仅 `yarn:<color>:L:raw` 且主档在册·pieceRateFen 非负整数分）/ issueOutwork（transition draft→issued 首次流转绑负 delta 出库；**库存不足补偿回滚**：条件更新 issued→draft 复原——技术性补偿非业务逆向流转，scm.spec 故意不声明该边，注释写明）/ receiveOutwork（同色带结 `yarn:<color>:L:knotted`·颜色⊆发料·每色 qty≤发料防收比发多·带结主档缺→NO_MATERIAL fail-closed 提示先建档不静默建；transition issued→delivered **同一次条件更新定格 payableFen=Σ收×单价 与 lossQty=Σ发−Σ收**，再入库，失败对称补偿）/ settleOutwork（delivered→settled 销账）/ cancelOutwork（仅 draft·已发料走调整单·蓝图定稿）。登记面：index.ts ACTIONS +6（不登记 ACTION_CAPS=默认拒仅超管·写类自动审计白捡）；admin `ScmOutwork.vue`（状态 chip/应付损耗列 + 建草稿表单[织女下拉·发料行·单价元→分边界一次] + 收货抽屉按色填收回数）+ api/cloud.js 6 封装 + 路由/侧边栏「外协加工」。
**守卫**：**零新守卫**（蓝图「白捡」兑现）——`order-transitions-declared` 按 scm\*.ts 前缀自动纳管本文件（transition 边/status 写入全走 scm.spec 声明·集合名故意写字面量保对账归属）、`material-stock-single-seam`（出入库只经门1）、`agent-rbac-gated`/`admin-actions-audited`/`interface-catalog-sync`/`admin-nav-route-synced` 全自动覆盖。
**验证**：新 `tests/cloud/scmOutwork.test.js` ×14（草稿校验拒/发料扣账+流水确定性 id+重放幂等/**不足回滚 draft 且足量行不动账**/收货定格应付损耗与流转同 doc+重放不双入库/收比发多拒/非发料色拒/带结未建档拒且不静默建/结算/已发料 cancel 拒/RBAC 外包 403）·`npm run check` 全绿·build:cloud+build:admin 过。系统事实 admin action 62→68。

## 2026-07-02 · 🏗️ SCM-0 进销存地基（独立 worktree `feat/scm-0`·避让主树 M⑦ CC·守卫先红后绿）
**来源**：蓝图落仓后用户「客服系统那边的 CC 还在工作，你直接新开一个 worktree，完全避开那边 CC」→ 按 `并行工作流.md` 纪律在 `/Users/sparrow/luckyducky-next-scm`（分支 `feat/scm-0`·起点 origin/main `f5c8f29`）独立施工；**不碰主树、不推 main、不部署**（部署会把主树 CC 刚上生产的 adminApi 滚回旧版），完毕打 tag `scm/0/ready-1` 等整合。
**做（蓝图 SCM-0 全项）**：① 7 集合登记 `collections.ts`+库权限期望表（37 集合·initDb 自动建）；② **门1** `kit/scmStock.ts`＝materials.stock/stockLedger 全库唯一读写（applyStockMoves：确定性流水 `_id=docType:docId:itemKey` claim 幂等 + 乐观 CAS 余额不为负 + 任一行失败全单回滚；主档 saveMaterialDoc uom 建档锁死/不碰 stock）；③ **门2** `shared/scm.spec.ts` 声明采购/外协两状态机 → gen-order-domain 扩域出 `scm.ts` 派生物 → `order-transitions-declared` 扩扫 `actions/scm*.ts`（按前缀·车道 A/B 新文件自动纳管）；④ **门3** `shared/scmBom.ts` resolveBom 纯函数（模板×差异位×套数·同料号合并·带结仅 L fail-closed）；⑤ **门5** `actions/scmMaterials.ts` 6 action（listMaterials/saveMaterial[料号推导]/listSuppliers/saveSupplier/adjustStock[必留因·adjustId 幂等]/listLedger·不登记 ACTION_CAPS=默认拒仅超管·写类自动审计）；⑥ admin「供应链」nav 组 + `ScmMaterials.vue`（建档/期初盘点/调整/流水查账/织女档案）+ api/cloud.js 封装。**纪律修正**：门4 produceStock 随车道 C 长出（唯一消费者且无并行冲突面·提前落=dead code·§7·同 M⑦ 地基先例·蓝图§3 已注记）。
**守卫（120→122 repoCheck + 1 test 型·反向自检×4 各咬红还原绿）**：新 `material-stock-single-seam`（roots #1/#2·镜像 stock-atomic-conditional·外部直碰 materials/stockLedger 注入→红）+ 新 `scm-ledger-idempotent`（roots #2·流水 id 改随机→红）+ 新 `scm-uom-integer`（test 型·roots #8·关 uom 锁→2 测试红）+ `order-transitions-declared` 扩面（scm action 写未声明边 draft→bogus→红=扩描面真咬·非守空气）。计数守卫连环逮出 6 处登记欠账（系统事实 action 册/计数·库权限表·console README·自动化验证系统）全补齐；连带逮出 `collection-count-synced` 窄解析器被集合注释里的花括号截断——改注释措辞并留告示。
**验证**：`npm run check` 全绿（结构 122·测试 885→**911**·+24 新例 3 文件：scmStock 幂等/回滚/fail-closed·scmMaterials 料号契约/uom 锁/调整幂等/RBAC 默认拒 403·scmBom 展开/合并/fail-closed）·`build:cloud`+`build:admin` 双过。生产仓零改动、主树零接触。
**✅ 整合+部署闭环（2026-07-02 续·用户「1」拍板整合）**：rebase 到 M⑦ 车道B（`4c0360d`）之上取并集（adminApi import 区 / 系统事实 admin action **62**=ACTIONS 59+pre-auth 3 · DB collection **37**）→ 并集树 check 全绿（守卫 122·测试 **919**）→ fetch 确认无竞争快进 main `ba0509a` + tag `scm/0/merged-1`。**部署**：CC 侧被权限层拦（生产部署 classifier deny·诚实记）→ 用户终端三步全过——deploy-fns **14 函数**（adminApi + kit 波及钱链/客服·preflight 钱链漂移清零）+ initDb 建齐 7 新集合 + 7 集合 **adminonly 逐一核过**（tcb permission get）+ smoke（adminApi 405/POST_ONLY·getProducts 真数据）。**SCM-0 整线闭环**；剩期初盘点录入（靠人）。顺带发现：生产库 products 有商品名「幸运小鸭礼盒」＝品牌禁用变体在**数据层**残留（brand-name-single-source 只扫仓不扫库·占位商品 R24 范畴·待用户在控制台/admin 改名或随 R24 清理）。

## 2026-07-02 · 📦 进销存 ERP 蓝图细化（docs 批·开新线）
**来源**：用户从需求出发 4 轮逐条拍板（毛线 颜色×三档克重×形态按团数·带结仅最大团 / 外协管到发料+计件工钱 / 全局一张配方模板+每产品三填空·数量重量全产品一致 / 下单扣账发货核对留痕）→「先蓝图细化指导执行·优先设计多 CC 并行·非耦合模块化/接口收束」。
**做**：① 新活文档家 `docs/进销存ERP/`（README=需求定稿+进度单源 / 施工蓝图=领域模型 7 集合 + P3 状态机声明[scm.spec 两表·learning 先例扩面] + **接口五门收束**[原料账单点 kit/scmStock / 状态机声明表 / 配方 resolveBom 纯函数 / 成品账 produceStock 唯一出口 / action 面文件级隔离] + 车道拆分 SCM-0 地基→A采购‖B外协→C组装‖D计划核销→Z整合 + 守卫候选 6 + 白捡 7）；② 需求登记 `需求清单` R31（L2 15→16·复核计数 31/31→32/32 同步·避 requirement-trace L1 映射面）；③ `现状与路线` 加线指针（镜像客服线登记式）。
**设计要点**：**不新增云函数**（全走 adminApi actions·RBAC 默认拒/审计/HTTP 闸白捡·部署面=adminApi 一个）；集合 SCM-0 预登记消灭最大并行冲突面；并行机制引用 CS 线 `并行工作流.md` 不重抄（单源）；范围钉死进销存最小闭环、财务凭证/多仓/批次效期明确不做（§7）。
**验证**：`npm run check` 全绿（纯 docs·守卫/测试计数不变）。生产仓零改动。

## 2026-07-02 · 📱 M⑦ 车道C 移动化：/agent 响应式单栏+抽屉 + ?session 会话直达（纯前端·M⑦ 代码全落）
**来源**：M⑦ 车道B 后接续车道 C（用户「继续」）——把 `/agent` 桌面三栏在手机窄屏可用 + 推送卡片点入直达会话。纯前端、最独立、可全程对 mock。
**做**：① `pages/Desk.vue` 响应式——桌面三栏 grid 不变；≤820px 媒体查询单栏切换（mobileView `queue⇄conv`·认领/打开自动切 conv·结束回 queue·会话头加「返回队列」）+ 客户360/快捷回复收进右滑抽屉（sideOpen·遮罩点关·选完快捷回复自动收）+ 顶栏窄屏省 brand-sub；② `?session=<id>` 会话直达——onMounted 读 `location.search` → 新纯函数 `logic/deepLink.resolveSessionDeepLink`（open 已在我名下 / claim 在待接队列 / notfound 已被接走 / none）决策 → open/claim/toast·读完清参防刷新重触发；经 OAuth 往返存活（车道B redirect_uri 含 search·stripOAuthParams 只删 code/state·?session 保留）。
**守卫**：**车道 C 无新守卫**（诚实·§7 + 工单原计划）——移动端「顺手」是根因#8 靠人（只真机验得了·机器证不出）；`resolveSessionDeepLink` 纯决策已 characterization test 锁（5 例），不为它硬上守卫注册（不防根因·摆设）。
**测试**：新 `tests/agent/deepLink.test.js` 5 例（open/claim/我的在接优先/notfound/none 空入参）·893→**898**。
**验证**：`npm run check` 全绿·守卫计数不变（120 repoCheck + test guards）·`build:agent` 过·零云函数改动（纯前端·不部署云函数）。
**✅ 前端已部署 /agent 静态托管**（`index-CqwXyJB_.js`·M⑦ 前端一次性上：车道B Login.vue 三态[env 空则回退口令·?code 处理 live] + 车道C 移动化+直达）——移动响应式即刻可用（坐席手机口令登录即享·桌面布局不变·零回归）；免登 auto-OAuth 待 VITE_WXWORK_* env 填好重部署激活、推送直达待 WXKF_AGENTID env 激活。四路径 curl 核验完好（根/q/admin/agent 均 200）。**⚠️ 多 CC 并行整合**：推送时 origin 已被并行 ERP CC 推进（SCM-0 地基）→ rebase 到 origin/main（git 干净自动合并 index.ts[wecomLogin+scmMaterials 并存]/重构日志/自动化验证系统）→ **合并态重跑 check 全绿 924 测试**（守卫计数同步·typecheck 过）才推（多 CC 纪律：rebase 后必重验组合态·别假设各自绿=合并绿）。adminApi 云函数含 ERP scmMaterials 未部署部分归 ERP CC。
**M⑦ 代码全落（地基+A+B+C）**。**剩靠人**：① env WXKF_AGENTID（kfCallback+adminApi·推送）② VITE_WXWORK_CORPID/AGENTID（agent·免登·填后重部署 /agent）③ 企微后台建外包部门/应用可见范围/应用主页 URL/OAuth 可信域名 ④ 真机验三件（推送到手机/免登/移动接待·根因#8）。

## 2026-07-02 · 🔑 M⑦ 车道B 免登线：企微 OAuth 免登（服务端 session 令牌·1 新守卫·fail-closed）
**来源**：M⑦ 车道A 后接续车道 B（用户「继续车道 B 免登」）——企业微信自建应用内打开 `/agent` 免口令登录（坐席不再记口令·外包/超管同门）。
**认证模型核对**：现有＝每请求带明文 key、checkKey 比 keyHash、无服务端 session。免登坐席无口令 → 必须服务端签发凭证。设计＝OAuth 验身份后签发 **session 令牌**（存账号 doc·checkKey 加分支认它·不新增集合）。
**做**：① kit/wecom.ts 新 `getWecomOAuthUserId`（`auth/getuserinfo`·code→userid·非成员返 openid 则拒）；② 新 `actions/wecomLogin.ts` `loginByWecomCode`（pre-auth·index.ts 特殊分发在能力闸前·同 login 受频控）：code→getCachedKfToken（不持密钥·根因#3）换 userid→查 adminConfig `wecomUserId` 匹配账号（外包/超管 auth）→ 签发高熵令牌（`randomBytes(32)`·存 `wecomSessionHash=sha`+`wecomSessionExp` 12h）；③ lib.ts checkKey 加 `resolveWecomSession`（口令全未命中 fallback·令牌 sha 撞不上 keyHash 故安全）+ 导出 capsForRole；④ 前端 agentApi `loginByWecomCode` + Login.vue 三态（企微 UA+配 CORPID/AGENTID 静默 OAuth / ?code 回跳换令牌+清 URL / 口令兜底）+ agent .env.production 加 VITE_WXWORK_CORPID/AGENTID 占位（靠人·非密钥）。
**fail-closed（守卫 wecom-login-gated·roots #3·测试锁·反向自检双向过）**：无 code BAD_ARGS / 无缓存令牌 KF_TOKEN_UNAVAILABLE / OAuth 无 userid BAD_CODE / userid 未绑 NO_BOUND_ACCOUNT / 停用 ACCOUNT_DISABLED → 一律拒不签发；checkKey 令牌过期 SESSION_EXPIRED / 停用 → 拒。反向：去过期检查→过期测试红；去 disabled 检查→停用测试红；还原绿。
**测试**：新 `tests/cloud/wecomLogin.test.js` 8 例（签发+入库 sha/令牌经 checkKey 认/超管 auth 免登/5 fail-closed 分支/过期/停用后失效/乱造令牌 BAD_KEY）·885→**893**。**登记**：系统事实 pre-auth 表加 loginByWecomCode 行 + admin action 55→56（ACTIONS 53 不变·pre-auth 三特例）。
**验证**：`npm run check` 全绿·守卫 120 repoCheck + test guard（wecom-login-gated）·`build:cloud(38)`/`build:agent` 过·不新增函数/集合（loginByWecomCode 在 adminApi 内·令牌存 adminConfig）。
**✅ 后端已部署生产**（CC 直部署 [[cc-deploy-authorized]]）：dry-run diff=1（adminApi·getWecomOAuthUserId 仅被 adminApi 引·tree-shaking 不波及他函数=比车道A干净）→ deploy 成功·preflight 钱链无漂移·smoke（HTTP POST loginByWecomCode 无 code→BAD_ARGS / 假 code→KF_TOKEN_UNAVAILABLE 不签发=fail-closed 接通）。**前端暂不部署**（Login.vue 免登需 VITE_WXWORK_CORPID/AGENTID·空配置版无用=churn·且不部署对现有口令登录零影响·待 env 填好一次性 build:agent+重部署）。**剩靠人**：① agent .env.production 填 VITE_WXWORK_CORPID（同 corpid）/VITE_WXWORK_AGENTID（1000002）→ 我重建重部署 /agent；② 企微应用主页 URL=www.luckyducky.cn/agent/ + OAuth 可信域名 luckyducky.cn；③ 真机验免登（企微内打开 /agent 静默进·不输口令）。

## 2026-07-02 · 📲 M⑦ 车道A 推送线：应用消息推新会话到坐席手机（企微自建应用·2 新守卫）
**来源**：M⑦ 地基后接续做车道 A（用户「你开」）——把「新会话待接 / 会话升级」经企业微信「应用消息」主动推到坐席手机（解「页面关着无提醒」·app 消息无 48h 窗口/无一次性订阅限制·根因：轮询只在页面开着时准实时）。
**做**：① kit/wecom.ts 新 `sendAppMessage`（原始接缝·调 message/send·touser `|` 连接·textcard）+ `sendAgentCard`（fail-soft 编排·取缓存令牌 getCachedKfToken + env WXKF_AGENTID·未配/无令牌/无收件人静默跳过）+ `AGENT_DESK_URL` 单源（env WXKF_AGENT_URL 可覆盖·默认 www.luckyducky.cn/agent/）·kit index 导出；② `enqueueSession`（dispatch.ts）改：追踪「真正新入队」（首建成功 or closed→pending 重开 moved）→ 只此时 `notifyOnlineAgents`（查 agentState status=online → 各 adminConfig doc 取 wecomUserId → sendAgentCard 推卡片带 `?session=`）·已 pending/active 不重复骚扰；③ `escalateToMerchant`（agentDesk.ts）升级成功后推商户超管（adminConfig 'auth'.wecomUserId）。全链 fail-soft。
**守卫（119→120 repoCheck + 1 test guard·反向自检各过）**：新 `app-message-single-seam`（roots #12·镜像 bot-push-single-seam·message/send 只在 kit/wecom.ts·别处出现即红·反向自检注入 agentDesk→红→还原）+ 新 `enqueue-push-fail-soft`（roots #8·typeAndTestGuards·reverseTest agentPush.test.js·**双层 fail-soft**[sendAgentCard 内 + notifyOnlineAgents 外]·反向自检去双层→测试红→还原·去单层不破不变量=纵深防御正确）。
**测试**：新 `tests/cloud/agentPush.test.js` 6 例（payload 形状/sendAgentCard 三跳过分支/新入队推在线坐席/已入队不重推/fail-soft 抛错不反噬）·878→**885**。
**验证**：`npm run check` 全绿·守卫 120 repoCheck·`build:cloud(38)` 过·M⑦ 全程不新增函数/集合（推送在 kit+kfCallback+adminApi 内扩）。
**✅ 已部署生产**（CC 直部署 [[cc-deploy-authorized]]）：kit/wecom.ts 改动＝shared-ripple → dry-run 全 38 函数 hash 变（钱链因此漂移红）→ `DEPLOY_ALLOWED=1 deploy-fns` 全量重部署 38 成功（行为不变·仅 kit barrel 新增导出）→ preflight 钱链漂移**清零** + smoke（getProducts 真数据 ok / createOrder 无 openid fail-closed）。**剩靠人**：配 env `WXKF_AGENTID`（自建应用 AgentId 1000002·配在 kfCallback+adminApi）+ 真机验推送到手机（顾客转人工→在线坐席手机收应用消息卡片）。

## 2026-07-02 · 🔌 M⑦ 承面C 增强·阶段0 地基：坐席账号 wecomUserId 字段全链（企微免登共同前置）
**来源**：用户拍板「企业微信自建应用坐席台」方案（推送+免登+移动化·工单 `docs/后台360工作站/承面推送免登工单.md`）→「开始」→ 以单 CC 做阶段0 地基（master 串行·扇出前置）。
**纪律修正**：工单 §4 原设想「loginByWecomCode/sendAppMessage 空实现登记 + 守卫空壳」与本仓「别建 dead config / 别摆设守卫」（CLAUDE §7）冲突——空壳=dead code。地基**收缩为唯一该现在落的真东西**：`wecomUserId` 字段（A 推送取它、B 免登查它的共同前置·先落避免两车道都改 agents.ts 冲突）；其余接口点用工单 I/O 描述当契约、车道各自实现。
**做**：① `agents.ts` createAgent 支持可选 `wecomUserId` + 新 `setAgentWecomUserId`（回填/改绑/解绑·作用于外包 agent 或超管 auth·超管也可免登§2.2）+ listAgents 回显 wecomUserId（白名单仍不回 keyHash）+ **唯一性校验 `wecomUserIdTaken`**（免登不歧义·两账号不撞同 userid·建时+回填时·exceptId 排除自身）；② ACTIONS 注册 setAgentWecomUserId（默认拒 admin:write·仅超管）+ 系统事实接口册行；③ admin `api/cloud.js` createAgent 加参 + setAgentWecomUserId + 错误码中文映射（WECOM_ID_TAKEN）；④ `Agents.vue` 建号表单加可选 userid 输入 + 列表「企微免登」列 + 每行 promptDialog 编辑（避 no-alert·用 utils/ui promptDialog）。
**守卫**：**本批不加结构守卫**（诚实·§7）——wecomUserId 是普通数据字段、无需焊的结构不变量；唯一性由写侧校验 + 测试锁，fail-closed 免登闸随**车道 B**（有真实消费者时）落。
**验证**：`npm run check` 全绿·测试 874→**878**（+4：写入回显/回填解绑/唯一性建时+回填/超管 auth 作用+非账号拒）·反向自检（关 wecomUserIdTaken→唯一性测试红→还原绿）·`build:cloud(38)`/`build:admin` 过。守卫 119 不变·集合 30 不变·云函数 38 不变（M⑦ 全程不新增函数/集合）。
**✅ 已部署生产**（CC 直部署 [[cc-deploy-authorized]]）：dry-run diff=1（adminApi）→ deploy 成功·smoke（invoke 非 POST 返 POST_ONLY=HTTP 闸正常）·preflight 钱链漂移清零 → adminApi LIVE（setAgentWecomUserId 可用）；build:admin 静态托管重部署（`index-aahAeDSU.js`·四路径核验完好）→ 超管即刻可在 `/admin` 外包账号页填/改企微 userid。
**剩**：车道 A 推送 ‖ B 免登 ‖ C 移动化（可开并行）+ 靠人企微后台配置 + 真机验。

## 2026-07-02 · 🗂️ admin 侧边栏业务域主类目重组（方案A 用户拍板·新守卫 admin-nav-route-synced）
**来源**：用户「/admin 类目没有逻辑排列，设计主类目展开小分类结构」→ 出两方案，拍板**方案A 业务域分组** + 默认全展开可手动收起。
**做（admin UI）**：Sidebar 重组——数据看板独立置顶当总览入口；其余 15 入口收「商品与内容（商品与上新[六步子级保留]/橱窗/库存/帮助视频）/ 订单与交易（订单/售后/对账）/ 客户服务（会话/客户360/节点诊断/知识库/满意度）/ 系统（外包账号/通知/外部后台）」四个可折叠主类目；折叠状态记 localStorage（回灌 sanitize）、跳进组内页面自动弹开；`.side` 加 overflow-y 防矮屏够不到底部账号区。
**守卫（118→119·反向自检×2）**：新 `admin-nav-route-synced`（roots #5 复制即漂移）双向核 Sidebar↔router.js——①引未注册路由=死链红；②注册页面无导航入口=孤儿页红（加页忘加导航）。反向①删库存入口→红、②改 /kb→/kbX→红、还原绿。
**指标**：守卫 118→**119** repoCheck·测试 872→**874**（provenance/coverage 元测随数组自增）·`npm run check` 全绿·`build:admin` 过·**✅ /admin 静态托管已重部署**（四路径 200 核验）。**续修（同日用户反馈）**：主类目与小分类颜色明显区分——主类目行深紫粗体（purple-ink 12.5px/700）+ 品牌紫图标，小分类保持灰（content-2），hover 转 brand-active；纯 token 引用无写死色。生产仓零改动。

## 2026-07-02 · 🔬 深度审核批（全项目高强度审核 → 钱链 4+1 修·守卫先红后绿·反向自检）
**来源**：用户「高强度审核：找 bug/冗余/更好实现」→ /deep-audit 逐行审交易链/安全闸/状态机/客服坐席线，逮出 5 件确凿问题（2 件钱向 P1），用户拍板全修（退款语义拍板「按当下重算封顶」）。
**修（先守卫红→改绿·实现前实证守卫红×3/测试红×6）**：
- **① approveRefund 数量级复核缺口（P1·钱）**：申请退款后又进课（qty≥2 未全进）同意仍按申请时刻件数打款＝多退一件的钱（原复核只拦整行 refundable=false）→ 同意时按**此刻**剩余可退件重算封顶：少了即把售后单 qty/itemTotal/refundAmount 同步降级（`requalifiedAt` 留痕）**与抢占同一次条件更新落库**、按新额触发工作流（refundCallback 金额核验依赖单据一致）；分摊公式抽 shared `refundShareFen` 单源（applyRefund 同步改用·根因#5 防两处漂移）。
- **② rejectRefund 非原子（P1·状态）**：读检查后无条件 doc().update 可与同意竞态把 approved 打回 rejected（钱退了单据却「已拒绝」·回调 from applied/approved 抢不到）→ 条件更新 where(status:'applied')。
- **⑤ confirmEnter 进课件数 RMW 互覆盖（P1·钱）**：items 整数组读-改-写无条件落库，同单并发进课互覆盖少记件数＝剩余可退虚高可多退 → `entVer` 顶层版本位 CAS（旧单 `exists(false)` 兼容·同 throttle 范式·冲突重读重试×3·耗尽 alert money REVOKE_RACE）。
- **④ pay 0元单不查 moved（P2）**：关单竞态窗口内谎报支付成功 → 查 moved；并发已付幂等 ok、否则 BAD_STATUS。
- **③ 购物车合计浮点直显（P2·展示）**：`￥{{ cart.selectedTotal }}` 价格设小数后显 59.699999999999996 → 过 `money()`（PriceSummary 同款；种子价现全整数=潜伏，admin 一改价即显）。
**守卫**：扩既有 `refund-remaining-qty-after-enter`（+approve 重算封顶/reject 条件更新/confirmEnter entVer 三断言·守卫数 118 不变）；测试桩新增 `setBeforeUpdate` 写前注入（照 setCallFunctionImpl 范式·并发窗口自此可测）。**反向自检×2 追加**：去 entVer 前置条件（字样仍在=静态守卫盲区）→ 并发测试红；reject 回退无条件 → 测试+守卫双红；均还原绿。
**审核全景（好的说好）**：createOrder/pay/回调/退款分摊/库存 CAS/频控 CAS/transition/activateCourse 一码一用/kfCallback 防吞单三态/坐席台 scoped 双闸/RBAC 默认拒——逐行验过全稳；npm audit 云函数运行时零外部依赖、34 漏洞全在 uni 构建链（产物不带）。未修低风险项过账 `待办与债` #34（getInventory 100 上限/refunded 痕/release TOCTOU/ensure 冗余往返/关键词单字）。
**指标**：守卫 118 不变（扩展非新增）·测试 866→**872**（+6）·`npm run check` 全绿·`build:cloud(38)`/`build:h5` 过。**✅ 部署（同日·CC 直部署 [[cc-deploy-authorized]]）**：dry-run diff=4（adminApi/applyRefund/confirmEnter/pay）→ `DEPLOY_ALLOWED=1 deploy-fns` 全成·manifest 更新·preflight 钱链漂移**清零**·smoke ping ok + 坏 key 401 fail-closed；前端 cart 改动随 v0.9.5+ 攒批。

## 2026-07-02 · 🔌 承面C 接真接口批（scoped 360 + 在接恢复 + /agent 接真网关·follow-up ①②③代码全清）
**来源**：三车道整合批（`8f0e97e`）记的 follow-up——① 外包 RBAC 收窄后 B 的 360 侧栏断头（直调 getCustomer360 已 403）；② 契约缺「列我在接」坐席刷新丢 active 会话；③ B 仍对 mock 未接真后端。
**做（守卫先红→实现到绿）**：
- **① getSessionCustomer360（外包唯一 360 读路径·§1.5 双闸）**：按**会话**取对应客户 360——分配 scope（`scopedLoad→assertOwnedByAgent`·只看自己 claim 的）+ 数据共享同意（`assertDataShareConsent`·客户未同意→403 NO_CONSENT·fail-closed·**同意闸自此有读侧真实消费者、不再只有 write 侧**）；超管＝数据控制者双闸 bypass；无身份桥接回 NO_BRIDGE（前端如实提示）；FORCE_AUDIT 加它（^get 破例强制留痕）。cap `agent:handle`（非 customer:view 批量读面）。
- **② listMyActive**：本坐席 active 会话（agentId=认证主体·天然 scoped·claimedAt 升序·bounded）——前端 QueueList 新「我的在接」区，刷新/重登恢复在接 + 多会话切换（之前认领第二个第一个就丢）。**连带补 escalated 黑洞**：listQueue 超管队列含 escalated（外包甩单原来无处可见/无人能接）。
- **③ /agent 接真后端**：`packages/agent/.env.production` 配生产 adminApi 网关（同 admin·mock↔真接缝 agentApi.js 自动切）；mock 补 2 handler 贴真语义（NO_BRIDGE 等）+ mock 登录 caps 对齐收窄后 `['agent:handle']`；Customer360Panel 由吃 openid 改吃 session（走 scoped action·NO_CONSENT/NO_BRIDGE 坐席可读提示）；根 `build:agent` script。
**守卫（不新增 id·扩 2 条既有）**：`cs-data-share-consented` 加④读侧消费者（agentDesk 须有 getSessionCustomer360 且真调 assertDataShareConsent(）；`cs-360-read-audited` FORCE_AUDIT 名单 3→4；`outsourced-reads-scoped` per-session 名单补新 action。**反向自检×2**：摘 consent 调用→红→还原；摘 FORCE_AUDIT 条目→红→还原。（附带教训：第一次还原误用 `git checkout` 把未提交实现一并冲掉——未提交态反向自检须定点还原。）
**指标**：守卫 **117 不变**（扩展非新增）·测试 852→**861**（+9：consent/scope/bypass/NO_BRIDGE/我的在接/超管见 escalated）·集合 30/云函数 38 不变·`npm run check` 全绿·`build:cloud(38)`·`build:agent`(121KB) 过·系统事实 +2 action 登记。
**续（同日）·initDb 单源化 + 建齐集合（`9219837`）**：用户控制台锁权限发现 csSession/agentState **不存在**——集合「动态建」＝首写才出现、锁不了。根因#5：initDb 建库清单硬编码 8 个（老清单）vs 现实 30 → 改引 kit `COLLECTIONS` 单源（加集合不用再改 initDb）+ 测试锁全量对齐（反向自检：缩短清单→红）。部署 initDb + invoke 真跑：**6 个集合此前一直不存在、这次建出**（csSession/agentState + inventory/auditLog/feedback[待办「控制台锁这三个」一直无从做的真因] + reviews）——**✅ 用户已全锁 ADMINONLY（同日确认）**，集合 ACL 账全平。测试 861→**862**。
**✅ 部署（同日·CC 直部署 [[cc-deploy-authorized]]）**：`deploy-fns` dry-run 14 函数（adminApi/dataConsent 本批 + 承面C 各批 kit/shared 波及累积：钱链5 + kf 系 + submit 系）→ 全量部署成功·manifest 37→**38**（+dataConsent）·preflight 钱链漂移**清零**；smoke：adminApi ping ok + listMyActive 坏 key 401 fail-closed。`build:agent` fresh → `tcb hosting deploy dist /agent`·curl 四路径核验完好（根=官网 / `/q/`=激活 / `/admin/`=控制台 / **`/agent/`=坐席工作台 LIVE**）。**剩靠人**：控制台锁 csSession/agentState 仅管理端（期望表已册）+ 真坐席真会话验（根因#8）。生产仓零改动。

## 2026-07-01 · 🅲🅱🅲 承面C 三车道并行 + master 整合（坐席台后端 A‖前端 B‖前置件 C·取并集 + 安全收敛）
**来源**：Batch 2 扇出闸后（`e69767d`）开 3 并行 CC（隔离 worktree）：车道 A 坐席台后端（`43115aa`）‖ B 前端对 mock（`16c9169`）‖ C 前置件（`594e750`），各自 check 绿 + push + tag。master 在 `integrate/cs-agent` 分支合三支 + 解接缝，squash 上 main。
**三车道产出**：**A** 坐席台 8 action（`adminApi/actions/agentDesk.ts`·listQueue/claim/release/send/getThread/setAgentStatus/escalate/close·走 `transition('csSession')`·接待上限派生 activeCount·发送经 cs/kfSend 接缝）；**B** 独立 `/agent` 工作台（新包 `packages/agent`·Vue3+Vite·mock↔真单点接缝 `agentApi.js`·队列/会话窗轮询/360 侧栏/快捷回复/认领·退回·升级·结束）；**C** C1 数据共享告知同意（`cs/dataConsent` 写 + miniapp 同意页 + kit `assertDataShareConsent` 读闸）+ C2 外包账号管理（`actions/agents.ts` createAgent/disableAgent/listAgents + admin Agents 页）+ C3 防导出 kit `assertOwnedByAgent`。
**master 整合的接缝与安全收敛（关键·非机械合并）**：
- **11 action 注册**：A/C 都把 adminApi `ACTIONS` 查表注册留 master——补 import + 8 agentDesk + 3 agents 查表项（否则 UNKNOWN_ACTION 不可达）+ 系统事实接口册 11 行。
- **外包 RBAC 收窄（安全·闭合批量导出洞·车道 C 报）**：A 曾设 `ROLES.outsourced=['customer:view','agent:handle']`——`customer:view` 让外包一建号即可 getCustomer360/searchCustomer 遍历全量客户＝批量导出（违 §1 定稿「分配制·只看自己 claim 的会话+对应360」）。**收窄为仅 `['agent:handle']`**：外包看 360 只能走「自己 claim 会话」的 scoped 路径。adminRbac/agentDesk/agents 三测试断言同步改为新模型（外包直调 360→403）。
- **scope 闸单源化（根因#5）**：A 内联 `ownsSession`（内存谓词·含超管 bypass）与 C 的 kit `assertOwnedByAgent`（load+check·fail-closed）同一不变量两处表达——收敛到 kit 单源：A 的 5 个 per-session action 改经 `scopedLoad`（超管走 loadSession·外包走 `assertOwnedByAgent()`·避双读）；C 的守卫 `outsourced-reads-scoped` 扫描路径由 `functions/cs/agentDesk/`（空目录·空扫）校正到 A 真实落点 `adminApi/actions/agentDesk.ts`，使守卫真咬。`kf-send-server-gated` 同步 `ownsSession`→`scopedLoad`。
- **listKb cap（车道 B 报的缺口）**：快捷回复读 kb 走 listKb，原默认 `admin:write` 外包调不了 → 标 `agent:handle`（kb=公司 FAQ·非 PII）。
- **守卫计数取并集**：A 只扩展不新增（守卫数不变）+ B 新增 3 + C 新增 2 = **117**（`自动化验证系统.md` 解冲突对齐·`guard-count-synced` 焊）。
**反向自检 ×2**：① ROLES 收窄——加回 customer:view→4 测试红（外包直调 360 非 403）→还原绿；② `outsourced-reads-scoped`——移除 agentDesk.ts 的 `assertOwnedByAgent`→守卫红。**顺手逮出并修一处假绿**：该守卫原用 `/assertOwnedByAgent/`（裸 token）匹配到 scopedLoad 注释里的字样即空绿 → 收紧为 `/assertOwnedByAgent\s*\(/`（真实调用·注释不算·根因#8「守卫匹配注释非代码」）。
**指标**：守卫 112→**117**（B+3/C+2·A 全扩展）·测试 787→**852**（+65·A 29 + C 25 + B 7 + 整合改）·集合 30/**云函数 38**（+dataConsent）·adminApi action +11·`npm run check` 全绿·`build:cloud(38)` + agent 包 `vite build`(120KB) 均过。
**剩靠人 / follow-up（未做·诚实标·待「接真接口 + 真机」阶段）**：① **scoped-360-for-outsourced**——外包收窄后不能直调 getCustomer360；B 的 360 侧栏（现 mock）接真接口时须建 scoped 路径（经 `assertOwnedByAgent` + `assertDataShareConsent`·后者当前 write 侧已活、read 强制待此落地=其真实消费者）；② **listMyActive**——契约只有 pending 队列，坐席刷新页丢在接会话，需给 listQueue 加 status/mine 过滤或补 action；③ **B 接真后端**（`VITE_ADMIN_API`）+ 真坐席真会话验「顺手/可靠」（根因#8）；④ 承面C 未上线·未部署（无新云函数上线面外 dataConsent 待随批部署）。生产仓零改动。

## 2026-07-01 · 🅲 承面C 阶段0地基 Batch 2（坐席台契约扇出闸·M0.a 契约 + M0.c 入队·cap 留车道A）
**来源**：`docs/后台360工作站/承面C工单.md` §2——开 3 车道并行前 master 串行立齐「契约 + 会话写入者」，否则各造一套 API＝整合灾难。
**做**：**M0.a** 坐席台 8 action（listQueue/claim/release/sendAgentMessage/getThread/setAgentStatus/escalate/close）I/O 契约单源 `packages/shared/src/csAgentDesk.ts`（纯类型·引 `CsSessionStatus`·车道 A 实现 + 车道 B 对 mock 共同遵·经 `@luckyducky/shared` 引·index 同步导出 `./cs`+`./csAgentDesk`）；**M0.c** `dispatch.ts` 加 `enqueueSession` + `kfCallback` 转人工分支调用——转人工即 upsert `csSession` `pending`（确定性 _id=`wxkf:<openKfId>:<euid>`·`.add` 撞 id 天然幂等·**不 clobber 已认领的 active**·根因#1），让 csSession 有真实 writer（listQueue 有数据）。
**cap/守卫的刻意不做（防过度工程·§7）**：cap `agent:handle` 运行时 wire（`ROLES.outsourced`+`ACTION_CAPS`）+ `order-transitions-declared` 扩扫 `functions/cs/` **归车道 A·与 action 同批落**——现网 ACTION_CAPS 只登记有实现的 action、未 gate 者默认拒 `admin:write`（守卫 `agent-rbac-gated` 在），空标 8 个不存在的 action = 空守（违 `lib.ts`「不立无 action 消费者的空 cap」+ Batch 1 提交明示）；守卫扫 `.collection('字面量')`/`transition('csSession',…)`，本批入队用 `COLLECTIONS.csSession` 常量 + 非 transition，扩扫要等车道 A 的字面量流转才真发挥。契约头注已文档化 cap 要求·车道 A 引同一 cap 串防漂移（根因#5）。
**守卫（反向自检×2）**：M0.c 行为测试（转人工入队 pending / active 不 clobber / 缺 euid 不写）——反向自检 `.add`→clobber 式 `.doc().set`→幂等测试红→还原绿；M0.a 契约在 typecheck 范围——反向自检引不存在类型→`build:shared` 报错→还原绿。**未加新结构守卫**（契约靠 typecheck + 消费侧类型约束·入队靠行为测试·为单条常量写加结构守卫＝过度工程）。
**指标**：守卫 **112 不变**·测试 783→**787**（+4）·集合 30 / 云函数 37 **不变**·`npm run check` 全绿·`build:cloud(37)` DONE。**剩**：master 起并行（预铺 csA/csB/csC worktree + `npm install`）→ 用户开 3 CC 贴工单 §6 → master 跑 `/loop` 整合。生产仓零改动。

## 2026-07-01 · 🔎 查订单身份桥接改走平台原生 batchget（idconvert 撞 48002 墙后·真机逼出）
**来源**：查订单真机验 → login idconvert 恒 `48002`（诊断布尔日志 pinpoint：hasUnionid/hasToken=true、hasExt=false·idconvert 是客户联系接口·Lucky Ducky 授权进客户联系可调用应用后**仍** 48002=更深权限墙）。用户拍「方案优先平台原生」→ 改用微信客服自己的顾客接口。**做**：① login 从 idconvert 改为**只存 unionid 到 users**（不调外部 API·不持密钥）；② kfCallback 收消息时 `ensureKfIdentity`——经**微信客服 `kf/customer/batchget`**（新 wecom helper）反查 external_userid→unionid→users.openid→建 kfIdentity（`resolveOpenid` 不动·读它查订单）·**绕客户联系 idconvert 48002**；已建映射即跳·best-effort 不反噬消息处理。**守卫**：`login-kf-identity-bridge` 改焊两端（login 存 unionid + kfCallback batchget 写 kfIdentity·反向自检过）。**测试**：login.test 重写（存 unionid）+ kfWecom 加 kfCustomerBatchget 单测。**指标**：守卫 111 不变·测试 774 不变·云函数 37·`npm run check` 全绿·`build:cloud(37)`·dry-run login+kfCallback。**剩**：部署 + 真机（顾客登录存 unionid → 发客服消息 batchget 建映射 → 查订单；batchget 是否受「顾客须授权/绑开放平台」限属根因#8 真机验）。

## 2026-07-01 · 📡 微信客服活体探针 kfHealthProbe（上线后静默故障告警·补调试日志 AA 隐患）
**来源**：独立端→企业微信内迁移中反复踩静默故障（FORGED/60020/40001·断了只在日志、没人盯就不知道·根因#8/调试日志 AA「上线后活体探针缺失」）→ 用户拍板补探针。**做**：新 `cs/kfHealthProbe`（30 分定时 `kfProbeTimer`·`isServerCall` 防客户端刷告警）→ `getAccessToken`(探 Secret) + `listKfAccounts`(新 wecom helper·真调 `kf/account/list` 探可信IP/权限·gettoken 抓不到 60020 须真调)→ 静默故障经 `notifyAlert` **唯一 botpush 接缝**推企微告警（webhook 已配·守 bot-push-single-seam）。**诚实边界**：回调验签漂移(FORGED)那类纯自动测不了（需合成真顾客消息）·靠「变更后手动发一条验」另兜。**守卫**：新 `kf-health-probe-wired`(roots #12·焊 ① isServerCall ② 真调 getAccessToken+listKfAccounts ③ 经 notifyAlert 接缝)·反向自检过。**指标**：守卫 110→**111**·测试 768→**774**(+5 探针 + 元测)·云函数 36→**37**·`npm run check` 全绿·`build:cloud(37)`·dry-run 仅 kfHealthProbe（wecom/kit 改经 tree-shaking 不波及）。**剩**：部署 + 定时真跑（故障时手机收告警）。

## 2026-07-01 · 🔗 P0 链② login 端客服身份桥接（开放平台认证通过后·查订单地基·不 republish·login 不持密钥）
**来源**：用户「1，开放平台通过」——① 绑开放平台认证通过 → 小程序登录能拿 unionid。补 login 端桥接（原「前端剩 login 存 unionid」）让客服会话「查你的订单」可用。**设计**：桥接放 login 云函数（backend-only·不 republish 发版），且 **login 不持微信客服密钥**——加 `getCachedKfToken`(kit) 复用 kfCallback 维护的缓存令牌做 idconvert（尊重刚做的密钥收敛·根因#3 密钥不扩散到 login）。
**做（cloud·best-effort 不反噬登录）**：① kit `currentUnionId`（经 kit 取 UNIONID·守 T2 禁裸 getWXContext·`as any` 绕 SDK 类型滞后）+ `getCachedKfToken`（只读 kfState 缓存令牌·不 gettoken 不需 secret）；② login 首次（有 unionid + 未 kfBound + 有缓存令牌 + idconvert 得 ext）建 `ext→openid` 映射写 kfIdentity + 标 `users.kfBound`（幂等·省 idconvert 限频）；取不到令牌/48h 会话身份就跳过下次再试。
**守卫（反向自检·109→110）**：新 `login-kf-identity-bridge`(roots #3) 焊 login 三链（currentUnionId 取 + unionidToExternalUserid 转 + 写 kfIdentity）·防桥接被摘致查订单静默失效。反向自检：去 currentUnionId→红→还原绿。
**测试**：桩加 `setUnionId` + getWXContext 返 UNIONID；新 `login.test.js` 4 例（桥接建映射+标 kfBound / 无 unionid 跳 / 无缓存令牌跳不崩 / 已绑不再 idconvert）。
**指标**：守卫 109→**110**·测试 763→**768**(+4 login + 元测)·云函数/集合不变（36/28）·`npm run check` 全绿·`build:cloud(36)`·**dry-run 仅 login 变**（kit 改经 tree-shaking 不波及他函数）。**剩**：部署 login + 真机验（测试顾客登录小程序→查 kfIdentity 有 ext→openid 映射→客服会话查订单）。生产仓零改动。

## 2026-07-01 · 📨 P0 ④ 客服主动发消息验证工具 cs/kfSend（send_msg 接待窗口真机验·承面C sendAgentMessage 雏形）
**来源**：P0 推进中——① 绑开放平台用户已提交等认证（查订单待认证通过 + 真机验 unionid·根因#8 不盲写）；② 客户联系后台核出小程序已绑 + 1 自建应用可调接口（大半就位）；转做**不卡认证、可今天验**的 ④「send_msg 人工接待窗口真机验」——承面C 收发命门（施工蓝图「没验不开建」）。机器人即时自动回复的 send_msg 已证能发，缺的是「非即时·坐席主动发」这一真机证据 + 一个能主动触发 send_msg 的入口（现无）。
**做（cloud·新最小函数）**：新 `cs/kfSend`——给定 external_userid + open_kfid + text，48h 窗口内经 `kit/wecom.sendMsg` 主动发文字。**服务端专用闸 `isServerCall`**（越权发送面·带 openid 客户端调用拒·根因#3）；errcode 原样带回便于真机联调看结果。**定位**＝承面C 坐席回复 `sendAgentMessage` 的验证版雏形（承面C 落地升级为坐席 RBAC agent:handle + 会话归属校验）——非开建承面C，是其买/建决策的前置命门验证。
**守卫（反向自检·108→109）**：新 `kf-send-server-gated`（roots #3）焊 ① isServerCall fail-closed 拒客户端 ② 真调 sendMsg。反向自检：篡改闸 `!isServerCall()`→`false`→红→还原绿。新错误码 `SERVER_ONLY`（登记 errors 册·known-error-codes 过）。
**登记**：cloudbaserc（36）+ 系统事实接口册 `cs.kf-send` 行 + 云函数计数 35→36（function-count-synced）+ guard-deploy SENSITIVE_FNS 加 kfSend（主动发消息敏感）。
**测试**：`kfSend.test.js` 4 例（服务端调用发出·客户端 SERVER_ONLY 拒·缺参 BAD_ARGS·未配置 KF_NOT_CONFIGURED）·桩 fetch 同 kfBind 法。
**指标**：守卫 108→**109**·测试 758→**763**（+4 kfSend + 守卫元测）·云函数 35→**36**·集合不变·`npm run check` 全绿·`build:cloud(36)` DONE。**剩靠人**：① 用户在 kfSend 函数设 `WXKF_CORPID`/`WXKF_SECRET` env（copy kfCallback·config手册 §3）② 非企业成员测试微信 ③ 真机联调（顾客发消息开窗→抓 external_userid→invoke kfSend 延时主动发→顾客确认收到=命门通过·根因#8）。生产仓零改动。

## 2026-07-01 · 🔏 B5.4 审计扩真实操作者身份（agent_id·§1.5 越权剩口·承面C 前置·不卡 P0）
**来源**：「继续客服系统」→ 核出 M①–M⑤ 全部署、M③/M⑥ 卡 P0（靠人运营前置）；不卡 P0 的真实缺口＝施工蓝图 §1.5 越权剩口「审计 agent_id 未做·多账号已上·审计现仍 `operator:'admin'`」。亲核（根因#8 看码非声称）：`kit/audit.ts` recordAudit 硬编码 `operator:'admin'`、注释自陈「多用户后扩为真实身份、痕迹结构已就位」——M⑤ 多账号 RBAC 既上线，正是补它之时。治 **根因#3 信任边界可追溯**。
**做（cloud·三处贯通·向后兼容）**：① `lib.ts` checkKey 三返回点加 `operator`（超管 doc=`name||'admin'`·外包账号=`name||_id`·bootstrap=`'admin'`）——身份随 caps 一起解析返回；② `index.ts` 分发处从 `auth` 取 `operator` 贯到审计两调用点（try/catch 各一）；③ `audit.ts` recordAudit 接 `operator?` 入参、以 `String(entry.operator||'admin').slice(0,60)` 落库（缺省回退 admin 保 fail-soft/向后兼容）。谁查/改了谁留真痕、不再全糊 admin。
**守卫（先红后绿·反向自检·107→108）**：新 `audit-operator-threaded`（roots #3）焊三链——① audit.ts operator 取自 entry.operator（非硬编码）② index.ts 读 auth.operator + recordAudit 调用传 operator ③ lib.ts checkKey 返回 operator。先红：4 子断言全红；后绿：转绿。反向自检：post-green 篡改 audit.ts arm①（退回 `operator:'admin'`）→红→还原→绿。**踩坑**：lib.ts 注释初写「recordAudit」字样触发 `admin-actions-audited` 单源守卫（禁 audit.ts/index.ts 外提该词）→改措辞「操作审计」避开（守卫正确工作·非误报）。
**测试**：audit.test.js 加「operator 贯入→记真实身份(agent-lin)」；原「未传→回退 admin」断言补注释保留。
**指标**：守卫 107→**108**（+audit-operator-threaded）·测试 756→**758**（+operator 贯入 + 守卫元测）·集合/云函数不变（35）·`npm run check` 全绿·`build:cloud(35)` DONE·计数同步 `自动化验证系统`(108)。**✅ 已部署 adminApi 生产**（`3bf03da` 后·preflight 全过·dry-run 挑 1 函数·smoke ping 200·operator 落地生效·三处均在 adminApi 打包单元内）。真机端到端（真账号登录→动状态→查 auditLog 见真 operator）属根因#8 靠人。生产仓零改动。

## → 已退役：2026-06-28~30 批次
2026-06-28~30 的客服自建建设线（选型→拆批→B0/B1.1→三波并行→M⑤ RBAC）+ 品牌字体真机修 + dev:h5 等**已关账**批次已卷档 → `archive/重构日志-202606客服自建.md`（索引 `archive/README.md`）。更早：`archive/重构日志-202606上半.md` / `-202606下.md` / `-第一次重构.md`。
