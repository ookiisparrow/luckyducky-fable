# 重构日志（样板房专属）

> 本文件只记重构线（Phase A / B0–B9）的批次结论与证据；生产线的工作日志在 `工作日志.md`（克隆自生产仓，本仓不再更新）。三支柱：`根因账本.md`（病根）+ `重构总计划.md`（v5：四主张+自动化优先）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-06-13 · 🏁 会话交接快照（switch-ready，待用户拍板切换日）

**新会话从这里接**：读 memory `rebuild-parallel-repo` → 本快照 → `git log --oneline -8`。从样板房 `luckyducky-next` 启动可直调技能 `/refactor-batch` `/deep-audit` `/acceptance-check`。

**当前状态**：重构主体 B0–B9 全完成 + 安全/自动化/接口**三轮深度审计 + 遗留项全收口**，**switch-ready**。生产仓 `luckyducky-miniprogram` 全程零改动、HEAD 仍 #78(`2dd5a72`)；样板房全推 GitHub 备份。
**数字**：测试 237；守卫 **10 repoCheck + 4 fileRule**（全表见 `自动化验证系统.md` §三）；28 云函数 6 域；build cloud/h5/mp-weixin/admin 全 DONE。

**本轮（审计后）已做**：金额硬边界 + 删除诚实化 · activateCourse 半步失败自愈 · 课程视频服务端保护(getCourses 剥 fileID + getPlaybackUrl 鉴权) · Fen 接钱链(createOrder 分整数) · 口令 bootstrap 关窗(债#15) · 依赖审计评估(构建工具链、运行时近零) · 前端分页四处接游标 · 桌面审计 P1/P3(下单库内价 fail-closed + trackEvent 进度防刷) · 自动化元审补 `writes-need-gate` 守卫 · 接口正册机器化 `interface-catalog-sync` + 补 getContent/getMyAfterSales 测试 · 方法论固化 5 个 `.claude/skills` · 深审④:用户域首写确定性 _id 收口(根因#1 补全, `kit/ids` ensureDoc, +3 并发反向测试)。

**唯一大事待办（用户拍板切换日）**：按 `acceptance-check` + `docs/切换runbook.md` 执行——`DEPLOY_ALLOWED=1` deploy-fns 真跑全量 28 · 控制台 `videos/` 改私有读 · console-assets drift-checklist 对照线上核 · 验收单 X(库权限)/Y(支付)/Z(退款) 真机重走 · 发布前恢复 manifest urlCheck。
**切换后/延后（runbook §六）**：CLAUDE 本体 reconcile · 债#14 users/progress 确定性_id 惰性迁移 · 后台账号 v2(产品级) · 依赖主版本升级(另开分支) · 小记债(Sass @use / trackEvent 事件限流 / 状态角标服务端计数)。

## 2026-06-13 · 接口正册落地 + 机器化（skill 扫描产物 → 守卫）

用户给 `docs/接口正册.md`（`$luckyducky-interface-catalog` skill 扫描 + 人工核对生成的全项目接口权威登记册：28 云函数 + 31 adminApi action + 16 集合 + 存储/回调/DB 契约，带鉴权/副作用/测试）。价值高——它自评 §10 P1=「只是 Markdown、无仓内自动校验，新增接口可能忘登记」。接上「能自动化就自动化」：
- **机器化其 P1**：check-structure 加 `interface-catalog-sync` 守卫——每云函数（functions/<域>/）+ 每 adminApi action（index.ts ACTIONS + ping/login）须在正册登记，否则红。守卫现绿＝**反证正册完整准确**（28 函数 + 31 action 全登记）。反向自检：造未登记函数→红→还原。
- **补其 P2**：正册标注 getContent、getMyAfterSales「未见独立测试」——补 `tests/cloud/getContent.test.js`、`getMyAfterSales.test.js`（契约锁：空态/有记录、本人隔离/分页结构）。
- 正册回写：P1/P2 标 ✅、两函数测试栏更新、closeExpiredOrders 补 isServerCall 注记；自动化验证系统.md §三 守卫表 9→10。

**验证**：npm run check 全绿（仓级 10 条）；测试 230→234。生产仓零改动。
**正册余项**（P2/P3，已在其 §10 记）：store/user.js·utils/track.js facade 特例、admin cloud.js 按域拆、videos/ 私有读（控制台）、client facade 级 called-but-unregistered 扫描（可迁 skill 逻辑入 scripts/）。

## 2026-06-13 · 自动化覆盖深度审核 + 补 writes-need-gate 守卫（根因#3 结构化）

用户「能自动化的都自动化了吗」——审自动化体系本身（哪些规矩还靠人/测试自觉、可机器化）。
盘点：CI/pre-commit/编辑 hook/deploy 闸齐；9 repoCheck（含本批 +1）+ 4 fileRule + typecheck + lint + 230 测试。
**挖出真缺口并补**：根因#3 的「不过闸写不出来＝结构事实」此前**未机器化**——closeExpiredOrders 写库却无任何闸（普通客户端可滥调，效果良性但属漏网）。
- 新 repoCheck `writes-need-gate`：functions 单元（单文件 + 目录型 adminApi 合并）凡写库（add/set/update/remove）必引 kit 闸（withOpenId/withAdminGate/defineNotifyCallback/isServerCall）或 checkKey；纯读公开目录豁免。新守卫立刻抓到 closeExpiredOrders。
- 修 closeExpiredOrders：加 `if (!isServerCall()) return ok({closed:0})`——仅定时器/服务端可触发，客户端拒。测试 +1（客户端调用不关任何单）。
- 同步 `自动化验证系统.md` §三为现行守卫全表（9+4，原停在 B1 的 3 条）。

**审核结论（诚实）**：能机器化的核心都机器化了（四主张 + 12 根因的可结构化部分各有守卫，反向自检证明会咬）。**仍由人/测试承担（本质难机器化）**：验收方法论真实样本（#8，prose+fixture 测试）· 工具实例守则（#10，prose）· 反向自检本身（meta 验证，手动）· 少数刻意例外（草稿 status/批量关单/有界 limit/院外 评价聚合，非状态机或已记债）。
**验证**：npm run check 全绿（仓级 8→9）；测试 229→230。反向自检：造无闸写库函数→writes-need-gate 红→还原绿。生产仓零改动。

## 2026-06-13 · 桌面三份审计报告处置（深度安全审计 P1/P3）

读桌面三份审计（全项目审核/复审/深度安全审计，基线 213→216→225 测试）。多数发现近期已修（视频保护/bootstrap 关窗/商品假删/发布价边界/浮点漂移/前端分页）；本批补两轮都点名的遗留：
- **P1 下单库内价 fail-closed（交易最终关口）**：publishProduct 只挡新发布，createOrder 直读库内 `p.price`/`sk.price` 未二次校验——历史脏数据/手工写库/迁移污染可穿透到订单与支付金额（负价→amount 被 clamp 0 即白拿）。修：createOrder 复用 isValidPriceYuan，主商品价/SKU 价无效即 `BAD_PRICE:<id>`/`BAD_SKU_PRICE:<id>:<sku>` 拒单。
- **P3 trackEvent 进度防刷**：原只验登录，任意用户可伪造任意课程进度污染看板/继续学习卡。修：折叠进度前校验本人已激活该课（无激活则只记事件、不折叠进度）。

**验证**：npm run check 全绿；测试 227→229（脏价拒单 + 防刷各一）。反向自检：神经掉两道闸→对应测试双红→还原绿。生产仓零改动。
**记账（runbook §三/§六）**：发布前恢复 manifest urlCheck；trackEvent 事件限流/采样 + segment 存在性校验；Sass `@import`→`@use`。**确认仍延后**：后台账号 v2（产品级）、依赖主升级（另开分支）、CLAUDE reconcile（切换时）、债#14（切换后）。

## 2026-06-13 · 体检遗留四项收口（用户「都解决掉」）

审计 + 外部 review 的遗留项逐个清，各自提交、全绿、反向自检：
- **Fen 金额接入钱链（根因#4/P2）**：createOrder 改分整数运算（toFen 累加 + asFen + fenToYuan 存元）消除元浮点累加漂移（19.9×3 不再 59.699…）；pay/payCallback 边界换 toFen；applyRefund 本已正确分运算不动。测试 +1（小数价无漂移）。
- **后台口令 bootstrap 关窗（债#15）**：空库首登「谁先登谁占」改为须匹配部署密钥环境变量 ADMIN_BOOTSTRAP_KEY；现网已初始化故切换不受影响。
- **依赖审计（P2）**：51 项全在构建工具链、不上线运行时（暴露近零）；非破坏性 audit fix 反引入 rolldown 一大坨、未采纳；根治须主升级 uni 栈、单开分支（评估入 runbook §六）。
- **前端分页「加载更多」（根因#7 前端侧关账）**：getMyAfterSales 后端补游标（原固定 50）；小程序 order-list/aftersales + 控制台 Orders/Refunds 四处全接 cursor（api 收 cursor 返 {list,nextCursor,hasMore}、store/页 loadMore + 触底/按钮）。测试 +1（aftersales loadMore）。

**验证**：npm run check 全绿；测试 218→227；三端构建（cloud 28 / h5 / mp-weixin / admin）全 DONE。生产仓零改动。
**仍属刻意/产品级延后**（runbook §六）：CLAUDE 本体 reconcile（切换时）· 债#14 确定性_id 惰性迁移（切换后更安全）· 后台账号 v2（产品拍板）· 角标服务端计数（低优先）· 依赖主升级（另开分支）。

## 2026-06-13 · 课程视频服务端保护（审计/外部 review P1，防付费课白嫖）

原 getCourses 公开返回 videoFileId、播放鉴权仅 UI＝付费课可白嫖。改「目录/播放分离」：
- **云侧**：getCourses 剥除 videoFileId（段只暴露 hasVideo + free）；新增 getPlaybackUrl
  (courseId,segmentId)——服务端定位分段 → 鉴权（免费段放行/付费段须本人已确认进课 enteredAt，
  与 getMyCourses 同闸）→ kit.getTempUrl 发短时效临时 URL；未剪段 url:null。kit 加 getTempUrl
  收口 cloud 存储原语。cloudbaserc 注册、产物 27→28（preflight/runbook/manifest 同步）。
- **小程序侧**：api/course +getPlaybackUrl、store/courses +playbackUrl（page→store→api）；
  播放器 fileMode 改判 hasVideo、videoSrc 用 ref playUrl、watch 当前段 id 重取地址；素材未剪仍占位。
- **控制台侧（git 外，待用户做）**：视频存储 `videos/` 改私有读——客户端不可直接 getTempFileURL，
  只能经 getPlaybackUrl。正册进 console-assets/02-库权限期望表.md；runbook §四加未购账号验。

**验证**：npm run check 全绿；测试 218→225（getCourses 不漏 fileID + getPlaybackUrl 六类鉴权）；
build:cloud 28 产物、build:h5 DONE。生产仓零改动。**真机/真视频路径待切换后按 runbook §四验**
（沙盒种子 videoFileId 全 null 走占位，鉴权链已单测锁）。

## 2026-06-13 · 彻底审计 + 体检加固批②：扫码激活半步失败自愈（自审 P2）

按 CLAUDE §8 分级尺子逐行深读交易链（下单→支付→回调→退款→售后）+ kit 安全内核 + 状态机 + 账号/口令。**结论：P0 面扎实、无致命伤**——回调防伪框架级强制、金额取库内、付款换分四舍五入、越权先验本人、transition 原子幂等、退款核验 fail-closed、管理函数加闸。**自审新挖 1 个 P2 并修复**：
- **activateCourse 半步失败**：transition 翻 qrcode（已用）与 activations.add 非原子，函数中途挂 → 码被消耗但激活记录没写成 → 顾客卡住（confirmEnter 报 NOT_ACTIVATED）、无自愈。修：抽 ensureActivation——确定性 `_id`=code 原子创建（撞重幂等）、抢到/本人重扫一律 ensure（自愈）、老随机 `_id` 命中即用不重建（惰性迁移，根因#1）。测试 +2（半步失败自愈 / 老数据兼容），现有 6 例全过。

**验证**：npm run check 全绿；测试 216→218。反向自检：抽掉自愈 add → 半步失败测试红 → 还原绿。runbook §〇 数据兼容性更新（activateCourse 改惰性迁移、仍零搬迁）。生产仓零改动。

**审计其余发现已分级归档（不丢）**：P1 课程视频服务端保护（getCourses 公开 videoFileId，待拍板）；P2 债#14（login/updateProfile/trackEvent 先查后建可重复，users/progress）、Fen 类型仅 applyRefund 用（钱链其余存元浮点、付款已纠分不算错钱）、债#15 口令 bootstrap+v1 明文 localStorage（环境已初始化即低风险）、依赖审计；P3 refundCallback 用 get+update 非 transition（幂等等值安全）、closeExpiredOrders 无运行时闸（效果良性，可加 isServerCall）、confirmEnter items 整组重写窄并发、前端分页未接游标。**审计够不到**：库权限档位（验收单 X 控制台核）、真支付/退款（验收单 Y/Z 真机）。

## 2026-06-13 · 体检加固批①：金额硬边界 + 删除诚实化（外部 code review 触发）

用户跑了一遍外部 code review，挑出若干项。逐条去代码核实后，本批补两处**切换前该堵的真窟窿**（均已去代码核实属实）：
- **P1 金额输入硬边界**：上架原 `!Number(d.price)` truthy 校验，`Number('-5')`=-5、`Number('Infinity')`=Infinity 穿透——可上架负价/无穷大价商品；下单只校验 qty 正整数，无数量/条数上限。新增 `packages/shared/src/limits.ts`（MAX_PRICE_YUAN/MAX_QTY/MAX_ORDER_LINES + isValidPriceYuan，单一来源防漂移）；publishProduct 价格/SKU/was 全过 isValidPriceYuan；createOrder 加 TOO_MANY_ITEMS/BAD_QTY 上限。**印证根因#4 只立柱未接线**（Fen 类型造了只用于 applyRefund，钱链其余仍裸算）——本批先堵输入边界，Fen 全面接轨留后续。
- **P2 删除诚实化**：控制台「删除·不可撤销」原只删 productsDraft、已上架 products 仍可被买。deleteDraft 改为草稿 + 已上架一并删（历史订单存快照不回读 products，删上架不伤历史）；createOrder 的 UNKNOWN_ITEM 天然拒已删商品。

**验证**：npm run check 全绿；测试 213→216（createOrder 上限 / 上架价边界 / 删除诚实各一）。反向自检：神经掉 BAD_QTY + 删上架两处 → 对应测试双红 → 还原绿。生产仓零改动。

**同批 review findings 已三类归档（不丢）**：
- 切换前不补、属已知决策/院外：后台口令 bootstrap + localStorage（债#15 + v1 刻意鉴权设计，账号体系升级时根治；当前环境已初始化即低风险）；商品「临时下架」功能（院外债#12，本批只做了「删除诚实」不含可逆下架）。
- 该正经单开一批：**课程视频服务端内容保护**（getCourses 公开返回 videoFileId，播放鉴权仅 UI；代码注明「另做」、设计规格 §四）——保付费课不被白嫖，**偏大、待用户拍板排期**。
- 切换后/另开分支：依赖审计升级（npm audit 多在构建工具链，开升级分支验 dcloud/vite/esbuild 组合）；前端分页「加载更多」（后端已就绪，债）。

## 2026-06-13 · B8：质量基线方法论收官（根治账本 #8/#10/#11）—— 极细四微步

用户「极细」指示：拆 4 个独立可验证/可回退的微步，各一提交。能机器化的全机器化（自动化优先）。
- **B8a #11 文档职责渗漏**：CLAUDE §1 加「文档体系规则」（条目≤8行/调试日志按季卷档/正册只放仍会被查的/约定·记录·参考三类边界·状态一处权威/本文件不放进度bug欠债）；守卫 `docs-budget`（CLAUDE.md ≤180 行，病史曾 314→收口 130）。
- **B8c #10/#12 工具+平台**：CLAUDE §10 加工具守则（build 前停自动化/工具怪象先 cli quit 重启）、§11 加平台接缝清单（cloudbase_module 收口 kit callFlow 单点，参数正册在 console-assets）；守卫 `flow-seam-single`（'cloudbase_module' 调用全库仅 kit/flow.ts，匹配带引号调用不误伤注释）——根因#12 绝迹证明 grep=1 机器化。
- **B8b #8 验证样本失真**：现有 uploadFinish 测试只用 4 字符玩具分片验 seq 逻辑，从不验真实尺寸。桩 uploadFile 记录落库字节（control.lastUpload）；adminMisc 加真实 200KB 图多片上传→重组字节完整断言（=原图大小未截断）+ 分片尺寸边界（90,000 放行/90,001 拒）；验收手册.md 加「六、验收方法论」三条（真实尺寸样本/凭证走完整请求形状/兜底覆盖全失败点且被测试逼出）+ 收尾三问。
- **B8d admin 基线**（账本§三顺手项）：admin 此前零测试，lint 已被根 eslint . 覆盖；vitest.config 加 admin project（tests/admin/**，node 环境），tests/admin/cardSvg.test.js 锁卡面 SVG 生成 + XSS 转义（标题含 <script> 被 esc）。

**机器守卫累计**：仓级不变量 6→8（+docs-budget +flow-seam-single），逐文件规则 4 条。测试 208→213（+#8 真实尺寸 2 + admin 3）；42 test files（admin project 入编）。每微步反向自检（CLAUDE 灌>180 红 / cloudbase_module 直调双红 / 还原绿）。生产仓零改动。
**留 B9**：CLAUDE 本体 §2/§5/§7 仍描述重构前架构（src//data·catalog 单源/H5 回退/四道闸），切换时 reconcile 到 packages//cloud-kit/api-cloud-only/八闸。

**剩 B9**（切换 runbook：deploy-fns 真跑全量 27、users/progress 惰性迁移、flowId/console-assets drift 核对、验收单 Y/Z 真机重走、回滚预案、CLAUDE 本体 reconcile；用户拍板切换日）——重构最后一批。

## 2026-06-13 · B7：部署自动化 + 四主张机器焊死收官

**四条架构主张此批起全部上锁机器守卫**（每主张→一条永久不变量）：
- **T1 砍多端** → `api-cloud-only`（api 禁 import @/data/，已在 B3a）。
- **T2 域分组** → `kit-only-cloud-primitives` + **新 `cloud-domain-grouped`**（functions/ 顶层禁裸 .ts，函数须在 catalog/learning/orders/user/system/admin 域内）。
- **T3 云唯一真相** → `seed-single-source`（已在 B3b/c）。
- **T4 按链内聚** → **新 `dep-direction`**（miniapp 依赖方向 pages→store/api→utils，data/utils 是叶；禁反向 import：utils→store/api、data→上层、api→store/pages、store→pages）。现有代码零违例＝守卫绿。

**部署自动化**：
- `scripts/deploy-fns.mjs`：build:cloud → 按产物内容 hash 算清单 → 与 `.deploy-manifest.json` diff → 报告变更函数 →（仅 `DEPLOY_ALLOWED=1`）逐函数 tcb 部署 + 更新清单。**三道部署保险**：默认/`--dry-run` 只报告；真部署须 `DEPLOY_ALLOWED=1`（样板房永不设置）；实际 tcb 仍过 guard-deploy（样板房 deny-all）。
- `.deploy-manifest.json`（入 git，函数→hash→时间）：初始 functions:{}＝尚未经 deploy-fns 部署；B9 切换首部署全量 27 个。
- `cloudbaserc.json`：`functionRoot` `cloudfunctions`（已删）→ `packages/cloud/dist`；删僵尸 `getOpenId` 条目。

**验证**：`npm run check` 全绿（仓级不变量 5→6，逐文件规则 3→4）；测试 208 不变（守卫/工具批，无行为变更）。反向自检：utils 注入 import @/store → dep-direction 红；functions/ 顶层放 .ts → cloud-domain-grouped 红；二者还原绿。deploy-fns：无 DEPLOY_ALLOWED 真部署被拒（exit 1）；`--dry-run` 正确报 27 函数待部署。生产仓零改动。

**剩 B8**（文档体系/验收方法论/工具守则，根因#8/#10/#11）→ **B9**（切换 runbook：deploy-fns 真跑全量 27、惰性迁移、flowId/console-assets drift 核对、验收单 Y/Z 真机重走、回滚预案；用户拍板切换日）。

## 2026-06-13 · B6：控制台资产正册化（根治账本 #9 + #12）

系统真实形态=git 内代码 + git 外配置（微信控制台连接器/工作流/库权限）；后者无版本管理、误删即支付/退款/安全瘫痪（根因#9）。正册化进 `console-assets/`：
- `01-支付退款工作流.md`：8 件资产（连接器 wxpay_33nb7su / 主支付流 sywxzfapifqzf_nncvqss2 / 退款流 kbgzl_n8gojr3a / paynotify·refundnotify / 转发节点 / config.pay 文档 / 商户号 1113881793）的 ID、入参表达式、config.pay JSON（密钥 REDACTED）+ **30 分钟重建手册**。
- `forward-node.js`：paynotify/refundnotify 转发节点代码正册副本（@cloudbase/node-sdk callFunction → payCallback/refundCallback）。
- `02-库权限期望表.md`：16 集合 + 云存储权限期望档位（固化验收单 X：严格 13 + 公开读 3）。
- `README.md`：「先 repo 后控制台」变更纪律 + drift-checklist + 运维坑册（工作流改完必发布 / 证书序列号无空格 / tcb 多函数名只发第一个 / paynotify 双保险 / 平台规则单方变=#12 接缝收口）。

**守卫** check-structure repoCheck `console-assets-present`（4 份正册文件须存在，git 外资产唯一版本化记录不可误删）；eslint 忽略 console-assets（参考副本非项目源）。**验证**：`npm run check` 全绿（仓级不变量 4→5）；测试 208 不变（纯文档）。反向自检：删一份正册 → 守卫红 → 还原绿。**用户动作**：照 README 的 drift-checklist 对照线上控制台核一遍（forward-node.js 系据记录重建，首次须校正为权威）。生产仓零改动。

**剩 B7**（部署自动化：deploy-fns manifest + cloudbaserc functionRoot 改指 packages/cloud/dist + 函数目录分组 repoCheck + DEPLOY_ALLOWED 硬闸）→ **B8**（文档体系/验收方法论/工具守则，根因#8/#10/#11）→ **B9**（切换 runbook，用户拍板）。

## 2026-06-13 · B5b-3：分页协议（根治账本 #7）—— B5 收官

根治账本 #7（列表固定 limit，规模即把旧数据挤出工作台）。**后端先行**：
- kit 新增 **`paging`**（`pageParams` 解析入参 + `pageQuery` 游标分页：按排序字段值翻页，多查一条判 hasMore，无 cursor/limit=首页）。**兼容无参**：旧前端读 `.list` 照常，新前端用 nextCursor 渐进取全量。
- 接入 3 处（≥2 消费者justified）：`getMyOrders`（orders/createdAt，首页 100）、adminApi `listOrders`（orders/createdAt，首页 200）、`listRefunds`（afterSales/appliedAt，首页 200）。返回 `{ list, nextCursor, hasMore }`。

**验证**：`npm run check` 全绿；测试 204→**208**（+4 paging：pageParams 钳取/首页/翻页/无参兼容）；`build:cloud` 27 产物。反向自检：limit+1 探测改回 limit → hasMore 永远 false，首页分页测试红 → 还原绿（hasMore 检测守护证）。**后端先行完成**；前端「加载更多」（小程序 order-list + 控制台订单/售后列表）随用随接（不阻塞，旧前端零回归）。生产仓零改动。

**🎉 B5 收官**：adminApi 迁 TS（B5a）→ 特征锁（B5b-1）→ 拆 actions 查表（B5b-2）→ 分页（B5b-3）。根因 #5（样板冗余）/#7（固定 limit）于此关账。**剩 B6**（console-assets 正册，根因#9）→ **B7**（部署自动化 + cloudbaserc functionRoot 改指 + 函数目录分组 repoCheck）→ **B8**（文档体系/验收方法论/工具守则，根因#8/#10/#11）→ **B9**（切换 runbook，用户拍板）。**记债**：users/progress 确定性_id 惰性迁移（债#14/根因#1 通例化）+ 前端分页「加载更多」。

## 2026-06-13 · B5b-2：adminApi 拆 actions/ 查表（根治账本 #5 最后一块）

752 行单体 → 多文件模块（行为由 B5b-1 的 22 断言锁住，拆后必须全过）：
- **build.mjs 支持函数目录**：collect() 扩成识别 `<域>/<name>/index.ts`（多文件，esbuild 内联本地 imports）+ `<域>/<name>.ts`（单文件）；manager-node 检测改读打包产物（多文件里它在 lib 不在 index）。
- **adminApi.ts → adminApi/**：`index.ts`（HTTP 外壳 + 口令闸 + **29 action 查表分发**）+ `lib.ts`（reply/sha/ensure/checkKey/cleanProduct/cleanCourse/storeImage/manager/str 共享件 + Ctx 类型）+ `actions/{products,courses,cards,batches,content,orders,refunds,dashboard}.ts`（按域分 8 模块，每 action 收 Ctx{db,cloud,data,drafts}）。approveRefund 续用 kit.callFlow；str 用 kit。

**验证**：`npm run check` 全绿（adminMisc+adminRefundActions+adminPipeline 三套 22 断言对拆分后**全过**＝faithful 拆分）；测试 204；`build:cloud` **27 产物**——adminApi 仍是 1 个产物（lib+actions 内联进 index.js，无孤儿函数），manager-node 依赖从 bundle 正确检测。净化：752 行 if/else 链 → 11 文件（index 外壳 + lib + 8 actions）+ 查表分发。**根治账本 #5（样板冗余）的 adminApi 单体收官**。生产仓零改动。

**剩 B5b-3**：分页协议（根因#7：list 固定 limit 挤出，admin 订单/售后 + 小程序 order-list，kit paging，后端先行兼容无参）。然后 B6 console-assets / B7 部署自动化（含 cloudbaserc 重指 + 函数目录分组 repoCheck）/ B8 文档体系·验收方法论 / B9 切换 runbook。

## 2026-06-13 · B5b-1：adminApi 特征测试扩充（拆分前锁行为）

宪章「先写意图」+ 计划「先扩 characterization 再拆」：adminApi 上新流水线/课程/卡片/内容/批次等 action **此前无测试**（adminMisc/adminRefundActions 只覆盖钱与杂项），拆 actions/ 前先锁住现行为。新增 `tests/cloud/adminPipeline.test.js`（10 例）：HTTP 外壳（OPTIONS 204/非POST 405/坏JSON 400）+ 口令闸（ping 免口令/401/login bootstrap）+ saveDraft 白名单清洗 + **publishProduct 四道门**（NEED_COVER/NEED_INFO/NEED_SKUS/成功转数字+首发 featured）+ saveShowcase + saveCourseDraft/publishCourse（NO_DRAFT 守卫）+ saveCard 双面/颜色校验/尺寸夹取 + saveHomeContent 白名单 + createBatch 互调 genQrcodes + listBatches 聚合。

**验证**：`npm run check` 全绿；测试 194→**204**（+10，纯特征锁，对现 adminApi 全过＝准确捕获行为）。无代码改动，无净化——这是 B5b-2 拆分的安全网（拆后此套必须仍全过）。生产仓零改动。

## 2026-06-13 · B5a：adminApi 迁入 TS（faithful 提升）—— **全部云函数迁移完成，cloudfunctions/ 清空**

最后一个、也最难的函数（752 行、28 action、HTTP 触发、node API + manager-node + 云存储）。**TS 基建**：cloud 包加 `@types/node`（Buffer/process/require/crypto 内建）、tsconfig types:['node']、删自造 crypto.d.ts、sdk.d.ts 补 uploadFile/getTempFileURL、build.mjs external 加 `@cloudbase/manager-node`（按源检测，仅 adminApi 的产物 package.json 带此依赖）。**faithful 提升**：adminApi → `admin/adminApi.ts`，28 action 逐字保留；db 经 kit.getDb（无裸 init）；**approveRefund 改 kit.callFlow**（callFlow 第 2 消费者——cloudbase_module 退款流收口，根因#12 完成）；adminApi 的 `str` 改 kit.str（优雅性 A-1 的 4 处重复彻底归 kit）。

**验证**：`npm run check` 全绿（adminMisc/adminRefundActions 全套：口令闸 401/uploadFinish seq/createBatch/getDashboard txAlerts/approveRefund 抢占+flow+回滚/shipOrder feeMismatch 通过；cloud TS typecheck 含 node 内建）；测试 **194**（2 重指向）；`build:cloud` **27 产物**（adminApi 带 manager-node 依赖）。反向自检：checkKey 永远放行 → 口令错误 401 红 → 还原绿（管理闸守护证）。

🏁 **里程碑：全部 27 个云函数迁入 packages/cloud（admin 1 / catalog 5 / learning 8 / orders 10 / system 1 / user 2 共 6 域），cloudfunctions/ 清空删除。** T2 域分组主体完成（adminApi 内部 actions/ 拆分 = B5b 收尾）。剩 **B5b**（adminApi 拆 actions/ 查表 + 分页根因#7）；**B5 末**：cloudbaserc functionRoot 改指 + 部署单元/域分组 repoCheck（随 deploy 自动化 B7）。生产仓零改动。

## 2026-06-13 · B4c：refundCallback 收回调框架（根治账本 #3/#5）—— B4 函数迁移收官（仅剩 adminApi）

退款回调收编 + 回调框架完成。refundCallback 有「非成功也留 refundStatus 痕」的副作用（payCallback 没有），暴露 B2 框架（success 门控）不够通用，故**泛化 `defineNotifyCallback`**：`{ack, refId, onNotify}`——共享外壳=**防伪闸（根因#3 全场最关键）+ ACK 协议 + id 提取**，成功/非成功判定移入各回调的 onNotify。**payCallback 同步改用新框架**（success 门控移进 onNotify，行为逐字保留，10 例全过）；**refundCallback 新增**（fail-closed 核验/非成功留痕/幂等 refunded/订单留痕，7 例全过）。

**这是根因#5 收编做对的样板**：防伪闸从「两处各抄一遍」收成**框架一处**，两回调不经框架写不出来＝杜绝「忘记防伪」。**验证**：`npm run check` 全绿；测试 **194**（notify 反向测试随框架改 4→3，少的那条 success 门控移到回调测试）；`build:cloud` **26 产物**（orders 域 10）。反向自检：去掉框架防伪闸 → **pay+refund 两个回调的防伪测试同时红** → 还原绿（一处守两回调的铁证）。净化：回调防伪/ACK/id 三处共享一份。生产仓零改动。

**B4（其余函数迁移）收官**：26 函数全迁入 packages/cloud（catalog/learning/orders/user/system 五域），死代码 getOpenId 删除；**cloudfunctions/ 仅剩 adminApi**。剩 **B5**（adminApi 752 行/28 action 拆分 + 分页 + callFlow 收退款流）；**B4/B5 末**：cloudbaserc functionRoot 改指 packages/cloud/dist + repoCheck 部署单元数钉住（待 adminApi 迁完 cloudfunctions/ 清空后做）。

## 2026-06-13 · B4b-3b：支付/退款流迁移（交易波之二，根治账本 #12 平台接缝）—— B4b 闸门波收官

支付/退款流对：
- `orders/pay`：三道闸 + 库内金额（不信任前端价）+ 惰性超时关单（transition pending→closed）+ 0 元单（transition pending→paid）+ **kit.callFlow** 触发支付工作流；UNIFIED_ORDER_FAIL 留 pending。
- `orders/applyRefund`：openid 本人 + 订单 paid/shipped/done + 在单内 + refundable + 一单一品一售后（确定性 _id）+ 金额云端分摊（分运算、累计≤实付）逐字保留。
- kit 新增 **`callFlow`**（cloudbase_module 单点收口，根因#12：与微信平台接缝收到一处，平台规则变更改动面最小；消费者 pay 现，adminApi.approveRefund B5 迁入）。
- **守卫** check-structure fileRule `flow-seam-via-kit`：functions 域禁裸 cloudbase_module（须经 kit.callFlow）。

**验证**：`npm run check` 全绿（pay 全套：工作流触发入参/惰性关单/0元单/UNIFIED_ORDER_FAIL/PAY_NOT_ENABLED + applyRefund 分摊 26.44/全额178/ALREADY_APPLIED/NOTHING_LEFT 通过）；测试 **195**（2 重指向）；`build:cloud` **25 产物**（orders 域 9）。反向自检：往 pay 注入裸 cloudbase_module → flow-seam-via-kit 红 → 还原绿。净化：2 函数 init+闸收编；cloudbase_module 收口单点。生产仓零改动。

**B4b 闸门波收官**（13 函数全迁：账户基建 4 + 课程评价 4 + 订单生命周期 3 + 支付退款 2）。**剩 2 个**：B4c refundCallback（收 defineNotifyCallback，与 payCallback 同框架）；B5 adminApi 拆分。

## 2026-06-13 · B4b-3a：订单生命周期迁移（交易波之一，根治账本 #2 状态机）

交易波（状态机主战场）拆两小波；本批=订单生命周期（无外部支付流的三件：建单→确认收货→超时关单）：
- `orders/createOrder`（128 行faithful 迁移）：openid 闸 + 云端定价（不信任前端价）+ SKU 校验 + 服务端不变量（主商品必含/地址四要素）+ **订单号 add 库级唯一防覆盖 + 重试**（A-4 资金安全）+ PAY_MODE 开关。ADDONS/券为占位（链4，与 data/checkout 镜像，P4 接真券时替换）。
- `orders/confirmReceive`：openid + 归属 + **kit.transition 第 3 消费者**（shipped→done 条件原子流转）。
- `orders/closeExpiredOrders`：定时触发无闸，pending 超 15 分批量 closed。

**验证**：`npm run check` 全绿（createOrder 全套 14 断言：定价/SKU/不变量/地址/撞号/PAY_MODE 通过）；测试 **195**（3 测试重指向）；`build:cloud` **23 产物**（orders 域 7 个）。反向自检：createOrder 的 add 换回 set（覆盖）→「撞号不覆盖旧单」红 → 还原绿（A-4 资金安全守护证）。净化：3 函数 init+闸收编；transition 三消费者（payCallback/activateCourse/confirmReceive）。生产仓零改动。

**剩 4 个**：B4b-3b 支付/退款流（pay/applyRefund，kit **callFlow** 收 cloudbase_module 单点，根因#12）；B4c refundCallback；B5 adminApi 拆分。

## 2026-06-13 · B4b-2：课程/评价一次性副作用迁移（闸门波之二，根治账本 #1 集中区）

闸门波最高价值一组——并发竞态/一次性副作用/库级唯一（账本 #1 的实例都在这）。迁入（每条安全语义逐字保留，全测试套通过）：
- `learning/activateCourse`：一码一用——**kit.transition 第 2 消费者**（qrcode _id===code，unused→activated 抢占，moved 即赢）；本人重扫/他人占用/INVALID_CODE 状态全保留。
- `learning/confirmEnter`：退货权法律节点（H/I 修复区）——`enteredAt` 原子抢占 + 失效与「首次进课」绑定（重复确认不多扣）、状态放宽 paid/shipped/done（收货后扫码也失效）逐字保留。
- `catalog/submitReview`：确定性 `_id=orderId__productId` 库级唯一（一单一品一评，根因#1 正确范例）+ 订单归属/已完成/在单内 + 昵称快照。
- `learning/genQrcodes`：withAdminGate + code 作 _id 唯一 + count 严格校验 + 防废码；crypto 用最小 `.d.ts` 声明（不引 @types/node）。

**验证**：`npm run check` 全绿（4 函数全测试套 25 断言通过；cloud TS typecheck 含 crypto.d.ts）；测试 **195**（4 测试重指向，安全套保留）；`build:cloud` **20 产物**（learning 8 / catalog 5 / orders 4 / system 1 / user 2）。反向自检：submitReview 确定性 _id 改非确定（带时间戳）→ REVIEWED 用例红 → 还原绿（根因#1 守护证）。净化：4 函数 init+闸样板收编；transition 坐实 ≥2 消费者。生产仓零改动。

**记债（根因#1 通例化未尽）**：users/progress 的「先查后建」（login/updateProfile/trackEvent，债#14）仍未改确定性 _id——涉及存量数据**惰性迁移**（命中确定 id 用之、未命中原子搬迁），单列迁移批做（kit ids 待该批 ≥2 消费者时提取）。本批已保留所有**现存**确定性/原子语义，未新引竞态。

## 2026-06-13 · B4b-1：账户/基建写入迁移（闸门波之一，根治账本 #3/#5）

B4b（闸门波 13 函数）太重（钱/权限，correctness 关键），再拆三小波。本批=账户/基建写入（最低风险一组）：迁 `user/login`（withOpenId upsert；无 openid 无法建身份→NO_OPENID，较原裸用空 openid 更正确）、`user/updateProfile`（withOpenId + 白名单 {nickname,bio,avatar} 截断 + avatar 仅 cloud:// 防注入）、`learning/trackEvent`（withOpenId + events 流水 + progress 折叠，addTo 集合自建保留）、`system/initDb`（withAdminGate + 建集合）。kit 新增 **`str`**（截断收口，消费者 updateProfile+trackEvent＝≥2 justified）。删 4 旧目录。

**验证**：`npm run check` 全绿（cloud TS typecheck 含 4 新函数 + kit str）；测试 187→**195**（+8=login 2 / updateProfile 3 / trackEvent 3，给账户写入与进度折叠加覆盖；initDb 测试重指向）；`build:cloud` **16 产物**（catalog 4 / learning 5 / orders 4 / system 1 / user 2）。反向自检：拆 updateProfile 的 avatar cloud:// 白名单 → 拒注入用例红 → 还原绿。净化：4 函数 init+闸样板收编进 kit。生产仓零改动。

**剩闸门波**（B4b-2 课程/评价一次性副作用：activateCourse/confirmEnter/submitReview/genQrcodes——需 kit ids 确定性_id + 一次性副作用绑首次流转；B4b-3 交易：createOrder/pay/confirmReceive/closeExpiredOrders/applyRefund——transition 重）；B4c refundCallback；B5 adminApi 拆分。

## 2026-06-13 · B4a：只读函数域分组迁移（T2 第一波，根治账本 #5）

B4（其余 ~21 函数迁入 kit + 域分组）分三波：**只读 → 闸门 → refundCallback**。本批=第一波只读（最低风险，无状态变更）。

迁入 packages/cloud（kit 收编 init+openid 闸样板）：catalog/`getContent`·`getReviews`（公开读，getReviews 含云端汇总逻辑原样保留）、learning/`getCourses`·`getMyCourses`·`getMyProgress`、orders/`getMyAfterSales`·`getOrderById`（后四个 openid 读用 withOpenId）。**删 `getOpenId`**（P0 连通性验证函数，miniapp 无调用＝死代码，回灌已确认连通，净化删除）。删 7 个旧 cloudfunctions 目录。

**验证**：`npm run check` 全绿（kit-only-cloud-primitives 守卫覆盖新迁函数）；测试 182→**187**（+5=getReviews 汇总 3 + getMyCourses 去重 2，新增覆盖；getOrderById 测试重指向新址）；`build:cloud` **12 产物，域均衡**（catalog/learning/orders 各 4）。反向自检：往新 read 注入裸 getWXContext → 守卫红 → 还原绿。净化：7 函数的 init 样板（×3 行）+ 4 处 openid 三行闸收编进 kit；getOpenId 死代码删除。生产仓零改动。

**剩（B4b 闸门波）**：createOrder/pay/confirmReceive/closeExpiredOrders/applyRefund（交易，transition+确定性id）/activateCourse/confirmEnter（一次性副作用绑首次流转）/submitReview（确定性id）/genQrcodes/initDb（withAdminGate）/updateProfile/trackEvent/login。**B4c**：refundCallback 收回调框架。adminApi 拆分=B5。

## 2026-06-13 · B3c：课程种子单一来源（T3，根治账本 #5 收口）

商品种子（B3b）的姊妹批，治课程种子。课程种子原为**三份复制**：miniapp `data/course.js`（含 buildSegments 派生 + 引 utils/format 的 mmss/parseDur）+ cloud `seedCourses`（CommonJS 整份 clone，连 mmss/parseDur 都自带一份），注释「⚠️ 要与 course.js 保持一致」。

归一：① `shared/src/seed/course.ts` 为 **canonical**——三层 `SEED_COURSES`，buildSegments 派生 + 私有 mmss/parseDur（种子模块自含，与 miniapp utils/format 的显示工具分属不同层）全收于此。② `data/course.js` 由 ~108 行降为 ~25 行**薄视图**——`COURSES=SEED_COURSES` + 派生 COURSE/ALL_LESSONS + 用户态样例 SAMPLE_PROGRESS（云端不存，留 miniapp）；不再 import utils/format、无 buildSegments/CHAPTERS 内联。③ `seedCourses` 迁 `packages/cloud/src/functions/learning/`（kit withAdminGate + import shared，esbuild 内联），删旧 `cloudfunctions/seedCourses`。withAdminGate 第 2 消费者（≥2 justified 坐实）。④ 守卫 `product-seed-single-source` 泛化为 **`seed-single-source`**，同时覆盖商品 + 课程（canonical 存在 + data 视图派生自 shared）。

**验证**：`npm run check` 全绿（结构仓级 4；cloud TS typecheck 含 seedCourses；course.test 三层契约用例对新 shared 派生数据全过——segment 时长之和=课时时长等）；测试 182；`build:mp-weixin`+`build:h5` DONE；`build:cloud` 5 产物（**域结构成形**：catalog/getProducts·seedProducts、learning/seedCourses、orders/getMyOrders·payCallback）。反向自检：data/course 去 shared 派生 → 守卫红 → 还原绿。净化：课程种子三份→1 处 canonical、data/course 由 108→25 行、镜像注释清零、mmss/parseDur 三处复制收敛。**至此 T3 种子侧（商品+课程）单一来源完成**；剩 initDb（无种子数据，纯建库，B4 随 withAdminGate 收）。生产仓零改动。

## 2026-06-13 · B3b：商品种子单一来源（T3，根治账本 #5）+ kit withAdminGate

T3 云唯一真相的种子侧。回灌探针已由用户 `tcb fn delete` 清除。

商品种子原为双份手工同步（miniapp `data/catalog.js` inline + cloud `seedProducts` inline，注释「⚠️ 要与 catalog.js 保持一致」）。归一：① `shared/src/seed/products.ts` 为 **canonical**（cloud 形态 superset：id/name/tag/price/was/featured/sort/courseId）。② `data/catalog.js` 降为**派生视图**——从 `@luckyducky/shared` 的 SEED_PRODUCTS 派生 CATALOG/getProduct/FEATURED_IDS（保同步身份，cart/detail 同步取数不变）。③ `seedProducts` 迁入 `packages/cloud/src/functions/catalog/`（TS，kit `withAdminGate` + import shared SEED_PRODUCTS，esbuild 内联），删旧 `cloudfunctions/seedProducts`——cloud 灌库与 miniapp 视图自此**同源**。④ kit 新增 **`withAdminGate`**（根因#3，审计 A-1：无 openid 放行/客户端须 isAdmin；消费者 seedProducts 现，seedCourses/initDb/genQrcodes B4 迁入＝≥2 justified）。

**守卫** check-structure repoCheck `product-seed-single-source`：canonical 须存在 + `data/catalog.js` 须派生自 @luckyducky/shared（防内联回潮）。**验证**：`npm run check` 全绿（结构仓级 3→4；cloud TS typecheck 含迁入的 seedProducts）；测试 182（seedGates 重指向新 seedProducts，行为不变）；`build:mp-weixin`+`build:h5` DONE（data/catalog 引 shared 两端构建吃下）；`verify:cloud` 4 产物（+seedProducts）本地真跑。反向自检：data/catalog 去 shared 派生 → 守卫红 → 还原绿。净化：商品种子双份→1 处 canonical、「保持一致」镜像注释清零、data/catalog 由数据降为薄视图。生产仓零改动。

**遗留（B3c/B4）**：课程种子（`data/course.js` 带 buildSegments 派生 + format 工具依赖，比商品种子复杂）+ cloud `seedCourses`/`initDb` 收口 shared，单列下批处理。

## 2026-06-13 · B3a：砍 api 层假数据回退（T1 第一刀，根治账本 #6）

回灌成功（getProductsKitProbe 真腾讯云跑通、176ms 返真实商品，B4 风险清零；探针待用户 `tcb fn delete` 清掉）后进 B3。只读扫描发现 `data/` 实为三类（① api 假数据回退源 / ② 规范常量 / ③ 刻意品牌样例与空态默认），**T1 只砍第①类**，故 B3 拆 B3a（本批）+ B3b（种子单源，下批）。

落地：① 删 api 层本地假数据——`api/order.js`（localCreateOrder/localOrderNo + createOrder/confirmReceive 的 `res===null` 回退分支 + 4 个 data/ import）/`api/shop.js`（localProducts）/`api/course.js`（COURSES 回退）/`api/activation.js`（演示模式「视为已激活」假成功 + getMyCourses 全解锁）。api 改为：交易类无云上抛、读类返回空、激活类返回 NETWORK 错误——绝不本地伪成功。`api/review.js`/`api/aftersales.js` 本就「云或 null」无本地生成，不动。② **守卫** check-structure `api-cloud-only`：`packages/miniapp/src/api/**` 禁 import `@/data/`——不引样例＝不可能再造假数据回退。③ **测试重构**：原 orders/activation/courses-store 测试靠本地回退当数据源（测试无 wx.cloud），改为 mock callCloud 提供云响应测 store 编排；删纯镜像 describe（orders「本地回退」3 + 「SKU 镜像」1 + activation「演示模式回退」2）——这些定价/快照/契约的真实校验已在 `tests/cloud/createOrder.test.js`。

**验证**：`npm run check` 全绿（结构含新 api-cloud-only，逐文件规则 1→2）；测试 188→**182**（−6 = 删镜像测试，净化指标）；`build:mp-weixin`+`build:h5` 均 DONE（H5 预览数据走空态不崩）。反向自检：api 文件注入 `@/data/` import → 守卫红 → 还原绿。**MP 真机行为零变化**（MP 上 callCloud 恒命中云、`res===null` 分支从不触发，删的是 H5/App 空想路径）。净化：src 约 −90 行 + 6 镜像测试；根治账本 #6（演示回退双路径）整类蒸发（parity 镜像测试自此不需要存在）。生产仓零改动。

## 2026-06-13 · B2：cloud-kit 内核 + shared 类型 + 域分组骨架 + 2 试点迁移（守卫先行）

T2/T3/根因 1/2/3/4/5 的立柱。**意图先行**：先写 kit 反向测试与守卫，再迁函数到绿。

落地：① **shared 类型**（packages/shared/src）——`money.ts`（`Fen` 金额品牌类型，浮点金额类型层绝迹，根因#4）+ `order.ts`（`OrderStatus` 联合类型，写错状态名编译失败，根因#2）+ `errors.ts`（错误码单源，根因#3）。② **packages/cloud（TS）+ cloud-kit**：`getDb`（收编 cloud.init 样板）/`ok·err`/`withOpenId`（收编 18 处 openid 三行闸，fail-closed）/`isServerCall`（回调防伪）/`transition`（条件原子状态流转 where+in，天然幂等，根因#2）/`defineNotifyCallback`（回调框架，收编 pay/refund 70% 重复，根因#3/#5）；其余 kit 件（withAdminGate/defineHttpApi/callPayFlow/ids/paging/validate）按宪章原则2「≥2 消费者才建」留 B4/B5。③ **2 试点迁移**：`orders/getMyOrders.ts`（openid 闸样板）+ `orders/payCallback.ts`（回调框架样板，v3/v2 兼容+幂等+金额留痕全保留）迁入 packages/cloud，**删旧 cloudfunctions/{getMyOrders,payCallback}**。④ **守卫**：check-structure 加 fileRule `kit-only-cloud-primitives`（packages/cloud/src/functions 下禁裸 cloud.init/getWXContext）；typecheck 扩到 cloud 包（tsc -p）。

**验证**：`npm run check` 全绿（conventions 61 + structure 仓级3+逐文件1 + cloud TS typecheck + lint + 测试 **170→187**，+17=kit 反向 15 + getMyOrders 试点 2）。**反向自检**：注入裸 getWXContext → 守卫红；去掉 withOpenId 的 fail-closed → gate 测试红；均还原绿。净化：2 函数的 init/闸/回调样板收编进 kit（B4 给其余 26 个复用）；旧函数目录净删。生产仓零改动。

**B2-follow（同日完成，就差用户点部署）**：① esbuild `build.mjs`（functions/<域>/<name>.ts → dist/<name>/{index.js,package.json}，kit/shared 内联+摇树，external wx-server-sdk）；`build:cloud`/`verify:cloud` 脚本 + esbuild 入 devDeps。② getProducts 迁入 catalog 域（kit.getDb 试点）作回灌探针，删旧 cloudfunctions/getProducts，测试 187→188。③ **本地验证** `scripts/verify-cloud-bundles.cjs`：把 3 个 esbuild 产物当真云函数 require 进来跑（内存桩喂数据），getProducts 升序 / getMyOrders fail-closed / payCallback 流转+防伪闸全保真——证明「TS→kit/shared 内联→esbuild CJS→真能加载跑对」，回灌前风险只剩 tcb 运行时一项。④ **回灌探针就绪**：用户一条命令部署 `getProductsKitProbe`（非敏感、不覆盖线上 getProducts）到生产验 tcb 兼容（命令见下）。cloudbaserc 仍列已删函数，B4 functionRoot 改指 dist 时reconcile（沙盒不部署无影响）。

## 2026-06-13 · B1：自动验证骨架——四主张一起扶正 + 自动化优先（守卫先行）

用户拍板：上帝视角四条架构主张（T1 砍多端 / T2 域分组函数 / T3 云唯一真相 / T4 按链内聚）**一起扶正，且用程序自动化方式扶正**——整个重构骑在一个可靠的自动验证+自动 hook 系统上。先建系统本身（B1），再做改造（B2+）。

落地：① 新 `scripts/check-structure.mjs`——结构不变量引擎（repoChecks 仓级 + fileRules 逐文件 + `--hook` 即时反馈），与 check-conventions 分工（它管单文件样式，structure 管跨文件/仓级架构不变量）；B1 种 3 条仓级不变量（stub-only-sdk 测试地基 / gate-single-source 三闸不漂移 / deploy-deny-all 禁部署）。② **三道闸单一定义**：pre-commit 与 CI 都改为只调 `npm run check`（=conventions+structure+typecheck+lint+test），由 gate-single-source 不变量自守（闸守着闸）。③ 编辑 hook 加 check-structure --hook。④ 设计与契约写进 `自动化验证系统.md`（五层验证 + 每主张→不变量→守卫映射），总计划升 v5（宪章加第四原则「自动化—流程即守卫」、四主张入册、批次表重排 B2–B9 每批带守卫列、§17 红线由 T4 软化）。

**反向自检（证明守卫会咬）**：篡改 CI 去掉 `npm run check` → check-structure 红（exit 1）→ 还原 → 绿。`npm run check` 全链全绿（conventions 61 + structure 3 + typecheck + lint + test 170）。纯新增基建，零行为改动，生产仓零改动。

## 2026-06-13 · 工作流技能：移植 systematic-debugging + writing-skills（按本项目约定，非整包）

用户问 superpowers/gstack 两个 skill 包——结论是别整包（与本项目守卫系统/CLAUDE.md 重叠且吃 context），**摘思路按本项目约定重写**。落地两个项目级 skill 于 `.claude/skills/`：① `systematic-debugging`（复现→根因→修复带守卫→记账，融入 logger/调试日志格式/根因账本十二类/反向自检/工具僵态守则）；② `writing-skills`（写 skill 的元技能，含「移植外部包要摘思路不整包」的教训）。放沙盒仓随重构版本管理，B9 切换时随主线进生产；生产仓零改动不破。

## 2026-06-13 · 上帝视角产品思路重审（独立于工程重构，海拔更高）

用户要求「从上帝视角扫描整个项目、搞清意图/需求/目标与每个逻辑来龙去脉、用可颠覆视角以根本意图为基础完全重构项目思路」。通读生产仓四份依据文档（设计规格/关键决策记录/业务逻辑架构/项目现状）后产出 `项目思路重构-上帝视角.md`。**核心发现**：根本意图=让钩织新手真把作品做完；验证过的真实痛点是计数/状态/记忆，团队自己写过「视频只解决看不懂的一半」并把「计数陪伴承重柱」停在探索区；而现行十条业务链 8.5/10 是电商+内容 DRM，**碰核心痛点的=0 条**——差异化押在了最浅处。**重构思路**：换重心不砍电商——电商脊柱原样承重（两种思路通用，工程重构 B1–B8 加固的正是它），学习层从「看视频」反转为「做东西被陪着」（progress→makingState、course→项目/行针数图谱、播放器→制作陪伴台，团队已为此留过 segment 粒度的位）。与工程重构**互补不冲突**（一个硬化地基、一个在地基上盖新承重柱）。**未改任何代码/计划/上线**；文末给所有者三选项（A 维持/B 扶正/C 先验证），建议先 C 后 B。纯文档。

## 2026-06-13 · 意图批：根因账本 + 总计划 v4 入仓——重构逻辑定调「根因驱动」

用户再沟通定调：吸取原项目**所有**经验、追溯本质问题、系统性根治；拍板①账本全覆盖（代码+运维+验收方法论+文档体系）②补强切分（分页入 B4、商品下架留生产线、埋点频控记债）。交付：`根因账本.md`（**十二类病根**五栏账：病史证据→本质→根治→落点批次→绝迹证明；+已根治留档+院外记账+完整性自查——调试日志 A–K/技术债 #1–#15/四份审核报告逐条归属无遗漏）+ `重构总计划.md`（v4：批次表挂病根编号；新增 B3.5 回退收口批；B1/B2/B4/B6/B7 按账本增补；A-2 翻案论证明写；权威版自此入仓受版本管理）。纯文档批，代码零改动。

## 2026-06-13 · Phase A：平行仓建立（四道栅栏）

克隆点 `2dd5a72`（v0.9 + 外部审核批次 A/B）。四道栅栏：① 远程隔离 ✅——origin 摘除生产仓指向，用户亲手建 GitHub 私库 `ookiisparrow/luckyducky-next` 后挂为唯一 origin 并推送（同日收口）；② 部署硬闸——`guard-deploy.mjs` 改为一切 tcb 部署/发布/删除 deny（八形态管道自测：5 deny / 3 放行）；③ CLAUDE.md/README 身份声明；④ 克隆体全绿（conventions 61 文件 / lint / test 170 / 双端 build）。

## 2026-06-13 · B0：workspaces 骨架 + miniapp/admin 整体平移 —— **布局成立，无需兜底**

**结论：总计划最高风险项（uni-app 进 npm workspaces）证实可行**，B1 起按原布局推进。

新布局：仓根 = workspaces 编排（`luckyducky-next`，scripts 全部 `-w` 转发）+ `packages/shared`（TS 占位，tsc 产 dist+d.ts）+ `packages/miniapp`（uni-app 整体平移，含其 16 个逻辑测试文件与 setup）+ `packages/admin`（平移，删独立 lock）。`cloudfunctions/` 与 `tests/cloud/`（闸门测试 + wx-server-sdk 内存桩）暂留仓根，B1 迁 `packages/cloud`。

**判定项证据（全过）**：
1. miniapp 子包 `build:h5` / `build:mp-weixin` 均 DONE；`dev:h5` 起服 499ms、探活 200；mp-weixin 产物结构与平移前一致（含 cloudfunctions 拷贝，vite 插件路径已改上溯两级）。
2. vitest projects（miniapp + cloud 两项目）接管 **170 例全绿**；云函数 `require('wx-server-sdk')` 经根 node_modules 命中 file: 内存桩（关键解析链验证 ✓）。
3. shared 哨兵常量（`SHARED_PKG_SENTINEL`，App.vue 引用）出现在 H5 assets 与 mp-weixin vendor.js——workspace TS 包被 uni 构建吃下 ✓。
4. admin 在 workspace 下 `vite build` ✓；`npm ls vue vite`：全仓 vue 3.5.35 / vite 5.2.8 各一份全 deduped（版本钉死目标达成）。

**路径接缝改动**：check-conventions SRC 常量、eslint ignores/no-console 路径、vite copyCloudfunctions 上溯、.prettierignore、运行说明.md（dist 路径 → packages/miniapp/dist）、visual-check.cjs projectPath。CI 工作流无需改（npm ci + 同名 scripts）。

**净化指标**：admin 独立 `package-lock.json` 删除（双 lock 归一）；vue/vite 全仓单副本；根 `check` 一条命令收口（conventions+typecheck+lint+test）。

**遗留**：微信开发者工具人工打开 `packages/miniapp/dist/build/mp-weixin` 的眼校留给用户（自动化判定已过）；admin 的 eslint/vitest 基线 = B7。

## 2026-06-13 · 深审整改：用户域首写确定性 _id 收口（根因#1 补全）+ 账本两处措辞收准

`/deep-audit` 全仓业务代码深审（交易链/安全闸/状态机/根因守卫/接口/依赖逐层**去代码核、非信声称**）。**结论：钱/权限/状态三条命脉经得起核**——10 条结构守卫逐条真咬、测试咬反向情形（防伪/金额不符/幂等 no-op/累计上限）。挑出 1 个真问题 + 一批账本措辞比代码满。

**真问题（P1，本批已修）**：用户域首写（login / updateProfile / trackEvent 进度）仍是「先查后 add（随机 _id）」——并发首写可重复建档（债#14 原形）。账本完整性表早把 `#14→病根1` 记作已根治，但 #1 的「确定性 _id 通例化」只接到了钱/学习域（applyRefund `orderId__productId`、activateCourse `_id=code`、confirmEnter 抢占），**用户域漏接**；`kit/ids` 助手一直没建。`userWrites.test` 只测顺序没测并发，绿灯掩盖了缺口。

**修法（先守卫红 → 改到绿 → 反向自检）**：① 先加 3 条并发反向测试（`Promise.all` 双调用 → `toHaveLength(1)`），随机 _id 下**红**；② 建 `kit/ids.ts` 的 `ensureDoc(coll,id,data)`——确定性 _id 幂等建档（撞号=并发已建→读回；**fail-safe**：以读回为准、真没落则抛不返回假成功；集合未建则建一次重试，对齐 addTo），收口三处首写（老随机 _id 档 where 命中即用，惰性迁移、无需搬数据）；③ `npm run check` 全绿 **237 例（+3）**；④ 反向自检：篡改 ensureDoc 去掉确定性 _id → **5 红**（3 新 + 2 既有幂等）→ 还原 **12 绿**，证守卫真咬。

**账本收准（非改码，把过满措辞收回真相）**：#2「编译期拒绝非法状态名」→ 限调用方用 `ORDER_STATUS` 常量、`transition()` 边界收 raw string 不挡 typo（跨集合泛型已知折中）；#3「金额不符不翻状态」→ refundCallback 严格不翻，**payCallback 例外**（钱已到账→置 paid 留 feeMismatch 痕，刻意且有测试）。

**净化指标**：确定性 _id 通例化覆盖「钱/学习域」→「+用户域全 3 处首写」；`kit/ids` 落地（≥3 消费者，符宪章原则2）；并发反向守卫 +3；账本两处过满措辞归真。**生产仓零改动**。

**够不到/延后（如实标）**：manifest urlCheck=false（上线前恢复，本仓禁部署够不到）；adminApi v1 CORS*/单口令/unsalted-sha（账本院外已记）；#6 parity 测试未见专门文件（B3.5 落点、疑未执行批次）；npm audit 51 项全在 uni 构建链（运行时暴露近零）；库权限/私有读/真支付/真机 = 验收单 X/Y/Z 控制台+真机核。
