# 重构日志（样板房专属）

> 本文件记重构线批次结论与证据（Phase A / B0–B9 = 第一次重构；下新增「## 第二次重构（演进）」段续记）；生产线工作日志已归 `archive/工作日志-生产线-至20260612.md`（冻结于 0612）。三支柱：`根因账本.md`（病根）+ `现状与路线.md`（live 地图；第一次重构总计划 v5 见 `archive/`）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`archive/项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-06-15 · 🏷️ v0.9.2 里程碑（代码里程碑·新会话先读这段）

**v0.9.2 = 代码里程碑**（tag 在 main `d182075`），收束 v0.9.1 tag 以来三条线；**不是新部署**（部署状态见末段）。
- **① v0.9.1 验收收尾**：验收单 X/Y/Z 真机真钱过——X 库权限客户端实测抓真 P0（13 集合曾可写 + `courses` 公开读漏 `videoFileId`）→ 16 集合锁仅管理端；本仓接管 tcb 部署闸（deny-all → 生产仓模型，敏感函数 `ask`）。
- **② 产品打磨**（前端 miniapp·用户可见）：微信登录半屏弹窗 + 启动加载页 + 登录流程完善（退出/一键登录/默认昵称 `Lucky friend`+随机鸭头像）；首页/购物车 UI 批（品牌 logo/卡片等高/购买滚动居中/顶栏吸顶/结算条对齐）；「我」页未激活课程空态；**返回重构**（Tab 页「再按一次退出」= scroll-view + page-container）；新 skill `/frontend-check`。
- **③ 治理脊柱**（第二次重构·演进；纯文档/守卫/工具，**不碰运行时**）：P0 知识地基（立 `演进地图`）→ 文档精简+架构校准（活文档瘦 ~40%）→ P1 需求显形（`需求清单` 31 条单源 + 逐条复核，修 R22 跑偏）→ 文档架构重设（19→13 份，客观/主观分治 + 反膨胀守卫 `docs-budget`）→ **P2 需求→守卫闭环**（`requirement-trace` 守卫 + `npm run trace` 爆炸半径）→ **P2b 须守卫覆盖**（钱/状态/安全类需求须有对应类守卫）。

**现状**：测试 **278** 全过；守卫 **15 repoCheck + 5 fileRule**；活文档 **13**；skills **7**；CLAUDE 144/180。生产仓 `luckyducky-miniprogram` 零改动（HEAD `2dd5a72`，回滚基线）。
**部署状态（honest，勿读成"已全上线"）**：后端云函数仍是 v0.9.1 部署的 **28 个**（治理 work 不碰运行时、无需重部署，后端已是生产随时服务）；前端登录/UI/返回 等产品改动**已合 main、未发布生产**——发布属「全客户上线」动作（`build:mp-weixin` + `urlCheck`→true + 微信后台合法域名 + 审核），仍待。
**容量评估（2026-06-15 看码评估·非压测）**：4 位数/天 架构可承——并发正确性稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）。上量前必清 4 件（详 `待办与债` §上量就绪）：① 数据库套餐档位（控制台查·最大未知）② 视频 CDN/VOD（R21 搁置·裸云存储吐视频带宽=头号成本/风险）③ 看板内存聚合 1000 条/表封顶（4 位数当天 GMV 即少算）④ 索引 + **从未压测**（根因#8 同理：单人能用 ≠ 千人同时）。

## 2026-06-18 · 🔒 付费视频白嫖防护结构守卫 video-url-via-cloud-only（优化批0618·T-B1 核查收口 + T-B2 复核·派单箱）

**来源**：parallel-c 派单箱 `queue/main.ndjson`「优化批0618」后端批（T-B1 视频存储后端核查收口 / T-B2 加载优化后端半）。
- **T-B1 核查（链路全对）**：付费课视频是受保护资产——`getPlaybackUrl` 是**唯一**鉴权后经 `getTempUrl` 换短时效临时 URL 的云函数（免费段放行 / 付费段须 `activations.enteredAt` 已确认进课·否则 `NOT_ENTITLED` 挡白嫖 / 素材未剪 url:null）；`getCourses` 公开目录把 `videoFileId` 剥成 `hasVideo` 布尔（连 `cloud://` 都不漏）；`getMyCourses`/`getMyProgress` 不返课内容。行为面已被 `getPlaybackUrl.test.js` 锁——**但结构无闸**：新加 learning 函数可能把 fileID 漏进公开返回 / 绕鉴权自造 URL。
- **守卫（codify 现有正确态·非「红→绿」，靠反向自检证齿）**：`video-url-via-cloud-only`（repoCheck·roots `#3` 信任边界）扫 `learning/`：① `getTempUrl` 只许 `getPlaybackUrl.ts` 调（播放 URL 单源经鉴权云函数）；② `videoFileId` 只许 `getPlaybackUrl.ts`(定位)/`getCourses.ts`(剥 hasVideo) 出现（小白名单·均已审计），其他 learning 函数引用即红；③ `getPlaybackUrl.ts` 须既经 `getTempUrl` 又有 `NOT_ENTITLED` 权属闸，缺任一即白嫖洞。
- **反向自检（三面齐红→还原绿）**：getCourses 加 `getTempUrl('x')` 调→红；getMyCourses 引 `videoFileId`→红；删 getPlaybackUrl 的 `NOT_ENTITLED`→红；还原均绿。
- **T-B2 复核（无需改码）**：看板/列表热读路径已有界——`dashboard.ts` 计数 `.count()`/GMV `aggregate`（精确不封顶）、最近单 `limit(5)`、仅 热度/卡点 `limit(SAMPLE)` 显式标「近 N 估算」；`batches.ts` `skip/limit` 分页取尽。`capacity-reads-bounded` 覆盖 dashboard/batches 充分。**索引核查＝控制台靠人**（#19 残·代码看不到）。**watch（备查·非本批）**：`admin/adminApi/actions/products.ts` 列表 `limit(100)`（SKU 破百静默截断·仅管理端·低危·「核查收口非重建」不动）。
- **净化指标**：守卫 **37→38 repoCheck**（+1 结构闸·强度+1）；测试 **391**（新守卫 +1 provenance；顺手校正 `现状与路线.md` stale 计数 37→38 repoCheck、378→391 与真值对齐）；`npm run check` 全绿（66 文件 / 391 测试）。**本批 commit 落本地 main，不 push origin**——优化批0618 协调模式：parallel-c 为 git owner、审核后 cherry-pick 整合上 origin/main，整合后本树 `git reset --hard origin/main` 同步。

## 2026-06-18 · 🦴 优化批0618·T-F2：可复用骨架屏 Skeleton + 数据页冷启占位（parallel-b·根因#8）

**痛**：数据页冷启/弱网空白或纯文字「加载中」（order-list 旧为「订单加载中…」文字、catalog 冷启空章节区）——白屏感、真机才暴露（根因#8）。
- **改（先红后绿）**：新 `components/Skeleton.vue`（纯 CSS shimmer·动 background-position·mp 安全·占位色走 token $bg-grey/$bg-faint）；`order-list` 列表未到时占 3 张订单卡骨架、`catalog` 冷启占 3 行章节骨架（皆绑真 `store.loading`，刷新已有列表不闪骨架）。新守卫 `list-pages-skeleton`（册=catalog/order-list 须挂 `<Skeleton>`），反向自检：sed 删 order-list `<Skeleton>`→红→还原绿。
- **honest 边界（根因#8·不做 theater）**：首页走 LoadingSplash 全屏开屏、detail 样例数据即时渲染——各有其载态，**不强塞骨架**（册内只收真有空载态的页）；card 列的「首页/detail」据此剔除，已在守卫 desc/注释写明。
- **净化指标**：守卫 38→**39 repoCheck**（parallel-c 同批整合 T-B1/B2 + T-F2·累计 39 与 `现状与路线` 对齐）；测试 **391** 全过；`npm run check` 全绿、`build:h5` DONE。**剩靠人（根因#8）**：真机验 shimmer 流畅度 + 弱网下骨架→数据平滑替换（card accept 真机项）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 🔄 优化批0618·T-F3：数据页下拉刷新（parallel-b·根因#8 双机制）

**痛**：数据页无下拉刷新——后台流转（订单付款/发货、退款状态、新评价）后用户只能退页重进才看到最新。
- **关键多端坑（根因#8·分两机制非一刀切）**：`onPullDownRefresh` 只由**页面级滚动**触发；内容装在整页 `<scroll-view>` 里滚动的页（index/me），页面本身不滚→页面级 `enablePullDownRefresh` 在 mp-weixin **根本不触发**（H5 还假绿）。故：
  - **页面级滚动数据页**（catalog/order-list/reviews/aftersales/order）→ `pages.json enablePullDownRefresh` + `onPullDownRefresh` 强刷 store + `uni.stopPullDownRefresh()` 收尾；
  - **整页 scroll-view 数据页**（index/me）→ scroll-view 自带 `refresher-enabled` + `:refresher-triggered` + `@refresherrefresh`，handler `finally` 收 triggered。
  - 七页刷新均 `try/finally` 收圈——**失败也收，不卡转圈**（accept 项）。
- **改（先红后绿）**：新守卫 `pull-refresh-stops`（roots `#8`）钉三件：① 列举的页面级数据页必开 `enablePullDownRefresh`；② 凡开 `enablePullDownRefresh` 的页必有 `onPullDownRefresh`+`stopPullDownRefresh`（防转圈不停）；③ 凡用 `refresher-enabled` 的页必绑 `@refresherrefresh`+`:refresher-triggered`。先红（7 页全缺）→改到绿。**反向自检三面**：删 order-list `stopPullDownRefresh`→红 / 删 catalog `enablePullDownRefresh`→红 / 删 index `@refresherrefresh`→红，均还原绿。
- **honest 边界（根因#8·不做 theater）**：**cart 不入册**——整页 scroll-view 但纯本地态（购物车条目本地持久化 + `CART_RECS` 静态推荐），无远端数据可刷，加下拉刷新＝假绿；静态页（about/agreement/welcome）无数据，不开。守卫 desc/注释写明。
- **净化指标**：守卫 39→**40 repoCheck**（+1·`现状与路线` 计数同步 39→40）；测试 **393** 全过；`npm run check` 全绿、`build:h5` DONE。**剩靠人（根因#8）**：真机验下拉触发+转圈正常收+失败反馈（accept 真机项）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 👉 优化批0618·T-F4：左右滑动（图廊 swiper + 订单 tab 横滑·parallel-b·根因#8）

**痛**：商品图廊只能点缩略图换图（不能左右滑）；订单 tab 只能点、不能横滑切换。
- **守卫先行（红→绿）**：新 `swipe-native-swiper`（roots `#8`）钉「左右滑面（DetailGallery + order-list）须用原生 `<swiper>`+`<swiper-item>`」——守卫指令「不自造 touchmove」：mp 横滑 vs 纵滚的手势消歧靠原生 swiper 才稳（自造 touchmove 真机易误触·H5 假绿）。先红（俩都没 swiper）→改到绿。
- **(a) 详情图廊 DetailGallery**：主图换 `<swiper :current="sel" @change>` 包 N 张 MediaSlot，左右滑切图；与缩略图高亮、计数「sel+1/total」**双向同步**（滑 swiper 写 sel、点缩略图改 sel→swiper 跟切）。swiper 定高 `100vw`（方图与原 ratio 1/1 等高）。
- **(b) 订单列表 order-list（实质重构）**：顶部 tab 下接 `<swiper :current="tabIndex" @change="onSwipe">` 包 5 个 tab，每 tab 一个竖滚 `<scroll-view>`；**tab↔swiper 双向联动**（点 tab `selectTab` 切 swiper·滑 swiper `onSwipe` 回写高亮）。列表移入 scroll-view 后页面级滚动没了 → **触底加载** onReachBottom→`@scrolltolower`、**下拉刷新** 页面级→scroll-view `refresher`（复用 T-F3 机制·共享 `refreshing`）。布局 `.coorl-page` flex 列定高 100vh、`.coorl-swiper` flex:1 占满。同步改 `pull-refresh-stops` 守卫：order-list 从「页面级册」迁「refresher 册」、pages.json 撤 `enablePullDownRefresh`。
- **honest 边界（根因#8·不做 theater）**：**catalog 不做**——派单写「catalog(分类)用 swiper 包 tab」，但 catalog 实为**单课程 + 竖向章节折叠（手风琴）**、无横向 tab、无可左右滑的面，强加 swiper＝造不存在的 tab。守卫 desc/注释写明不入册。
- **净化指标**：守卫 40→**41 repoCheck**（+1 swipe-native-swiper·`现状与路线` 同步 40→41）；测试 **394** 全过；`npm run check` 全绿、`build:h5` DONE。**反向自检**：删 DetailGallery `<swiper`→红 / 删 order-list `<swiper`→红 / 删 order-list `refresher-enabled`→红，均还原绿。**剩靠人（根因#8·accept 真机项）**：真机验图廊能滑+指示准、order-list 横滑联动高亮、**横滑不误触纵滚**、swiper `flex:1/height:0` 在 mp 高度真撑开（H5 已 DONE·mp 待真机）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 🎧 微信客服小程序卡片降级文字（thumb 缺则不报 40058·配 WXKF_SERVICER/MINIAPP_APPID）

**痛**：配「转人工 + 小程序卡片」3 个 env 时发现——小程序卡片必带封面素材 `thumb_media_id`，而企业微信素材 id 只活 3 天（固定 env 不可行）、仓里 logo 又是 SVG（素材要栅格图），卡片真做＝需「自动上传栅格 logo+缓存续期」一整套；且卡片只在小程序已发布后才跳得动。**决策**：本批不做完整 thumb 自动管理（过度工程·留作后续），先让卡片路由在「appid/thumb 未配齐」时**降级发引导文字**，杜绝 thumb 空→`send_msg 40058`、且让「申请售后/看课程」两个菜单项当下即可用（功能不缺、只是文字非卡片）。
- **改（先红后绿）**：`dispatch.ts` Route.miniprogram 加 `fallbackText`；`route()` 两处补文案；`handleMessage` case miniprogram 改「`cfg.appid && cfg.thumbMediaId` 才发卡片、否则发 fallbackText」。tests/ 加「缺 thumbMediaId→降级文字」用例，反向自检：强制永远发卡片→该测试红→还原绿。
- **配套（用户·env）**：`WXKF_SERVICER=ZhaoTian`（转人工接待人员·真机验·servicer/list API 被 48002 挡无法预检）、`WXKF_MINIAPP_APPID=wxcbd2fb68b81bcfb1`（已加）。
- **净化指标**：测试 384→**385**；守卫数不变；`npm run check` 全绿、`build:cloud` 31 过、部署 kfCallback。**剩（后续·真机已发布小程序后值得做）**：栅格 thumb 自动上传+缓存→真小程序卡片（需方形 logo PNG/JPG，仓里现仅 SVG）。
- **续（同日·转人工 48002 降级）**：真机点转人工**无反应**——日志 `TRANSFER_FAILED errcode:48002`（api forbidden）。查微信诊断（devtool/query?e=48002 第4条）+ 实测定性：会话分配/接待人员接口（`service_state/trans`/`servicer/list`）属**「客户联系」权限域**，企业内部接入密钥无权（收发消息属另一档·正常）。路 A（自建应用+客户联系权限+可调用应用列表+固定出口IP）过重、小店不值当。**路 B 已上**：`handleMessage` transfer 分支**先发「已转接人工，请稍候」文字兜底**再尽力转接——顾客有反馈、接待人员接待台手动接入、权限通了自动生效无需改码。测试更新（human→发 1 条文字+仍 transfer·反向自检删兜底→红→还原）。记入 `微信客服配置手册.md` §6/§8。

## 2026-06-17 · 🎧 微信客服智能客服真机上线 + 修真机暴露 2 bug（kfCallback·根因#8）

**真机全链路打通（用户真机·靠人前置全配齐）**：企业内部接入（kf.weixin.qq.com 开发配置·非自建应用接收消息——客服消息走前者）+ 回调 URL `https://kf.luckyducky.cn/kfcallback`（自有域名备案同主体）+ Token/AESKey 用**无歧义字符**重发（曾踩大写 I/小写 l 抄串致 echostr 验不过）+ **`WXKF_SECRET` 改用微信客服开发配置的 Secret**（曾误填自建应用 Secret → sync_msg 95012 无权限；实测自建应用 secret 调 kf API 60020/95012、微信客服 secret 正常返客服账号）+ 建 `kfState`/`kfIdentity` 集合（缺集合 → firstSeen 的 add 失败→每条被当已读 dup-skip 跳过不回）。真机验证：发「物流/退款」→ 关键词识别 → msgmenu 高亮气泡，去重正常（一条一回）。诊断手段＝构造真 echostr 验证请求 + 实测两 secret 调 kf/account/list（根因#8：真机/真请求形状才证得了「过≠能用」）。
- **bug① 进会话欢迎 send_msg 40058**（先红后绿）：event 消息 `external_userid` 在 **event 子对象**、代码取顶层取空 touser → 40058。抽纯函数 `extUserId(msg)`（dispatch.ts·两 wire 形状都取得到），normalize + enter_session 两处都经它；tests/ 加 wire 形状测（text 顶层 / event 子对象 / 双缺空串），反向自检：退回只取顶层 → event 测试红 → 还原绿。
- **bug② 游标不推进**（观测先行）：sync 每次从 `cursor=''` 重拉历史（dedup 兜住正确性、但 seen 痕与重拉无界增长）。dedup 写入证明持久化正常、读法符合本仓 `doc().get().data` 对象约定，故疑 `next_cursor` 未回——加 `[kf] cursor-read{has}` + `[kf] sync{nc:!!next_cursor}` 观测，待下条真机消息定根因再修（根因#8：不猜、instrument）。
- **净化指标**：测试 380→383（+3 wire 形状）；守卫数不变；`npm run check` 全绿、`build:cloud` 31 函数过。部署 kfCallback（敏感函数·guard-deploy ask）。**剩**：游标根因待真机日志定夺；进会话欢迎对「全新进会话无 48h 窗口」是否需 welcome_code+send_msg_on_event 待真机观察。

## 2026-06-16 · 🎧 微信客服 in-chat 智能客服后端（CS 域·R18 升级·交接规格落地）

**痛/需求**：客服已接通微信客服（`openCustomerServiceChat`·R18），但只是「点客服→人工」。要升级成 **in-chat 统一场景**：点客服→对话框→**关键词识别 + 高亮 `msgmenu` 气泡分流**→同一对话转人工。避免幻觉第一＝确定性 msgmenu + 查实时数据，非生成式 AI。规格＝`docs/微信客服智能客服-后端交接.md`（落地后卷档 archive）。
- **接缝单源**（病根#12）：`kit/wecom.ts` 收口企业微信「微信客服」平台接缝——验签(msg_signature)+AES 解密(WXBizMsgCrypt) + access_token DB 缓存(7200s·类比 throttle) + sync_msg/send_msg/idconvert/转人工；出网用 node18 全局 fetch（fetchImpl 可注入打桩）。
- **fail-closed by construction**（病根#3）：`defineKfCallback`（类比 `defineNotifyCallback`）强制「验签→验不过不进 onEvent + 伪造 `[LD_ALERT]` 告警」；任何客服回调经此外壳才写得出业务。
- **函数**（CS 域·T2）：`cs/kfCallback`（HTTP 访问服务·GET 验 echostr / POST 拉消息 cursor+msgid 去重 → 关键词识别发 msgmenu / miniprogram 卡片 / 文字 / 转人工）+ `cs/kfBind`（`withOpenId`+`withRateLimit`·unionid→external_userid→建 `kfIdentity` 映射·身份桥接查「你的订单」）。
- **守卫**（先红后绿）：`writes-need-gate` GATE 正则纳入 `defineKfCallback`（反向自检：摘掉即 kfCallback 写库变红→还原绿，证闸承重）；`interface-catalog-sync` 登记两函数 + webhook；`known-collections-only`/`collection-count-synced` 加 `kfState`/`kfIdentity`（18→20·含库权限表）；`known-error-codes` 登记 4 个身份桥接码；新敏感函数进 `guard-deploy` SENSITIVE_FNS + `cloudbaserc.json`。验签 + 解密 + 分流 + 身份 + kfBind 全单测（含 fail-closed 反向自检）。
- **净化指标**：守卫 35 repoCheck + 5 fileRule（强度+1：GATE 纳新闸·非新增条目）；云函数 29→31、集合 18→20、测试 351→378（+27）；`npm run check` 全绿。**靠人前置**（代码外）：企业微信认证 + 小程序绑开放平台 + 微信客服后台填回调 URL/Token/AESKey + 绑 HTTP 路径 + 配 `WXKF_*` env + 控制台锁 kfState/kfIdentity 权限 + 真机验（根因#8：msgmenu wire 形状/echostr/转人工以真机为准）。

## 2026-06-16 · 🔢 守卫计数机器同步守卫 guard-count-synced（巡检 #009 ④/💡·规则⑥·loop 第2轮）

**痛**：桌面只读巡检 #009 标头号漂移——`现状与路线.md` 守卫数仍写「31 repoCheck」（真值 35）、测试数行3「337」vs 行19「326」自相矛盾。根因＝客观计数（守卫数/测试数）被手抄进活文档、加守卫天天涨没同步 → stale（同 `collection-count-synced` 治集合数漂移的同型病·病根#11·规则⑥「客观计数只机器维护、别处不手抄」）。巡检 💡 明确建议「把守卫计数也机器同步」。
- **守卫**（先红后绿）：`check-structure` 加 `guard-count-synced`（roots #11）——repoChecks/fileRules 数组长度＝守卫数真值（含本守卫自己），核 `现状与路线.md`「N repoCheck / M fileRule」报同一数；测试数无静态真值源（vitest 才报准、跑全套太重不进静态闸）故只校验文档内多处「测试 N」互相自洽（封死 337 vs 326 自相矛盾）。无魔法数字（真值从数组动态读）。
- **改到绿**：`现状与路线.md` 行3/行19 守卫数「31」→「35 repoCheck + 5 fileRule」、测试数「337」「326」→「351」（真值＝加本守卫后 vitest 报 351·因 `guard-provenance` it.each 遍历守卫数组、每守卫 +1 provenance 测试）。
- **反向自检**：① 篡守卫数 35→33 → 守卫红「31 repoCheck≠真值35」类 → 还原绿；② 篡测试数造矛盾 351 vs 340 → 守卫红「测试数多处不一致」→ 还原绿。加守卫前现状即红（31≠35、326 vs 337）＝先红证据。
- **净化指标**：守卫 **34→35 repoCheck**；`npm run check` 全绿、测试 **350→351**（+1＝新守卫的 provenance 测试）。低风险→main 直提。**自主 loop 第 2 轮**（main/后端 lane·并行 CC 在 parallel-b/c·零竞争）。**巡检 #009 ④ 关账**：文档计数漂移点从「靠人巡查」升「机器守卫」，可像 ①④ 退役交守卫（💡 落地）。

## 2026-06-16 · 🌐 生产 env id 脚本侧单源守卫（病根#5·债#30①·loop·用户点选）

**痛**：生产 env id `cloudbase-d4gcssqbv06865479` 散在 4 个脚本——`deploy-fns`（部署目标 ENVID）/ `loadtest`·`deploy-test`（拒生产安全闸 PROD_ENV）各写一份。改一处漏一处 → 「拒生产」闸与部署目标不一致（最坏：往生产打了压测/测试部署）＝**真病根#5（非纯低收益）**。债#30① 原判"顺手收"，但脚本侧这块有安全含义、值得焊。
- **诚实分流**：`#30①③` 大部分是**前端**（miniapp/admin env id + 状态字面量 ~30 处）＝并行 CC 的 lane（隔离·不碰）+ 项目既有判断搁置（高churn·低收益）；`guard-deploy` 的同 id **仅在注释 + 确认消息文案**（非逻辑常量）→ 不动（避动 PreToolUse 钩子 runtime）。只收**脚本侧逻辑常量**这块（我的 lane·有安全价值）。
- **守卫**（先红后绿）：`scripts/lib/env.mjs` 导出 `PROD_ENV`；`prod-env-single-source`（check-structure）读其值、扫 `scripts/*.mjs`（豁免 guard-deploy）有无硬编码该 id；3 脚本改 `import { PROD_ENV } from './lib/env.mjs'`。无魔法数字（id 从单源读）。运行时验：`loadtest --env <生产>` → 「✗ 拒绝：生产环境」（导入的 PROD_ENV 闸正常）。
- **反向自检**：临时把字面量加回 loadtest → 守卫红 → 删→绿。
- **净化指标**：守卫 **32→33 repoCheck**；`npm run check` 全绿、测试 349。低风险→main 直提。**自主 loop·用户从 backlog 点选 #30①③**（我做其中 my-lane 安全片·前端片明确不碰）。

## 2026-06-16 · 🎧 客服升级独立微信客服（openCustomerServiceChat·R18/⑨·决策§19）

**痛**：客服一直是 `open-type=contact` **小程序客服**（功能简陋·单渠道），用户拍板升级独立「微信客服」（跨渠道/企业微信接待）。前序卡在「待用户给真 corpId/url 才写（根因#8 不写盲·快递100 plugin-private 教训）」——本批用户给真值（corpId `wwda…dd9` / url `…/kfid/…`），落地。
- **做**：新建 `utils/customerService.js` 收 corpId/url **单源** + `openCustomerService()`（`wx.openCustomerServiceChat`·内吃 mp/非 mp 条件编译·失败 toast 兜底·不静默吞）。4 入口接通：展示组件 DetailDock/ServicePanel `emit('service')`→父级（detail/index·HelpSheet/index）调（§6 副作用不进深层展示组件）；页面 me/aftersales 直接 `@tap`。旧 `open-type=contact` 原生按钮 + 各页 `view+toast` 兜底分支 + 死样式（.pdp/my/co-contact-row·hs-chip/send-btn）+ 两处死 `toast()` 函数全清（守反冗余）。
- **守卫**（先红后绿）：扩 `customer-service-wired`（roots R18）从「4 入口含 open-type=contact」→ ① helper 单源（导出 openCustomerService + 调 openCustomerServiceChat + corpId/extInfo 参数齐）② corpId 字面量只在单源文件（病根#5 防散落）③ 4 调用点接通 ④ 旧 contact 按钮在 4 原入口绝迹 ⑤ 假 Toast 绝迹。初始红逐条咬缺口。
- **反向自检**：临时往 me 页塞 corpId 字面量 → 守卫红「含 corpId 字面量·须收口单源（病根#5）」→ 还原绿（专证新 ③ 单源不变量；①②④⑤ 在先守卫红那步已证咬）。
- **验证**：`npm run check` 全绿（约定 + 33 结构守卫 + 349 测试）+ `build:h5` DONE。
- **诚实边界（根因#8）**：代码侧接通≠真能用——「点客服→真进微信客服会话」仍待**小程序后台绑同主体企业 ID + 配接待人 + 真机验**（靠人·详 待办⑨）。**单源落 `utils/` 非决策草拟的 `constants/`**：值唯一消费者是 helper、co-locate 最内聚。

## 2026-06-16 · 🔗 新建关于我们页 + 接线页脚死链接（守卫 footer-links-wired·病根#6 占位未接真）

**痛**（用户报）：首页页脚 SiteFooter「关于我们」「Lucky 鸭」是纯 `<text>`、无 @tap、点了没反应＝死链接占位（用户语「这两个是空的」）。属「占位/死按钮占真入口」家族（同 detail-share-wired/customer-service-wired/home-card-add-real·病根#6「用户动作无真效果」）。
- **做**：新建 `pages/about/index.vue` 关于我们页（照 agreement 内容页骨架·CoNavBar + 品牌故事**初稿 demo 待定稿**·店名引 `BRAND_NAME` 单源不硬编码·调性沿用首页 BrandIntro/ClosingCTA）+ pages.json 注册；SiteFooter 两链接绑 @tap→`uni.navigateTo('/pages/about/index')`。用户拍板「新建独立页」（非滚动锚点/非删除）。
- **守卫**（先红后绿）：`check-structure` 加 `footer-links-wired`（roots #6）——锁两链接必带 @tap 导航 + about 页存在，防回退死链接。
- **反向自检**：拆「关于我们」@tap → 守卫红「未绑 @tap·死链接占位」→ 还原绿。
- **净化指标**：守卫 **33→34 repoCheck**（接 prod-env-single-source 之后）；`npm run check` 全绿、测试 349、约定 66 文件；`build:mp-weixin` DONE（about 页进产物）。**前端 lane（parallel-b）**：提交 b → cherry-pick main → main rebuild → push（用户工具跑 main build·见 memory 构建可见性陷阱）。残：品牌文案待用户定稿。

## 2026-06-16 · 🔢 集合计数单源守卫（巡检反复标 16/17/18 口径漂移·规则⑥·自主 loop 第1轮）

**痛**：桌面只读巡检 #001/#002/#003 反复标「集合 16/17/18 口径漂移」——库权限表标题写「17 个集合」、注写「16 个」、`系统事实` 写「17+1」，而真值（`kit/collections.ts` COLLECTIONS 册）＝**18**。根因＝客观计数被**手抄在多处**、加集合（rateLimit 等）没同步 → stale（文档体系规则⑥：客观计数只系统事实机器维护、别处不手抄）。
- **守卫**（先红后绿）：`check-structure` 加 `collection-count-synced`——解析 COLLECTIONS 集合数（真值 N=18），核 `系统事实`「DB collection」计数列 === N + `库权限表`标题「## N 个集合」；不符即红。无魔法数字（N 随 COLLECTIONS 动态）。**踩坑**：`find('DB collection')` 先匹配到散文 bullet 行（无 `|`）→ 限定只认表格行（`|` 开头）修正。
- **改到绿**：`系统事实`「17+1」→「18」、`库权限表`标题「17」→「18」+ 注「16」→「18」（表本就列 18 行·纯 stale 计数·零权限含义变化）。
- **反向自检**：系统事实计数列篡 18→17 → 守卫红「计数列为17≠真值18」→ 还原绿。
- **净化指标**：守卫 **31→32 repoCheck**；`npm run check` 全绿、测试 346。低风险→main 直提。**自主 backlog loop 第 1 轮**（main/后端 lane·并行 CC 在 parallel-b·零竞争）。巡检 #003 待办②关账。

## 2026-06-16 · 🛡️ preflight 加「钱链改了未部署→红」守卫（部署漂移·根因#8「已提交≠已部署」）

**痛**：并行 CC 只读巡检 #003 抓出 + git 验证为真——deploy-manifest 停 06-15 12:31，之后 4 commit 改了钱链/看板云函数（[LD_ALERT] 告警/支付窗口/精确 GMV）却没部署、漂了 4 天没人发现。**preflight 原只验 `dist≡cloudbaserc`（结构完整）、不验「部署时效」**＝盲区。已 `DEPLOY_ALLOWED=1 deploy-fns` 全 29 函数重部署消除漂移（`6d85446`·smoke 健康），但「靠人记得部署」迟早再漂——**把它焊成机器闸**。
- **守卫**：`scripts/lib/deploy-drift.mjs`（`MONEY_CHAIN` 钱链 6 函数 + 纯 `detectDrift(cur,deployed,fns)`）+ preflight 新增一项：build:cloud 后比每个钱链函数 `dist/<fn>/index.js` 的 sha256 前 12（同 deploy-fns 算法）vs manifest 已部署 hash，不符即红「钱链改了未部署——先 deploy-fns」。**为何只钱链**：钱/状态最高危（漂移＝告警网/支付逻辑落后生产）；非钱链漂移低危、不卡 preflight。**为何 preflight 不进 `npm run check`**：提交时代码本就领先部署（设计如此）、进 check 会每次提交假红；preflight 是**部署前闸**，那里「钱链未部署」才是真信号。
- **单源**：`MONEY_CHAIN ≡ guard-deploy SENSITIVE_FNS 钱链段`（注释同步·暂不抽共享模块进 guard-deploy：它是每次 Bash 的 PreToolUse 钩子、改其 runtime 有锁死风险，保守不动）。
- **反向自检**：① 单测——短路 `detectDrift` → 3 红（hash不符/manifest缺/产物缺）→ 还原绿；② 集成——篡 manifest pay hash → preflight `❌ pay(deadbeef→真hash)` → `git checkout` 还原绿。
- **净化指标**：守卫 31 repoCheck 不变（preflight 是部署闸、非 check 注册表项；#8 靠人豁免根·单测=回归锁）；`npm run check` 全绿、测试 **339→345**（+6 deploy-drift）。**多-CC 隔离后首批**：本批全程在 main 工作树（并行 CC 在 `parallel-b`），零竞争。关账 [[deploy-drift-202606]] 残项。

## 2026-06-16 · ✅ 频控端到端真证——压测台扩 `--mode throttle`（债#19/#13·根因#8）

接压测台真跑：上一步证了 DB 地基（CAS/破千），但**频控（防爆破/防刷）仍只桩绿**——`throttleConcurrency.test.js` 是内存桩、真库下 throttle 那套操作的并发行为没验。**黑盒打真函数走不通**（`withRateLimit` 在 `withOpenId` 内、`tcb invoke` 给不了微信 openid 上下文；且 throttle.ts 用 wx-server-sdk 形状 node-sdk 跑不动它本体）；**搬真 throttle.ts 进探针云函数**会撞 `deploy-config-complete`（dist≡cloudbaserc 不变量）、有生产风险——故选**压测台复刻 throttle.ts `bumpWindowed` 的确切 DB 操作**打真测试库。
- **真库证据**：`--mode throttle --n 20 --max 10` → `放行 10（=max）· hits=20（=n）· 无泄漏 · 重试耗尽 0` ⇒ 真库下首写 `add({_id})` 撞号竞争（恰一胜）+ 窗内 `where({_id,旧窗,旧计数}).update` CAS 计数 都原子，并发下**恰 max 放行、丢更新不会让超额请求漏过频控**。频控「只桩绿」缺口闭合（根因#13 并发面真证）。
- **诚实边界**：复刻的是 throttle 的 DB 操作（node-sdk 形状），非 throttle.ts 本体（wx-server-sdk·已桩证）；DB 并发行为真证 + 逻辑桩证 + CAS 原语真证 三者合一即闭环。`exists(false)` 跨系列窗口初始化分支（throttle.ts:71）属 #33 防御分支、另有桩测。
- **守卫**：抽 `readDoc`（throttle 模式读 hits/hitWindowStart 所依）+ `tests/scripts/loadtest.test.js` 补 3 条锁两形状；反向自检：篡 `readDoc` 去数组分支 → 3 测试红 → 还原绿。
- **净化指标**：守卫 31 不变（#8/#13 靠人豁免根·单测=回归锁）；`npm run check` 全绿、测试 **334→337**。关账债#19 频控腿（残：索引核查＝控制台）。

## 2026-06-16 · ✅ 压测台真跑落地——两块地基「桩绿→真证」+ 修 SDK 形状假阴（债#19·根因#8）

债#19：压测台骨架（`scripts/loadtest.mjs`）此前只在内存桩上绿，从未打真库。本批跟用户协作把它真跑通测试环境 `luckyducky-test`，**throttle CAS / count·aggregate 两块地基从「桩绿」升「真证」**——这是「桩绿≠真证」（根因#8）那条腿，落地。
- **真库证据**：`--mode cas` → `v=20·0 丢更新`（真库 `where().update()` CAS 原子 ⇒ throttle CAS#21 / transition 状态机 / 激活码抢占 并发正确性坐实）；`--mode capacity` → `count=2000=aggregate gmv=2000·991ms`（破千不被 1000 封顶 ⇒ #18 看板 GMV 当初少算的根治证据）。
- **真跑暴露并修三层 SDK 形状假阴**（桩按云函数 `wx-server-sdk` 写、真跑用 `node-sdk` 直连，返回形状不同 → 全程假「丢更新」，详 `调试日志` P）：① `update()` 返回 `{updated}` 非 `{stats:{updated}}`；② `get().data` 是数组非对象；③ `set/update/add` 吃裸对象、不要 `{data:}` 包层。修：`readV`/`updatedCount` 兼容两形状 + 去包层 + `createCollection` 兜底（CloudBase 不自动建集合）+ 缺密钥诊断 + 小批并发灌种子。
- **守卫（可机器化的那片）**：`readV`/`updatedCount` 导出 + `tests/scripts/loadtest.test.js`（6 条·两 SDK 形状都锁）。根因#8 是 CLAUDE 靠人豁免根，本批不增 repoCheck；此单测是回归锁（防手滑改回单形状）。**反向自检**：`updatedCount` 去 node-sdk 分支 → `{updated:1}` 返 0 → 测试红 → 还原绿。
- **真容量副得**：环境对突发 DB 有 QPS 限流（200 并发即 `EXCEED_RATELIMIT`、20 安全）——上量峰值非无限，记入容量评估。
- **净化指标**：守卫 31 不变（单测=行为锁，非新主张）；`npm run check` 全绿、测试 **328→334**。关账债#19 压测腿（残：索引核查＝控制台动作）。

## 2026-06-16 · 🧪 throttle 防御分支补测试覆盖（债#33·保留防御非删·根因#8）

债#33（质量回顾发现⑥）：`bumpWindowed` 在「记录已存在、本系列窗口字段缺失」时 CAS 前置用 `_.exists(false)`（throttle.ts:71），但当前 throttleHit/throttleFail 用不同 key 不混用 → 该防御分支不可达、原**未被真测**（根因#8「逻辑存在 ≠ 被验证」同源）。用户从「#10/#11/#30③/#33」一组债里**只选 #33**（其余 #10 admin 盲拆无测试网 / #11 颜色易视觉漂移＝债文档判断不该主动做、勘察核实成立 / #30③ 中 churn，均尊重既有判断暂缓）。
- **改＝补测试逼出（不删·保留防御）**：`tests/cloud/throttleDefenseBranch.test.js`（2 条）构造混用场景——同 key 先建 hit 系列记录（hits/hitWindowStart）再调 throttleFail（用 windowStart）、及反向——显式覆盖 `exists(false)` 分支；断言「本系列窗口字段被首设 + 计数落地 + 不踩另一系列字段」。纯补测试、零运行时改动，逻辑本就正确故测试即绿。
- **反向自检**：把 `_.exists(false)` 改 `_.exists(true)` → 2 测试均红（CAS 条件永不匹配缺失字段 → updated=0 重试耗尽 → 记录不写）→ 还原绿，证测试真咬该分支（非摆设）。
- **净化指标**：守卫 31 不变（测试即行为守卫，非新主张）；`npm run check` 全绿、测试 **326→328**。关账待办#33。

## 2026-06-16 · 🔒 隐私挂载可达性守卫加固（债#25·把靠人变机器守·根因#8）+ 上线 runbook

债#25（上线前置加固·用户选 scope＝隐私加固 + runbook）：隐私授权闸 `registerPrivacyGate` 全局注册（`onNeedPrivacyAuthorization` 任意页触发），但 `<PrivacySheet/>` 须挂在该页才渲得出——守卫 `privacy-authorize-wired` 原只验「四件套存在」、不验挂载可达性＝盲区，靠人记「接第一个隐私接口前挂」。
- **改＝扩守卫补挂载可达性**（`check-structure.mjs`·扩现有守卫·非新 id·强度+1）：加 `PRIVACY_INTERFACES` 登记册 + `mpReachableText`（剥 `#ifndef MP-WEIXIN`/`#ifdef H5|APP` 等非 mp 条件编译块）；扫 `pages/**/*.vue`，命中 mp 可达涉隐私接口的页 → 其根 `index.vue` 必挂 `<PrivacySheet/>`，否则红。宁过勿漏（脚本级未 gate 调用也计入·多挂无害、漏挂出事）。
- **诚实边界（根因#8）**：mp-weixin **当前无涉隐私接口**（`profile-edit` 的 `uni.chooseImage` 在 `#ifndef MP-WEIXIN` 路径、mp 走 exempt `chooseAvatar` 按钮 + `nickname` 输入）——本守卫是**前瞻 + 补盲区**，把「接第一个隐私接口前记得挂」从靠人变机器守；真机授权弹窗渲染仍待 mp 后台登记《指引》+ 真机验（㉒）。守卫今日即咬：profile-edit 脚本级 chooseImage 计入 → 已挂 sheet → 绿（非空转）。
- **反向自检**：删 `profile-edit` `<PrivacySheet/>` 挂载 → `privacy-authorize-wired` 红（报「调涉隐私接口却未挂」）→ 还原 → 绿（证守卫真咬）。
- **上线 runbook**（`现状与路线.md`·「下一步」扩为有序段）：散在待办各 # 的上线前置排成 10 步有序清单（内容→合规→协议→隐私→域名→库权限→发布→守望→客服·每步 现状/谁做/验收信号/债#），计划单源、引债#不复抄；关账待办#25。
- **净化指标**：守卫 31（强度+1）；`npm run check` 全绿（测试 326 不变·无运行时改动）。**不含**发货自动上传#26（§20 手动录入先）/ patrol 装置（另案·plan 在 `~/.claude/plans/`）/ 翻 urlCheck（发布动作）。

## 〔06-14/06-15 批次明细已卷档〕

> v0.9.1 上线 cutover + v0.9.2 批次（压测台/精确GMV/钱链告警[LD_ALERT]/频控CAS原子/客服R18/分享R29/协议隐私R27/P1加固线/支付fail-closed/events清理 等 ~29 批）完整明细已归 `archive/重构日志-202606上半.md`；里程碑结论见上方 v0.9.2 段。

## 第二次重构（演进·技术整合）

> 计划见 `现状与路线.md`；第一次重构批次证据已归 `archive/重构日志-第一次重构.md`。

### 2026-06-15 · P2 需求→守卫闭环（requirement-trace + trace 工具）
P1 的 `需求清单` 实现链接机器化：加「需求→实现映射」表（R1–R15 → 函数/测试/守卫，full 粒度）；`check-structure` 加 `requirement-trace` repoCheck（仿 guard-coverage：每 ✅ 实现需求有映射行 + 行内 函数(系统事实)/测试(tests/cloud)/守卫(注册表) resolve，断链当场红）；`scripts/trace.mjs` + `npm run trace`（正查 R# 爆炸半径、反查 `--fn`/`--guard`）。**反向自检**：映射改 confirmEnter→不存在名 → 守卫红「函数未见于系统事实」→ 还原绿。仓级守卫 14→15、测试 277→278。生产仓零改动。**P2b 已做**：映射加 `类` 列 + requirement-trace ③ 段核「钱→钱守卫/状态→状态守卫/安全→闸守卫」，反向自检过（删 R2 钱守卫即红）。

### 2026-06-14 · P0 知识地基 + 文档精简
- P0：立 `现状与路线.md`（五阶）；归档第一次重构总计划；指针改指地图。
- 文档精简（搬历史进 archive·零丢失）：审核报告×2 + 工作日志（冻结于 0612）整份归档；验收手册§三 / 重构日志第一次重构批次 / 项目现状历史 拆出归档；CLAUDE 修「工作日志冻结·重构日志=live」矛盾。活文档瘦约 1/3。
