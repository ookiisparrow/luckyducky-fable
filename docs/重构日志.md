# 重构日志（样板房专属）

> 本文件记重构线批次结论与证据（Phase A / B0–B9 = 第一次重构；下新增「## 第二次重构（演进）」段续记）；生产线工作日志已归 `archive/工作日志-生产线-至20260612.md`（冻结于 0612）。三支柱：`根因账本.md`（病根）+ `现状与路线.md`（live 地图；第一次重构总计划 v5 见 `archive/`）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`archive/项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-06-15 · 🏷️ v0.9.2 里程碑（代码里程碑·新会话先读这段）

**v0.9.2 = 代码里程碑**（tag 在 main `d182075`），收束 v0.9.1 tag 以来三条线；**不是新部署**（部署状态见末段）。
- **① v0.9.1 验收收尾**：验收单 X/Y/Z 真机真钱过——X 库权限客户端实测抓真 P0（13 集合曾可写 + `courses` 公开读漏 `videoFileId`）→ 16 集合锁仅管理端；本仓接管 tcb 部署闸（deny-all → 生产仓模型，敏感函数 `ask`）。
- **② 产品打磨**（前端 miniapp·用户可见）：微信登录半屏弹窗 + 启动加载页 + 登录流程完善（退出/一键登录/默认昵称 `Lucky friend`+随机鸭头像）；首页/购物车 UI 批（品牌 logo/卡片等高/购买滚动居中/顶栏吸顶/结算条对齐）；「我」页未激活课程空态；**返回重构**（Tab 页「再按一次退出」= scroll-view + page-container）；新 skill `/frontend-check`。
- **③ 治理脊柱**（第二次重构·演进；纯文档/守卫/工具，**不碰运行时**）：P0 知识地基（立 `演进地图`）→ 文档精简+架构校准（活文档瘦 ~40%）→ P1 需求显形（`需求清单` 31 条单源 + 逐条复核，修 R22 跑偏）→ 文档架构重设（19→13 份，客观/主观分治 + 反膨胀守卫 `docs-budget`）→ **P2 需求→守卫闭环**（`requirement-trace` 守卫 + `npm run trace` 爆炸半径）→ **P2b 须守卫覆盖**（钱/状态/安全类需求须有对应类守卫）。

**现状**：测试 **278** 全过；守卫 **15 repoCheck + 5 fileRule**；活文档 **13**；skills **7**；CLAUDE 144/180。生产仓 `luckyducky-miniprogram` 零改动（HEAD `2dd5a72`，回滚基线）。
**部署状态（honest，勿读成"已全上线"）**：后端云函数仍是 v0.9.1 部署的 **28 个**（治理 work 不碰运行时、无需重部署，后端已是生产随时服务）；前端登录/UI/返回 等产品改动**已合 main、未发布生产**——发布属「全客户上线」动作（`build:mp-weixin` + `urlCheck`→true + 微信后台合法域名 + 审核），仍待。
**容量评估（2026-06-15 看码评估·非压测）**：4 位数/天 架构可承——并发正确性稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）。上量前必清 4 件（详 `待办与债` §上量就绪）：① 数据库套餐档位（控制台查·最大未知）② 视频 CDN/VOD（R21 搁置·裸云存储吐视频带宽=头号成本/风险）③ 看板内存聚合 1000 条/表封顶（4 位数当天 GMV 即少算）④ 索引 + **从未压测**（根因#8 同理：单人能用 ≠ 千人同时）。

## 2026-06-27 · 🏷️ 品牌名统一：中文名漂移「幸运小鸭」绝迹 → 小棉鸭（病根#5 缺口补堵）
**来源**：用户问 SEO/GEO 提前工作 → 校出店内品牌中文名分裂：小程序名/商品/课程/分享/卡片散落「幸运小鸭」，与品牌单源 `BRAND_NAME='Lucky Ducky 小棉鸭'` 不一致（CC 早前自作主张掺入的中文名漂移）。用户给全口径：中文名**小棉鸭**、英文名 **Lucky Ducky（带空格，沿用不动）**、中文 slogan「学习钩织，创造幸运」、英文 slogan「Get ducky get lucky」、各平台注册名 **LuckyDucky小棉鸭 初学钩织入门**（后台手动·非代码·尾带 SEO 关键词）。**根因（病根#5 复制漂移）**：守卫 `brand-name-single-source` 当初只堵旧占位「易织」+ 完整店名，**没堵「幸运小鸭」**→中文名从 seed/manifest/组件多处漂进来。**修**：① 守卫绝迹名单加「幸运小鸭」+ 扫描扩到 shared 种子/cloud 云函数/根级 tests 夹具（原只扫 miniapp/admin src=盲区）；② walk 不扫 .html → 对 `/q` 落地页（用户可见）加显式 ③ 检查；③ 全替「幸运小鸭」→「小棉鸭」约 20 文件（商品 prod-1/4、课程 course-duck、分享默认标题、productDetail、order rebuy、pay 微信支付描述、卡片 brandText×3、StepInfo 占位、/q 落地页文案、6 个 cloud 测试夹具 + cardSvg/share/courses 测试断言）；④ 品牌规范全口径写进 `brand.js` 单源注释。**英文名 Lucky Ducky（带空格）经核本就正确、一字未动**（差点误改成 LuckyDucky，确认口径前止住）。**反向自检（备份-还原·不用 git checkout 防冲未提交改动）**：注入「幸运小鸭」到 src/tests/`/q` 三处 → ②③ 各自红 → 还原绿；中途逮出真假绿——把 admin/public 加进 walk 但 .html 不被 walk 扫＝摆设守卫，改为移出 walk 列表 + 显式 ③ 钉死（根因#8）。**指标**：守卫 84 repoCheck（`brand-name-single-source` 扩，不计新）·5 fileRule·测试 **621** 全过·云函数 33·`npm run check` 全绿·build:cloud(33)/h5 DONE。**剩靠人（你终端）**：① 各平台后台设注册名「LuckyDucky小棉鸭 初学钩织入门」② 前端随 v0.9.5 republish ③ 重部署 pay/adminApi(cards) 云函数 ④ build:admin + 重部署静态托管（/q 落地页 + admin UI 品牌字）⑤ 文档/设计稿里「幸运小鸭」（CLAUDE/README/运营规划§六/待办/archive×4 + design/console.pen + prototypes）属项目昵称/历史记录，未在本批扫——按需另开 docs 批清理。

## 2026-06-27 · 🚀 上线收尾批群（用户逐条核销上线必做·协议/告警/发货合规闭环）
**来源**：用户「现在所有待办有哪些」起，逐条推进上线必做（详情已在 `现状与路线` live 地图 + `调试日志 Z` + 各 commit·此处只账本）。**① 发货合规 #26 openapi 权限自动声明（`f37ecbd`）**：build.mjs 加 `OPENAPI_PERMS` + 按产物产 config.json（adminApi 得 `wxaSecOrder.uploadShippingInfo`）、修正 shipping.ts 注释权限串（REST→JS 调用路径）、新守卫 `openapi-perm-declared`(#12)。**② deploy-fns hash 盲区（`16b7f3f`·根因#8）**：用户跑 deploy 显「待部署 0」→查实漂移 hash 只算 index.js·config.json 改动漏判→hash 非对称纳入 config.json + 守卫扩第③关（反向自检逮假绿收紧正则）。**③ 协议第三方补快递100（`601c4ce`·㉑）**：隐私政策第三条漏披露快递100 插件（共享快递单号）→如实补 + `agreement-text-real` 扩断言（事实对齐非法律判断·[[cc-scope-mechanical-not-legal]]）。**④ ㉑协议用户拍板通过（`edf68c4`）**：无外部法务·经营者评估自担风险·去页面「初稿·待法务复核」用户可见提示。**⑤ #23 企微告警闭环（`5917e15`+部署）**：notifyAlert→pushBotAlert 接缝早落·用户建群机器人 webhook→curl 验通→admin「消息与通知」存（webhook key 凭证只存后台 DB·不入库）。**⑥ 根治 `_id`-in-set 类 bug（`3d93d73`·根因#8 桩≠真 SDK）**：用户存 webhook 报 500→真 wx-server-sdk `doc(id).set({data})` data 含 _id 即 reject·saveSettings `{...cur}`(get 回含 _id)二次存必 500·内存桩默接受 _id 藏住坑（同 throttle 老坑）→全扫 4 处同根（saveSettings 硬 500 / wecom token + kfCallback cursor 缓存 fail-soft 静默废 / kfBind 潜伏）去 data 里 _id + 双守卫（桩对齐真 SDK 即抛 + 静态 `no-id-in-set-data`）+ 往返测试·反向自检×3。**部署上线（`eac1fa0`）**：adminApi/kfBind/kfCallback 3 函数上生产·webhook 已存成·#23 彻底闭环。**净**：守卫 82→**84 repoCheck**（+`openapi-perm-declared`/`no-id-in-set-data`）·测试 617→**621**·云函数 33·`npm run check` 全绿。**上线状态**：①支付②退款③⑱扫码域名㉒隐私指引㉑协议#26 发货合规#23 告警 全✅；上线🔴 实质只剩 ⑭关键课真视频 + 商品图（运营填）+ v0.9.5 前端发版前置（合法域名/锁集合 ACL/urlCheck）。

## 2026-06-26 · 🧹 外部审核 R1-R4·上线前安全/卫生扫尾（P1.7 漏的 P1 + P2.13 + P2.14；P2.18 by-design）
**来源**：C 簇毕后用户问「还有什么」→ 盘点发现 **P1.7 是 9 条 P1 里唯一漏的**（A/B/C 三簇都没碰），且 CC 能做；连同 P2 里便宜确凿的 P2.13/P2.14 一批扫。**P1.7 登出泄露 PII（根因#3 信任边界）**：`logout()` 原只清 user store（token/openid/profile），address store 持久化姓名/电话/收货地址=PII、cart 持久化购物车 → 同设备登出后另一人登录，新用户看见并误用上一位收货信息。修＝logout `$reset` 全部用户态 store（address/cart/orders/aftersales/progress/activation·这些 store 不 import user.js 无循环·useXxx 仅在 action 内实例化）；address/cart 持久化故 $reset 连带清 storage，orders 等清内存防加载前残留。新守卫 `logout-resets-user-scoped`(#3·锁 logout 须 reset 这 6 个 store·无前端 pinia 测试基建故以结构守卫兜)。**P2.13 地址字段缺长度上限（根因#3 不信前端）**：createOrder 地址四要素 trim 后加 `.slice`（name40/phone20/region60/detail120）防超长撑爆订单文档/拖慢列表；前端表单本有限、云端再兜一道。inline·**无专守卫**（防御性一行·§7 不为它上守卫）·测试断截断。**P2.14 rateLimit/kfSeen 无 TTL（债#9 无界增长同治）**：throttle.ts patch 加 `updatedAt`（每次写更新·不入 CAS cond 不扰抢占）；cleanupEvents 扩清过期 `rateLimit`（按 updatedAt < 90 天·非 createdAt 创建即定会误删活跃 key）+ `kfState seen:*`（按 `at` 字段·cursor:*/token 文档无 at 故不误删）。扩 `events-cleanup-wired` 守卫（断言 `collection('rateLimit'/'kfState')...remove` 实清代码·非仅 token——防注释含字样却没真清·同 stock-cas 注释坑）。**P2.18 经核 by-design 不改**：grep 全云端**无** transaction_id 入日志（仅 refundCallback DB 存 refundTransactionId）；唯一在告警的完整订单号（refundCallback MISMATCH 的 outTradeNo）是 CLAUDE §4 明许的对账标识（§4「ctx 只放非敏感标识 orderId/金额分/状态/IP」·禁的是 openid/口令/凭证）→ 脱敏订单号会反噬对账可观测、且违背既有有意设计·不改。**守卫/测试（先红后绿·反向自检×2 过）**：logout 去一个 $reset→守卫红；cleanupEvents `collection('rateLimit')`→改名→守卫红；均还原绿。cleanupEvents 测试 +「清过期 rateLimit/kfSeen·保活跃/token/cursor」、createOrder 测试 +「地址超长截断」。**指标**：守卫 81→**82 repoCheck**（+`logout-resets-user-scoped`·`events-cleanup-wired` 扩不计新）·5 fileRule·测试 614→**617**·云函数 33·`npm run check` 全绿·build:cloud(33)。**🔴 待你重部署**：createOrder/cleanupEvents（+ throttle 改属 kit·随任一调它的函数重打包）；前端 user store 随 v0.9.5 republish。**外审 R1-R4 代码侧全清**：P1 9/9（含本补 P1.7）+ P2 便宜确凿批；剩 P2 设计类（P2.4 admin 认证模型/P2.6 内容编辑 CAS/P2.1 支付冷却 等·未排期）+ D 簇靠人（控制台 ACL/真机/隐私）。

## 2026-06-26 · 🧮 外部审核 R1-R4·C 簇 P1.3 数量级退款（进课件级 + 退剩余·中批低风险）
**来源**：C 簇 P1.1（`e0aef2e`）后续——用户「接 P1.3」。先把退款回调链摸清（refundCallback 按售后 _id 抢占翻 refunded、按 as.qty 回补库存），再用 AskUserQuestion 把**深度决策**摆给用户（动钱链·不可逆）：**用户拍板「进课件级 + 退剩余全部」**（中批低风险）而非「任意件数多次部分退」（大批·改钱链并发/_id/释放模型）。**痛（P1.3·根因#1 数量级权益不足）**：`refundable` 行级布尔，买 N 件进课 1 件 → confirmEnter 整行翻 false → 剩 N−1 件白丢退货权。**修**：订单行加 `enteredQty`（件级进课账）——① createOrder 每行 `enteredQty:0`；② **confirmEnter 进课按件递增**（找一条 `refundable!==false && enteredQty<qty` 的本课程行，`enteredQty++`、`refundable=enteredQty<qty`·全进才整行不可退·非整行作废）；③ **applyRefund 退剩余件**（`refundableQty=qty−enteredQty`·`refundable===false||refundableQty<=0` 拒 NOT_REFUNDABLE·`itemFen` 金额按 refundableQty 摊不再整行 item.qty·`afterSale.qty=refundableQty` 供 refundCallback restoreStock 回补对件数）；④ 前端售后页 `row.qty=剩余可退件`（模板 ×N/￥price×N 自动反映·云端权威重算不信前端）。**向后兼容**：旧订单行无 enteredQty 视 0＝整行可退（qty=1 行进一次即 refundable=false·与原整行翻 false 等效）；**既有 applyRefund/confirmEnter 测试 seed 的 item 无 enteredQty＝兼容路径·全过**。**范围诚实**：一行一售后、确定性 _id 不变（退一次=退剩余全部·**不支持「退1留1」**·剩余走人工·同现状）——「任意件数多次部分退」是 P1.3+（需行级 CAS 抢占 + 售后自动 _id + reject 释放件数·真有需求再上·§7 先打地基按需再抽）。**守卫/测试（先红后绿·反向自检过）**：新 `refund-remaining-qty-after-enter`(#1·锁 createOrder 行带 enteredQty + confirmEnter 件级递增 + applyRefund 按 refundableQty 摊)·初版对当前码红 3 项→改到绿；新增 confirmEnter「买3进1仍可退/进满3不可退」+ applyRefund「买3进1退2件·金额按2件摊·afterSale.qty=2」+「全进课行 NOT_REFUNDABLE」用例；反向自检＝confirmEnter 退回整行翻 false（去 enteredQty）→守卫红→还原绿。**指标**：守卫 80→**81 repoCheck**·5 fileRule·测试 609→**614**·云函数 33·`npm run check` 全绿·build:cloud(33)。**🔴 待你重部署**：createOrder/applyRefund/confirmEnter；前端售后页随 v0.9.5 republish。**C 簇 P1.1+P1.3 全毕**（D 簇控制台 ACL/真支付退款真机/隐私登记够不到·靠人）。

## 2026-06-26 · 🧱 外部审核 R1-R4·C 簇 P1.1 订单行稳定身份 lineId（分两批·本批先打地基）
**来源**：B 簇（`9923c12`）后续——用户「继续/开始」接 C 簇。先用寓言把 C 的本质讲清（C＝账本「精度欠债」·掀账本前先想清值不值），**用户拍板分两批：本批先做 P1.1 行身份地基,P1.3 数量级权益留下一批**。**痛（P1.1·根因#1 唯一标识粒度不足·同 P1.9 batchId 一根）**：`order.items` 行靠 productId 定位（applyRefund/submitReview 找行 + 售后/评价确定性 `_id=orderId__productId`），同商品多 SKU 同单（红款+蓝款）→ _id 撞、退错/评错行、第二 SKU 直接 ALREADY_APPLIED/REVIEWED·业务不变量不成立。**修**：引入稳定 `lineId=productId__spec`（与 inventory idOf 同款·单内唯一·可读）——① createOrder 每行带 lineId（搭配购行 + 商品行）；② applyRefund/submitReview 收 `lineId`（兼容旧前端传 productId），按**有效行键 `it.lineId||it.productId`** 定位行（两端一致）、`_id=orderId__lineId`、查重按有效键、记录加 lineId、productId 从命中行派生（不信前端）；③ approveRefund 复核（P1.2）改按有效键定位行；④ confirmEnter revoked 带 lineId；⑤ 前端 review 页提交传 lineId、aftersales 页 row/has/applyFor/key 用 lineId、aftersales store has 按有效键查重。**向后兼容**：旧订单 item 无 lineId 字段 → 读路径回退 productId（有效键 `it.lineId||it.productId`）；**既有 applyRefund/submitReview 测试 seed 的 item 本就无 lineId＝兼容路径,全程未改全过＝兼容性内建验证**。**守卫/测试（先红后绿·反向自检过）**：新 `order-line-identity`（roots#1·锁 createOrder 行带 lineId + applyRefund/submitReview _id 用 lineId 不再纯 orderId__productId）·初版对当前码红 4 项→改到绿；新增「同 productId 双 SKU 两行各自退/评（不再撞 _id）」+「旧订单传 productId 仍可退（lineId 落库=productId）」用例；反向自检＝submitReview _id 退回 orderId__productId→守卫红→还原绿。**指标**：守卫 79→**80 repoCheck**·5 fileRule·测试 605→**609**·云函数 33·`npm run check` 全绿·build:cloud(33)/h5 DONE。**🔴 待你重部署**：createOrder/applyRefund/submitReview/adminApi/confirmEnter；前端 review/aftersales 随 v0.9.5 republish。**P1.3 数量级部分退款留下一批**（待真有顾客要「买3进1退2」再定模型 计数 vs ledger·refundable 暂留整行布尔·符合 §7 先打地基按需再抽）。**D 簇仍够不到·靠人**（控制台 ACL/真机/隐私）。

## 2026-06-26 · 🛡️ 外部审核 R1-R4·B 簇上量真钱/数据正确性四修（P1.2/P1.8/P1.4/P1.5）
**来源**：A 簇（`40bca80`）后续——用户「继续」接 B 簇（真钱/数据窗口）。四修各配守卫先红后绿、反向自检×4 全过。**P1.2 同意退款未复核进课**（approveRefund 只查售后状态不复核订单行·用户先申请退款后又确认进课·confirmEnter 翻 refundable=false→admin 同意退款形成「已交付课程+已退款」）→ 触发退款工作流前读该售后对应订单行 refundable、已撤即拒 `ENTERED_NOT_REFUNDABLE`·不触发退款（新守卫 `approve-refund-rechecks-entered`·roots#1）。**P1.8 库存绝对写覆盖并发预留**（admin 开旧页面保存 setStock 无条件 set·把下单并发预留扣减覆盖回旧值→超卖窗口）→ setStock 接 `expectedUpdatedAt` 走条件写（where{updatedAt}+updated===1·否则 conflict）·saveStock action 透传并映射 `STOCK_CONFLICT`·Inventory.vue 加载存 updatedAt + 保存回传 + 冲突刷新提示（新守卫 `stock-cas-conditional-save`·roots#1；上新/首设无 expectedUpdatedAt 走原 upsert·不限量 SKU 无并发预留可踩）。**P1.4 客服认领基建错伪装重复**（firstSeen 把 add 任何异常都返 false=「已处理·跳过」·DB 权限/集合缺/瞬时错被当重复→真实客服消息无回复无告警不重试=永久吞消息·根因#8）→ firstSeen 改三态 first/duplicate/error（add 失败回查 seen 文档存在性区分撞号 vs 基建错）·processKfBatch 对 error 告警 `CLAIM_FAILED`+anyFailed 保留游标重试·未认领不处理（新守卫 `kf-claim-distinguishes-infra`·roots#8）。**P1.5 回调超时吞消息**（函数超时 20s·单批旧默认 1000 逐条串行可能做不完→已认领副作用未完成被硬超时杀·下次 seen 跳过·根因#8）→ index 单批 limit 降 50 + 墙钟时间预算 15s（`Date.now()-startedAt` 临近超时停·保留旧游标续拉·游标只整批成功才推进故 break 不丢）（新守卫 `kf-callback-bounded-and-budgeted`·roots#8）。**反向自检×4**：refunds 改错误码/dispatch 改 CLAIM_FAILED/index 放大 limit→对应守卫红；stock 守卫初版被同 token 的 docstring 满足（假绿·守卫够不到「码 vs 注释」）→改 docstring 去字面 token + 守卫收紧 `conflict:true`·改 blocked→红→还原绿（自检暴露的真改进）。**指标**：守卫 75→**79 repoCheck**（+4）·5 fileRule；测试 →**605**·云函数 33·`npm run check` 全绿。**剩靠人（你终端）**：重部署 adminApi(P1.2/P1.8)+kfCallback(P1.4/P1.5)；前端 P1.8 Inventory.vue 随 admin 域改动、客服/退款无前端发版。**C/D 簇仍待排期**：C（P1.1+P1.3 订单 lineId+数量级 ledger·退款模型改动·待决策）· D（控制台集合 ACL / 真支付退款真机 / 隐私登记·够不到）。

## 2026-06-25 · 🧹 外部审核 R1-R4 评估 + A 簇便宜确凿四修（P1.6/P2.11/P2.19/P1.9）
**来源**：评估桌面《LuckyDucky全项目深度审核-R1-R4》（基线 `62426fd`=当前 HEAD 前 2 docs·零代码漂移）→ `/deep-audit` 亲核 9 条 P1 全 + 5 条关键 P2（P2.3/2.9/2.11/2.12/2.19），14/14 命中真实模式·**无 P0 认同**。校准两处：**P1.9 实为 P2**（码 `_id` 仍全局唯一·只后台批次分组合并）、**P2.12 安全性被高估**（mp 端鉴权用 WX 上下文 OPENID 非客户端 `'wx'` token）。用户拍板先修 **A 簇（便宜确凿）四条**·各配守卫先红后绿。**P1.6 样例地址进真实下单链**（store/address.js 初始播 `{...SAMPLE_ADDRESS 陈圆圆,id:1}`·真支付一开首购没改地址误发样例人）→ 初始 `list:[]` + 删 data/address.js 死代码（空态由 checkout `addr=null` 既有路径兜·新守卫 `address-no-sample-seed`·roots#6）。**P2.11 客服卡片打不开**（dispatch route 写 `pages/aftersale`少 s + `pages/course`实际在 pkg-video 分包·桩测把错路径锁成假绿·根因#8）→ 修 `pages/aftersales/index`+`pkg-video/courses/index`·新守卫 `kf-card-page-registered`（提取 dispatch `page:'…'` 交叉 pages.json 主包+分包注册集·路由改名/迁包当场红）。**P2.19 客服查单运单号永显不出**（发货写 `shipping.trackingNo`·摘要错取顶层 `o.trackingNo`·fixture 也错置顶层＝失真样本根因#8）→ 取 `o.shipping.trackingNo` + fixture 改真实嵌套形状。**P1.9 批次 ID 分钟粒度合并**（同课同分钟两批撞 batchId 被 batches.ts 合并）→ 提取 `makeBatchId` + 到秒 + 随机后缀（batches 按存库字段分组·不解析串·加后缀不破坏分组）。**反向自检×4 全过**：address 非空/路径写错→结构红；trackingNo 退顶层/batchId 去随机→对应测试红；还原全绿。**指标**：守卫 73→**75 repoCheck**（+`address-no-sample-seed` +`kf-card-page-registered`）·5 fileRule；测试 →**596**·云函数 33·`npm run check` 全绿·build:cloud(33)/h5 DONE。**剩靠人（你终端）**：重部署 kfCallback(P2.11/P2.19) + genQrcodes(P1.9)；前端 P1.6 随 v0.9.5 republish。**B/C/D 簇待排期**：B（P1.2 approveRefund 复核进课 / P1.8 库存 CAS / P1.4·P1.5 客服防吞单残洞）· C（P1.1+P1.3 订单 lineId+数量级 ledger·已待决策）· D（控制台 ACL / 真支付退款 / 隐私登记·够不到）。

## 2026-06-25 · ⏱️ 关单回补非原子竞态缓冲（6-agent 审计 P2·钱链边缘·不超卖前提下消误判售罄）
**痛**：超时关单（closeExpiredOrders 定时 / pay 惰性）是「先原子转 closed、再回补预留库存」两步（跨 orders/inventory 集合·无法单事务原子）。晚到的成功 payCallback 若卡在「已 closed 但回补未落定」那一瞬重抢库存，会对**本单自己**尚未还回的预留扣 second 次而失败 → **误判售罄**把已付款单推进 refund_required（钱已收却拒履约·非真缺货）。**抉择**：不能改「先回补后关单」——那会在回补已落、关单未定的瞬时窗被并发新单买走→**重新引入 P0 超卖**（更糟）。故保「先 closed 后回补」（错向安全侧：宁误判可退款·不超卖），在**消费侧**缓冲。**修**：payCallback closed 复活的重抢包 `reserveWithRetry`（瞬时窗很快闭合·重试即成功；**真被别人买走**则重试到上限仍失败→refund_required·不超卖）；sleep 可注入。一处修同时覆盖 cron 关单与 pay 惰性关单两条竞态。closeExpiredOrders 补非原子契约注释。**测试（先红后绿·反向自检过）**：payCallback.test +3 例（失败转成功/真售罄到顶/首发即成）注入 noSleep 不真等；反向自检去重试循环→「失败转成功」红→还原绿。守卫不变（既有 paycallback-revive-reserves-stock 行为锁覆盖）·测试 →**593→后**·云函数 33·check 全绿·build:cloud(33)。**剩靠人**：重部署 payCallback。**诚实**：跨集合无真原子·这是有界重试吸收瞬时窗（根因#8 时序）+ 错向安全；残留仍是「真售罄→可退款」非超卖。

## 2026-06-25 · 📏 学习域按用户读有界（6-agent 审计 P2·上量·防默认 100 截断）
**痛**：`getMyCourses`/`getMyProgress`/`confirmEnter` 对 activations/progress/orders 的本人查询裸 `.get()`——吃 CloudBase 默认 100 上限静默截断：重度用户漏课/漏进度，confirmEnter 漏到要撤退货权的订单（>100 单时退货权失效漏判=可白退·钱）。**修**：三处加 `.limit(200)` 显式上界（本人激活/进度/订单远不及 200·一次读尽不截断；元 mock 不复现默认 100 截断·根因#8 桩≠真）。**守卫**：新 `learning-user-reads-bounded`（roots #7·结构守卫钉这三函数本人读须带 `.limit()`·防回退裸 `.get()`；因 mock 测不出截断故以结构守卫兜·反向自检去 limit→红→还原绿）。守卫 71→**72 repoCheck**·测试 589·云函数 33·check 全绿·build:cloud(33)。**剩靠人**：重部署 getMyCourses/getMyProgress/confirmEnter。**未做（诚实）**：>200 单的极端重度买家仍可能漏（本店规模下不现实·`.limit` 已覆盖现实上量·真正无限只能游标分页·ROI 低暂不做）。
## 2026-06-25 · 🔬 外部视频审核报告评估 + 修我域三处（P2-5/P2-4/P1-3 尾）
**来源**：评估桌面《视频链路多agent审核报告》（基线 6fe5518·14 条）——走 `/deep-audit` 逐条核当前 origin/main：其 P1-3(播放器 courseId 路由)/P1-4(缓存键 courseId) 我已修(#3/#1·含报告要的跨课测试)；P2-2(all-or-nothing)用户拍板 deferred、P2-3(free 段)moot(无免费视频)；admin 发布链(P1-1/2/P2-1)+帮助视频(P1-6/P2-6/P2-7/P3-1)属并行 agent 域、路由给他们；**本批修我域真问题三条**。**P2-5 进度 fail-open**（mp 端 getMyProgress 失败→`remote=false`→ofLesson 退 SAMPLE_PROGRESS 演示进度→真用户看假「已学完」误导）→演示回退由 `DEMO_FALLBACK` 门控、mp(#ifdef)下 false（fail-closed 空进度·`let`+重赋避 no-redeclare）；新守卫 `progress-mp-fail-closed`。**P2-4 trackEvent enteredAt**（=#11·折叠进度只查 activation 存在、不查 enteredAt→「已激活未确认」直调可污染进度/看板）→查询加 `enteredAt:_.neq(null)` 与 getPlaybackUrl/getMyCourses 同闸；扩 `video-url-via-cloud-only` 锁。**P1-3 两尾巴**：① 鉴权失败 redirect 带 courseId 落本课目录(F7)；② 带 courseId 却命中不到课(漂移/下架)→回目录别在错课上播；扩 `playback-course-identity-explicit` 锁。**反向自检×3 均红→还原**。附修 progress.test 改测 mp fail-closed 路径(报告同建议·演示分支编译期 vitest 测不到)。**指标**：rebase 并行「学习域有界」批后守卫 72→**73 repoCheck**(+1 progress-mp-fail-closed)·5 fileRule；测试 589。`npm run check` 全绿·build:cloud(33)/h5 DONE。**⚠️ 待重部署**：trackEvent(P2-4 改了云函数)。**P1-5 退货权粒度**(整行 vs 单件·订单模型改动)留待用户定方向。隔离 worktree·rebase 并行批后 push。

## 2026-06-25 · 🛟 客服消息防吞单（6-agent 审计 P2·崩溃/超时不再永久丢顾客消息）
**痛**：`cs/kfCallback` 原把每条消息**先标 seen 再处理**（firstSeen 认领后才 handleMessage），且单条 `handleMessage` 抛错**未 try/catch**——一条消息 DB 读/send 网络抛错就 unwind 整个 sync 循环：游标不推进 + 已认领消息永久跳过 = 顾客消息被静默吞掉、无重试无告警。**修（抽可测函数 + 防吞单）**：dispatch 抽 `processKfBatch(db,msgs,handleOne)`＝逐条原子认领去重 + **每条包 try/catch**（单条失败不中断整批·其余照常处理）+ 失败时**撤回认领 + alert HANDLE_FAILED** + 返回 anyFailed；index 据 anyFailed **保留旧游标**下次重拉（成功项 seen 去重、失败项重试）。firstSeen 一并移进 dispatch（可测）。抛错路径均瞬时（DB/网络·send errcode 不抛已自 alert）故无 poison-pill 死循环。**测试（先红后绿·反向自检过）**：kfDispatch.test 加 processKfBatch 三例（单条失败不中断+撤认领+anyFailed / 全成功全认领 / 已 seen 去重）；反向自检去掉撤认领→「seen 不含失败项」红→还原绿。守卫不变·测试 →**588**·云函数 33·check 全绿·build:cloud(33)。**剩靠人**：重部署 kfCallback。**并行同步**：ff origin/main `fa7d4f4` 再续·只 add 本批 3 文件 + 本条。

## 2026-06-25 · 🔐 客服回调 receiveId 绑 corpid（6-agent 审计 suspect P1 #4·用户确认企业内部单 corp）
**痛**：`kit/wecom.ts defineKfCallback` 解密后丢弃 `receiveId`、从不校验是否等于自己 corpid（WXBizMsgCrypt 验签三要素缺一）——跨 corp 共享/泄漏 AESKey 的合法签名消息会被本应用处理；且 `decryptKfMessage` 的 pad/msgLen 无边界检查（验签异常路径可吐越界切片）。用户确认威胁模型＝企业内部单 corp。**修**：① `defineKfCallback` 加 `corpid?:()=>string`，GET/POST 解密后 `receiveId !== corpid` 即拒（POST 告警 `RECEIVEID_MISMATCH` 不进 onEvent）——配了 corpid 才生效（fail-closed·未配不阻断现网）；kfCallback 注入 `WXKF_CORPID`。② `decryptKfMessage` 加 pad∈[1,32] + `4+msgLen≤data.length` 边界，非法即抛（调用方 try/catch 转 DECRYPT_FAILED 告警）。**测试（先红后绿·反向自检）**：kfCrypto.test 加「跨 corp receiveId 拒+告警」「本 corp 不误伤」「msgLen 越界抛」；均篡改→红→还原绿。守卫不变·测试 →**585**·云函数 33·check 全绿·build:cloud(33)。**剩靠人**：重部署 kfCallback（你终端）。**并行同步**：先 ff origin/main `22982e1` 再续。

## 2026-06-25 · 🔒 视频审计续·收口 #9/#10 + #5 定性（onLoad 过期回调 + getCourses fail-closed）
**来源**：4-agent 视频审计的剩余条目，用户逐条定夺后收口。**#9 onLoad 过期回调（前端·P2 防御）**：播放页 onLoad async，慢网下 `await Promise.all([load,loadMine])` 期间用户快返离开 → 回调回来仍写已卸载页状态、最坏执行 `redirectTo` 把人从别页拽回目录。修＝`onUnload` 置 `unloaded=true`、onLoad await 后 `if (unloaded) return`（onHide 切后台不算卸载·不置）。**#10 getCourses fail-open（云端·latent 加固）**：课/章/节原 `...c/...ch/...l` 整体展开、只段层白名单——今天不漏（敏感字段都在段），但往课/课时文档加内部字段（成本价/解锁密钥/运营备注）会静默漏进公开返回、且守卫只盯 videoFileId 抓不到。修＝逐层显式白名单 publicCourse/Chapter/Lesson（fail-closed·新增对外字段须显式加）。**#5 退款后仍可看（定性·不改代码）**：核实实情——「确认进课」本身就失效退货权（confirmEnter 置订单 refundable=false + admin 退款审核以 activations 为「已激活=退货权失」判据），**正常路径走不到「退款后还在看」**；唯一能到的是 admin 强行批退（无视失效退货权），那是一单一议的人工灰色（实体材料包+一次性扫码·收回不干净）→ 有意设计、不自动失效 activations，只在 getPlaybackUrl 注释写清「将来若定退款即收回，在退款链回写 activations、别在这加」。**#4 撤销**：用户指出小程序无免费视频→「免费段无限换链」前提不成立。**守卫（先红后绿·均扩现有·无新增）**：扩 `player-resume-no-flash` 锁「onLoad await 后 unloaded 早退 + onUnload 置 unloaded」（#9）；扩 `video-url-via-cloud-only` 锁「getCourses 不裸 ...c/...ch/...l 展开」（#10）。**反向自检×2**：删 #9 早退→红、getCourses 退回 ...c→红，均还原绿。**指标**：守卫 71 不变（扩现有）·5 fileRule；测试 582。`npm run check` 全绿·build:cloud(33)/h5 DONE。**⚠️ 待你重部署**：getCourses + getPlaybackUrl（云函数改了·#10 白名单 + #5 注释）。隔离 worktree·rebase 并行 6-agent 审计批后 push。

## 2026-06-25 · 🛡️ 6-agent 全项目审计·修 P1 三处（重复退款 + 退款超卖 + 播放器导航困死）
**背景**：用户派 6 agent 按非交叉目录树（orders/·learning+catalog/·adminApi/·kit+cs+feedback+system+user/·miniapp/·admin/）全项目找 bug，合并去重**无 P0**，先修最高置信三 P1（动钱/卡用户）。**#1 重复退款窗口**（两 agent 独立确认）：`adminApi/actions/refunds.ts` approveRefund 触发退款失败时**无条件** `.doc().update(applied)` 回滚——callFlow 超时返 null 时微信可能已真退、退款回调抢先 approved→refunded，无条件回滚把 refunded 打回 applied→可二次审批重复退款。修＝条件回滚 `where({_id,status:'approved'})`（只回滚未被回调推进那笔）。**#2 退款超卖**：`orders/refundCallback.ts` 每次成功退款都 restoreStock，但 applyRefund 允许 shipped/done 退款——已发货实物在客户手里、回补凭空多一件→超卖。修＝仅订单 `status==='paid'`（未出库）才回补，读不到/已发货不回补（保守）。**#3 播放器导航困死**：`segNav.js` 跨课时只跳过空课时、不跳「有段没传视频(hasVideo:false)」课时，落到「整理中」占位而底部上/下一段控件 `v-if stage==='ready'` 全隐藏→用户困死回不去（半上线相邻无视频课时·真机）。修＝stepSegment `playable=有段且每段 hasVideo`（=播放器 fileMode 口径）跳过不可播课时。**守卫/测试（先红后绿·反向自检×3 全过）**：#1 给 callFunction mock 加注入钩子 `setCallFunctionImpl` 模拟回调抢先 refunded·adminRefundActions.test 锁条件回滚不 clobber；#2 refundCallback.test 锁 paid 回补/shipped 不回补；#3 segNav.test 锁跳过无视频课时 + fixture 补 hasVideo。三测均篡改源→红→还原绿。**指标**：守卫 71 repoCheck 不变（行为锁走测试）·测试 →**582**·云函数 33·check 全绿·build cloud(33)/h5 DONE。**卷档**：docs-budget 满→06-24 六批卷 archive。**剩靠人（你终端）**：① `! deploy-fns`（推 adminApi[#1]+refundCallback[#2]）② 小程序 #3 随 v0.9.5 republish。suspect P1 #4(kf receiveId)/#5(评价游标)待确认口径·P2/P3 待排期。

## 2026-06-25 · 🐞 4-agent 并行审计修三处（缓存串课#1 + retry 空操作#2 + 课程身份寄生#3）
**来源**：用户要"多 agent 找 bug"，起 4 个并行子 agent（状态机/多端真机/鉴权安全/取址缓存）审主播放器全链路，汇总去重后挑确凿+便宜的三条修。**#1 缓存串课定时炸弹（C+D 独立挖·已核实 seed）**：取址缓存键原只 segId，而种子段 id 课内局部命名 `${lessonId}-s${i}`（lessonId=l1..l16·不带 courseId）跨课不唯一——上第二门课（小熊课在路上）复用课时模板即撞同段 id → 命中别课临时 URL 真机播错课（25min TTL·根因#8 单课样本掩盖）。修＝解析器缓存键 `courseId::segId`、courseId 作参数流入（删模块裸变量 `_courseId`·同时消除审计 #3 的取址竞态串课）。**#2 retry 撞失效缓存（D 补充·我上批的洞）**：错误「点此重试」→ 命中那条 TTL 内仍"新鲜"但服务端已失效的 URL → 重试形同空操作。修＝解析器加 `invalidate`、player retry 先失效本段再重取。**#3 播放页身份寄生全局 currentId（A+C+D）**：入口只传 lessonId，鉴权/定位/续段/返回全靠 store.currentId——多课+跨入口易串课。修＝入口（catalog/me）player URL 带 courseId、onLoad 据 o.courseId setCurrent。**守卫（先红后绿）**：新 `playback-course-identity-explicit`（roots #8·锁 keyOf 含 courseId + store 传 current.id + onLoad setCurrent + 双入口带 courseId）；扩 `player-loading-until-firstframe` 锁「retry 须 invalidate」。**反向自检×2 各逮一次假绿**：#1 守卫初版全文搜 `${courseId}::${segId}` 串、被我自己写在注释里的同字样匹配致假绿 → 锚定 keyOf 定义本身才真红；#2 retry 限函数体内匹配。**指标**：守卫 70→**71 repoCheck**·5 fileRule；测试 +6（playbackCache 跨课隔离/失效 8→11 + 其它）→ 578。`npm run check` 全绿·build:h5/mp-weixin DONE。**审计未修（待你定/真机）**：#4 getPlaybackUrl 缺频控（改云函数·待重部署）、#5 退款后仍可看（产品决策）、原生 video 同层渲染无 cover-view 兜底（真机·最大风险）、横屏旋转几何、底部 env() 等。隔离 worktree·rebase 并行批后 push。

## 2026-06-25 · 🔧 播放器广度审计修两处（F1 错误态黏住 + F3 进度条首触不响应·/frontend-check）
**来源**：`/frontend-check` 广度审计主播放器全链路。**F1（P1·静态可判）**：`stage` 一旦 `videoError=true` 恒为 `'error'`，而 videoError 只有点重试/切段才清——`onPlay` 不清它 → 任何瞬时 `@error`（切后台回前台/网络抖动/src 空一瞬）后即使视频已正常播放，「加载失败·点此重试」浮层仍盖死、声音照出、不自消。修＝`onPlay` 真在播即 `videoError=false`（自愈）。**F3（P2·真机）**：`measureScrub` 在 `onLoaded` 时 stage 多半还是 loading、底部控件未渲染 → 量不到进度条几何 → 首次点/拖进度条用空几何不响应。修＝`watch(stage)` 进 ready 后 `nextTick(measureScrub)` 补量。**守卫（先红后绿）**：扩 `player-loading-until-firstframe` 加「onPlay 须清 videoError」一项（错误态自愈）；**反向自检暴露守卫 bug**——初版正则 `[\s\S]*?` 跨函数误匹配 retry/goSeg 的 videoError 清除→删 onPlay 里的仍假绿，收紧为 `function onPlay\(\)\s*\{[^}]*…` 限本体内→反向自检真红→还原绿。**指标**：守卫 70 不变（扩现有·非新增）·5 fileRule；测试 572。`npm run check` 全绿·build:h5/mp-weixin DONE。**未修（待你定/记录）**：F2 object-fit cover 横屏裁切（待确认素材朝向）、F4 切段缓冲无 spinner（刻意取舍）、F5/F6/F7 P3。隔离 worktree·rebase 49f0ec3 后 push。

## 2026-06-25 · 🔙 播放页返回固定指向「本课程目录」（用户报·根因#8 真机才显）
**痛**：播放页返回键走 `goBack('/pages/catalog/index')`——`goBack` 栈里有上一页就 `navigateBack`，于是从「我」页续看 / 我的课程列表进来时，返回回到**来源页**而非课程目录（仅冷启/深链无历史才 reLaunch 到目录）。用户拍板：退出/返回应固定落「本课程目录」。**修**：`back()` 改判——上一页是 `pages/catalog/index` 则 `navigateBack`（保留目录滚动）；否则 `redirectTo /pages/catalog/index?courseId=<本课>`（带 courseId 聚焦本课·不回来源页）。去掉不再用的 `goBack` import。**守卫（先红后绿）**：新 `player-back-to-catalog`（roots #8）——锁 player back `redirectTo /pages/catalog/index?courseId=`（带 courseId 区别于鉴权失败那条无 courseId 的 redirect）。**反向自检**：去掉 `?courseId=`→守卫红→还原绿。**指标**：守卫 69→**70 repoCheck**·5 fileRule；测试 572。`npm run check` 全绿·build:h5/mp-weixin DONE。**并行纪律**：本批在隔离 worktree 做、rebase 并行「辅助视频」批后 push（[[dont-commit-user-live-edits]]）。

## 2026-06-25 · 🆘 帮助视频上传管理后台（求助面板辅助视频接云·全流程·用户请求）
**痛**：小程序播放页「求助 →遇到问题了」的辅助视频（如起手结/拆线重来/排查卡点）原是前端硬编码样例（`HelpSheet/data.js` TROUBLE）+ 计时模拟播放，**无后台可上传管理、不放真视频**。用户拍板：**全局共用一份**（通用钩织技法·与具体课程无关·每课重传不合理）+ **打通完整链路**。**做（cloud+admin+miniapp 三端）**：① **云存储**——存 `content` 集合 doc `helpVideos`（复用 home 内容模式·非新集合）；新增管理端 action `listHelpVideos`/`saveHelpVideos`（actions/content.ts·白名单限长 + 封顶 20 条 + 剔空·读回原始 fileID 因已过口令闸）；新公开读云函数 `catalog/getHelpVideos`（withOpenId·按 fileID 服务端换短时临时 URL·`videoFileId` 不出接口）。② **admin 管理页** `HelpVideos.vue`（增删改 + 拖拽排序 + 每条传视频[复用上新第④步直传通道 `uploadVideo('help',…)`]·防抖自动保存）+ 路由 `/help-videos` + 侧栏「经营」组加「帮助视频」（LifeBuoy 图标）。③ **小程序接真**——`api/help.js getHelpVideos`；HelpSheet 壳首开拉一次经 `items` 传入；TroublePanel 去模拟计时、改真 `<video>`（src=临时 URL·无视频显占位「整理中」）+ 空态；data.js 删 TROUBLE 样例。**守卫（先红后绿·反向自检过）**：新 `help-video-url-via-cloud-only`（roots #3·仿付费版 video-url-via-cloud-only）——catalog 域内帮助视频临时 URL 只许 `getHelpVideos.ts` 经 getTempUrl 下发、其他 catalog 函数引用 `videoFileId`/`getTempUrl` 即红（防漏 fileID 进公开返回·合规红线·通用免费内容故无 NOT_ENTITLED 权属闸）。`interface-catalog-sync` 自动咬住三接口登记系统事实。**反向自检**：getContent.ts 加引用 `videoFileId`→守卫红→还原绿。**指标**：守卫 67→**68 repoCheck**（+1）·5 fileRule；测试 565→**570**（+5：getHelpVideos 公开读契约 2[空/换临时URL不漏 fileID] + adminPipeline saveHelpVideos 净化/剔空 + 取回 3）；云函数 32→**33**。`npm run check` 全绿·build:cloud(33)/h5/admin DONE。**剩靠人**：① 后端部署 `getHelpVideos`+`adminApi`（你自己终端·CC 跑 deploy-fns 绕 guard-deploy）② admin 重部署静态托管 ③ 控制台传真实帮助视频 ④ 小程序改动随 v0.9.5 republish 生效（republish 前先在微信后台「下载合法域名」加 `videos/` 临时 URL 域名·否则正式版黑屏）。**首版已部署上线后**（后端 `2a4155e`/`6f510bf`·admin index-C8_KaXAk）。**续（用户追加·`<本批>`）：改两级「主题→小段」**——一个主题（如「起手结」）下分多个小段连续播（同课程播放器分段思路）。数据模型 `helpVideos.items[].videoFileId` → `items[].segments[]{id,name,dur,videoFileId}`；getHelpVideos 嵌套换临时 URL（每段 url·fileID 不出口）、saveHelpVideos 两级白名单（主题封顶 20×每主题段封顶 20·剔无视频段·剔空主题）；admin HelpVideos.vue 升两级编辑器（主题/小段各增删 + 多列表拖拽·复用 StepVideos 拖拽范式）；小程序 TroublePanel 去单视频、改**分段播放**（顶部 stories 式分段进度条 + 段末 autoplay 接续 + 点段跳转·@timeupdate 驱动填充）。守卫 `help-video-url-via-cloud-only` 不变仍咬（嵌套 fileID 仍只在 getHelpVideos.ts 经 getTempUrl·反向自检换掉 getTempUrl→红→还原）。测试同数（getHelpVideos/adminPipeline 两级重塑·非增减）·守卫 69 repoCheck（含并行 main-no-subpackage-import）不变·云函数 33·check 全绿·build cloud(33)/h5/admin(index-B0sqOmoz) DONE。**并行同步**：本批前先 ff origin/main `df9c605`（含并行白屏修）再续。**两级版已部署生产**（`49f0ec3` 记账·deploy-fns 推 getHelpVideos+adminApi + admin index-B0sqOmoz·先 ff 并行 `f3bb377` 返回修）。**再续·精简求助菜单（用户报「有些冗余选项」）**：删求助面板两项——「加入学习交流群」（纯占位·假二维码/假群号/假人数·复制只 toast）+「反馈视频问题」（提交只 toast 不入库·与在线客服重复）；删 GroupPanel.vue/ReportPanel.vue + HELP_OPTS 去两项 + data.js 去 REPORT_TYPES + brand.js 病根#5 注释去 GroupPanel。保留 客服/帮助视频/常见问题 三项。纯前端·零守卫（删占位非新不变量）·check 全绿 572·build:h5 DONE·随 v0.9.5 republish。
## 2026-06-25 · 🐛 修「我」页真机白屏：playbackCache 从分包挪进主包 utils（mp-weixin 主包不能 require 分包·根因#8）
**痛（真机调试才暴露·H5 假绿·`npm run check` 测不出）**：上两批把分段地址解析器 `playbackCache.js` 放在 `pkg-video/`（**分包**），让主包 `store/courses.js` import 它。mp-weixin **硬规则：主包不能 require 分包模块**——构建产物 store/courses.js 出 `require("../pkg-video/player/playbackCache.js")`，真机加载时找不到 → `useCoursesStore` 模块求值即抛 → 用到它的「我」页白屏（catalog 等同理）。H5 无分包概念故构建/预览全绿、机器守卫也没这条 → 典型「构建过≠真机能用」（根因#8）。**修**：`git mv` playbackCache.js → 主包 `utils/`（store 在主包·主包 import 主包 utils 合法；player 经 store 方法用·从不直引该文件）；改 store/test import 路径 + `player-playback-prefetch-cache` 守卫路径同步。**守卫（先红后绿）**：新 `main-no-subpackage-import`（roots #8）——扫主包源码（src/ 除 pkg-video/pkg-extra）禁 import/from/require 指向 `@/pkg-video|pkg-extra`（navigateTo 路由字符串放行·只扫引模块语句），把这一**类**真机白屏一次性焊死。**反向自检**：store 改回 import 分包路径→守卫红「主包 import 了分包模块」→还原绿。**验证**：worktree build:mp-weixin 产物 store/courses.js 现 `require("../utils/playbackCache.js")`、playbackCache 落主包 utils（分包里已无）。**指标**：rebase 并行「辅助视频」批后守卫 68→**69 repoCheck**·5 fileRule；测试 570（playbackCache 测随迁·无新增）。`npm run check` 全绿·build:mp-weixin DONE。**并行纪律**：发现时主树有并行 agent 未提交的「辅助视频」feature（含其 check-structure/现状与路线 改动）→ 本修在**独立 worktree（基于 origin/main a9bd55a）**做、push 抢先、并行 agent 自行 reconcile，全程不碰其工作树（[[dont-commit-user-live-edits]]）。

## 2026-06-25 · 🎬 首次进课不黑屏：列表页预取首段 + 播放页加载态/兜底（视频提速·用户请求 ①+②）
**痛（根因#8 真机才显·dev 快网掩盖）**：首次进课，视频区在「拉课程→鉴权→取址→缓冲首帧」整段只是 #000 黑底——① 无任何加载提示；② `placeholderMode=!fileMode` 在加载途中误闪「课程视频整理中」；③ 「目录标了 hasVideo 却取不到地址（服务端无 videoFileId/未授权/网络失败）」时**永久黑**。**做（纯前端·零云函数改动）**：**① 列表页预取**——catalog `onShow` 数据就绪后对已解锁课「续看课时」首段 `store.prefetchPlaybackUrl`，进播放页 `refreshPlayUrl` 直接命中跨页共享缓存、省掉那趟 getPlaybackUrl 往返（课程/鉴权进页时已热，取址是唯一剩的延迟）；**② 播放页加载态**——`onLoad` 把 `store.load`+`act.loadMine` 改 `Promise.all` 并行（少串一趟）；引 `dataReady/firstFrame(sticky)/videoError` → `stage` 三态机（loading/placeholder/error/ready）：加载浮层（三点+「正在加载课程视频…」纯 CSS）盖到首帧（onPlay）真正出来、`@error`/取址空 → 「视频加载失败·点此重试」兜底，且「整理中」只在加载完确无视频时显（不再途中误闪）。控件统一按 `stage==='ready'` 显。**守卫（先红后绿·反向自检过）**：扩 `player-playback-prefetch-cache` 加「catalog 列表页须 prefetchPlaybackUrl」一项；新 `player-loading-until-firstframe`（roots #8）锁 firstFrame + stage='loading' + vp-loading 浮层 + @error/videoError 兜底。附修 `player-resume-no-flash` 正则（认 `await Promise.all([store.load…])` 并行形态·不再死认串行 `await store.load`）。**反向自检**：删 player firstFrame→红；去 catalog 预取→红；均还原绿。**指标**：守卫 66→**67 repoCheck**（+1）·5 fileRule；测试 565（无新测·UX/生命周期靠结构守卫 + 既有 playbackCache 测）。`npm run check` 全绿·build:h5 DONE。**剩靠人**：随 v0.9.5 republish 真机验首屏（黑→加载态）+ 段间命中（dev 测不出·根因#8）。

## 2026-06-25 · ⚡ 分段视频地址「缓存 + 预取」（视频加载提速·用户请求 ①⑤）
**痛（根因#8 真机才暴露·dev/模拟器快网掩盖）**：播放器原只在「段 id 真变」那刻才云调 getPlaybackUrl 换临时 URL（watch 触发），当前段播放期间下一段地址闲着没取、回看过的段也重新现取 → 切段/续播要先等一个云往返才起播。**做（纯前端·零云函数改动）**：抽纯函数解析器 `pkg-video/player/playbackCache.js`（`createPlaybackResolver`）—— ⑤ 缓存（按 segId+TTL，保守 25min<服务端临时 URL 过期；命中即返回·回看/重复段零取址）+ in-flight 去重（同段取址在途复用同一 Promise·不双取两个不同临时 URL 落同一 src 重载闪）+ ① 预取（prefetch 静默预热·失败不影响主流程）。store/courses.js 经解析器收口 `playbackUrl`（缓存优先）+ 新 `prefetchPlaybackUrl`；player `onLoaded`（当前段就绪）经 stepSegment 定位下一段→预取，用户点「下一段」时直接命中缓存（接近秒切）。`lastFetchedSeg` 同段去重保留（缓存返回同串本就不重载·双保险）。**守卫（先红后绿）**：新 `player-playback-prefetch-cache`（roots #8）锁 playbackCache 导出 createPlaybackResolver + store 经它收口且有 prefetchPlaybackUrl + player 预取下一段；`tests/playbackCache.test.js` 8 例钉缓存命中/过期/空不缓存/in-flight 去重/预取预热/已新鲜 no-op。**反向自检**：删 player 预取调用→守卫红「未预取下一段」→还原绿。**指标**：守卫 65→**66 repoCheck**（+1）·5 fileRule；测试 +8（playbackCache）。`npm run check` 全绿·build:h5 DONE。**剩靠人**：随 v0.9.5 republish 真机验段间转场感知（dev 快网测不出差·根因#8）。**并行插曲**：本批进行中用户在同主树并行提交了 admin 批量传视频（`0dd53ed`），本批 6 文件与其正交、settled 后干净叠在其上（[[dont-commit-user-live-edits]]·只显式 add 本批文件不 -A）。

## 2026-06-25 · 🎬 上新第4步「课时级批量传视频」（用户请求·一次多选灌进小段）
**痛/做**：教学视频原「一小段一传」（每段一传按钮·`pickVideo` 一次一文件），课时小段多时逐个点费手。用户拍板方案 A：课时层加「⬆ 批量传」一次选 N 个视频 → 按文件名 numeric 排序依次灌进小段、段不够自动新建（段名取文件名·已存在段保留原名只灌视频）、串行逐个传带每段进度 + 课时级「上传中 i/N」。fail-soft：单文件失败 toast 跳过其余照传（含 >15MB 被 uploadVideo 拒）。**纯前端便利·零后端**（逐文件复用 `uploadVideo` 直传 COS+分片回落·不碰播放器/数据模型·视频仍绑小段、播放器仍按段播）。DRY 抽 `uploadOne(sg,file)` 单传+批量共用。**守卫（先红后绿·反向自检过）**：纯映射逻辑抽 `utils/videoBatch.js`（`planLessonBatch`/`stripExt`）+ `tests/admin/videoBatch.test.js` 9 例钉顺序/复用/新建/numeric 排序/stripExt 边界；反向自检 isNew 阈值 off-by-one→2 断言红→还原绿。check 全绿（65 repoCheck+5 fileRule·admin 测试 +9）·build:admin DONE。**剩靠人**：build:admin 重部署 + cloud 模式真传眼验（dev 无 VITE_ADMIN_API 不真传）。

## 2026-06-25 · 🧭 控制台侧栏按设计稿重做（步骤补图标 + 分组小标题·UX）
**痛/做**：侧栏与 `design/console.pen`「Component/Sidebar」漂移——6 步骤项丢了图标改纯文字左缩进、步骤被后加模块挤到中段、11 项平铺。逐属性对齐设计稿：① 6 步补回 lucide 图标（image/file-text/tags/clapperboard/qr-code/printer·17px·设计同序）去左缩进；② 步骤挪到「商品与上新」正下方；③ 其余 7 模块按 经营/数据/系统 三组加小标题（设计稿小标题语言延伸到 v1 后新增模块·用户拍板分组）；④ 选中项图标改品牌紫（标签仍 purple-ink·NavActive 一致）；⑤ 商品与上新图标 Store→Package（贴设计）、nav gap 8→10、图标 16→17。logo🦆 保留（设计本身即 emoji）。**续（用户拍板·看 dev server 后）**：六步收进「商品与上新」内——改可展开父项 + 折叠箭头（ChevronDown），进上新区（列表/向导）`watch` 自动展开、别处可手动收起（手风琴·子项缩进 + 左连接线·侧栏更清爽）。**无守卫**：图标选型/分组 IA 属 cosmetic·非机器可判（同前两批侧栏·[靠人] 范式·硬加反成过度工程）故无反向自检。check 全绿（65 repoCheck+5 fileRule·546 测试不变）·build:admin DONE。**剩靠人**：build:admin 重部署 + 眼验。

## 2026-06-25 · 💀 加载骨架 Skeleton（数据页加载态 纯文字→shimmer·UX）
**做**：新 `components/Skeleton.vue`（N 条 shimmer 占位条·props rows/height）替 5 个数据页加载态纯文字——订单/退款/对账/库存表格(card 内 6~7 行) + 看板(4×72 块)；撑住将到版式·减少布局跳动 + 感知更快。**克制（防过度工程）**：只上表格/看板数据页（骨架贴表格才有意义）；向导步/表单加载（StepVideos/Cards/Batch/Showcase/Notifications）留文字「加载中…」（表单上骨架反而怪）。**无守卫**：cosmetic（admin 不像 miniapp 立 list-pages-skeleton·5 页低风险·[靠人] 范式）。check 全绿（65 repoCheck+5 fileRule·546 测试）·build:admin DONE。**剩靠人**：build:admin 重部署 + 眼验。

## 2026-06-25 · 🎨 全站图标 emoji→lucide 收尾（页面级 chrome 图标·UX 一致性·续侧栏批）
**做**：页面级 **chrome 图标** emoji 换 lucide——5 个 `↻ 刷新` 按钮(订单/对账/退款/库存/看板)→`RefreshCw`、订单 `⚠ 去核对`→`AlertTriangle`、上架 CTA `🏪`→`Store`、批次 `📱 测试`→`Smartphone`、外部后台 7 张直达卡 emoji→lucide(Database/Package/Activity/MessageSquare/Wallet/Wrench/Headphones)。**克制（防过度工程）**：只换交互 chrome 图标；**留**文本性/装饰 glyph——内联 `✓` 确认字（"已保存✓"等 24 处·在动态字符串里·componentize 纯 churn）、品牌 🦆、图片占位 📦、`↗` 链接箭头。**无守卫**：图标选型 cosmetic·非机器可判（同侧栏批·[靠人] 范式）。check 全绿（65 repoCheck+5 fileRule·546 测试）·build:admin DONE。**剩靠人**：build:admin 重部署 + 眼验。

## 2026-06-25 · 🔔 统一操作反馈 toast + 确认/输入弹窗（替 29+1 处原生 alert/confirm/prompt·UX P1）
**痛**：紫色卡片/抽屉体系里一报错/确认/输入就弹浏览器灰色原生框（阻塞线程·视觉断裂），全站 30 处。**做**：新 `utils/ui.js`（toast 右上淡出 + confirmDialog`Promise<bool>` + promptDialog`Promise<string|null>`）+ `FeedbackHost.vue`（toast 栈 + 居中确认/输入弹窗·遮罩/Esc 取消）挂 App.vue 一次；9 页 30 处全替（confirm→`await confirmDialog`·handler 转 async；validation `return alert`→`return toast(_,'err')`；成功→toast ok；prompt→promptDialog）。**守卫**：eslint `no-alert` 锁 `packages/admin/src`（比 check-structure 更地道·防回归）——先开规则红 30→替到绿。check 全绿（65 repoCheck+5 fileRule·546 测试）·build:admin DONE。**反向自检**：注入 alert→lint 红→还原绿。**剩靠人**：build:admin 重部署 + 眼验 toast/弹窗。

## 2026-06-25 · 🎨 控制台侧栏图标 emoji→lucide（P3·用户拍板覆盖「不做」建议）
**痛/做**：admin 侧栏原用 emoji（🏪📦💰…），用户要对齐设计稿 lucide。加依赖 `lucide-vue-next`，Sidebar 9 导航项换 `<Store/Truck/RotateCcw/Boxes/ChartColumn/Wallet/Bell/ExternalLink/Smartphone :size=16/>`（图标 currentColor 跟随 .nav/.nav.on 颜色态）+ 步骤直达项左缩进对齐；logo 🦆 保留（品牌·lucide 无鸭）。**范围克制**：只换侧栏（用户点名）；页面级 emoji（Externals/刷新键等）不动——全站换才一致但属另一决策。**无守卫**：图标选型 cosmetic·非机器可判不变量·不配守卫（同 [靠人] 范式，硬加反成过度工程）；故无反向自检。check 全绿（65 repoCheck+5 fileRule·546 测试不变）·build:admin DONE（+~7KB·tree-shaken 9 图标）。**剩靠人**：build:admin 重部署 + 眼验。

## 2026-06-25 · 📦 批量发货 shipOrders（P1·上量硬瓶颈·补设计稿 S14 批量发货缺口）
**痛**：原 Orders 只能单单发（抽屉一单一发），上量后逐单是吞吐瓶颈。**做**：抽 `shipOne` 核心（shipOrder/shipOrders 共用·DRY 单源·状态闸+feeMismatch+条件转移+微信 upload_shipping_info 合规上报全保留）+ 新 action `shipOrders`（逐单串行·一单失败不拖累其余·per-order okCount/failCount/results·上限 100/批）；前端 Orders 待发货 tab 行多选+全选+浮现批量操作栏→批量发货面板（选快递公司+逐单填运单号→shipOrders·成功移出/失败留重试）。**诚实裁剪（根因#8）**：电子面单取号/打印需快递平台 API 账号·延后，当前手动填运单号（面板如实标）。**指标**：守卫数不变（行为走 `shipOrders.test.js`·shipOrder 老测试仍绿证抽取无回归）；测试 543→**546**；`shipOrders` 自动审计（shouldAudit 非 list/get/upload）；注册 ACTIONS+系统事实（admin action→43）。`npm run check` 全绿（65 repoCheck+5 fileRule）·build:cloud(32)/admin DONE。**反向自检**：shipOne 关 feeMismatch 闸→批量测试红（okCount 2≠1）→还原绿。**剩靠人**：重部署 adminApi + build:admin + 真机验批量发货。

## 2026-06-25 · 📊 S15 微增强 Dashboard：上新未完成待办卡 + 最近动态混合流（不单建工作台）

**来源**：设计稿 S15 工作台与现 Dashboard 重叠 ~70%，用户拍板「不单建独立页·微增强 Dashboard」（避免两块近一样的屏·§7 防过度工程）。
- **上新未完成卡**（前端·零后端）：待办行动条加第 5 卡，`store.statusOf(p)==='preparing'` 计数（S11 同口径·商品草稿全量小），点进 `/products`。
- **最近动态混合流**（后端 `getDashboard`）：原「最近订单」升级为 `recentActivity`——近期 订单(createdAt) + 激活/进课(activations·进课取 enteredAt) + 退款申请(afterSales.appliedAt) 合并按事件时间倒序取 8；各路 `orderBy+limit(6)` 有界（债#18·不扫全表）；前端「最近动态」区带类型标签。
- **诚实裁剪（根因#8）**：S15 第 6 卡「低激活率批次」**本批不做**——需逐批激活率聚合（热路径扫 qrcodes＝容量回归#18，或 group-by-batch 聚合＝桩改）+ 阈值/账龄产品判断（机械计数会把新批次误报成待办），留独立批次定阈值再做。
- **净化指标**：守卫数不变（行为不变量走 `dashboard.test.js` recentActivity 合并/排序断言）；测试 **542→543**；`npm run check` 全绿（65 repoCheck+5 fileRule）、build:cloud(32)/build:admin DONE。**反向自检**：recentActivity 排序改升序→测试红（at 序与 'o2' 断言双红）→还原绿。**前序**：我并行做了一版 S16（financeLedger/Finance.vue）撞车——并行 agent 已在 origin/main 落更完整 S16（外部对账单端到端），我 `reset --hard origin/main` 弃冗余提交、采纳其版（教训：开工前先 fetch 探竞争）。**剩靠人**：重部署 adminApi（getDashboard recentActivity 生效）+ build:admin 重部署 + 真机验。

## 2026-06-25 · 🔗 S16 外部对账 Batch 2-3（微信支付 v3 账单接缝 + 逐笔比对）

Batch 1（内部账）合 main+部署后接 Batch 2：建微信支付商户 API v3 接缝拉权威账单（云开发支付未封装账单下载·只能商户 API 直取）。新 `kit/wxpay.ts`（根因#12 接缝单点·与 flow.ts 并列）：`wxpaySign` v3 RSA-SHA256 签名 + `parseTradeBill` 交易账单 CSV 解析（按表头取列/去反引号/跳汇总区）+ `fetchTradeBill` 两段签名拉取（可注入 fetch·fail-soft）；`actions/wxbill.ts downloadBill`（读 config.pay.subMchId+env 凭证 → upsertBills 确定性 _id 幂等落 `wxBills`·缺凭证 fail-soft）；集合 23→24。守卫：`tests/cloud/wxpay-bill.test.js`（**测试密钥对验签密码学正确**·非字符串形状·根因#8 拿到≠用通 / CSV 解析 / 注入下载 / 幂等 / 缺凭证 fail-soft）+ 结构 `wxpay-seam-single`（主机 `api.mch.weixin.qq.com` 只许 kit/wxpay·64→65 repoCheck）。**反向自检×2**：签名缺尾 \n→验签红 / 别处引主机→结构红，均还原。check 全绿（85 文件/**538 测试**）+ build:cloud(32)。**Batch 3 逐笔比对已建**：`getBillMatch`（我方付款单 ⋈ wxBills by transactionId → 四类[平/微信有我方无/我方有微信无/金额不符]·账单覆盖日防误报）+ 前端 Reconciliation 页加「微信账单对账」区（拉取并对账 + 差异清单）+ 守卫 `bill-match.test.js`（反向自检×1：去覆盖防误报→红）。**adminApi 已部署**（spike 用·含 downloadBill 诊断增强·只部署 adminApi 不碰钱链函数·用户授权）；用户确认**普通商户**（不验服务商·无需 sub_mchid）；env `WXPAY_MCH_*` 已设。check 全绿（86 文件/**540 测试**·65 repoCheck）。⚠️**事故**：核实环境变量时跑 `tcb fn detail` 回显值致**商户私钥泄露进对话**——用户拍板先用泄露钥搭完→验钱链畅通→最后多证书平滑换钥（详独立记忆 wxpay-key-leak-incident）。**未合 main·真账单端到端待用户在控制台拉一次确认**（根因#8）。

## 2026-06-25 · 🧭 防过度工程流程补丁：业务预留分流 + skill 落地

**痛**：防过度工程 loop 首轮把 `MediaSlot` future media types、`logger` error-report hook、`TroublePanel` 真素材说明误判为投机抽象；用户纠正为业务明确预留。**修法**：① `CLAUDE.md` §7 加「业务预留分流」：遇「以后/预留/将来」先查需求/ADR/现状路线/用户明确意图，能对上即保留，不确定先问；② ADR §22 加边界补丁「未来式 ≠ 投机式」与三分流；③ 新增本机 skill `/anti-overengineering-check`（`~/.codex/skills/anti-overengineering-check`）固化约束→具体实现→脏样本/characterization test→抽象交学费→小批次 check；④ 独立 agent forward-test 通过，能保住业务预留并指出已实现链路的过时「以后/将来」注释；⑤ 小批次只清注释/README（首页云端 content、segment progress、登录 token、media 目录归属），不动运行时。**证据**：`npm run check` 全绿（64 repoCheck+5 fileRule·526 tests）；skill `quick_validate.py` 通过。**性质**：纯流程治理 + 注释校准；不配机器守卫，因业务预留 vs 投机抽象仍是判断题，硬 grep 会反成过度工程。

## 2026-06-25 · 💰 S16 财务对账 Batch 1（内部账·控制台最后一屏起步）

接 P0/P1 安全修部署（createOrder/payCallback 防超卖等 4 函数上生产）后开 S16；用户拍板「完整外部对账」，去风险先落 **Batch 1 我方账**（不依赖商户凭证）。后端新 `getReconciliation`：累计走 aggregate `$.sum` 精确（不封顶·债#18续）、每日 CST 日分桶（有界拉取+approx·同 dashboard SAMPLE 诚实范式）、内部异常复用 `getTxAlerts`（与 `PAID_STATUSES` 抽进 `lib.ts` 与看板单源·防口径漂移·同 `activationFor` 范式）；金额累加整数分 `toFen/asFen/fenToYuan`（根因#4 避浮点）。前端 `Reconciliation.vue`（累计/区间收支卡+每日流水表+异常区+日期范围+CSV 导出）+ api/路由/侧栏。守卫 `reconciliation.test.js`（精确/范围/分桶/异常同源·**反向自检×1**：去 inRange 边界→红→还原）；登记 `系统事实.md`。check 全绿（84 文件/**530 测试**）+ build:cloud(32)+build:admin；**未部署·未合 main**。**外部对账（微信账单逐笔比对）= Batch 2/3·需商户 APIv3 凭证**（商户号 1113881793 自有·见 `console-assets/01`）。

## 2026-06-24 · 🧹 结构收束·守卫读声明 + 文档对齐 refund_required（结构审核 #4/#5·根因#2）

**来源**：桌面《结构收束与整洁度审核》（写于 `83b3e73`·判 PARTIALLY CONVERGED）。其头号 P1（停售没收束到交易入口）已由 `6cadb65` 闭环；本批收它指出的两处真缺口（#4 守卫字符串假设 / #5 文档滞后）——均是 P0 加 refund_required 时留的尾巴。
**#4 守卫读声明（让机器读声明·不靠正则/枚举追状态·本仓元模式核心）**：① `order-status-frontend-via-shared` 的状态枚举从**硬编码** `pending|…|rejected`（漏 refund_required）改为**从 `order-domain.generated.json` 派生**（orders+afterSales 全状态）——新增状态自动纳入覆盖；② `order-transitions-declared` 的 from/status 字面量正则 `[a-z]+`→`[a-z_]+`，认下划线状态名（refund_required 当前是终态 to·走 `\w+` 没被坑，但下划线 from/写库字面量是潜在漏捕边界）。
**#5 文档对齐**：`业务逻辑架构.md` 订单状态机图加 `closed→refund_required` + 重抢库存规则说明；`系统事实.md` payCallback 响应/webhook/timer 三行从「`closed→paid` 盲复活」更新为「重抢库存成功才复活·售罄进 refund_required + PAID_BUT_OOS 告警」。
**净化指标**：守卫数不变（硬化两条现有 repoCheck·非新增）、测试 519 不变（守卫内部逻辑·repoCheck 由 check-structure 运行 + 反向自检验证·无单测形态）。`npm run check` 全绿（纯守卫脚本 + 文档·无运行时改动·无需 build）。**反向自检×2**：① 前端裸写 `=== 'refund_required'`→order-status-frontend-via-shared 红（旧硬编码枚举会漏）→还原绿；② 注入未声明边 `transition(['refund_required'],'paid')`→order-transitions-declared 咬「未声明边」红（旧 `[a-z]+` 会漏捕下划线 from）→还原绿。**仍开放（结构审核 #2·较大）**：refund_required 后台退款执行闭环（admin action+审计+tab+测试+守卫）列后续·待办#5b。生产仓 `2dd5a72` 零改动。

## 2026-06-24 · 🔐 P1 admin 频控加固·全局/账户级失败兜底（审核 P1·根因#13）

**痛（上线前审核 P1）**：adminApi 认证频控 key 取 `x-forwarded-for` 首个 IP（`index.ts` clientKey）——该 header **可伪造**，攻击者每次换 IP 可让 per-IP 失败计数永不达 5 次锁定阈值，**轮换伪造 header 绕过爆破锁**。原测试只覆盖「同 header 5 次失败锁定」「不同 IP 隔离」，没覆盖轮换攻击。
**不变量**：admin 认证防爆破不能只靠 per-IP；须有跨所有 IP 的**全局/账户级失败兜底**。
**修法（先守卫红→绿）**：保留 per-IP 细粒度隔离，叠加 `GLOBAL_KEY='adminlogin:global'` 全局失败计数（阈值 20/10 分·高于 per-IP 5·单管理员正常极少触发）：① `throttleGate` 查 per-IP 或全局**任一锁定即拒**（Promise.all 取较长剩余）；② `throttleFailBoth` 失败时两维度同记；③ 成功只清 per-IP，全局靠滚动窗口自然衰减（不被单次成功抹平·防分布式爆破信号丢失）。代价：全局锁期间所有 admin 登录受阻（攻击者可短时 DoS 真管理员），但 lockMs 仅 5 分自愈、控制台内部用·可接受（已注释标明）。
**新守卫 `admin-throttle-global-backstop`**（test 型 17→18·roots `#13`·reverseTest=adminThrottle.test.js）锁「轮换伪造 x-forwarded-for 仍在全局维度触顶锁定」；既有结构守卫 `admin-login-throttled`（throttleLocked/throttleFail 在场）仍绿（被 throttleGate/throttleFailBoth 内联调用）。
**净化指标**：repoCheck 仍 64 + fileRule 5、test 守卫 17→18；测试 **517→519**（adminThrottle +2：轮换 header 全局锁/全局阈高于 per-IP 不误伤别 IP）。`npm run check` 全绿、build:cloud(32) DONE（纯云函数）。**反向自检**：throttleGate 去全局只查 per-IP→轮换 header 测试红→还原绿。**仍靠人（审核留痕）**：生产验证 CloudBase 网关是否强制覆盖 `x-forwarded-for`（若覆盖则 per-IP 本就可信·全局是额外加固；未证实前 per-IP 不算强边界）——记 `待办与债`。**剩靠人**：部署 adminApi。生产仓 `2dd5a72` 零改动。

## 2026-06-24 · 🚫 P1 停售商品挡交易入口（审核 P1·债#12·根因#3）

**痛（上线前审核 P1）**：软下架（`unpublishProduct` 置 `listed:false`）此前只挡 `getProducts` 列表过滤，**`createOrder` 不校验 listed**——顾客从旧购物车/页面缓存/直调云函数仍能买已停售商品。另一隐患：`publishProduct` 整文档 `set` 不含 listed→编辑并重新上架已停售品时 listed:false 被抹掉，按「无字段=可售」**隐式复活销售**。
**不变量**：停售商品在**交易入口**也 fail-closed（不止列表）；重新上架不隐式改变销售状态。
**修法（先守卫红→绿）**：① `createOrder` 条目循环内、定价/预留库存**之前**对主商品 `p.listed === false` 拒单 `UNLISTED_ITEM:<id>`（整单拒·不建单·不扣库存·根因#3 信任边界）；② `publishProduct` 读 `exist.listed` 经 `...carry` 保留旧 `listed`/`unlistedAt`（已停售品编辑重发仍停售·须显式 `republishProduct` 恢复）。
**新守卫 `createorder-rejects-unlisted`**（test 型 16→17·roots `债#12`/`#3`·reverseTest=createOrder.test.js）锁「下单拒停售品·不扣库存 + 重发保留 listed」——补审核 P2 指出「`product-unpublish-effective` 守卫只覆盖列表/管理端、漏交易入口」缺口。
**净化指标**：repoCheck 仍 64 + fileRule 5、test 守卫 16→17；测试 **512→517**（createOrder +3：停售拒单/夹带整单拒/在售不受影响；productListed +2：重发保留 listed:false/在售保持可售）。`npm run check` 全绿、build:cloud(32) DONE（本批纯云函数·无前端改动）。**反向自检×2**：① 去 createOrder 停售拒单→createOrder.test 2 红→还原绿；② 去 publishProduct carry→productListed.test 1 红→还原绿。**剩靠人**：部署 createOrder + adminApi（属人工·本仓禁部署）。审核 NO-GO 阻断项 P0 钱链超卖 + P1 停售下单**均已闭环**；余 P1（admin 频控信 x-forwarded-for / npm audit 9 high）+ 控制台 X/Y/Z + 真机 smoke + urlCheck 待续。生产仓 `2dd5a72` 零改动。

## 2026-06-24 · 💰 P0 钱链：关单回补后晚到回调防超卖（审核 P0·根因#1/#2）

**痛（上线前审核 P0）**：过期关单（`closeExpiredOrders`/`pay` 惰性）会 `restoreStock` 回补预留库存，但 `payCallback` 允许 `closed→paid` 盲目复活——风险链：A 单关单回补→B 单买走→A 晚到的成功回调复活 paid，两笔 paid 争一份库存=**超卖/履约冲突**。声明流转与 `payCallback.test.js` 当时还把「closed 复活 paid」锁成预期。
**不变量**：`paid` 单必须**真正持有**其 reserved 库存。closed 单（库存已释放）复活前须**重新 CAS 抢回**才可转 paid。
**修法（先守卫红→改到绿）**：① 反向测试先红——payCallback 拆 pending/closed 两路：pending 直接翻（库存自下单持有）；**closed 复活前 `reserveStock(reserved)` 重抢**，抢到才 `closed→paid`（并发败者 `restoreStock` 还回·防双扣），抢不到（已售罄）进**新增 `refund_required` 待退款死信态**（钱已收·不超卖·不静默吞钱·`notifyAlert PAID_BUT_OOS` 告警人工退款）。② 用户拍板新增 `refund_required` 状态（非「留 closed+flag」）：声明 `order.spec.ts` +1 终态/+1 流转→`gen-order-domain.mjs` 重生成→`order.ts`/JSON 派生；前端 `data/orders.js` +「待退款」展示配置（`OS.REFUND_REQUIRED` 计算键·走 shared 单源）+ admin `Orders.vue` STATUS +「待退款」chip（可见）。
**新守卫**：`paycallback-revive-reserves-stock`（test·roots `#1`/`#2`·reverseTest=payCallback.test.js）锁「closed 复活须重抢库存·售罄进 refund_required 不超卖」——补审核 P2 指出的「`order-reserves-stock` 守卫存在但没守住关单+晚到组合」缺口。
**净化指标**：test 型守卫 **15→16**（+1 组合行为闸·repoCheck 仍 64 + fileRule 5）；测试 **509→512**（payCallback +3：重抢成功复活/售罄进待退款不超卖/reserved 空直接复活；下游 orderCounts +refund_required:0、genOrderDomain 提取正则认下划线状态名）；订单状态机 5→6 态。`npm run check` 全绿、build:cloud(32)/build:h5/build:admin DONE。**反向自检×2**：① payCallback 退回「盲目复活」→反向测试 2 红→还原绿；② 删声明 `closed→refund_required` 边重生成→`order-transitions-declared` 咬「越流转」红→还原绿。**剩靠人（出审核 NO-GO 第 1 条）**：本批闭环 P0 超卖；admin 对 `refund_required` 单的退款执行工具（凭 transactionId 手动发起微信退款）列后续；部署 payCallback（属人工·本仓禁部署）。生产仓 `2dd5a72` 零改动。

## 2026-06-24 · 🎬 视频播放器交互 7 修 + 整合并回 main + 全量重部署（R8·根因#8）

**真机逐 bug 修（各带守卫·纯函数+单测）**：①下一段=小段切换连续跨课时(`stepSegment`/`player-seg-nav`) ②切段卡顿=新段地址异步未到就 play 竞态→onLoaded 就绪再起播(`player-seg-play-on-ready`) ③进度条拖拽(`scrubTimeAt`/`player-scrub-draggable`) ④继续学习按观看记录 courseId 定位非默认 list[0]演示课(`resolveContinue`·扩 `catalog-course-by-activation`) ⑤全部教程→「我的课程」列表非单课课时(`pkg-video/courses`/`resolveMyCourses`/`my-courses-entry`) ⑥继续播放黑屏=首段 id 不变 watch 不取址→onLoad 显式取址+自动起播(`player-resume-no-flash`) ⑦续播定位到原小段(`lastWatch.segmentId`)。卡片冷启骨架防演示闪现(扩 `list-pages-skeleton`)。
**整合并回 main（`e5d5d7f`）**：多 worktree-agent 并行（库存/审计/企微/admin 重设计），把 main 合进 feat·在 feat 解 6 单源冲突取并集·计数对齐 63 repoCheck/23 集合/488 测试·main 末刻 ff（遵 [[dont-commit-user-live-edits]]·main 全程不破）。
**全量重部署生产（2026-06-24）**：`deploy-fns` 32 函数全上（shared/订单域合并→hash 全变）·smoke 健康（getProducts 真数据 / createOrder 加载+withOpenId 闸过·钱链未崩 / submitFeedback 上线）·manifest 29→32。剩：前端 v0.9.5 republish（build:mp-weixin 已出包·待上传提审·urlCheck 前先配视频临时 URL 域名合法）+ 控制台锁新集合 inventory/auditLog/feedback🔴。

## 2026-06-24 · 🔑 退款决策激活判据：激活码状态数据链（闭 S10「自动判据」洞·根因#8）

**痛**：S10 退款决策抽屉之前只有人工勾选闸——审核员靠猜买家激没激活课程；自动判据当时标"需数据补链·不伪造徽章"列后续。本批补真数据链闭掉它。
- **数据链（查证可行）**：afterSale 有 `_openid`（买家）；`activations` 表直存 `{_openid, courseId, enteredAt}`（activateCourse 写）。故 afterSale→`products.courseId`（回退 `course-<productId>`·与 genQrcodes/StepBatch 同口径）→查 `activations` 即得买家是否**已激活/已进课**该课程。
- **后端**：adminApi 加 `getRefundDetail`（读类·查 afterSales/products/activations·不写库）；index 注册 + 系统事实登记。**前端**：`Refunds.vue` 决策抽屉打开异步 `getRefundDetail`→显**退货权判语**（未激活未进课=未失·可退[绿] / 已激活=已失·谨慎[琥珀]）+ 激活课程/进入学习/激活码三行；人工验收闸（寄回签收·无数据靠人）保留·**激活/进课来自真数据不伪造**。
- **守卫**：`tests/cloud/refundDetail.test.js`（5 条·已激活进课/已激活未进课/未激活/courseId 回退/NO_RECORD）；反向自检不读 activations→2 判别红→还原绿。`npm run check` 全绿（64 repoCheck·测试 509→**514**）；build:cloud(32)+build:admin 过；CDP 真渲染验抽屉判语+三行+激活码·零报错。
- **续（VMlhp 订单详情·补一条解两处）**：把激活查询抽成共享 `lib.activationFor`（getRefundDetail 重构复用）；新 `getOrderDetail`（逐商品激活态）。S8 订单「查看」抽屉升级成富版订单详情：**进度时间线**（下单→付款→发货→收货·从订单字段派生）+ **交易单号** + **逐商品激活态标签**（已激活/已进课/未激活）+ **微信发货合规上报状态**。守卫 `orderDetail.test.js`（2 条）；反向自检篡改**共享 helper→退款+订单两套都红**→还原绿。`npm run check` 全绿（测试 →**516**）；build 双端过；CDP 验时间线 4 步全 done+激活标签+交易号+合规·零报错。物流轨迹/打印面单需外部 API·诚实延后。

## 2026-06-24 · 🖥️ 控制台逐屏重设计（密集表格+服务端计数/筛选/搜索·根因#7·进行中）

**痛**：管理各列表页同病——标签计数只数「已加载页」（分页失真）、切状态前端 filter（漏单）、无搜索、卡片占地。**通法**：交易类列表后端加 `xCounts`（按域状态单源 `.count()`·精确不封顶）+ `listX` 接 `status`（云端 where）/`q`（精确搜）·系统事实登记；前端卡片流→密集表格 + 标签读服务端计数 + 切状态服务端重载 + 搜索 + 操作收右侧抽屉；守卫＝每屏 `xCounts.test.js`（计数/筛选跨页/搜索·反向自检「忽略 filter+计数数全集」即红）。隔离分支 `feat/admin-orders-redesign`。证据：`npm run check` 全绿（63 repoCheck+5 fileRule·测试 488→**506**）·build:cloud(32)+build:admin 过·生产仓零改动。**续**：仅剩 S16 财务对账（后端无对账逻辑·涉钱重·须先规划·可能接微信支付账单 API）。
- **S8 订单**（`yf2oZ`·`e2f8cbf`）：`orderCounts`+`listOrders` status/q（单号 `_id`）·发货/改单号/查看抽屉。
- **S10 退款**（`HYvNb`/`l1Vd9r`·`030278d`）：`refundCounts`+`listRefunds` status/q（订单号）·同意/拒绝 funnel 决策抽屉·核心＝人工验收闸。自动判据（激活码/进课/寄回）需数据补链·**不伪造徽章**（根因#8）列后续。
- **S11 商品**（`nz3xH`·`94c797b`）：卡片网格→密集表格+三态标签（全部/在售/筹备中/已下架）+搜索。商品是草稿量小全量加载（无分页）故计数/筛选/搜索前端做即诚实、**不引 xCounts**（不过度工程）；后端唯一改＝`listDrafts` 附 `listed` 映射（bounded≤100）让**软下架（债#12）显形**+下架/恢复操作·守卫 `productListed.test.js` 扩 2 条。
- **S12 看板**（`JHhim`）：顶部「待处理」行动条（待发货/待审退款/低库存/钱链告警·点进对应页）复用 `orderCounts`/`refundCounts`/`listInventory`+去emoji+激活率进度条。**纯前端无后端无新守卫**（计数端点已守）。续补**转化速览**（getDashboard 加 paidOrders 计数·下单→支付→激活·.count() 精确·指示性非 cohort）；访问/加购**无前端埋点未采**、完课/趋势/真漏斗需埋点+时序聚合，**不伪造**列后续（根因#8）。
- **S17 通知**（`JxJm7`）：新 `Notifications.vue`+路由+侧栏，补**企微告警#8 孤儿配置 UI**——webhook 接 `getSettings`/`saveSettings`（后端早有 alertWebhook/alertEvents·形态校验）+ 5 个真实钱链告警码开关（notifyAlert 实际调用点）。**纯前端无后端**；设计稿更丰富的告警（新订单/堆积/库存/日报）与微信订阅消息后端未接入，**如实标「规划中·未接入」不做假开关**（根因#8）。
- **S9 二维码批次**（`zAWGl`）：**用户拍板「放进上新向导」**——曾建独立 `Batches.vue` 跨课程总览页（含 KPI/三态/课程筛选/搜索），后按用户意「增强向导第6步」撤独立页+侧栏入口、把能力并入 `steps/StepBatch.vue`（该商品批次加**激活进度条+三态标签+查看码弹层[列码+复制]**·原有测试二维码/下载印刷包/生成批次保留）。**再续×2（用户反馈迭代）**：① 批次区加**状态筛选+时间快捷区间+分页(8/页)**；② **默认折叠仅显最近 3 条**（批次倒序）+「查看更多（共 N 个）」展开才出筛选+分页+「收起」。均客户端 over 已加载（批次/课程数小·诚实·无后端）。CDP 真机验：折叠 3 行无筛选→查看更多→8 行+筛选+分页→收起回 3 行·筛已用尽 3 行·查看码 20 码·零报错·侧栏入口已撤。
- **S13 外部后台**（`t8CK6`）：新 `Externals.vue`+路由+侧栏，7 个官方后台直达外链网格（云开发/套餐用量/运维监控/公众平台/支付商户/开发者工具/企微·新窗口打开·云开发三入口带本 envId）。「分工」不嵌不重造（决策）。**纯前端·纯静态·无后端无 cloudMode 依赖**·CDP 真渲染验过零报错。

## 2026-06-24 · 📮 反馈入口 + 前端错误自动上报（运营钩子①·待办#23 前端半边·上线前埋）

**痛（运营规划「上线前必埋钩子」第①类·后补极贵）**：上线初期是 bug 高发期，若无用户反馈入口 + 前端错误自动上报，线上 JS 错误/接口失败只静默在用户端、开发侧看不见。
- **① 反馈云函数**（新 Feedback 域）：`functions/feedback/submitFeedback.ts` 经 `withOpenId`（写库过闸 fail-closed·根因#3）+ `withRateLimit`（10/分·根因#13 防刷）写**新集合 `feedback`**；白名单字段 category(枚举越界归 other)/content(必填≤500)/contact(选填≤100) + 端上下文。`feedback` 登记 `kit/collections.ts` COLLECTIONS（20→21·`known-collections-only` 同治）+ 错误码 `EMPTY_FEEDBACK` 登记 ERR 册 + `系统事实`/cloudbaserc/库权限表/guard-deploy 同步。
- **② 前端错误自动上报**：新 `utils/report.js` 的 `reportError` 经既有 `track`→`trackEvent`→events 通道（**不改 trackEvent 本体**·events 有 cleanupEvents TTL 兜底）；App.vue 全局 `onError`/`onUnhandledRejection` 既本地 logger 又上报。
- **③ 反馈入口 UI**：「我」页「意见反馈」行 → `pkg-extra/feedback`（新分包·避主包预算 18 上限·`main-package-budget`）反馈表单页（分类 chips + 内容 + 选填联系方式·遵 px/主题色/Icon image/view+@tap 样式守卫）；api facade `api/feedback.js`。**产品最小默认**：无图片上传（避相册隐私+存储成本）、入口落「我」页、分类 3 档。
- **守卫**（先红后绿+反向自检均过）：新 repoCheck `feedback-write-gated`（过闸+限频+写 feedback+集合登记+cloudbaserc）+ `error-report-wired`（report.js 经 track + App.vue 两条全局兜底都调）；行为测试 `tests/cloud/submitFeedback.test.js`（6 条·NO_OPENID/EMPTY/白名单/截断/限频）入 typeAndTestGuards `feedback-throttled-gated`。
- **证据**：`npm run check` 全绿（53 repoCheck+5 fileRule·测试 428→436）；build:h5 DONE + build:cloud 32 函数（含 submitFeedback）；反向自检三守卫/测试篡改必红→还原。**靠人接手**：部署后控制台锁 `feedback` 集合「仅管理端」（与其余 20 集合同档·库权限表已登记）；前端攒批 republish。
## 2026-06-24 · 🧾 操作审计落地：管理端动钱/状态操作留痕（操作审计#4·根因#3）

**痛**：退款/发货/改价/调库存等管理操作无痕——加客服/发货员即责任黑洞+合规风险（商业审#4）。**设计**：新集合 `auditLog` + `kit/audit`（recordAudit fail-soft 写痕·summarize 剥凭证[口令/webhook/token]·shouldAudit 只记动钱/状态操作·跳 list*/get*/upload*/ping/login）；adminApi 分发处**单点 wrap**——mutating action 经 recordAudit 留 `{action/operator/ip/summary/ok/ts}`（operator 暂 'admin'·多用户后扩）·审计失败不反噬响应。**守卫（反向自检×2 全咬）**：结构 `admin-actions-audited`（recordAudit 仅 kit/audit 定义 + adminApi 单点调）+ 行为 `admin-action-audit-logged`（test·写痕/剥凭证/shouldAudit 范围）；散引→结构红 / 不剥凭证→测试红，还原均绿。**净化**：集合 21→**22**、守卫 53→**54 repoCheck**、测试 →**445**·`npm run check` 全绿·`build:cloud`(31)。**剩靠人**：控制台锁 auditLog 权限、多用户后 operator 真实身份 + 审计查询页（前端）。隔离：worktree `feat/audit-log`。

## 〔06-14~06-18 + 06-24 六批（库存预留/企微告警/dev:h5视频路/P3 订单域 codegen/P3 前端状态收口/评价汇总精确）批次明细已卷档 archive/重构日志-202606上半.md〕

> ① v0.9.1 上线 cutover + v0.9.2 批次（压测台/精确GMV/钱链告警[LD_ALERT]/频控CAS原子/客服R18/分享R29/协议隐私R27/P1加固线/支付fail-closed/events清理 等 ~29 批）；② 06-16 基建批次（守卫计数/集合计数同步·env 单源·footer 接线·preflight 部署漂移·压测台真证·throttle 防御·隐私挂载加固）+ **微信客服 in-chat 智能客服后端（06-16·CS 域 kfCallback/kfBind）+ 真机上线修 2 bug（06-17·extUserId/游标观测）**；③ **优化批0618 前端批 T-F2 骨架屏 / T-F3 下拉刷新 / T-F4 左右滑动 swiper / T-F5 微动效 / 主滚动弹性拖动感 / T-F6 加载优化分包**（2026-06-24 卷档·已整合 origin/main·里程碑见 `现状与路线`「其它活跃线」）——完整明细均归 `archive/重构日志-202606上半.md`；里程碑结论见上方 v0.9.2 段。

## 第二次重构（演进·技术整合）

> 计划见 `现状与路线.md`；第一次重构批次证据已归 `archive/重构日志-第一次重构.md`。早期治理批次（06-14 P0 知识地基 / 06-15 P2 需求→守卫闭环）已卷档 `archive/重构日志-202606上半.md`，里程碑结论见上方 v0.9.2「③ 治理脊柱」。活文档瘦约 1/3。

### 2026-06-27 · 🛡️ 治理层大体检批群（病根#11 作用在治理自身·三批·各配守卫+反向自检）
**来源**：治理层大体检（/deep-audit 治理版）——代码的客观计数/指针有守卫，治理文档/skill 自己的却没有、漂了没人发现（元守卫 guard-coverage 只核「病根↔守卫」逻辑闭环、不核这些）。**三批根治**：① **计数机器守**：扩 `guard-count-synced` 真值＝repoChecks/fileRules 数组长 + 根因账本 §一 `### N.` 病根数（与 guard-coverage 同源），扫全治理文档「N repoCheck / M fileRule / K 类病根」须报同一数；修现漂（自动化验证系统 13→86 repoCheck、元模式§B/账本 12→13 类病根）+ CN 数字归一为机器可扫「13 类病根」+ 注册表去过度声称（「全表/均反向自检」→ 当前数·下表代表性）。② **skill 指针不悬空**：新守卫 `skills-referenced-exist`（CLAUDE §9 每 `/skill` 须有 SKILL.md·同 archive-index-synced）+ 补建悬空的 `/anti-overengineering-check` skill（落 §7 抽象简约/业务预留分流为流程）。③ **首批退役**（退役制首次执行）：待办与债 13 项已关账（全 ✅·无开口尾·#13/#15/#18看板/#20/#21/#22/#25/#27-#29/#31-#33）退役→ `archive/待办与债-已关账-202606下.md` + 索引登记（156→143 行·顺带消 dup「18」）。**反向自检**：计数篡改 14≠13/85≠86 红、隐藏 skill 目录红、移除归档索引行红——各还原绿。**净**：守卫 86→**87 repoCheck**·5 fileRule·**633** 测试·全绿·待办 156→143。**根治病根#11**（治理自身的计数/指针/退役三缝·补 guard-coverage 逻辑闭环够不到的部分）。
