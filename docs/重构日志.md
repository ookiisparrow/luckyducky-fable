# 重构日志（样板房专属）

> 本文件只记重构线（Phase A / B0–B9）的批次结论与证据；生产线的工作日志在 `工作日志.md`（克隆自生产仓，本仓不再更新）。三支柱：`根因账本.md`（病根）+ `重构总计划.md`（v5：四主张+自动化优先）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-06-13 · B4b-3b：支付/退款流迁移（交易波之二，根治账本 #12 平台接缝）—— B4b 闸门波收官

支付/退款流对：
- `orders/pay`：三道闸 + 库内金额（不信任前端价）+ 惰性超时关单（transition pending→closed）+ 0 元单（transition pending→paid）+ **kit.callFlow** 触发支付工作流；UNIFIED_ORDER_FAIL 留 pending。
- `orders/applyRefund`：openid 本人 + 订单 paid/shipped/done + 在单内 + refundable + 一单一品一售后（确定性 _id）+ 金额云端分摊（分运算、累计≤实付）逐字保留。
- kit 新增 **`callFlow`**（cloudbase_module 单点收口，根因#12：与微信平台接缝收到一处，平台规则变更改动面最小；消费者 pay 现，adminApi.approveRefund B5 迁入）。
- **守卫** check-structure fileRule `flow-seam-via-kit`：functions 域禁裸 cloudbase_module（须经 kit.callFlow）。

**验证**：`npm run check` 全绿（pay 全套：工作流触发入参/惰性关单/0元单/UNIFIED_ORDER_FAIL/PAY_NOT_ENABLED + applyRefund 分摊 26.44/全额178/ALREADY_APPLIED/NOTHING_LEFT 通过）；测试 **195**（2 重指向）；`build:cloud` **25 产物**（orders 域 9）。反向自检：往 pay 注入裸 cloudbase_module → flow-seam-via-kit 红 → 还原绿。净化：2 函数 init+闸收编；cloudbase_module 收口单点。生产仓零改动。

**B4b 闸门波收官**（13 函数全迁：账户基建 4 + 课程评价 4 + 订单生命周期 3 + 支付退款 2）。**剩 2 个**：B4c refundCallback（收 defineNotifyCallback，与 payCallback 同框架）；B5 adminApi 拆分。

## 2026-06-13 · B4b-3a：订单生命周期迁移（交易波之一，根治账本 #2 状态机）

交易波（状态机主战场）拆两小波；本批=订单生命周期（无外部支付流的三件：建单→确认收货→超时关单）：
- `orders/createOrder`（128 行faithful 迁移）：openid 闸 + 云端定价（不信任前端价）+ SKU 校验 + 服务端不变量（主商品必含/地址四要素）+ **订单号 add 库级唯一防覆盖 + 重试**（A-4 资金安全）+ PAY_MODE 开关。ADDONS/券为占位（链4，与 data/checkout 镜像，P4 接真券时替换）。
- `orders/confirmReceive`：openid + 归属 + **kit.transition 第 3 消费者**（shipped→done 条件原子流转）。
- `orders/closeExpiredOrders`：定时触发无闸，pending 超 15 分批量 closed。

**验证**：`npm run check` 全绿（createOrder 全套 14 断言：定价/SKU/不变量/地址/撞号/PAY_MODE 通过）；测试 **195**（3 测试重指向）；`build:cloud` **23 产物**（orders 域 7 个）。反向自检：createOrder 的 add 换回 set（覆盖）→「撞号不覆盖旧单」红 → 还原绿（A-4 资金安全守护证）。净化：3 函数 init+闸收编；transition 三消费者（payCallback/activateCourse/confirmReceive）。生产仓零改动。

**剩 4 个**：B4b-3b 支付/退款流（pay/applyRefund，kit **callFlow** 收 cloudbase_module 单点，根因#12）；B4c refundCallback；B5 adminApi 拆分。

## 2026-06-13 · B4b-2：课程/评价一次性副作用迁移（闸门波之二，根治账本 #1 集中区）

闸门波最高价值一组——并发竞态/一次性副作用/库级唯一（账本 #1 的实例都在这）。迁入（每条安全语义逐字保留，全测试套通过）：
- `learning/activateCourse`：一码一用——**kit.transition 第 2 消费者**（qrcode _id===code，unused→activated 抢占，moved 即赢）；本人重扫/他人占用/INVALID_CODE 状态全保留。
- `learning/confirmEnter`：退货权法律节点（H/I 修复区）——`enteredAt` 原子抢占 + 失效与「首次进课」绑定（重复确认不多扣）、状态放宽 paid/shipped/done（收货后扫码也失效）逐字保留。
- `catalog/submitReview`：确定性 `_id=orderId__productId` 库级唯一（一单一品一评，根因#1 正确范例）+ 订单归属/已完成/在单内 + 昵称快照。
- `learning/genQrcodes`：withAdminGate + code 作 _id 唯一 + count 严格校验 + 防废码；crypto 用最小 `.d.ts` 声明（不引 @types/node）。

**验证**：`npm run check` 全绿（4 函数全测试套 25 断言通过；cloud TS typecheck 含 crypto.d.ts）；测试 **195**（4 测试重指向，安全套保留）；`build:cloud` **20 产物**（learning 8 / catalog 5 / orders 4 / system 1 / user 2）。反向自检：submitReview 确定性 _id 改非确定（带时间戳）→ REVIEWED 用例红 → 还原绿（根因#1 守护证）。净化：4 函数 init+闸样板收编；transition 坐实 ≥2 消费者。生产仓零改动。

**记债（根因#1 通例化未尽）**：users/progress 的「先查后建」（login/updateProfile/trackEvent，债#14）仍未改确定性 _id——涉及存量数据**惰性迁移**（命中确定 id 用之、未命中原子搬迁），单列迁移批做（kit ids 待该批 ≥2 消费者时提取）。本批已保留所有**现存**确定性/原子语义，未新引竞态。

## 2026-06-13 · B4b-1：账户/基建写入迁移（闸门波之一，根治账本 #3/#5）

B4b（闸门波 13 函数）太重（钱/权限，correctness 关键），再拆三小波。本批=账户/基建写入（最低风险一组）：迁 `user/login`（withOpenId upsert；无 openid 无法建身份→NO_OPENID，较原裸用空 openid 更正确）、`user/updateProfile`（withOpenId + 白名单 {nickname,bio,avatar} 截断 + avatar 仅 cloud:// 防注入）、`learning/trackEvent`（withOpenId + events 流水 + progress 折叠，addTo 集合自建保留）、`system/initDb`（withAdminGate + 建集合）。kit 新增 **`str`**（截断收口，消费者 updateProfile+trackEvent＝≥2 justified）。删 4 旧目录。

**验证**：`npm run check` 全绿（cloud TS typecheck 含 4 新函数 + kit str）；测试 187→**195**（+8=login 2 / updateProfile 3 / trackEvent 3，给账户写入与进度折叠加覆盖；initDb 测试重指向）；`build:cloud` **16 产物**（catalog 4 / learning 5 / orders 4 / system 1 / user 2）。反向自检：拆 updateProfile 的 avatar cloud:// 白名单 → 拒注入用例红 → 还原绿。净化：4 函数 init+闸样板收编进 kit。生产仓零改动。

**剩闸门波**（B4b-2 课程/评价一次性副作用：activateCourse/confirmEnter/submitReview/genQrcodes——需 kit ids 确定性_id + 一次性副作用绑首次流转；B4b-3 交易：createOrder/pay/confirmReceive/closeExpiredOrders/applyRefund——transition 重）；B4c refundCallback；B5 adminApi 拆分。

## 2026-06-13 · B4a：只读函数域分组迁移（T2 第一波，根治账本 #5）

B4（其余 ~21 函数迁入 kit + 域分组）分三波：**只读 → 闸门 → refundCallback**。本批=第一波只读（最低风险，无状态变更）。

迁入 packages/cloud（kit 收编 init+openid 闸样板）：catalog/`getContent`·`getReviews`（公开读，getReviews 含云端汇总逻辑原样保留）、learning/`getCourses`·`getMyCourses`·`getMyProgress`、orders/`getMyAfterSales`·`getOrderById`（后四个 openid 读用 withOpenId）。**删 `getOpenId`**（P0 连通性验证函数，miniapp 无调用＝死代码，回灌已确认连通，净化删除）。删 7 个旧 cloudfunctions 目录。

**验证**：`npm run check` 全绿（kit-only-cloud-primitives 守卫覆盖新迁函数）；测试 182→**187**（+5=getReviews 汇总 3 + getMyCourses 去重 2，新增覆盖；getOrderById 测试重指向新址）；`build:cloud` **12 产物，域均衡**（catalog/learning/orders 各 4）。反向自检：往新 read 注入裸 getWXContext → 守卫红 → 还原绿。净化：7 函数的 init 样板（×3 行）+ 4 处 openid 三行闸收编进 kit；getOpenId 死代码删除。生产仓零改动。

**剩（B4b 闸门波）**：createOrder/pay/confirmReceive/closeExpiredOrders/applyRefund（交易，transition+确定性id）/activateCourse/confirmEnter（一次性副作用绑首次流转）/submitReview（确定性id）/genQrcodes/initDb（withAdminGate）/updateProfile/trackEvent/login。**B4c**：refundCallback 收回调框架。adminApi 拆分=B5。

## 2026-06-13 · B3c：课程种子单一来源（T3，根治账本 #5 收口）

商品种子（B3b）的姊妹批，治课程种子。课程种子原为**三份复制**：miniapp `data/course.js`（含 buildSegments 派生 + 引 utils/format 的 mmss/parseDur）+ cloud `seedCourses`（CommonJS 整份 clone，连 mmss/parseDur 都自带一份），注释「⚠️ 要与 course.js 保持一致」。

归一：① `shared/src/seed/course.ts` 为 **canonical**——三层 `SEED_COURSES`，buildSegments 派生 + 私有 mmss/parseDur（种子模块自含，与 miniapp utils/format 的显示工具分属不同层）全收于此。② `data/course.js` 由 ~108 行降为 ~25 行**薄视图**——`COURSES=SEED_COURSES` + 派生 COURSE/ALL_LESSONS + 用户态样例 SAMPLE_PROGRESS（云端不存，留 miniapp）；不再 import utils/format、无 buildSegments/CHAPTERS 内联。③ `seedCourses` 迁 `packages/cloud/src/functions/learning/`（kit withAdminGate + import shared，esbuild 内联），删旧 `cloudfunctions/seedCourses`。withAdminGate 第 2 消费者（≥2 justified 坐实）。④ 守卫 `product-seed-single-source` 泛化为 **`seed-single-source`**，同时覆盖商品 + 课程（canonical 存在 + data 视图派生自 shared）。

**验证**：`npm run check` 全绿（结构仓级 4；cloud TS typecheck 含 seedCourses；course.test 三层契约用例对新 shared 派生数据全过——segment 时长之和=课时时长等）；测试 182；`build:mp-weixin`+`build:h5` DONE；`build:cloud` 5 产物（**域结构成形**：catalog/getProducts·seedProducts、learning/seedCourses、orders/getMyOrders·payCallback）。反向自检：data/course 去 shared 派生 → 守卫红 → 还原绿。净化：课程种子三份→1 处 canonical、data/course 由 108→25 行、镜像注释清零、mmss/parseDur 三处复制收敛。**至此 T3 种子侧（商品+课程）单一来源完成**；剩 initDb（无种子数据，纯建库，B4 随 withAdminGate 收）。生产仓零改动。

## 2026-06-13 · B3b：商品种子单一来源（T3，根治账本 #5）+ kit withAdminGate

T3 云唯一真相的种子侧。回灌探针已由用户 `tcb fn delete` 清除。

商品种子原为双份手工同步（miniapp `data/catalog.js` inline + cloud `seedProducts` inline，注释「⚠️ 要与 catalog.js 保持一致」）。归一：① `shared/src/seed/products.ts` 为 **canonical**（cloud 形态 superset：id/name/tag/price/was/featured/sort/courseId）。② `data/catalog.js` 降为**派生视图**——从 `@luckyducky/shared` 的 SEED_PRODUCTS 派生 CATALOG/getProduct/FEATURED_IDS（保同步身份，cart/detail 同步取数不变）。③ `seedProducts` 迁入 `packages/cloud/src/functions/catalog/`（TS，kit `withAdminGate` + import shared SEED_PRODUCTS，esbuild 内联），删旧 `cloudfunctions/seedProducts`——cloud 灌库与 miniapp 视图自此**同源**。④ kit 新增 **`withAdminGate`**（根因#3，审计 A-1：无 openid 放行/客户端须 isAdmin；消费者 seedProducts 现，seedCourses/initDb/genQrcodes B4 迁入＝≥2 justified）。

**守卫** check-structure repoCheck `product-seed-single-source`：canonical 须存在 + `data/catalog.js` 须派生自 @luckyducky/shared（防内联回潮）。**验证**：`npm run check` 全绿（结构仓级 3→4；cloud TS typecheck 含迁入的 seedProducts）；测试 182（seedGates 重指向新 seedProducts，行为不变）；`build:mp-weixin`+`build:h5` DONE（data/catalog 引 shared 两端构建吃下）；`verify:cloud` 4 产物（+seedProducts）本地真跑。反向自检：data/catalog 去 shared 派生 → 守卫红 → 还原绿。净化：商品种子双份→1 处 canonical、「保持一致」镜像注释清零、data/catalog 由数据降为薄视图。生产仓零改动。

**遗留（B3c/B4）**：课程种子（`data/course.js` 带 buildSegments 派生 + format 工具依赖，比商品种子复杂）+ cloud `seedCourses`/`initDb` 收口 shared，单列下批处理。

## 2026-06-13 · B3a：砍 api 层假数据回退（T1 第一刀，根治账本 #6）

回灌成功（getProductsKitProbe 真腾讯云跑通、176ms 返真实商品，B4 风险清零；探针待用户 `tcb fn delete` 清掉）后进 B3。只读扫描发现 `data/` 实为三类（① api 假数据回退源 / ② 规范常量 / ③ 刻意品牌样例与空态默认），**T1 只砍第①类**，故 B3 拆 B3a（本批）+ B3b（种子单源，下批）。

落地：① 删 api 层本地假数据——`api/order.js`（localCreateOrder/localOrderNo + createOrder/confirmReceive 的 `res===null` 回退分支 + 4 个 data/ import）/`api/shop.js`（localProducts）/`api/course.js`（COURSES 回退）/`api/activation.js`（演示模式「视为已激活」假成功 + getMyCourses 全解锁）。api 改为：交易类无云上抛、读类返回空、激活类返回 NETWORK 错误——绝不本地伪成功。`api/review.js`/`api/aftersales.js` 本就「云或 null」无本地生成，不动。② **守卫** check-structure `api-cloud-only`：`packages/miniapp/src/api/**` 禁 import `@/data/`——不引样例＝不可能再造假数据回退。③ **测试重构**：原 orders/activation/courses-store 测试靠本地回退当数据源（测试无 wx.cloud），改为 mock callCloud 提供云响应测 store 编排；删纯镜像 describe（orders「本地回退」3 + 「SKU 镜像」1 + activation「演示模式回退」2）——这些定价/快照/契约的真实校验已在 `tests/cloud/createOrder.test.js`。

**验证**：`npm run check` 全绿（结构含新 api-cloud-only，逐文件规则 1→2）；测试 188→**182**（−6 = 删镜像测试，净化指标）；`build:mp-weixin`+`build:h5` 均 DONE（H5 预览数据走空态不崩）。反向自检：api 文件注入 `@/data/` import → 守卫红 → 还原绿。**MP 真机行为零变化**（MP 上 callCloud 恒命中云、`res===null` 分支从不触发，删的是 H5/App 空想路径）。净化：src 约 −90 行 + 6 镜像测试；根治账本 #6（演示回退双路径）整类蒸发（parity 镜像测试自此不需要存在）。生产仓零改动。

## 2026-06-13 · B2：cloud-kit 内核 + shared 类型 + 域分组骨架 + 2 试点迁移（守卫先行）

T2/T3/根因 1/2/3/4/5 的立柱。**意图先行**：先写 kit 反向测试与守卫，再迁函数到绿。

落地：① **shared 类型**（packages/shared/src）——`money.ts`（`Fen` 金额品牌类型，浮点金额类型层绝迹，根因#4）+ `order.ts`（`OrderStatus` 联合类型，写错状态名编译失败，根因#2）+ `errors.ts`（错误码单源，根因#3）。② **packages/cloud（TS）+ cloud-kit**：`getDb`（收编 cloud.init 样板）/`ok·err`/`withOpenId`（收编 18 处 openid 三行闸，fail-closed）/`isServerCall`（回调防伪）/`transition`（条件原子状态流转 where+in，天然幂等，根因#2）/`defineNotifyCallback`（回调框架，收编 pay/refund 70% 重复，根因#3/#5）；其余 kit 件（withAdminGate/defineHttpApi/callPayFlow/ids/paging/validate）按宪章原则2「≥2 消费者才建」留 B4/B5。③ **2 试点迁移**：`orders/getMyOrders.ts`（openid 闸样板）+ `orders/payCallback.ts`（回调框架样板，v3/v2 兼容+幂等+金额留痕全保留）迁入 packages/cloud，**删旧 cloudfunctions/{getMyOrders,payCallback}**。④ **守卫**：check-structure 加 fileRule `kit-only-cloud-primitives`（packages/cloud/src/functions 下禁裸 cloud.init/getWXContext）；typecheck 扩到 cloud 包（tsc -p）。

**验证**：`npm run check` 全绿（conventions 61 + structure 仓级3+逐文件1 + cloud TS typecheck + lint + 测试 **170→187**，+17=kit 反向 15 + getMyOrders 试点 2）。**反向自检**：注入裸 getWXContext → 守卫红；去掉 withOpenId 的 fail-closed → gate 测试红；均还原绿。净化：2 函数的 init/闸/回调样板收编进 kit（B4 给其余 26 个复用）；旧函数目录净删。生产仓零改动。

**B2-follow（同日完成，就差用户点部署）**：① esbuild `build.mjs`（functions/<域>/<name>.ts → dist/<name>/{index.js,package.json}，kit/shared 内联+摇树，external wx-server-sdk）；`build:cloud`/`verify:cloud` 脚本 + esbuild 入 devDeps。② getProducts 迁入 catalog 域（kit.getDb 试点）作回灌探针，删旧 cloudfunctions/getProducts，测试 187→188。③ **本地验证** `scripts/verify-cloud-bundles.cjs`：把 3 个 esbuild 产物当真云函数 require 进来跑（内存桩喂数据），getProducts 升序 / getMyOrders fail-closed / payCallback 流转+防伪闸全保真——证明「TS→kit/shared 内联→esbuild CJS→真能加载跑对」，回灌前风险只剩 tcb 运行时一项。④ **回灌探针就绪**：用户一条命令部署 `getProductsKitProbe`（非敏感、不覆盖线上 getProducts）到生产验 tcb 兼容（命令见下）。cloudbaserc 仍列已删函数，B4 functionRoot 改指 dist 时reconcile（沙盒不部署无影响）。

## 2026-06-13 · B1：自动验证骨架——四主张一起扶正 + 自动化优先（守卫先行）

用户拍板：上帝视角四条架构主张（T1 砍多端 / T2 域分组函数 / T3 云唯一真相 / T4 按链内聚）**一起扶正，且用程序自动化方式扶正**——整个重构骑在一个可靠的自动验证+自动 hook 系统上。先建系统本身（B1），再做改造（B2+）。

落地：① 新 `scripts/check-structure.mjs`——结构不变量引擎（repoChecks 仓级 + fileRules 逐文件 + `--hook` 即时反馈），与 check-conventions 分工（它管单文件样式，structure 管跨文件/仓级架构不变量）；B1 种 3 条仓级不变量（stub-only-sdk 测试地基 / gate-single-source 三闸不漂移 / deploy-deny-all 禁部署）。② **三道闸单一定义**：pre-commit 与 CI 都改为只调 `npm run check`（=conventions+structure+typecheck+lint+test），由 gate-single-source 不变量自守（闸守着闸）。③ 编辑 hook 加 check-structure --hook。④ 设计与契约写进 `自动化验证系统.md`（五层验证 + 每主张→不变量→守卫映射），总计划升 v5（宪章加第四原则「自动化—流程即守卫」、四主张入册、批次表重排 B2–B9 每批带守卫列、§17 红线由 T4 软化）。

**反向自检（证明守卫会咬）**：篡改 CI 去掉 `npm run check` → check-structure 红（exit 1）→ 还原 → 绿。`npm run check` 全链全绿（conventions 61 + structure 3 + typecheck + lint + test 170）。纯新增基建，零行为改动，生产仓零改动。

## 2026-06-13 · 工作流技能：移植 systematic-debugging + writing-skills（按本项目约定，非整包）

用户问 superpowers/gstack 两个 skill 包——结论是别整包（与本项目守卫系统/CLAUDE.md 重叠且吃 context），**摘思路按本项目约定重写**。落地两个项目级 skill 于 `.claude/skills/`：① `systematic-debugging`（复现→根因→修复带守卫→记账，融入 logger/调试日志格式/根因账本十二类/反向自检/工具僵态守则）；② `writing-skills`（写 skill 的元技能，含「移植外部包要摘思路不整包」的教训）。放沙盒仓随重构版本管理，B9 切换时随主线进生产；生产仓零改动不破。

## 2026-06-13 · 上帝视角产品思路重审（独立于工程重构，海拔更高）

用户要求「从上帝视角扫描整个项目、搞清意图/需求/目标与每个逻辑来龙去脉、用可颠覆视角以根本意图为基础完全重构项目思路」。通读生产仓四份依据文档（设计规格/关键决策记录/业务逻辑架构/项目现状）后产出 `项目思路重构-上帝视角.md`。**核心发现**：根本意图=让钩织新手真把作品做完；验证过的真实痛点是计数/状态/记忆，团队自己写过「视频只解决看不懂的一半」并把「计数陪伴承重柱」停在探索区；而现行十条业务链 8.5/10 是电商+内容 DRM，**碰核心痛点的=0 条**——差异化押在了最浅处。**重构思路**：换重心不砍电商——电商脊柱原样承重（两种思路通用，工程重构 B1–B8 加固的正是它），学习层从「看视频」反转为「做东西被陪着」（progress→makingState、course→项目/行针数图谱、播放器→制作陪伴台，团队已为此留过 segment 粒度的位）。与工程重构**互补不冲突**（一个硬化地基、一个在地基上盖新承重柱）。**未改任何代码/计划/上线**；文末给所有者三选项（A 维持/B 扶正/C 先验证），建议先 C 后 B。纯文档。

## 2026-06-13 · 意图批：根因账本 + 总计划 v4 入仓——重构逻辑定调「根因驱动」

用户再沟通定调：吸取原项目**所有**经验、追溯本质问题、系统性根治；拍板①账本全覆盖（代码+运维+验收方法论+文档体系）②补强切分（分页入 B4、商品下架留生产线、埋点频控记债）。交付：`根因账本.md`（**十二类病根**五栏账：病史证据→本质→根治→落点批次→绝迹证明；+已根治留档+院外记账+完整性自查——调试日志 A–K/技术债 #1–#15/四份审核报告逐条归属无遗漏）+ `重构总计划.md`（v4：批次表挂病根编号；新增 B3.5 回退收口批；B1/B2/B4/B6/B7 按账本增补；A-2 翻案论证明写；权威版自此入仓受版本管理）。纯文档批，代码零改动。

## 2026-06-13 · Phase A：平行仓建立（四道栅栏）

克隆点 `2dd5a72`（v0.9 + 外部审核批次 A/B）。四道栅栏：① 远程隔离 ✅——origin 摘除生产仓指向，用户亲手建 GitHub 私库 `ookiisparrow/luckyducky-next` 后挂为唯一 origin 并推送（同日收口）；② 部署硬闸——`guard-deploy.mjs` 改为一切 tcb 部署/发布/删除 deny（八形态管道自测：5 deny / 3 放行）；③ CLAUDE.md/README 身份声明；④ 克隆体全绿（conventions 61 文件 / lint / test 170 / 双端 build）。

## 2026-06-13 · B0：workspaces 骨架 + miniapp/admin 整体平移 —— **布局成立，无需兜底**

**结论：总计划最高风险项（uni-app 进 npm workspaces）证实可行**，B1 起按原布局推进。

新布局：仓根 = workspaces 编排（`luckyducky-next`，scripts 全部 `-w` 转发）+ `packages/shared`（TS 占位，tsc 产 dist+d.ts）+ `packages/miniapp`（uni-app 整体平移，含其 16 个逻辑测试文件与 setup）+ `packages/admin`（平移，删独立 lock）。`cloudfunctions/` 与 `tests/cloud/`（闸门测试 + wx-server-sdk 内存桩）暂留仓根，B1 迁 `packages/cloud`。

**判定项证据（全过）**：
1. miniapp 子包 `build:h5` / `build:mp-weixin` 均 DONE；`dev:h5` 起服 499ms、探活 200；mp-weixin 产物结构与平移前一致（含 cloudfunctions 拷贝，vite 插件路径已改上溯两级）。
2. vitest projects（miniapp + cloud 两项目）接管 **170 例全绿**；云函数 `require('wx-server-sdk')` 经根 node_modules 命中 file: 内存桩（关键解析链验证 ✓）。
3. shared 哨兵常量（`SHARED_PKG_SENTINEL`，App.vue 引用）出现在 H5 assets 与 mp-weixin vendor.js——workspace TS 包被 uni 构建吃下 ✓。
4. admin 在 workspace 下 `vite build` ✓；`npm ls vue vite`：全仓 vue 3.5.35 / vite 5.2.8 各一份全 deduped（版本钉死目标达成）。

**路径接缝改动**：check-conventions SRC 常量、eslint ignores/no-console 路径、vite copyCloudfunctions 上溯、.prettierignore、运行说明.md（dist 路径 → packages/miniapp/dist）、visual-check.cjs projectPath。CI 工作流无需改（npm ci + 同名 scripts）。

**净化指标**：admin 独立 `package-lock.json` 删除（双 lock 归一）；vue/vite 全仓单副本；根 `check` 一条命令收口（conventions+typecheck+lint+test）。

**遗留**：微信开发者工具人工打开 `packages/miniapp/dist/build/mp-weixin` 的眼校留给用户（自动化判定已过）；admin 的 eslint/vitest 基线 = B7。
