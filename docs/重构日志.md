# 重构日志（样板房专属）

> 本文件记重构线批次结论与证据（Phase A / B0–B9 = 第一次重构；下新增「## 第二次重构（演进）」段续记）；生产线工作日志已归 `archive/工作日志-生产线-至20260612.md`（冻结于 0612）。三支柱：`根因账本.md`（病根）+ `现状与路线.md`（live 地图；第一次重构总计划 v5 见 `archive/`）+ `自动化验证系统.md`（每主张→不变量→守卫）。
> ⚠️ 产品海拔另有一份：`archive/项目思路重构-上帝视角.md`（产品角度，已被用户校正为非本轮重点——本轮是程序构建角度；保留作侧写）。

## 2026-06-15 · 🏷️ v0.9.2 里程碑（代码里程碑·新会话先读这段）

**v0.9.2 = 代码里程碑**（tag 在 main `d182075`），收束 v0.9.1 tag 以来三条线；**不是新部署**（部署状态见末段）。
- **① v0.9.1 验收收尾**：验收单 X/Y/Z 真机真钱过——X 库权限客户端实测抓真 P0（13 集合曾可写 + `courses` 公开读漏 `videoFileId`）→ 16 集合锁仅管理端；本仓接管 tcb 部署闸（deny-all → 生产仓模型，敏感函数 `ask`）。
- **② 产品打磨**（前端 miniapp·用户可见）：微信登录半屏弹窗 + 启动加载页 + 登录流程完善（退出/一键登录/默认昵称 `Lucky friend`+随机鸭头像）；首页/购物车 UI 批（品牌 logo/卡片等高/购买滚动居中/顶栏吸顶/结算条对齐）；「我」页未激活课程空态；**返回重构**（Tab 页「再按一次退出」= scroll-view + page-container）；新 skill `/frontend-check`。
- **③ 治理脊柱**（第二次重构·演进；纯文档/守卫/工具，**不碰运行时**）：P0 知识地基（立 `演进地图`）→ 文档精简+架构校准（活文档瘦 ~40%）→ P1 需求显形（`需求清单` 31 条单源 + 逐条复核，修 R22 跑偏）→ 文档架构重设（19→13 份，客观/主观分治 + 反膨胀守卫 `docs-budget`）→ **P2 需求→守卫闭环**（`requirement-trace` 守卫 + `npm run trace` 爆炸半径）→ **P2b 须守卫覆盖**（钱/状态/安全类需求须有对应类守卫）。

**现状**：测试 **278** 全过；守卫 **15 repoCheck + 5 fileRule**；活文档 **13**；skills **7**；CLAUDE 144/180。生产仓 `luckyducky-miniprogram` 零改动（HEAD `2dd5a72`，回滚基线）。
**部署状态（honest，勿读成"已全上线"）**：后端云函数仍是 v0.9.1 部署的 **28 个**（治理 work 不碰运行时、无需重部署，后端已是生产随时服务）；前端登录/UI/返回 等产品改动**已合 main、未发布生产**——发布属「全客户上线」动作（`build:mp-weixin` + `urlCheck`→true + 微信后台合法域名 + 审核），仍待。
**容量评估（2026-06-15 看码评估·非压测）**：4 位数/天 架构可承——并发正确性稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）。上量前必清 4 件（详 `待办与债` §上量就绪）：① 数据库套餐档位（控制台查·最大未知）② 视频 CDN/VOD（R21 搁置·裸云存储吐视频带宽=头号成本/风险）③ 看板内存聚合 1000 条/表封顶（4 位数当天 GMV 即少算）④ 索引 + **从未压测**（根因#8 同理：单人能用 ≠ 千人同时）。

## 2026-06-24 · 🎬 视频播放器交互 7 修 + 整合并回 main + 全量重部署（R8·根因#8）

**真机逐 bug 修（各带守卫·纯函数+单测）**：①下一段=小段切换连续跨课时(`stepSegment`/`player-seg-nav`) ②切段卡顿=新段地址异步未到就 play 竞态→onLoaded 就绪再起播(`player-seg-play-on-ready`) ③进度条拖拽(`scrubTimeAt`/`player-scrub-draggable`) ④继续学习按观看记录 courseId 定位非默认 list[0]演示课(`resolveContinue`·扩 `catalog-course-by-activation`) ⑤全部教程→「我的课程」列表非单课课时(`pkg-video/courses`/`resolveMyCourses`/`my-courses-entry`) ⑥继续播放黑屏=首段 id 不变 watch 不取址→onLoad 显式取址+自动起播(`player-resume-no-flash`) ⑦续播定位到原小段(`lastWatch.segmentId`)。卡片冷启骨架防演示闪现(扩 `list-pages-skeleton`)。
**整合并回 main（`e5d5d7f`）**：多 worktree-agent 并行（库存/审计/企微/admin 重设计），把 main 合进 feat·在 feat 解 6 单源冲突取并集·计数对齐 63 repoCheck/23 集合/488 测试·main 末刻 ff（遵 [[dont-commit-user-live-edits]]·main 全程不破）。
**全量重部署生产（2026-06-24）**：`deploy-fns` 32 函数全上（shared/订单域合并→hash 全变）·smoke 健康（getProducts 真数据 / createOrder 加载+withOpenId 闸过·钱链未崩 / submitFeedback 上线）·manifest 29→32。剩：前端 v0.9.5 republish（build:mp-weixin 已出包·待上传提审·urlCheck 前先配视频临时 URL 域名合法）+ 控制台锁新集合 inventory/auditLog/feedback🔴。

## 2026-06-24 · 🖥️ 控制台逐屏重设计（密集表格+服务端计数/筛选/搜索·根因#7·进行中）

**痛**：管理各列表页同病——标签计数只数「已加载页」（分页失真）、切状态前端 filter（漏单）、无搜索、卡片占地。**通法**：交易类列表后端加 `xCounts`（按域状态单源 `.count()`·精确不封顶）+ `listX` 接 `status`（云端 where）/`q`（精确搜）·系统事实登记；前端卡片流→密集表格 + 标签读服务端计数 + 切状态服务端重载 + 搜索 + 操作收右侧抽屉；守卫＝每屏 `xCounts.test.js`（计数/筛选跨页/搜索·反向自检「忽略 filter+计数数全集」即红）。隔离分支 `feat/admin-orders-redesign`。证据：`npm run check` 全绿（63 repoCheck+5 fileRule·测试 488→**506**）·build:cloud(32)+build:admin 过·生产仓零改动。**续**：仅剩 S16 财务对账（后端无对账逻辑·涉钱重·须先规划·可能接微信支付账单 API）。
- **S8 订单**（`yf2oZ`·`e2f8cbf`）：`orderCounts`+`listOrders` status/q（单号 `_id`）·发货/改单号/查看抽屉。
- **S10 退款**（`HYvNb`/`l1Vd9r`·`030278d`）：`refundCounts`+`listRefunds` status/q（订单号）·同意/拒绝 funnel 决策抽屉·核心＝人工验收闸。自动判据（激活码/进课/寄回）需数据补链·**不伪造徽章**（根因#8）列后续。
- **S11 商品**（`nz3xH`·`94c797b`）：卡片网格→密集表格+三态标签（全部/在售/筹备中/已下架）+搜索。商品是草稿量小全量加载（无分页）故计数/筛选/搜索前端做即诚实、**不引 xCounts**（不过度工程）；后端唯一改＝`listDrafts` 附 `listed` 映射（bounded≤100）让**软下架（债#12）显形**+下架/恢复操作·守卫 `productListed.test.js` 扩 2 条。
- **S12 看板**（`JHhim`）：顶部「待处理」行动条（待发货/待审退款/低库存/钱链告警·点进对应页）复用 `orderCounts`/`refundCounts`/`listInventory`+去emoji+激活率进度条。**纯前端无后端无新守卫**（计数端点已守）；趋势/漏斗（`B7KMb8`）需事件时序聚合+容量评估（根因#8）列后续不伪造。
- **S17 通知**（`JxJm7`）：新 `Notifications.vue`+路由+侧栏，补**企微告警#8 孤儿配置 UI**——webhook 接 `getSettings`/`saveSettings`（后端早有 alertWebhook/alertEvents·形态校验）+ 5 个真实钱链告警码开关（notifyAlert 实际调用点）。**纯前端无后端**；设计稿更丰富的告警（新订单/堆积/库存/日报）与微信订阅消息后端未接入，**如实标「规划中·未接入」不做假开关**（根因#8）。

## 2026-06-24 · 📮 反馈入口 + 前端错误自动上报（运营钩子①·待办#23 前端半边·上线前埋）

**痛（运营规划「上线前必埋钩子」第①类·后补极贵）**：上线初期是 bug 高发期，若无用户反馈入口 + 前端错误自动上报，线上 JS 错误/接口失败只静默在用户端、开发侧看不见。
- **① 反馈云函数**（新 Feedback 域）：`functions/feedback/submitFeedback.ts` 经 `withOpenId`（写库过闸 fail-closed·根因#3）+ `withRateLimit`（10/分·根因#13 防刷）写**新集合 `feedback`**；白名单字段 category(枚举越界归 other)/content(必填≤500)/contact(选填≤100) + 端上下文。`feedback` 登记 `kit/collections.ts` COLLECTIONS（20→21·`known-collections-only` 同治）+ 错误码 `EMPTY_FEEDBACK` 登记 ERR 册 + `系统事实`/cloudbaserc/库权限表/guard-deploy 同步。
- **② 前端错误自动上报**：新 `utils/report.js` 的 `reportError` 经既有 `track`→`trackEvent`→events 通道（**不改 trackEvent 本体**·events 有 cleanupEvents TTL 兜底）；App.vue 全局 `onError`/`onUnhandledRejection` 既本地 logger 又上报。
- **③ 反馈入口 UI**：「我」页「意见反馈」行 → `pkg-extra/feedback`（新分包·避主包预算 18 上限·`main-package-budget`）反馈表单页（分类 chips + 内容 + 选填联系方式·遵 px/主题色/Icon image/view+@tap 样式守卫）；api facade `api/feedback.js`。**产品最小默认**：无图片上传（避相册隐私+存储成本）、入口落「我」页、分类 3 档。
- **守卫**（先红后绿+反向自检均过）：新 repoCheck `feedback-write-gated`（过闸+限频+写 feedback+集合登记+cloudbaserc）+ `error-report-wired`（report.js 经 track + App.vue 两条全局兜底都调）；行为测试 `tests/cloud/submitFeedback.test.js`（6 条·NO_OPENID/EMPTY/白名单/截断/限频）入 typeAndTestGuards `feedback-throttled-gated`。
- **证据**：`npm run check` 全绿（53 repoCheck+5 fileRule·测试 428→436）；build:h5 DONE + build:cloud 32 函数（含 submitFeedback）；反向自检三守卫/测试篡改必红→还原。**靠人接手**：部署后控制台锁 `feedback` 集合「仅管理端」（与其余 20 集合同档·库权限表已登记）；前端攒批 republish。
## 2026-06-24 · 🧾 操作审计落地：管理端动钱/状态操作留痕（操作审计#4·根因#3）

**痛**：退款/发货/改价/调库存等管理操作无痕——加客服/发货员即责任黑洞+合规风险（商业审#4）。**设计**：新集合 `auditLog` + `kit/audit`（recordAudit fail-soft 写痕·summarize 剥凭证[口令/webhook/token]·shouldAudit 只记动钱/状态操作·跳 list*/get*/upload*/ping/login）；adminApi 分发处**单点 wrap**——mutating action 经 recordAudit 留 `{action/operator/ip/summary/ok/ts}`（operator 暂 'admin'·多用户后扩）·审计失败不反噬响应。**守卫（反向自检×2 全咬）**：结构 `admin-actions-audited`（recordAudit 仅 kit/audit 定义 + adminApi 单点调）+ 行为 `admin-action-audit-logged`（test·写痕/剥凭证/shouldAudit 范围）；散引→结构红 / 不剥凭证→测试红，还原均绿。**净化**：集合 21→**22**、守卫 53→**54 repoCheck**、测试 →**445**·`npm run check` 全绿·`build:cloud`(31)。**剩靠人**：控制台锁 auditLog 权限、多用户后 operator 真实身份 + 审计查询页（前端）。隔离：worktree `feat/audit-log`。

## 2026-06-24 · 📦 库存落地：下单即预留·乐观 CAS 防超卖（库存#1·根因#1/#2）

**痛**：全仓无库存概念（createOrder 全程不碰库存·SKU 仅 {name,price}）——实物电商上量必超卖。
- **设计（下单预留·乐观 CAS·安全迁移）**：新集合 `inventory`（_id=productId__spec·stock(null=不限量)/threshold）；`kit/inventory`（reserveStock/restoreStock/setStock/getInventory·**乐观 CAS**：读 stock→`where(stock===读值)` set−qty·撞改重试·不用 _.inc/_.gte 测试桩不支持）。createOrder 建单前对实物主商品逐 SKU 预留、任一不足整单 `OUT_OF_STOCK`（已扣回滚）、reserved 记入订单；超时关单（closeExpiredOrders 改按单循环 bounded≤200 + pay 惰性）抢占成功才回补、refundCallback 成功改抢占式翻 refunded 只一次→回补（绑状态转移·幂等防重复回补）。**缺 inventory 文档=不限量**（现有商品零改动·安全迁移）。admin listInventory/saveStock（写库存收口 kit/inventory）。
- **守卫（先红后绿·反向自检×2 全咬）**：① 结构 `stock-atomic-conditional`（roots#1/#2）＝inventory 仅 kit/inventory 读写、扣减走条件 where(stock) CAS、其余 cloud/src 直碰即红；② 行为 `order-reserves-stock`（test）＝`tests/cloud/inventory.test.js` 锁 OUT_OF_STOCK/预留扣减/超时回补幂等/CAS 抢最后一件只一个赢/不限量放行。反向自检：散碰 inventory→结构红 / 短路 OUT_OF_STOCK→测试红，还原均绿。
- **净化指标**：集合 20→**21**（inventory·库权限表同步锁仅管理端）；守卫 52→**53 repoCheck**＋typeAndTestGuards +1；测试 →**440**（inventory ×6）；`npm run check` 全绿、`build:cloud`(31) DONE。**剩（靠人）**：上线前控制台锁 inventory「仅管理端」；真并发不超卖＝压测台对真库证（单测已锁 CAS 逻辑·根因#8 真并发须真库）。**并发隔离**：worktree `feat/wecom-alert`·未上 main。**前端已接（同批续·feat/wecom-alert）**：admin `pages/Inventory.vue`（商品×SKU 行·状态标签·调整→saveStock·留空=不限量）+ api `listInventory/saveStock` + 路由 `/inventory` + 侧栏「🗄️ 库存管理」·`build:admin` 编译过（功能版·沿用现有 admin 风；S14 精致视觉属整体重设计另算）。

## 2026-06-24 · 🔔 企微告警落地：钱链失败代码侧直推群机器人（债#23续·根因#13）

**痛**：`[LD_ALERT]` 原只打日志、企微推送是「控制台配日志告警」外部靠人步骤——owner 须盯控制台才知钱链出事。
- **设计（fail-soft 加法·不碰钱流）**：新 `kit/botpush.ts`（企微群机器人 webhook markdown 推送·fetch 可注入·**永不抛错**）；`kit/observe` 加 `notifyAlert`＝原 `alert()` 记 `[LD_ALERT]` 日志 + 读 `adminConfig/settings.alertWebhook`（+alertEvents 开关·默认全开）经唯一接缝 fail-soft 推送；钱链回调三处（payCallback UNKNOWN_ORDER / refundCallback UNKNOWN_AFTERSALE·MISMATCH·NOT_SUCCESS / shipOrder WX_SHIP_UPLOAD_FAIL）`alert→notifyAlert`（推送失败绝不反噬钱流·回调照常 ACK）；payCallback FEE_MISMATCH 在 transition 同步 updater 内不能 await、留日志（下批）。`saveSettings` 改**合并保存**（webhook 与 urlPrefix 互不抹除）+ 企微 webhook 形态校验。
- **守卫（先红后绿·反向自检×2 全咬）**：① 结构 `bot-push-single-seam`（roots#13/债#23）＝pushBotAlert 仅定义于 kit/botpush、仅 kit/observe 调用，cloud/src 其余不得引用（防散调/绕开关/凭证多处）；② 行为 `bot-alert-fail-soft`（test·roots#13/债#23）＝`tests/cloud/kit/botpush.test.js` 锁 fail-soft（抛错/坏 webhook/errcode 一律返 ok:false 不抛）；③ 演进 `moneychain-alert-wired` 认 `notifyAlert`（=alert+推送·信号未丢）。反向自检：散调 pushBotAlert→结构红 / fail-soft 改抛错→测试红，还原均绿。
- **净化指标**：守卫 51→**52 repoCheck** + typeAndTestGuards +1；测试 →**433**（botpush ×4）；`npm run check` 全绿、`build:cloud`(31) DONE。**剩（靠人#8）**：真推一条须 owner 把企微群机器人 webhook key 贴进设置(消息通知屏)+真发验证；安全告警(throttle/wecom)推送 + FEE_MISMATCH 推送下批。**并发隔离**：本批在 worktree 分支 `feat/wecom-alert`、与视频 cc 并行；**未上 main**（待视频落地后合·check-structure/现状与路线 计数行为已知合并点）。

## 2026-06-24 · 🎬 dev:h5 视频演示路：让视频板块在浏览器里测（T1 逃生舱·双门控）

**痛（用户：视频板块在微信开发者工具里测很麻烦，要弄很多东西）**：T1「微信原生单源」下 `utils/cloud.js` 的 `callCloud` 在 H5 返回 null → `getCourses`/`getMyCourses`/`getPlaybackUrl` 全空 → 播放页「拿不到课程→current.id 空→未解锁→弹回目录」，视频在 H5 **完全跑不起来**，测视频只能开开发者工具 build/导入/模拟激活/真机抽查。
- **设计（dev-only 逃生舱·不碰生产架构）**：新增 `utils/devCloudMock.js`——`callCloud` 在 `// #ifdef H5` 块内、经 `import.meta.env.DEV` 门控路由到它，喂 ① 样本课程（复用 `@luckyducky/shared` 种子单源·每段 `hasVideo:true` 触发 fileMode 真实播放流）② `getMyCourses` 全解锁（鉴权放行）③ `getPlaybackUrl` 返回**内联 data URI 样本视频**（自制 8s 160×96 H.264·5.8KB→base64 7.8KB）。**mock 落 utils 不碰 api/，故 `api-cloud-only` 天然全绿、无需任何豁免**（比预想干净）。**video 走 data URI 而非文件**：uni-app 不认 vite 的 `public/`（实测 H5 产物无此目录），H5 静态只走 `src/static/`——而那目录会被无条件拷进 mp-weixin（视频撑爆微信 2MB 主包限）；内联进 dev-only 模块则随双门控从两类生产包一并消失，且 `data:` 非 http 不触 `no-external-video-src`。
- **守卫（先红后绿·反向自检×2 全咬）**：新增结构守卫 `dev-mock-h5-only`（roots T1）＝① `devCloudMock.js` 只许 `utils/cloud.js` 一处引用（不外溢）② cloud.js 对它的 import/调用须全落在 `#ifdef H5` 块内（mp-weixin 编译期删除）③ 调用块须含 `import.meta.env.DEV`（H5 正式包不启用）。反向自检：去 DEV 门控→「H5 正式包会启用 mock」红 / 去 `#ifdef H5`→「mp-weixin 会带上 mock」红，还原均绿。
- **产物层零泄漏实证（根因#8·产物才是真证）**：`build:mp-weixin` 包内 `devMockCloud`/`devCloudMock`/`data:video/mp4`/`.mp4` **全 0**、主包仍 1.7M（`#ifdef H5` 编译删除）；`build:h5`（生产 DEV=false）同样 **全 0**（tree-shake）。两道门均在产物层证实。
- **净化指标**：守卫 50→**51 repoCheck**；测试 428 全过（本批无新测试·428 是既存漂移、已同步现状文档 427→428）。`npm run check` 全绿、`build:h5`/`build:mp-weixin` DONE。**剩（根因#8·靠人）**：实际 `dev:h5` 浏览器里点进播放页看视频真播 = 用户那一下交互验证（类比真机验，此环境点不了）；H5 测的是播放器外壳/交互，**不替代上线前 mp-weixin 真机同层渲染验**。纯前端+守卫·不碰生产运行时·进攒批发（无需发版）。

## 2026-06-24 · 🧬 P3 安全处生成 spike：订单域声明→类型/契约/守卫（北极星 A·路径 C）

**路线图 P3（⬜→✅ spike）**：「订单域从声明生成类型/接口契约/守卫，证 ROI 再扩」。北极星 A（需求当单一源、向下派生）的最小切口验证。
- **声明单源**：`packages/shared/src/order.spec.ts`＝订单/售后两台状态机的声明式权威（状态集 + 合法流转表 from[]→to + trigger）。纯数据、零运行时逻辑（根因#8 铁律：只生成数据/类型/契约/守卫，**永不重生成真机验过的运行时/UI**——本批一行业务函数都没改写）。
- **生成器** `scripts/gen-order-domain.mjs`：从声明派生 ① `order.ts`（`OrderStatus`/`AfterSaleStatus` 联合 + `ORDER_STATUS`/`AFTERSALE_STATUS` 常量 + 机读 `ORDER_TRANSITIONS`/`AFTERSALE_TRANSITIONS`）② `scripts/order-domain.generated.json`（守卫读·免在守卫里解析 TS）。`--check` 模式供守卫调。
- **守卫（先红后绿·反向自检×3 全咬）**：`gen-order-domain-synced`（改声明忘重生成→漂移红）+ `order-transitions-declared`（把散在 6 个云函数里的 raw `transition()`/`status:'X'` 写入与声明流转表**对账**——函数私自越流转/写未声明状态即红，根因#2 从「靠人背规则」升「机器对账」）。反向自检：篡改生成物→①红；函数走未声明边 shipped→pending→②红；删声明边再生成→实现红，还原均绿。
- **ROI 结论：✅ 值得扩**。省的＝订单域状态机此前**只散在每个函数的字面量里、无单一权威**（`OrderStatus` 联合手维护且函数根本没用它·全 raw 字符串），现声明是唯一源、改流转改一处再生成、漂移机器咬。爆炸半径：改状态机从「grep 全库逐处手改 + 靠 review 不漏」缩到「改 spec + 跑生成器 + check」。回归风险≈0（不碰运行时·435 测试全绿）。**扩法**：其余域（learning 激活/qrcodes、cs）套同一范式（一张声明表 + 生成器 + 对账守卫）即可，无需为每台状态机加新守卫——守卫读声明表、新机只加表。
- **净化指标**：守卫 51→**53 repoCheck**；测试 428→**435**（+5 生成器行为锁 `tests/scripts/genOrderDomain.test.js`）。`npm run check` 全绿。纯声明/类型/契约/守卫·不碰生产运行时。

## 2026-06-18 · 📃 评价列表 cursor 分页 #13：getReviews 去固定 limit（根因#7·债#13 收尾）

**痛（债#13·根因#7 固定 limit 规模挤出）**：订单/我的订单/后台订单/后台售后早已 cursor 分页（#18/#22 批），**唯一遗留**＝评价 `getReviews` 仍 `limit(200)` 固定——某商品评价过 200 时「全部评价」页旧评价被截断挤出。
- **设计（列表分页·汇总 bounded 不动）**：列表改 kit `pageQuery`（默认 20/页·游标 createdAt·无参=首页兼容旧前端）返 `nextCursor/hasMore`；**汇总仅首页（无 cursor）返回**，基于 bounded 样本（≤200·加 `approx` 标注·同 dashboard 近似口径）——**真全量增量聚合仍属院外债#13 后半**（规模到再治·不在本批）。前端 store/reviews 加 `cursor/done/loadingMore` + `loadMore()` 追加，「全部评价」页 `onReachBottom` 触底续页 + 底部「加载中/没有更多了」提示；汇总首页缓存、翻页不重算。
- **守卫（先红后绿·反向自检×2 全咬）**：① 结构 `reviews-list-paged`（roots#7）＝getReviews.ts 列表须经 `pageQuery(`（不再固定 limit 截断）；② 行为 `reviews-paged-effective`（test·roots#7）＝`tests/cloud/getReviewsPaged.test.js` 端到端锁首页 limit 截断+nextCursor+汇总全样本、续页接上不重算汇总、只取本商品。反向自检：去 pageQuery→结构红 / 忽略 event 分页参数→行为红，还原均绿。
- **净化指标**：守卫 49→**50 repoCheck** + typeAndTestGuards +1；测试 422→**427**（getReviewsPaged ×4 + 既有 getReviews 兼容仍过）；系统事实 getReviews 行更新（cursor/limit 入参 + approx）。`npm run check` 全绿、`build:cloud`(31)/`build:h5` DONE。**剩**：评价汇总真全量增量聚合＝院外债#13 后半（规模触发）；「全部评价」触底续页真机验（根因#8）。后端+前端·进攒批发。

## 2026-06-18 · 🛒 商品停售/恢复 #12：软下架路径（getProducts 按 listed 过滤·债#12 院外转已做）

**痛（债#12·原院外·用户拍板按需做）**：`publishProduct` 写 `products` 后商品**永久可售**——`deleteDraft` 是硬删、`featured` 只管首页橱窗、`getProducts` 全量下发不过滤。持续上新的店要**临时停售某款 / 撤回误发布**时无路径（只能手动改库）。
- **设计（软下架·可恢复）**：新增 adminApi `unpublishProduct`（置 `products.listed=false` + unlistedAt）/ `republishProduct`（恢复）；`getProducts` 加 `.where({ listed: db.command.neq(false) })`——只下发 listed≠false，**旧无 listed 字段的商品 neq(false) 仍命中＝可售**（向后兼容·无需回灌历史）。区别硬删：保留记录、可恢复；**详情页直达（按 id）与历史订单快照不受影响**（订单存快照不回读 products）。admin 橱窗面板每行加「停售/恢复销售」按钮 + 「已停售」标（独立于橱窗 featured 开关）。
- **守卫（先红后绿·反向自检×2 全咬）**：① 结构 `catalog-getproducts-listed-filter`（roots 债#12）＝getProducts 必含 `.where({...listed...})` 真过滤（**收紧到认 where 子句·不认注释里的 listed 字样**——反向自检首轮因注释含 listed 假绿、当场收紧）；② 行为 `product-unpublish-effective`（test·roots 债#12）＝`tests/cloud/productListed.test.js` 端到端锁停售消失/恢复出现/旧字段兼容/软删非硬删。反向自检：getProducts 去 where→红 / unpublish 不置 listed→行为测试红，还原均绿。
- **净化指标**：守卫 48→**49 repoCheck** + typeAndTestGuards +1；测试 416→**422**（productListed ×5 + 既有 getProducts 兼容仍过）；adminApi action +2（系统事实登记）。`npm run check` 全绿、`build:cloud`(31)/`build:admin` DONE。**剩**：硬删仍用现有 deleteDraft 兜底（不另开 deleteProduct）；admin 真机点验停售按钮（根因#8）+ republish adminApi 后台。纯后端+admin·进攒批发。

## 2026-06-18 · 📝 协议措辞精修 #21：隐私政策「不采集手机号」→「不通过微信授权获取」（消矛盾·R27）

**痛（债#21·2026-06-16 扫出的措辞矛盾）**：隐私政策两处 blanket 写「不采集手机号 / 不会采集你的手机号」，但 `address-edit` 收货表单本就让用户**手填收货联系电话**——自相矛盾、措辞失真（根因#8 同源：声称与实际不符）。
- **改**：① intro + 第一条 改为「本小程序**不通过微信授权获取**你的手机号（收货联系电话由你自行填写、仅用于配送联系）」——精确到「微信授权口径」，与手填电话不再矛盾；② 守卫 `agreement-text-real` 必含承诺从 `不采集手机号` 收紧到 `不通过微信授权获取`（先红后绿）。
- **反向自检**：把新措辞 sed 回 `不采集`→守卫红→还原绿。`npm run check` 全绿、`build:h5` DONE。守卫数/测试数不变（仅改 agreement-text-real 判据 + 协议文本）。**余仍待法务复核定稿**（㉑🔴）。纯前端·进攒批发。

## 2026-06-18 · 📦 发货合规 #26 代码侧自动化：shipOrder 接微信 upload_shipping_info（休眠态·fail-soft·根因#12）

**痛（合规债#26·🔴上线必做的代码侧半边）**：微信「实物 + 微信支付」小程序支付后须上传发货信息，否则**订单资金冻结/无法结算** + 后台反复弹「待接入发货管理」。原 `shipOrder` 只把 company/trackingNo 写自己 `orders` 表、**没调微信发货接口**＝合规缺口（测试单没事，真顾客订单的钱会被锁）。
- **关键事实**：本仓是**微信云开发**云函数（wx-server-sdk），调微信发货走「云调用」`cloud.openapi`——access_token 由云环境注入、**无需 AppSecret**（区别 kit/wecom 企业微信走 qyapi 须自管 token）。故自动化无外部密钥阻碍，能纯代码就位。
- **接缝（根因#12 平台接缝单点）**：新建 `kit/shipping.ts` 的 `uploadShippingToWx`（云调用上传·收口微信发货接缝一处）+ `wxDeliveryCode`（物流公司中文名→微信编码映射·未命中 OTHER·与快递100 com 编码不同套）。`shipOrder` 本地翻 shipped 成功**后**追调上传。
- **fail-soft（钱链不反噬·根因#3 边界）**：上传失败绝不回滚本地发货——留痕 `wxShipUploaded/wxShipError` + 打 `[LD_ALERT] sev=money` 告警人工补录，发货本身照成（return `{ok:true, wxShip}`）。缺 transactionId 直接不打微信、留 `NO_TRANSACTION_ID` 痕。
- **守卫（先红后绿·反向自检×3 全咬）**：① 结构 `shipping-info-uploaded-to-wx`（roots#12）＝上传调用 `uploadShippingInfo` 收口 kit/shipping.ts 单点（别处直调即红）+ shipOrder 成功路径须**调** `uploadShippingToWx(`（收紧到带括号·防只 import 不调假绿——反向自检首轮暴露并修）；② 行为 `shipping-upload-fail-soft`（roots#12·test）＝ `tests/cloud/shipOrder.test.js` 锁上传失败本地仍 shipped + 留痕 + 告警。反向自检：去 shipOrder 调用→红 / 别处直调上传→红 / 删告警→行为测试红，还原均绿。
- **净化指标**：守卫 47→**48 repoCheck** + typeAndTestGuards +1（shipping-upload-fail-soft）；测试 410→**416**（shipOrder ×5 + wxDeliveryCode ×1）；扩测试桩 `cloud.openapi`（嵌套 Proxy·可控成功/失败）。`npm run check` 全绿、`build:cloud`(31) DONE。
- **⚠️ 真发前置（靠人·根因#8「拿到≠用通」；未满足时 fail-soft→本地照发+告警·不阻塞）**：① mp 后台开通「发货信息管理」能力 ② 云函数 config.json 声明 openapi 权限 `wxa.sec.order.uploadShippingInfo` ③ 部署 adminApi + 真单真机验上传成功 + 后台无「待接入发货管理」提醒。**未开通前仍按 #26 决策手动录入兜底。** 代码替不了这三步，只把链路做成「配好即生效」。纯后端·进攒批发。

## 2026-06-18 · 🖼️ 激活欢迎流背景图按「课程×状态」分管（用户请求·扩 activation-bg-wired）

**痛（用户请求）**：欢迎流原来**全 7 屏共用一张背景图**。用户要分屏分课管：欢迎页(confirm 两页)、欢迎回来(mine)、已被激活(CODE_TAKEN) 三态**按课程单独管**；正在激活(loading) **全局管**（拍板：loading 时还没识别出课程·做不了按课程）。
- **设计**：`content.home.activationBgByCourse[courseId]` 从单字符串扩成 `{welcome,welcomeBack,taken}`（读写均兼容旧字符串＝welcome）；新增全局 `content.home.loadingBg`。welcome 页按屏取图（`bgSrc` computed）：loading→loadingBg｜confirm→byCourse.welcome｜mine→byCourse.welcomeBack｜CODE_TAKEN→byCourse.taken｜无码/INVALID_CODE/NETWORK→activationBg｜兜底 hero。
- **链路（守卫先行·扩 `activation-bg-wired` +10 token·先红后绿）**：云端 `kit/reply.ts`(err 接 extra) + `activateCourse.ts`(CODE_TAKEN 返 qr.courseId·供已被激活屏按课程取图) + `adminApi/content.ts`(白名单收 loadingBg + activationBgByCourse 三键对象净化封顶) ｜ admin StepImages(一门课传 welcome/welcomeBack/taken 三张) + Showcase(全局 loadingBg 上传) ｜ miniapp welcome(bgSrc 按屏) + content store(状态化 `activationBgFor(courseId,state)` + loadingBg getter)。
- **附带修一个 c422025 引入的潜伏 P1**：Showcase 保存整存 `home.value`，但其字段集**漏了 activationBgByCourse**——管理员编辑橱窗内容会把 StepImages 配的按课程图整存抹掉。本批让 Showcase 透传保全 activationBgByCourse（init 纳入 + 不编辑只回灌）。
- **决策（用户拍板）**：loading 单加一张全局「激活中」图（不按课程）；其余三态按课程；都在课程创建流程一起传。
- **净化指标**：守卫数不变（扩 `activation-bg-wired`·按「课程×状态」四态链路纳入·强度 +10 token）；测试 **399→405**（content store +6：loadingBg/三态取图/回退/全空/旧字符串兼容/默认 state；activateCourse CODE_TAKEN 加 courseId 断言）；`npm run check` 全绿、build:cloud(31)/build:admin/build:h5 DONE。**反向自检**：① 改 activateCourse 去 courseId→cloud 测红→还原绿；② 抹 welcome loadingBg 接线→结构守卫红→还原绿。**剩靠人**：部署 adminApi + activateCourse + admin 重部署 + 真机验 + 小程序 republish v0.9.5（攒批·见 memory miniapp-publish-rate-limited）。单 CC 全权限·直接上 main。

## 2026-06-18 · 🖼️ 欢迎页背景图按课程对应（用户请求·前后端一起做·parallel-b 跨 lane）

**痛（用户请求）**：「扫码进入欢迎页，欢迎页的图片在控制台更换、管理」+ 澄清「欢迎页与产品对应·同课程同图」。原激活欢迎背景是**全局单图**（`content.home.activationBg`），所有产品共用。
- **设计**：加按课程映射 `content.home.activationBgByCourse = { courseId: fileID }`。扫码 `activate` 返回 `courseId` → 欢迎页按它取图（回退全局 activationBg → 回退默认 hero）。键 courseId（产品种子 `product.courseId` 已绑课程·admin 产品 `courseId||course-<id>` 与 activate 一致），**同课程产品共用一图**。
- **链路（守卫先行·扩 `activation-bg-wired` 至按课程四处·先红后绿）**：① 云端 `saveHomeContent` 白名单纳 `activationBgByCourse`（净化键≤40/值≤200·封顶100·空剔除·否则整存抹掉）；② admin 上新向导 **StepImages** 加「激活欢迎页背景图」上传——按产品 courseId 写映射（复用 uploadImage + get/saveHomeContent）；③ miniapp content store 暴露 `activationBgByCourse` + 解析器 `activationBgFor(courseId)`（回退）；④ welcome 改 `activationBgFor(courseId)`。
- **决策（用户拍板）**：绑课程（同课程同图）· 管理放上新向导 · 只换背景图（其余引导/ logo 不变）。
- **⚠️ 跨 lane + 部署**：用户授权「前后端一起做」，本批**碰了云函数 `content.ts`**（平时后端 main CC 的 lane）——整合时需协调者知会后端、防撞 content.ts。**白名单改动须部署 adminApi 才生效**（本仓禁部署·属人工/协调者动作·我做不了）；未部署前后台上传的 `activationBgByCourse` 会被旧白名单整存抹掉。
- **净化指标**：守卫数不变（扩 `activation-bg-wired` 现有守卫·按课程链路四处纳入·强度+）；测试 **399** 全过；`npm run check` 全绿、`build:cloud`(31)/`build:admin`/`build:h5` DONE。**反向自检**：云端去 `activationBgByCourse`→红 / welcome 去 `activationBgFor`→红，均还原绿。**剩靠人**：部署 adminApi（白名单生效）+ 真机验（扫不同产品码进各自欢迎图）+ 小程序 republish。commit 落 **parallel-b**（不推 origin/main·待 parallel-c 整合·须协调后端 lane）。

## 2026-06-18 · 🔒 付费视频白嫖防护结构守卫 video-url-via-cloud-only（优化批0618·T-B1 核查收口 + T-B2 复核·派单箱）

**来源**：parallel-c 派单箱 `queue/main.ndjson`「优化批0618」后端批（T-B1 视频存储后端核查收口 / T-B2 加载优化后端半）。
- **T-B1 核查（链路全对）**：付费课视频是受保护资产——`getPlaybackUrl` 是**唯一**鉴权后经 `getTempUrl` 换短时效临时 URL 的云函数（免费段放行 / 付费段须 `activations.enteredAt` 已确认进课·否则 `NOT_ENTITLED` 挡白嫖 / 素材未剪 url:null）；`getCourses` 公开目录把 `videoFileId` 剥成 `hasVideo` 布尔（连 `cloud://` 都不漏）；`getMyCourses`/`getMyProgress` 不返课内容。行为面已被 `getPlaybackUrl.test.js` 锁——**但结构无闸**：新加 learning 函数可能把 fileID 漏进公开返回 / 绕鉴权自造 URL。
- **守卫（codify 现有正确态·非「红→绿」，靠反向自检证齿）**：`video-url-via-cloud-only`（repoCheck·roots `#3` 信任边界）扫 `learning/`：① `getTempUrl` 只许 `getPlaybackUrl.ts` 调（播放 URL 单源经鉴权云函数）；② `videoFileId` 只许 `getPlaybackUrl.ts`(定位)/`getCourses.ts`(剥 hasVideo) 出现（小白名单·均已审计），其他 learning 函数引用即红；③ `getPlaybackUrl.ts` 须既经 `getTempUrl` 又有 `NOT_ENTITLED` 权属闸，缺任一即白嫖洞。
- **反向自检（三面齐红→还原绿）**：getCourses 加 `getTempUrl('x')` 调→红；getMyCourses 引 `videoFileId`→红；删 getPlaybackUrl 的 `NOT_ENTITLED`→红；还原均绿。
- **T-B2 复核（无需改码）**：看板/列表热读路径已有界——`dashboard.ts` 计数 `.count()`/GMV `aggregate`（精确不封顶）、最近单 `limit(5)`、仅 热度/卡点 `limit(SAMPLE)` 显式标「近 N 估算」；`batches.ts` `skip/limit` 分页取尽。`capacity-reads-bounded` 覆盖 dashboard/batches 充分。**索引核查＝控制台靠人**（#19 残·代码看不到）。**watch（备查·非本批）**：`admin/adminApi/actions/products.ts` 列表 `limit(100)`（SKU 破百静默截断·仅管理端·低危·「核查收口非重建」不动）。
- **净化指标**：守卫 **37→38 repoCheck**（+1 结构闸·强度+1）；测试 **391**（新守卫 +1 provenance；顺手校正 `现状与路线.md` stale 计数 37→38 repoCheck、378→391 与真值对齐）；`npm run check` 全绿（66 文件 / 391 测试）。**本批 commit 落本地 main，不 push origin**——优化批0618 协调模式：parallel-c 为 git owner、审核后 cherry-pick 整合上 origin/main，整合后本树 `git reset --hard origin/main` 同步。

## 2026-06-18 · 🦴 优化批0618·T-F2：可复用骨架屏 Skeleton + 数据页冷启占位（parallel-b·根因#8）

**痛**：数据页冷启/弱网空白或纯文字「加载中」（order-list 旧为「订单加载中…」文字、catalog 冷启空章节区）——白屏感、真机才暴露（根因#8）。
- **改（先红后绿）**：新 `components/Skeleton.vue`（纯 CSS shimmer·动 background-position·mp 安全·占位色走 token $bg-grey/$bg-faint）；`order-list` 列表未到时占 3 张订单卡骨架、`catalog` 冷启占 3 行章节骨架（皆绑真 `store.loading`，刷新已有列表不闪骨架）。新守卫 `list-pages-skeleton`（册=catalog/order-list 须挂 `<Skeleton>`），反向自检：sed 删 order-list `<Skeleton>`→红→还原绿。
- **honest 边界（根因#8·不做 theater）**：首页走 LoadingSplash 全屏开屏、detail 样例数据即时渲染——各有其载态，**不强塞骨架**（册内只收真有空载态的页）；card 列的「首页/detail」据此剔除，已在守卫 desc/注释写明。
- **净化指标**：守卫 38→**39 repoCheck**（parallel-c 同批整合 T-B1/B2 + T-F2·累计 39 与 `现状与路线` 对齐）；测试 **391** 全过；`npm run check` 全绿、`build:h5` DONE。**剩靠人（根因#8）**：真机验 shimmer 流畅度 + 弱网下骨架→数据平滑替换（card accept 真机项）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 🔄 优化批0618·T-F3：数据页下拉刷新（parallel-b·根因#8 双机制）

**痛**：数据页无下拉刷新——后台流转（订单付款/发货、退款状态、新评价）后用户只能退页重进才看到最新。
- **关键多端坑（根因#8·分两机制非一刀切）**：`onPullDownRefresh` 只由**页面级滚动**触发；内容装在整页 `<scroll-view>` 里滚动的页（index/me），页面本身不滚→页面级 `enablePullDownRefresh` 在 mp-weixin **根本不触发**（H5 还假绿）。故：
  - **页面级滚动数据页**（catalog/order-list/reviews/aftersales/order）→ `pages.json enablePullDownRefresh` + `onPullDownRefresh` 强刷 store + `uni.stopPullDownRefresh()` 收尾；
  - **整页 scroll-view 数据页**（index/me）→ scroll-view 自带 `refresher-enabled` + `:refresher-triggered` + `@refresherrefresh`，handler `finally` 收 triggered。
  - 七页刷新均 `try/finally` 收圈——**失败也收，不卡转圈**（accept 项）。
- **改（先红后绿）**：新守卫 `pull-refresh-stops`（roots `#8`）钉三件：① 列举的页面级数据页必开 `enablePullDownRefresh`；② 凡开 `enablePullDownRefresh` 的页必有 `onPullDownRefresh`+`stopPullDownRefresh`（防转圈不停）；③ 凡用 `refresher-enabled` 的页必绑 `@refresherrefresh`+`:refresher-triggered`。先红（7 页全缺）→改到绿。**反向自检三面**：删 order-list `stopPullDownRefresh`→红 / 删 catalog `enablePullDownRefresh`→红 / 删 index `@refresherrefresh`→红，均还原绿。
- **honest 边界（根因#8·不做 theater）**：**cart 不入册**——整页 scroll-view 但纯本地态（购物车条目本地持久化 + `CART_RECS` 静态推荐），无远端数据可刷，加下拉刷新＝假绿；静态页（about/agreement/welcome）无数据，不开。守卫 desc/注释写明。
- **净化指标**：守卫 39→**40 repoCheck**（+1·`现状与路线` 计数同步 39→40）；测试 **393** 全过；`npm run check` 全绿、`build:h5` DONE。**剩靠人（根因#8）**：真机验下拉触发+转圈正常收+失败反馈（accept 真机项）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 👉 优化批0618·T-F4：左右滑动（图廊 swiper + 订单 tab 横滑·parallel-b·根因#8）

**痛**：商品图廊只能点缩略图换图（不能左右滑）；订单 tab 只能点、不能横滑切换。
- **守卫先行（红→绿）**：新 `swipe-native-swiper`（roots `#8`）钉「左右滑面（DetailGallery + order-list）须用原生 `<swiper>`+`<swiper-item>`」——守卫指令「不自造 touchmove」：mp 横滑 vs 纵滚的手势消歧靠原生 swiper 才稳（自造 touchmove 真机易误触·H5 假绿）。先红（俩都没 swiper）→改到绿。
- **(a) 详情图廊 DetailGallery**：主图换 `<swiper :current="sel" @change>` 包 N 张 MediaSlot，左右滑切图；与缩略图高亮、计数「sel+1/total」**双向同步**（滑 swiper 写 sel、点缩略图改 sel→swiper 跟切）。swiper 定高 `100vw`（方图与原 ratio 1/1 等高）。
- **(b) 订单列表 order-list（实质重构）**：顶部 tab 下接 `<swiper :current="tabIndex" @change="onSwipe">` 包 5 个 tab，每 tab 一个竖滚 `<scroll-view>`；**tab↔swiper 双向联动**（点 tab `selectTab` 切 swiper·滑 swiper `onSwipe` 回写高亮）。列表移入 scroll-view 后页面级滚动没了 → **触底加载** onReachBottom→`@scrolltolower`、**下拉刷新** 页面级→scroll-view `refresher`（复用 T-F3 机制·共享 `refreshing`）。布局 `.coorl-page` flex 列定高 100vh、`.coorl-swiper` flex:1 占满。同步改 `pull-refresh-stops` 守卫：order-list 从「页面级册」迁「refresher 册」、pages.json 撤 `enablePullDownRefresh`。
- **honest 边界（根因#8·不做 theater）**：**catalog 不做**——派单写「catalog(分类)用 swiper 包 tab」，但 catalog 实为**单课程 + 竖向章节折叠（手风琴）**、无横向 tab、无可左右滑的面，强加 swiper＝造不存在的 tab。守卫 desc/注释写明不入册。
- **净化指标**：守卫 40→**41 repoCheck**（+1 swipe-native-swiper·`现状与路线` 同步 40→41）；测试 **394** 全过；`npm run check` 全绿、`build:h5` DONE。**反向自检**：删 DetailGallery `<swiper`→红 / 删 order-list `<swiper`→红 / 删 order-list `refresher-enabled`→红，均还原绿。**剩靠人（根因#8·accept 真机项）**：真机验图廊能滑+指示准、order-list 横滑联动高亮、**横滑不误触纵滚**、swiper `flex:1/height:0` 在 mp 高度真撑开（H5 已 DONE·mp 待真机）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · ✨ 优化批0618·T-F5：滑动微动效（克制版·parallel-b·根因#8）

**痛**：交互缺微反馈——点卡片无按压感、列表项硬切无进场、整体偏「静」。决策＝克制版纯 CSS（transform/opacity 合成层·不引重动画库·避 mp 低端机掉帧）。
- **基建（单一来源）**：uni.scss 加微动效 token（`$ease-out`/`$dur-press`/`$dur-rise`）；App.vue 加 3 个全局工具类——`.ld-press`（卡片点击态 scale .97+变淡）、`.ld-tap`（整宽行点击态只变淡不缩放·行缩放露边显怪）、`.ld-rise`+`@keyframes ld-rise`（列表项淡入上移进场·`both` 保留终态·无 stagger 避长列表掉帧）。
- **应用（克制·选高频面）**：订单卡 `coorl-card` 挂 `ld-press`+`ld-rise`（按压+进场）；商品卡 ProductCard 根因已有 `transition:box-shadow`+切换式 `is-flash` 动画，**在其 scoped 内并入点击态**（全局类会被 scoped 特异性盖过）、**不挂 ld-rise**（is-flash 撤回会令 animation 回落 ld-rise→高亮后重放进场 glitch）；课时行 `vc-lesson`、「我」菜单行 `my-row`×2 挂 `ld-tap`（行原本无/硬切反馈→平滑变淡）。旧散落 `:active{opacity}` 收口到全局类。
- **honest 边界（根因#8·不做 theater）**：`scroll-with-animation` 只对**程序化滚动**生效（首页回到顶部已设），列表页是原生惯性滚动、加了是 no-op → 不新增。**视觉手感（跟手/进场顺滑/低端机不卡）按派单决策无机器守卫**——靠 /frontend-check 真机验（#8 已 CLAUDE 靠人）。
- **守卫（可机器化的子约束）**：新 `anim-no-heavy-lib`（roots `#8`·codify 现状非红→绿）钉「packages/miniapp 不依赖也不 import 重 JS 动画库（gsap/lottie/animate.css/framer-motion…）」——微动效不滑向重库。**反向自检**：package.json 注入 `gsap` 依赖→红 / src import `lottie-web`→红，均还原绿。
- **净化指标**：守卫 41→**42 repoCheck**（+1 anim-no-heavy-lib·`现状与路线` 同步 41→42）；测试 **395** 全过；`npm run check` 全绿、`build:h5` DONE。**剩靠人（根因#8·accept 真机项）**：真机验滑动跟手、进场顺滑不掉帧、低端机不卡（视觉项·/frontend-check）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 🪀 优化批0618 续·主滚动恢复弹性「拖动感」（用户报·parallel-b·根因#8）

**痛（用户报）**：「页面整体需要拖动感」——最初 UI 设计里有，不只顶/底橡皮筋回弹，板块/元素间也有微弱弹性；现在端口后整页发死、到边硬停。
- **根因（查原型证据定性）**：原设计＝HTML 原型（`design/prototypes/login-loading.html` 等）在手机上跑，有 **iOS 原生弹性滚动**（橡皮筋回弹 + 整体跟手的弹性惯性·这就是「板块间微弱弹性」来源）；原型动效语言是 ease-out 渐入 + scale 点击态（无 CSS 弹簧过冲）。端口到 uni-app `<scroll-view>` 后这层原生弹性**丢失**——mp-weixin scroll-view 默认刚性、到边硬停无回弹（H5/快网假绿·根因#8）。
- **改（先红后绿·mp 唯一恢复途径）**：4 个整页 scroll-view 承载滚动的页（index/me/cart/order-list）主 scroll-view 加 `enhanced`（增强模式）+ `bounces`（iOS 橡皮筋）。新守卫 `main-scroll-elastic`（roots `#8`·列举四页须带二者）防再丢。**反向自检**：cart 去 `enhanced`→红 / 去 `bounces`→红，还原绿。
- **honest 边界（根因#8）**：① 页面级滚动页（catalog/reviews/aftersales/order）走 mp 页面滚动、iOS 默认就有回弹，无 scroll-view 可改、不入册；② `bounces` 仅 iOS 生效（Android mp 无原生回弹·平台限制非代码缺陷）；③ **enhanced 模式与现有 `:scroll-top`/@scroll（首页回到顶部·导航滚动追踪）、refresher（下拉刷新）共存须真机验**——H5 build DONE 但 mp 行为只真机证（若首页 backToTop/导航滚动被 enhanced 破，回退首页 enhanced、另寻途径）。
- **净化指标**：守卫 42→**43 repoCheck**（+1·`现状与路线` 同步 42→43）；测试 **396** 全过；`npm run check` 全绿、`build:h5` DONE。**剩靠人（根因#8·真机项）**：真机验四页弹性回弹手感、首页 backToTop/导航滚动不被 enhanced 破、下拉刷新与回弹不打架。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 📦 优化批0618·T-F6：加载优化（分包 + preloadRule + 图片 lazy-load·parallel-b·根因#8）

**痛**：当前**零分包**——19 页全塞主包，冷启动要加载全部（含最重的非首屏视频页 player ~124K）；真机/真网冷启才慢（dev/快网无感·根因#8）。
- **守卫先行（红→绿）**：新 `subpackage-config-present`（pages.json 须有非空 subPackages + preloadRule）+ `main-package-budget`（最重非首屏页 player 须在分包不在主包·主包 pages ≤18）。先红（零分包）→改到绿。
- **改**：① **分包 `pkg-video` = {player}**——`git mv pages/player → pkg-video/player`（最重非首屏页·仅 2 处导航引用·无相对引用断裂全用 @/ 别名），pages.json 移出主包入 subPackages；② **preloadRule**：在 catalog/me（进 player 的入口）预载 pkg-video（`network:all`），首次进 player 不卡；③ **图片 lazy-load**：MediaSlot `<image>` 加 `lazy-load`（列表/卡片图按需载·**首屏 Hero 不加**避免延迟 LCP）；④ 同步改 2 处导航 URL（catalog/me→`/pkg-video/player/index`）+ `customer-service-wired` 守卫 2 处 player HelpSheet 路径。
- **honest 边界（根因#8·不做过度）**：① **welcome 不分包**——在扫码激活关键路径上 + 绑 2 个激活守卫（activation-scan-routed/bg-wired），移它风险高于收益（仅 20K），激活流留主包稳；② detail（104K）/checkout 等转化主路径留主包（分包会给首跳加载延迟）；③ 首屏接口**已并发**（App.onLaunch products.load + index content.load 各自 async 不互等、刷新走 Promise.all），无需改；④ player 移出后 `privacy-authorize-wired` 仍只遍历 `pages/`——player 不调隐私接口故无缺口（记此，未来分包页若调隐私接口须扩遍历）。
- **净化指标**：主包 **~1740→1652 KB**（player 124K 移出冷启路径·按需+预载）；守卫 43→**45 repoCheck**（+2·`现状与路线` 同步 43→45）；测试 **398** 全过；`npm run check` 全绿、`build:mp-weixin`/`build:h5` DONE。**反向自检**：删 subPackages→红 / player 加回主包→红，均还原绿。**剩靠人（根因#8·accept 真机项）**：真机验 player 分包跳转正常 + preloadRule 预载不误伤（进 catalog/me 后进 player 不卡）。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 2026-06-18 · 🎬 优化批0618·T-F7：视频去外链占位（合规·parallel-b·根因#8）

**痛（合规红线）**：player 硬编码 Google 外链占位视频 `commondatastorage.googleapis.com/...ForBiggerBlazes.mp4`——非法域名，小程序 urlCheck 翻 true 后真机根本播不了（dev/未校验时能播=假绿·根因#8）。
- **守卫先行（红→绿）**：新 `no-external-video-src`（roots `#8`）扫 `packages/miniapp/src` 禁裸 http(s) 视频 URL 字面量（.mp4/.m3u8/.mov/.webm 等·跳注释行）。先红（抓 player:26 外链）→改到绿。
- **改（窄·按 T-F7-c1 预审）**：删 `SRC` 常量；`videoSrc = fileMode ? playUrl : ''`（无真视频不再回退外链·**真播放支路 fileMode=true 走 getPlaybackUrl 不动**）；新 `placeholderMode=!fileMode`——无真实分段视频时显**本地占位封面**（纯文字「课程视频整理中／敬请期待」·深色播放页白字·零外链零外图），并隐藏播放控件（vp-hit/bigplay/底部控件/分段进度），顶栏返回键保留（z-index 高于占位层）。
- **honest 边界（根因#8）**：① 占位用纯文字非外链图（合规彻底）；② 真播放支路（getPlaybackUrl 换云端短时效 URL·anti-leech 后端 T-B1 已收口）原样不动，accept「有 fileID 真机能播」继续成立；③ `<video>` 仍常驻（空 src·占位层覆盖），不动 ctx 创建时序避真播放支路回归。
- **净化指标**：守卫 45→**46 repoCheck**（+1·`现状与路线` 同步 45→46）；测试 **399** 全过；`npm run check` 全绿、`build:mp-weixin`/`build:h5` DONE。**反向自检**：player 塞回任意 http 视频 URL→红，还原绿。**剩靠人（根因#8·accept 真机项）**：真机验「无 fileID 显占位非外链」+「有 fileID 真机能播」+ urlCheck 翻 true 不破。commit 落 **parallel-b 分支**（不推 origin/main·待 parallel-c 整合）。

## 〔06-14/06-15/06-16/06-17 + 06-18 微信客服卡片降级 批次明细已卷档〕

> ① v0.9.1 上线 cutover + v0.9.2 批次（压测台/精确GMV/钱链告警[LD_ALERT]/频控CAS原子/客服R18/分享R29/协议隐私R27/P1加固线/支付fail-closed/events清理 等 ~29 批）；② 06-16 基建批次（守卫计数/集合计数同步·env 单源·footer 接线·preflight 部署漂移·压测台真证·throttle 防御·隐私挂载加固）+ **微信客服 in-chat 智能客服后端（06-16·CS 域 kfCallback/kfBind）+ 真机上线修 2 bug（06-17·extUserId/游标观测）**——完整明细均归 `archive/重构日志-202606上半.md`；里程碑结论见上方 v0.9.2 段。

## 第二次重构（演进·技术整合）

> 计划见 `现状与路线.md`；第一次重构批次证据已归 `archive/重构日志-第一次重构.md`。早期治理批次（06-14 P0 知识地基 / 06-15 P2 需求→守卫闭环）已卷档 `archive/重构日志-202606上半.md`，里程碑结论见上方 v0.9.2「③ 治理脊柱」。活文档瘦约 1/3。
