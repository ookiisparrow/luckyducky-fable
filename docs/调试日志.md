# 调试日志 / 复盘记录

> 项目的「问题账本」——每发现一个问题就记一条。目的不是记录 bug 本身,而是
> **追到结构性根因、顺藤摸出同类隐患**,避免碎片化地一个个救火。
>
> 配套基础设施:全局错误捕获(`main.js` / `App.vue` → `utils/logger.js`)、
> 数据契约校验(`store/persist.js` 的 `sanitize` + `utils/validate.js`)、
> 回归测试(`tests/`,`npm run test`)。
>
> **每条格式**:日期 / 现象 / 根因 / 同类隐患(启发式) / 是否结构性 / 状态。

---

## 已结案（教训沉淀,详细推理见 git 历史）

### 2026-07-01 · AB：微信客服 bot「不回复」——顾客点了「找人工」后会话转人工态(service_state=3)、bot 在人工态误发 send_msg 报 95018；bot 本身正常（根因#12 平台会话状态机·测试落在人工会话上）　✅ 已修带守卫·真机验过（新会话 state 0→回复）
- **现象**：向客服发消息 bot 不回。线上日志（`tcb fn log kfCallback`）四轮实测收敛：① 原始 `sync msgs:1`→`handle`→`[LD_ALERT] SENDMSG_FAILED errcode:95018`；② 加会话态代码后 `assistant-gate:skip`；③ 打印确切态 `service-state {state:3}`（人工接待）；④ 顾客侧聊天截图＝铁证：顾客点过「🙋 其它/找人工」→ bot 回「已为你转接人工」→「已接入人工服务」→ 此后所有消息都在人工态、bot 正确闭嘴。
- **根因（四次收敛·三次纠偏）**：微信客服 `send_msg` 由 API 发**仅在 service_state=1（智能助手）允许**；顾客点「找人工」→`transferToServicer` 置 state 3（人工接待）→旧代码仍在人工态发菜单→`95018`（真机实证 state 3 API 发不了·只有坐席能发）。「完全不回复」的观感＝测试会话一直卡在人工态 + 接待台无人值守。**bot 对正常新会话本就工作**（截图里欢迎菜单/转接确认都是 bot 发的）。**纠偏①** 上批把 95018 当「回复太晚」挪 batchget（latency 非因）。**纠偏②** 初判「新会话默认 state 0 漂移」→实测 state 3。**纠偏③** 再判「账号未开 API 托管」→后台截图证 Lucky Ducky 已托管该账号（API 托管、回调全通）；真因是**测试落在了一条被转人工的会话上**，非配置缺失。
- **修（代码侧·已上·真机验过）**：接缝 `kit/wecom.ts` 加 `getServiceState`/`enterSmartAssistant`(trans→1·不需 servicer)/`ensureSmartAssistant`（读态决策：**1 直发·0新会话/-1读失败 尽力 trans→1 接管·2 排队/3 人工/4 已结束 让位 skip**——平台不允许 2→1、4→1[真机 4→1 报 95013]，只硬试新会话 0 免告警噪声）；`kfCallback` 回复前置态。守卫 `kf-reply-after-smart-assistant`（roots #12）+用例·反向自检×2 过。真机验：新微信首条→`service-state {state:0}`→`assistant-gate {proceed}`→`sent {msgmenu}`→顾客收到 ✅。
- **同类隐患（结构性·根因#8）**：AA「回调 Token 飘」、AB「测试样本失真——测在人工会话上误判 bot 全坏」＝**同宗#8**：真机验必用**贴近真实的会话状态样本**（干净新会话 vs 被转人工的旧会话，表象天差地别）；`kfHealthProbe` 探 token/IP，探不到「会话态/send 端到端」（合成新会话消息才测得出）。config手册 §6 坑⑤ 已录本类排障。
- **状态**：✅ 全部结案。代码已修带守卫 + 已部署（守卫 112·测试 783·check 全绿）+ **真机验过**（新会话 bot 回复）。**运营模型用户拍板 A（零改动·非代码 bug）**：「找人工」转人工后用**企业微信原生接待台**（App→工作台→微信客服→客户咨询）+ 接待人员原生**在线状态**（🟢正在接待/🟡暂时挂起/⊖停止接待）——在线时顾客找人工进接待台由人工回，离线时会话超时自动关闭（截图见「对方超时未回复，聊天已结束」）；不另做自建开关（免双处管理）。接待台可用性用户已真机确认（能看到 Pokebaby/小草青青 会话）。
- **状态**：🟡 代码侧已修带守卫 + 已部署（`98e98cb`·守卫 112·测试 783·check 全绿·观测 `[kf] service-state`/`assistant-gate` 上线）。🔴 **待用户后台开「通过API管理微信客服账号」**（真正解锁项·代码够不到）+ 真机验（开后发消息→日志 `service-state {state:0}`→`assistant-gate {gate:'proceed'}`→`sent`→收到回复）——根因#8「构建过≠真能用」。

### 2026-07-01 · AA：微信客服机器人静默罢工——回调验签 Token 与后台飘掉（根因#8 配过一次≠一直通·真机 ④ 验时逮出）　✅ 已修（配置侧·非代码）
- **现象**：真机验 ④（send_msg 坐席主动发）时测试微信发消息机器人不回；日志全是 `[LD_ALERT] FORGED_CALLBACK`（验签失败被拒）、0 条成功 `[kf] onEvent`。实时对照（用户发一条→13:03:40 回调确到 kfCallback→仍 FORGED）证消息到了但验签不过。
- **根因**：kfCallback 的 `WXKF_TOKEN`(env) 与微信客服后台「回调配置」的 Token 不一致 → `verifyKfSignature` 恒 false → 每条真回调当伪造拒（`kit/wecom.ts` fail-closed）。何时飘不明（06-17 真机通过后至 07-01 无人再验·env 本身完好非本次误删）。**bot 已静默罢工可能数日、无顾客无人察觉。**
- **修**：新生成干净 Token+AESKey → 先更 kfCallback env（env 先于后台）→ 后台回调配置填同值 → 点完成 echostr 重验过 → 恢复（13:12:35 成功 onEvent→sync→sent msgmenu）。顺带轮换 Token/AESKey（此前 CC 误经 `tcb fn detail` 明文打印泄露·教训已记 memory：查密钥类 env 别 dump 值·用长度/hash 比对）。
- **同类隐患（结构性·根因#8）**：① **上线后活体探针缺失**——「配过一次通」≠「一直通」，关键外部接缝（微信客服回调/支付回调）宜有定期真机或合成探活；② FORGED_CALLBACK 只进日志不触达运营（无人盯日志）→ 静默故障，「持续 FORGED>0 且 onEvent=0」是真外观信号、可告警（单条 FORGED 可能是公网扫描器噪声·须按「持续零成功」判防噪）；③ config手册 §6 已录 FORGED 排障（坑②），本次是其实例。
- **状态**：✅ 已修（配置侧·无代码改）。探活/告警增强＝待办（未排期·根因#8 靠人面）。

### 2026-06-27 · Z：admin 存企微 webhook → 500——`.set({data})` 带回 get 的 `_id`（根因#8 桩≠真 SDK·同 P）　✅ 已修：真 wx-server-sdk `doc(id).set({data})` data 含 `_id` 即 reject；saveSettings `{...cur}`(get 回含 _id)二次存必 500（fallback `.add` 撞键再抛·未 catch）。内存桩 `.set` 默接受 _id 藏住（`{...data,_id:this.id}`）。全扫 4 处同根：saveSettings(硬500)/wecom token+kfCallback cursor 缓存(fail-soft 静默废·每次重取)/kfBind(潜伏)·spread seed/wxbill 无 _id 安全。修=4 处去 data 里 _id +双守卫(桩对齐真 SDK 即抛 + 静态 `no-id-in-set-data` 扫字面·`.add` 例外)+往返测试·反向自检×3·守卫 83→84·测试→621。🔴 须重部署 adminApi/kfCallback/kfBind 才生效；前端「请检查 webhook 格式」误导提示未改(小 UX 债)。

### 2026-06-25 · Y：视频上传回落分片容量 ~12MB < 体积闸 15MB → 12~15MB 视频走回落必 BAD_FINISH（审计 P2·根因#8 边界·直传主路径不受影响）　✅ 已修：uploadFinish 片数上限 200→300（覆盖 15MB 闸 + 余量）+ adminMisc.test「263 片≈15MB 重组」红→绿锁；课程/帮助视频共用此路径同时受益。

### 2026-06-24 · X：H5 console 报 `main.value.scrollTop = val` null——uni-h5 框架 `<scroll-view>` 内部空引用，H5-dev 态瞬时（根因#8）　✅ 不修（非生产 bug）
- **现象/排查**：cmux 浏览器格子跑 `dev:h5` 首页，`errors list` 抓到 `null is not an object (evaluating 'main.value.scrollTop = val')`。真实点 TabBar 与 hash-goto 各 3 来回（共 12 次）均 0 复现；原始那次蹭在首屏按需编译 + LoginSheet 消失 + 乱点 的 churn 窗口里偶发一次。
- **根因/影响**：出自 `@dcloudio/uni-h5` `_scrollTopChanged` 对 `main.value`（滚动容器 ref）无 null 兜底，`:scroll-top` 的 watch 撞上 dev 态重挂载窗口、容器 ref 瞬时为 null 即抛。**非本仓代码**，且只存在于 H5 运行时——生产目标 mp-weixin 的 `<scroll-view>` 是原生组件、无此 JS 路径，生产端不可能发生。P3：页面照常渲染，仅跳过一拍程序化滚动定位，无崩溃/数据/钱影响。
- **状态**：✅ 不修（第三方框架 + 无稳定复现 + 非生产平台，遵「没复现别改」）。归因根因#8「H5 console 噪声 ≠ 真会坏」。未动任何代码。

### 2026-06-18 · W：扫码激活跳目录仍显默认课（激活小熊看到小鸭）——current 恒 list[0]，单课样本掩盖（根因#8）　✅ 已修
- **根因**：`store/courses.js` `current` 写死 `list[0]`（注释自承「暂只有一门·多课按激活集取」TODO 未做）。种子/测试只 `course-duck` 一门时 `list[0]` 恰等于用户课、看不出；真上小熊第二门后，激活它跳目录仍取列表第一门。catalog 也从不收 courseId（welcome `redirectTo` 不带参）。**与订单白屏 [[Q]]/admin 白屏 [[T]] 同根**：单一样本碰不到「该取哪门」分支。
- **修复 / 同类隐患 / 状态**（结构性·根因#8）：courses store 加 `currentId`+`setCurrent`，`current` 改「currentId 命中→那门·未指定/脏 id→回退 list[0]」；welcome 跳目录带 `?courseId=`（confirm/mine 两路）；catalog `onLoad` 读 courseId→setCurrent、`onShow` 无指定默认聚焦 `act.mine` 已解锁课。隐患：凡「多实例按用户态选一个」别写死 `[0]`（单实例恒对·多实例才暴露；本仓 products/orders 展示全部、progress 按 courseId 已对）。守卫 `catalog-course-by-activation`(roots#8) 锁三处接线 + `courses-store.test.js` 2 门课样本行为锁；反向自检×2 过。✅ 守卫 46→47·测试 405→410·check 全绿·**纯前端进 v0.9.5 攒批发**（[[miniapp-publish-rate-limited]]）。

### 2026-06-18 · V：首页「回到顶部」按钮 iPhone 错位——bottom 写死不含底部安全区、没跟 TabBar 抬高（根因#8）　✅ 已修
- **现象**（用户真机·iPhone 报·安卓正常）：首页滚动后右下「顶部」按钮位置不对（相对浮动 TabBar 偏低/相贴）。
- **根因**：TabBar 底部 `padding: 0 28px calc(8px + env(safe-area-inset-bottom))`，iPhone home indicator 让 TabBar 整体上抬 ~34px；BackTop 浮于其上却写死 `bottom: 96px`（不含安全区）→ 不跟随、相对 TabBar 错位。安卓无 home indicator（inset=0）故 96px 正好——构建/安卓看不出（根因#8）。
- **修复**：BackTop `bottom: calc(96px + env(safe-area-inset-bottom))`，与 TabBar 同步随底部安全区抬高，两端保持恒定间距。
- **同类隐患（结构性·根因#8）**：浮于 TabBar 之上的**底部固定浮层须含底部安全区**——全库扫 welcome(`56px+env`)/cart(`84px+env`) 等均已带 `env(safe-area-inset-bottom)`，唯 BackTop 漏（已补）。底部安全区走 `env(safe-area-inset-bottom)`、顶部走 `systemBar.js`（CLAUDE §6·别混）。
- **是否结构性 / 状态**：归因根因#8（iPhone 真机安全区机器测不出·已 CLAUDE 靠人；同类已扫齐）。✅ 已修（待用户 iPhone 复验）。

### 2026-06-18 · U：订单列表 5 tab 横滑掉帧——enhanced scroll-view 嵌 swiper 内与横滑手势抢资源（根因#8 真机才暴露）　✅ 已修
- **现象**（用户真机报）：order-list 五个状态 tab 手指横滑切换不顺滑、掉帧。H5/快网无感（根因#8）。
- **根因（多因·分两轮收敛）**：① 上一批「恢复拖动感」给 swiper-item scroll-view 加了 `enhanced`+`bounces`，mp `enhanced` 带自己手势识别器、5 个嵌 swiper 内与横滑手势仲裁抢资源；② swiper **一次性渲染全部 5 个 tab 列表**，横滑时要合成全部内容 → 掉帧；③ swiper 默认切换时长 **500ms 偏长** → 「慢」。
- **修复（两轮）**：第一轮撤 `enhanced`/`bounces`（→普通 scroll-view，保留 refresher 下拉+`@scrolltolower` 触底+原生惯性）——用户复验仍掉帧+慢，证明 enhanced 只是其一。第二轮：**懒渲染**（`v-if="nearTab(ti)"` 只渲当前±相邻 tab，相邻已预渲染、逐个横滑不空白；远跳中间短暂空·罕见可接受）+ **`:duration="280"`** 缩短切换。守卫 `main-scroll-elastic` 同步移除 order-list（注明 scroll-view 嵌 swiper 内不开 enhanced）。
- **同类隐患（结构性·根因#8）**：① `enhanced` scroll-view 不嵌 swiper——全库扫只 order-list（DetailGallery swiper 内无 scroll-view）；② swiper 内多个长列表须懒渲染（只渲相邻）+ 控切换时长，否则横滑合成全部内容掉帧——本仓多 tab swiper 仅此一处。弹性回弹与「横滑切 tab」二选一时横滑顺滑优先。
- **是否结构性 / 状态**：结构性（守卫 `main-scroll-elastic` 划清「swiper 内不开 enhanced」；懒渲染/时长属 swiper 内多列表的性能套路·记此账本备查）。归因根因#8（真机才暴露+「构建过≠真机顺」·已 CLAUDE 靠人）。✅ 已修（待用户真机复验顺滑）。

### 2026-06-17 · T：admin「商品与上新」整页白屏——一个缺 skus/images 的脏商品让列表渲染崩（根因#8 真实数据形状·同 Q）　✅ 已修
- **现象/判定**（用户报·配 urlPrefix 时进 `https://www.luckyducky.cn/#/products` 白屏，其它页正常）：admin ProductList 白屏。其它路由正常＝定位钥匙（数据相关、单页崩）。
- **根因**：`store/products.js` `load()` 把云端 productsDraft **原样赋值、未归一化**。库里有缺 `skus`/`images` 的脏商品（旧 schema / 半建 / 控制台手改——`newProduct()` 不产此形状故为外部数据），ProductList 模板 `p.skus.length`/`p.images.length` 与 `stepDone` 的 `p.skus.every` 遇 undefined 即抛 → 整页白屏。**与订单白屏 [[Q]] 同根**：列表对云返回单条文档做 `.length`/`.every`，未当不可信形状边界防御。
- **修复**：入库边界一处收口——抽纯逻辑到 `store/productShape.js`（无云依赖·可测）+ `normalizeProduct` 保证 images/skus/params/detailSections/kit 恒为数组，`load()` 里 `list.map(normalizeProduct)`；脏商品降级安全形状、绝不白屏。顺带解耦：纯形状逻辑从引云 API 的 store 抽出，store re-export 保持调用方 import 不变。
- **同类隐患（结构性·根因#8）**：「列表/向导渲染云返回文档」都是不可信形状边界，`.length`/`.map`/`.every` 前须保证字段存在。测试样本永远带全字段＝样本失真。守卫：`tests/admin/products.test.js` 4 条锁（缺键/null/undefined→[]、保留已有、渲染访问点+stepDone 不抛）。反向自检：去归一化→3 红（含「渲染访问点不抛」）。
- **状态**：✅ 已修（`npm run check` 全绿·测试 +4）。**残：需重新部署 admin 才修复线上白屏**（线上是旧构建·代码侧已修）；控制台核查/补脏商品 items 是数据清理、非代码缺陷。

### 〔06-16 及更早已结案旧案已卷档〕
> 06-13~16（S 遮罩滚动锁 / R sticky 假绿 / Q 脏订单白屏 / P 压测桩≠真 / O 物流跳转 / N 加购假反馈 / M 返回防误退 / L 首页 UI）→ `archive/调试日志-至20260616.md`；06-12 及更早（K 外部审核批次A / 购物车 / login _openid / 沉浸页避让 / 工具僵尸实例 等）→ `archive/调试日志-至20260612.md`


---

## 待修（已记录,排期单独处理）

### H · 退货权失效只认 'paid' 订单——真实流程里几乎永远不触发（启发式审核 2026-06-11）　✅ 已修
- **现象/判定**(2026-06-11 启发式逻辑链审核发现,非线上报障):业务规则「确认开始观看 → 失去该商品退货权」。`confirmEnter` 里失退货权的反查写死 `orders.where({ _openid, status: 'paid' })`——只在订单**仍是「待发货」**时才翻 `items[].refundable=false`。
- **根因**:触发条件与真实顺序错位。真实流程是 下单(paid) → 商家发货(shipped) → 顾客收货(done) → 扫包装内码 → `confirmEnter` 确认进课。**等顾客确认进课时订单早已不是 paid**(是 shipped/done),`where({status:'paid'})` 扫不到 → 退货权失效**静默不执行**。顾客已进课,系统却仍标 `refundable:true`。与「确认收货不能跳过发货状态」同类:一个状态条件设窄,让本应发生的链路断掉。
- **同类隐患/启发式**:状态机里凡是「A 动作触发 B 副作用」,要核对 **B 反查的状态集是否覆盖 A 可能发生的所有时点**——退货权失效(B)能在订单任意状态(A 时点)发生,却被钉死在单一状态。`shipOrder`/`confirmReceive` 的状态门是「正向流转」(只允许特定前态),而本处是「副作用反查」,反查不该用正向流转的窄条件。
- **影响/紧迫度**:当前**潜在**——真实退款功能尚未实现(售后仍样例),没有代码读 `refundable` 去放行/拒绝退款,故暂无线上后果;但逻辑是错的,**P4 接真实退款时若不先修,会按错误标记放行退款**(已进课仍可退)。规格注明 `refundable` 为「展示性标记,裁决以退货验收扫码为准」→ 有兜底,故非 P1;但 `status:'paid'` 的窄化是疏漏而非设计意图。
- **修法**:✅ 已修(2026-06-11,本批)——订单状态过滤改 `status: _.in(['paid','shipped','done'])`,收货后(shipped/done)进课也能失退货权;补 `tests/cloud/confirmEnter.test.js` 用例锁定(shipped 订单进课失退货权),反向自检:还原成 'paid' 该用例变红。
- **是否结构性**:业务逻辑层,非工具/环境。原 P2,与 I 同批修复并补测试关账。**待 P4 真实退款上线时复验**(届时 refundable 才被真正读取)。

### I · confirmEnter 退货权失效未限定「首次进课」，重复调用会多扣（逻辑架构再审核 2026-06-11）　✅ 已修
- **现象/判定**(2026-06-11 基于 `业务逻辑架构.md` 逐链复核发现):`confirmEnter` 里写 `enteredAt` 被 `if (!enteredAt)` 守着（只写一次,幂等）,**但紧随其后的「退货权失效」块在 if 之外、无条件每次都跑**。同一个激活码若 `confirmEnter` 被调用第二次,会跳过 enteredAt 写入、却**再次执行失效反查**,翻掉「下一条最早仍可退」的条目 → 多扣一笔订单的退货权。
- **根因**:副作用（失退货权）应与「首次进课」原子绑定,却被放在幂等守卫之外。
- **触发难度/现状**:客户端有两道软防护——`confirming` 标志防双击、已确认后重扫返回 `'mine'` 态不再进确认屏 → 正常 UI 下每码只确认一次。**但服务端不幂等**:双击竞态、重试、或绕过 UI 直调都会重复执行。当前**潜在**(真实退款未实现,无人读 refundable)。
- **同类隐患/启发式**:「法律节点」「一次性副作用」类云函数(确认进课、未来支付回调、核销)必须**服务端自身幂等**,不能只靠前端防重——与 [[H]] 同根(都在 confirmEnter 失效块),应一并修。
- **修法**:✅ 已修(2026-06-11,与 H 同批)——失退货权块**移进 `if (!enteredAt)` 内**,只在首次进课执行一次;重复 confirmEnter 不再翻第二笔。补 confirmEnter 测试:两笔 paid 订单 + 二次确认,断言只扣最早一笔、第二笔仍可退。
- **是否结构性**:业务逻辑层。P3,与 H 同批关账。**待 P4 真实退款复验**。

### E · 真机调试画面空白 +「我」页自动化挂死（工具僵尸实例复发）　⏳ 待复验
- **现象**(2026-06-11):① 用户开真机调试,手机画面空白;② 同一工具实例上自动化 reLaunch/screenshot
  「我」页两次挂死(首页正常),早前一次 RPC 探活通过但后续调用永不回包。
- **根因**(判定):2026-06-10「工具僵尸实例」坑复发——本会话曾在工具开着项目时执行 `build:mp-weixin`
  覆盖 dist,且强杀过一个 reLaunch 悬挂中的自动化脚本,实例进入「握手成、RPC 不回包」僵态;
  僵态期间发起真机调试,推到手机的是坏会话 → 白屏。**非业务代码问题**:按既定解法
  `cli quit` + `cli auto` 重启后,模拟器 首页/「我」/资料编辑 三页渲染与截图全部正常。
- **同类隐患**:自动化、`build`、真机调试共用一个工具实例时互相踩;build 覆盖 dist 期间 / 强杀自动化之后,工具状态不可信。
- **是否结构性**:工具/流程层。守则:跑 build 前先停自动化;工具任何怪象(白屏/假性缺件/卡死)先
  `cli quit` + `cli auto` 重启,别在僵实例上排查业务代码。
- **状态**:⏳ 工具已重启(项目 `dist/build/mp-weixin`,资料上云新代码),模拟器眼校全过;
  真机调试重连由用户复验,过了即关账。

### D · 开发者工具里调云函数触发 WAServiceMainContext timeout　⏳ 待查
- **现象**:微信开发者工具里页面调云函数(如 `callCloud('getProducts')`)触发 `WAServiceMainContext timeout`。
- **应急(已撤销)**:2026-06-07 曾在工作区临时把 `utils/cloud.js` 改成 `CLOUD_ENABLED=false` 全禁云、页面回退本地数据;
  这批改动**未提交、已 `git restore` 撤销**——只是把"临时关云"的应对去掉,**timeout 本身根因未查**。
- **根因**:待查。候选方向(未验证):工具版本/基础库版本、`wx.cloud.init` 时机、网络/环境配置、模拟器与真机差异
  (真机上 `getOpenId`/`getProducts` 此前 invoke 是 ok 的,疑似集中在开发者工具模拟器)。
- **同类隐患**:所有走 `callCloud` 的页面在开发者工具里都会受影响;别因工具端 timeout 误判云函数本身有问题。
- **是否结构性**:待定(可能是工具/环境层,非业务代码)。
- **状态**:⏳ 观察中。2026-06-10 P3 端到端验证在开发者工具连续多轮 `callFunction`(激活/确认/下单/查询)均正常,**未复现**;
  可能与当时工具版本或环境瞬态有关。再次出现时按原计划看控制台/网络面板定位;长期不复现可关账。

---

## 已知边界 / 设计权衡(故意保留,非 bug)
- **默认样例地址**(`address.js` 初始 `SAMPLE_ADDRESS`):新用户不填也能下单。外部审核 P1 建议改空;**2026-06-07 决定保留**——demo 开箱即用、方便演示下单闭环。真实上线前应改 `list: []`。
