# 调试日志 / 复盘记录（luckyducky-fable 专属）

> 项目的「问题账本」——每发现一个问题就记一条，追根因、防同类隐患（配套基础设施与格式约定见 `CLAUDE.md`）。
> 分叉前（next 仓的完整案底）已归档：`docs/archive/调试日志-next基线-至20260703.md`。

## A（fable）· 2026-07-04 · 真单联测退款触发失败 REFUND_TRIGGER_FAIL（进行中·待控制台取证）

- **现象**：M2 钱链真单联测——新线 app 下单支付成功（订单 `2026070413019480`·2 元·微信交易号 `4200003134202607040164483193` 在案），申请售后成功，旧线 admin 审批同意 → `REFUND_TRIGGER_FAIL`，重试 3 次同败（adminApi 日志 13:04 三连）。
- **已排除**（逐项证据）：① `config.pay` 与正册 `console-assets/01` 零漂移（flowId/refundFlowId/mode/subMchId 逐字对上·tcb db 实读）；② 支付工作流同一连接器同一模块**当场走通**（真交易在）→ 连接器凭证、模块可调性无恙；③ refundCallback 零调用日志 → 微信侧从未受理退款（不是回调丢失）；④ 售后单被条件回滚保护回 `applied`、订单仍 `paid`——**账面零损伤，随时可重试**；⑤ 同商品同 2 元退款 6/12、6/14 两次真退成功 → 旧线代码与入参形状无回归（今日入参与当时逐字同构）。
- **收窄结论**：败点隔离在**退款工作流 `kbgzl_n8gojr3a` 本身**（控制台资产 #3）——6/14 后平台侧漂移（被删/未发布态/平台改版失效三选一），代码侧无法进一步取证：`kit/flow.callFlow` 的 `.catch(() => null)` 把模块真实报错吞了（**次生可观测性缺陷**·新旧两线同有——镜像继承）。
- **待办**：① 用户按 `console-assets/README` preflight 查控制台工作流状态 + 运行记录错误文本；② ✅ 已修（`59cd5b4`·callFlow 两失败分支落 `[LD_ALERT]`·守卫 rw-flow-observable 先红后绿）；③ 修复后重审批该售后单即可原路退回（无需新单）。
- **归因**：根因#12（平台接缝单方漂移——正册预言的场景第一次真实命中）+ **根因#14 失败静默化**（本案为扳机·2026-07-04 全日志回归分析并立，同宗 AA-②/AC-②/AD 一并归位）。结构性：是（接缝资产在 git 外，只能靠正册+预检勤核）。
