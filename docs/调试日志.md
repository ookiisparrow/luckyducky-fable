# 调试日志 / 复盘记录

> 项目的「问题账本」——每发现一个问题就记一条。目的不是记录 bug 本身,而是
> **追到结构性根因、顺藤摸出同类隐患**,避免碎片化地一个个救火。
>
> 配套基础设施:全局错误捕获(`main.js` / `App.vue` → `utils/logger.js`)、
> 数据契约校验(`store/persist.js` 的 `sanitize` + `utils/validate.js`)、
> 回归测试(`tests/`,`npm run test`)。
>
> **每条格式**:日期 / 现象 / 根因 / 同类隐患(启发式) / 是否结构性 / 状态。

---

## 2026-06-07 · 购物车列表显示空白行　✅ 已系统性处理

- **现象**:小程序里购物车商品行只剩复选框 +「¥」,图/名/价/数量步进器全空;合计仍显示 ¥1。
- **根因**:持久化的**历史脏数据**——早期测试存进 storage 的残缺条目(缺 name/price 等),
  冷启动被 `persist.js` 原样 `$patch` 回灌。精确产生时刻不可考(开发期跨版本/热重载),
  但**真正病根是持久化回灌零校验**:`$patch(JSON.parse(saved))` 只防 JSON 解析失败,
  不防"解析成功但内容残缺"。
- **同类隐患(启发式)**:
  - 三个 store 都持久化、都无回灌校验(cart / address / user)。
  - **数组类状态才是脆弱点**:`$patch` 对对象深合并(user.profile 有完整 default,相对安全),
    对数组整体替换(cart.items / address.list 元素无兜底,直接撑乱)。
  - **address.list 同病、且更危险**:残缺地址会污染结算页默认地址、影响下单。
  - 无 schema 版本/迁移 → 每次改数据结构都在埋雷,会反复发生。
- **是否结构性**:**是**。不是购物车一个页面的 bug,是持久化层 + 数据边界的结构性缺陷。
- **状态**:✅ 已处理。数据契约校验(`persist.sanitize` + `keepValid`,cart/address/user 各配契约,
  清洗时 `logger.warn` 留痕)+ 全局错误捕获 + 回归测试(`tests/persist.test.js`、`validate.test.js`
  锁定);残留的临时脏数据用微信工具「清缓存」清除。

---

## 待修（已记录,排期单独处理）

### A · 沉浸页顶部悬浮按钮过高,撞状态栏 + 胶囊　⏳ 待修
- **现象**:商品详情「分享」、课程目录「收藏」、播放页「更多」等浮在图/视频上的按钮 top 太靠上,
  撞状态栏、撞微信胶囊。
- **根因**:浮层按钮 top 用固定 `env(safe-area-inset-top)`+小值,既没可靠避状态栏、也没给胶囊让位
  (06-07「非沉浸页避胶囊」那次,沉浸页按约定未动)。
- **同类隐患**:涉及 detail / catalog / player / welcome 多个沉浸页。
- **是否结构性**:中等——可复用已建的 `utils/systemBar.js`(statusBarHeight / capsuleGap)统一修。
- **状态**:⏳ 待修。

### B · 视频播放页顶部下移避胶囊后露大面积黑块　⏳ 待修
- **现象**:播放页顶部 UI 整体下移避胶囊后,顶部露出大片黑块。
- **根因**:待查(可能视频/背景布局假设了顶部贴边)。
- **是否结构性**:单页,需单独设计。
- **状态**:⏳ 待修(建议与 A 一起做,都属沉浸页顶部)。
