# 调试日志 / 复盘记录

> 项目的「问题账本」——每发现一个问题就记一条。目的不是记录 bug 本身,而是
> **追到结构性根因、顺藤摸出同类隐患**,避免碎片化地一个个救火。
>
> 配套基础设施:全局错误捕获(`main.js` / `App.vue` → `utils/logger.js`)、
> 数据契约校验(`store/persist.js` 的 `sanitize` + `utils/validate.js`)、
> 回归测试(`tests/`,`npm run test`)。
>
> **每条格式**:日期 / 现象 / 根因 / 同类隐患(启发式) / 是否结构性 / 状态。

---

## 已结案（教训沉淀,详细推理见 git 历史）

### 2026-06-15 · O：物流卡点不进——快递100 插件跳转写法错（plugin-private:// → plugin://别名）　✅ 已修
- **现象**（用户报）：admin 填发货信息后，小程序订单页出现物流信息条，但**点了进不去**快递100 轨迹页。
- **根因**：上一批（express-plugin-wired）写的跳转 URL `plugin-private://wx6885acbedba59c14/pages/result/result?nu=&com=` **基于公开资料推断、未真机验（根因#8）**，写法错——微信插件正确跳法是 **`plugin://<别名>/<导出页>`**（别名＝manifest plugins 的 key `kuaidi100`，导出页 `index`），不是 `plugin-private://APPID/内部页`。且参数应为 `num`（非 `nu`）+ `appName`（小程序名·必填）+ `com`（编码·**选填，不传插件自动识别单号**）。
- **修复**：`plugin://kuaidi100/index?num=<运单号>&appName=<店名>&com=<编码>`；因 com 选填，去掉「未命中编码就只复制」的门槛——**微信端总是开插件**（com 有则带、无则插件自认），打开失败/非微信端才回退复制。守卫 `express-plugin-wired` 同步改判 `plugin://kuaidi100/`（反向自检：改坏 scheme→红→还原）。
- **同类隐患（结构性·根因#8）**：**平台 SDK/插件的调用契约（scheme/页名/参数）必须真机验，不能照搬博客/推断**——「构建过 + 守卫绿」只证「按我以为的写法编进去了」，不证「写法对」。守卫只能锁「用了 plugin:// 别名跳转」，**跳转真能打开仍须真机**。
- **状态**：✅ **已修并验证通过**（2026-06-15 用户工具/真机验：插件能打开 + 物流信息正确）。残留「加载慢」＝快递100 插件实时查件的第三方延迟、咱不可控（传 com 省自识别一步略快）。首次跳转弹「安装插件」须点安装。

### 2026-06-15 · N：首页商品卡「＋」加购按钮接成「已收藏」假反馈　✅ 已修（守卫 home-card-add-real）
- **现象**（用户报）：首页商品卡的 ＋ 加购按钮，点了弹「已收藏」、不进购物车。
- **根因**：`pages/index/index.vue` 的 `onProductAdd`（ProductCard `@add` 处理）函数体只写了 `ping('已收藏 …')`＝收藏占位⑪ 的假反馈，**漏调 `cart.add`**——ProductCard 契约里 ＋＝「加入购物车」(`emit('add')`)，父页处理却接成了收藏。**病根#6**「假反馈泄漏」（用户动作伪成功反馈而无真效果，同账本 K-⑤「拒单吞进回退＝本地伪成功」）。
- **修复**：`onProductAdd` 按 `p.id` 取 canonical 商品（`products.getById` / `getProduct` 回退，价为数字）→ `cart.add({id,name,tag,price,was})` → toast「已加入购物车」。**注**：FeatureProducts 传上来的是**展示形状**（价为 `yuan()` 字符串、字段 `now` 非 `price`），直接塞 cart.add 会存成 `￥undefined`——故按 id 回取 canonical（同详情页 addToCart 模式）。
- **同类隐患（结构性）**：「占位假 toast 占了真功能按钮」家族——分享(R29)/客服(R18) 已各治，加购此条同源；卡片 `@add/@open/@service` 等动作事件，父页 handler 要真做事、别留占位假反馈。守卫 `home-card-add-real`（onProductAdd 须调 cart.add 且无「已收藏」）。
- **是否结构性**：是（假反馈泄漏 病根#6 + 展示/canonical 形状混用陷阱）。
- **状态**：✅ 已修（守卫 24→25 repoCheck、反向自检过、测试 309、build:h5+mp-weixin DONE、真机待验加购进车）。

### 2026-06-14 · M：Tab 页「返回直接退出小程序」防误退　✅ 已解（分支 v0.9.1.1返回重构版）
- **现象**：自绘 TabBar 用 reLaunch 切页，Tab 页是栈底页 → 返回手势/键 = 直接退出小程序，用户常误退。
- **两个死路（真机实测排除）**：① `enableAlertBeforeUnload` 只拦「页面间 navigateBack」，**拦不住栈底 Tab 页退出**（真机无弹窗）；② `page-container` 能拦返回手势/键，但 **show=true 武装时锁死页面级滚动**（overlay=false 也改不了，真机页面不能滑）。
- **活路（PoC 验证成立）**：页面内容套 `<scroll-view>` → 滚动发生在 scroll-view 内部、page-container 锁的是「页面级滚动」锁不到 → 既能武装拦返回、内容又能滚。双击退出：第一次弹 toast「再按一次退出」+解除武装，2s 内再返回放行、2s 后重武装（`composables/useExitGuard.js`）。
- **迁移代价**：三 Tab 页内容进 scroll-view；首页页面级滚动 API 全迁——`onPageScroll`→`@scroll`、`pageScrollTo`→受控 `:scroll-top`（先同步当前位、下一拍再设目标，绕过「相同值不触发」）、购买滚到商品卡改 FeatureProducts emit 目标给父页滚。
- **同类隐患（结构性）**：mp-weixin **无 onBackPress、拦不住栈底页退出**；要拦返回手势只能 page-container，而它锁页面滚动 → **凡需拦返回的可滚页面，内容都得放 scroll-view**（新 Tab 页照此）。
- **状态**：✅ 三页真机过（能滚 + 双击退出 + 首页购买/回顶/探索滚动正常）。在分支，待商量合 main。

### 2026-06-14 · L：首页 UI 三处（假指示条黑块 / 购买滚不到卡 / 横滑卡不等高）　✅ 已修
- **现象**：① tabbar 下方多一条黑（真机）；② 点首页「购买」滚动后只看到大图、商品卡在屏外；③ 首页商品卡 / 买家秀卡高度参差（「幸运小鸭礼盒」矮于「进阶套装·小伙伴们」）。
- **根因**：① TabBar 照网页原型多画了个假 home 指示条（`#1c1c20` 小条），真机系统自带、重复成黑块；② 「购买」滚到商品区**区顶**，而区顶是全屏方图+文案、卡片在 ~550px 下，且方图高随屏宽变、固定偏移对不准；③ 横滑卡**内容行数不固定**——ProductCard 名称、ReviewCard 文案长短不一，撑出不同高度。
- **修复**：① 删假指示条；② FeatureProducts 暴露 `scrollToProducts`，按 `rect.top + 当前滚动量` 算、让卡片行**垂直居中**；③ 名称/文案 `-webkit-line-clamp` 固定行数 + `min-height`、tag 保底 1 行。
- **同类隐患（结构性）**：**凡横滑/网格卡片，内容（名称/文案/标签）必须固定行数 + min-height 才能等高**——新卡片组件照此。假"系统 UI 仿制元素"（指示条/状态栏）真机会与系统重复，移植网页原型时要剔除。
- **是否结构性**：③ 是（卡片等高范式）；①② 偏一次性（原型移植残留 / 单处滚动逻辑）。
- **状态**：✅ 已修（纯 UI，`npm run check` 全绿 277 + 真机验）。

### 2026-06-12 · K：外部审核四份对账——批次A 修六类安全/钱问题　✅ 已修（批次A）
- **来源**:桌面四份外部审核报告(前三份基线为当日上午 PR-1 阶段,其 P1「前端直跳支付成功」「pending-pay 假页」当天已被 #68/#69 修复;有效清单以 20:56 v0.9 基线那份为准)。
- **批次A 修复**(每条带反向测试,测试 149→162):
  ① **回调防伪闸**(最关键):payCallback/refundCallback 可被小程序客户端 callFunction 伪造「支付/退款成功」——客户端调用必带 OPENID、工作流服务端调用无,见 OPENID 即拒改状态(静默 ACK 防探测)。
  ② **退款回调核验 fail-closed**:成功通知须订单号+退款金额与售后单一致,不符留 refundMismatch 痕不置 refunded。
  ③ **退款审批原子抢占**:approveRefund 条件更新 applied→approved 抢占成功才触发外部退款(防双窗口并发双退款),工作流失败回滚 applied 可重试。
  ④ **下单服务端不变量**:主商品必含(搭配购不可单买)+地址四要素必填+手机号基本校验;订单号 doc.set 改 add 库级唯一+撞号重试(同分钟仅 9000 随机空间,原实现碰撞会覆盖旧单)。
  ⑤ **交易动作禁本地伪成功**:api/order.js 区分「无云(null)→演示回退」与「云异常/拒单→上抛」;顺带修掉原 catch 把拒单 throw 也吞进回退的暗病。
  ⑥ **状态流转条件更新**:confirmReceive(仍 shipped 才 done)/shipOrder(仍 paid|shipped 才写,防把 done 回滚)/confirmEnter(enteredAt null 抢占,防并发双扣退货权)。
- **折中决策**(用户拍板):支付回调金额不符**保持置 paid**(金额源自库内、通道可信、钱已收)但留 feeMismatch 痕 → 控制台标红+禁发货+「已核实解除」动作(adminApi.clearFeeMismatch),兼顾用户体验与发货安全。
- **同类隐患**(记账,批次B/技术债):SKU 购物车残留、播放器段末跳段、genQrcodes count 静默为 1、分片 seq 连续性、固定 limit 系列(订单20/后台200/评价200)、users/progress 先查后建重复文档、admin 口令 bootstrap 抢占窗口。
- **现象**:云开发控制台绑定商户号报错 268500896——「小程序提供实物交易服务,要求接入订单发货管理。暂不支持使用云调用支付能力,请使用工作流接入微信支付」。`cloudPay.unifiedOrder` 整条路被平台对实物类小程序封死。
- **根因**:微信平台规则(非代码缺陷)——实物交易类小程序必须接入「订单发货管理」,且支付只开放「工作流」通道(商户 APIv3 凭证 + 控制台工作流),旧云调用通道不再开放。
- **改造**:`pay` 函数下单段改为触发支付工作流(`cloudbase_module`,flowId 存 `config.pay.flowId`,openid 显式传入、金额仍取库内);`payCallback` 归一化兼容 v3 回调形态(`out_trade_no/trade_state/transaction_id/amount`)与旧 v2 字段,幂等逻辑不变。闸门/前端/测试体系零改动。
- **同类隐患**:① 「订单发货管理」开通后,发货须向微信上报运单号,否则资金结算受限——`shipOrder` 后续要补上报(Batch 2 范围,初期可在小程序后台手工录);② 控制台侧资产(连接器/工作流)不在 git 里,已在工作日志记「正册」清单防误删(连接器 wxpay_33nb7su / 主工作流 sywxzfapifqzf_nncvqss2 / paynotify·refundnotify 回调流)。
- **状态**:✅ 代码已改造(134 测试绿);配置侧全通(签名/回调地址验证至 openid 必填即终点);剩部署 + paynotify 转发节点 + 真机验收单 Y。

### 2026-06-07 · 购物车列表显示空白行　✅ 已系统性处理
- **现象**:storage 历史脏数据被 `persist.js` 原样 `$patch` 回灌,购物车商品行字段全空。
- **教训**:**持久化回灌必须过数据契约校验**(`$patch(JSON.parse(...))` 只防解析失败,不防内容残缺;数组类状态最脆弱)。
- **状态**:✅ 已处理——`persist.sanitize` + `keepValid` 契约(cart/address/user)+ 全局错误捕获 + `tests/persist.test.js` 锁定。

### 2026-06-07 · 购物车契约漏校验 qty（外部审核 P2）　✅ 已修
- **现象**:`sanitizeCart` 漏校验 qty,无 qty 的脏条目过清洗 → `count` 累加成 NaN。
- **教训**:**契约要覆盖模板 / getter 实际读取的所有字段**,测试数据要贴近真实结构;布尔字段也要归一化。
- **状态**:✅ 已修——qty 正整数校验 + selected 归一化 + `persist.test.js` 断言锁定。

### 2026-06-07 · 云函数 login 漏写 _openid（P0 登录联调）　✅ 已修
- **现象**:云函数 `add` 的 `users` 记录没 `_openid`,按 `_openid` 查不回。
- **教训**:**云函数是管理端,写库必显式写 `_openid`**(只有小程序端 add 自动写;将来 activations/orders 同理);返回别依赖"add 后立即查",直接构造返回。
- **状态**:✅ 已修——`login` 显式 `_openid: OPENID`。

### 2026-06-10 · 沉浸页浮层撞状态栏/胶囊 + 播放页避让露黑块（原待修 A/B）　✅ 已修并眼校关账
- **现象**:详情「分享」、目录「收藏」、欢迎「关闭」、播放「更多」等浮层按钮撞状态栏/胶囊(A);
  播放页整体下移避让后顶部露大片黑块(B)。
- **教训**:**沉浸页避让一律"动浮层、不动底层媒体/背景"**;顶部定位别只靠 `env(safe-area-inset-top)`,
  用 `systemBar.getSystemBarVars()` 与胶囊同水平带垂直居中 + 横向让位。
- **状态**:✅ 已修(PR #5)+ 自动化眼校通过(2026-06-10,`scripts/visual-check.cjs`:
  四沉浸页按钮矩形 vs 胶囊/状态栏判定全 ✓、与胶囊中线差 0px,截图复核无黑块;
  真机同层渲染如有差异再开新单)。

### 2026-06-11 · 控制台传图失败 UPLOAD_FAIL（HTTP 服务请求体 ~100KB 上限,原 F）　✅ 已结案
- **现象**:上传真实产品图报 UPLOAD_FAIL;此前 AI 的 E2E 用 1×1 测试图通过。实测 50KB 过/200KB 拒(`EXCEED_MAX_PAYLOAD_SIZE`),真实照片 base64 200–700KB 必超。
- **教训**:① **E2E 样本要贴近真实数据尺寸**——最小样本只验逻辑不验边界;② 同通道响应体也有上限,listDrafts 商品多后需分页;③ 网关层错误与业务错误形状不同,适配层必须兜底。
- **状态**:✅ 分片上传修复(`uploadChunk`/`uploadFinish`,~80K/片)已部署;用户随后用真实封面图上架成功、验收单 M 通过 = 复验关账(2026-06-11)。

### 2026-06-11 · 控制台传视频「直传失败 HTTP 403」（直传凭证只认 POST 表单,原 G）　✅ 已结案
- **现象**:第④步上传 mp4 报 403 终止。根因:`getUploadMetadata` 签发的凭证按微信 HTTP API 约定只认 **POST 表单上传**(签名串含请求方法),前端用 `PUT + Authorization 头` → 签名不符被拒;直传路径此前从未被真实走通(E2E 只验到取凭证成功)。
- **教训**:① 凭证类接口要按签发方约定的**完整请求形状**使用(方法/字段都参与签名,不能只对上 token);② 「双保险」要覆盖主路径的**全部失败点**,只兜第一步等于没兜;③ E2E 过了「拿到凭证」≠ 过了「用凭证」(与上条"最小样本只验逻辑"同根)。
- **状态**:✅ 直传改 POST 表单 + 上传失败自动回落分片(PR #52)并重发线上控制台;用户重传成功——云端小熊课程已带真实视频段并发布 = 复验关账(2026-06-11)。

### 2026-06-10 · 开发者工具假性「component not found」+ 自动化 RPC 卡死（工具坑,非业务）　✅ 已结案
- **现象**:① 工具报 `pages/checkout/index.json` 引用的页内组件 not found,但 dist 里 8 个产物文件齐全、路径正确;
  ② 自动化 `connect` 握手成功但所有 RPC(systemInfo 等)永不回包,visual-check 卡死。
- **教训**:① **build 重写 dist 期间工具会读到中间状态**(json 先落盘、组件文件后落盘)→ 报假性缺件,点「编译」即消;
  跑自动化眼校期间不要执行 `build:mp-weixin`。② **connect 成功 ≠ RPC 可用**:中途 `cli open` 重开项目会把
  自动化端口留成僵尸;解法 `cli quit` 彻底退出再 `cli auto --auto-port 9420` 重启(已在脚本加 RPC 探活守卫)。
- **状态**:✅ 已结案——脚本注释 + 探活守卫沉淀;重启后四沉浸页眼校全 ✓。

---

## 待修（已记录,排期单独处理）

### H · 退货权失效只认 'paid' 订单——真实流程里几乎永远不触发（启发式审核 2026-06-11）　✅ 已修
- **现象/判定**(2026-06-11 启发式逻辑链审核发现,非线上报障):业务规则「确认开始观看 → 失去该商品退货权」。`confirmEnter` 里失退货权的反查写死 `orders.where({ _openid, status: 'paid' })`——只在订单**仍是「待发货」**时才翻 `items[].refundable=false`。
- **根因**:触发条件与真实顺序错位。真实流程是 下单(paid) → 商家发货(shipped) → 顾客收货(done) → 扫包装内码 → `confirmEnter` 确认进课。**等顾客确认进课时订单早已不是 paid**(是 shipped/done),`where({status:'paid'})` 扫不到 → 退货权失效**静默不执行**。顾客已进课,系统却仍标 `refundable:true`。与「确认收货不能跳过发货状态」同类:一个状态条件设窄,让本应发生的链路断掉。
- **同类隐患/启发式**:状态机里凡是「A 动作触发 B 副作用」,要核对 **B 反查的状态集是否覆盖 A 可能发生的所有时点**——退货权失效(B)能在订单任意状态(A 时点)发生,却被钉死在单一状态。`shipOrder`/`confirmReceive` 的状态门是「正向流转」(只允许特定前态),而本处是「副作用反查」,反查不该用正向流转的窄条件。
- **影响/紧迫度**:当前**潜在**——真实退款功能尚未实现(售后仍样例),没有代码读 `refundable` 去放行/拒绝退款,故暂无线上后果;但逻辑是错的,**P4 接真实退款时若不先修,会按错误标记放行退款**(已进课仍可退)。规格注明 `refundable` 为「展示性标记,裁决以退货验收扫码为准」→ 有兜底,故非 P1;但 `status:'paid'` 的窄化是疏漏而非设计意图。
- **修法**:✅ 已修(2026-06-11,本批)——订单状态过滤改 `status: _.in(['paid','shipped','done'])`,收货后(shipped/done)进课也能失退货权;补 `tests/cloud/confirmEnter.test.js` 用例锁定(shipped 订单进课失退货权),反向自检:还原成 'paid' 该用例变红。
- **是否结构性**:业务逻辑层,非工具/环境。原 P2,与 I 同批修复并补测试关账。**待 P4 真实退款上线时复验**(届时 refundable 才被真正读取)。

### I · confirmEnter 退货权失效未限定「首次进课」，重复调用会多扣（逻辑架构再审核 2026-06-11）　✅ 已修
- **现象/判定**(2026-06-11 基于 `业务逻辑架构.md` 逐链复核发现):`confirmEnter` 里写 `enteredAt` 被 `if (!enteredAt)` 守着（只写一次,幂等）,**但紧随其后的「退货权失效」块在 if 之外、无条件每次都跑**。同一个激活码若 `confirmEnter` 被调用第二次,会跳过 enteredAt 写入、却**再次执行失效反查**,翻掉「下一条最早仍可退」的条目 → 多扣一笔订单的退货权。
- **根因**:副作用（失退货权）应与「首次进课」原子绑定,却被放在幂等守卫之外。
- **触发难度/现状**:客户端有两道软防护——`confirming` 标志防双击、已确认后重扫返回 `'mine'` 态不再进确认屏 → 正常 UI 下每码只确认一次。**但服务端不幂等**:双击竞态、重试、或绕过 UI 直调都会重复执行。当前**潜在**(真实退款未实现,无人读 refundable)。
- **同类隐患/启发式**:「法律节点」「一次性副作用」类云函数(确认进课、未来支付回调、核销)必须**服务端自身幂等**,不能只靠前端防重——与 [[H]] 同根(都在 confirmEnter 失效块),应一并修。
- **修法**:✅ 已修(2026-06-11,与 H 同批)——失退货权块**移进 `if (!enteredAt)` 内**,只在首次进课执行一次;重复 confirmEnter 不再翻第二笔。补 confirmEnter 测试:两笔 paid 订单 + 二次确认,断言只扣最早一笔、第二笔仍可退。
- **是否结构性**:业务逻辑层。P3,与 H 同批关账。**待 P4 真实退款复验**。

### E · 真机调试画面空白 +「我」页自动化挂死（工具僵尸实例复发）　⏳ 待复验
- **现象**(2026-06-11):① 用户开真机调试,手机画面空白;② 同一工具实例上自动化 reLaunch/screenshot
  「我」页两次挂死(首页正常),早前一次 RPC 探活通过但后续调用永不回包。
- **根因**(判定):2026-06-10「工具僵尸实例」坑复发——本会话曾在工具开着项目时执行 `build:mp-weixin`
  覆盖 dist,且强杀过一个 reLaunch 悬挂中的自动化脚本,实例进入「握手成、RPC 不回包」僵态;
  僵态期间发起真机调试,推到手机的是坏会话 → 白屏。**非业务代码问题**:按既定解法
  `cli quit` + `cli auto` 重启后,模拟器 首页/「我」/资料编辑 三页渲染与截图全部正常。
- **同类隐患**:自动化、`build`、真机调试共用一个工具实例时互相踩;build 覆盖 dist 期间 / 强杀自动化之后,工具状态不可信。
- **是否结构性**:工具/流程层。守则:跑 build 前先停自动化;工具任何怪象(白屏/假性缺件/卡死)先
  `cli quit` + `cli auto` 重启,别在僵实例上排查业务代码。
- **状态**:⏳ 工具已重启(项目 `dist/build/mp-weixin`,资料上云新代码),模拟器眼校全过;
  真机调试重连由用户复验,过了即关账。

### D · 开发者工具里调云函数触发 WAServiceMainContext timeout　⏳ 待查
- **现象**:微信开发者工具里页面调云函数(如 `callCloud('getProducts')`)触发 `WAServiceMainContext timeout`。
- **应急(已撤销)**:2026-06-07 曾在工作区临时把 `utils/cloud.js` 改成 `CLOUD_ENABLED=false` 全禁云、页面回退本地数据;
  这批改动**未提交、已 `git restore` 撤销**——只是把"临时关云"的应对去掉,**timeout 本身根因未查**。
- **根因**:待查。候选方向(未验证):工具版本/基础库版本、`wx.cloud.init` 时机、网络/环境配置、模拟器与真机差异
  (真机上 `getOpenId`/`getProducts` 此前 invoke 是 ok 的,疑似集中在开发者工具模拟器)。
- **同类隐患**:所有走 `callCloud` 的页面在开发者工具里都会受影响;别因工具端 timeout 误判云函数本身有问题。
- **是否结构性**:待定(可能是工具/环境层,非业务代码)。
- **状态**:⏳ 观察中。2026-06-10 P3 端到端验证在开发者工具连续多轮 `callFunction`(激活/确认/下单/查询)均正常,**未复现**;
  可能与当时工具版本或环境瞬态有关。再次出现时按原计划看控制台/网络面板定位;长期不复现可关账。

### C · 订单无实体数据贯通(外部审核 P1)　✅ 已修(2026-06-10 订单上云,PR #25)
- **现象**:提交 → 支付成功 → 订单详情不是同一笔数据(订单号随机、跳静态样例页;checkout 只传 amount)。
- **修法**:直接以「订单上云」解决——`createOrder` 云函数(云端定价 + 快照)+ `store/orders.js` 收口,
  checkout → paysuccess → order 详情按订单 id 读同一笔;原「前端快照」方案降级为 H5/App 回退。
- **教训**:贯通型问题若已临近对应的云改造里程碑,直接在云改造里一并解决,避免先做一版前端临时方案再推翻。
- **状态**:✅ 已修;真机端到端按 `验收手册.md` 验收单 E 验证。

---

## 已知边界 / 设计权衡(故意保留,非 bug)
- **默认样例地址**(`address.js` 初始 `SAMPLE_ADDRESS`):新用户不填也能下单。外部审核 P1 建议改空;**2026-06-07 决定保留**——demo 开箱即用、方便演示下单闭环。真实上线前应改 `list: []`。
