# 调试日志 / 复盘记录（luckyducky-fable 专属）

> 项目的「问题账本」——每发现一个问题就记一条，追根因、防同类隐患（配套基础设施与格式约定见 `CLAUDE.md`）。
> 分叉前（next 仓的完整案底）已归档：`docs/archive/调试日志-next基线-至20260703.md`。

## R（fable）· 2026-07-13 · 真机逮出：订单横滑「下半屏无效」＝手势区没盖满整屏 + 补方向滑入动画（根因#8）

用户真机走查 mp-7fixes 批2（订单横滑换 tab）逮出两条：
- **下半屏滑动无效**（已修·fix-order-swipe 批）：手势 `bind:touchstart/touchend` 原绑在 `.coolist-body` 上，而 body 无 min-height、高度只到订单卡底部——订单少时下半屏落在 body 外（是 `.coolist` 根 min-height:100vh 的背景区），touch 事件不落在绑定节点上→下半屏无反应。修：手势绑定上移到全高根 `.coolist`（bind 不 catch·不影响纵向滚/上拉翻页/卡片点按）。归因根因#8：静态检查/单测（测的是 swipeDir/nextTabKey 纯函数）逮不住「手势区实际覆盖屏幕多少」这种真机布局事，只有真机点得出来。守卫：`order-swipe.test.ts` 源码扫描断言手势绑在 `.coolist` 根（非 body）+ 根有 min-height:100vh（反向自检：绑定挪回 body 即红）。
- **滑动没有动画**（已补·同批）：原设计刻意抬指切换不做动画（防过度工程·spec-batch2 明载），用户真机后要动画。补轻量 CSS 方向滑入 keyframe（`anim-next` 自右入/`anim-prev` 自左入·0.24s）——switchTab 按 tab 索引算方向置 `_animDir`，reload 落新数据时消费一次应用到 `.coolist-body`（onShow/翻页 reload 不带动画）；不上 `<swiper>` 跟手（那要拆五份独立 tab 状态·与游标分页/_seq 冲突）。守卫：断言两方向 keyframe + wxml `{{anim}}` + ts 置向存在。

## Q（fable）· 2026-07-13 · mp-7fixes 定性两条：「大家都在问」错位＝兜底落页残留滚动位；首页无图＝数据缺失非代码

- **点「继续观看」页面上方冒出「大家都在问」**（已修 252f70f）：该文案全仓唯一出现点是首页第⑦板块 FAQ 标题（home.wxml）。「我」页学习卡兜底/支付成功回首页等 5 处意图性 `wx.switchTab('/pages/home/home')` 落地时，tab 页实例常驻、上次滚动位置被保留——用户此前滚到过 FAQ 区块，落地视口顶部即该标题，被误读为「弹出错误文案」。修：lib/homeIntent 读即清标记 + home.onShow 消费回顶 + `goHomeTab()` 统一出口（评审扩线逮出 paysuccess/checkout×2 同类漏挂）；正常手点 tabbar 切页不置位、滚动位置保留不变。归因根因#8 姊妹病：静态检查/测试逮不住「导航落点视口语义」，靠现象反推 + 全仓唯一文案定位法破案。
- **首页图片不显示**（定性·代码零改动）：tcb 只读实查生产库——products 7 条中 prod-1~5（种子直灌）根本无 cover 字段（键不存在，非空串）；content/home 文档只有文案字段，hero.img/feature/reassure/reviews/closing 整组视觉字段从未写入。前端 wx:if fail-soft 色块占位按设计工作，非 bug；「换链失败/cloud:// 直渲」次级假设被数据实查排除。修复=运营 admin 补录（见待办与债「mp-7fixes 战役遗留」）。教训：症状像前端 bug 的先查数据实况再动代码——本次靠只读 DB 实查免去一轮无谓代码「修复」。
- **worktree 首次导入开发者工具连环坑**（2026-07-13 用户验证走查踩中·纯本地已解·记录防复发）：`project.config.json` 属 gitignore 本地文件→新 worktree 里不存在→工具导入时自建空壳（无 appid→「清除登录状态」报 41002 appid missing；无 `useCompilerPlugins:["typescript"]`→`.ts` 不编译），并给「缺 .js」的页面自动铺 quickstart 空模板 `.js`（本次 23 个·同名 `.js` 打包优先于 `.ts`→页面真逻辑被遮蔽、custom-tab-bar 报 `module not defined`——CLAUDE §2「quickstart 填充」坑的 worktree 变体）。解法：从主 checkout 拷 `project.config.json` + `project.private.config.json` 两份本地文件进 worktree 再导入；已生成的散落 `.js` 先 `git ls-files` 核实仓内零跟踪 `.js` 再全删。工具若再弹「补全缺失文件」一律拒绝。

## P（fable）· 2026-07-10 · bug 清除战役 II：9 轮 sweep·13 批 55 项确认修复·Round9 零确认闭环

worktree 分支 `worktree-bugsweep-ii` 13 个 squash（`84dc398`→`bbe643f`），测试 1547→1601、守卫 158→159（`rw-mp-await-side-effect-unloaded-recheck` 点名 13 方法）。每条 finding 对抗验证滤假阳（refuted 11），承重 diff fable 亲读。类目（明细看各 commit message）：
- **钱/库存链**：deleteDraft 双吞恒 ok（「后台删了前台在卖」·顺序反转+失败可见）；casIncrement 回补丢失零信号（GIVEBACK_LOST 留痕）；scmStock rollback 失败删审计迹（保留流水+ROLLBACK_FAIL）；checkout/order 卸载后支付弹窗劫持家族（unloaded 复核收敛成守卫）。
- **竞态家族**：admin 跨目标表单误关/旧回包灌回 17 处（useLatest/快照锚）；agent Desk 五个会话敏感异步动作全收口（draft/act/send/claim+panels PII 残留）；mergeThread 去重键 msgid 优先（同秒两图不再误杀）。
- **静默失败**：sendAgentMessage reject 假成功（真丢话假留痕）；setAgentStatus/getUser/kb·checkpoints GC 吞删/closeEditor 假 saved；Checkpoints 空 nodeId 静默删档（fail-closed 双闸）。
- **契约与文案**：footer.links 塌 [object Object]；listQueue 手写分页收口 pageQuery（+order 参数）；agreement/首页页脚死链接真页；「订单页随下一批开通」等过期承诺清理。
- **防线自身**：取真源断言剥注释（注释掉保护代码必红·E1 翻版）；spyOn try/finally；守卫判据认可选链；isDocMissing 桩文案失真（双源判据）。残余 P3 与潜在债 → `待办与债.md`「战役 II 遗留」节。

## O（fable）· 2026-07-09 · 批6 findings 复审·2 条 P2（文档失真 + Desk 并发逻辑补回归测）

对批6（下条 N）产物做的独立复审，逐条核实评审员出的 2 条 finding，均确认为真。`npm run check` 1544→1547（净增 3 个测试：Desk setStatus 并发收敛 3 场景）全绿。

- **checkout.ts:24-25 内联注释与本批同一次改动矛盾**（P2）：该注释断言「守卫规则保持简单不做『是否在 await 之后』的静态分析」，但同一批 `scripts/check-structure.mjs`（见下条 N 第四项）恰恰新增了这层按 await 位置定位区间的静态分析——注释已过期，容易误导后来者以为本文件对新代码「赋值前任意位置皆可」仍普遍成立。核实：checkout.ts 里 backTimer 赋值本身确实在该方法唯一的 await 之前（文本序无前置 await，守卫按「无 await 同步路径」判、走的是旧口径），所以「本页风险低」这句仍对；错的是「守卫整体不做这层分析」这句已被同批改动证伪的断言。修：改写为如实反映——本页恰好落入无 await 分支、守卫按 Round3 item4 扩面后的新判据整体确已做 await 位置分析。
- **Desk setStatus 的 confirmedGen/inFlight 并发收敛逻辑无自动化测试**（P2）：批6 落地时只有代码注释里的人工推演背书（`rewrite/agent/tests/desk.test.ts` 全文只测 `lib/desk.ts` 纯函数，未碰 setStatus），后续改动可能悄悄破坏某个乱序分支而不被 `npm run check` 拦到。仓库未装 `@vue/test-utils`/jsdom，`rewrite/agent/tsconfig.json`/`vitest.config.mjs` 又不在本批白名单内、不能加装依赖或切测试环境。修（白名单内可行方案）：把 `setStatus` 的并发收敛逻辑抽成与 Vue 响应式无关的纯函数 `createStatusController`，放进 `Desk.vue` 顶部一个非 `<script setup>` 的普通 `<script>` 块（Vue 官方支持二者并存、`<script setup>` 直接可见其顶层绑定，UI 落地经 `apply` 回调交回调用方）；测试用 `@vue/compiler-sfc` 的 `parse()` 把这块源码原样抽出（`Desk.vue?raw`，`vite/client.d.ts` 已声明该类型）、`esbuild.transform` 剥 TS 类型、拼 `data:` URL 动态 `import()`——测的是 `Desk.vue` 里那份真代码，不是另抄一份影子实现；不落临时文件、不碰 `node:fs`/`Buffer`（避免连锁碰白名单外 tsconfig 缺 `"node"` 类型）。新增 3 个用例覆盖批6 注释里写的三场景（fresh 失败+stale 成功乱序 / 双成功乱序 / 单请求）。
- **反向自检**：Desk 条目——临时把 `confirmedGen` 闸去掉（`if (r.ok && my > confirmedGen)` 还原成批5 旧写法 `if (r.ok)`）→ 新增的「双成功乱序」用例真红（`expected 'busy' to be 'offline'`）→ 原样还原→复证绿（`Desk.vue` 与 `/tmp` 备份逐字节 diff 一致）。checkout.ts 条目——读码复核：确认该方法唯一 await 在赋值点之后（文本序），守卫按无 await 分支判定，`npm run check` 结构不变量段仍全绿，无行为改动、纯注释订正。
- **归因**：两条均归根因#11（文档防职责渗漏/客观事实单源——checkout.ts 条属"代码内联注释未随同批改动同步更新"，文档体系规则⑥客观/主观分治的一个变种：注释描述的是"守卫怎么判"这一客观事实，本该跟着改守卫的同一批同步，却漏了）；Desk 条目归根因#8（构建过≠真机能用，此处是"人工推演过≠真被测试钉住"的姊妹病：静态检查/typecheck 都通不过防这类并发时序 bug，只能靠专门测试或真机）。结构性：无新增病根，测试基础设施缺口（无 @vue/test-utils/jsdom）属已知环境限制，non-setup `<script>` + compiler-sfc 抽取属一次性变通、未见第三次重复，不立新约定。

**跟进（2026-07-18·B9）**：上述『仓库未装 @vue/test-utils/jsdom』的环境限制已解除（装 @vue/test-utils+happy-dom，vitest 4.1.8 内置 per-file `@vitest-environment` 覆盖）；admin 三个钱链页已补挂载级渲染测试（`rewrite/admin/tests/{refunds,orders,products}-render.test.ts`）。本条 Desk.vue 的 compiler-sfc 纯函数抽取变通法不受影响、不在本批范围内强制迁移——`setStatus` 并发收敛逻辑仍留在非 setup `<script>` 块，若后续要补 Desk.vue 的挂载级回归可比照本批模式，非本批任务。

## N（fable）· 2026-07-09 · Round3 收敛轮确认项·4 条确认 bug（fable 亲读现场定案·批6 落地）

对批5 修改面复审输出的 4 条 fable 亲读定案项（2 条验证员输出超限被丢弃、2 条被误判 KNOWN，均由 fable 亲读现场重新确认）。`npm run check` 1544→1544（净增测试 0——4 条均落 `.vue`/Page 内联时序豁免或纯守卫判据修正，无新增自动化测试面；后续复审补了 2 条，见上条 O）全绿。

- **admin Fulfill truncNote 抢占 v-else 链整页瘫痪**（P1·`pages/Fulfill.vue:306`）：`<p v-else-if="truncNote">` 占据 `loadErr`/`loading` 的 `v-else` 分支链末位，truncNote 置位（全量扫描触顶）时 Vue 的 `v-else` 绑紧邻前兄弟——`<template v-else>`（三步工作台全部内容）失去绑定目标，整页只剩截断提示、发货流程彻底不可用。修：truncNote 挪到 `loadErr` 之前独立成一行 `<p v-if="truncNote">`，不再占用 `v-else` 链位；其后保持 `loadErr`→`loading`→`v-else`（主内容）完整三段链。截断横幅与工作台从此共存（部分数据仍可处理）。
- **mp checkout refresh() 恢复点不复核 submitting**（P3·`pages/checkout/checkout.ts:46`）：入口已查过 `submitting`，但 `await getAllProducts()` 等待期间若用户提交成功（`finishSubmitted` 清空草稿），await 恢复后仍用空草稿继续 `setData`，支付面板背后瞬时渲染空列表/¥0（不触发 `navigateBack`——空车分支在 await 之前，已无害，纯展示层瞬时错位）。修：`await` 之后紧跟 `if (this.data.submitting || this.unloaded) return` 复核。
- **agent Desk setStatus 锚定在 fresh 失败+stale 成功乱序下 UI 无自愈**（P2·`Desk.vue:90`·批5 修复遗留矛盾源头）：批5「过期成功也无条件刷锚」的写法本身是新矛盾源头——序列「切 busy(R1)→切 offline(R2)；R2 先回包失败回滚到旧锚 online；R1 后回包成功只刷锚 busy 不碰 UI」下，UI 停在 online、服务端已是 busy、锚也已是 busy，三者不一致且无自愈。修（两半）：① 锚按代际取最新成功——新增 `confirmedGen`，成功回包只在 `my>confirmedGen` 时才刷新锚（过期成功不再倒退更新的锚）；② `inFlight` 在途计数，全部请求落定（`inFlight===0`）后无条件把 `status.value` 收敛到 `confirmedStatus`，兜住乱序下未被显式回滚分支覆盖到的错位态。合并推演（三场景：本条乱序/双成功乱序/单请求）写入代码注释；残余边界（无服务端版本号、不保证与服务端处理顺序强一致）同批记档在注释里。
- **守卫 `rw-mp-navback-timer-cleaned` 判据文本位置≠控制流位置**（P3·`scripts/check-structure.mjs`·扩面部分假阴）：原判据只查「方法体内赋值之前存在 `this.unloaded` 检查」，检查写在 `await` **之前**也放绿——但写在 await 之前的检查在 await 恢复执行时早已跑过，拦不住迟到回包（猎手用构造样例证实假阴）。修：取每个 `backTimer=setTimeout` 赋值点前文最后一个 `await`；若存在，则要求 `this.unloaded` 检查落在「该 await 之后、赋值之前」区间内；无 await 的同步路径（如 checkout 空车分支）保持原判据。**先构造两反例验证新判据**（检查在 await 前插一枚临时 fixture 页→红；检查在区间内→绿，验毕删除 fixture）→ 对现网 4 个 backTimer 页（review/profile-edit/feedback/checkout）跑真现状→全绿（批5 落点均正确，无回归）→ **反向自检**：临时把 profile-edit.ts 的检查行挪到 await 之前→守卫真红（"缺 this.unloaded 早退检查"）→原样还原→复证绿。
- **反向自检**：Fulfill/checkout/Desk 三条走读码复核对照 Vue `v-else` 绑定规则/既有 submitting 复核范式/既有代际守卫范式逐分支核对（`.vue`/Page 内联时序类豁免，沿批2–5 先例）；守卫判据修正条走机器守卫本身红灯实测（见上，构造 fixture + profile-edit 临时篡改两组红证）；`npm run check` 全量复证绿。
- **归因**：Fulfill/checkout/守卫判据三条均归根因#8（构建过≠真机能用——模板绑定歧义/并发时序/静态判据盲区，构建期查不出）；Desk 条目是批5 已修 bug 的「修复本身引入新矛盾」再返工，仍归根因#8（并发时序类）。结构性：无新增，`confirmedGen`/`inFlight` 是既有代际守卫范式向「取最新成功+静止收敛」方向的一次延伸，未见满三条新组合、不立新根因。

## M（fable）· 2026-07-09 · L 批复查·5 条残留 bug 修复 + 1 条越界记账（review 猎手对 L 批产物的对抗复核·fable 亲核修复）

对 L 批（下条）产物做的一轮独立复核，逐条核实评审员出的 8 条 finding。**订正 L 批反向自检的过度自信**：L 批「反向自检」小节声称「`.vue`/Page 内联时序类逐条读码复核…逐分支核对一致，未发现遗漏分支」——本轮复核证明这一结论对 Products.vue doSave 不成立（见下），系上一批只核对了「排空一次」这一层，未推演「排空期间又冒出新编辑」的二次分支。`npm run check` 1544→1544（净增断言 0 新增·仅在既有 saveShowcase 用例内补一条 anomalies 落库断言）全绿。

- **admin Products doSave 仍会静默丢弃「排空期间的新编辑」**（P1·`pages/Products.vue:doSave`·L 批 item7 修复不彻底）：L 批把 `saveDraft` 裸调用改成 `await flushSave()` 后即读 `autoState==='saved'` 关闭编辑器，但 `flushSave()` 排空期间若用户又编辑，`watch(edit)` 只会另起一枚**独立**的 900ms 防抖计时器（不经过 `trigger()`，不会让本轮 `serialSave` 的 `dirty=true`）；若网络往返快于 900ms（常见），该计时器仍孤悬未触发时 `flushSave()` 已 resolve，`doSave` 随即把 `edit.value` 置空关闭编辑器，900ms 后孤儿计时器再调 `flushSave()`→`autosave()` 因 `edit.value` 已空静默 no-op——这次编辑从未落库、且无任何报错。用脱离组件的最小复现脚本按 `serialSave.ts` 真实语义验证过（`savedLog` 只含旧值）。修：`doSave` 改 `for(;;)` 循环——每轮排空后检查 `saveTimer` 是否仍孤悬，孤悬则清掉再排一轮，直到没有为止（`run()` 现读 `edit.value` 最新值，天然 latest-wins，收敛于用户停止输入）。读码复核+反向自检（临时改回单次 `await flushSave()`→用最小复现脚本验证确实丢字段→原样恢复）。
- **admin Products openEdit 无代际守卫**（P1·`pages/Products.vue:openEdit`）：L 批 item10 把 `openEdit` 改成 `await silentRefresh()` 超龄先刷新，但没装代际守卫；urlMap 超龄期间连续快点打开两件不同商品，若网络乱序（先点的那次回包更晚落地），会用旧商品数据覆盖用户实际正在看的新编辑器。修：仿 `me.ts`/`order-list.ts` 的 `_seq` 范式加 `openEditGen`，await 后核对仍是最新一次调用再赋值 `edit.value`，否则丢弃；`newProduct`/`closeEditor` 也顺手递增该代际号，防止关闭/新建后一个仍在途的旧 `openEdit` 悄悄把编辑器重新弹开。
- **agent Desk setStatus 过期成功回包被一并丢弃**（P1·`Desk.vue:setStatus`·L 批 item15 修复不彻底）：`if (my!==statusSeq) return` 挡在判断 `r.ok` 之前，无论成功失败一律早退，`confirmedStatus` 只在「非过期」时更新——三连快切+穿插成败序列下 `confirmedStatus` 会锚不到服务端最新真值，之后失败回滚的落点本身就是错的。用具体序列验证：`busy` 请求先成功后到（过期）被早退丢弃→`confirmedStatus` 未刷新仍是 `online`→`offline` 请求随后失败→错误地回滚到 `online`，而服务端真实状态是 `busy`。修：`r.ok` 时无论是否过期都刷新 `confirmedStatus`（过期调用的成功仍是服务端真值），只丢弃过期调用的**失败**回滚（那会用旧锚覆盖更晚一次乐观更新的 UI）。
- **mp checkout refresh() draft/summaryFen 分裂窗口**（P2·`pages/checkout/checkout.ts:refresh`）：L 批 item5 把 `refresh()` 改成 `async` 插入 `await getAllProducts()`，但 `draft`（await 前捕获）与 `summaryFen()`（await 后读取）取自两个不同时刻的 `draftItems`；若窗口期内又触发 `onToggleAddon`，会出现「勾选态未更新但总价已把新搭配购算入」的错位画面（不影响实付金额，`onSubmit` 独立重读 draft）。修：把 `draft` 的读取挪到 `await getAllProducts()` 之后，与 `summaryFen()` 取同一时刻的快照。
- **cloud saveShowcase 部分失败走裸 `alert()` 未真留痕**（P2·`adminApi/actions/products.ts:saveShowcase`·L 批修复不彻底）：L 批把吞错改成计数失败回 `ok:false`，但留痕调用的是 `alert()`（只打一行 `console.error`），不落 `anomalies` 账本、admin `listAnomalies` 查不到、企微群也收不到——不满足「留痕」二字，与本仓 `learning.ts ensureActivation` 注释、`orders.ts shipOne` 既有写法（均用 `notifyAlert`）不一致。修：换 `await notifyAlert(...)`。测试先红后绿：临时改回 `alert()` 复现 `anomalies` 断言真红，原样恢复复证绿。
- **admin Shell.vue 侧栏「按步骤直达」绕开批量上传锁（确认成立·本批未修·记债）**：`Wizard.vue go(n)` 前置拦截了 `isUploadLocked()`，但 `Shell.vue` 侧栏「按步骤直达」用裸 `router-link` 改 `query.step` 直接跳步，完全不经过 `go()`；这与 `go()` 同属「仅 query 变化＝路由 update 非 leave」，`Courses.vue` 的 `onBeforeRouteLeave` 同样不会触发——批量上传在途时点侧栏可直接卸载 Courses、复现 item12 原本要堵的孤儿化。浏览器前进/后退是同类第二条旁路。**未修原因**：正确修法需要动 `Shell.vue`（侧栏点击拦截）或 `router.ts`（全局 `beforeEach` 收口两处一次性解决），两者均不在本批白名单内（本批白名单对 `Wizard.vue` 明确限定「仅 go() 上传锁拦截」），按错题本 E3 纪律不越界，留债由下一批（需扩白名单）处理。已记入 `docs/待办与债.md`。**→ 已收口（同日·router.beforeEach 全局闸）**：`router.ts` 全局 `beforeEach` 会话闸旁加一条 `isUploadLocked()` 判断（`to.path!=='/login' && isUploadLocked() && to.fullPath!==from.fullPath` 即 `return false`+`window.alert` 提示），一次性覆盖侧栏 router-link 与浏览器前进/后退两条旁路；会话分支（会话丢失/已登录访问 /login）排前优先，不受上传锁卡死。`Wizard.go()`/`Courses.onBeforeRouteLeave` 原样保留纵深。
- **反向自检**：cloud saveShowcase 走自动化测试（真红复现，见上）；Products.vue doSave/openEdit 走脱离组件的最小复现脚本按真实 `serialSave.ts`/组件语义建模验证（改回原实现→复现丢失/覆盖→改回修复版→复现消失）；Desk.vue setStatus / checkout.ts refresh() 走读码复核对照具体序列手工推演；`npm run check` 全绿复证。
- **归因**：均归根因#8（构建过≠真机能用·并发时序类，静态测试查不出）；Shell.vue 越界项不改代码、只记债，归错题本 E3（白名单边界纪律）。结构性：无新增，`openEditGen`/doSave 循环排空/`confirmedStatus` 无条件刷新均是既有 latest-wins/代际守卫范式在新位置的复用，未见满三条新组合。

## L（fable）· 2026-07-09 · Round2 确认项落地批·15 条确认 bug + 1 条越界债目勾销（re-audit×4 + concern×8 + 对抗验证·批5 落地）

对 J/K 两批产物做的又一轮 re-audit + concern 镜头扫描后确认的项，覆盖 mp 6 项（含 1 条守卫扩面）+ admin 8 项 + agent 1 项 + cloud 1 项，同批勾销 K 批记的 1 条越界债目。`.vue`/Page 内联时序类沿批2–4 已确立豁免（读码复核+反向自检代替自动化测试）；可测项（cloud saveShowcase）先红后绿；`npm run check` 1543→1544（净增 1 条·saveShowcase golden 测试）全绿。

- **mp me 页缺 `_seq` 代次守卫**（P2·`pages/me/me.ts:16`）：`onShow→refresh()` 四路并发 `Promise.all` 直接 `setData`，全仓同类页早装了 `_seq`（order-list/aftersales/order/detail 等），唯此页漏。修：照 order-list 范式装 `_seq`，过期回包丢弃。
- **mp profile-edit/feedback 无 `unloaded` 守卫**（P2·`profile-edit.ts:16`/`feedback.ts:19`）：保存/提交在途手动返回，迟到回调仍建 `backTimer→navigateBack`，对下一页多余 toast+返回。修：照 `review.ts` 已有 `unloaded` 范式（`onUnload` 置位·回调开头判退）补齐。
- **mp player watchReported onHide 后不复位**（P2·`player.ts:273` 附近）：`onHide` 报过一次 `watch_at` 后返回继续看同段，再 `onHide`/`onUnload` 因 `watchReported` 一次性锁不再报，后续观看进度静默丢失。修：`onShow`（回前台）时复位 `watchReported=false`（回台＝新观看单元，与「切段=新观看单元」同语义）；`reportWatchAt` 仍只在 `state==='playing'` 时真报，非播放态多复位几次也是无害 no-op，不破坏原有「同段 onHide+onUnload 双报」防护。
- **mp checkout 封面绕过时效兜底**（P2·`lib/checkout.ts`+`pages/checkout`）：结算页草稿封面直接取购物车持久化原值，cart 页已修的 `freshCover`（临时址时效兜底·根因#15 兄弟路径）没覆盖到下一屏。修：checkout 页 `refresh()` 取封面走同一 `freshCover` helper（`getAllProducts()` 复用会话缓存，miss 兜底重拉一次）。
- **mp order↔aftersales 互跳环无叠栈守卫**（P3·`order.ts:159`/`aftersales.ts`）：照 `detail.ts` 已有 `getCurrentPages().length>=8→redirectTo` 降级范式，两侧跳转点对等补上。
- **mp 守卫扩面**：`rw-mp-navback-timer-cleaned` 从「onUnload+clearTimeout」扩到统一不变量——凡有 `backTimer=setTimeout` 赋值的页面，① `onUnload` 须置 `this.unloaded=true`；② 每个赋值点所在方法体内、赋值前须有 `this.unloaded` 早退检查。扩面前红灯实测：临时改回 profile-edit.ts 触发两条新校验（onUnload 未置位 + onSave 方法体缺早退检查），核对与预期一致后原样恢复；扩到全仓 4 个 backTimer 页（review/profile-edit/feedback/checkout）后全绿。守卫内新增按 2 空格缩进枚举顶层方法名（`^ {2}(?:async\s+)?name(...)` ）+ `methodBody()` 定位赋值点所属方法体，未改 `guard-strip-single-source` 焊死的两个字面量单源。
- **admin Products doSave 未真入队**（P1·`pages/Products.vue:237`·批3 修复不彻底）：`saveDraft(edit.value)` 裸调用绕开 `serialSave`；网络等待期防抖到点会并发第二条请求乱序覆盖。修：照 Cards.finalize 范式——`doSave` 改 `await flushSave()`（trigger 链现读 `edit.value`，latest-wins），收尾读 `autoState` 判成败再走 reload/message；同时 `watch(edit)` 防抖回调补 `saveTimer=null`（照 Courses.vue:146 写法，原不置 null 使 `if(saveTimer)` 判断失准）。
- **admin Products 四动作失败提示被 reload 覆盖**（P1·`pages/Products.vue:273`）：上架/下架/恢复/删除失败提示被紧随的 `void reload()` 同步清掉（reload 起手同步写 `message.value='加载中…'`）。修：照 Anomalies/Inspect 已有规避范式——失败分支直接 return、不 reload；只有成功才清空 message 再 reload。四动作（doPublish/doUnpublish/doRepublish/doDelete）同款改。
- **admin Agents 停用/绑定提示同款被吞**（P2·`pages/Agents.vue:55`）：同上条范式，`reload()` 内 `await listAgents()` 落地后异步重写 `message.value`，把刚设的操作结果文案覆盖。修同款。
- **admin Products urlMap 长会话临时址过期**（P3·`pages/Products.vue:161`）：`urlMap` 只在写操作后刷新，长会话 `openEdit` 预览可能挂裂图（云存储临时址约 2h）。修最小化：记录 `urlMapAt` 取数时刻，仅在 `openEdit` 入口超龄（阈值 90 分钟）时先 `await silentRefresh()` 再开编辑器；列表渲染路径不挂钩子、不做定时轮询。
- **admin Fulfill 全量扫描触顶静默截断**（P2·`pages/Fulfill.vue:58`）：25 页保险丝（5000 单）触顶不报，同 `getInventory truncated` 惯例本页却漏做。修：触顶时 `truncNote` 黄条显式提示「订单量超出一次装载上限，列表不完整」。
- **admin Courses batchUpload 离页/切步孤儿化**（P1·`pages/Courses.vue:153`·批3 修复不彻底）：Courses 有两种宿主形态——独立路由页 + 被 Wizard.vue `v-else-if="step===4"` 嵌入（Wizard 切步＝仅 query 变化＝路由 update 非 leave，`onBeforeRouteLeave` 不触发但组件照样被卸载——这是主触发路径）。修三点合一：① 新立模块级上传锁 `rewrite/admin/src/lib/uploadLock.ts`（照 `scmHandoff` 单例范式）；Courses `batchUpload` 开始 set、finally clear；② Courses 内 `onBeforeRouteLeave`：`isUploadLocked()` 时阻止并提示（覆盖独立页离开与整向导页离开）；③ Wizard.vue `go(n)` 前置拦截：`isUploadLocked()` 时提示「切步将丢失未落库的视频关联」并 return（覆盖主触发路径）。不选「后台继续+补落库」（组件卸载后 course 对象失联，补救复杂度不成比例）。
- **admin Customer360 search 与 open 状态互踩**（P2·`pages/Customer360.vue:26`）：`search()` 起始不清 `hits`（旧命中列表在途可点）、结尾 `message` 赋值无 `current` 判据（迟到搜索回包覆盖已打开客户的状态文案）。修：`search()` 起始同批清 `hits`；`message` 赋值只在 `!current.value` 时执行（AND 语义——`isStale` 早退之后「本代际仍最新」在此恒真，若纳入 OR 判定等于恒真、起不到防护作用）。
- **admin Orders/Refunds 角标不消费 `counts.partial`**（P2·已记债项·`pages/Orders.vue:67`/`Refunds.vue:76`）：partial 时按状态角标把占位 0 当真值显示。修：两页读 counts 时消费 `partial`——`partial:true`（或计数请求本身失败）时角标区显示警示态 `?`（红底·title 提示），不再显示假 0。同批勾销 `docs/待办与债.md`「批4 派生」节该债目（勾销前现场 grep 复核两文件均已接线，错题本 E7）。
- **agent Desk setStatus 回滚锚错**（P2·`rewrite/agent/src/Desk.vue:86`·批4 修复不彻底）：`prev` 取当前 UI 乐观值，连续两次都失败时会回滚到从未被服务端确认过的中间态。修：维护 `confirmedStatus`（初始＝`status` 初值；每次 `r.ok` 时更新为该次目标值），失败回滚 `status.value=confirmedStatus` 而非 `prev`；`statusSeq` 过期判退逻辑保留不动。
- **cloud saveShowcase 部分失败仍回 ok:true**（P2·`adminApi/actions/products.ts:196`）：逐条 `update().catch(()=>{})` 吞错、不计成败，前端永远「已保存」。修：计数失败条数，任一失败回 `{ok:false, error:'PARTIAL_WRITE:<n>'}` + `alert('anomaly',...)` 留痕（kit `observe.alert` 单出口·根因#14）；消费方 admin Showcase.vue 已按 `r.ok` 通用判成败，无需改动。测试先红后绿（临时改回旧 `.catch(()=>{})`→新测试真红「expected true to be false」→原样还原、复证绿）。
- **反向自检**：cloud saveShowcase 走自动化测试（见上，真红复现）；守卫扩面走机器守卫本身红灯实测（见上）；其余 `.vue`/Page 内联时序类逐条读码复核——对照同文件/同页既有同构范式（`review.ts` unloaded 范式 / `Cards.finalize` serialSave 范式 / `detail.ts` redirectTo 降级范式 / `Anomalies.vue` 失败不 reload 范式 / `scmHandoff` 单例范式 / `useLatest` latest-wins 范式）逐分支核对一致，未发现遗漏分支；`npm run check` 全绿复证。
- **归因**：Products doSave/urlMap/四动作提示、Agents 提示、Courses 孤儿化、Customer360 状态互踩均归根因#8（构建过≠真机能用·并发时序/生命周期类，静态测试查不出）；saveShowcase 归根因#14（失败被吞/不可观测）；me/player/checkout/order↔aftersales/Fulfill 截断/counts.partial 均为「同类已知病根在新页面/新路径的再现」，未见满三条新组合、不立新根因。结构性：`backTimer` 守卫扩面（同类满三条收敛）本身即结构性收口；`uploadLock` 单例范式可供后续「组件嵌入宿主+独立路由双形态」场景复用。

## K（fable）· 2026-07-09 · J 批复查·2 条残留回归 + 1 条越界记账（review 猎手对 J 批产物的对抗复核·fable 亲核修复）

对 J 批（下条）产物做的一轮独立复核，发现 J 批「身份头解耦」修复（item3）本身在 `search()` 留了残留回归窗口（未覆盖到的状态清空缺口，非新病根，仍归 J 批同款回归），另确认 1 条 J 批已知未闭环的账（partial 消费面不全，受本批白名单边界约束，非本轮新增）：

- **Customer360 search() 未清 user 导致跨客户身份头残留**（P1·`pages/Customer360.vue:26`）：J 批 item3 把身份头 Card 移出 `v-if="panels.length"`（改依附独立的 `v-if="user"`），本意是「面板取数失败时身份头仍可见」；但 `search()` 从始至终只重置 `message`/`panels`/`current`，从未碰 `user.value`——旧版本身份头因裹在 `panels.length` 内、`search()` 清 panels 时被连坐隐藏，掩盖了 `user` 未重置这处既有小疏漏；本批解耦后掩盖失效，变成真实可复现回归：搜 A→点开 A（`user` 填充 Alice 数据）→不点「返回结果」、直接改词搜 B→B 的命中列表上方仍挂着 Alice 的姓名/头像/手机号，与新搜索结果客户身份错配。该页本身以 FORCE_AUDIT 强调「A 的私域全景别落到标 B 的头下」，这条回归恰落在同一类风险上。修：`search()` 起始处补 `user.value = null`，与 `panels`/`current` 同批清空，保持「开始新一轮搜索即清空上一客户全部残留状态」的不变量；与 `open()` 起始处、「返回结果」按钮里既有的清空逻辑对齐。
- **Desk setStatus 回滚未做代际守卫的 ABA 竞态**（P2·`rewrite/agent/src/Desk.vue:86`）：J 批 item9 给 `setStatus` 加了失败回滚（`status.value=prev`），但未引入「是否为最新请求」判断——同批 item8 治 `pollThread`/`open` 跨会话串号用的是 sid 快照代际治法，`setStatus` 的回滚逻辑未同款处理，属同一批次内治法不一致导致的新竞态缺口：快速连续切状态（如 busy→offline）时，先发的 R1（busy）若晚于后发的 R2（offline，已成功）才返回且失败，会用 R1 捕获的旧 `prev`（online）覆盖已被 R2 确认的正确状态 offline，UI 与服务端真实状态错位。修：加 `statusSeq` 自增代际号，`setStatus` 发起时取号，回包后只在「本次代际仍是最新」时才允许回滚，否则丢弃过期失败回滚。
- **看板待办 partial 消费面不全（越界记账，未修代码）**（P2·`pages/Orders.vue:65`/`Refunds.vue:76`）：J 批 item7 给 `orderCounts`/`refundCounts` 加的 `partial` 目前只接通了 Dashboard.vue 的待办卡消费；`Orders.vue`/`Refunds.vue` 的按状态 tab 计数徽标读的是同一个 `counts` 返回值，却完全不看 `partial`，某状态 `count()` 失败时这两页的角标仍会把占位 0 当真值显示——与「看板待办计数假绿」是同一失败模式换了消费页面。这两个文件不在批4 白名单内（白名单只含 Customer360/Conversations/ScmMaterials/Dashboard 四页），本轮不越界修改，改记入 `docs/待办与债.md`「cloud bug sweep Round1 批4 派生」节，留待下一批带上这两个文件时一并接上。
- **反向自检**：Customer360 临时改回旧版（去掉 `search()` 里的 `user.value = null`）→ 复现搜 A 开详情→改词搜 B→身份头仍显 Alice（红）→原样还原、复读代码确认已加回；Desk.vue 临时把 `if (my !== statusSeq) return` 删掉→读码复核 ABA 时序（R1 慢于 R2 场景下会错误回滚，逻辑上必现）→原样还原。`npm run check` 全绿复证（测试数不变，两处均为 `.vue` 内联时序修复，本仓无组件挂载测试基础设施，按同批2/批3/J 批既有先例走「读码复核 + 反向自检」豁免自动化测试，不为凑测试硬抽抽象）。
- **归因**：两条均归根因#8（构建过≠真机能用·并发时序类，静态测试查不出）；第三条不新增归因，是「已知未闭环账目」的越界记账处理，同 J 批「反向自检」纪律里对 admin2 loop 的既有处理方式。

## J（fable）· 2026-07-09 · admin/agent/cloud bug sweep Round1 批4·11 条确认 bug（两维 sweep+对抗验证·批4 落地）

全仓 bug 清除两维 sweep Round1 确认项落地批，覆盖 admin cms/cs/scm/看板 + agent 坐席台 + cloud kb/checkpoints/orderCounts/refundCounts。纯函数/cloud action 类先红后绿补 golden 测试；`.vue` 内联异步时序类（items 3/4/5/6/8/9）本仓无组件挂载测试基础设施，按同批2/批3 先例走「对照既有同构范式逐行核对 + 反向自检读码复核分支覆盖」豁免自动化测试。`npm run check`（typecheck/lint/test）全绿，测试 1534→1543（净增 9 条：mapContent 2 + mapMoney 1 + kb 1 + checkpoints 1 + counts-partial 4；另 1 处既有 fixture 订正不增计数·初记「1531→/新增12」为笔误，fable 收尾订正）。

- **①②mapContent 放心区/买家秀条目非空判据漏 img/user**（P2·`lib/mapContent.ts:99,103`）：`homePayload` 的空块/空评价剔除判据原只看 title/body（放心区）与 quote（买家秀），「先传图后补文案」的中间态条目被当空块静默丢——运营传完图切走、回来发现图没了。修：判据各加 `img.trim()`（放心区）/`img.trim()||user.trim()`（买家秀）任一非空即入库。golden 测试 2 条（先红）；顺带订正既有往返测试一条 fixture（原 `user:'匿名'` 在新判据下不再算空评价，改为三字段皆空白以保留其「空评价剔除」验证意图）。
- **③Customer360 身份头被 panels 连坐隐藏**（P2·`pages/Customer360.vue:117`）：身份头（`getUser` 独立成功的数据）原裹在 `v-if="panels.length"` 里，360 面板取数失败（`panels=[]`）时连身份头一起消失，只剩报错文案、看不到基本身份信息。修：身份头 Card 移出该 `v-if`，与面板区解耦、各自独立渲染。
- **④Customer360 search() 无乱序守卫**（P2·`pages/Customer360.vue:25`）：连续搜索时旧请求后到会覆盖新搜索结果（同页 `open()` 早有 `panoGen` 治过同类问题，`search()` 漏配）。修：新增 `searchGen = useLatest()` 同款治法。
- **⑤Conversations more() 成功不清旧错**（P2·`pages/Conversations.vue:55`）：翻页失败后再次成功翻页，旧的「加载更多失败」提示不会被清掉，长期挂着误导。修：成功路径 `message.value = ''`。
- **⑥ScmMaterials loadLedger 无 latest-wins**（P2·`pages/ScmMaterials.vue:26`）：快速切料号连查流水，旧回包可能后到覆盖新查询结果（列表显示的流水与当前选中料号不符）。修：`useLatest()` 同款治法。
- **⑦看板待办计数假绿**（P1·`lib/mapMoney.ts:52` + cloud `orders.ts`/`refunds.ts`）：`orderCounts`/`refundCounts` 内部 `.count().catch(()=>0)` 把「某状态查询失败」静默兜成「真为 0」，混进精确计数、外层整包仍 `ok:true`，前端旧 `deriveDashboardTodos` 只看四路 `.ok` 判 partial、测不出这层内部失败——运营可能因此漏看真实待发货/待审退款。修两侧：cloud 侧改整体 `partial` 标记（任一状态失败即整包带 `partial:true`，不逐路置 null，保持形状简单——数字失真时前端已按「整体不可信」统一处理，无需精确到哪个状态失败，此为选定方案·已写入代码注释）；admin 侧 `TodoInputs`/`deriveDashboardTodos` 消费该 partial（OR 进整体 partial）。Dashboard.vue 既有 `todoPartial` UI 分支（病根#14 既有治法）已原生兼容，无需改动。golden 测试双侧各 2 条（先红）。
- **⑧Desk 切会话跨会话串号**（P1·`rewrite/agent/src/Desk.vue:31`）：`pollThread()`/`open()` 里的在途轮询/360 请求不校验 sessionId，切会话后旧会话的回包落地到新会话——消息串号/游标损坏/360 错配。修：`pollThread()` 请求前快照 `sid=currentId.value`，回包后比对 `sid!==currentId.value` 即丢弃；`open()` 的 360 回包同款比对 `sessionId!==currentId.value`。
- **⑨Desk setStatus 失败静默错位**（P2·`rewrite/agent/src/Desk.vue:83`）：`status.value=s` 乐观更新后不检查回包，服务端拒绝时前端仍显示切换成功的假状态、无提示。修：失败时回滚 `status.value` 到旧值 + `message.value = deskErrorText(r.error)`（照本文件既有错误提示范式）。
- **⑩kb saveKb order=0 被 falsy 吞**（P3·`kb.ts:47`）：`Number(e.order)||i` 把合法的 `order:0`（首位）当 falsy 换成循环序号 i，非首位的 0 号条目排序错位。修：`Number.isFinite(Number(e.order)) ? Number(e.order) : i`。golden 测试 1 条（order:0 放第 2 条精确逮住 falsy 吞坑）。
- **⑪checkpoints 定义 _id 冒号拼接可撞键**（P3·`checkpoints.ts:38`）：`'def:'+courseId+':'+nodeId` 若 courseId/nodeId 本身含 `:`，段界错乱、跨课/跨节点撞出同一 `_id` 互相覆盖。修：`saveCheckpoints` 入参校验——courseId 或任一 nodeId 含 `:` 一律拒（`error:'BAD_ARGS:COLON_IN_ID'`，沿用仓内既有「冒号后缀带因」错误码形状），且校验全部通过才开始落库（fail-closed，不留半份写入）。不改既有 `_id` 编码（存量数据零迁移）。golden 测试 1 条。
- **反向自检**：①②⑦⑩⑪各自临时改回旧实现跑对应新测试→真红（分别 length 0≠1/2、partial false≠true×4、order 1≠0、error undefined≠'BAD_ARGS:COLON_IN_ID'×2）→原样还原、`npm run check` 全绿复证；③④⑤⑥⑧⑨（`.vue` 内联时序类·本仓无组件挂载测试基础设施）改走读码复核：逐行核对新增守卫逻辑与同文件/同页既有同构范式（`panoGen`/`useLatest`/`deskErrorText` 既有调用点）分支覆盖一致，未发现遗漏分支。
- **归因**：⑦⑧同根因#14（失败被吞/静默错位，动作类失败需可观测/可纠正）；③④⑥同根因#8（构建过≠真机能用·并发时序类，静态测试查不出）；①②⑩⑪同类「非空/序号判据用了会误伤合法边界值（空字符串+图片、falsy 0）的简化写法」，未见满三条故未立新根因，仍归各自最贴近的既有根因。结构性：⑦是（cloud 计数 partial 化是可复用范式，两个 action 同款修·未来新增计数 action 应默认带此模式，已在代码注释固化为「选定方案」供后续抄用）；其余为局部点修。

## I（fable）· 2026-07-09 · H 批复查·4 条残留时序 bug（review 猎手对 H 批产物的对抗复核·fable 亲核修复）

对 H 批（下条）产物做的一轮独立复核，发现 H 批「选定方案」本身留了残留窗口（均为原修复引入/未覆盖到的并发时序缺口，非新病根，仍归 H 批同款根因#8），另有 1 条 H 批已知未闭环的账在本轮复核中再次确认仍未解除（受同一白名单边界约束，非本轮新增）：

- **①Cards.vue finalize 仍可被旧 draft 盖回**（P1·H 批 ③选定的 `.settled()`-only 方案未堵死）：`.settled()` 只排空「点击那一刻已在途」的链，finalize 自身的写却是旁路直发 `saveCard`——settled() 排空期间若又触发一次新 autosave（防抖到点），其 `run()` 现读 `card.value.status` 仍是本地翻转前的旧值，可能与 finalize 的写并发在途、谁的响应后到谁落库。改走「并入同一条 serialSave 队列」方案（H 批 ③本身列出的备选）：新增闭包变量 `pendingStatus`，finalize 时置位后 `await flushSave()`（非 `.settled()`）——`trigger()` 对已在途的链只标脏不并发，链上「当前轮+尾随补跑轮」全携带 `pendingStatus`，天然堵死旁路乱序；收尾读 `autoState.value` 判定链上最后一轮真实成败。
- **②Courses.vue batchUpload 上传中可被切课**（P1·H 批 ⑦锁了重排/删除交互但漏锁切课入口）：`batchUploading` 只挡住了 move/delChapter/delLesson/delSegment 四类按钮，`load()`（真正把 `course.value` 整体换掉）与 courseId 输入框/载入按钮完全未锁——切课会让在途上传循环持有的课时对象裸引用断链，仍在跑的上传把 `videoFileId` 写回一棵不可达的树，永久丢失且无报错（云存储留孤儿文件）。修：`load()` 顶部拦截 `batchUploading` 为真时提示并 return；courseId 输入框与「载入」按钮补 `:disabled="batchUploading"`（同已有的重排/删除锁一致）。
- **③Products.vue doSave 关闭编辑器丢失在途期新编辑**（P2·H 批 ④接入 `.settled()` 只解决了「已在途 autosave 晚于 doSave 落地」的乱序）：doSave 自身 `saveDraft()` 网络等待期间若又有新编辑（触发新的 900ms 防抖），成功后直接 `edit.value = null` 会让这枚待发计时器到点时因 `autosave()` 内 `if (!edit.value) return` 静默丢弃这次编辑、无任何提示。修：照 `closeEditor()` 已有模式——成功后若仍有排着的 `saveTimer`，清掉它、抓一份快照单独补存再关闭编辑器。
- **④scripts/check-structure.mjs `setupFnBody` 花括号计数不排除字符串内容**（P2·潜在假阴性，未在现存 12 页触发）：花括号配对计数直接扫原始字符，含奇数个字面量花括号字符的字符串（如值为「incomplete: {」的字符串）会打乱配平、把函数体边界错算到下一个函数头上——独立复现证实：构造两个相邻函数、前者字符串含奇数花括号，`setupFnBody` 会把后者整个函数体（含其真实复位赋值）错吞进前者的返回值，导致 `rw-admin-armed-reset-on-load` 误判。修：新增 `maskStringLiterals()`（单/双引号与模板字面量内容逐字符替换成占位符、保持等长，供计数用；`setupFnBody` 返回值仍切片自原始 src）；刻意放在 `setupFnBody` 定义**之后**（而非之前）——插在 `methodBody`/`setupFnBody` 之间会把 `guard-strip-single-source` 的 span 终点提前截断，重演错题本 E8。
- **⚠️ 未闭环再确认（白名单冲突·非本轮新增·记账不擅自处理）**：H 批遗留的 `repoChecks.length` 157→158 与 `docs/自动化验证系统.md:37` 手抄数不同步，本轮复核仍在，`docs/自动化验证系统.md` 仍不在本轮白名单（白名单仅含本文件/`docs/待办与债.md`+ admin 页面/lib/tests/check-structure.mjs），未擅自越界改。`check:structure` 因此仍报同一处红（本轮独立复跑核实：结构不变量 1 处未通过，即此条；无其余新增红）；`typecheck`（1 处）/`lint`（0 error）/`test`（1534 测试）分别独立验证全绿。**→ 已解除**：fable 主循环批3 收尾同步数字（见案H 尾注）。
- **反向自检**：①②③临时回退各自修复一行核心逻辑（Cards 的 `pendingStatus`/Courses 的 `load()` 拦截/Products 的补存分支）后跑既有测试套件（cards.test.ts/videobatch.test.ts/products-ui.test.ts）不会变红——三条均是既有测试基础设施覆盖不到的 Vue 组件内并发时序 bug（同 H 批已知限制，人工审出，非可测归纳）；④对 `setupFnBody` 独立跑构造用例：还原掩蔽前的旧实现 → 相邻函数体互相吞并复现（红）；改回掩蔽版 → 边界正确隔离（绿）；已还原到掩蔽版（最终态）。
- **归因**：根因#8（构建过≠真能用）。①②③与 H 批同根同款——「选定方案/新锁只挡住了描述场景本身，未覆盖『修复过程期间又发生一次同类触发』这一圈」，是复核而非新病根；④是静态字符串处理的经典边界（不掩蔽字面量），已并入 `setupFnBody` 单源实现内部，不构成新守卫（helper 内部健壮性修复，不是新的独立不变量）。

## H（fable）· 2026-07-09 · admin 后台 bug sweep Round1 批3·8 条确认 bug + 危险 armed 态复位守卫（两维 sweep+对抗验证·批3 落地）

全仓 bug 清除两维 sweep Round1 猎手发现、确认后的 admin 承重批（钱面/商品/课程时序类，均历史上曾出 P1 同款：迭代I 曾因 armed 态残留误删/误发）。`rewrite/admin/src/lib/serialSave.ts` 新增 `.settled()`（只加不改既有语义·golden 测试覆盖）；结构守卫新增 `rw-admin-armed-reset-on-load`（危险 armed 态随刷新必复位·配新 helper `setupFnBody` 花括号配对计数截取 `<script setup>` 顶层函数体，不复用 `methodBody`——其逗号收尾启发式对裸函数实测错抓）。`npm run typecheck`/`lint`/`test`（1534 测试·初记 1539 为笔误，复核订正）全绿；`check:structure` 白名单冲突已解除（见文末）。

- **①Orders.vue 金额核对两步确认 armed 残留**（P1）：`reload()` 顶部复位 `clearConfirmId`——切标签/搜索/翻页/发货成功自动刷新后不再让旧武装的「去核对」按钮残留、重进同一行一击直发。
- **②Refunds.vue 决策抽屉在途关闭竞态**（P2）：`doApprove`/`doReject` 发起时捕获目标单号 `targetId`，回包后仅在 `decideRow.value?.id === targetId`（`stillHere`）时才 `closeDecide()`/挂 `decideErr`；`actionMsg`/`reload()` 与抽屉是否仍是该单无关、恒执行（真退款结果不因抽屉已切走而被吞）。
- **③Cards.vue finalize 与在途 autosave 竞态**（P1）：选定方案＝`finalize()` 前 `await flushSave.settled()`——`clearTimeout` 只挡得住未触发的防抖定时器，挡不住已发出、正在等回包的旧快照 autosave；`.settled()` 只等在途链、idle 时不像 `flushSave()` 那样强制多发一次 POST。
- **④Products.vue doSave 绕开 serialSave 排空**（P2）：`doSave()` 同款接入 `flushSave.settled()`，排空在途 autosave 后再发本次保存，防旧快照回包乱序覆盖新编辑。
- **⑤Products.vue editCourse 旧快照冲掉新 courseId**（P3）：`editCourse` 写库前，若编辑器正开着同一件商品（`edit.value.id === row.id`），先把 `courseId` 同步进编辑器快照——防 `closeEditor`/`onBeforeUnmount` 补存时用编辑器里的旧值（空）整档覆盖，抹掉刚写的 courseId（后写不抹先写）。
- **⑥Courses.vue 切课危险态残留**（P1×2）：`load()` 顶部复位 `confirmKey`（删章/删课时/删段确认）与 `publishConfirm`（发布确认）。
- **⑦Courses.vue batchUpload 上传中重排/删段错配**（P1）：plan 时深捕获目标段对象引用（JS 对象身份不随数组重排改变），循环内按引用重定位（`l.segments.includes(sg)`），引用已不在数组（被删）即跳过并如实报告；新增 `batchUploading` 锁在途期间的重排/删章/删课时/删段交互入口（两层都做：数据面按稳定引用兜底、交互面锁入口）。
- **⑧Checkpoints.vue 切课 confirmKey 残留**（P1）：`load()` 顶部复位 `confirmKey`——此页「保存整课」全量覆盖，残留误删会被永久上云，风险高于普通列表页。
- **⑨同类扩面**（P1·13 个 armed ref / 12 页，fable 权威 grep 清点）：Products/HelpVideos/Kb（含 `loadPreset`）/ScmOutwork/ScmPurchase/Anomalies/Agents/ScmBom/Wizard（`reloadProduct`）各自的顶层 `load*`/`reload*` 函数补齐对应 armed ref（`confirmKey`/`confirmId`/`runConfirm`/`pubConfirm`）复位——不动迭代A–I 已修的载入失败保护/autosave 载入闸逻辑，只加复位行。
- **守卫**：`rw-admin-armed-reset-on-load`（roots #8）——扫 `rewrite/admin/src/pages/*.vue`，文件内每个顶层 `const <名>=ref(...)`（名匹配 `/confirm/i`）的 armed ref，须被文件内每个顶层 `load*`/`reload*` 函数体重置赋值 `<名>.value=`（`===`/`!==` 比较不算复位）。红灯实测：修复前咬出 14 处违例（12 文件·Courses/Kb 各 2）；配套 `setupFnBody` helper（花括号配对计数，供 `<script setup>` 裸函数取体，与既有 `methodBody` 并列、零改动其本体与 6 处既有调用点）。
- **反向自检（① `guard-strip-single-source` 风格三式，实测于 Orders.vue reload）**：ⓐ删掉复位赋值 → 红（未复位 armed ref「clearConfirmId」）；ⓑ复位赋值写进注释 → 仍红（`stripComments()` 生效）；ⓒ复位赋值改成比较 `clearConfirmId.value === ''` → 仍红（赋值/比较区分生效）；三式后原样还原、复跑绿。`.settled()` 新增两条 golden 测试（idle 不多发一次 / 有在途才等排空）随 `serialSave.test.ts` 跑绿。
- **⚠️ 未闭环（白名单冲突·记账不擅自处理）**：新增 1 条 repoCheck 使 `repoChecks.length` 157→158，触发既有元守卫 `guard-count-synced`（`docs/自动化验证系统.md` 硬编码「当前 157 条仓级 repoCheck」）——该文档**不在本批白名单**（白名单仅含 `docs/调试日志.md`/`docs/待办与债.md`），本批未改动它。`check:structure` 因此报 1 处红（仅此一处，已核实非本批其余改动引入）；`typecheck`/`lint`/`test` 分别独立验证全绿。需下一批（或授权扩白名单）同步该数字，或改造 `guard-count-synced` 使计数免手改。**→ 已解除（2026-07-09 同日）**：fable 主循环批3 收尾时直接同步 `自动化验证系统.md` 157→158（主循环不受批次白名单约束·执行者不越界的纪律判断正确）；「计数改由 `repoChecks.length` 派生免手改」留 `待办与债.md` 作根治项。
- **归因**：根因#8（构建过≠真能用——armed 态残留/在途请求乱序均是异步真实使用才现的时序缺口，静态测试不落它）。结构性：是——「危险 armed 态随刷新未复位」在 12 个页面反复复现，已从「靠人记」升级为机器守卫（元模式：同类满三条收敛）。

## G（fable）· 2026-07-09 · F 批复查·2 条残留 bug（review 猎手对 F 批产物的对抗复核·fable 亲核修复）

对 F 批（下条）产物做的一轮独立复核，发现 F 批修复本身留了两处残留窗口（均为原修复引入/未覆盖到的并发时序缺口，非新病根，仍归 F 批同款根因#8）：

- **player 换段未清零 `_at/_dur`**（P2·`pages/player/player.ts` `playSegment`）：F 批 ⑭修复给 watch_at/segment_done 埋点补了 `this._at/this._dur`，但换段（`playSegment`）只重置了 `curSec/durSec` 等展示字段，未清零这两个新增实例字段。换段起点（url 落定、`state` 置 `'playing'`）早于新段首个 `timeupdate` 事件，此窗口内若触发 `onHide`/`onUnload`，`reportWatchAt` 会把**上一段**的 `_at/_dur` 当**新段**的播放位置上报——F 批本想修的「学习位置不准」在切段场景下被自己重新引入。修：`playSegment` 换段起点一并 `this._at = 0; this._dur = 0`。
- **detail 重试无代次守卫**（P2·`pages/detail/detail.ts` `loadProduct`）：F 批 ③给 detail 加了 `loadFailed` 态+重试按钮，但 `loadProduct`/`onRetryLoad` 未照 F 批同批 ④⑮给 home/aftersales 加的 `_seq` 代次守卫。加载失败后快速连点「重试」（按钮无 busy/disable 态）可并发在途，晚发的请求若先落地、早发的慢请求后落地，会用旧结果覆盖新结果（例如已重试成功却被姗姗来迟的失败响应打回 `loadFailed`）。修：照 home.ts/aftersales.ts 同款 `_seq` 范式补上。
- **反向自检**：临时回退两处修复（去掉 `_at=0/_dur=0` 与 `_seq` 守卫两行）→ `npm run check`（typecheck + 1531 测试）仍全绿——证实这两条是本仓 Page 级测试基础设施覆盖不到的时序 bug（同 F 批已知限制），只能人工审出；已还原、复测全绿。
- **归因**：根因#8（构建过≠真机能用，回包乱序/换段窗口工具端不暴露）延续 F 批同款根因，未命中新病根。

## F（fable）· 2026-07-09 · mp 页面 bug sweep Round1 批2·15 条确认 bug（两维 sweep+对抗验证·猎手发现／fable 亲核）

全仓 bug 清除两维 sweep Round1 猎手发现、fable 亲读代码确认后的 mp 承重批（P2×7、P3×8·对应 E 的 mp 半边）。金额/失败态/并发时序类抽出纯函数（`lib/checkout.ts`）先红后绿 + 反向自检（临时改坏→测试真红→已还原绿）；其余为 Page 级生命周期/时序修法（glass-easel 无 Page 单测基础设施，同 D 批 iter1 范式：typecheck+人工审+待真机走查，未新增 Page 级测试）。`npm run check` 1531 测试全绿（checkout.test.ts 新增 3 组 6 用例）。

- **①checkout 成功页金额用前端自算值**（P2·`pages/checkout/checkout.ts`）：createOrder 回包权威 `order.amount`（元）未被使用，成功页透传的是本地 `summaryFen()` 估算值，缺货重试/搭配购变化等场景与云端现算分叉。修：新增纯函数 `checkout.resolveOrderAmountFen(order, fallbackFen)`（回包缺失/非法数字才回退前端估算值），三处 `?amount=` 随之取该值。
- **②COUPON_EXCEEDS_GOODS 吞成通用失败**（P3·同上）：`msg.startsWith('OUT_OF_STOCK')` 外全部错误码归一显示「下单没成功，稍后再试」，用户以为网络问题反复重试。修：新增纯函数 `checkout.mapCreateOrderError(msg)` 加一档专属文案「优惠券暂不支持抵扣这单，请调整商品数量」。
- **③detail 加载失败伪装「已下架」**（P2·`pages/detail/detail.ts`）：`getProductById` 内部 `getAllProducts()` 失败与 id 未命中同归 null，页面统统显「商品不存在或已下架」，网络抖动被误导成真下架。修：改走 `getAllProducts()` 直接判两态——`all===null` 落 `loadFailed`（新增「网络开小差」态+重试按钮 `onRetryLoad`）；`all` 有值但未命中维持 `missing`。
- **④home reload 无代次守卫**（P2·`pages/home/home.ts`）：下拉刷新与首屏 onLoad 并发时慢回包可覆盖快回包的新结果。修：照 order-list `_seq` 范式加代次，丢弃过期回包。
- **⑤me 三格不带状态跳转**（P2·`pages/me/me.ts` + `me.wxml`）：待付款/待发货/待收货三格与「全部订单」行为相同，进 order-list 后还要手动切 tab。修：三格加 `data-tab`，`onOrders` 读 dataset 拼 `?tab=` 查询串（order-list.ts 原有 query.tab 契约生效）。
- **⑥order 写评价跳转 lineId 未编码**（P3·`pages/order/order.ts`）：`name` 经 `encodeURIComponent` 而 `lineId` 未编码，若含 URL 保留字符会截断查询串。修：`encodeURIComponent(lineid)`，`review.ts` onLoad 对应加 `decodeURIComponent`（与 name 同一往返约定，编码/解码成对）。
- **⑦player error 态无换段控件**（P3·`pages/player/player.wxml`+`.ts`）：媒体不可恢复错误落 `state:'error'` 后只有文案、无可操作控件，用户卡死。修：wxml 加「重试本段」/「选其他段」两按钮；ts 新增 `onRetryError()` 复用现有段加载方法重置重试计数。
- **⑧profile-edit 保存中重选头像被静默丢**（P2·`pages/profile-edit/profile-edit.ts`）：`onChooseAvatar` 无 busy 闸，保存在途重选头像后 `onSave` 已捕获的旧 `avatarTemp` 与新选分叉，实际上传的是旧图。修：`onChooseAvatar` busy 期间直接 return（与保存按钮 busy 语义对齐）。
- **⑨review 提交在途退页后迟到副作用**（P2·`pages/review/review.ts`）：`submitReview` 回包落地前用户已手动返回，迟到成功仍弹 toast + 二次 `navigateBack`（打到错误的上一页）。修：`onUnload` 置 `unloaded` 标记，回包处理开头 `if (this.unloaded) return`（提交本身已达服务端，无需补偿）。
- **⑩reviews 触底翻页重复 append**（P2·`pages/reviews/reviews.ts`）：无在途标记，快速多次触底并发请求各自 append 造成重复行。修：加 `loadingMore` 在途标记，请求发起即置位、落地释放。
- **⑪cart 首建实例 onShow 抢跑**（P3·`pages/cart/cart.ts`）：微信 onLoad→onShow 相邻触发，`onLoad` 的 `await getAllProducts()` 未落地时 `onShow` 已用空 `allRaw` 跑一次 `refresh()`，与 `onLoad` 完成后的 `refresh()` 互相踩踏。修：`ready` 标记，`onLoad` 首拉完成后置 true，`onShow` 里 `!ready` 直接 return。
- **⑫address-edit 新增态标题误显「编辑」**（P3·`pages/address-edit/address-edit.ts`）：`address-edit.json` 固定 `navigationBarTitleText:"编辑收货地址"`，新增地址（`id===null`）也顶着这标题。修：`id==null` 时 `wx.setNavigationBarTitle({title:'新增收货地址'})`。
- **⑬首页加购不刷新 TabBar 角标**（P3·`pages/home/home.ts`）：`onAddProduct` 的 `add` 分支直接加购，未同步刷新自绘 TabBar 购物车角标（需切 tab 才看到新数字）。修：照 `cart.ts:24` 范式补 `getTabBar().setActive('home')`（内部含 `cartCount()` 重算）。
- **⑭player watch_at/segment_done 不带 at/dur**（P2·`pages/player/player.ts`）：云端 `trackEvent` 已支持折叠 `last.at/last.dur`（`learning.ts:272-273`）但 mp 端从不传，360 客户视图「学习位置」恒 0 秒。修：`onTimeUpdate` 每次事件（不受 250ms 节流影响、不 setData）更新实例字段 `this._at/this._dur`；`onEnded`（segment_done）与 `reportWatchAt`（watch_at）的 trackEvent meta 补 `at/dur`。
- **⑮aftersales 缺回包乱序护栏**（P2·`pages/aftersales/aftersales.ts`）：`reload()`/`onReachBottom()` 无代次守卫，同 order-list 病灶（多触发点并发慢回包覆盖新结果/错配游标）。修：照 order-list `_seq` 范式补齐。
- **反向自检**：①②抽出的纯函数临时改坏（`resolveOrderAmountFen` 恒回退 fallback／`mapCreateOrderError` 去掉 COUPON_EXCEEDS_GOODS 专属分支）→ `checkout.test.ts` 对应用例真红（`expected 999 to be 19800`；文案不匹配）→ 已还原、复测全绿。③–⑮为 Page 级时序/wiring 修法，无 Page 级单测基础设施（glass-easel 环境限制，同错题本既往批次结论），走 typecheck + 人工比对既有同构范式（order-list `_seq`/cart.ts `setActive` 等）核验，反向自检以「读代码复核逻辑分支覆盖两态」代替测试红绿。
- **待人·真机走查**（根因#8·记账不改代码）：`privacyGate open()` 覆盖 pending 的并发窗口（UNCERTAIN·需真机双击「添加照片」复现·静态判不了，记账见 `待办与债.md`）。
- **归因**：根因#8（构建过≠真机能用·回包乱序/抢跑/迟到副作用工具端不暴露）×多（④⑤⑨⑩⑪⑮）+ 根因#14（可观测/静默失败·②⑭）+ 根因#12（接缝参数往返约定·⑥）+ 金额单源（①）。未命中新病根，均归口既有账本。

## E（fable）· 2026-07-09 · cloud 钱链/状态机 bug sweep Round1 承重组·7 条确认 bug（两维 sweep+对抗验证·批1 落地）

全仓 bug 清除两维 sweep Round1 猎手发现、fable 亲读代码三层确认后的承重批（P1×3、P2×4），逐条测试先红后绿（`rewrite/cloud/tests/` mock db harness）、反向自检覆盖 1/2/6 三条（临时改回坏行为→测试真红→已还原绿）。`npm run check` 1514→1527（+13 测试）全绿。

- **① overrideRefund 行级钱守恒缺失+无幂等+add 静默吞**（P1·`adminApi/actions/refunds.ts`）：旧版 `used` 只算全单已退，同一行反复调用越规退款可越退成兄弟行的钱（share 每次重算不看「这一行自己已拿走多少」）；`asId` 带 `Date.now()` 后缀无幂等；`.add().catch(()=>{})` 写失败仍触发真退款、回调按 outRefundNo 反查将无单。修：行/单双口径分别封顶取小（usedLine/usedOrder）；确定性 `_id=orderId__lineId__ovrN`（N=既有同前缀单数·撞键天然幂等）；add 失败必中止，读回 `doc(asId)` 判定撞键（409 CONCURRENT）或真写失败（500 WRITE_FAIL + console.error 留痕）。
- **② payCallback 关单复活漏窗**（P1·`callbacks/payCallback.ts`）：`transition()` 返回的 `doc` 是条件更新前的读；关单定时器在「读到 pending」与「条件更新」之间抢先转 closed 时，回调误判「已 paid：幂等 no-op」静默返回——钱已收、单卡死 closed、无告警。修：`moved===false` 且读时值仍是 `pending` → 重读一次现值再枚举四分支（closed 走复活/paid 系列 no-op/refund_required no-op/意外值告警）。
- **③ ensureActivation 双失败假成功**（P2·`app/actions/learning.ts`）：add 抛错且读回也失败时返回本地伪造 doc，`activateCourse` 据此回「activated」但库里无激活记录。修：双失败返 null，`activateCourse` 对 null 回 `ERR.NOT_ACTIVATED`（复用既有码，客户端已有失败反馈路径；用户重扫走 `qr.activatedBy===OPENID` 幂等自愈）+ `alert('anomaly',...)` 留痕。
- **④ getInventory 页查失败当到底**（P1·`kit/inventory.ts`）：分页扫描单页 `.catch(()=>({data:[]}))` 把失败页当空页、误判「扫到底」，静默返回不完整列表且 `truncated:false`（Dashboard 低库存待办漏报）。修：页查失败 fail-loud（去掉局部 catch），交唯一调用方 adminApi 域出口统一 try/catch 兜底 500（不加重复局部保护）。
- **⑤ getTxAlerts 无 limit 静默截断**（P2·`adminApi/lib.ts`）：三路裸 `.get()` 命中服务端默认 100 条截断，与注释「精确不漏老单」矛盾。修：三查各加 `.limit(1000)`。
- **⑥ pageQuery 游标无 tiebreaker**（P2·`kit/paging.ts`）：单一时间戳字段游标，跨页边界同值记录被 `lt(cursor)` 永久跳过。修：复合游标 `{v,id}` + `_id` 第二排序列打破平局（`v` 严格小 或 `v` 相等且 `_id` 严格小）；向后兼容旧纯值游标。测试桩 `tests/helpers/wx-server-sdk/` 同批扩展链式多列 `orderBy`（`command.and/or` 首版曾用过，Round2 复核后已改用不依赖它的写法，见下）。
- **⑦ createAgent/setAgentWecomUserId 先查后写竞态**（P2·`adminApi/actions/agents.ts`）：keyHash/wecomUserId 唯一性先查后写，并发可撞出两账号共享同一凭证（登录串号）。修：写后复核——重查计数 >1 即回滚自己刚写的那份（createAgent 删整个新建 doc；setAgentWecomUserId 还原字段旧值）并回 409。
- **测试桩配套扩展**（`tests/helpers/wx-server-sdk/`，供本批+后续并发测试复用）：`DocRef.set()` 复用 `beforeUpdate` 钩子（createAgent 等 `doc().set()` 落库点可注入并发）；新增 `beforeAdd`/`beforeGet` 钩子（模拟 add 撞键/真写失败、分页扫描单页查询失败）；链式多列 `orderBy`。
- **归因**：根因#1（副作用/幂等/状态机）×3（①②⑦）+ 根因#7（分页/计数/截断失真）×3（④⑤⑥）+ 根因#14（失败不可观测/假成功）×2（①c 与①合并计、③）。结构性：是——「先查后写」「单一游标无 tiebreaker」「裸 .get() 默认截断」在本仓其余域也可能有同构实例，留 `待办与债.md` 追踪同类扫描。

### E-Round2（2026-07-09 同日）· 走查复核 3 条（P0×1 确认修复、P2×2 确认修复）

对上述 Round1 落地代码做走查复核（三层：猎手→对抗验证→fable 亲读代码），发现 Round1 自身引入/遗留的 3 个问题，逐条修复、`npm run check` 全绿（1514→1528，+14 测试）、反向自检全部覆盖（临时改回坏行为→测试真红→已还原绿，见下）：

- **P0·overrideRefund 与客户端 applyRefund 跨函数 TOCTOU**（`adminApi/actions/refunds.ts`）：①的行级封顶只防「overrideRefund 自己跟自己撞」——它与 `app/actions/orders.ts` 的 `applyRefund` 各用不相交的确定性 `_id`（`orderId__lineId__ovrN` vs `orderId__lineId`），互不占位；两者「读 exist（都读到空）→算金额→写」的窗口一旦交叠，同一行可被两条路径各批一次，累计退款达该行分摊 2 倍且吃空兄弟行的钱。修：**首个记录改与 applyRefund 共用同一 `_id`（裸位 `orderId__lineId`）**——只有该行已存在任意记录（含 applyRefund 写的/本函数写的/rejected 的）时才落编号位 `__ovrN`。两条路径的首次写并发时物理撞同一 `_id`，输家 add() 必炸：overrideRefund 侧读回判 409 CONCURRENT，applyRefund 侧走它自己既有的撞键分支判 ALREADY_APPLIED（均是既有幂等收尾逻辑，只是此前各占一个 _id、从不相撞）。orders.ts 不在本批白名单，全程未改它一行——修复完全在 refunds.ts 一侧收口。测试新增「跨函数 TOCTOU 收口」用例（beforeAdd 钩子精确复现「读时无、写时撞」）+ 更新既有 2 条 asId 相关用例的种子/断言对齐新 id 命名空间。
- **P2·pageQuery 复合游标用了未经真实云数据库验证的 `db.command.and()/or()`**（`kit/paging.ts`）：Round1 的 tiebreaker 修复是全仓首次用 `and/or` 嵌套，只被同批自制的测试桩验证过（桩的 and/or 语义也是本函数作者自己写的，等于自证自绿，[靠人#8]「构建过≠真机能用」）。修：改用全仓已被生产大量验证过的单字段 `lt`/等值（timers/throttle/refunds 等到处在用）——拆两条独立查询（tie 段 `cursorField===v && _id<id` / lt 段 `cursorField<v`）并行取 `limit+1` 后内存合并裁剪，语义等价、零新增未验证原语。测试桩里已无用途的 `command.and/or`/`matchDoc` 递归分支同批退役（不留一套没有生产用途、也未经真机验证的桩能力给后人误当参照抄）。
- **P2·ensureActivation 双失败告警未真正留痕**（`app/actions/learning.ts`）：③的修复调 `alert('anomaly',...)`，但 `kit/observe.ts` 的裸 `alert()` 只打一行 `console.error`，不落 `anomalies` 账本、运营后台 `listAnomalies` 查不到、企微群也收不到，不满足自己注释所称的「留痕」，也是全仓应用层唯一一处违反「`sev='anomaly'` 直接调 `recordAnomaly()`」既有惯例的调用点。修：改调 `recordAnomaly('invariant-violation', 'ACT_PERSIST_UNVERIFIED', {fn:'activateCourse', code}, 'high')`——首见即桥接 alert + 持久化 anomalies + （配了 webhook 则）推企微，三头都占（`kit/anomaly.ts` 单源）。测试补断言 `anomalies` 账本确有该条记录。
- **反向自检**：三条均临时改回 Round1 原实现（asId 恢复固定用 `__ovrN`/paging 去掉 tie 查询只留 lt(v)/learning 改回裸 `alert()`）逐一跑对应新测试→真红（分别是 500≠409、`seen.size` 1≠3、`anomalies` 空断言失败）→原样还原、全量 `npm run check` 复绿。
- **归因**：P0 与根因#1（副作用/幂等/状态机·跨函数维度此前未覆盖）同源；两条 P2 都落 `[靠人#8 验证样本失真]`（and/or 未过真机）与根因#14（留痕失真）范畴，未立新根因。

## D（fable）· 2026-07-07 · mp C 类竖切自修循环·6 确认逻辑 bug（并行审计+对抗验证·5→1→0→0 双连续零闭环）

C 类 5 竖切落地后跑逻辑自修循环（并行审计工作流：7 区逐区审计 + 每 finding 对抗验证滤假阳 → 确认 bug → 守卫先行自修 → 再审计）。**收敛序列 iter1=5→iter2=1→iter3=0→iter4=0**（双连续零正式闭环）。共 **6 条确认逻辑 bug**（0 P0·0 P1·6 P2），全在本轮新代码或其「同口径」对应文件。

- **iter1·5 bug**（`6046c6c`）：① **phone 纯非数字误当清空**（cloud user.ts·契约矛盾）——`'abc'` 归一化为空命中 `d===''` 清空分支→清掉已存号，与注释「非法剔除不覆盖」相悖；修：区分 `raw.trim()===''`（显式清空）vs 归一化为空（剔除不改）。② **客服360 学习完成度 percent>100**（cloud customer360 provider·跨模块口径分叉）——provider doneCount 数全部 done 键未取交集→stale 段键顶爆，而 C 端 mapLearning 已取交集且注释声称「同口径」；修：provider 分子 ∩ 段集（percent 恒≤100·两端真同口径）。③ **my-courses onShow 回包乱序**（mp·生命周期）——无代次守卫·慢/超时 ok:false 迟到回包盖成空态误显「输入激活码」；修：`_seq` 代次落地前比对丢过期回包。④ **地址新增双击双条**（mp·幂等）——新增态 onSave 无锁+不回写 id+navigateBack 异步→连点 push 两条抢默认；修：校验后 saving 锁贯穿到销毁。⑤ **资料载入失败清空手机号**（mp·fail-open）——onLoad login 失败→字段空→onSave 恒发 `phone=''` 静默覆盖清空已存号；修：`loaded` 闸未成功载入禁存（读故障不触发破坏性写）。
- **iter2·1 bug**（`9fb9703`）：**order.ts reload 回包乱序覆盖过期态**（mp·与 ③ 同类遗留实例·loop re-audit 价值）——onShow/取消/支付/确认收货多触发点并发 reload，慢回包把横幅盖回过期态（取消成功已 closed 又被在途 onShow 的 pending 盖回·在已关闭单重现取消/支付按钮）；修：reload 加 `_seq`。**顺带收口 order-list.ts 同类**（既有 tab-token 挡不住同 tab 乱序·亲读确认后补 `_seq`）。
- **iter3/iter4·0 bug**：iter3 回归复审 iter2 代次守卫 + 完备性复扫（cancelOrder 状态机边界/cover 字段对 applyRefund·submitReview 副作用/评分口径一致/region 串一致/phone 联动）零确认；iter4 确认性零轮（钱幂等·隐私·状态机新视角对抗全扫）零确认。
- **守卫**：2 机器守卫（app-user 纯字母不覆盖旧号例 + app-admin5 学习 percent 封顶例·均反向自检咬红）；4 处 Page 级生命周期修（glass-easel 无 Page 单测·typecheck+审+真机·同 mp loop 批E/F 范式）。
- **归因**：根因#8（构建过≠真机能用·回包乱序/双击/弱网载入工具端不暴露）+ 根因#3（fail-open→fail-closed）+ 跨模块口径分叉（provider vs C 端 mapLearning）。**结构性：是**——「回包乱序覆盖成过期态」在本轮出现 3 处实例（my-courses/order/order-list），是活跃页面反复复现的类，靠人记必漏、靠 loop re-audit 逐轮逼出（player 已有 playToken 结构守卫·此三处为 Page 级窄修·未上结构守卫）。
- **⚠️ 待人·真机走查**（root#8·工具/单测证不出）：① 订单详情取消/支付/确认收货后后台切回不重现过期按钮；② 我的课程弱网初载慢+快速进课返回不误显「输入激活码」；③ 地址新增页连点保存不落两条；④ 资料页 login 失败态保存被拦而非清空手机号；⑤ 订单封面图真机 cloud:// 直渲、课程进度条真值、地区级联三级、手机号回显。

## C（fable）· 2026-07-05 · recordAnomaly 真机不落库（部署 inspect 逼出·根因#8·已修+守卫·结案）
**现象**：巡检机 inspect 部署到生产、`tcb fn invoke` 回 `red:["stuck-order"]`，但 `anomalies` 集合恒空（`inspectRuns` 却有 2 条）。**根因（#8 构建过≠真机能用）**：`recordAnomaly` 用 `doc().update()`-first，真机 wx-server-sdk 对缺失文档/未建集合 update **抛错**（非返 updated:0），被外层 fail-soft `catch` 静默吞、再到不了 add fallback——`inspectRuns` 有 add+createCollection 兜底故正常，`anomalies` 无、恒空；**整个 bug 收集器半边（含 notifyAlert 桥）真机不落库**。**为何测试没抓**：桩 `doc().update()` 缺失文档返 updated:0 + `add` 自动建集合，双重掩过真机（桩≠真机经典）。**修**：`recordAnomaly` 改 add-first + createCollection 兜底（不依赖 update-on-missing）。**守卫（治本·让桩诚实）**：桩加 `control.markUncreated` opt-in 逼真机「未建集合 add/update 抛错」+ anomaly.test「集合尚未建也能落库」用例（旧写法必红）。**验证**：重部署 inspect + 重调 → anomalies 落库成功（stuck-order 带 9 单 samples）；check 1386→1387 全绿·反向自检禁 createCollection→用例红→还原绿。**结案**（详 `重构日志.md 2026-07-05 部署记账`）。

## A（fable）· 2026-07-04 立 / 2026-07-05 结案 · 真单联测退款触发失败 REFUND_TRIGGER_FAIL（真根因订正·已修+守卫）

- **现象**：M2 钱链真单联测——新线 app 下单支付成功（订单 `2026070413019480`·2 元·微信交易号 `4200003134202607040164483193` 在案），申请售后成功，admin 审批同意 → `REFUND_TRIGGER_FAIL`、售后单条件回滚回 `applied`（卡"待审核"）。
- **真根因（2026-07-05 订正·取到微信原文报错定案）**：退款经 `callFlow` → 微信，`out_refund_no` = 售后单 `_id` = `orderId__lineId`，而 `lineId = productId__spec`。**新线**订单行带 `lineId`，此单 SKU spec = `sku测试文案`（**含中文**），故 `_id` = `2026070413019480__pmq8yfwvp__sku测试文案`。微信退款单号仅允许 `^[0-9a-zA-Z_|*@-]{1,64}$`，中文必拒——`59cd5b4` 修好 callFlow 可观测性后，adminApiV2 日志 7/05 拿到微信原文：`PARAM_ERROR ... "/body/out_refund_no" ... 必须匹配 ^[0-9a-zA-Z_|*@-]{1,64}$`。**是代码把含中文的内部主键当外部平台单号用，不是平台漂移。**
- **推翻旧结论**：原判「退款工作流 `kbgzl_n8gojr3a` 平台漂移（被删/失效）」，依据「今日入参与 6/14 成功单逐字同构」——**该前提错了**：6/12–6/25 成功单是**旧线**建的（订单行**无 lineId**→ `_id=订单__productId`·纯 ASCII 退成功），7/04 是新线建的（`_id` 带中文 spec）。out_refund_no 形状 ASCII→含中文，正是新旧线差异。**工作流一直是好的**（它正确转发、微信才拒）——曾差点据错误结论去动控制台资产 #3，是可观测性缺陷（`.catch(()=>null)` 吞真错·#14）把排障引偏。
- **修复（本批·根因#12 接缝契约）**：kit `refundNoFor(_id)` 把内部 _id 规整为微信合规单号（已合规 ASCII 老单原样·含非 ASCII/超长者派生「订单号前缀 + cyrb53 短哈希」）；applyRefund/overrideRefund 建单落 `outRefundNo` 字段、approveRefund 取库存或现派生并落库、发微信改用它；refundCallback 按 `outRefundNo` 反查（回退 `.doc(_id)` 兼容纯 ASCII 老单）。守卫 `rw-money3-golden`/`rw-admin3-golden`（roots 加 #12·断言 out_refund_no 恒配平台正则 + 回调按字段反查命中·反向自检 identity 篡改 4 红）。
- **待办③ 收尾（✅ 已闭环·2026-07-05）**：部署 `adminApiV2` + `refundCallback` 到生产（`app` 未部署·approveRefund 对无 outRefundNo 的老单现场派生，故非必需·M5 同名替换时再上；adminApiV2 冒烟 `/adminv2` ping 200·HTTP 未坏）→ 用户后台重点该单"同意"→ **派生合规单号 `2026070413019480pmq8yfwvpsku-ho0hyd0n1g` 被微信受理**（不再 PARAM_ERROR）→ ~12s 后 refundCallback 按 outRefundNo 反查认领 → `status:refunded`、`refundTransactionId:4200003134202607040164483193`、`refundedAt` 在案。**2 元原路退回·端到端生产验证通过·无需新单**。
- **归因**：根因#12（平台接缝——非"平台单方漂移"，是**接缝未校验传出参数合规**：内部标识符直接当外部平台单号用，违反平台字符集/长度契约。#12 根治由"接缝收口单点"扩为"收口 + 传出合规"）+ **根因#14**（`.catch(()=>null)` 吞微信真错致排障引偏·扳机已由 `59cd5b4` 补 `[LD_ALERT]`）。结构性：是。

## B（fable）· 2026-07-05 · 控制台换皮回归族·5 bug（六域审计发现·修复批先红后绿复现）

- **共因**：M3 UI 换皮 batch2-18「逻辑零改·check 全绿」实为丢逻辑——`map*.ts` 单测只覆盖抽出的纯映射函数，**不覆盖 .vue 模板交互与功能完整性**，故换皮删交互/功能一路全绿漏过（根因#8「构建过≠真能用」·验证样本失真）。旧线参照 `packages/admin/src/pages/*`；还原 backlog 见 `待办与债.md` 同名节。
- **B1 组装幂等退化**（`rewrite/admin/src/api/scm.ts:30` runAssembly 每次现生成 assemblyId·旧 ScmAssembly 跨重试复用 pendingAssemblyId、成功才换）：双击/慢网 → 双扣料 + 双入成品（碰钱与库存·P1）。**✅ 已修 2026-07-05 还原批11**：assemblyId 移出 api、ScmBom 页 `pendingAsmId` 持有（确认时生成一次·失败复用·成功才换·改组装表单 watch 重置）·runAssembly 收 assemblyId 参；后端 `scmAssembly.ts _id=assemblyId` 确定性 claim 幂等闸不变（撞 id 409 不双扣·rw-scm-golden 覆盖）。
- **B2 对账内部异常恒 0**（`rewrite/admin/src/lib/mapSystem.ts:76` 把后端 `exceptions` 对象当数组取 `.length`·实为 `{feeMismatch,refundMismatch,stuckRefunds}`·reconciliation.ts:129 仍返回）：异常明细永不渲染（P2）。修：按对象形状渲染三类明细。
- **B3 Kb 新建 FAQ 键失联**（`rewrite/admin/src/pages/Kb.vue` addRow 生成随机 `faq-<ts>`·旧线须手填=客服菜单叶子 id 如 `logistics:eta` 才被 bot 命中）：新建 FAQ 客服 bot 永远命不中·路由断链（P1·隐性损坏）。修：还键输入框+唯一性校验+载入预设。
- **B4 物料安全库存阈值失效**（`ScmMaterials` 建档无 threshold 输入·matForm.threshold 恒 0 存库·列表 `.low` 判定永不触发）：低库存告警实质废（P2）。**✅ 已修 2026-07-05 还原批12·核实后端就绪**：后端 `scmMaterials.ts:25,59` 本有 threshold 字段（parse+validate+存）、matForm+saveMaterial 也带 threshold、列表 `.low`（`m.threshold && m.stock<=m.threshold`）本就 threshold-aware——**只缺建档表单那个输入框**（恒 0）。补 `<input v-model.number="matForm.threshold">` 即真修复（非摆设·后端全链已就绪）。
- **B5 看板误删真数据**（Dashboard 以"后端无源"删 hot/stuck/recentActivity·实 dashboard.ts:133-135 仍返回·并误记进待办 stale 条目）：非崩溃 bug，归还原 backlog + 已订正待办 stale 记账。
- **B6 已证伪**（不修）：疑「存 Settings 抹掉 alertEvents」——查 `rewrite/cloud/.../actions/cards.ts:9,26` 合并保存·非整存·互不抹除 → 安全。「验证不是相信」拦下一条假 bug。
- **归因**：根因#8（验证样本失真——单测钉纯函数、放过模板行为）。结构性：是——换皮期无「交互/功能完整性」层校验，机器守不了模板交互（同 #8/#10），靠人 review + 逐页 1:1 对旧线兜。
