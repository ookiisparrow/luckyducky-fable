# 项目现状

更新日期：2026-06-14

本文件只记「当前事实」和「下一步」。历史里程碑（生产线建设 / 切换前留档）已归 `archive/工作日志-生产线-至20260612.md` + `重构日志.md`。

## 当前阶段
**v0.9.1（重构版）已部署到生产云环境**（2026-06-14，28 云函数全量上线、替换 v0.9）。本仓 `luckyducky-next` 即生产源、云环境从本仓迭代（身份见 `CLAUDE.md`）。全栈功能在线（商品/课程/订单/真实支付/售后退款/进度/评价/发货/管理控制台），功能等价 v0.9 + 重构全链路加固（四主张 T1–T4 + round-2 体检）；getProducts 真 tcb 验证、真实数据完好。距「全客户上线」剩：前端发布 + 真机验收 + 内容后补（见下一步）。
**第二次重构（演进·技术整合）进行中**：见 `演进地图.md`（P0 知识地基 + 文档精简已做）。

## 仍是样例或占位
- ✅ 微信登录（静默 openid + 登录半屏弹窗 + 默认昵称/随机头像）+ 商品/课程/订单已上云 + 真实支付/退款已通；协议为占位（上线前补，见占位清单 §七）。
- 优惠券 / 积分 / 客服 仍样例或 Toast。
- 图位多为灰占位；视频素材为占位 / 外链。

## 交接快照（v0.9.1，2026-06-14）
🏷️ **v0.9.1 已部署生产**（本仓为生产源）：守卫 14 repoCheck + 5 fileRule、277 测试。**控制台正册资产勿删**：连接器 wxpay_33nb7su、支付工作流 sywxzfapifqzf_nncvqss2、退款工作流 kbgzl_n8gojr3a、paynotify/refundnotify。🎉 验收单 X/Y/Z 全过。**剩全客户上线**：前端发布 + urlCheck→true + 微信合法域名；内容后补：关键课视频/扫码域名/店名/客服。

## 运维事实
- 后端 Serverless（云函数×28 + 云数据库 + 云存储 + HTTP 服务，上海地域，CDN 自带）。
- **环境续费到期 2026-12-07**（个人版）。
- 管理控制台已发布静态托管（固定网址 + 口令）。

## 验证缺口
- 小程序真机：顶部安全区、视频同层渲染需真机抽查。
- 外链视频需配合法域名或关闭校验。

## 下一步
1. **全客户上线**（用户择时）：前端 `build:mp-weixin` → 上传审核发布（发布前 `manifest.urlCheck`→true + 微信后台配齐合法域名）；控制台 `build:admin` 重部署。验收 Y/Z 真机已过，库权限 X 上线前核。
2. **内容后补**（不阻塞后端）：关键课真视频 / 扫码自有域名 / 店名 / 客服——见 `上线前占位清单.md`。
3. **第二次重构演进**：按 `演进地图.md` P1→P4 各自设计推进。
4. **欠债**：依赖主升级（另开分支）、admin 账号 v2、Sass `@use`——见 `技术债与重构.md`。

## 产品方向探索
「制作陪伴」承重柱探索（数针/记状态 vs 卖包+锁视频）见 `项目思路重构-上帝视角.md`——独立 owner track、待拍板。
