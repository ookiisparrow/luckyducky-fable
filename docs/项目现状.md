# 项目现状

更新日期：2026-06-11

本文件只记录"当前事实"和"下一步",避免 `CLAUDE.md` 变成工作日志。每个里程碑更新这里 + `工作日志.md`。

## 当前阶段
前端主干已基本完成。**微信云开发 P0 已接入、静默微信登录已跑通**;项目正从"纯前端样例"向"云开发全栈"过渡。距真实上线还缺:其余业务数据上云、真实支付、真实素材、小程序真机验证。

## 已完成主干
- 页面:首页 / 商品详情 / 全部评价 / 购物车 / 结算 + 支付成功 / 个人中心 / 地址簿 / 资料编辑 / 订单 + 待支付 / 售后 + 评价 / 视频教程流程(欢迎 → 目录 → 播放 + 求助面板)。
- 下单闭环已通:详情 → 加购 → 购物车 → 结算 → 支付成功。
- 工程化:Pinia + 持久化(`persist` + `sanitize` 数据契约)、`logger`、`validate`、systemBar、ESLint/Prettier、Vitest。
- 顶部安全区:非沉浸页已接 `utils/systemBar.js`(避状态栏 + 微信胶囊)。
- **微信云开发 P0**:`wx.cloud.init`(`utils/cloud.js`)+ `cloudfunctions/`(getOpenId / initDb / login)+ 六个集合已建 + 静默登录(openid upsert `users`)真机跑通。环境 `cloudbase-d4gcssqbv06865479`、AppID `wxcbd2fb68b81bcfb1`。
- **商品数据上云**:`getProducts` 云函数 + 商品 store(`store/products.js`)+ `api/shop.js`(`callCloud` 收口);首页横滑 / 详情头部读云端,H5 / App 回退本地 `catalog`。`tcb` CLI 已部署 + seed,invoke `getProducts` 验证 5 个商品在云端。
- **课程三层模型上云**:`data/course.js` 改 chapter / lesson / segment 三层(规格 v2 §三,进度剥离为用户态 `SAMPLE_PROGRESS`);`seedCourses` / `getCourses` 云函数 + `api/course.js` + `store/courses.js` 照商品三件套;catalog / player 从 store 取数;契约 + 回退测试(27 用例)。已部署 + seed,invoke 验证三层数据在云端;开发者工具四沉浸页眼校 ✓。技术债 #4 同批清账(PR #11–#14)。
- **技术债全部清零**(2026-06-10):独立项(#1/#2/#6/#7)+ 云开发捆绑项(#3/#4/#5,PR #13/#15/#16)均关账;订单上云的 checkout 侧已就位(`CheckoutSubmitDock` 留提交态扩展位)。
- **订单上云**(2026-06-10,PR #24/#25):`createOrder`(openid 闸门 + 云端定价 + 名称/地址快照 + **模拟支付** paid)+ `getMyOrders`;下单 → 支付成功 → 订单详情**同一笔**(调试日志 C 关账);H5/App 回退本地同规则生成。已部署,闸门 invoke 验证;端到端走验收单 E。
- **P3 二维码激活闭环**(2026-06-10,PR #27/#28):`genQrcodes`/`activateCourse`(一码一用并发安全)/`confirmEnter`(enteredAt 留证 + 启发失效退货权)/`getMyCourses`(确认是进课唯一闸门);welcome 两页式课程欢迎页 + catalog 锁态 + player 鉴权。**E2E 已在工具端真实 openid 全链路验证**(含启发失效命中);剩真机验收(验收单 F)+ 印刷物料(普通链接二维码,决策 §13,剩域名/规则/清单/扫码验证,见 下一步 #4)。
- **观看进度上云 + 通用埋点**(2026-06-11):`trackEvent`(events 流水 + segment 进度折叠,一次埋点两用,规格 §七 事件模型)/`getMyProgress` 云函数 + `utils/track.js` 收口 + `store/progress.js`;播放页段末/播完/离开上报,目录角标与「我」页继续学习卡接真(无云回退样例)。lint/test(53)/双端 build/眼校 ✓,PR #33 已合并。部署:trackEvent / getMyProgress 均 ✓(闸门 invoke ✓);**E2E PASS**(2026-06-11 工具端真实 openid,`scripts/progress-verify.cjs`:segment_done/watch_at 写入 → progress 折叠 → 读回全对,events/progress 集合自建生效)。剩验收单 I 用户走查。
- **订单列表接真单**(2026-06-11):新页 `order-list`(五状态 tab,云端订单,点卡进详情同一笔)+「我」页九宫格/全部订单接真实订单与角标(真实计数)+ 详情按真实 status 映射(`ORDER_STATUS` 单一来源)。模拟支付阶段新订单直接进「待发货」(待支付为空属正常,P4 接真实支付后才有 pending 态)。工具眼校真实云单 ✓;验收走验收单 H。
- **资料上云**(2026-06-11):`updateProfile` 云函数(openid 闸门 + 白名单字段)+ 资料编辑页接微信头像昵称能力(`chooseAvatar`/`type="nickname"`,条件编译)+ 头像传云存储换 fileID + 登录后云端资料回灌展示;**手机号从 UI 全面移除**(规格落地)。lint/test(45)/双端 build 全绿;云函数已部署 + CLI 验 `NO_OPENID` 闸门 ✓;**验收单 G 用户验收通过**(2026-06-11,PR #31 已合并)。

## 仍是样例或占位
- ✅ 微信登录(静默)+ 商品 / 课程 / **订单**均已上云(订单为**模拟支付**,P4 接真实支付)。
- 无真实微信支付。
- 售后 / 物流 / 优惠券 / 积分 / 客服 均样例或 Toast(~~「我」页订单入口走样例~~ ✅ 2026-06-11 已接真单,见 订单列表)。
- 商品详情的规格 / 评价 / 图文 / 套装仍共用样例;推荐位可能推荐到当前商品。
- 图位多为灰占位;视频素材为占位 / 外链。

## 验证缺口
- 小程序真机:顶部安全区、视频同层渲染仍需真机抽查(沉浸页胶囊避让已于 2026-06-10 用 `scripts/visual-check.cjs` 在开发者工具自动化验证通过)。
- 外链视频在小程序端需配合法域名或关闭校验。

## 下一步
> 2026-06-10 需求复盘后,以 `设计规格-课程电商系统.md` v2 为准(课程三层模型 / 两页欢迎页 / 条目级退货权 / 放弃手机号)。
1. 拍板规格 §七 余下待确认项(✅ 管理后台形态已定 §11;✅ 二维码类型已定 §13,2026-06-11;仍待:课程编排形态、视频存储);微信支付资质:✅ **用户确认即将提交申请**(2026-06-11,进行中)。
   - 2026-06-10 补充需求:**商品款式持续新增、配套课程同步新增**(规格 §四-6「持续上新」流水线)。数据模型已支持(多课数组 + products.courseId);主要影响后台课程编排的上线时机。
2. ~~修调试日志 A / B~~ ✅ 已关账(PR #5 修复 + 2026-06-10 自动化眼校通过);独立技术债 #2/#6/#7 亦已清零(PR #6–#8)。
3. ~~P2 课程三层模型上云(照三件套模式,捆绑技术债 #4)~~ ✅ 全部关账(PR #11–#14);~~订单上云~~ ✅ 已完成(PR #24/#25,模拟支付,2026-06-10)。**P2/订单验收剩真机抽查**:验收单 B(云端课程)+ E(下单贯通)+ 视频同层渲染,见 `验收手册.md`。
4. ~~P3 二维码激活闭环~~ ✅ 核心闭环完成(PR #27/#28,E2E 验证全过,2026-06-10)。**剩**:真机验收(验收单 F,含他人扫码需第二微信号;2026-06-11 已备 3 个未用测试码见验收单 F)、**印刷物料**(二维码类型已定=普通链接二维码,决策 §13;剩:~~①确认域名~~ ✅ 已确认有备案域名(2026-06-11) ②小程序后台配「扫普通链接二维码」规则(需域名根目录放校验文件) ③批次 URL 清单导出 ④真机扫码验证)、退货验收扫码查询工具(归管理后台,P5/CMS 安装时做)。
5. ~~完善资料 UI(头像 / 昵称;**手机号已放弃**)~~ ✅ 已完成(2026-06-11,云函数已部署;剩 PR #31 合并 + 验收单 G);真实微信支付、真实素材、真机验证。
6. **视频上传控制台**(2026-06-11 用户提出:定制 UI + 上传逻辑,给课程 segment 灌真实视频素材)。按约定先改规格再动代码;关联待确认项:视频存储方案(云存储 fileID v1 / VOD 防盗链 P5)、控制台形态(小程序内管理页 / 网页端)。

## 产品方向探索（已讨论 · 待推进,勿丢失）
> 从原 `CLAUDE.md`「产品探索书签」迁入。
- **已验证的真实新手痛点**:数错针 / 看不懂针目 / 记错针数(= 计数 · 识别 · 记忆)。
- **关键洞察**:这是"计数 / 状态"问题,视频再好也只解决"看不懂"的一半,解决不了数错 / 记错。
- **候选承重柱(暂缓)**:交互式「**计数陪伴**」—— app 替你数针 / 记进度 + 每步"针目特写"参照;视频降为素材。
- **登月级(待可行性 spike)**:AI 视觉点数(拍照识别并数针),可行性未知。
- **当前决定**:先把视频 UI 做完作为保底;以上方向以后推进,不锁死。
