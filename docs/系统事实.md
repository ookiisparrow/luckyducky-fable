# 系统事实（指路卡·零副本）

> 2026-07-23 减法批改制：本册名册层已退役——**接口/集合/计数不存在第二份，代码即唯一真值**，此处只留查询钥匙（指路卡上没有会过期的内容）。旧线正册见史料「系统事实-旧线正册-至20260723.md」；活线名册史料「系统事实-名册终版-至20260723.md」（git 历史可查）。

| 要什么 | 真值单源（直接读/跑） |
|---|---|
| 云函数（部署单元） | `cloudbaserc.json` `functions`；部署别名 `scripts/lib/deploy-aliases.mjs` |
| app action | `rewrite/cloud/src/functions/app/index.ts` 注册表 |
| adminApi action | `rewrite/cloud/src/functions/adminApi/index.ts` `ACTIONS` + main() pre-auth 判支 |
| 集合 | `rewrite/shared/src/collections.ts` `COLLECTIONS` |
| 模块归属/危险分级 | 仓根 `modules.json`（守卫 `module-registry-complete` 焊与源码双向一致） |
| 云调用权限 | `rewrite/cloud/build.mjs` `OPENAPI_PERMS`（守卫 `rw-openapi-perm-declared` 焊） |
| 静态托管路径 | `docs/运维手册.md` §⑩ |
| 需求→函数→守卫链 | `docs/需求清单.md` 映射表（守卫 `requirement-trace` 对代码注册表核） |
