# LuckyDucky 系统事实

生成日期：2026-06-13  
代码基线：`0cc7f9d`  
生成方式：`$luckyducky-interface-catalog` + `scan_interfaces.py` + 人工核对关键实现

## 1. 正册范围

本文件是项目接口的权威登记册。这里的“接口”不只指 HTTP API，也包括：

- 小程序端 API facade：`packages/miniapp/src/api/**`
- 管理端 API facade：`packages/admin/src/api/cloud.js`
- 云函数：`packages/cloud/src/functions/**`
- `adminApi` HTTP 访问服务及其 `action`
- 支付、退款、定时任务、工作流回调
- 云存储 fileID、临时 URL、上传前缀
- DB collection 的读写契约

新增、删除、改名、改请求响应结构、改鉴权或改副作用时，必须同步更新本文件。

## 2. 接口归口规则

| 规则 | 当前正册口径 |
| --- | --- |
| 小程序页面 | 页面不直接调用云函数，优先经 `store/**`，再经 `packages/miniapp/src/api/**` 或明确的 utility facade。 |
| 管理后台 | UI 只经 `packages/admin/src/api/cloud.js`，由该文件统一 `post(action, data)` 到 `adminApi`。 |
| 云函数 | 按域放在 `packages/cloud/src/functions/<domain>/`，共享能力走 `packages/cloud/src/kit/**`。 |
| 管理 action | 统一登记在 `packages/cloud/src/functions/admin/adminApi/index.ts` 的 `ACTIONS` 查表。 |
| 支付/退款回调 | 只允许工作流服务端调用，带用户 `OPENID` 的调用视为伪造并 ACK no-op。 |
| 付费视频 | `videoFileId` 不出公开目录接口，只由 `getPlaybackUrl` 服务端鉴权后换短时 URL。 |
| 云存储 | 存储路径、读写者、是否发临时 URL 都视为接口契约。 |

## 3. 扫描基线

> **⚠️ 2026-07-09 M5 切换注记**：本表数字为**仓内旧线（`packages/` 冻结参照）静态扫描真值**（守卫焊 repo、非云端）。**云端实况自切换起＝重写线**：云函数 15（`rewrite/cloud`·同名替换上线）+ 平台自带 lowcode 2；旧线 26 函数已从云端清退（`重构日志` 同日切换条有终态清单）。查云端事实以切换条与 `rewrite/M5-切换runbook.md` §一 为准；本表随 `packages/` 冻结原样保留供参照。

| 类别 | 数量 | 说明 |
| --- | ---: | --- |
| 云函数 | 38 | 来自 `cloudbaserc.json` 与 `packages/cloud/src/functions/**`（含 CS 域 `kfCallback`/`kfBind`/`kfSend`[客服主动发·§P0 ④·承面C sendAgentMessage 雏形]/`kfHealthProbe`[微信客服活体探针·30 分定时·静默故障推企微告警]/`recallScan`·主动召回 B4.4 定时 + `dataConsent`[数据共享告知同意写侧·后台360工作站 B3.3·承面C 车道 C·守卫 `cs-data-share-consented`] + Feedback 域 `submitFeedback`·待办#23 + learning 域 `submitCheckpointPhoto`·节点诊断 B2.2）。计数守卫 `function-count-synced` 焊死、别手抄（规则⑥）。 |
| 小程序/云端调用 | 26 | `callCloud`、`wx.cloud.callFunction`、`cloud.callFunction` 调用。 |
| admin action | 107 | `adminApi/index.ts` 的 `ACTIONS`（104）+ pre-auth `ping/login/loginByWecomCode`。计数守卫 `admin-action-count-synced` 焊死、别手抄（规则⑥）。 |
| DB collection | 37 | 真值＝`kit/collections.ts` COLLECTIONS 册 37 个（含 进销存 SCM 7 集合·`materials`/`suppliers`/`purchaseOrders`/`outworkOrders`/`assemblyOrders`/`stockLedger`/`bomProfiles`·蓝图 docs/进销存ERP/·SCM-0 预登记消灭并行冲突面·materials/stockLedger 仅 kit/scmStock 读写；含 `rateLimit` 频控·根因#13；`inventory` 实物库存·库存#1·仅 kit/inventory 读写；`auditLog` 操作审计·操作审计#4·仅 kit/audit 写；`kfState`/`kfIdentity` 微信客服·CS 域 + `feedback` 意见反馈·待办#23 + `wxBills` 微信账单缓存·S16 外部对账·仅 adminApi.downloadBill 写 + `checkpoints` 节点诊断·后台360工作站 B2.2·仅云函数读写 + `conversations` 客服会话归档·后台360工作站 B5.1·仅 cs/kfCallback 写·adminApi.searchConversations 读 + `kb` 知识库·后台360工作站 B4.1·FAQ 答案单源 + `csat` 客服满意度·后台360工作站 B4.3·仅云函数读写 + `csSession` 承面C 会话态·后台360工作站 B6·状态机 pending→active→closed(+escalated) + `agentState` 承面C 坐席在线态·后台360工作站 B6·仅坐席台 actions 写 动态建）。计数守卫 `collection-count-synced` 焊死、别手抄（规则⑥）。 |
| 存储引用 | 55 | `fileID`、`cloudPath`、`getTempFileURL`、`uploadFile`、`videos/` 等。 |
| webhook/任务候选 | 7 | 支付回调、退款回调、微信客服回调、定时关单、转发脚本等。 |

扫描缺口解释：

- `cloudbase_module` 是云开发工作流平台适配入口，不是本仓库本地云函数。
- `adminApi`、`initDb`、`seedProducts`、`seedCourses`、`closeExpiredOrders` 属 HTTP/运维/定时入口，不要求有小程序端调用。
- `packages/miniapp/src/utils/cloud.js:18` 是注释中的调用示例，不是实际散落调用。
- `login` 当前由 `packages/miniapp/src/store/user.js` 直接调用云函数，`trackEvent` 由 `packages/miniapp/src/utils/track.js` 调用云函数。两者应在正册登记为特例，后续可迁到 `api/user.js`、`api/progress.js` 或保留为专用 facade。

## 4. Client Facade Map

### 4.1 小程序端

| Facade | 云函数 | 消费者 | 失败语义 |
| --- | --- | --- | --- |
| `api/shop.js.getProducts` | `getProducts` | `store/products.js` | 失败返回空列表。 |
| `api/content.js.getContent` | `getContent` | `store/content.js` | 失败或无记录走本地默认内容。 |
| `api/course.js.getCourses` | `getCourses` | `store/courses.js`、播放页 | 失败返回空课程列表。 |
| `api/course.js.getPlaybackUrl` | `getPlaybackUrl` | 播放页 | 未授权/无素材/失败返回空字符串。 |
| `api/activation.js.activateCourse` | `activateCourse` | 激活页 | 失败返回 `{ ok:false, error:'NETWORK' }`。 |
| `api/activation.js.confirmEnter` | `confirmEnter` | 进课确认页 | 失败返回 `{ ok:false, error:'NETWORK' }`。 |
| `api/activation.js.getMyCourses` | `getMyCourses` | `store/activation.js` | 失败按未解锁处理。 |
| `api/order.js.createOrder` | `createOrder` | `store/orders.js`、结算页 | 写侧失败抛错，不本地伪成功。 |
| `api/order.js.payOrder` | `pay` | 支付页 | `PAY_CANCELLED`、`PAY_NOT_ENABLED` 等向上抛。 |
| `api/order.js.confirmReceive` | `confirmReceive` | 订单页 | 写侧失败抛错。 |
| `api/order.js.getOrderById` | `getOrderById` | 订单详情 | 失败返回 `null`。 |
| `api/order.js.getMyOrders` | `getMyOrders` | 订单列表 | 失败返回空分页结果。 |
| `api/aftersales.js.getMyAfterSales` | `getMyAfterSales` | 售后列表 | 失败返回 `null`，避免覆盖已有数据。 |
| `api/aftersales.js.applyRefund` | `applyRefund` | 售后申请 | 云端拒绝抛错；无云返回 `null`。 |
| `api/review.js.getReviews` | `getReviews` | 商品评价 | 失败返回 `null`，页面可回退样例。 |
| `api/review.js.submitReview` | `submitReview` | 评价提交 | 云端拒绝抛错；无云返回 `null`。 |
| `api/user.js.updateProfile` | `updateProfile` | 个人资料页 | 云端拒绝抛错。 |
| `store/user.js.login` | `login` | 用户 store | 当前直接 `wx.cloud.callFunction`，建议后续迁入 `api/user.js`。 |
| `utils/track.js.track` | `trackEvent` | 页面/播放埋点 | fire-and-forget，失败只记日志。 |
| `api/feedback.js.submitFeedback` | `submitFeedback` | 意见反馈页 | 成功 toast，失败/无云返回 false 提示重试。 |
| `utils/report.js.reportError` | `trackEvent`（经 track，type=error） | App.vue 全局错误兜底 | fire-and-forget，错误进 events 通道（待办#23）。 |

### 4.2 管理端

| Facade | 后端入口 | 鉴权 | 说明 |
| --- | --- | --- | --- |
| `admin/src/api/cloud.js.login` | `adminApi` action `login` | 管理口令，首次 bootstrap 需 `ADMIN_BOOTSTRAP_KEY` | 口令本地保存到 `localStorage`，供后续请求带 `key`。 |
| `admin/src/api/cloud.js.post` | `adminApi` action 查表 | 除 `ping/login` 外全部先 `checkKey` | 所有管理端业务接口统一走这一层。 |
| 商品、课程、卡片、批次、内容、订单、退款、看板函数 | `adminApi` actions | 管理口令 | 当前都在单个 `cloud.js` 内，后续可按域拆 facade，但不改变后端入口。 |

## 5. 云函数正册

字段压缩说明：`鉴权/授权` 写身份闸和关键授权谓词；`副作用` 写 DB、支付、存储、状态流转；`测试` 写主要覆盖文件。

### 5.1 Catalog 域

| ID | 运行名 | Provider | 消费者 | 鉴权/授权 | 请求 | 响应/副作用 | 集合/存储 | 测试 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `catalog.get-products` | `getProducts` | `packages/cloud/src/functions/catalog/getProducts.ts` | `api/shop.js` | 公开读 | 无 | `{ ok, list }`，按 `sort` 升序；价格仅供展示，下单重新定价 | 读 `products` | `tests/cloud/getProducts.test.js` | active |
| `catalog.get-content` | `getContent` | `packages/cloud/src/functions/catalog/getContent.ts` | `api/content.js` | 公开读 | 无 | `{ ok, home|null }`；无文档返回 `null` | 读 `content/home` | `tests/cloud/getContent.test.js` | active |
| `catalog.get-help-videos` | `getHelpVideos` | `packages/cloud/src/functions/catalog/getHelpVideos.ts` | `api/help.js` | `withOpenId`（求助面板在播放页内·均已登录·通用免费内容无权属闸） | 无 | `{ ok, items:[{ id,title,sub,desc,dur,url }] }`；`url` 为短时临时 URL（无视频则 `null`），不暴露 `videoFileId`（守卫 `help-video-url-via-cloud-only`） | 读 `content/helpVideos`；读 `videos/**` fileID 换临时 URL | `tests/cloud/getHelpVideos.test.js` | active |
| `catalog.get-reviews` | `getReviews` | `packages/cloud/src/functions/catalog/getReviews.ts` | `api/review.js` | 公开读 | `productId`、`cursor?`、`limit?` | `{ ok, list, nextCursor, hasMore, summary? }`：列表 cursor 分页（债#13）；汇总仅首页·基于 ≤200 样本·`approx` 标注 | 读 `reviews` | `getReviews.test.js`、`getReviewsPaged.test.js` | active |
| `catalog.submit-review` | `submitReview` | `packages/cloud/src/functions/catalog/submitReview.ts` | `api/review.js` | 用户；本人订单、订单 `done`、商品在单内、一单一品一评 | `orderId`、`productId`、`rating`、`text?`、`tags?`、`anon?` | 写评价；昵称云端快照或匿名 | 读 `orders/users`，写 `reviews` | `tests/cloud/submitReview.test.js` | active |
| `catalog.seed-products` | `seedProducts` | `packages/cloud/src/functions/catalog/seedProducts.ts` | 运维/CLI | `withAdminGate`：服务端无 `OPENID` 或 `users.isAdmin` | seed 数据 | 初始化商品样例 | 写 `products` | `tests/cloud/seedGates.test.js` | internal |

### 5.2 User 域

| ID | 运行名 | Provider | 消费者 | 鉴权/授权 | 请求 | 响应/副作用 | 集合/存储 | 测试 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `user.login` | `login` | `packages/cloud/src/functions/user/login.ts` | `store/user.js` | `withOpenId` | 无 | upsert 用户档案 + **best-effort 存 unionid 到 users**（有 unionid 时·§查订单·供 kfCallback batchget 反查 openid·不调外部 API·不反噬登录），返回 `{ isNew, user }` | 读写 `users`（含 unionid） | `tests/cloud/userWrites.test.js`、`login.test.js` | active |
| `user.update-profile` | `updateProfile` | `packages/cloud/src/functions/user/updateProfile.ts` | `api/user.js` | `withOpenId`；只能更新本人 | `nickname?`、`bio?`、`avatar?`；`avatar` 只收 `cloud://` 或空串 | 白名单截断后更新或建档 | 读写 `users`；头像 fileID 只保存引用 | `tests/cloud/userWrites.test.js` | active |

### 5.3 Learning 域

| ID | 运行名 | Provider | 消费者 | 鉴权/授权 | 请求 | 响应/副作用 | 集合/存储 | 测试 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `learning.get-courses` | `getCourses` | `packages/cloud/src/functions/learning/getCourses.ts` | `api/course.js` | 公开读 | 无 | 返回三层课程目录；段只暴露 `id/name/dur/hasVideo`，不返回 `videoFileId`（段级免费预览已撤除·守卫 `free-trial-extinct`） | 读 `courses` | `tests/cloud/getPlaybackUrl.test.js` | active |
| `learning.get-playback-url` | `getPlaybackUrl` | `packages/cloud/src/functions/learning/getPlaybackUrl.ts` | `api/course.js` | `withOpenId`；全段一律须本人 `activations.enteredAt != null`（免费预览通道已撤·旧库残留预览标记不构成授权） | `courseId`、`segmentId` | 返回短时播放 URL 或 `url:null`；不暴露 fileID | 读 `courses/activations`；读 `videos/**` fileID 换临时 URL | `tests/cloud/getPlaybackUrl.test.js` | active |
| `learning.activate-course` | `activateCourse` | `packages/cloud/src/functions/learning/activateCourse.ts` | `api/activation.js` | `withOpenId`；码未用或本人重扫 | `code` | `qrcodes.unused -> activated`；幂等补 `activations` | 写 `qrcodes/activations` | `tests/cloud/activateCourse.test.js` | active |
| `learning.confirm-enter` | `confirmEnter` | `packages/cloud/src/functions/learning/confirmEnter.ts` | `api/activation.js` | `withOpenId`；本人已激活 | `code` | 首次写 `enteredAt`；把本人订单内对应商品 `refundable=false` | 读写 `activations/orders`，读 `products` | `tests/cloud/confirmEnter.test.js` | active |
| `learning.get-my-courses` | `getMyCourses` | `packages/cloud/src/functions/learning/getMyCourses.ts` | `api/activation.js` | `withOpenId`；只看本人 | 无 | 返回本人已确认进课列表，同课去重 | 读 `activations` | `tests/cloud/getMyCourses.test.js` | active |
| `learning.get-my-progress` | `getMyProgress` | `packages/cloud/src/functions/learning/getMyProgress.ts` | `api/progress.js` | `withOpenId`；只看本人 | 无 | 返回本人进度列表，集合不存在返回空 | 读 `progress` | 间接覆盖于 `tests/cloud/trackEvent.test.js` | active |
| `learning.track-event` | `trackEvent` | `packages/cloud/src/functions/learning/trackEvent.ts` | `utils/track.js` | `withOpenId`；只写本人事件 | `type`、`page?`、`targetId?`、`meta?`，`meta <= 1024` | 写 `events`（含 `first_frame` 首帧耗时·meta.ms/kind）；`segment_done/watch_at` 折叠写 `progress` | 写 `events/progress` | `tests/cloud/trackEvent.test.js` | active |
| `learning.gen-qrcodes` | `genQrcodes` | `packages/cloud/src/functions/learning/genQrcodes.ts` | `adminApi.createBatch`、运维 | `withAdminGate`；课程必须存在 | `courseId`、`count`，上限 500 | 生成唯一激活码和批次 | 读 `courses`，写 `qrcodes` | `tests/cloud/genQrcodes.test.js` | internal/admin |
| `learning.seed-courses` | `seedCourses` | `packages/cloud/src/functions/learning/seedCourses.ts` | 运维/CLI | `withAdminGate` | seed 数据 | 初始化课程样例 | 写 `courses` | `tests/cloud/seedGates.test.js` | internal |
| `learning.submit-checkpoint-photo` | `submitCheckpointPhoto` | `packages/cloud/src/functions/learning/submitCheckpointPhoto.ts` | `pages/checkpoint`（节点诊断拍照页·miniapp） | `withOpenId` + `withRateLimit`；须本人已进课（`activations.enteredAt`）；**UGC 图片入库前必过 `imgSecCheck` 内容安全·fail-closed**（守卫 `ugc-imgsecchecked`·根因#3） | `courseId`、`nodeId`、`fileId` | 校验通过才写一条节点照片提交（确定性 `_id=sub:openid:course:node`·幂等重传覆盖） | 读 `activations`，写 `checkpoints`，调 `cloud.openapi.security.imgSecCheck`（经 kit/contentsec） | `tests/cloud/checkpoints.test.js` | active |

### 5.4 Orders/Payment 域

| ID | 运行名 | Provider | 消费者 | 鉴权/授权 | 请求 | 响应/副作用 | 集合/存储/外部 | 测试 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `orders.create-order` | `createOrder` | `packages/cloud/src/functions/orders/createOrder.ts` | `api/order.js` | `withOpenId`；前端不传价，云端按 `products` 定价 | `items[]`、`address` | 创建订单；`PAY_MODE=real` 写 `pending`，否则 mock 直写 `paid` | 读 `products/config`，写 `orders` | `tests/cloud/createOrder.test.js` | active |
| `orders.pay` | `pay` | `packages/cloud/src/functions/orders/pay.ts` | `api/order.js` | `withOpenId`；本人 `pending` 单；惰性超时关单 | `id` | 触发支付工作流；0 元单直接 `paid` | 读写 `orders`，读 `config/pay`，调用 `cloudbase_module` | `tests/cloud/pay.test.js` | active |
| `orders.pay-callback` | `payCallback` | `packages/cloud/src/functions/orders/payCallback.ts` | 微信支付工作流、`console-assets/forward-node.js` | 系统回调；`defineNotifyCallback` 拒绝带 `OPENID` 调用 | v2/v3 支付通知 | 成功幂等翻 paid：`pending -> paid`（库存自下单持有）；`closed -> paid` 须重抢库存成功，抢不到（售罄）`closed -> refund_required` + `PAID_BUT_OOS` 告警（防超卖·审核 P0）；金额不符留 `feeMismatch` | 写 `orders` | `tests/cloud/payCallback.test.js` | webhook |
| `orders.close-expired-orders` | `closeExpiredOrders` | `packages/cloud/src/functions/orders/closeExpiredOrders.ts` | `cloudbaserc.json` timer | 系统定时任务；`isServerCall` 拒带身份的客户端调用 | 无 | 每 5 分钟关 15 分钟以上 `pending` 单 | 写 `orders` | `tests/cloud/closeExpiredOrders.test.js` | system |
| `system.cleanup-events` | `cleanupEvents` | `packages/cloud/src/functions/system/cleanupEvents.ts` | `cloudbaserc.json` timer | 系统定时任务；`isServerCall` 拒带身份的客户端调用 | 无 | 每日删 90 天以上 `events`（债#9 防无界增长；events 无人读、删旧安全） | 删 `events` | `tests/cloud/cleanupEvents.test.js` | system |
| `orders.get-my-orders` | `getMyOrders` | `packages/cloud/src/functions/orders/getMyOrders.ts` | `api/order.js` | `withOpenId`；只返回本人 | `cursor?` | 游标分页 `{ list,nextCursor,hasMore }` | 读 `orders` | `tests/cloud/getMyOrders.test.js` | active |
| `orders.get-order-by-id` | `getOrderById` | `packages/cloud/src/functions/orders/getOrderById.ts` | `api/order.js` | `withOpenId`；本人订单，不泄露他人存在性 | `id` | 返回本人订单或 `NOT_FOUND` | 读 `orders` | `tests/cloud/getOrderById.test.js` | active |
| `orders.confirm-receive` | `confirmReceive` | `packages/cloud/src/functions/orders/confirmReceive.ts` | `api/order.js` | `withOpenId`；本人 `shipped` 单 | `id` | 原子 `shipped -> done`，写 `doneAt` | 写 `orders` | `tests/cloud/confirmReceive.test.js` | active |
| `orders.apply-refund` | `applyRefund` | `packages/cloud/src/functions/orders/applyRefund.ts` | `api/aftersales.js` | `withOpenId`；本人订单、状态可退、商品在单内、未失退货权 | `orderId`、`productId`、`reason?` | 按订单实付比例算退款额；一单一品一售后 | 读 `orders`，写 `afterSales` | `tests/cloud/applyRefund.test.js` | active |
| `orders.get-my-after-sales` | `getMyAfterSales` | `packages/cloud/src/functions/orders/getMyAfterSales.ts` | `api/aftersales.js` | `withOpenId`；只返回本人 | `cursor?` | 游标分页售后单 | 读 `afterSales` | `tests/cloud/getMyAfterSales.test.js` | active |
| `orders.refund-callback` | `refundCallback` | `packages/cloud/src/functions/orders/refundCallback.ts` | 微信退款工作流、`console-assets/forward-node.js` | 系统回调；`defineNotifyCallback` 拒绝带 `OPENID` 调用 | v3 退款通知 | 核验订单号+金额；成功 `approved/applied -> refunded`；不符留 `refundMismatch` | 写 `afterSales/orders` | `tests/cloud/refundCallback.test.js` | webhook |

### 5.5 Admin/System 域

| ID | 运行名 | Provider | 消费者 | 鉴权/授权 | 请求 | 响应/副作用 | 集合/外部 | 测试 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `admin.admin-api` | `adminApi` | `packages/cloud/src/functions/admin/adminApi/index.ts` | `packages/admin/src/api/cloud.js` | HTTP POST；`ping` 免口令，`login` bootstrap，其他 action 全部 `checkKey` | `{ action,key,data }` | action 分发；统一 CORS/JSON/错误包装 | 多集合；存储；退款工作流 | `tests/cloud/adminPipeline.test.js`、`adminMisc.test.js`、`adminRefundActions.test.js` | active |
| `system.init-db` | `initDb` | `packages/cloud/src/functions/system/initDb.ts` | 运维/CLI | `withAdminGate` | 无 | 初始化集合/索引类工作 | 多集合 | `tests/cloud/seedGates.test.js` | internal |

### 5.6 CS 域（微信客服 in-chat 智能客服）

接缝收口于 `packages/cloud/src/kit/wecom.ts`（企业微信「微信客服」平台接缝·根因#12）：验签 + AES 解密（`defineKfCallback` fail-closed·根因#3）+ access_token DB 缓存 + sync_msg/send_msg/idconvert/转人工。密钥走云环境变量 `WXKF_*`（前置：企业微信认证 + 小程序绑开放平台·靠人）。

| ID | 运行名 | Provider | 消费者 | 鉴权/授权 | 请求 | 响应/副作用 | 集合/外部 | 测试 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `cs.kf-callback` | `kfCallback` | `packages/cloud/src/functions/cs/kfCallback/index.ts` | 企业微信微信客服回调（HTTP 访问服务·待绑路径） | `defineKfCallback`：验签(msg_signature)+AES 解密，验不过不进 onEvent（根因#3 fail-closed·伪造告警） | GET `echostr` 验 URL / POST 事件密文 | 拉 `sync_msg`(cursor+msgid 去重)→关键词识别发 `msgmenu`/`miniprogram`/文字/转人工 | 读写 `kfState`，读 `kfIdentity/orders`，调企业微信 API（kit/wecom） | `tests/cloud/kfCrypto.test.js`、`kfDispatch.test.js`、`kfWecom.test.js` | active |
| `cs.kf-bind` | `kfBind` | `packages/cloud/src/functions/cs/kfBind/index.ts` | 小程序（登录拿 unionid 后主动触发） | `withOpenId` + `withRateLimit`（转换 API 限频·须用户主动触发） | `unionid` | unionid→external_userid（企业微信转换 API）→建 `ext:euid→openid` 映射 | 写 `kfIdentity`，读写 `kfState`(token)，调企业微信 API | `tests/cloud/kfBind.test.js` | active |
| `cs.kf-send` | `kfSend` | `packages/cloud/src/functions/cs/kfSend/index.ts` | 后端/CLI（§P0 ④ send_msg 真机验 · 承面C 坐席台 sendAgentMessage 雏形） | `isServerCall`（服务端专用·带 openid 的客户端调用拒·根因#3·守卫 `kf-send-server-gated`） | `externalUserId`/`openKfId`/`text` | 48h 接待窗口内经微信客服 `send_msg` 主动发文字给顾客（验「非即时·坐席主动发」命门·根因#8） | 调企业微信 API（kit/wecom `sendMsg`）·不写库 | `tests/cloud/kfSend.test.js` | active |
| `cs.data-consent` | `dataConsent` | `packages/cloud/src/functions/cs/dataConsent/index.ts` | 小程序（客户在同意页同意/撤回·pages/consent） | `withOpenId` + `withRateLimit`（只写本人·根因#3·防刷） | `agree`（true 同意/false 撤回） | 数据共享告知同意写侧（B3.3·承面C 车道 C）：写 `users.csDataShare{agreed,at}`；外包坐席读客户 360 前经 `kit/csAccess.assertDataShareConsent` 校验此态·未同意即拒（守卫 `cs-data-share-consented`）；独立于 C 端微信隐私授权 | 写 `users`（csDataShare 字段） | `tests/cloud/dataConsent.test.js` | active |
| `cs.recall-scan` | `recallScan` | `packages/cloud/src/functions/cs/recallScan/index.ts`（+纯决策 `rules.ts`） | 定时触发器（recallTimer·每日 9 点·见 cloudbaserc） | `isServerCall`（无 openid·客户端带身份拒·根因#3） | 无 | 读 bounded 近窗数据→纯决策算召回四类（催付/物流/激活未进课/进课未学）→经 botpush 唯一接缝（`notifyRecall`）推运营群（守卫 `recall-via-bot-seam`·根因#12）；只读+推送不写库 | 读 `orders/activations/progress`（各 bounded ≤500） | `tests/cloud/recall.test.js` | active |
| `cs.kf-health-probe` | `kfHealthProbe` | `packages/cloud/src/functions/cs/kfHealthProbe/index.ts` | 定时触发器（kfProbeTimer·每 30 分·见 cloudbaserc） | `isServerCall`（无 openid·客户端拒·防刷告警·根因#3） | 无 | 微信客服活体探针：`getAccessToken`(探 Secret)+`listKfAccounts`(真调探可信IP/权限·60020)→静默故障经 `notifyAlert` 唯一 botpush 接缝推企微告警（守卫 `kf-health-probe-wired`·调试日志 AA 隐患补法）；只读+推送不写库 | 调企业微信 API（kit/wecom） | `tests/cloud/kfHealthProbe.test.js` | active |

### 5.7 Feedback 域（运营钩子①·待办#23）

| ID | 运行名 | Provider | 消费者 | 鉴权/授权 | 请求 | 响应/副作用 | 集合/存储 | 测试 | 生命周期 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `feedback.submit` | `submitFeedback` | `packages/cloud/src/functions/feedback/submitFeedback.ts` | `api/feedback.js`（反馈页） | `withOpenId` + `withRateLimit`（10/分·根因#13）；只写本人 | `content`（必填·≤500）、`category?`（bug/idea/other·越界归 other）、`contact?`（≤100）、`page?/version?/platform?` | 白名单截断后写一条反馈；空 content 拒 `EMPTY_FEEDBACK` | 写 `feedback`（仅管理端可读·控制台锁权限） | `tests/cloud/submitFeedback.test.js` | active |

## 6. Admin API Action 正册

统一入口：`POST VITE_ADMIN_API`，请求体 `{ action, key, data }`。  
统一鉴权：除 `ping` 与 `login` 外，全部先 `checkKey(db, key, false)`。  
统一 provider：`packages/cloud/src/functions/admin/adminApi/index.ts` 的 `ACTIONS` 查表指向 `actions/*.ts`。

### 6.1 系统与会话

| Action | Provider | 请求 | 响应/副作用 | 测试 |
| --- | --- | --- | --- | --- |
| `ping` | `index.ts` | 无 | 免口令健康检查 `{ ok, ts }` | `adminPipeline.test.js` |
| `login` | `lib.checkKey` | `key` | 首次 bootstrap 需匹配 `ADMIN_BOOTSTRAP_KEY`，写 `adminConfig/auth`（带盐 scrypt）；复登校验盐感知 `keyMatches`（带 `keySalt` 走 scrypt·legacy 无盐存量走 sha256、命中即就地升级为带盐·批6） | `adminPipeline.test.js` |
| `loginByWecomCode` | `actions/wecomLogin.ts` | `code` | M⑦ 车道B·企微 OAuth 免登（pre-auth·同 login 受频控）：code→userid→查绑定账号（wecomUserId）→签发 session 令牌（存 sha·checkKey.resolveWecomSession 认·12h）。fail-closed：无 code/换不到 userid/未绑账号/停用/无缓存令牌拒（守卫 `wecom-login-gated`） | 写 `adminConfig`（session 令牌） | `wecomLogin.test.js` |

### 6.2 商品、橱窗、图片

| Action | Provider | 请求 | 响应/副作用 | 集合/存储 | 测试 |
| --- | --- | --- | --- | --- | --- |
| `listDrafts` | `actions/products.ts` | 无 | 返回草稿和 fileID 到临时 URL 的映射 | 读 `productsDraft`，读 `products/**` 图 | `adminPipeline.test.js` |
| `saveDraft` | `actions/products.ts` | `product` | `cleanProduct` 白名单清洗后保存 | 写 `productsDraft` | `adminPipeline.test.js` |
| `deleteDraft` | `actions/products.ts` | `id` | 删除草稿和已上架商品 | 写 `productsDraft/products` | `adminMisc.test.js` |
| `uploadImage` | `actions/products.ts` | `b64`、`ext`、`pid` | 小图直接上传，返回 `fileID/url` | 写 `products/<pid>/**` | 间接覆盖 |
| `publishProduct` | `actions/products.ts` | `id` | 草稿发布到 `products`；价格边界校验；首发设置 `sort/featured` | 读写 `productsDraft/products` | `adminPipeline.test.js`、`adminMisc.test.js` |
| `unpublishProduct` | `actions/products.ts` | `id` | 软停售：置 `products.listed=false`→`getProducts` 不再下发（债#12） | 写 `products.listed` | `productListed.test.js` |
| `republishProduct` | `actions/products.ts` | `id` | 恢复销售：置 `products.listed=true`（债#12） | 写 `products.listed` | `productListed.test.js` |
| `listShowcase` | `actions/products.ts` | 无 | 返回商品和封面临时 URL | 读 `products`、存储临时 URL | `adminPipeline.test.js` |
| `saveShowcase` | `actions/products.ts` | `items[]` | 更新排序和上下架展示位 | 写 `products.sort/featured` | `adminPipeline.test.js` |

### 6.3 课程、视频、分片上传

| Action | Provider | 请求 | 响应/副作用 | 集合/存储 | 测试 |
| --- | --- | --- | --- | --- | --- |
| `getVideoUploadMeta` | `actions/courses.ts` | `courseId`、`name`、`ext` | 通过 `manager-node` 签发 COS 表单直传元数据 | 写入目标为 `videos/<courseId>/**` | 间接覆盖 |
| `getCourseDraft` | `actions/courses.ts` | `courseId` | 返回课程草稿；无草稿时可回落已发布课程 | 读 `coursesDraft/courses` | `adminPipeline.test.js` |
| `saveCourseDraft` | `actions/courses.ts` | `course` | `cleanCourse` 三层白名单清洗后保存 | 写 `coursesDraft` | `adminPipeline.test.js` |
| `publishCourse` | `actions/courses.ts` | `courseId` | 草稿整体覆盖 `courses`，老学员引用自动生效 | 读 `coursesDraft`，写 `courses` | `adminPipeline.test.js` |
| `uploadChunk` | `actions/courses.ts` | `uploadId`、`seq`、`b64` | 保存 80KB 级分片，单片不超过 90,000 字符 | 写 `uploadChunks` | `adminMisc.test.js` |
| `uploadFinish` | `actions/courses.ts` | `uploadId`、`total`、`ext`、`pid`、`kind?` | 校验分片完整性，拼接后上传；清理分片 | 写 `products/**` 或 `videos/**`，清 `uploadChunks` | `adminMisc.test.js` |

### 6.4 卡片、设置、批次、首页内容

| Action | Provider | 请求 | 响应/副作用 | 集合/存储 | 测试 |
| --- | --- | --- | --- | --- | --- |
| `getSettings` | `actions/cards.ts` | 无 | 返回 `adminConfig/settings` | 读 `adminConfig` | 间接覆盖 |
| `saveSettings` | `actions/cards.ts` | `urlPrefix` | 保存二维码网址前缀 | 写 `adminConfig/settings` | 间接覆盖 |
| `getCard` | `actions/cards.ts` | `productId` | 返回卡片草稿和正面素材临时 URL；兼容旧结构 | 读 `cards`、存储临时 URL | `adminPipeline.test.js` |
| `saveCard` | `actions/cards.ts` | `card` | 白名单、颜色、尺寸校验后保存 | 写 `cards` | `adminPipeline.test.js` |
| `listBatches` | `actions/batches.ts` | `courseId` | 聚合批次总数和激活数 | 读 `qrcodes` | `adminPipeline.test.js` |
| `createBatch` | `actions/batches.ts` | `courseId`、`count` | 服务端互调 `genQrcodes` 生成码 | 调 `genQrcodes`，写 `qrcodes` | `adminPipeline.test.js`、`adminMisc.test.js` |
| `listBatchCodes` | `actions/batches.ts` | `batchId` | 返回该批次码列表 | 读 `qrcodes` | `adminPipeline.test.js` |
| `getHomeContent` | `actions/content.ts` | 无 | 返回首页内容草稿 | 读 `content/home` | `adminPipeline.test.js` |
| `saveHomeContent` | `actions/content.ts` | `home` | 白名单和条数上限后保存首页内容 | 写 `content/home` | `adminPipeline.test.js` |
| `listHelpVideos` | `actions/content.ts` | 无 | 返回求助面板辅助视频列表（管理端读回原始 `videoFileId`，已过口令闸） | 读 `content/helpVideos` | `adminPipeline.test.js` |
| `saveHelpVideos` | `actions/content.ts` | `items` | 白名单限长 + 封顶 20 条 + 剔空后保存辅助视频（全局共用） | 写 `content/helpVideos` | `adminPipeline.test.js` |

### 6.5 订单、退款、看板

| Action | Provider | 请求 | 响应/副作用 | 集合/外部 | 测试 |
| --- | --- | --- | --- | --- | --- |
| `listOrders` | `actions/orders.ts` | `cursor?`、`status?`、`q?` | 订单游标分页；`status` 服务端筛选、`q` 按单号精确搜索（根因#7 筛选/搜索失真） | 读 `orders` | `orderCounts.test.js` |
| `orderCounts` | `actions/orders.ts` | — | 按状态服务端精确计数（`.count()` 不封顶；绑 `ORDER_STATUS`）+ 全部总数，供标签计数（根因#7 计数失真） | 读 `orders` | `orderCounts.test.js` |
| `getOrderDetail` | `actions/orders.ts` | `id` | 订单详情补充：逐商品激活码状态数据链（复用 `lib.activationFor`·与退款判据同口径），供 VMlhp 订单详情面板 | 读 `orders/products/activations` | `orderDetail.test.js` |
| `shipOrder` | `actions/orders.ts` | `id`、`company`、`trackingNo` | `paid/shipped -> shipped`；金额异常单需先解除；**首次 shipped 流转逐行落 ship 核销流水**（SCM-D·fg 行只留痕不动账·确定性 `_id=ship:<orderId>:fg:<pid>__<spec>` 幂等·改单号不重记·守卫 ship-verify-ledger） | 写 `orders.shipping/status` + `stockLedger`（经门1 kit/scmStock） | `adminRefundActions.test.js`、`scmShipLedger.test.js` |
| `shipOrders` | `actions/orders.ts` | `items[{id,trackingNo,company?}]`、`company?` | 批量发货（P1·上量瓶颈）：逐单走共享核心 `shipOne`（同 shipOrder 状态闸+feeMismatch+微信上报），per-order 回报 okCount/failCount；上限 100/批；电子面单取号需快递 API·诚实延后 | 写 `orders.shipping/status` | `shipOrders.test.js` |
| `clearFeeMismatch` | `actions/orders.ts` | `id` | 人工复核后清 `feeMismatch` | 写 `orders` | `adminRefundActions.test.js` |
| `listRefunds` | `actions/refunds.ts` | `cursor?`、`status?`、`q?` | 售后单游标分页；`status` 服务端筛选、`q` 按订单号精确搜索（根因#7 筛选/搜索失真） | 读 `afterSales` | `refundCounts.test.js` |
| `refundCounts` | `actions/refunds.ts` | — | 按状态服务端精确计数（`.count()` 不封顶；绑 `AFTERSALE_STATUS`）+ 全部总数，供标签计数（根因#7 计数失真） | 读 `afterSales` | `refundCounts.test.js` |
| `getRefundDetail` | `actions/refunds.ts` | `id` | 退款决策判据：按 `_openid`+商品对应课程查 `activations` 算买家是否已激活/已进课该课程（闭 S10 自动判据·根因#8） | 读 `afterSales/products/activations` | `refundDetail.test.js` |
| `approveRefund` | `actions/refunds.ts` | `id` | 原子抢占 `applied -> approved`，触发退款工作流；失败回滚 | 读写 `afterSales/orders`，调 `cloudbase_module` | `adminRefundActions.test.js` |
| `rejectRefund` | `actions/refunds.ts` | `id`、`reason` | 拒绝售后，记录原因 | 写 `afterSales` | `adminRefundActions.test.js` |
| `overrideRefund` | `actions/refunds.ts` | `orderId`、`lineId`、`reason` | 管理员越规退款（决策§26·cap `refund:manage`·净新增超旧线 parity）：越资格规则（拒后锁/一行一售后/已进课不可退）主动退，保留钱守恒（≤实付分摊−已退·refundShareFen 封顶）；直建 approved 售后单+触发退款工作流 | 读 `orders`，写 `afterSales`，调 `cloudbase_module` | `app-admin3.test.ts` |
| `getDashboard` | `actions/dashboard.ts` | 无 | 聚合用户、订单、码、学习进度、交易异常 | 读 `users/orders/qrcodes/progress/courses/afterSales` | `adminMisc.test.js` |
| `getReconciliation` | `actions/reconciliation.ts` | `from?`、`to?`（'YYYY-MM-DD'） | 财务对账（S16·内部账）：累计收支(aggregate 精确)+每日流水(CST 日分桶)+内部异常(txAlerts 同源)；已付营收口径 | 读 `orders/afterSales`（经 getTxAlerts） | `reconciliation.test.js` |
| `downloadBill` | `actions/wxbill.ts` | `date`（'YYYY-MM-DD'）、`billType?` | 外部对账（S16·Batch 2）：经 kit/wxpay v3 签名拉微信交易账单→落 `wxBills`（确定性 _id 幂等）；凭证缺/拉取失败 fail-soft | 读 `config`，写 `wxBills`（经 kit/wxpay 出站） | `wxpay-bill.test.js` |
| `getBillMatch` | `actions/reconciliation.ts` | `from?`、`to?`（'YYYY-MM-DD'） | 外部对账（S16·Batch 3）：我方付款单 ⋈ `wxBills` by transactionId → 四类差异（平/微信有我方无/我方有微信无/金额不符）+ 账单覆盖日防误报 | 读 `orders/wxBills` | `bill-match.test.js` |
| `listInventory` | `actions/inventory.ts` | `productIds?` | 读 SKU 库存（库存#1·S14） | 读 `inventory`（经 kit/inventory） | `inventory.test.js` |
| `saveStock` | `actions/inventory.ts` | `productId,spec,stock,threshold?` | 设/调 SKU 库存（绝对值·null=不限量） | 写 `inventory`（经 kit/inventory setStock） | `inventory.test.js` |
| `listMaterials` | `actions/scmMaterials.ts` | `limit?` | 读物料主档（进销存 SCM-0·bounded ≤500） | 读 `materials`（经 kit/scmStock listMaterialDocs） | `scmMaterials.test.js` |
| `saveMaterial` | `actions/scmMaterials.ts` | `name,category,uom,color?/tier?/form?/productId?/slug?,supplierId?,threshold?` | 建/改物料档案（确定性料号推导·毛线 yarn:color:tier:form 带结仅 L·uom 建档锁死 UOM_LOCKED·守卫 scm-uom-integer） | 写 `materials`（经 kit/scmStock saveMaterialDoc·不碰 stock） | `scmMaterials.test.js` |
| `listSuppliers` | `actions/scmMaterials.ts` | 无 | 读往来单位（厂家/织女·bounded ≤200） | 读 `suppliers` | `scmMaterials.test.js` |
| `saveSupplier` | `actions/scmMaterials.ts` | `supplierId?,name,type(factory|outworker),contact?,note?` | 建/改往来单位（织女每人一档） | 写 `suppliers` | `scmMaterials.test.js` |
| `adjustStock` | `actions/scmMaterials.ts` | `materialId,delta,reason,adjustId` | 期初盘点/人工调整（必留因·delta 非零整数·同 adjustId 重放幂等·余额不为负） | 写 `materials.stock`+`stockLedger`（经 kit/scmStock applyStockMoves 门1） | `scmMaterials.test.js`、`kit/scmStock.test.js` |
| `listLedger` | `actions/scmMaterials.ts` | `materialId?,limit?` | 原料/成品流水查账（bounded ≤200·按料号过滤） | 读 `stockLedger`（经 kit/scmStock） | `scmMaterials.test.js` |
| `listPurchases` | `actions/scmPurchase.ts` | `status?,limit?` | 读采购单（进销存车道 A·bounded ≤200·createdAt 倒序·可按状态过滤） | 读 `purchaseOrders` | `scmPurchase.test.js` |
| `savePurchase` | `actions/scmPurchase.ts` | `purchaseId?,supplierId,lines[{materialId,qty,unitPriceFen}]` | 建/改采购草稿（仅 draft 可改 NOT_DRAFT·供应商须 factory·qty 正整数/价非负整数分/料号在主档/同料拒重复行·totalFen 服务端算不信前端） | 写 `purchaseOrders`（读 `suppliers`+主档校验经 kit/scmStock listMaterialDocs） | `scmPurchase.test.js` |
| `markOrdered` | `actions/scmPurchase.ts` | `purchaseId` | 采购下单（transition draft→ordered·门2 声明表 scm.spec） | 写 `purchaseOrders`（经 kit/transition） | `scmPurchase.test.js` |
| `receivePurchase` | `actions/scmPurchase.ts` | `purchaseId` | 到货入库（transition ordered→received·**首次流转绑门1 入库** purchase_in 各行 qty 正数·重放 moved=false 不双入库·流水 `purchase_in:<单据id>:<料号>` 幂等） | 写 `purchaseOrders`+`materials.stock`+`stockLedger`（经 kit/transition+kit/scmStock applyStockMoves） | `scmPurchase.test.js` |
| `cancelPurchase` | `actions/scmPurchase.ts` | `purchaseId` | 取消采购单（transition draft/ordered→cancelled·received 后声明表无边天然拒·入库后走调整单） | 写 `purchaseOrders`（经 kit/transition） | `scmPurchase.test.js` |
| `listOutworks` | `actions/scmOutwork.ts` | `status?,workerId?,limit?` | 读外协单（进销存车道 B·bounded ≤200·createdAt 倒序） | 读 `outworkOrders` | `scmOutwork.test.js` |
| `saveOutwork` | `actions/scmOutwork.ts` | `outworkId?,workerId,issueLines[{materialId,qty}],pieceRateFen` | 建/改外协草稿（仅 draft 可改·workerId 须织女档·发料行仅 `yarn:<color>:L:raw` 且主档在册·单价非负整数分） | 写 `outworkOrders`，读 `suppliers`+`materials`（经 kit/scmStock 只读出口） | `scmOutwork.test.js` |
| `issueOutwork` | `actions/scmOutwork.ts` | `outworkId` | 发料：transition draft→issued 首次流转绑原团出库（负 delta·流水 `outwork_issue:<单>:<料号>` 幂等）；库存不足补偿回滚回 draft（宁不动账勿错账） | 写 `outworkOrders`+`materials.stock`+`stockLedger`（经门1） | `scmOutwork.test.js` |
| `receiveOutwork` | `actions/scmOutwork.ts` | `outworkId,receiveLines[{materialId,qty}]` | 收货：同色带结 `yarn:<color>:L:knotted`（颜色⊆发料·每色≤发料防收比发多·主档缺 NO_MATERIAL fail-closed）；transition issued→delivered 同一次条件更新定格 `payableFen=Σ收×单价`/`lossQty=Σ发−Σ收`，再入带结账 | 写 `outworkOrders`+`materials.stock`+`stockLedger`（经门1） | `scmOutwork.test.js` |
| `settleOutwork` | `actions/scmOutwork.ts` | `outworkId` | 结算：transition delivered→settled 记 settledAt（工钱付清销账） | 写 `outworkOrders` | `scmOutwork.test.js` |
| `cancelOutwork` | `actions/scmOutwork.ts` | `outworkId` | 取消：transition draft→cancelled（仅未发料·已发料异常走物料页调整单·蓝图定稿） | 写 `outworkOrders` | `scmOutwork.test.js` |
| `getBomSetup` | `actions/scmBom.ts` | 无 | 配方一览（进销存车道 C）：全局模板 + 全部产品差异位（bounded ≤200） | 读 `config`（doc `scmBomTemplate`）+`bomProfiles` | `scmAssembly.test.js` |
| `saveBomTemplate` | `actions/scmBom.ts` | `template{commonLines[{materialId,qtyPerSet}],yarnSlots[{tier,form,qtyPerSet}]}` | 保存全局模板（白名单清洗·数量正整数·带结槽仅 L 档 fail-closed·镜像门3 校验口径） | 写 `config`（doc `scmBomTemplate`） | `scmAssembly.test.js` |
| `saveBomProfile` | `actions/scmBom.ts` | `profile{productId,yarnColors{L,M,S},packagingMaterialId,cardMaterialId}` | 保存产品差异位（_id=productId·三色 slug + 专属包装/卡片料号全必填） | 写 `bomProfiles` | `scmAssembly.test.js` |
| `previewAssembly` | `actions/scmAssembly.ts` | `productId,sets` | 打包预演（只读）：门3 resolveBom 展开用料 × 主档库存对照（need/stock/short/missing·不动账） | 读 `config`+`bomProfiles`+`materials`（主档经 kit/scmStock 只读出口） | `scmAssembly.test.js` |
| `runAssembly` | `actions/scmAssembly.ts` | `assemblyId,productId,spec?,sets` | 组装执行（单步·无状态机·撤销走调整单）：门3 解析→**快照冻结**存单（守卫 bom-snapshot-frozen·历史不追新）→门1 扣原料 assembly_out（all-or-nothing·不足撤 claim 回滚）→门4 produceStock 入成品→assembly_in fg 流水留痕；`_id=assemblyId` 确定性 claim 重放 409 不双扣 | 写 `assemblyOrders`+`materials.stock`+`stockLedger`（经门1）+`inventory`（经门4 kit/inventory produceStock） | `scmAssembly.test.js` |
| `listAssemblies` | `actions/scmAssembly.ts` | `limit?` | 组装单列表（bounded ≤100·at 倒序·含快照与用料） | 读 `assemblyOrders` | `scmAssembly.test.js` |
| `getRestockPlan` | `actions/scmPlanner.ts` | `targets[{productId,sets}]` | 备货计算器（进销存车道 D·只读）：Σ目标×门3 resolveBom 聚合需求 × 主档库存 → ① 外协缺口（带结不外购·缺口=应发同色 L 原团·派生原团需求叠回采购口径）② 采购缺口按供应商分列（够扣不出行·未建档点名）·fail-closed（坏 targets/NO_PROFILE 带 productId/NO_TEMPLATE） | 读 `config`+`bomProfiles`+`materials`（主档经 kit/scmStock 只读出口）+`suppliers` | `scmPlanner.test.js` |
| `getFgSummary` | `actions/scmPlanner.ts` | 无 | 产销统计（同车道 D·只读）：`stockLedger` 按 `itemKey`（`fg:<productId>__<spec>`）分组求和——`assembly_in`＝打包累计、`ship`＝发货/销售累计（delta 负转正展示）；不动账、不产生新写入 | 读 `stockLedger`（经门1 kit/scmStock.sumLedgerByItemKey · DB aggregate 分组） | `scmPlanner.test.js` |
| `getScmOverview` | `actions/scmOverview.ts` | 无 | 供应链总览（批 B2·只读聚合着陆页）：缺料预警（materials stock<threshold·gap 降序·limit 50·suggest 带结→outwork/其余→purchase）+ 应付未结（outworkOrders status=delivered 按 workerId 分组求和 payableFen·扫描上限 200 超界标 truncated）+ 在途计数（purchaseOrders status=ordered / outworkOrders status=issued，走 `.count()`）+ 最近流水（stockLedger 最新 8 条）；金额分整数出，不编数 | 读 `materials`（经 kit/scmStock 只读出口）+`outworkOrders`+`purchaseOrders`+`stockLedger`+`suppliers` | `app-scm.test.ts`§L、`scm-overview-bounded.test.ts` |
| `getConfigChecklist` | `actions/configChecklist.ts` | 无 | 人工配置清单（批 B9·只读·只探测状态·零回显）：12 项散落配置拼一屏「配了没」——① adminApi 进程内可信 env 布尔 `WXKF_AGENTID` ② 仅 cs 函数链路引用的 env（`WXKF_CORPID`/`WXKF_SECRET`/`WXKF_TOKEN`/`WXKF_AESKEY`）恒 `check` ③ DB 字段存在判定（`alertWebhook`/`scmBomTemplate`）④ 业务初始化态有界判定（`stockLedger` 有 adjust 流水 + `materials` 有 threshold>0，两者皆备才 `ok`）⑤ 纯人工外部后台 8 项恒 `check`；未登记 ACTION_CAPS 默认仅超管 | 读 `adminConfig`（doc `settings`）+`config`（doc `scmBomTemplate`）+`stockLedger`+`materials` | `config-checklist.test.ts` |
| `getCustomer360` | `actions/customer360.ts` | `openid` | 客户360 只读聚合（B1.1·后台360工作站）：经 provider registry 遍历 enabled 板块（画像/订单/激活/学习位置/节点照片·铁律三）组装客人全貌。**越权面（§1.5·根因#3）**：能力闸 `ACTION_CAPS=customer:view`（cs-360-rbac-gated）+ `FORCE_AUDIT` 强制留痕（cs-360-read-audited·破 shouldAudit 跳 ^get） | 读 `orders/activations/events/progress/courses/checkpoints/config`（各 provider bounded） | `customer360.test.js`、`customer360-learning.test.js` |
| `searchCustomer` | `actions/customer360.ts` | `q` | 客户检索（B1.2）：按 openid/手机/订单号/昵称精确命中（昵称模糊待真机后升级·根因#8），返匹配 openid+摘要供坐席选人。**越权读（§1.5·根因#3）**：`customer:view` 闸 + `FORCE_AUDIT` 留痕 | 读 `users/orders`（bounded·limit 20） | `customer360.test.js` |
| `getUser` | `actions/customer360.ts` | `openid` | 单客户画像（B1.2）：读 `users` 一条（按 `_openid` 兼容新旧档·bounded limit 1），白名单字段回；无档回 `user:null`。**越权读（§1.5·根因#3）**：`customer:view` 闸 + `FORCE_AUDIT` 留痕（破 shouldAudit 跳 ^get） | 读 `users` | `customer360.test.js` |
| `listCheckpoints` | `actions/checkpoints.ts` | `courseId?` | 节点诊断·列关键节点定义（B2.2·后台360工作站·`type:'def'`·bounded·按 order 升序） | 读 `checkpoints` | `checkpoints.test.js` |
| `saveCheckpoints` | `actions/checkpoints.ts` | `courseId`、`nodes[{nodeId,title,remedy,order}]` | 节点诊断·整课覆盖式策展关键节点+挽回办法（upsert def·删本课不在列表的旧 def·不碰用户 sub 提交·≤50 节点） | 写 `checkpoints` | `checkpoints.test.js` |
| `searchConversations` | `actions/conversations.ts` | `openid?`、`externalUserId?`、`channel?`、`keyword?`、`cursor?`、`limit?` | 客服会话检索（B5.1·后台360工作站·外包管控底座）：按客户+渠道筛选·`pageQuery` cursor 分页拉会话时间轴（bounded·conversations-search-bounded）·keyword 页内子串过滤。**越权读他人会话全文（§1.5·根因#3）**：`ACTION_CAPS=customer:view` 闸（conversations-pii-gated）+ shouldAudit 默认留痕 | 读 `conversations` | `conversations.test.js` |
| `conversationsReport` | `actions/conversations.ts` | `slaMs?`、`channel?` | 客服质检报表（B5.3·后台360工作站·外包管控底座）：近 1000 条会话 bounded 样本（conversations-report-bounded·超量标 approx）算会话量/首次响应时长/SLA 达标/答复率（按客户配对入站→其后首个出站）。运营聚合无逐人 PII·同 dashboard 不设 cap 闸 | 读 `conversations` | `conversations.test.js` |
| `listKb` | `actions/kb.ts` | 无 | 知识库（B4.1·后台360工作站）·列全部 FAQ/知识条目（bounded·按 分类→order 升序）供 admin 维护页编辑 | 读 `kb` | `kb.test.js` |
| `saveKb` | `actions/kb.ts` | `entries[{key,question,answer,category,enabled,order,featured?}]` | 知识库（B4.1）·整体覆盖式策展 FAQ 条目（白名单+长度上限·upsert+删不在列表的旧条目·≤100 条·`_id=key`）；客服 bot dispatch 读同一份发 FAQ 答案（守卫 `faq-via-kb-single-source`·根因#5）；`featured`（R37b·净新增）精选标记，默认 `false`——公开读 `getPublicFaq` 只回此=true 的条目 | 写 `kb` | `kb.test.js`、`app-admin5.test.ts` |
| `getCsatReport` | `actions/csat.ts` | 无 | 客服满意度报表（B4.3·后台360工作站·只读）：聚合 `csat` 出 均分/1-5 分布/总数/有备注数（bounded ≤1000·approx 标注） | 读 `csat` | `csat.test.js` |
| `listCsatEntries` | `actions/csat.ts` | `cursor?`、`limit?`、`from?`、`to?`、`maxScore?` | 满意度明细钻取（批 B6·后台360工作站·只读）：`pageQuery` cursor 分页·`[from,to)` 含起不含止（`to` 走原生 filter·`from` 走页后过滤+触界即截断）·`maxScore`（`_.in([1..N])`）。`csat` 集合结构性无 `agentId`（`recordCsat` 从未写入）——不产出坐席维度。`sessionKey` 回 `externalUserId`。默认拒 `admin:write`（未登记 ACTION_CAPS，同 `getCsatReport`·仅超管） | 读 `csat` | `csat.test.js` |
| `listQueue` | `actions/agentDesk.ts` | `cursor?`、`limit?` | 承面C 坐席台（B6·§1 定稿）·待接队列（`pending` 会话·FIFO createdAt 升序·bounded cursor/limit·根因#7）。cap `agent:handle`（外包最小权·ACTION_CAPS） | 读 `csSession` | `agentDesk.test.js` |
| `claimConversation` | `actions/agentDesk.ts` | `sessionId` | 承面C·认领会话（`pending/escalated→active`·绑认证坐席 agentId·接待上限校验 activeCount<limit·`transition('csSession')`）。cap `agent:handle` | 读/写 `csSession`（transition） | `agentDesk.test.js` |
| `releaseConversation` | `actions/agentDesk.ts` | `sessionId` | 承面C·退回会话（`active→pending`·清归属·分配 scope `scopedLoad→assertOwnedByAgent`）。cap `agent:handle` | 读/写 `csSession` | `agentDesk.test.js` |
| `sendAgentMessage` | `actions/agentDesk.ts` | `sessionId`、`text` | 承面C·坐席回复（分配 scope + 接待窗口 active + 经 `cs/kfSend` send_msg·出站落 `conversations`·守卫 `kf-send-server-gated`）。cap `agent:handle` | 读 `csSession`·写 `conversations`·调 `kfSend` | `agentDesk.test.js` |
| `getThread` | `actions/agentDesk.ts` | `sessionId`、`cursor?` | 承面C·会话消息流（cursor 增量·前端轮询·分配 scope `scopedLoad`·bounded THREAD_LIMIT·根因#7）。cap `agent:handle` | 读 `csSession`/`conversations`/`kfIdentity` | `agentDesk.test.js` |
| `setAgentStatus` | `actions/agentDesk.ts` | `status(online/busy/offline)` | 承面C·坐席在线态（写 `agentState`·排队分配据此·B6.3）。cap `agent:handle` | 写 `agentState` | `agentDesk.test.js` |
| `escalateToMerchant` | `actions/agentDesk.ts` | `sessionId` | 承面C·升级转商户超管（`active→escalated`·外包只能升不能拍板·分配 scope）。cap `agent:handle` | 读/写 `csSession` | `agentDesk.test.js` |
| `closeConversation` | `actions/agentDesk.ts` | `sessionId` | 承面C·结束会话（`pending/active/escalated→closed`·触 CSAT 提示·分配 scope）。cap `agent:handle` | 读/写 `csSession`·调 `kfSend` | `agentDesk.test.js` |
| `listMyActive` | `actions/agentDesk.ts` | `limit?` | 承面C·本坐席在接（active）会话（刷新/重登恢复在接·多会话切换·天然 scoped agentId=认证主体·bounded）。cap `agent:handle` | 读 `csSession`/`kfIdentity` | `agentDesk.test.js` |
| `getSessionCustomer360` | `actions/agentDesk.ts` | `sessionId` | 承面C·按会话看对应客户 360（外包唯一 360 读路径·双闸：分配 scope `assertOwnedByAgent` + 数据共享同意 `assertDataShareConsent`·超管 bypass·无桥接回 `NO_BRIDGE`·FORCE_AUDIT 留痕）。cap `agent:handle` | 读 `csSession`/`kfIdentity`/`users`·经 `assembleCustomer360` 聚合 | `agentDesk.test.js` |
| `createAgent` | `actions/agents.ts` | `name`、`key` | 承面C 外包账号管理（B5.2·车道 C）·建外包坐席账号（keyHash 入库不存明文·role=outsourced·撞口令拒·≥6 位）。**默认拒 `admin:write`**（未标 ACTION_CAPS→天然仅超管） | 写 `adminConfig` | `agents.test.js` |
| `disableAgent` | `actions/agents.ts` | `id`、`disabled` | 承面C·停/启外包账号（不许停超管 `auth`）。默认拒 `admin:write`（仅超管） | 写 `adminConfig` | `agents.test.js` |
| `listAgents` | `actions/agents.ts` | 无 | 承面C·列外包账号（白名单不回 keyHash·回 wecomUserId 供超管看绑没绑·排除超管）+ 看板扩展字段（批 B6·per-agent 三次有界读/count）：`status`/`stateUpdatedAt`←`agentState`（无档兜底 `offline`/null）、`activeCount`←`csSession{agentId,status:active}` count、`todayClosed`←`csSession{agentId,status:closed,updatedAt≥今日零点+08:00}` count（`closeConversation` 转 closed 只写 `updatedAt`，无独立 `closedAt` 字段）。默认拒 `admin:write`（仅超管） | 读 `adminConfig`/`agentState`/`csSession` | `agents.test.js` |
| `setAgentWecomUserId` | `actions/agents.ts` | `id`、`wecomUserId` | M⑦ 地基·回填/改绑企微 userid（免登用·空串解绑·唯一性防歧义·作用于外包或超管 auth）。默认拒 `admin:write`（仅超管） | 写 `adminConfig` | `agents.test.js` |

## 7. Webhook、任务、外部平台接口

| ID | Kind | Provider/来源 | 信任边界 | 幂等与失败语义 |
| --- | --- | --- | --- | --- |
| `workflow.cloudbase-module` | external workflow adapter | `packages/cloud/src/kit/flow.ts` 调 `cloudbase_module` | 仅云端 `pay`、`approveRefund` 调用；flowId 来自 `config/pay` | 返回 `result.data`；异常返回 `null`，调用方 fail-closed。 |
| `webhook.pay-callback` | webhook | `payCallback`，可由 `console-assets/forward-node.js` 转发 | `defineNotifyCallback` 要求无 `OPENID`，客户端伪造 ACK no-op | 成功才改状态；重复通知 no-op；关单单复活前重抢库存（售罄→`refund_required`+告警·防超卖）；金额不符留 `feeMismatch`。 |
| `webhook.refund-callback` | webhook | `refundCallback`，可由 `console-assets/forward-node.js` 转发 | `defineNotifyCallback` 要求无 `OPENID`，客户端伪造 ACK no-op | 成功且订单号/金额匹配才退款；不符留 `refundMismatch`；非成功留 `refundStatus`。 |
| `timer.close-expired-orders` | scheduled job | `cloudbaserc.json` `closeExpiredTimer` | 云开发定时触发，无用户身份 | 每 5 分钟关闭超 15 分钟 pending 单 + 回补预留库存；晚到支付回调须重抢库存才复活 paid（售罄→`refund_required`）。 |
| `timer.cleanup-events` | scheduled job | `cloudbaserc.json` `cleanupEventsTimer` | 云开发定时触发，无用户身份 | 每日（3:00）删 90 天以上 `events` 流水（债#9 防无界增长；events 只写不读、删旧安全）。 |
| `webhook.kf-callback` | webhook | `kfCallback`（企业微信微信客服回调·HTTP 访问服务·待绑路径） | `defineKfCallback` 验签(msg_signature)+AES 解密，验不过不进 onEvent（根因#3 fail-closed），伪造 `[LD_ALERT]` 告警 | GET 验 URL 回明文 echostr；POST 拉 `sync_msg`(cursor+msgid 去重)幂等处理；回空即可（消息经 send API 异步推）。 |

## 8. 云存储契约

| ID | 路径/对象 | 写入者 | 读取者 | 暴露规则 | 备注 |
| --- | --- | --- | --- | --- | --- |
| `storage.product-media` | `products/<pid>/*.{jpg,png}` | `adminApi.uploadImage`、`uploadFinish(kind != video)` | `listDrafts/listShowcase` 换临时 URL；小程序商品数据保存 fileID | 管理端展示拿临时 URL；商品详情是否可直接使用 fileID 受 CloudBase 权限控制 | 商品图属于公开展示资产。 |
| `storage.course-videos` | `videos/<courseId>/*.{mp4,mov}`（求助辅助视频走 `videos/help/`） | `getVideoUploadMeta` 直传或 `uploadFinish(kind=video)` | 课程段仅 `getPlaybackUrl`、求助辅助视频仅 `getHelpVideos` 服务端换短时 URL | `getCourses` 绝不返回 `videoFileId`；课程段一律需 `enteredAt` 授权（免费预览已撤）；求助辅助视频通用免费、登录可看；发布/保存时做孤儿视频 GC（旧份引用 − 新份引用 即删·`publishCourse`/`saveHelpVideos`） | 需在 CloudBase console 确认 `videos/` 私有读。 |
| `storage.card-art` | `cards.front.art` 引用的 `cloud://` fileID | 管理端上传并保存到 `cards` | `getCard` 换临时 URL | 仅管理端预览/打印使用 | 卡面 SVG 生成本身在前端工具，素材引用在云端。 |
| `storage.admin-upload-chunks` | `uploadChunks` collection 内 base64 分片 | 管理端 HTTP 分片上传 | `uploadFinish` 拼接后删除 | 不应被业务读取 | 临时数据，`uploadFinish` 会清理 1 小时前残留。 |

## 9. DB Collection 正册

| Collection | 主要写入者 | 主要读取者 | 契约/敏感点 |
| --- | --- | --- | --- |
| `users` | `login`、`updateProfile`、运维设 `isAdmin` | `submitReview`、`getDashboard`、`withAdminGate` | `_openid` 是用户归属；`isAdmin` 控制客户端管理闸。 |
| `productsDraft` | `adminApi.saveDraft/deleteDraft/publishProduct` | `adminApi.listDrafts/publishProduct` | 管理草稿，不直接给小程序。 |
| `products` | `adminApi.publishProduct/saveShowcase/deleteDraft`、`seedProducts` | `getProducts`、`createOrder`、`confirmEnter`、admin actions | 展示价来自这里；下单必须重新读取并校验。 |
| `coursesDraft` | `adminApi.saveCourseDraft` | `adminApi.getCourseDraft/publishCourse` | 管理草稿，可能含 `videoFileId`。 |
| `courses` | `adminApi.publishCourse`、`seedCourses` | `getCourses`、`getPlaybackUrl`、`genQrcodes`、dashboard | 公开目录需脱敏；原始文档可含 `videoFileId`。 |
| `qrcodes` | `genQrcodes` | `activateCourse`、`listBatches/listBatchCodes`、dashboard | `_id === code`，状态 `unused/activated`，一次性资源。 |
| `activations` | `activateCourse`、`confirmEnter` | `getMyCourses`、`getPlaybackUrl` | `_openid + courseId + enteredAt` 是进课和付费视频授权依据。 |
| `orders` | `createOrder`、`pay`、`payCallback`、`confirmReceive`、`closeExpiredOrders`、admin 发货、退款回调留痕 | 订单列表、评价、退款申请、看板 | `_openid` 归属；金额、状态、退款、发货均为敏感字段。 |
| `afterSales` | `applyRefund`、`approveRefund/rejectRefund`、`refundCallback` | 售后列表、看板 | `_id=orderId__productId`；退款状态和金额为对账来源。 |
| `reviews` | `submitReview` | `getReviews` | `_id=orderId__productId` 防重复；公开读会脱去 `_openid/orderId`。 |
| `content` | `adminApi.saveHomeContent/saveHelpVideos` | `getContent`、`getHomeContent`、`getHelpVideos`、`listHelpVideos` | `home` 文档驱动首页内容；`helpVideos` 文档驱动求助面板辅助视频（全局共用·`videoFileId` 不出公开接口）。 |
| `cards` | `adminApi.saveCard` | `adminApi.getCard` | 打印卡片配置，可能引用云存储素材。 |
| `adminConfig` | `adminApi.login/saveSettings` | `adminApi.checkKey/getSettings` | `auth.keyHash` 是管理认证核心；`settings.urlPrefix` 影响印刷二维码。 |
| `config` | 运维配置 | `createOrder`、`pay`、`approveRefund` | `pay.mode`、`flowId`、`refundFlowId` 控制真实收付款。 |
| `uploadChunks` | `adminApi.uploadChunk` | `adminApi.uploadFinish` | 临时分片，上传完成后应删除。 |
| `events` | `trackEvent` | 暂无业务读 | 事件流水，可能含用户行为数据。 |
| `progress` | `trackEvent` | `getMyProgress`、dashboard | 用户学习进度；按 `_openid/courseId` 折叠。 |
| `kfState` | `kfCallback`（cursor/去重）、`kit/wecom`（token 缓存） | `kfCallback` | 微信客服内部状态：`_id=token` access_token 缓存 / `cursor:<openKfId>` 同步游标 / `seen:<msgid>` 去重痕；客户端禁读写。 |
| `kfIdentity` | `kfCallback`（收客服消息时经微信客服 `kf/customer/batchget` 反查 unionid→users.openid 建映射·§查订单·守卫 `login-kf-identity-bridge`）、`kfBind`（小程序侧 idconvert·遗留） | `kfCallback`（`resolveOpenid` 查「你的订单」） | 身份桥接 `_id=ext:<external_userid>` → `openid`（根因#3 不信前端·login 存 unionid→users、kfCallback batchget 反查建此映射·绕客户联系 idconvert 48002）；客户端禁读写。 |
| `feedback` | `submitFeedback`（用户写本人反馈） | 暂无业务读（仅管理端控制台查·待办#23） | 意见反馈：`{category,content,contact?,page,version,platform,_openid,createdAt}`；仅管理端可读，控制台锁权限。 |
| `checkpoints` | `adminApi.saveCheckpoints`（def 节点定义）、`submitCheckpointPhoto`（sub 用户拍照·imgSecCheck 过后） | `getCustomer360`（节点照片 provider）、`adminApi.listCheckpoints` | 节点诊断（B2.2·后台360工作站）：一集合两形状·`_id=def:<courseId>:<nodeId>`（策展定义）/ `sub:<openid>:<courseId>:<nodeId>`（用户提交·幂等）；UGC 入库前过内容安全；仅云函数读写，控制台锁「仅管理端」。 |
| `conversations` | `cs/kfCallback`（入站 `archiveInbound`·`_id=wxkf:in:<msgid>` 幂等 / 出站 `archiveOutbound`·自动 _id） | `adminApi.searchConversations` | 客服会话归档（B5.1·后台360工作站·外包管控底座）：一会话消息一文档·字段 `channel/direction/externalUserId/openid/msgtype/text/at`；含 PII（会话全文·协议页已声明）；仅 cs/kfCallback 写、adminApi 读（customer:view 闸 + 留痕），客户端禁，上线前控制台锁「仅管理端」。 |
| `kb` | `adminApi.saveKb`（整体覆盖式策展） | `cs/kfCallback`（dispatch `faqAnswer` 读发 FAQ 答案）、`adminApi.listKb`、`app.getPublicFaq`（R37b·净新增·公开只读只挑 `featured:true` 且 `enabled!==false`） | 知识库（B4.1·后台360工作站）：FAQ/知识条目·`_id=key`（同分流菜单叶子 id）·字段 `{question,answer,category,enabled,order,featured}`；FAQ 答案单源（守卫 `faq-via-kb-single-source`·根因#5·替代原 dispatch 写死 TEXT_ANSWERS——**守卫扫描面暂未纳入 `getPublicFaq`**，批次白名单不含 `scripts/`，见 `docs/待办与债.md` 记账）；仅云函数读写，控制台锁「仅管理端」。 |
| `csat` | `cs/kfCallback`（dispatch `recordCsat`·会话后评分 + `attachCsatNote` 回写备注） | `adminApi.getCsatReport`（均分/分布报表） | 客服满意度（B4.3·后台360工作站）：`_id=csat:<euid>:<createdAt>`·字段 `{externalUserId,openid?,score(1-5),note,createdAt}`；score 入库前 fail-closed 校验 1..5（守卫 `csat-score-bounded`·根因#3）；仅云函数读写，控制台锁「仅管理端」。 |
| `csSession` | 承面C 坐席台 actions（B6·claim/close/escalate 经 `transition('csSession')`·尚未落地·阶段0地基先声明契约） | 承面C 坐席台（listQueue/getThread） | 承面C 会话态（B6·后台360工作站·板块#12）：`_id=wxkf:<openKfId>:<externalUserId>`·状态机 `pending→active→closed(+escalated)`（单源 `shared/cs.spec.ts`·守卫 `order-transitions-declared` 对账）·字段 `{status,externalUserId,openKfId,openid?,agentId?,claimedAt?,lastCursor?,updatedAt}`；与微信平台 service_state 两层；仅坐席台 actions 写，控制台锁「仅管理端」。 |
| `agentState` | 承面C 坐席台 actions（B6·setAgentStatus/claim/release·尚未落地·阶段0地基先声明契约） | 承面C 排队分配（B6.3·选可接坐席） | 承面C 坐席在线态（B6·后台360工作站）：`_id=agentId`·字段 `{status(online/busy/offline),activeCount,limit,updatedAt}`；排队据此选可接坐席（示忙/满额排除）；仅坐席台 actions 写，控制台锁「仅管理端」。 |

## 10. 当前治理缺口

| 优先级 | 缺口 | 影响 | 建议 |
| --- | --- | --- | --- |
| P2 | `store/user.js` 直接调用 `login`，`utils/track.js` 直接调用 `trackEvent`。 | 不是页面散落调用，但与“所有云函数经 `api/**`”规则不完全一致。 | 迁到 `api/user.js.login` 和 `api/progress.js.trackEvent`，或在规则中明确 `utils/track.js` 是 event facade。 |
| P2 | `admin/src/api/cloud.js` 同时承载所有 admin facade。 | 文件继续增长后，action 和 UI 工作流难以局部审计。 | 按域拆 `admin/src/api/products.js`、`courses.js`、`orders.js` 等，底层仍复用 `post`。 |
| P2 | 云存储 `videos/` 私有读依赖 CloudBase console 权限。 | 代码已通过 `getPlaybackUrl` 收口，但控制台权限不可由本仓库完全证明。 | 在上线 runbook 中保留人工验收项：未授权客户端不能直接读 `videos/**`。 |
| P3 | `cloudbase_module` 是外部平台工作流适配，不在本地函数列表中。 | 扫描工具会把它报成“调用但无本地实现”。 | 在后续校验脚本中加入 allowlist：`cloudbase_module` 属 external。 |

## 11. 新增接口流程

新增接口时按这个顺序改：

1. 先确定接口种类：`clientFacade`、`cloudFunction`、`adminAction`、`webhook`、`storageContract`、`dbContract` 或 `event`。
2. 小程序端先加/改 `packages/miniapp/src/api/**`，页面不要直接 `callCloud`。
3. 管理端先加/改 `packages/admin/src/api/cloud.js` 或其后续域 facade。
4. 云端实现放到对应 domain；共享鉴权、分页、回调、工作流走 `packages/cloud/src/kit/**`。
5. 本文件登记：运行名、provider、consumer、auth、authorization、request、response、side effects、collections、storage、idempotency、privacy、tests、owner、lifecycle。
6. 高风险接口必须补测试：订单、支付、退款、激活、视频、admin、存储。
7. 若新增云函数，同步 `cloudbaserc.json`、部署闸、验证脚本和本正册。

## 12. 再生成命令

```bash
python3 ~/.codex/skills/luckyducky-interface-catalog/scripts/scan_interfaces.py --root . --format markdown --output /private/tmp/luckyducky-interface-inventory.md
python3 ~/.codex/skills/luckyducky-interface-catalog/scripts/scan_interfaces.py --root . --format json --output /private/tmp/luckyducky-interface-inventory.json
```

扫描结果必须人工核对后再进入本正册，尤其是注释命中、外部平台接口、定时任务和 admin action。

## 13. 重写线（rewrite）正册

> M1 后端重写产物（ADR §23·2026-07-04 批18 收口）。**只 build 不部署**（并行期·部署是 M5 切换日动作）；打包 `npm run build:rw-cloud` → `rewrite/cloud/dist/`（每函数 index.js+package.json+按需 config.json）。守卫 `rw-interface-catalog-sync` 焊本节与源码同步。

**云函数 16 个**（函数单元＝`export const main`·含批2 净新增巡检机 `inspect` + 批 B5 净新增媒体下载 `kfMedia`）：

| 函数 | 形态 | 说明 |
| --- | --- | --- |
| `app` | 新名·**已部署 2026-07-04**（并行期验证·零覆盖线上·smoke getProducts 真数据过） | 用户端聚合网关·28 action（净新增 `getPublicFaq`·批 B8·R37b）·每 action 自带 withOpenId/withRateLimit 闸（`getProducts`/`getContent`/`getPublicFaq` 三个公开只读例外·无鉴权） |
| `adminApi` | **以新名 `adminApiV2` 部署 2026-07-04·重部署 2026-07-05**（HTTP 路径 `/adminv2`·零覆盖·旧 `/adminapi`→旧 adminApi 原样）·M5 收口定名 | 管理端 v2·HTTP 触发·源码全量 action（92 旧线对齐 + `overrideRefund` 决策§26 + ops 批系统巡检/异常 4 action）+ pre-auth `ping`/`login`/`loginByWecomCode`·口令+会话令牌·RBAC 默认拒·认证频控·写类自动审计。**2026-07-05 重部署**：修「inspect 已上线但 adminApiV2 stale 缺 ops 4 action」不一致·配置 15s/256MB 无损·smoke ping 200/错口令 401 过（待：`anomalies`/`inspectRuns` 两集合建库·ops 页读侧另一半部署） |
| `payCallback` `refundCallback` | 同名替换 | 支付/退款回调·defineNotifyCallback 防伪·幂等 |
| `closeExpiredOrders` `kfHealthProbe` `recallScan` `cleanupEvents` | 同名替换 | 定时器四件（关单/客服探针/召回扫描/流水清理·`recallRules` 为库不独立成函数） |
| `kfCallback` `kfSend` | 同名替换 | 微信客服回调（验签 fail-closed·dispatch/archive 内联）与主动发送接缝 |
| `kfMedia` | **净新增**（B5·顾客发图坐席可见） | 服务端专用（`isServerCall`）：`kf/media/get` 下载顾客发图临时素材（media_id 3 天有效）→ base64 回传；经 adminApi `getMediaUrl` 跨函数调用（同 `sendAgentMessage`→`kfSend` 模式）·平台接缝单点#12 |
| `genQrcodes` `seedProducts` `seedCourses` `initDb` | 同名替换·只部署勿调用 | 激活码生成（withAdminGate）·种子×2·建库（清单=COLLECTIONS 单源全量） |
| `inspect` | **净新增**（无旧线对应·观测·`timers/inspect`） | 巡检机（不依赖 AI·守卫 `rw-inspect-golden`·防治静默 bug）：定时跑 A 基建存活 + B 业务不变量（db-reachable / money-conserved 钱守恒 / stuck-order 卡单+死信）→写 `inspectRuns` 体检报告 + 每条红落 `recordAnomaly`（违反→bug 账本闭环·高危推告警）；`isServerCall` 拒客户端；**只读**不改业务集合；手动触发走 `adminApi.inspect.runNow`（批3）；触发器与只读部署走批5（含 anomalies/inspectRuns 两新集合建库+锁 adminonly） |

**静态托管（并行期临时路径·M5 切正式路径后清退）**：新 admin → `https://www.luckyducky.cn/admin2/`（**2026-07-05 重托管**·包 `index-CcbP1d9j.js`·连 `/adminv2`·含换皮 batch2-18 + Orders 还原 + 系统巡检/异常页；原 07-04 包 `index-CkFlxmNs.js`）· 新 agent → `https://www.luckyducky.cn/agent2/`（2026-07-04·包 `index-BzUP4OhN.js`）；旧 `/admin/`（`index-BTcGUD64.js`）`/agent/` 与根官网核验未覆盖。

**app action 28 个**：`login` · `updateProfile` · `getProducts` · `getContent` · `activateCourse` · `confirmEnter` · `getCourses` · `getPlaybackUrl` · `getMyCourses` · `getMyProgress` · `trackEvent` · `getHelpVideos` · `getPublicFaq` · `getReviews` · `getRatingSummary` · `submitReview` · `createOrder` · `pay` · `applyRefund` · `confirmReceive` · `cancelOrder` · `getMyOrders` · `getOrderById` · `getMyAfterSales` · `kfBind` · `dataConsent` · `submitFeedback` · `submitCheckpointPhoto`

**adminApi action 104 个**（92 与旧线逐行对齐 + 1 净新增 `overrideRefund`·决策§26 + 4 运行期观测 `runInspect`/`getInspectStatus`/`listAnomalies`/`resolveAnomaly`·批3 治病根#14 告警进人眼·未登记 ACTION_CAPS 默认仅超管·写类自动审计 + 1 总览 `getScmOverview`·批 B2 + 1 净新增 `getMediaUrl`·批 B5 顾客发图坐席可见·cap `agent:handle` + 1 净新增 `listCsatEntries`·批 B6 满意度明细钻取 + 3 净新增 `sampleQc`/`saveQcMark`/`listQcSampled`·批 B7 质检抽检·csSession 会话档级·未登记 ACTION_CAPS 默认仅超管·sampleQc/saveQcMark 写类自动审计 + 1 净新增 `getConfigChecklist`·批 B9 人工配置清单·只读·零回显·未登记 ACTION_CAPS 默认仅超管）：`listDrafts` · `saveDraft` · `deleteDraft` · `uploadImage` · `publishProduct` · `unpublishProduct` · `republishProduct` · `listShowcase` · `saveShowcase` · `getVideoUploadMeta` · `getCourseDraft` · `saveCourseDraft` · `publishCourse` · `uploadChunk` · `uploadFinish` · `getSettings` · `saveSettings` · `getCard` · `saveCard` · `listBatches` · `createBatch` · `listBatchCodes` · `getHomeContent` · `saveHomeContent` · `listHelpVideos` · `saveHelpVideos` · `listOrders` · `orderCounts` · `getOrderDetail` · `shipOrder` · `shipOrders` · `clearFeeMismatch` · `listRefunds` · `refundCounts` · `getRefundDetail` · `approveRefund` · `rejectRefund` · `overrideRefund` · `getDashboard` · `getReconciliation` · `getBillMatch` · `downloadBill` · `listInventory` · `saveStock` · `getCustomer360` · `searchCustomer` · `getUser` · `listCheckpoints` · `saveCheckpoints` · `searchConversations` · `conversationsReport` · `sampleQc` · `saveQcMark` · `listQcSampled` · `listKb` · `saveKb` · `getCsatReport` · `listCsatEntries` · `listQueue` · `claimConversation` · `releaseConversation` · `sendAgentMessage` · `getThread` · `getMediaUrl` · `setAgentStatus` · `escalateToMerchant` · `closeConversation` · `listMyActive` · `getSessionCustomer360` · `createAgent` · `disableAgent` · `listAgents` · `setAgentWecomUserId` · `listMaterials` · `saveMaterial` · `listSuppliers` · `saveSupplier` · `adjustStock` · `listLedger` · `listPurchases` · `savePurchase` · `markOrdered` · `receivePurchase` · `cancelPurchase` · `listOutworks` · `saveOutwork` · `issueOutwork` · `receiveOutwork` · `settleOutwork` · `cancelOutwork` · `getBomSetup` · `saveBomTemplate` · `saveBomProfile` · `previewAssembly` · `runAssembly` · `listAssemblies` · `getRestockPlan` · `getFgSummary` · `getScmOverview` · `runInspect` · `getInspectStatus` · `listAnomalies` · `resolveAnomaly` · `getConfigChecklist`

**parity 清点（2026-07-04 批18 机器核·2026-07-05 决策§26 增 1）**：旧线 38 函数全部有新线对应（14 独立函数 + 23 个用户函数收编为 app action + kfBind/dataConsent 并入 app）·adminApi 注册表 92 与旧线逐行对齐 **+ 1 净新增 `overrideRefund`**（客服端越规退款·超出旧线 parity·决策§26 显式授权·M5 整体替换不受影响：admin-only、不动数据迁移面/客户行为面）·集合面共用同一 COLLECTIONS 37 册（数据零迁移·确定性 _id 幂等并存）。云调用权限：`security.imgSecCheck`→app、`wxaSecOrder.uploadShippingInfo`→adminApi（build 产 config.json·守卫 `rw-openapi-perm-declared`）。
