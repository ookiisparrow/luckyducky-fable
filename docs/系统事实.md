# LuckyDucky 系统事实

生成日期：2026-06-13  
代码基线：`0cc7f9d`  
生成方式：`$luckyducky-interface-catalog` + `scan_interfaces.py` + 人工核对关键实现

## 1–12. 旧线正册（已退役 → `archive/系统事实-旧线正册-至20260723.md`）

> 旧线 `packages/` 已于 2026-07-23 瘦身拍板批删除，其接口/集合/facade 正册（原 §1–§12）随之整块退役归档。查旧线事实去 archive 分册或 next 仓；本文件活体＝下方计数表与 §13 重写线正册（守卫 `rw-interface-catalog-sync`/`function-count-synced`/`admin-action-count-synced` 机器维护）。

**活线计数（机器核·别手抄）**：

| 项 | 数 | 说明 |
|---|---|---|
| 云函数 | 18 | 真值＝`cloudbaserc.json` functions（2026-07-19 批L 已迁活线口径）＝17 个产物 + 1 个部署别名 `adminApiV2`（映射单源 `scripts/lib/deploy-aliases.mjs`）。计数守卫 `function-count-synced` 焊死。 |
| admin action | 115 | `adminApi/index.ts` 的 `ACTIONS`（112）+ pre-auth `ping/login/loginByWecomCode`。计数守卫 `admin-action-count-synced` 焊死。 |

## 13. 重写线（rewrite）正册

> M1 后端重写产物（ADR §23·2026-07-04 批18 收口）。**已是云端唯一版本**（2026-07-09 M5 切换同名替换上线，此后随批部署）；打包 `npm run build:rw-cloud` → `rewrite/cloud/dist/`（每函数 index.js+package.json+按需 config.json）。守卫 `rw-interface-catalog-sync` 焊本节与源码同步。

**云函数 17 个**（函数单元＝`export const main`·含批2 净新增巡检机 `inspect` + 批 B5 净新增媒体下载 `kfMedia` + 批B2 净新增每日对账 `billReconcile`）：

| 函数 | 形态 | 说明 |
| --- | --- | --- |
| `app` | 新名·**已部署 2026-07-04**（并行期验证·零覆盖线上·smoke getProducts 真数据过） | 用户端聚合网关·30 action（批1 净新增 `getProductDetail`·列表瘦身配套）·每 action 自带 withOpenId/withRateLimit 闸（`getProducts`/`getContent`/`getPageContent`/`getPublicFaq`/`getProductDetail` 五个公开只读例外·无鉴权（getPageContent 自 CMS 批A 即无闸·此前漏计）） |
| `adminApi` | **以新名 `adminApiV2` 部署 2026-07-04·重部署 2026-07-05**（HTTP 路径 `/adminv2`·零覆盖·旧 `/adminapi`→旧 adminApi 原样）·M5 收口定名 | 管理端 v2·HTTP 触发·源码全量 action（92 旧线对齐 + `overrideRefund` 决策§26 + ops 批系统巡检/异常 4 action）+ pre-auth `ping`/`login`/`loginByWecomCode`·口令+会话令牌·RBAC 默认拒·认证频控·写类自动审计。**2026-07-05 重部署**：修「inspect 已上线但 adminApiV2 stale 缺 ops 4 action」不一致·配置 15s/256MB 无损·smoke ping 200/错口令 401 过（待：`anomalies`/`inspectRuns` 两集合建库·ops 页读侧另一半部署） |
| `payCallback` `refundCallback` | 同名替换 | 支付/退款回调·defineNotifyCallback 防伪·幂等 |
| `closeExpiredOrders` `kfHealthProbe` `recallScan` `cleanupEvents` | 同名替换 | 定时器四件（关单/客服探针/召回扫描/流水清理·`recallRules` 为库不独立成函数） |
| `kfCallback` `kfSend` | 同名替换 | 微信客服回调（验签 fail-closed·dispatch/archive 内联）与主动发送接缝 |
| `kfMedia` | **净新增**（B5·顾客发图坐席可见） | 服务端专用（`isServerCall`）：`kf/media/get` 下载顾客发图临时素材（media_id 3 天有效）→ base64 回传；经 adminApi `getMediaUrl` 跨函数调用（同 `sendAgentMessage`→`kfSend` 模式）·平台接缝单点#12 |
| `genQrcodes` `seedProducts` `seedCourses` `initDb` | 同名替换·只部署勿调用 | 激活码生成（withAdminGate）·种子×2·建库（清单=COLLECTIONS 单源全量） |
| `inspect` | **净新增**（无旧线对应·观测·`timers/inspect`） | 巡检机（不依赖 AI·守卫 `rw-inspect-golden`·防治静默 bug）：定时跑 A 基建存活 + B 业务不变量（db-reachable / money-conserved 钱守恒 / stuck-order 卡单+死信）→写 `inspectRuns` 体检报告 + 每条红落 `recordAnomaly`（违反→bug 账本闭环·高危推告警）；`isServerCall` 拒客户端；**只读**不改业务集合；手动触发走 `adminApi.inspect.runNow`（批3）；触发器与只读部署走批5（含 anomalies/inspectRuns 两新集合建库+锁 adminonly） |
| `billReconcile` | **净新增**（批B2·钱链·`timers/billReconcile`·待部署+触发器人工挂） | 每日拉前一日微信交易账单（kit/wxpay `fetchTradeBill`·凭证 secureConfig/wxpay+config/pay.subMchId·守卫 `rw-bill-reconcile-golden`）→落 `wxBills`（确定性 _id 幂等）→`matchBillRows` 比对：wxOnly（微信有款我方无单·最危险类）/金额不符→`recordAnomaly` 高危入账（指纹去重·同日重跑幂等）+首见 [LD_ALERT]+新指纹才推 `BILL_RECON_DISCREPANCY` 汇总；oursOnly 刻意不告警（边界易误报·留 admin getBillMatch 手动核）；`isServerCall` 拒客户端；无账单日/未配凭证 fail-soft skipped（后者留 low 面包屑） |

**静态托管（2026-07-09 M5 切换后正式路径·admin2/agent2 已清退）**：admin → `https://www.luckyducky.cn/admin/`（2026-07-12 清空重传 135 文件·线上入口 `index-D-EKF33g.js` 实测核验）· agent → `https://www.luckyducky.cn/agent/`（2026-07-12·线上入口 `index-DxuJsLsj.js` 实测核验）· 根路径 = `rewrite/site` Astro 内容站（2026-07-09 上线 13 文件·标题「钩织材料包与新手教程 · Lucky Ducky 小棉鸭」）；并行期临时路径 `/admin2/` `/agent2/` 已随切换退役。

**app action 30 个**（批1 净增 `getProductDetail`·列表 getProducts 瘦身不下发 images[]、详情页按 id 专拉全图册·公开只读·同 getProducts 换址口径；此前 2026-07-12 同日两战役并行净增 `getPageContent`·页面内容 CMS 批A 公开只读·5 页白名单·`catalogPlayer.catalog.heroImage` cloud:// 换临时 URL + `getPublicFaq`·批 B8 R37b·KB featured 精选只读·字段瘦身）：`login` · `updateProfile` · `getProducts` · `getProductDetail` · `getContent` · `getPageContent` · `activateCourse` · `confirmEnter` · `getCourses` · `getPlaybackUrl` · `getMyCourses` · `getMyProgress` · `trackEvent` · `getHelpVideos` · `getPublicFaq` · `getReviews` · `getRatingSummary` · `submitReview` · `createOrder` · `pay` · `applyRefund` · `confirmReceive` · `cancelOrder` · `getMyOrders` · `getOrderById` · `getMyAfterSales` · `kfBind` · `dataConsent` · `submitFeedback` · `submitCheckpointPhoto`

**adminApi action 112 个**（92 与旧线逐行对齐 + 1 净新增 `overrideRefund`·决策§26 + 4 运行期观测 `runInspect`/`getInspectStatus`/`listAnomalies`/`resolveAnomaly`·批3 治病根#14 告警进人眼·未登记 ACTION_CAPS 默认仅超管·写类自动审计 + 2 净新增页面内容 CMS `savePageContent`/`getPageContent`·批A·5 页白名单每页净化·未知 page fail-closed·默认 admin:write·save 自动审计 + 1 总览 `getScmOverview`·批 B2 + 1 净新增 `getMediaUrl`·批 B5 顾客发图坐席可见·cap `agent:handle` + 1 净新增 `listCsatEntries`·批 B6 满意度明细钻取 + 3 净新增 `sampleQc`/`saveQcMark`/`listQcSampled`·批 B7 质检抽检·csSession 会话档级·未登记 ACTION_CAPS 默认仅超管·sampleQc/saveQcMark 写类自动审计 + 1 净新增 `getConfigChecklist`·批 B9 人工配置清单·只读·零回显·未登记 ACTION_CAPS 默认仅超管 + 2 净新增 `saveSecureConfig`/`savePayConfig`·2026-07-12 人工配置清单可填写化·敏感凭证/支付接缝配置入库单源·未登记 ACTION_CAPS 默认仅超管·写类自动审计·payload 嵌套防审计日志泄露 + 2 净新增 `getVodUploadSignature`/`syncVodMedia`·决策§31 转码管线批2·VOD 直传签名（UGC 纯本地 HMAC·未配置回 VOD_NOT_CONFIGURED 供 admin 回退老路）+ 转码状态 on-demand 同步（回写段 vodUrl/vodPoster/vodSpriteVtt/dur·rev 递增·publishCourse 增 VOD_PROCESSING 发布闸）·默认 admin:write·syncVodMedia 写类自动审计 + 1 净新增 `listAudit`·批 B6 操作审计#4 读出口·cursor 分页筛 operator/actionPrefix/时间窗·未登记 ACTION_CAPS 默认仅超管 + 1 净新增 `reportClientError`·批 B7 admin/agent web 前端错误上报（治病根#14 client-error 通道）·三路捕获收口·会话内去重封顶+服务端 agentId 二级节流·cap 复用 `agent:handle`·`shouldAudit` 排除）：`listDrafts` · `saveDraft` · `deleteDraft` · `uploadImage` · `publishProduct` · `unpublishProduct` · `republishProduct` · `listShowcase` · `saveShowcase` · `getVideoUploadMeta` · `getCourseDraft` · `saveCourseDraft` · `publishCourse` · `uploadChunk` · `uploadFinish` · `getSettings` · `saveSettings` · `getCard` · `saveCard` · `listBatches` · `createBatch` · `listBatchCodes` · `getHomeContent` · `saveHomeContent` · `listHelpVideos` · `saveHelpVideos` · `savePageContent` · `getPageContent` · `listOrders` · `orderCounts` · `getOrderDetail` · `shipOrder` · `shipOrders` · `clearFeeMismatch` · `listRefunds` · `refundCounts` · `getRefundDetail` · `approveRefund` · `rejectRefund` · `overrideRefund` · `getDashboard` · `getReconciliation` · `getBillMatch` · `downloadBill` · `listInventory` · `saveStock` · `getCustomer360` · `searchCustomer` · `getUser` · `listCheckpoints` · `saveCheckpoints` · `searchConversations` · `conversationsReport` · `sampleQc` · `saveQcMark` · `listQcSampled` · `listKb` · `saveKb` · `getCsatReport` · `listCsatEntries` · `listQueue` · `claimConversation` · `releaseConversation` · `sendAgentMessage` · `getThread` · `getMediaUrl` · `setAgentStatus` · `escalateToMerchant` · `closeConversation` · `listMyActive` · `getSessionCustomer360` · `createAgent` · `disableAgent` · `listAgents` · `setAgentWecomUserId` · `listMaterials` · `saveMaterial` · `listSuppliers` · `saveSupplier` · `adjustStock` · `listLedger` · `listPurchases` · `savePurchase` · `markOrdered` · `receivePurchase` · `cancelPurchase` · `listOutworks` · `saveOutwork` · `issueOutwork` · `receiveOutwork` · `settleOutwork` · `cancelOutwork` · `getBomSetup` · `saveBomTemplate` · `saveBomProfile` · `previewAssembly` · `runAssembly` · `listAssemblies` · `getRestockPlan` · `getFgSummary` · `getScmOverview` · `runInspect` · `getInspectStatus` · `listAnomalies` · `resolveAnomaly` · `getConfigChecklist` · `saveSecureConfig` · `savePayConfig` · `getVodUploadSignature` · `syncVodMedia` · `listAudit` · `reportClientError`

**parity 清点（2026-07-04 批18 机器核·2026-07-05 决策§26 增 1）**：旧线 38 函数全部有新线对应（14 独立函数 + 23 个用户函数收编为 app action + kfBind/dataConsent 并入 app）·adminApi 注册表 92 与旧线逐行对齐 **+ 1 净新增 `overrideRefund`**（客服端越规退款·超出旧线 parity·决策§26 显式授权·M5 整体替换不受影响：admin-only、不动数据迁移面/客户行为面）·集合面共用同一 COLLECTIONS 37 册（数据零迁移·确定性 _id 幂等并存）。云调用权限：`security.imgSecCheck`→app、`wxaSecOrder.uploadShippingInfo`→adminApi（build 产 config.json·守卫 `rw-openapi-perm-declared`）。
