# 根因账本——原项目全部经验 → 本质问题 → 系统性解法

> **框架循环**（痛→本质→不变量→守卫→反向自检→归因）的 canonical 定义在 `docs/元模式.md` §A；本账本是其 §B 的「病根清单」实例（14 类病根），不复述循环本身。覆盖率由 `check-structure` 的 `guard-coverage` 机器核（见 §四）。
> 重构的脊柱文档（2026-06-13 立，用户拍板：**全覆盖**——代码架构 + 运维资产档案 + 验收方法论 + 文档体系规则）。
> 方法：通读生产仓全部问题史（调试日志 A–K、技术债 #1–#15、四份审核报告、工作日志教训），聚类出**反复咬人的本质问题**，每类配一个让它「从此绝迹」的系统性解法；批次跟着病根走，批次表见 `现状与路线.md`（第一次重构 v5 批次表归 `archive/`）。
> 证据引用缩写：日志X=生产仓 `docs/调试日志.md` 条目；债#N=`待办与债.md`；定向/优雅性=两份 202606 审核报告；K-①…⑥=日志 K 批次A 六类修复。

## 一、14 类病根（病史 → 本质 → 根治 → 落点 → 绝迹证明）

### 1. 并发「先查后写」、一次性副作用不幂等
- **病史**：债#14（login/updateProfile/trackEvent 并发首写可重复建档，读侧取第一条不稳定）；日志 I（confirmEnter 失退货权块在幂等守卫外，重复调用多扣一笔）；K-③（退款审批无原子抢占曾可双退款）；K-④（订单号 doc.set 撞号会覆盖旧单）；定向 D-1（submitReview count 预检 TOCTOU，修成确定性 `_id=orderId__productId` 成为孤例正确范例）。
- **本质**：把「检查」与「写入」当两步做，并发窗口必然存在；一次性副作用没有和「首次」原子绑定。
- **根治**：确定性 `_id` 通例化（kit `ids.js` 规范：`${OPENID}__${courseId}` 等，撞 id 即并发方已写，天然幂等）+ 写抢占一律条件更新（`transition()`）+ 一次性副作用绑定首次状态转移；存量数据惰性迁移（命中新 id 用之，未命中原子搬迁，任意时刻部署都安全）。
- **落点**：B1（kit ids/transition）/ B2（全函数接入 + 迁移脚本 dry-run）。
- **绝迹证明**：反向测试「并发双调用只产生一条文档」——恢复先查后建写法必红。

### 2. 状态转移散写、副作用反查状态集窄化
- **病史**：日志 H（退货权失效反查写死 `status:'paid'`，真实流程走到 shipped/done 时静默断链）；K-⑥（六处状态流转各自手写条件更新：confirmReceive/shipOrder/confirmEnter…）；订单状态机散在 6+ 函数无统一闸。
- **本质**：状态机没有单一权威表达，每处转移各自背诵规则；「正向流转窄条件」被误用于「副作用反查」。
- **根治**：kit `transition(coll, id, fromStatuses, toStatus, patch)` 携带合法流转表，钱相关转移自动留痕（stateEvents lite，单据仍是唯一账本——账本拆分已否决勿捡回）；TS 状态联合类型让写错状态名=编译失败。
- **落点**：B1（kit + shared 类型）/ B2（钱链函数全部换用）。
- **绝迹证明**：流转表 from 改宽一档对应 BAD_STATUS 测试必红；编译期拒绝非法状态名（**限调用方用 `ORDER_STATUS` 常量**；`transition()` 边界收 raw string、不挡 typo——跨集合泛型的已知折中，2026-06-13 深审收准）。

### 3. 信任边界靠每个函数自觉
- **病史**：K-①（payCallback/refundCallback 曾可被客户端伪造支付成功——全场最险）；定向 A-1（seed/init 三函数裸奔，任意登录用户可把生产商品表抹回种子）；日志「login 漏写 _openid」；18 处 openid 三行闸、4 处 admin 闸纯手抄；债#15（admin 口令 bootstrap 抢占窗口）；createOrder 支付配置缺失曾 fail-open→mock 直接 paid（2026-06-15 九流程审核 P1，config 漂移/误删即「未付变已付」进发货链）。
- **本质**：安检不是缺省而是每次手写的自觉——忘一处就是钱/权限的洞；越「不像业务」的函数（seed/回调）越容易忘。
- **根治**：kit gate fail-closed 缺省——`withOpenId`（不带身份进不来）/`withAdminGate`/`assertServerCall`（回调防伪内置：见 OPENID 即拒）/`defineNotifyCallback`（幂等+核验 fail-closed 框架；金额/订单号不符留痕——refundCallback 严格不翻状态，**payCallback 例外：钱已到账→置 paid 留 feeMismatch 痕供人工对账**，2026-06-13 深审收准）；bootstrap 须匹配部署密钥（堵 #15）。新函数用 kit 定义器才能成型，**不过闸无法写库成为结构事实**。
- **落点**：B1（kit）/ B2（28 函数全接入）。
- **绝迹证明**：闸还原（去掉 gate）对应反向测试必红；防伪闸自检（带 OPENID 调回调必拒）；createOrder 缺支付配置必拒 PAY_CONFIG_MISSING（守卫 pay-config-fail-closed，仅 env ALLOW_MOCK_PAY=1 放行 mock）。

### 4. 金额浮点靠手工纪律
- **病史**：applyRefund 退款分摊靠「分运算防浮点」的手工纪律（K 批次 PR #73）；全库价格存元、运算时各自小心。
- **本质**：金额的安全性依赖每个写代码的人记得换算，类型系统不设防。
- **根治**：shared 包 `Fen` 品牌类型（branded type），金额全链「分」整数，元只在展示层出现；云函数边界收元转分一次完成。
- **落点**：B1（shared 类型）/ B2（钱链迁移时换轨）。
- **绝迹证明**：编译期——浮点金额赋给 Fen 直接编译失败。

### 5. 样板复制即漂移
- **病史**：28 份 `cloud.init` 样板；payCallback↔refundCallback 70% 重复；种子数据 data/ 与 cloudfunctions/seed 双份手工同步（「需与 xx 保持一致」镜像注释）；adminApi `str` 助手曾 4 处重复（优雅性 A-1）；admin 防抖三件套散三处、安全审修 flush 时逐个手补正是「散落必漏」实证（定向 B-2 → 优雅性 B-1 抽 useAutosave）。
- **本质**：部署边界（每函数独立打包）曾使共享「不划算」（优雅性 A-2 旧判定），于是复制成为默认——而复制必然漂移。**A-2 翻案依据**：共享面已从「省几行」变成 闸×3+transition+留痕+分页+确定性id 的整套内核，esbuild 构建期内联使共享与独立打包不再矛盾——旧取舍的前提消失。
- **根治**：cloud-kit 共享内核（esbuild 内联+摇树，部署形态与现状同构）；种子单一来源进 shared；镜像注释清零，「保持一致」从人工义务变机器保证。
- **落点**：B1（kit）/ B2（收编样板）/ B3（种子归一）。
- **绝迹证明**：净化指标 commit 报数（init/闸/双回调归零，cloud 层净减 ≥25%）；种子生成物 diff 空；全库 grep「保持一致」类注释为零。

### 6. 演示回退双路径泄漏
- **病史**：K-⑤（api/order.js 曾把云端拒单吞进本地回退=交易动作本地伪成功——暗病）；8 个 api 文件各写各的回退形状；优雅性 A-3（createOrder↔api/order.js 订单镜像靠注释维系）；债#8（pending-pay 演示孤页滞留到 P4 才清退）。
- **本质**：「无云回退演示」与「云端真路径」并存却无统一契约，错误归类（拒单≠无云）只能靠每处 catch 自己分辨；镜像一致性靠注释。
- **根治**：回退策略收口为统一形状——**无云(null)→演示回退；云异常/拒单→上抛**，8 个 api 一个模式；镜像对照测试（parity test：本地生成器 vs 云函数同输入同输出）把镜像从注释变机器锁。
- **落点**：B3.5（新增批：miniapp 回退收口）。
- **绝迹证明**：parity 测试锁订单号格式/金额一致；「拒单被吞」反向用例必红。

### 7. 固定 limit，规模即挤出
- **病史**：债#13（订单 100/后台订单 200/后台售后 200/评价 200 固定条数，真实增长后旧数据被挤出工作台；评价汇总只算最近 200 条非全量口径）；定向 A-1 提及 listDrafts 同通道响应体上限。
- **本质**：「先写个数字够用」没有退出机制，数据增长是必然而 limit 是静默炸弹。
- **根治**：kit `paging.js` 统一分页协议（cursor/limit 契约），后端先行且兼容无参调用（部署窗口内新旧前端都不坏），再接 admin 订单/售后页与小程序 order-list 翻页。【用户拍板：纳入重构】
- **落点**：B4（随 adminApi 拆分同批接入）。
- **绝迹证明**：分页契约测试；无参调用兼容用例。

### 8. 验证样本失真——「E2E 过了」≠「真的能用」
- **病史**：日志 F（1×1 测试图过、真实 200KB 照片必拒——最小样本只验逻辑不验边界）；日志 G（凭证只验「拿到」没验「用」，直传路径从未真走通；「双保险」只兜第一步等于没兜）；日志 P（内存桩按 wx-server-sdk 形状写、真跑 node-sdk 形状不同→全假阴，桩绿≠真证）；日志 Q（一条缺 items 的脏订单让「全部订单」整页白屏——渲染热点 `.items.reduce/.map` 未防御真实数据形状，测试样本永远带 items＝样本失真）。
- **本质**：验证的置信度取决于样本与真实的距离；走到链路第一步就宣告通过。
- **根治**：验收方法论入册（样板房验收手册/CLAUDE.md）：① E2E 必须用真实尺寸样本（测试基建备真实大小 fixture）；② 凭证/签名类必须按签发约定的完整请求形状走完整链路；③ 兜底路径必须覆盖主路径全部失败点并被测试逼出。
- **落点**：B7（质量基线批）。
- **绝迹证明**：方法论清单进验收手册模板；fixture 进测试基建；订单脏数据白屏**双层守**——store 入库归一（`normalizeOrder`）+ 渲染访问点安全访问器（`data/orders.js orderItems/orderQty` 单源，列表/详情用之），`tests/orders.test.js` 两层各有直测锁（单层只测入库＝Q 复发暴露的样本盲点）。

### 9. git 外资产无正册
- **病史**：日志 J/K（八件控制台资产——连接器 wxpay_33nb7su/支付流 sywxzfapifqzf_nncvqss2/退款流 kbgzl_n8gojr3a/paynotify·refundnotify 及 script1 转发代码——只散记在工作日志，误删即支付瘫痪）；定向 C（15 个集合的库权限规则 CLI 读不到、从未入档，验收单 X 靠人肉）。
- **本质**：系统的真实形态 = git 内代码 + git 外配置，后者没有版本管理、没有重建手册，灾难恢复靠记忆。
- **根治**：`console-assets/` 正册化——八件资产副本（密钥 REDACTED）+ 工作流节点与入参表达式 + script1 代码 + **库权限规则期望表**（验收单 X 固化）+ drift-checklist + 30 分钟重建手册；变更纪律「先 repo 后控制台」。
- **落点**：B6。
- **绝迹证明**：对照控制台人工核一遍；重建手册演练（纸面）。

### 10. 工具实例状态不可信
- **病史**：日志 E（工具僵尸实例复发：build 覆盖 dist + 强杀自动化 → 握手成 RPC 不回包 → 真机推坏会话白屏）；日志「假性 component not found」（build 中间状态被工具读到）；日志 D（工具端云函数 timeout，未复现待观察）；验收 2026-06-14（`cloud://` 商品封面图在工具渲染 500 / 被当 `/components/` 本地路径，真机正常——工具云图代理抽风，又一例：cloud 图眼校只认真机）。
- **本质**：开发者工具是有状态的共享实例，build/自动化/真机调试互踩后其表象不可信——在僵实例上排查业务代码是纯浪费。
- **根治**：守则机器化能做的已做（visual-check RPC 探活守卫）；余者成文入样板房 CLAUDE.md：跑 build 前停自动化；工具怪象先 `cli quit`+重启再排查。
- **落点**：B7（守则随质量基线入册）。
- **绝迹证明**：守则进 CLAUDE.md；脚本探活守卫保留。

### 11. 文档职责渗漏
- **病史**：现状与路线.md 单条目膨胀 60+ 行（下一步 #6 即实例）；CLAUDE.md 曾 314 行后靠专项收口到 ~130；活文档/参考/archive 边界曾模糊，状态曾三处重复漂移（2026-06-10 专项治理过一轮，但靠人自觉维持）。
- **本质**：文档没有「容量与归档」规则，膨胀是默认走向；规则不机器化就会回潮。
- **根治**：文档体系规则入样板房 CLAUDE.md：现状条目 ≤8 行、溢出沉工作日志；调试日志按季卷档；正册只放「仍会被查」的内容；约定/记录/参考三类边界明示。【用户拍板：纳入全覆盖】
- **落点**：B7（与验收方法论同批）；样板房新文档（重构日志/账本/总计划）开局即守此规则。
- **绝迹证明**：规则入 CLAUDE.md；每批收尾自查条目行数。

### 12. 平台规则外部风险
- **病史**：日志 J/K 头（实物类小程序云调用支付被平台整条封死，强制工作流——非代码缺陷，整改一天）；发货信息管理入口等微信侧同步（合规项）。
- **本质**：平台规则可单方面变化且无预告，无法预防——能做的是把「与平台的接缝」收到最少点位，变化来时改动面最小。
- **根治**：`callPayFlow` 单点收口 cloudbase_module 调用（现 2 处）；接缝清单（flowId/refundFlowId/notify_url）入 console-assets 正册与 B8 runbook。
- **落点**：B1（callPayFlow）/ B6（接缝清单）。
- **绝迹证明**：全库 cloudbase_module 调用点 grep=1 处（kit 内）。

### 13. 无频控，认证/写入端点可被爆破或滥用穿透
- **病史**：上线前安全体检（2026-06-15）：管理后台 `adminApi` 是公网 HTTP 端点（通配 CORS）、口令仅 6 位下限 + sha256 快哈希、**无任何重试限制** → 可从任意地方暴力破解到完全接管后台；用户端写函数（trackEvent 无界写 / createOrder / login）同样无频控，可刷爆库 QPS / 堆垃圾数据。
- **本质**：认证与写入端点一旦暴露公网，没有「砸门」限速则口令可枚举、资源可耗尽——门锁（认证/授权/防伪/钱）再硬也挡不住无限次尝试。
- **根治**：`kit.throttle`（滚动窗口失败计数 + 达阈锁定，按 key 隔离）；adminApi 口令校验路径先过 `throttleLocked` 闸、失败 `throttleFail`、成功 `throttleReset`。守卫 `admin-login-throttled` 焊「口令路径必经频控」。
- **落点**：fix-后台登录限流（P1）+ fix-用户端写函数频控（P2：withRateLimit + `user-writes-throttled` 守卫），均 2026-06-15。
- **绝迹证明**：守卫 `admin-login-throttled`（grep adminApi=在）+ `user-writes-throttled`（trackEvent/createOrder/login/updateProfile 须包 withRateLimit）；测试 adminThrottle（5 错→第 6 次正确口令亦 429 + IP 隔离）+ userRateLimit（超限即 RATE_LIMITED + openid 隔离）。**两段（P1 认证 + P2 用户写）齐，落点闭合。**

### 14. 失败静默化——吞错 / 伪成功 / 告警不进人眼（2026-07-04 立·全日志回归分析）
- **病史**：next 日志 AA-②（FORGED_CALLBACK 只进日志无人盯→bot 静默罢工可能数日）；AC-②（sendAgentMessage 失败回 `{ok:true,errcode}`→前端当成功清输入框零提示，「发不出」演成「无声消失」）；AD（held 闸静默吞顾客明确的「找人工」；`.add` 撞 closed 旧 doc 被静默吞→老客二次求助永进不了队列）；Z 顺带（wecom token/cursor 缓存写失败 fail-soft 静默废）；fable 日志 A（`callFlow` `.catch(()=>null)` 吞平台真实报错→REFUND_TRIGGER_FAIL 无从取证——立本病根的扳机）。
- **本质**：失败点的缺省行为是「静默」——catch 默认吞、发送失败默认装 ok、告警默认只进无人盯的日志；fail-soft（降级返回语义）与「失败不可观测」被混为一谈，故障发现只能靠下次真用撞见。旧线 K-⑤/N 的「伪成功」是它在演示回退域的特例（已归 #6），本条是通例。
- **根治**：**失败必可观测**——kit `observe.alert`（`[LD_ALERT]` 结构化留痕 + webhook 触达运营）为唯一告警出口；动作类失败（接缝外呼/发送/回调验签）分支必留痕，fail-soft 只许改返回语义、不许抹可观测性；读路径 `doc.get().catch(()=>null)`（缺席=null 惯用法）不在此列；刻意静默须行内注释声明为什么。
- **落点**：M2 批9（callFlow 两失败分支落 alert·`59cd5b4`）；M3 批8 坐席「发送成功才清框」（承 AC）；旧线钱链告警接入（债#23·moneychain-alert-wired）。**靠人面**：外部接缝「配过一次≠一直通」的活体探活（AA-①）未排期，见日志 AA 待办。
- **绝迹证明**：守卫 `rw-flow-observable`（反向测试：失败分支不留痕即红）+ `moneychain-alert-wired`（payCallback/refundCallback 静默语义失败须经 alert——原寄养在 #13 下，自本条起归位 #14）。

## 二、已根治留档（经验已沉淀为机制，重构只需保持纪律）

- **边界与归位类**（债#1–#7 全清零，2026-06-10）：组件直取数/页面组件错位/store 跨域耦合/样式双写/storage 散落——根治机制=CLAUDE.md §8 尺子 + 约定检查闸 + persist/sanitize 契约 + useTimers/STORAGE_KEYS 收口。**教训沉淀**：useTimers/useAutosave 证明「散落副作用抽组合式收口」范式有效——cloud-kit 是同一哲学在云函数层的放大。
- **沉浸页避让**（日志 A/B）：systemBar 动态计算 + 「动浮层不动底层」守则 + visual-check 自动化眼校。
- **持久化脏数据**（日志「购物车空白行」「qty NaN」）：sanitize 数据契约 + 测试锁定——「不信任 storage」已是缺省。
- **订单贯通**（日志 C）：教训「贯通型问题临近云改造里程碑就直接在云改造里解决，不做临时方案再推翻」——B 批次排序沿用此原则。
- **质量闸体系**（2026-06-12 三批）：pre-commit/CI/约定脚本/编辑 hook/部署闸——样板房全数继承，B5 部署闸升级为 manifest+DEPLOY_ALLOWED。

## 三、院外记账（明确不入本重构，防丢失）

| 项 | 去向 | 依据 |
|---|---|---|
| 商品下架/停售能力（债#12） | 生产线按需做（新功能非病根） | 用户拍板 2026-06-13 |
| 埋点频控/TTL（债#9，定向 A-3） | 记债，规模起来再做（配额风险非正确性） | 用户拍板 2026-06-13 |
| 评价汇总增量聚合（债#13 后半） | 缓——分页解决工作台挤出后，全量口径问题规模到了再治 | 同上切分 |
| 小程序硬编码颜色 token 化（债#11，优雅性 C-1） | 备选债——非低风险（易视觉漂移），扩 token 体系时再统一 | 优雅性判定 |
| admin 三大文件拆分（债#10，优雅性 B-2） | B7 顺手项（待触碰时拆），不设硬指标 | 优雅性判定 |
| adminApi v1 鉴权边界（定向 B-3：单口令/CORS */localStorage） | 已定 v1 设计，账号体系升级时一并 | 规格 §三 |
| 默认样例地址 SAMPLE_ADDRESS | 生产线上线前占位拍板（占位清单在册） | 设计权衡 |

## 四、完整性自查（每条经验都有归属）

> **覆盖率现由机器核**：`check-structure` 的 `guard-coverage` 读本账本 §一 病根 + 守卫 `roots` + CLAUDE `[靠人:#N]` 锚，断言每条病根都有守卫或靠人豁免，缺一即红（反向自检：删某病根覆盖 → 红，已验）。下方人工对照降级为「可读复核」。

- **调试日志**：A/B→留档(沉浸页)；C→留档(贯通)；D→病根10；E→病根10；F/G→病根8；H→病根2；I→病根1；J→病根12；K-①→病根3；K-②→病根3(defineNotifyCallback)；K-③→病根1；K-④→病根1；K-⑤→病根6；K-⑥→病根2；购物车两条+login _openid→留档/病根3。
- **账本立后新案（06-14 起·各条日志自带归因，此处复核）**：N→病根6；L/M/O/P/Q/R/S/T/U/V/W/X/Y/Z/AE→病根8（多端假绿/样本失真/桩≠真族）；AB/AC-①→病根12；AA-②/AC-②/AD/fable-A(次生)→**病根14**（2026-07-04 全日志回归分析并立）。
- **技术债**：#1–#7→留档；#8→病根6 病史；#9→院外；#10→院外(B7 顺手)；#11→院外；#12→院外；#13→病根7(+评价聚合院外)；#14→病根1；#15→病根3。
- **审核报告**：定向 A-1→病根3、A-2 正向确认→kit 内置化保持、A-3→院外、B-2→病根5、B-3→院外、C→病根9、D-1→病根1 范例；优雅性 A-1/B-1→病根5、A-2→病根5 翻案、A-3→病根6、B-2→院外、C-1→院外、C-2→债#8；v0.1 与两份早期审核→债#1–#7 已沉淀留档。
- **工作日志运维坑**：工作流改完必须发布/证书序列号不带空格/tcb 一条命令多函数名只发第一个→病根9 坑册（B6）。
