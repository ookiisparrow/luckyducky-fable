# 待办与债·已关账-202607（2026-07-23 瘦身大作战退役）

> 五个整节已全额 ✅ 勾销、无残留决策点，自 `docs/待办与债.md` 退役。另附「挂起问题·退款平台侧失效」条的结案核正记录（活档保留改写后的结案条）。

## admin Shell.vue 侧栏切步绕开批量上传锁（2026-07-09·M 批 re-audit 确认越界未修→同日单点收口批已修 ✅）
- ✅ **已修**（2026-07-09·同日单点收口批·现场重新 grep 核实无残留）：修在 `rewrite/admin/src/router.ts` 全局 `router.beforeEach`（会话闸旁新增一条判断，约第89行）——`to.path !== '/login' && isUploadLocked() && to.fullPath !== from.fullPath` 时 `return false` + `window.alert` 一次性提示，一次性收口 Shell 侧栏「按步骤直达」router-link 与浏览器前进/后退两条旁路（对任何会改变当前路由的导航必过，含仅 query 变化的 update 类导航）；会话分支（`to.path!=='/login' 且无会话`/`to.path==='/login' 且已有会话`）排在其前先行判断，登录态丢失/手动登出跳 `/login` 不受上传锁影响。`Wizard.vue go()` 与 `Courses.vue onBeforeRouteLeave` 两处既有检查原样保留（纵深防御）。`npm run check` 全绿（1544 测试不变，router 行为改动走反向自检读码复核，非自动化测试）。
- **原始问题（保留存档）**：`Wizard.vue go(n)` 已前置拦截 `isUploadLocked()`（批量上传在途禁切步），但 `Shell.vue`（`src/shell/Shell.vue` 约150行）侧栏「按步骤直达」用裸 `router-link` 改 `query.step` 直接跳步，完全不经过 `go()`——同属「仅 query 变化＝路由 update 非 leave」，`Courses.vue` 的 `onBeforeRouteLeave` 不会触发，点侧栏可在批量传视频在途时直接卸载 Courses、复现孤儿化（`videoFileId` 写回不可达的树）。浏览器前进/后退按钮是同类第二条旁路。详见 `调试日志.md` 案 M。

## 退款链跟进（2026-07-05 调试案 A 修复批派生）
- ✅ **P2·退款判据徽章不绑单**（已修 2026-07-05·见 `重构日志.md` 同日 P2 批）：getRefundDetail 加 `lineRefundable`（绑本单订单行·与 approveRefund 同口径）+ 纯函数 `refundVerdict` + Refunds.vue 判据 banner 以本单可退性为准、"进课"降为课程级提示——买家经别单/别码进过课不再误显"会拦"。
- ✅ **P2·admin 同意后真错被"加载中"吞**（已修 2026-07-05·同批）：doApprove/doReject 失败不关抽屉、不刷新，真实原因留抽屉红条 `decideErr`；成功走持久 `actionMsg`（reload 不清）。
- ~~P3·索引建议（待）：afterSales.outRefundNo~~ **✅ 已入正册（勾销 2026-07-19·Phase3 批J 复核）**：`console-assets/03-复合索引期望表.md:25` 已收录（控制台建索引照正册执行=人工动作·正册即活单源，本条不再双记）。

## cloud bug sweep Round1 批4 派生（2026-07-09·详见 `调试日志.md` 案 J）
- ✅ **P2·Orders.vue/Refunds.vue 的 tab 计数徽标已接 partial**（已修 2026-07-09·bug sweep Round2 item14·现场核实 `countsPartial.value = c.ok ? !!(c as any).partial : true`，两页均已接线）：两页各加 `countsPartial` ref，`reload()` 内消费 `orderCounts()`/`refundCounts()` 回包的 `partial` 字段（请求本身失败也按不可信处理，同 Dashboard.vue `todoPartial` 风格）；tab 角标 partial 时显警示态 `?`（红底·title 提示「计数不可用」），不再显示假 0。

## 战役 II 遗留（2026-07-10·bug 清除战役 II 收账）
- ✅ 全部收口（2026-07-10 批N）：Products closeEditor 失败提示补 openEditGen 代际锚 ／ Anomalies 页补「共 N 条」总数提示（ops.ts listAnomalies 同 filter 补 `.count()`） ／ scmAssembly.ts/agents.ts 单文档补偿回滚失败接 notifyAlert 留痕（病根14口径） ／ home.ts hidden 病根归属已拍板：并根因#5/#8 家族、不立新病根。详见 `重构日志.md` 批N 短账 / `调试日志.md` 案 P。

## 品牌字体接入 rewrite/mp（2026-07-12 用户拍板·业务预留）
- ✅ **已核销（两步走完）**：① 接入已由批4 字体落地（`rewrite/mp/utils/brandFont.ts` + app.wxss 全局 page 应用·重构日志 7be8a8c 条）——本条「零引用」描述其后即 stale；② 2026-07-16 字体分层批再造加载效率（三层子集 1.9MB→首屏 220KB + USER_DATA_PATH 持久缓存 + 弱网重试·守卫 `rw-mp-font-tier-subset-covers`），部署与真机走查项见该批记账（重构日志）。
