# 验收手册（非技术版）

> 2026-06-10 立。给不读代码的验收人用：**验收看现象，不看实现**。
> 你只需要回答「它的行为对不对」——这恰好只有懂业务的人能判断。
> 约定：每个里程碑收尾，AI 交付一份「验收单」（操作步骤 + 预期现象），照单操作，全过才算完成。

## 一、三个观察窗口（都不需要懂代码）

1. **云开发控制台**（后端的"货架"）
   - 入口：微信开发者工具顶部「云开发」按钮 → 控制台。
   - 「数据库」= 所有业务数据的表格视图。点开集合（如 `products`）就能看到每条记录的字段和值。
   - 「云函数」→ 某个函数 → 「日志」= 每次操作的流水记录（什么时候被调用、成功还是失败）。
   - 「统计」= 调用量、存储量等运行指标。
2. **小程序本体**（行为是最终事实）
   - 开发者工具模拟器或手机真机（工具栏「预览」扫码）。
   - 最硬的验证手法：**后台改一下 → 前台变没变 → 改回来**（见下方验收单示例）。
3. **项目文档**（中文，无代码）
   - `工作日志.md`：每天做了什么。`项目现状.md`：到哪了、下一步。`关键决策记录.md`：为什么这样做。
   - 文档说的和你看到的现象应该一致；不一致就是问题，直接质问。

## 二、黄金路径（核心业务流程清单）

每个大里程碑收尾后，这条线要能从头走通（现阶段到 4 为止，后续逐步延长）：

1. 打开小程序 → 首页商品正常显示（价格、标签）。
2. 进商品详情 → 加购 → 购物车 → 结算 → 支付成功页。
3. 视频教程：欢迎页 → 课程目录（章节/课时/试看标记）→ 播放页（分段进度条、段末自动停、求助面板）。
4. 「我」页：地址簿增删改、资料页、订单列表（角标与订单状态对得上）。
5. （P3 后）扫激活码 → 课程欢迎页 → 确认 → 我的课程出现该课。
6. （P4 后）真实支付 → 订单状态流转 → 条目级退货。

## 三、已完成部分的验收单（现在就可以做）

**A. 商品数据在云端**
1. 开发者工具 → 云开发 → 数据库 → `products`：应有 5 条记录，价格与小程序首页一致。
2. （可选实验）把某商品 `price` 改个数 → 小程序杀掉重开 → 首页该商品价格变了 → **改回来**。

**B. 课程数据在云端（三层结构）**
1. 数据库 → `courses`：应有 1 条 `course-duck`，点开能看到 chapters（4 章）→ lessons（每章 4 课）→ segments（每课 5 段）。
2. （可选实验）把课程 `title` 改一个字 → 小程序重开进「课程目录」→ 标题变了 → **改回来**。

**C. 静默登录在工作**
1. 数据库 → `users`：应有你真机登录产生的记录（一串 o 开头的 openid）。

**D. 云函数在被调用**
1. 云开发 → 云函数 → `getCourses` → 日志：每次打开课程目录页应新增一条成功记录。

**F. 二维码激活闭环（P3，2026-06-10 上线）**
> 备好**未用过**的激活码（让 AI 生成：管理通道 `genQrcodes`。2026-06-11 新批次 3 个未用码：`LD2M67QXDDR6` / `LDT2MHJRXY59` / `LDMXHRWULQ7B`；早先示例 `LDW39AS59G6T` 若已用过换新码即可）。
> **第二个微信号是干什么的**（对应第 7 步）：一个激活码只能绑定一个微信账号，需要另一个账号扮演「别人」，验证别人用你已用过的码会被拒绝。两个号都需在小程序后台加为**体验成员**，用体验版测试。
> **测试不需要真印二维码**：开发者工具用编译模式、真机用体验版「启动参数」填 `code=<码>`，效果等同扫码进入；等域名规则配好后（决策 §13），把测试网址用任意在线二维码工具转成图，即可测「真扫码」。
1. 开发者工具 → 工具栏「编译」按钮旁的下拉 → 「添加编译模式」：启动页面填 `pages/welcome/index`，启动参数填 `code=LDW39AS59G6T` → 确定（这一步是在模拟「扫包装内二维码」）。
2. 应看到「课程已激活」引导页（显示课程名）→ 下一页 → **确认页明示「确认开始观看后，这件商品将不再支持退货」** → 点「确认开始观看」→ 自动进入课程目录，章节全部可见。
3. 控制台 → 数据库 → `qrcodes`：该码 `status` 变为 `activated`、`activatedBy` 是你的 openid；`activations` 多了一条记录且 `enteredAt` 有时间戳。
4. **重复进入**同一编译模式（重扫同码）：应直接显示「欢迎回来 · 继续学习」，不再出现退货权文案。
5. **退货权联动**（若该账号下过含该商品的订单）：`orders` 里对应条目的 `refundable` 应变为 `false`。
6. 换一个**编造的码**（如 `code=LDFAKE123456`）：应提示「激活码无效」。
7. （需第二个微信号，真机验收做）他人扫已用码 → 应提示「已被其他账号使用」。
8. 反向验证锁态：用一个**没激活过的微信号**（或清掉 activations 记录）打开课程目录 → 应看到「课程需扫码激活」锁卡，播放页会被弹回目录。

**E. 订单在云端（下单链路贯通，2026-06-10 上线）**
1. 小程序（真机预览或开发者工具）：任选商品 → 加购 → 去结算 → 提交订单。
2. 记下支付成功页的「订单编号」；点「查看订单」→ 详情页的编号、金额、商品、收货地址应与刚才**完全一致**（同一笔数据，支付方式显示「微信支付（模拟）」属正常——真实支付在 P4）。
3. 开发者工具 → 云开发 → 数据库 → `orders`：应出现一条新记录，`id` 等于刚才的订单编号，`amount` 与页面金额一致，`items` 里是你买的商品和数量。
4. （安全验证）记录里每件商品的 `price` 应等于 `products` 集合里的价格——价格由云端现算，前端即使被篡改也改不了应付金额。

**G. 资料上云（头像 / 昵称，2026-06-11 完成开发）** ✅ 2026-06-11 用户验收通过
> 前置：云函数 `updateProfile` 已部署 ✓（2026-06-11）。
1. 小程序「我」页 → 点「编辑」：资料页应只有 头像 / 昵称 / 个性签名 三项，**不再有手机号**（已按规格放弃）；「我」页头部也不再显示手机号行。
2. 点头像：应弹出微信自带的「用微信头像 / 从相册选 / 拍照」选项（不是直接进相册）。
3. 点昵称输入框：键盘上方应出现「使用微信昵称」一键填入。
4. 改好三项 → 保存 → 提示「资料已保存」→ 自动返回「我」页，头部头像昵称签名即时更新。
5. **杀掉小程序重开**：「我」页仍显示刚才的头像昵称（资料在云端，不再回到样例「蹦蹦跳跳的小鸭」）。换台设备（或清缓存后）用同一微信进入，资料应跟着账号走。
6. 开发者工具 → 云开发 → 数据库 → `users`：本人记录的 `nickname` / `bio` 是刚填的内容，`avatar` 是 `cloud://` 开头的一串（云存储文件 ID）；云开发 → 存储 → `avatars/` 目录里能看到刚上传的头像图片。
7. （安全验证）命令行 `tcb fn invoke updateProfile`：应返回 `NO_OPENID`——没有微信身份的调用一律被拒，别人改不了你的资料。✅ AI 已代跑（2026-06-11，返回 `{"ok":false,"error":"NO_OPENID"}`）。

**H. 订单列表与「我」页入口接真单（2026-06-11 上线）**
> 背景：你 2026-06-11 验收反馈「待支付/待发货/待收货里找不到刚下的单、缺全部订单页」——原因是旧入口走样例数据，本单验证接真后的行为。
> 先记住一条规则：**模拟支付阶段，新订单一律直接进「待发货」**（下单即视为已付款；「待支付」要等 P4 接真实微信支付后才会有单）。
1. 「我」页 → 我的订单：**「待发货」角标数字 = 你实际未发货的订单数**（可与数据库 `orders` 集合里 `status` 为 `paid` 的条数核对）；待支付 / 待收货当前没有订单，应**无角标**。
2. 点「待发货」→ 进「我的订单」列表并停在「待发货」标签：应看到你下过的订单，时间倒序，每张卡有 下单时间 / 状态 / 商品 / 件数 / 实付金额。
3. 点「全部订单」→ 列表「全部」标签：所有订单都在。
4. 点任意一张订单卡 → 订单详情：编号、金额、商品、地址与列表一致（同一笔数据）。
5. 切「待支付」「待收货」「已完成」标签：当前应是空态，提示文案会注明模拟支付的去向（这就是之前"找不到订单"的答案，属正常）。
6. 再下一单新订单 → 回「我」页：「待发货」角标 +1；进列表，新单出现在最上面。

**I. 观看进度记住了（2026-06-11 完成开发）**
> 前置：云函数已部署 ✓（trackEvent / getMyProgress，2026-06-11，E2E 已由 AI 预验通过——`scripts/progress-verify.cjs` 写入折叠读回全对）。需要已激活课程的账号。注：AI 预验在你的账号写了一条测试进度（第 1 节第 1 段看完、停在 0:55），「我」页继续学习卡现在显示的就是它；你实际看视频后会被真实进度覆盖。
1. 进播放页，把一段看到末尾（它会自动停下弹「重复播放」）→ 返回目录：这一节应显示进度（看完全部 5 段会变成「已看完」打勾）。
2. **杀掉小程序重开** → 进目录：进度还在（记在云端，不在手机里）。
3. 「我」页「继续学习」卡：显示你最后看的那一节（章节、节名、看到的位置、百分比），点「继续观看」直接进那一节。
4. 开发者工具 → 云开发 → 数据库：`progress` 集合有你的记录（`done` 里是看完的段、`last` 是最后位置）；`events` 集合有 `segment_done` / `watch_at` 流水（这份流水以后做「哪段最多人卡住」的分析）。
5. （安全验证，可让 AI 代跑）`tcb fn invoke trackEvent`：应返回 `NO_OPENID`。
6. 没看过任何段的新账号：目录不应显示任何进度（空是事实，不造假）。

**J. 管理控制台 v1 · 流水线骨架（2026-06-11 完成开发，本机演示）**
> 前置：终端运行 `cd admin && npm install && npm run dev`，浏览器打开提示的 localhost 地址。本批为本地演示模式（数据存浏览器，云端接线在下一批）。
1. 登录页：输任意账号密码 → 进入「商品与上新」列表；左侧栏与设计稿一致（按步骤直达 1–6 + 数据看板规划中）。
2. 点「新建商品」→ 直接进入**第 1 步 · 产品图片**：上传一张封面图（紫框 + 「封面图」角标）+ 几张其余图；拖动其余图可调顺序，× 可删。
3. 「下一步 · 商品信息」：填名称和价格（边输边自动保存）；「下一步 · 商品 SKU」：添加两个规格（名称 + 价格），可编辑可删除。
4. 顶部步骤条：①②③应随填写变成绿色对勾；点任意步骤圆点可直接跳转；④⑤⑥显示「v1.5」占位说明。
5. 回「商品列表」：刚才的商品卡显示封面图、价格、规格数、六步完成度圆点（3/6）；「继续上新」会进到第一个未完成的步骤。
6. **刷新浏览器**：数据还在（自动保存生效）；左侧「按步骤直达」点 3 → 直接进入该商品的 SKU 步骤。
7. 右下角「退出登录」→ 回登录页。

**K. 控制台云端模式（2026-06-11 完成开发）**
> 前置：云函数与 HTTP 服务已部署 ✓、`admin/.env.local` 已配 ✓（2026-06-11）。
1. 登录页提示变为「云端模式」；**首次登录**输入的密码（≥6 位）自动成为管理员口令——记住它。
2. 退出再用**错误口令**登录 → 应提示「口令不正确」。
3. 走一遍验收单 J 的建商品流程（传图/填信息/SKU）→ **换一个浏览器**（或无痕窗口）用同一口令登录 → 商品和图片都在（数据在云端，不再是本浏览器）。
4. 开发者工具 → 云开发 → 数据库 → `productsDraft`：能看到商品草稿记录（cover 为 cloud:// 文件 ID）；云开发 → 存储 → `products/` 目录：能看到上传的图片。
5. （安全验证）不带口令 / 短口令 / 错口令直接调接口 → 一律被拒。✅ AI 已代跑（2026-06-11，三种非法口令均 401 拒绝）。

**L. 控制台第④步 · 视频编排（2026-06-11 完成开发）**
> 前置：`tcb fn deploy adminApi --force` 已执行。准备一两个 ≤10MB 的 mp4 短视频。
1. 控制台 → 商品工作台第 4 步：首次进入自动建课程草稿；「添加章节 → 添加课时 → 添加小段」搭出结构，名称在输入框里直接改（自动保存）。
2. 给一个小段「⬆ 传视频」：选 mp4 → 进度条走完 → 变「✓ 已传」，时长自动填上；顶部素材进度条计数 +1。
3. 拖住 ⠿ 把两个小段换顺序 → 刷新页面 → 顺序保持（草稿在云端）。
4. 勾一个小段「试看」。点「🚀 发布更新」→ 确认 → 显示「已发布 ✓」。
5. **小程序侧验证**：开发者工具重新打开小程序 → 视频课程目录应出现这门新课/新结构（已激活学员自动看到新内容——引用模型）。
6. 数据库验证：`coursesDraft` 有草稿、`courses` 出现发布后的同 id 记录；存储 `videos/` 目录有刚传的视频文件。
7. 第 4 步圆点：所有小段都传完视频后，工作台第 4 步应变绿勾。

**M. 商品上架小程序（2026-06-11 完成开发）**
> 前置：adminApi 已部署 ✓。控制台里有一个走完前三步的商品（封面图 + 名称价格 + SKU）。
1. 商品工作台顶栏 → 「🏪 上架小程序」（前三步没完成时按钮是灰的）→ 确认 → 提示「已上架 ✓」；顶栏出现「在售」徽标，商品列表该商品变「在售」。
2. **小程序验证（关键一刻）**：开发者工具重新编译小程序 → 首页商品横滑出现这款新商品——**显示你在控制台上传的真实封面图**（不再是灰占位）；价格、名称、角标与控制台一致。
3. 点进详情页：头图画廊显示封面图 + 其余图，缩略图可切换。
4. 数据库验证：`products` 集合出现该商品记录（price 为数字、cover 为 cloud:// 文件 ID、含 skus 与 courseId）。
5. 改价格再点「更新上架信息」→ 小程序杀掉重开 → 价格已更新。
6. 老商品（prod-1~5）不受影响，样例数据照常显示。

**N. 小程序橱窗 · 排序与上下架（2026-06-11 完成开发）**
> 前置：adminApi 已部署 ✓。控制台左侧新增「📱 小程序橱窗」。
1. 打开橱窗页：左侧手机画面（iPhone 17 比例）显示小程序首页的样子——人气商品横滑里是真实在售商品（含你传的封面图）。
2. **在手机画面里**把一张商品卡拖到另一个位置（紫线指示落点）→ 右侧列表顺序同步变化 → 顶部显示「已保存 ✓」。
3. **在右侧列表里**再拖一次 → 手机画面同步变。
4. 关掉某商品的开关 → 手机画面里它立刻从橱窗消失（行变灰标「不在首页橱窗」）；再打开 → 回来。
5. **小程序验证**：开发者工具重新编译 → 首页商品顺序与橱窗一致；被下架橱窗的商品不出现在首页横滑，但详情页直达仍可访问。
6. 数据库验证：`products` 集合各商品的 `sort` / `featured` 与你的排列一致。

**O. 卡片设计器与印刷包（2026-06-11 完成开发，上新六步全通）**
> 前置：adminApi 已部署 ✓。用一个已走完①–④步的商品。
1. 工作台第 5 步：预览卡随尺寸（预设/自定义 mm）实时变比例；点「取商品封面图」图案上卡；改三段文字、换底色，预览即时跟随；点「✅ 定稿并启用」→ 状态变「已定稿」，第 5 步圆点变绿。
2. 第 6 步：顶部显示「✓ 卡面已定稿——可以生成批次」；填好**网址前缀**（你的域名 + /q/，决定印刷出的二维码指向）。
3. 填数量（如 50）→「生成批次并打包下载」→ 浏览器下载 `印刷包-b-xxx.zip`。
4. 解压检查三个文件：**卡面.svg**（浏览器打开 = 你设计的卡面，中间虚线框是二维码位）、**二维码地址.csv**（Excel 打开，一行一码一网址）、**说明.txt**（给印刷厂的话）。
5. 数据库验证：`cards` 有卡面记录、`qrcodes` 多了一批 code（courseId 对应该商品配套课程）；已有批次行可「重新下载印刷包」。
6. 工作台六步圆点：①–⑥ 全绿，商品卡显示「6/6」。

**P. 详情页 SKU 全链路（2026-06-11 完成开发）**
> 前置：`tcb fn deploy createOrder --force` 已执行。用一个控制台配了 2 个规格的在售商品。
1. 小程序进该商品详情页 → 点「已选」行 → 弹出规格选择（每项带价格）→ 选第二个规格 → 价格与「已选」名称立即切换。
2. 分别用两个规格各加购一次 → 购物车出现**两条独立条目**（名称相同、规格价格不同），数量、删除互不影响。
3. 去结算 → 提交订单 → 订单详情每件商品的规格名与你选的一致、价格按规格计。
4. 数据库验证：`orders` 新记录 items 里 `spec` = 规格名、`price` = 该规格在 `products.skus` 里的价格（云端定价，改前端也篡改不了）。
5. 样例老商品（prod-1~5，无规格数据）：详情页「已选」行保持原样例显示，点它提示「敬请期待」，不报错。

**Q. 首页文案橱窗化（2026-06-11 完成开发）**
> 前置：adminApi 与 getContent 已部署 ✓。
1. 控制台 → 小程序橱窗：面板下方新增「头图文案 / 信任条 / FAQ」编辑区。
2. 改主标题（如「创造幸运」→「六月上新」）→ 左侧手机画面头图文字**立即跟随**；顶部「已保存 ✓」。
3. 小程序重新编译 → 首页大标题变成你改的字；信任条短语、FAQ 问答同理。
4. 防误清空验证：把 FAQ 全部删光 → 小程序首页 FAQ 显示**默认三条**（云端空块自动回退默认，不会让线上开天窗）。
5. 数据库验证：`content` 集合 `home` 文档与你的编辑一致。

**R. 详情页图文与套装接真（2026-06-11 完成开发）**
> 前置：adminApi 已部署 ✓。
1. 控制台 → 商品工作台第 2 步：下方新增三个编辑区——参数表（如 材质/尺寸/产地）、图文段落（小标题+正文）、套装清单（图标+物品+数量）。各填两条。
2. 上架（或更新上架信息）→ 小程序进该商品详情页：「商品详情」显示你的参数表与段落（**段落配图自动用第 1 步其余图的第 N 张**）、「套装包含」显示你的清单。
3. 三块留空的老商品：详情页照常显示通用样例，不报错不空白。
4. 数据库验证：`products` 记录含 params / detailSections / kit 字段。

**S. 数据看板（2026-06-11 完成开发，控制台收官）**
> 前置：adminApi 已部署 ✓。
1. 控制台 → 数据看板（侧边栏「规划中」标签已摘）：四张统计卡——小程序用户数、订单数与金额合计、激活码激活率、在学学员数；与数据库实际条数一致。
2. 「热点」列表 = 看完人数最多的段；「卡点」= 学员最后停留最多的段（哪段卡人，哪段就值得重讲）。
3. 「最近订单」显示最新 5 笔（商品摘要 + 时间 + 金额）。
4. 下一单 / 看完一段视频后点「↻ 刷新」→ 数字变化。

**T. 播放页按段播放真实视频（2026-06-11 完成开发，内容管线闭环）**
> 前置：控制台第④步给**某一个课时的全部小段**传好视频并「发布更新」。
1. 小程序进该课时播放页：播放的就是你传的第 1 段视频（不再是占位视频）。
2. 第 1 段播完 → 自动停下弹「重复播放」条 → 点画面继续 → 自动切到你传的第 2 段。
3. 顶部分段进度条逐段点亮；段名与【N/总段】计数随段切换。
4. 没传全视频的课时：仍按老方式播放占位视频（等分模拟分段），不报错。
5. 目录角标与「我」页继续学习卡照常记录（看完的段打勾）。

## 四、不懂代码时怎么判断质量（四个代理信号）

1. **验收单全过**：行为对，比代码"看起来对"重要得多。
2. **测试数量只增不减**：`工作日志` 里每批改动会写"test N ✓"（当前 27）。数字变小要问为什么。
3. **文档与现实一致**：`项目现状.md` 说完成的，验收单能验证；说没完成的，小程序里确实没有。
4. **问题有账本**：出过的问题在 `调试日志.md` 里有现象/根因/教训，而不是"修好了就忘"。

## 五、何时该打回 / 该追问

- 验收单有一条不过 → 打回，不接受"代码层面是对的"。
- 演示只在模拟器跑、没在真机验证过涉及真机的项 → 追问。
- 一个改动说不清"用户会看到什么变化" → 追问（说不清现象的改动要警惕）。
- 文档没更新就说完成 → 打回（按 CLAUDE.md 记录约定，工作日志 + 项目现状是收尾的一部分）。
