# 现状与路线

更新日期：2026-07-04。

## 当前阶段
`luckyducky-fable` = Lucky Ducky 的**重构线**，2026-07-03 从 `luckyducky-next`（HEAD `ff02afb`）复制而来。**M0 地基与 M1 后端重写已完成**（批1–18·测试 1033→1264·新增 rw 守卫 23 条 + oldline-frozen）：新代码全在 `rewrite/{shared,cloud,golden,spike-skyline}`，旧 `packages/` 字节级冻结为参照基线（守卫 `oldline-frozen`）。M1 产物**只 build 不部署**——14 个云函数打包形态就绪（`npm run build:rw-cloud`），部署是 M5 切换日动作；并行期线上跑的仍是旧线函数。下一步 M2 小程序原生重写（真机节点需用户配合）。

**云环境**：与 next 共用同一个 cloudbase 环境（`cloudbase-d4gcssqbv06865479`），不新建、不隔离，沿用上一次「luckyducky-miniprogram → luckyducky-next」切换验证过的路数（见 `CLAUDE.md` 身份段 / `docs/archive/切换runbook.md`）。**next 仓已冻结**（只做紧急止血，不再迭代新功能），部署权转到本仓，直到重构完成、走切换手续那天。

**分叉前历史**（next 仓截至分叉点的完整进度/债/bug 记录，已归档、可查、未删除）：
- `docs/archive/现状与路线-next基线-至20260703.md`
- `docs/archive/重构日志-next基线-至20260703.md`
- `docs/archive/调试日志-next基线-至20260703.md`
- `docs/archive/待办与债-next基线-至20260703.md`

## 重构目标（2026-07-03 定标·三轮对齐十二项拍板·全文 `关键决策记录.md §23`）
**定性=整体重写（非重构）**：深度理解业务与根本目标后重写，摆脱非根本目标的枷锁；承重柱**维持 A（电商+锁课）**，计数陪伴留探索区。新系统形态：**微信端**原生 TypeScript + glass-easel（默认 WebView、关键页按页开 Skyline，弃 uni-app，UI 允许重设计）；**后端**留微信云开发、38 云函数全部重写（支付资产/数据/域名原地，确定性 _id 数据零迁移）；**web 端**新增 Astro SSG 教程内容站+官网（GEO 语料源，成交全留小程序）；**后台** admin+agent 一并重写（栈保留 Vue3+Vite）；**切换**=线上 v0.9.4 冻结只止血、新版同 AppID 整体替换，next 仓为回滚基线。依据：Phase A 六维深度理解调研（2026-07-03，关键数字亲核）。

## 路线图（重写 M0–M5）
> 纪律：每期走 `/refactor-batch`；黄金用例（1033 条测试的行为断言抽取）做验收基准；新函数新命名空间与旧系统并行，回调点位（paynotify/refundnotify/kfCallback）切换日同名替换；坐席台（在用人工客服唯一通道）切换期新旧并行零断档。

| 期 | 干什么 | 验收信号 | 状态 |
|---|---|---|---|
| M0 地基 | 新工程骨架重组 + 元模式§A 治理落地 + 黄金用例抽取 + Skyline 真机 spike（CSS 子集/遮罩/自绘 TabBar 双引擎）+ 调研报告归档 | 骨架 check 全绿；用例成册；spike 结论记档 | ✅ 2026-07-04（批1–5·spike 真机三存疑全过·吸顶未证实不计收益） |
| M1 后端重写 | shared 契约→kit 重建（fail-closed 闸/transition/幂等/分页/接缝单点）→函数逐域重写（钱链最后）；测试库先行验证 | 后端域黄金用例全绿；测试库过钱链 | ✅ 2026-07-04（批6–18·app 25 action + adminApi 92==92 parity + 回调/定时器/客服/ops·旧 38 函数零缺口·14 函数 build 形态就绪·部署留 M5） |
| M2 小程序重写 | 23 页原生化（glass-easel）；WebView 先行、关键页开 Skyline；播放器行为照用例钉 | 全页真机走查；黄金路径体验版对新后端走通 | 🚧 页面面收齐（批1–14·19 页·真数据·支付链真单验证过半——退款平台侧挂起）·**待用户全页真机走查**（手册 `rewrite/mp/README.md`）·首发全 WebView 拍板·未搬面清点入待办 |
| M3 后台重写 | admin+agent 重写（运营动作清单零缺失）；坐席台新旧并行 | 运营九类动作+坐席接待新台演练通过 | 🚧 代码面收齐（批1–9·admin 24 页六组 IA + agent 单屏·对接已部署 adminApiV2）·**待用户演练**（手册 `rewrite/admin/README.md`+`rewrite/agent/README.md`）·低频三项待办 |
| M4 web 内容站（与 M1 并行） | Astro 站（官网+教程+Schema/Q&A+sitemap）上现有托管；搜一搜收录/品牌官方区（靠人）；内容 AI 起草用户审 | luckyducky.cn 可爬可收录 | 🚧 代码面收齐（批1–2·10 页·教程 5 篇互链+四类 JSON-LD+OG）·**待用户审稿 5 篇**（全部 reviewed:false）·审毕部署静态托管收口 |
| M5 切换 | **runbook 已成文 `rewrite/M5-切换runbook.md`**（批0·前置闸×8/切换日四阶段带验证回滚点/48h 守望/清退转正·守卫 `rw-m5-runbook-synced` 焊函数清单同步）；待前置闸全过定切换日（闸1 合规补批是我这边唯一剩余代码批） | 线上真单黄金路径过；48h 无 P0 | 🚧 批0 runbook 成文 |
