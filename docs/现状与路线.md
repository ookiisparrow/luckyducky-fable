# 现状与路线

更新日期：2026-07-23。

## 现状（三行）

- 本仓＝生产源：云环境唯一版本＝`rewrite/` 重写线（2026-07-09 切换转正）；重写 M0–M5 全部完成，历次 bug 清除战役全部闭环。
- 技术面全绿（`npm run check`）；距上线只差下方待办，主要是微信后台人工动作与真机验证。
- 过程史全部在 git 史料层（`docs/史料索引.md` 为钥匙）；批次账看 `docs/重构日志.md`；重写定标全文见 `docs/关键决策记录.md` §23。

## 待办（未完成才在此·完成即删）

1. mp 上传/提审/发布（微信后台·人工）。
2. 发布后真机走查（手册：`rewrite/mp/README.md`）。
3. admin/agent 演练 + 内容站审稿（按需推进）。
4. `docs/人工配置清单.md` 过一遍（🔴3 项优先）。
5. VOD 转码管线控制台配置 + 真实视频全链验证（决策§31·照 `console-assets/` 04 册五步·前置 secureConfig 集合锁）。
6. 优惠券商家券灰度状态待用户核实后排实现批（`docs/优惠券对接微信小店-方案.md`）。
7. 盲区体检拍板清单 + 云函数拓扑收编提案（17→14）——单源 `docs/待办与债.md`。
