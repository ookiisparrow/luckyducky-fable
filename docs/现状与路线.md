# 现状与路线

更新日期：2026-06-15（v0.9.2 + P1 加固线部署生产 + 占位补全/上量稳健性/数据散落·简洁度清债/压测台搭建·均代码侧落 main；守卫→31、测试→326）

当前事实 + 下一步 + 第二次重构路线图。历史里程碑已归 `archive/工作日志-生产线-至20260612.md` + `重构日志.md`。

## 当前阶段
**v0.9.1（重构版）已部署到生产云环境**（2026-06-14，28 云函数全量上线、替换 v0.9）。本仓 `luckyducky-next` 即生产源、云环境从本仓迭代（身份见 `CLAUDE.md`）。全栈功能在线（商品/课程/订单/真实支付/售后退款/进度/评价/发货/管理控制台），功能等价 v0.9 + 重构全链路加固（四主张 T1–T4 + round-2 体检）。距「全客户上线」剩：前端发布 + 真机验收 + 内容后补。
**第二次重构（演进·技术整合）进行中**：见下方 §路线图。

## 仍是样例或占位
- ✅ 微信登录（静默 openid + 登录半屏弹窗 + 默认昵称/随机头像）+ 商品/课程/订单已上云 + 真实支付/退款已通。协议/隐私正文已补（初稿·待法务复核）、微信隐私授权代码侧已接（待 mp 后台登记+真机，见 `待办与债.md` ㉑㉒）。
- **2026-06-15 一批占位已落代码**：R29 分享（详情转发/朋友圈）、R23 店名定名「Lucky Ducky 小棉鸭」（收口 brand.js）、R18 客服（微信客服 open-type=contact）、R17 物流轨迹（快递100 免费插件 v1.1.4·余真机+隐私登记）已做；购物车/首页商品卡进详情补全、首页加购修复。
- 优惠券 / 积分 仍样例或 Toast；图位多为灰占位；视频素材为占位 / 外链。**发货合规（待办#26 🔴上线必做·后台手动录入）**、客服接待人/隐私插件登记＝代码外靠人。

## 交接快照（2026-06-15 · 发布前 P1 加固线已部署生产）
🏷️ v0.9.2 代码里程碑后，**2026-06-15 审核驱动 P1 加固线已部署生产**（preflight 漂移修 + 支付 fail-closed + build 并发 + 6 函数上线，详 `重构日志` 顶部）。**后端现 29 云函数部署**（cleanupEvents + 支付 fail-closed/频控 LIVE）、**前端产品改动（登录/UI/返回）已合 main 未发布生产**。**控制台正册资产勿删**：连接器 wxpay_33nb7su、支付工作流 sywxzfapifqzf_nncvqss2、退款工作流 kbgzl_n8gojr3a、paynotify/refundnotify。🎉 验收单 X/Y/Z 全过。**残**：控制台锁 `rateLimit` 集合「仅管理端」+ 真机下单 smoke 验 createOrder。**4 位数/天 就绪**：见 `待办与债` §上量就绪（DB 套餐＝标准版·已查）。

**2026-06-15 续（占位补全 + 上量稳健性 + 数据散落/简洁度清债 + 压测台·均代码侧已落 main 未发布）**：守卫 **31 repoCheck + 5 fileRule**、**326 测试**、活文档 13、skills 7。① **占位补全**：R29 分享 / R23 店名「Lucky Ducky 小棉鸭」/ R18 客服（微信客服）/ R17 物流（快递100 插件 v1.1.4）+ 购物车·首页商品卡进详情 + 首页加购修复（病根#6）。② **上量稳健性 + 清债（用户拍板逐一清）**：频控 CAS 原子化#21 / 钱链告警 `[LD_ALERT]`#23代码侧 / 看板容量 count·分页#18 / 精确 GMV（aggregate·已付口径）#18续 / 集合名册#28 / 错误码册#29 / throttle 收口 bumpWindowed#27 / 支付窗口·店名后缀等单源（病根#5）#30②；新守卫 known-collections-only·known-error-codes·capacity-reads-bounded·pay-window-single-source·moneychain-alert-wired 等。③ **压测台#19**：`scripts/loadtest.mjs`（cas/capacity 直连测试库验真库 CAS/count/aggregate 地基）+ `npm run deploy:test` + 测试环境 `luckyducky-test` 已建、node-sdk 已装——**待跑**（卡 CAM 密钥 + 微信云开发↔腾讯云关联，详 待办#19）。**剩全客户上线**：前端发布 + urlCheck→true + 微信合法域名；内容后补：关键课视频/扫码域名 + 协议法务定稿。**代码外靠人**：发货合规手动录入（待办#26 🔴上线必做）、客服接待人配置、隐私插件登记、㉑㉒ 真机验、物流插件真机验。

## 运维事实
- 后端 Serverless（云函数×29 + 云数据库[标准版] + 云存储 + HTTP 服务，上海地域，CDN 自带）。
- **环境续费到期 2026-12-07**（个人版）。
- **管理控制台**（自建 admin·发货/上新/橱窗/卡片/看板）已发布云开发静态托管，固定网址 **`https://cloudbase-d4gcssqbv06865479-1440904924.tcloudbaseapp.com`**（同域名 `/q/`＝扫码落地页），登录用管理口令（sha256）。**部署**：2026-06-15 已从本仓 `build:admin` 重部署到根目录（`tcb hosting deploy`·叠加上传不清旧·根 index.html+assets 更新到当前构建；`/q/` 内容不变·`cloud-admin/`+`__auth/` 未动）——此前线上是 06-12 旧构建。再发新版同此命令（部署闸 ask 二次确认）。
- **回滚预案**：回旧仓 v0.9 基线（`luckyducky-miniprogram` `2dd5a72`），详见 `archive/切换runbook.md` §五。

## 验证缺口
- 小程序真机：顶部安全区、视频同层渲染需真机抽查。外链视频需配合法域名或关闭校验。

## 下一步
1. **全客户上线**（用户择时）：前端 `build:mp-weixin` → 上传审核发布（发布前 `manifest.urlCheck`→true + 微信后台配齐合法域名）；控制台 `build:admin` 重部署。验收 Y/Z 真机已过，库权限 X 上线前核。
2. **内容后补 + 欠债**：见 `待办与债.md`（关键课视频/扫码域名 + 发货合规手动录入#26 + 依赖升级/admin 账号 v2 等；店名/客服/分享/物流 2026-06-15 已落代码）。
   - **压测台#19 待跑（活跃线）**：本地 export CAM 密钥 → `npm run loadtest -- --env luckyducky-test-d6fgztai8647df45 --mode cas`；先排「密钥未设」(secret id error)、再查「微信云开发↔腾讯云关联」——把 throttle CAS / count / aggregate 从桩绿变真证（本会话改动唯一缺的「对真实」那条腿）。
3. **第二次重构演进**：按下方 §路线图 续推。

## 路线图（第二次重构 · 演进 · 技术整合）
> 第二次重构 live 计划，接替 `archive/重构总计划-v5.md`（第一次重构 B0–B9）。三支柱：`根因账本`（病根）+ 本节（路线）+ `系统事实`/守卫。
- **北极星 A**：需求当单一源、向下派生。**路径 C**：把元模式从「痛→守卫」长成「需求→功能→守卫」。
- **铁律**：只在安全处生成（数据/类型/接口契约），**永不重生成真机验过的运行时/UI**（根因#8）；分支做、不碰 live。
- **scope**：技术整合；产品重心（承重柱 电商 vs 制作陪伴）独立 owner track、不碰。**标尺**：让非技术所有者更易逼近专业结果。
- **四道缝**（诊断）：① 需求没进代码 ② 真相散文档 ③ 架构技术分层非业务域 ④ 自动化在守不在生。

| 阶段 | 干什么 | 状态 |
|---|---|---|
| P0 知识地基 | 立路线 + 整顿文档（归档第一次重构总计划） | ✅ |
| 文档精简 + 校准 | 搬历史进 archive（活文档瘦 40%）+ 校准架构地图 | ✅ |
| P1 需求显形 | 需求收成 `需求清单`（31 条单源·已逐条复核，抓出 R22 跑偏） | ✅ |
| 文档架构重设 | 19→13（客观→机器维护「系统事实」/主观→单源·反膨胀守卫焊死） | ✅ |
| P2 需求→守卫闭环 | `requirement-trace` 守卫 + `npm run trace` 爆炸半径（guard-coverage 泛化）| ✅ a 可追溯+爆炸半径 + b 须守卫覆盖（钱/状态/安全） |
| P3 安全处生成 spike | 订单域：从声明生成类型/接口契约/守卫，证 ROI 再扩 | ⬜ |
| P4 架构对齐（证据闸·可选）| 仅当 P1–P3 证明技术分层真挡「需求→代码」才带证据重议§17 | ⬜ |

> 纪律：每阶段先压测设计再动手、走 `/refactor-batch`；批次证据记 `重构日志.md`。

## 产品方向探索
「制作陪伴」承重柱探索（数针/记状态 vs 卖包+锁视频）见 `archive/项目思路重构-上帝视角.md`——独立 owner track、R0 已判维持（见 `需求清单.md`）。
