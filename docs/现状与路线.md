# 现状与路线

更新日期：2026-06-27。**当前态（从 git 部署记账派生·非凭 prose）**：后端 **33 云函数全部署**（`eef3cbb`·钱链漂移清零）；外审 R1-R4 **P1 9/9 全清且已部署**（`59d0f01` 等）；发货合规 #26 链路部署（`bbf86eb`·剩真单真机验）；企微告警闭环·webhook 真发验通（`eac1fa0`）；`urlCheck`→true·合法域名 / 协议㉑ / 隐私㉒ / 扫码域名⑱ 全 ✅。**真正剩 🔴（全客户上线）**：前端 v0.9.5 republish（提审发布）· 锁新集合 ACL（控制台 inventory/auditLog/feedback/rateLimit/kfState/kfIdentity）· 真机 smoke 验下单 · 内容（关键课真视频 / 商品图）。**批次史见 `重构日志.md` 顶部；上线剩余逐项见下 §上线执行 runbook。**

当前事实 + 下一步 + 第二次重构路线图。历史里程碑已归 `archive/工作日志-生产线-至20260612.md` + `重构日志.md`。

## 当前阶段
**v0.9.1（重构版）已部署到生产云环境**（2026-06-14，28 云函数全量上线、替换 v0.9）。本仓 `luckyducky-next` 即生产源、云环境从本仓迭代（身份见 `CLAUDE.md`）。全栈功能在线（商品/课程/订单/真实支付/售后退款/进度/评价/发货/管理控制台），功能等价 v0.9 + 重构全链路加固（四主张 T1–T4 + round-2 体检）。距「全客户上线」剩：前端发布 + 真机验收 + 内容后补。
**第二次重构（演进·技术整合）进行中**：见下方 §路线图。

## 仍是样例或占位
- ✅ 微信登录（静默 openid + 登录半屏弹窗 + 默认昵称/随机头像）+ 商品/课程/订单已上云 + 真实支付/退款已通。协议/隐私正文已补（初稿·待法务复核）、微信隐私授权代码侧已接（待 mp 后台登记+真机，见 `待办与债.md` ㉑㉒）。
- **2026-06-15 一批占位已落代码**：R29 分享（详情转发/朋友圈）、R23 店名定名「Lucky Ducky 小棉鸭」（收口 brand.js）、R18 客服（微信客服 open-type=contact·**in-chat 智能客服后端已落 CS 域 `kfCallback`/`kfBind`·余靠人前置见末段**）、R17 物流轨迹（快递100 免费插件 v1.1.4·余真机+隐私登记）已做；购物车/首页商品卡进详情补全、首页加购修复。
- 优惠券 / 积分 仍样例或 Toast；图位多为灰占位；视频素材为占位 / 外链。**发货合规（待办#26 🔴上线必做·后台手动录入）**、客服接待人/隐私插件登记＝代码外靠人。

## 运维事实
- 后端 Serverless（云函数×33 + 云数据库[标准版·24 集合] + 云存储 + HTTP 服务，上海地域，CDN 自带）。**2026-06-24 全量重部署生产**（manifest 33·含库存预留/操作审计/反馈/企微告警/P3 订单域 + 钱链·smoke 健康：getProducts 真数据 / createOrder withOpenId 闸过 / submitFeedback 上线）。
- **环境到期 2026-07-11**（标准版·已开自动续费；2026-06-24 经 `tcb env list` + 控制台「套餐用量」核实，旧记「2026-12-07/个人版」有误已订正）。
- **管理控制台**（自建 admin），固定网址 **`https://cloudbase-d4gcssqbv06865479-1440904924.tcloudbaseapp.com`**（同域名 `/q/`＝扫码落地页），登录用管理口令（sha256）。**2026-06-24 控制台逐屏重设计 + 激活数据链（已合 main + 已部署上线）**：① **7 屏重设计**（S8 订单发货[密集表格+服务端计数/筛选/搜索+发货抽屉]·S10 退款[决策抽屉+退货权判据]·S11 商品[三态标签+软下架显形]·S12 看板[待处理行动条+转化速览]·S13 外部后台[官方后台直达]·S17 通知[企微告警配置]·S9 批次回归上新向导第6步[激活进度+三态+查看码+筛选/分页/折叠]）；② **激活码状态数据链**（`lib.activationFor` 共享件→`getRefundDetail` 退款判据 + `getOrderDetail` 订单详情 VMlhp[进度时间线+交易单号+逐商品激活态+合规上报]）。详 `archive/重构日志-202606下.md`（06-24/25 批次）。**部署机制**：`build:admin` + `tcb hosting deploy packages/admin/dist /admin -e <env>`（**2026-06-27 reorg 后控制台在 `/admin`·根=官网·⚠️ 部署到根 `/` 会覆盖官网！**·叠加不清旧·部署后 curl 核验 根官网/`/q/`扫码/`/admin/`控制台 三路径完好·`__auth/` 未动）；adminApi 后端改动用 `tcb fn deploy adminApi` 或 `deploy-fns`（敏感函数·部署闸 ask）。线上最新：前端 `index-B0sqOmoz.js`（2026-06-25 部署·「帮助视频」管理页两级「主题→小段」编辑器[经营组]·前序侧栏重做 + 六步折叠子菜单 + admin UX 三批 + S15 看板增强/批量发货 UI）、adminApi（含 getRefundDetail/getOrderDetail + listHelpVideos/saveHelpVideos[两级]·已部署）；云函数 `getHelpVideos`（求助面板辅助视频·两级主题→小段·已部署上线）。**2026-06-25 S15 微增强 Dashboard（已合 main·待部署）**：待办行动条加「上新未完成」卡 + 「最近订单」升级为「最近动态」混合流（订单/激活/进课/退款·getDashboard.recentActivity）；**不单建工作台**（与 Dashboard 重叠 70%·§7）。S15 第 6 卡「低激活率批次」诚实裁剪待独立批次定阈值（根因#8）。**2026-06-25 批量发货 shipOrders（P1·上量瓶颈·已合 main·待部署）**：订单发货待发货 tab 行多选→批量发货面板逐单填运单号→一次发（抽 shipOne 与 shipOrder 共用核心·各自上报微信合规）；电子面单取号需快递 API·诚实延后。待部署 adminApi + build:admin。
- **回滚预案**：回旧仓 v0.9 基线（`luckyducky-miniprogram` `2dd5a72`），详见 `archive/切换runbook.md` §五。

## 验证缺口
- 小程序真机：顶部安全区、视频同层渲染需真机抽查。外链视频需配合法域名或关闭校验。

## 上线执行 runbook（全客户上线·有序·谁做/验收/债#）
> 依赖驱动排序。代码侧多已落（各有守卫）；🔴=上线必做、其余靠人/外部。明细引债#不复抄。

| # | 项 | 现状 | 谁做 | 验收信号 / 债# |
|---|---|---|---|---|
| 1 | 关键课真视频 | 占位/外链（小熊已有真视频） | 靠人·运营 | 关键课能播真视频（⑭🔴） |
| 2 | 商品图/封面 | 灰占位·可控制台点亮 | 靠人·运营 | 首页/详情显真图（⑬） |
| 3 | 发货合规 | **代码侧自动上传已落** + **①mp后台「发货信息管理」已开通✅(2026-06-26) + ②config.json openapi 权限✅(2026-06-26·build.mjs 自动产·守卫 openapi-perm-declared) + ③adminApi 已部署生产✅(2026-06-27·hash d323cf7c·先修 deploy-fns hash 盲区才识别·`bbf86eb`)** | 靠人·真机 | 剩仅 **④ 真单真机验上传成功**（根因#8·当前无待发货订单·手动录入兜底）（#26🔴） |
| 4 | 协议定稿 | 正文已补·标「初稿」 | 靠人·法务 | 法务复核签字（㉑🔴） |
| 5 | 隐私指引 | **✅ 已完成（2026-06-26 用户确认）**：代码侧已接 + 守卫护挂载可达性 + mp 后台已登记《隐私保护指引》+ 真机验授权弹窗 | ✅ 已做 | ㉒ 闭环 |
| 6 | 自有域名 | **✅ 已完成（2026-06-26 用户确认）**：`luckyducky.cn`(已备案) 接 CloudBase 静态托管——www 主站 / q 二维码落地页 / kf 客服回调，DNS 阿里云(万网·根域名无 CNAME→用 www)，HTTPS 验过；urlPrefix→`www.luckyducky.cn/q/` + 入口页扫码转发 welcome（守卫 `activation-scan-routed`）；**微信「扫普通链接二维码」规则(前缀 `https://www.luckyducky.cn/q/`)已配 + 真机扫码验过跳小程序激活** | ✅ 已做（余 `urlCheck` 随发版翻） | ⑱ 闭环。**余**：合法域名配齐（含视频临时 URL 域名·见#8 发版前置）→才翻 `urlCheck` |
| 7 | 库权限 X | 16+ 集合已锁仅管理端 | 靠人·控制台 | 上线前核 + rateLimit 集合锁（#20注） |
| 8 | 前端发布 | 改动已合 main·未发布 | 靠人·发布 | `build:mp-weixin` → `urlCheck`→true → 上传提审发布；admin `build:admin` 重部署 |
| 9 | 上线后守望 | 钱链 `[LD_ALERT]` 代码侧已打 | 靠人·控制台 + loop | 云开发原生告警 + 企微推送落地（#23续）/ 发布窗口守望 loop |
| 10 | 客服升级（可后置） | 小程序客服已通·**客服自建方案成型(2026-06-30)** | 靠人·企业微信 → 我 | 拿 corpId/url → 接 `openCustomerServiceChat`（⑨）;**自建路线图见上节「客服自建路线图」+ `archive/客服选型研究.md`** |

## 客服自建路线图（2026-06-30 确定自建·**活文档家＝ `docs/后台360工作站/`**）
> 客服需求经 10 轮爆破 + 场景审核 + 反向审核收敛(决策见 `关键决策记录.md §19`·调研史+需求场景总表见 `archive/客服选型研究.md`)。**确定自建**(性价比:特异层 SaaS 给不了 + 微信深绑 + 数据自主;承面C 那刻再重评)。**顶层设计＝组件化/模块化**(解耦/通用接口收束/板块可开关/组件化接入·见 `后台360工作站/架构规范.md`)。底座~8 成现成,90% 新建轻量拼装,唯一大块=承面C(L)。**严格分期·先只读见效·承面C 放最后按需**。
- **P0 前置(靠人·并行办·只卡承面/自动跳/查订单·不卡 B1.1)**:链②绑开放平台(360 自动跳/查订单命门) + 48002 + 课程资质核 + `send_msg` 会话窗口验。
- **执行序(外包中期·2026-06-30 校准)**:M① 只读360查询页(手动搜·天级·可立即·不待 P0·含模块框架地基·原 B0 并入) → M② 学习位置+节点诊断 → 〔中期外包线〕会话归档+RBAC+质检 → 承面C 外包后台(L·重评自建vsSaaS) → 承面A 内部VIP(可并行)+知识库/CSAT/召回 → 合规补全。押后 AI·方案外 直播。
- **完整规范/蓝图/进度(状态单源)见 `docs/后台360工作站/`**(README 进度表=状态单源·架构规范·施工蓝图)。

## 其它活跃线
- **已完成批（证据见 `重构日志`/`archive`）**：防过度工程流程（`CLAUDE.md` §7 + ADR §22 + `/anti-overengineering-check` skill）· 优化批0618 前端批（T-F2~F7 骨架屏/下拉刷新/横滑/微动效/分包·8 守卫）· 压测台#19 真证（CAS/容量/频控对真库·**真容量副得：环境对突发 DB QPS 限流·200 并发即挡/20 安全**·余索引核查见 `待办与债`#19）· T-F1 品牌字体延后（需字体 CDN）。
- **余真机项（根因#8·accept）**：弹性回弹手感[bounces 仅 iOS]、分包跳转+preloadRule、视频占位/真播放、首页 enhanced 与 backToTop 共存。
- **第二次重构演进**：按下方 §路线图 续推。

## 路线图（第二次重构 · 演进 · 技术整合）
> 第二次重构 live 计划，接替 `archive/重构总计划-v5.md`（第一次重构 B0–B9）。三支柱：`根因账本`（病根）+ 本节（路线）+ `系统事实`/守卫。
- **北极星 A**：需求当单一源、向下派生。**路径 C**：把元模式从「痛→守卫」长成「需求→功能→守卫」。
- **铁律**：只在安全处生成（数据/类型/接口契约），**永不重生成真机验过的运行时/UI**（根因#8）；分支做、不碰 live。
- **scope**：技术整合；产品重心（承重柱 电商 vs 制作陪伴）独立 owner track、不碰。**标尺**：让非技术所有者更易逼近专业结果。
- **四道缝**（诊断）：① 需求没进代码 ② 真相散文档 ③ 架构技术分层非业务域 ④ 自动化在守不在生。

| 阶段 | 干什么 | 状态 |
|---|---|---|
| P0 知识地基 | 立路线 + 整顿文档（归档第一次重构总计划） | ✅ |
| 文档精简 + 校准 | 搬历史进 archive（活文档瘦 40%）+ 校准架构地图 | ✅ |
| P1 需求显形 | 需求收成 `需求清单`（31 条单源·已逐条复核，抓出 R22 跑偏） | ✅ |
| 文档架构重设 | 19→13（客观→机器维护「系统事实」/主观→单源·反膨胀守卫焊死） | ✅ |
| P2 需求→守卫闭环 | `requirement-trace` 守卫 + `npm run trace` 爆炸半径（guard-coverage 泛化）| ✅ a 可追溯+爆炸半径 + b 须守卫覆盖（钱/状态/安全） |
| P3 安全处生成 spike | 订单域：从声明生成类型/接口契约/守卫，证 ROI 再扩 | ✅ ROI 成立·值得扩（order.spec.ts 声明单源 + gen-order-domain.mjs 生成器 + gen-order-domain-synced/order-transitions-declared 对账守卫；详 `archive/重构日志-202606下.md` P3 段） |
| P4 架构对齐（证据闸·可选）| 仅当 P1–P3 证明技术分层真挡「需求→代码」才带证据重议§17 | ⬜ |

> 纪律：每阶段先压测设计再动手、走 `/refactor-batch`；批次证据记 `重构日志.md`。

## 产品方向探索
「制作陪伴」承重柱探索（数针/记状态 vs 卖包+锁视频）见 `archive/项目思路重构-上帝视角.md`——独立 owner track、R0 已判维持（见 `需求清单.md`）。
