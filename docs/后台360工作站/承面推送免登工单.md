# 承面 C 增强（企业微信自建应用坐席台：推送 + 免登 + 移动化）· 施工工单 + 多 CC 并行

> **这是什么**：把已上线的承面 C `/agent` 坐席台（独立网关站·口令登录·桌面三栏）**升级为「装进企业微信自建应用 Lucky Ducky」**——新会话主动推送到坐席手机、OAuth 免登、手机窄屏可用。是 M⑥ 之后的运行时增强（**M⑦**），非重写：`/agent` 前端 + agentApi 后端全复用，只加接缝。并行**机制**（worktree / git-tag 自动中继 / master loop / 单源纪律）在 `并行工作流.md`，本文只讲**这批的活怎么切、每车道做什么**，不重抄机制。
> **决策来源**：2026-07-02 用户拍板「企业微信自建应用线」方案（自建页面必要[业务数据企微读不了] + 装进自建应用最大化用企微框架 + 内部走企微/外包可企微可小程序·推送彻底解决靠企微应用消息）。
> **天花板诚实标（根因#8）**：推送真达、OAuth 免登真通、移动端「顺手」——三者都只能**真机 + 真企业微信**验，机器守卫证不出「配好了 ≠ 真能用」；企业微信后台配置（部门/可见范围/应用主页/可信域名）靠人。

## 0. 背景锚（真值见 `README.md` 进度表）

- ✅ M⑥ 承面 C 整线闭环、真坐席验收通过（2026-07-02）：`/agent` 独立站接真网关 LIVE，口令登录，桌面三栏（QueueList + ConversationWindow + Customer360Panel）。
- ✅ 微信客服已在企业微信自建应用 Lucky Ducky（AgentId 1000002）内托管；app token 缓存接缝 `kit/wecom.getCachedKfToken` 在用；`send_msg` 真机验过。
- ✅ 值班在线态已有：`setAgentStatus` 写 `agentState` 集合（status ∈ AGENT_STATUS）——**推送目标名单 = agentState 里 online 的坐席**，无需新建。
- ✅ 坐席账号在 `adminConfig`（`_id=agent:<rand>`·字段 name/role/keyHash/disabled）——**绑定企微身份 = 给该文档加 `wecomUserId` 字段**，无需新集合。
- 关键红利：**本批不新增云函数、不新增集合**（推送/免登都在 kit + adminApi + kfCallback 内扩）→ cloudbaserc 函数计数、集合册两个单源登记面**无冲突**，并行面小。

## 1. 定稿决策（据此实现·别再问）

| # | 问题 | 定稿 | 理由 |
|---|---|---|---|
| 绑定机制 | 企微 userid ↔ 坐席账号怎么对上 | **超管在 `/admin` 建/编辑坐席账号时填 `wecomUserId`** | 账号本就超管建·顺手填最简；免自助「绑定码」多一轮交互（防过度工程） |
| 推送目标 | 新会话推给谁 | **广播给 `agentState` 里 status=online 的坐席**；escalated 升级推商户超管 | 值班名单已存·谁在线谁收·先到先 claim（沿用队列 claim 语义·不做自动派单） |
| 实时机制 | 要不要长连 | **沿用轮询**（`/agent` 已有 4s 队列轮询 + 2.5s 会话轮询）；推送只补「页面关着」这一格 | 云函数不适合长连 WS·轮询在页面开着时已准实时·推送解决触达而非提频 |
| 微信小程序备胎 | 现在做不做 | **本批不做**（企微线一步到位）；标 **M⑧ 待定**——仅当外包坚决不装企微 App 才做 | 小程序无推送（订阅消息一次性）·是降级备胎不是主线·别为不确定需求先建 |
| 富媒体（图片） | 顺带做不做 | **不并本批**（另立·见 §8 剩项）——本批只文字收发升级 | 拉素材+上传素材是独立工作面·别把批次撑大 |

## 2. 技术方案细化

### 2.1 推送线（企业微信应用消息）
- **kit 接缝**：`kit/wecom.ts` 加 `sendAppMessage(token, { agentid, touser: string[], textcard })`——调企微 `message/send`，token 复用 `getCachedKfToken`（同自建应用密钥·app 消息与微信客服共用应用 token）。**单接缝**：全库应用消息只此一处出（守卫 `app-message-single-seam`·类比 `bot-push-single-seam`）。
- **挂钩点**：`kfCallback` 转人工分支 `enqueueSession`（现 upsert csSession pending）后，查 `agentState` status=online 的坐席 → 取其 `wecomUserId`（缺则跳）→ 推 textcard「新会话待接｜顾客摘要｜[点击接待]」，卡片 url = `www.luckyducky.cn/agent/?session=<sessionId>`。
- **fail-soft 铁律**：推送失败**绝不反噬入队**（try/catch 吞·同 `archive` 铁律）——推送挂了不能致转人工丢单。
- **escalate**：`escalateToMerchant`（active→escalated）时推商户超管（其 wecomUserId）。

### 2.2 免登线（企业微信 OAuth）
- **agentApi 新 action**：`loginByWecomCode`——收 OAuth `code` → 企微 `auth/getuserinfo` 换 `userid` → 在 adminConfig 查 `wecomUserId===userid` 且 `disabled!==true` 的坐席账号 → 命中即按其 role/caps 签发 session（同现口令登录出口）。**fail-closed**：无 code / 换不到 userid / 无匹配账号 / 账号停用 → 一律拒、不签发（守卫 `wecom-login-gated`）。
- **前端 `/agent` 登录三态**（`pages/Login.vue` + 入口判定）：① 在企业微信内打开 → 走 OAuth 静默授权（snsapi_base）→ `loginByWecomCode` 免登；② 在微信内打开（备胎壳·M⑧ 占位）→ 提示或 wx.login 分支；③ 其他浏览器 → 保留现有口令登录（超管仍可口令进）。
- **超管侧**：超管也可填自己的 wecomUserId → 企微内免登进 `/agent`（复用同 action，按超管 caps 签发）。

### 2.3 移动端适配（前端·工作量最大）
- `/agent` 桌面三栏 → 手机窄屏：队列/会话/360 三区改**单栏切换或抽屉**（会话为主视图·队列抽屉进出·360 侧滑）。
- 消费**会话直达路由** `?session=<id>`：推送卡片点入直接打开该会话（若未 claim 先走 claim 流）。
- QuickReplies / Customer360Panel / AgentStatusToggle 移动布局校准。
- 纯前端、对 mock 可全程独立开发（`VITE_AGENT_MOCK=1`）。

### 2.4 靠人配置清单（用户·企业微信管理后台 work.weixin.qq.com）
1. 建部门「外包客服」→ 拉外包成员（填手机号邀请·外包装企业微信 App 接受）；
2. 通讯录可见范围限制（外包只见本部门·看不到全公司）；
3. 自建应用 Lucky Ducky「可见范围」= 外包客服 + 内部客服部门；
4. 应用主页 URL = `https://www.luckyducky.cn/agent/`；OAuth 可信域名 = `luckyducky.cn`（已备案·无需新购/新备案）；
5. **微信客服「接待人员」不加外包**（他们只经 /agent 接待·不碰原生接待台·免 95018 互斥）；
6. 超管在 `/admin` 坐席账号里填每个坐席的企微 `wecomUserId`；
7. 坐席日常把自己在 `/agent` 置「在线」（agentState online）才进推送名单；
8. **配云函数 env**（车道 A 推送需要）：`WXKF_AGENTID`=自建应用 Lucky Ducky 的 AgentId（1000002）；可选 `WXKF_AGENT_URL`=坐席台地址（默认 `https://www.luckyducky.cn/agent/`·推送卡片点入）——配在 kfCallback + adminApi（推送发起方）。

## 3. 守卫清单（先红后绿·反向自检）
| 守卫 | roots | 焊什么 |
|---|---|---|
| `app-message-single-seam` ✅ | #12 | 应用消息（message/send）只经 kit/wecom.ts 一处出（平台接缝单点）——**已落·车道 A** |
| `wecom-login-gated` ✅ | #3 | `loginByWecomCode` fail-closed：无 code/换不到 userid/未绑账号/停用/无令牌拒·不签发；checkKey 令牌过期/停用拒——**已落·车道 B** |
| `enqueue-push-fail-soft` ✅ | #8 | 推送失败不反噬 enqueueSession（双层 fail-soft·转人工不丢单）行为测试锁——**已落·车道 A** |
- 无新集合 → 不动 `collection-count-synced`；无新云函数 → 不动 `function-count-synced`；`loginByWecomCode` 入 adminApi ACTIONS + 系统事实接口册；新守卫更新 `guard-count-synced` 计数。

## 4. 阶段0 地基（master 串行·扇出前必完成）
> **纪律修正（2026-07-02 施工时）**：原设想「空实现登记 + 守卫空壳」与本仓「别建 dead config / 别摆设守卫」纪律（CLAUDE §7）冲突——`loginByWecomCode`/`sendAppMessage` 应和真实实现+调用者一起在车道里落，现在建空壳=dead code。故地基**收缩为唯一该现在落的真东西**：`wecomUserId` 字段全链（A 推送取它、B 免登查它的共同前置，先落避免两车道都改 agents.ts 冲突）。其余接口点用**本工单的 I/O 描述**当契约（§2.1/§2.2·非代码空壳），车道各自照实现、master 整合对齐。
> **✅ 已落（`<本批>`）**：坐席账号 `wecomUserId` 字段全链——`agents.ts` create 支持写 + 新 `setAgentWecomUserId`（回填/改绑/解绑·作用于外包或超管 auth）+ list 回显 + **唯一性校验**（免登不歧义·两账号不撞同一 userid）；admin `Agents.vue` 建号表单加可选 userid 输入 + 列表「企微免登」列 + 每行 promptDialog 编辑；ACTIONS 注册 + 系统事实接口册。**未加结构守卫**（诚实：字段无需焊的不变量·唯一性由写侧+测试锁·fail-closed 免登闸随车道 B 落）。测试 +4（写入/回显/回填/唯一性/超管 auth）·反向自检（关唯一性→红→还原）过。
- **会话直达路由约定** `/agent/?session=<id>`（车道 A 推送写它、车道 C 前端消费它）——URL 约定·无需代码，车道各自实现时遵此。

## 5. 车道划分（并行·master + 3 车道，峰值 4 树）
| 车道 | 做什么 | 性质 | 主要文件 |
|---|---|---|---|
| 🏗️ 地基(master) | ✅**已落**：`wecomUserId` 字段全链（§4）·扇出面已开 | 串行·先 | agents.ts / admin cloud.js / Agents.vue / ACTIONS / 系统事实 |
| **A 推送线** | ✅**已落**：kit `sendAppMessage`（原始接缝·message/send）+ `sendAgentCard`（fail-soft 编排·取缓存令牌+agentid）+ `AGENT_DESK_URL` 单源；enqueueSession 真正新入队（首建/closed 重开）→ notifyOnlineAgents 推在线坐席（agentState online→adminConfig wecomUserId）；escalateToMerchant 推商户超管（auth.wecomUserId）。守卫 `app-message-single-seam`（message/send 只在 wecom.ts）+ `enqueue-push-fail-soft`（双层 fail-soft·测试锁）·反向自检各过。**剩靠人**：配 env `WXKF_AGENTID`（自建应用 AgentId 1000002）+ 可选 `WXKF_AGENT_URL`（默认 www.luckyducky.cn/agent/）·真机验推送到手机。 | 后端·自包含 | kit/wecom.ts · cs/kfCallback/dispatch · agentDesk.escalate |
| **B 免登线** | ✅**已落**：`loginByWecomCode`（新 actions/wecomLogin.ts·pre-auth 特殊分发·同 login 受频控·OAuth code→userid→查 wecomUserId 账号→签发服务端 session 令牌[存 sha·12h]·fail-closed）+ checkKey 加 `resolveWecomSession` 令牌分支（口令未命中 fallback·过期/停用拒）+ 前端 Login.vue 三态（企微内静默 OAuth / ?code 回跳换令牌 / 口令兜底）+ agentApi.loginByWecomCode。守卫 `wecom-login-gated`（fail-closed 测试锁·反向自检双向过）。**剩靠人**：agent .env.production 填 `VITE_WXWORK_CORPID`/`VITE_WXWORK_AGENTID`（非密钥）+ 企微应用主页 URL/OAuth 可信域名 + 真机验免登。 | 后端+前端接缝 | wecomLogin.ts + lib.checkKey + agentApi.js + Login.vue |
| **C 移动化** | /agent 三栏→窄屏响应式/抽屉 + `?session` 直达消费 + 组件移动布局 | 纯前端·对 mock·最独立·量最大 | packages/agent/src/components + pages/Desk.vue |

> 冲突面：A/B 都碰 `kit/wecom.ts`（A 加 sendAppMessage·B 加 OAuth helper·同文件不同函数→master 整合取并集）；A/B 都碰 agentDesk/adminApi 但 A 动 escalate、B 动 login+ACTIONS→热点是 ACTIONS 注册（归 B）。C 纯前端 packages/agent 与 A/B 几乎零重叠。**诚实：并行提速有限**（后端 A/B 共文件），真正独立的是 C；规模不大，也可 master + 2 车道（后端合一 ‖ 前端 C）。

## 6. 节奏 + worktree + 中继（机制详见 `并行工作流.md`）
```
阶段0 [master 串行] §4 地基五件（接口点 + 守卫壳）→ push main
   ↓ 定义了三车道遵的接口 + 守卫（没好不开并行）
阶段1 [并行] A 推送 ‖ B 免登 ‖ C 移动化（各 worktree·端到端·各自 npm run check 绿 → 打就绪 tag）
   ↕ 自动中继：master /loop 轮询就绪 tag
阶段2 [master 自动] 见 tag → merge → 全局 check → 反向自检 → 记账 → push main
   ↓ 代码落地
阶段3 [靠人] 企业微信后台配置（§2.4）+ 超管填 wecomUserId
阶段4 [真机验] 见 §8——推送到手机 / 免登 / 移动接待（根因#8）
```
- worktree 布局同 `并行工作流.md` §3：master=`luckyducky-next`(main)·车道各一棵树(首次 `npm install`)·分支 `feat/csx-push`/`feat/csx-login`/`feat/csx-mobile`。
- 部署授权见 memory `cc-deploy-authorized`（本仓 CC 可 `DEPLOY_ALLOWED=1 deploy-fns`·纪律：check→preflight→dry-run→deploy→smoke→记账）；本批部署 = adminApi + kfCallback + build:agent 静态托管。

## 7. 冲突热点（单源登记面·串行化纪律）
| 登记面 | 本批是否碰 | 归属 |
|---|---|---|
| adminApi ACTIONS 注册 | ✅ loginByWecomCode | 车道 B |
| 系统事实接口册 | ✅ loginByWecomCode 行 | 车道 B（A/C 无新接口） |
| 守卫计数 guard-count-synced | ✅ +3 守卫 | master 整合时统一 |
| 集合册 collection-count | ❌ 无新集合（wecomUserId 是 adminConfig 加字段） | — |
| cloudbaserc 函数计数 | ❌ 无新云函数 | — |

## 8. 验收（真机·根因#8）+ 剩项
**真机验收单**：
- 推送：测试顾客发消息→点「找人工」→值班坐席手机企业微信收到「新会话待接」应用消息→点卡片→免登直达该会话→认领回复→顾客收到。
- 免登：企业微信内打开应用→OAuth 静默→不输口令进 `/agent`→身份正确（超管见全队列·外包只见自己 claim）。
- 移动：手机窄屏能看队列/认领/回复/看 360/升级/结束。
- 隔离：外包账号在企业微信只见 Lucky Ducky 一个应用·看不到通讯录全员·碰不到原生接待台。

**剩项（不并本批）**：① 富媒体（顾客发图坐席看图/回图·拉+传素材）；② M⑧ 微信小程序备胎（外包拒装企微才做）；③ 承面 A 内部 VIP（企微侧栏·M③·独立线）。
