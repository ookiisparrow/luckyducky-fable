# 待办与债（技术债 backlog + 上线前占位）

> 来源:2026-06-07 两轮审核(① 代码可维护性 ② 逻辑拓扑结构)+ v0.1 全量审核(原报告已归档至 `archive/审核报告-v0.1.md`,其 P2/P3 结论与本清单一一对应,无遗漏新增)。
> 这些**不是功能 bug**(主流程/测试/构建都稳),是**长期可维护性**的改进项。
> 分工:功能 bug → `调试日志.md`;技术债/重构 → 本文件;新系统设计 → `archive/设计规格-课程电商系统.md`。
>
> ⚠️ **关键判断**:项目即将接**微信云开发**(见设计规格)。**部分重构与云开发数据层改造重叠** ——
> 标 `[结合云开发]` 的建议届时一起做(现在改完会被重构覆盖,白做);标 `[独立]` 的可随时做。

## 总体结论
已达 Demo 可用标准(主流程/测试/构建稳)。主要问题不是跑不通,而是**边界不够清晰**:部分组件
承担数据入口、部分页面专属逻辑放在全局目录、store 与 demo 数据跨域耦合。

## 已做好的(保持,勿回退)
- `QuantityStepper`/`PriceSummary`/`AddressBlock`/`OrderItem` 抽象方向正确。
- `cart`/`persist`/`validate` 有测试覆盖。
- checkout 空草稿守卫、购物车精确扣减、脏数据清洗均已改善。
- `npm run test`、lint、H5、小程序构建均通过。

## 优先级 backlog
1. ~~**[真 bug · 独立] `sanitizeCart` 小数 qty**~~ ✅ v0.1 已修(qty 要求整数且 ≥ 1 + 补测试,见 v0.1 审核修复):`qty: 0.5` 能通过校验、再被 `Math.floor` 成 `0`。
2. ~~**[独立] `HelpSheet` 归位**~~ ✅ 2026-06-10 已完成:移到 `src/pages/player/components/HelpSheet/`,拆为 壳(index)+ 5 面板 + data.js;副作用(toast)上提到壳;`fmt`/`parseDur` 收进 `utils/format.js`(补测试)。
3. ~~**[结合云开发] `SAMPLE_ADDRESS` 解耦**~~ ✅ 2026-06-10 已完成(订单上云前置,PR #15):样例地址迁出 `data/checkout.js` → 新建 `data/address.js`,用户域不再依赖结算页数据;地址上云后这份样例只作回退或删除。
4. ~~**[结合云开发] 首页 section 组件改 props 输入**~~ ✅ 2026-06-10 已完成(P2 捆绑,PR #13):`TrustStrip`/`Reassurance`/`Reviews`/`FAQ` 改纯展示组件收 props,数据首页收口;`FeatureProducts` 已在商品上云时接 store。将来这些数据上云只改页面取数处。
5. ~~**[结合云开发] 拆 `CheckoutAddonList` / `CheckoutSubmitDock`**~~ ✅ 2026-06-10 已完成(订单上云前置,PR #16):拆为页内组件(`pages/checkout/components/`),组件纯展示发事件、状态与提交守卫留页面;`.co-dock` 四件套取值照 #6 模式收进 `co-mixins.scss` 两侧 @include。接云订单时提交态在 `CheckoutSubmitDock` 扩展。
6. ~~**[独立] 统一样式策略**~~ ✅ 2026-06-10 已完成:排查确认双写仅 4 个 class(`co-card`/`co-price`/`co-item-spec`/`co-addr-tag`,其中 co-addr-tag 已发生取值漂移)。**未走"全局化"**(co.scss 本就是被各页 scoped @import 的设计,scoped 边界是铁律),改为取值收进 `styles/co-mixins.scss` 单一来源,co.scss 与组件两侧 `@include`;编译产物与重构前等价(已对比验证)。
7. ~~**[独立] storage constants / service**~~ ✅ 2026-06-10 已完成:`src/constants/storage.js`(STORAGE_KEYS)收敛协议字符串(persist 的 key 本就按 `ld_store_<id>` 派生,不散落);顺带修审核 v0.1 P3 #10——新建 `composables/useTimers.js`(onUnmounted 自动清理),全部页面级 `setTimeout` 收口。

## 拓扑结论
主依赖方向 `pages → components / store / data / utils`,**无循环依赖**(优点)。不漂亮处即上面的
#4(组件直接取数)、#2(HelpSheet 错位)、#3(store → checkout 数据)、#6(样式职责混)、
#7(storage key 散落)。

## 建议节奏
- ~~**现在可做(独立、不被云开发覆盖)**:#1(真 bug)、#2、#6、#7。~~
- ~~**押后到云开发阶段一起做**:#3、#4、#5(都属数据层 / 业务流,届时本就要重构)。~~
- **2026-06-10 backlog 清零**:#1–#7 全部关账(#4 随课程上云 PR #13;#3/#5 作订单上云前置 PR #15/#16)。新债随新阶段(订单上云 / P3 激活闭环)再记。

## 新增（2026-06-11 起）
8. ~~**[结合 P4 支付] `pending-pay` 样例页已无入口**:订单入口接真单后(订单列表页上线),「我」页不再跳
   `pages/pending-pay/index`,该样例页成孤页(仍注册在 pages.json)。P4 接真实支付时二选一:改造成真
   待支付流程(订单详情 pending 态 + 去支付),或删页。同批处理 `data/orders.js` 的 `ORDER_CFG`
   样例路径(订单详情 `?status=` 演示驱动,接真后可删)。~~ ✅ **已关账**(2026-06-12,P4 支付 PR-3):
   选「删页」——真待支付承载在订单详情 pending 态(去支付 + 倒计时,PR #68);删 `pending-pay` 页 +
   pages.json 注销 + `ORDER_CFG` 演示驱动整体删除,订单详情只由 `?id=` 真单驱动,paysuccess 兜底改进订单列表。
9. ~~**[独立] `trackEvent` events 写入放大无频控**(定向审核 2026-06-11 A-3,P3)~~ ✅ **已根治**(2026-06-15)：两条建议都落地——① **限频**=P2 `withRateLimit`(trackEvent 60/分,守卫 `user-writes-throttled`)堵进水速度;② **TTL**=`system/cleanupEvents` 每日删 90 天以上 events(守卫 `events-cleanup-wired`)堵存量增长。events 只写不读、删旧安全。
10. **[独立] admin 三个大文件偏大**(优雅性审核 2026-06-11 B-2,P3):`StepVideos.vue`(583)/`Showcase.vue`(576)/
    `StepCards.vue`(509)。均功能密集(三层树编辑+拖拽+视频直传 / 一比一手机预览+双向拖拽 / 双面 SVG 卡片设计器)。
    拆分(预览组件 / 拖拽逻辑抽出)属判断题、有回归风险,非机械整洁化。**待触碰该页时顺手拆**,不主动重构。
11. **[独立·备选] 小程序硬编码颜色 token 化**(优雅性审核 2026-06-11 C-1,P3):src 有 17 处硬编码颜色,多为
    刻意一次性用色(播放器深色琥珀 / 紫头渐变 / 微妙描边 tint),`uni.scss` 无对应 token。**非低风险项**
    (改了易视觉漂移)。仅当未来扩展 token 体系时再统一,否则保留。
12. **[独立] 已上架商品无下架/删除路径**(逻辑架构再审核 2026-06-11 链2,P2):`publishProduct` 写入 `products`
    后商品**永久可售**——`deleteDraft` 只删 productsDraft 草稿、橱窗 featured=false 只去首页轮播、`getProducts`
    全量下发不按状态过滤。持续上新的店要**停售某款 / 撤回误发布**时无路径。建议:加 `unpublishProduct`(软下架,
    products 加 `listed:false` + getProducts 过滤) 或 `deleteProduct`(硬删)。控制台商品列表加「下架」动作。
    随商品管理增强一并做(非紧迫,当前可手动改库兜底)。
13. **[独立] 列表分页体系**(外部审核 2026-06-12,调试日志 K,P3):订单/后台订单/后台售后/评价均固定 limit
    (现已临时调到 100/200/200/200),真实数据增长后旧数据会被挤出工作台;评价汇总只基于最近 200 条非全量口径。
    建议:列表分页 + 评价汇总改增量聚合字段。批次B 已做缓解(getMyOrders 提到 100 + `getOrderById` 详情兜底),
    规模起来(单量过百/评价过百)再做完整分页。
14. ~~**[独立] users/progress 先查后建可重复建档**(外部审核 2026-06-12,P3)~~ ✅ **已根治**(2026-06-15 上线前安全体检确认):login/updateProfile/trackEvent 均经 `ensureDoc` 确定性 `_id`(`OPENID` / `OPENID__courseId`),并发首写撞号即幂等、不重复建档;仍先 `where` 命中老随机 _id 档作惰性迁移,读侧稳定。
15. ~~**[独立] admin 口令 bootstrap 抢占窗口**(外部审核 2026-06-12,P3)~~ ✅ **已根治**(2026-06-15 确认):`checkKey` bootstrap 须 `ADMIN_BOOTSTRAP_KEY` 部署密钥匹配,未设该环境变量=禁 bootstrap——「空库谁先登录谁占管理员」窗口已关。

## 上量就绪（4 位数/天 · 2026-06-15 看码评估）
> 容量 ≠ 正确性。并发正确性已稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）；下列是**上量前必清**，按风险排（背景见 `重构日志` v0.9.2 里程碑容量评估）。**从未压测**——以下是看码推断、非压测证明（根因#8 同理：单人能用 ≠ 千人同时）。
16. **[控制台·头号未知] 数据库套餐档位**：读写全经云函数→库（16 集合锁后库是总闸），峰值 QPS 上限取决于套餐档位、代码里看不到。上量前去控制台确认档位能扛峰值（4 位数/天 ≈ 峰值每秒几十、爆发到 ~100）。
17. **[结合 R21·头号成本] 视频 CDN/VOD**：R21「视频上 VOD/CDN」标 P5 搁置——现视频很可能仍裸云存储直吐，4 位数/天观看的带宽/流量费是压倒性大头，可能先顶爆存储带宽。上量前定方案（与 #9 events 同属规模成本）。**上传侧（性能复审 P2）**：视频上传主路径 COS 直传（方向对），直传失败回退 base64 分片、云端 `uploadFinish` 卡 `total<=200`（≈12MB 上限）——大视频回退路径慢/吃内存/吃请求体；上量/大视频前把 fallback 改显式降级提示、别当常态主路径。
18. **[独立] 看板内存聚合 1000 条/表封顶**：`getDashboard` 各表 `limit(1000)` 内存聚合（注释自承「≤1000 条/表」）——4 位数当天订单破千，GMV/统计即少算。上量前换增量计数或真聚合（与 #13 评价聚合同类）。
19. **[独立] 索引核查 + 压测**：`closeExpiredOrders` 等 `where({status,createdAt})` 在增长的 orders 上需复合索引（`_openid` 微信云开发自动建、无忧）；上量前补索引核查 + 跑一次峰值并发压测，把「推断」变「证明」。
20. ~~**[独立·安全] 用户端写函数频控（P2，根因#13）**~~ ✅ **已做**（2026-06-15）：`trackEvent`/`createOrder`/`login`/`updateProfile` 经 `withRateLimit` 按 openid 限频（60/20/30/20 次/分），守卫 `user-writes-throttled` + 测试 userRateLimit。后台登录限流（P1）同批前置。
> 🔒 **部署动作**：新增 `rateLimit` 集合（throttle 动态建）须在控制台锁「仅管理端」（与其余 16 集合同档，见 `console-assets/02-库权限期望表`）。
21. ~~**[独立·安全] 频控计数非原子（并发窗口）**~~ ✅ **已根治**（2026-06-15，用户拍板"先打#2/#21提前"·从上量线提前做）：`throttleHit`/`throttleFail` 改 **CAS 重试（乐观并发）**——读旧值 → `where({_id,旧值}).update`，`stats.updated=0` 即并发抢先改过则重读重试（与 `transition()`/激活码抢占同范式·根因#1），窗口初始化用 `_.exists(false)` 前置；杜绝读-改-写在突发并发同 key 下读到同一旧值各自写回致计数偏小/锁定漏过。**守卫=行为测试**（验行为非验存在）`tests/cloud/throttleConcurrency.test.js`：`Promise.all` 灌并发，断言 throttleHit 无丢更新+恰 max 放行、throttleFail 突发爆破仍锁定——原非原子实现下必红、CAS 后绿（=反向自检）；扩测试桩 `command.exists`。诚实边界：真库严格限流仍待真压测（根因#8·归 #19）。`throttleFail`（登录爆破）同批一起治。
22. **[独立·性能] 后台读路径固定限量（性能复审 2026-06-15 P2）**：除 #18 看板外，`actions/batches.ts`/`courses.ts` 的 `qrcodes.where({courseId|batchId}).limit(1000)`（批次列表/码导出）+ `uploadChunks.where({uploadId}).limit(1000)`（分片拼接）同属固定上限——批次/码/分片增长后先撞顶、再退化为「全量拉取+内存聚合」。改：批次单独汇总文档 + 码列表分页/流式导出。评价 `getReviews` 最近 200 + 请求时现算评分/标签＝**院外债#13 后半**（写入时维护 `reviewStats/productId` 增量统计·规模到再治），同此类、不在此重列。
23. **[独立·运维] 线上监控/告警缺失（可观测性，对照项目流程 2026-06-15）**：**代码侧已做（2026-06-15·用户拍板"#23 代码侧"）**：钱链「语义级/静默失败」+ 爆破锁定经 `kit/observe` 的 `alert()` 打统一可告警标记 `[LD_ALERT] sev=money|security`——payCallback 金额不符/未知订单、refundCallback 与单不符/非成功/未知售后、pay 无预付单、notify 防伪命中、throttleFail 锁定；守卫 `moneychain-alert-wired` + 行为测试 `moneychainAlert`（5 条）。平台指标管「函数挂没挂」、本标记管「钱链对不对/被没被刷」。**余外部步骤（靠人·控制台侧，本债续开到落地）**：① 对 `[LD_ALERT]` 配云开发日志告警 + 企微推送 ② 平台原生指标（调用量/错误率/P95/超时）阈值告警 ③ 容量水位看板。——原始三件套规格：29 函数上生产但无实时监控——出错/超时/被刷只能事后翻日志、无主动告警（评估侧有 `/capacity-check`，实时侧空）。三件套：① **埋点对象**＝钱链函数（createOrder/paynotify/refundnotify/callFlow）调用量·错误率·P95 耗时 + throttle 锁定数（爆破信号）；② **告警阈值**＝错误率突增 / 支付回调失败 / 函数超时率 / DB·带宽逼近套餐档位（接 #16/#17 水位）；③ **容量水位看板**＝日订单·GMV·活跃 + 函数调用数 + DB 读写 QPS + 存储/CDN 流量，对照套餐余量。优先用云开发自带监控告警（控制台配·代码看不到）+ 关键项推送（如企业微信机器人）。上量前补，与 #16/#17 容量档位配套。
   > **↳ #23 落地前的过渡守望 loop（发布窗口用·配齐云开发原生告警即退役）**：发布当天起开只读 `/loop`（省略间隔自定速，在 `packages/cloud` 跑，先 `tcb login` + `tcb fn list` 验通路；CLI 只读得到**部署函数**日志，控制台工作流节点 paynotify/refundnotify 仍看控制台监控）。
   > - **每轮**（只读，绝不 `tcb fn invoke`/写命令）：钱链 `createOrder pay payCallback applyRefund refundCallback closeExpiredOrders adminApi` 各跑 `tcb fn log <fn> --error --json -l 30 --order desc`，按 reqId 比上轮、只报**新增** error；createOrder/pay/payCallback 加 `--orderBy duration --order desc` 看慢调用（超时代理，阈值 `tcb fn detail` 取）。
   > - **判级**：🔴 立即停+报人＝payCallback/refundCallback 任何新 error（钱回调失败零容忍）｜ createOrder/pay/applyRefund 新 error≥3 或翻倍 ｜ 任一超时 ｜ adminApi 锁定/失败口令激增（爆破）｜ tcb 连 2 轮自身失败（提示重 `tcb login`）。🟡 非钱链零星 error / 时长升高 → 压 270s 连看 3 轮。🟢 无新 error。
   > - **节拍**：峰值 270s（缓存热）/ 平静夜间 1200–1800s，**避 300s**。**出口**：连 6 轮全绿问是否收 ｜ 说「收」即停 ｜ 发布后 48h 自动总结收尾。
   > - **边界**：只覆盖错误/可用性半边；容量水位（DB QPS/带宽/GMV/调用量）看控制台监控——本 loop 是 #23 的人肉过渡版，原生告警 + 企微推送配齐即退役。

## 产品功能缺口（规模 / 业务节奏触发）
24. **[独立·产品] 搜索 + 商品分类浏览未开发**（页面版图核查 2026-06-15）：首页「搜索框」是装饰（`@tap` 仅滚到品牌介绍区、无结果页），`catalog` 是课程目录、无独立商品分类/筛选页——商品浏览全靠首页陈列。**精品店当前 SKU 少（seed 仅 5 占位）属合理轻量、不阻塞上线**；SKU 上百后补真搜索（商品/课程）+ 分类筛选。其余功能缺口已各有册：客服 #9 / 分享 #10 / 收藏 #11 / 优惠券 #6·R20 / 物流 #3，订阅消息见《运营与增长规划》上线前钩子。
25. **[独立·安全] 隐私授权弹窗挂载范围 < 闸触发范围**（代码审核 2026-06-15）：`registerPrivacyGate` 在 App 全局注册、`privacySheetVisible` 是模块级单例，任意页被微信触发隐私授权都会翻 true；但 `<PrivacySheet/>` 现**只挂在 `profile-edit` 一页**（对照同款 `LoginSheet` 挂在 checkout/me/index 每个触发页）。当前 mp-weixin 无涉隐私接口调用、**惰性无害**（见占位㉒），但接第一个隐私接口时若在别页触发，弹窗渲不出 → 用户点不了同意 → `resolve` 永挂。守卫 `privacy-authorize-wired` 只校验「四件套存在」、不验挂载可达性＝盲区。**接第一个隐私接口前**：把 `<PrivacySheet/>` 挂到该页（仿 LoginSheet）或全局挂一次 + 给守卫补挂载可达性检查。
26. 🔴 **[上线必做·合规] 发货信息管理未接微信**（物流调研 2026-06-15）：微信要求**实物+微信支付**的小程序，支付后须上传发货信息（`upload_shipping_info`，用小程序自己 AppSecret，**与快递公司 API 无关**），否则**订单资金冻结/无法结算** + 后台反复弹「待接入订单发货管理」。本仓 `shipOrder` 只把公司+单号存进自己库、**没调微信发货上传接口**＝合规缺口（现真钱验收是测试单没事，真实顾客订单的钱会被锁）。**决策（关键决策§20）＝上线前先「后台手动录入」**（卖家在 mp 后台「交易/订单→发货信息录入」逐单填，零代码、不阻塞）；量起来再接 `upload_shipping_info` 自动上传（数据已齐：order.id=out_trade_no + 买家 `_openid`，shipOrder 顺手调）。配套「确认收货组件」可选（微信超时自动确认，本仓已有自家 confirmReceive）。**上线前务必去 mp 后台确认有没有在弹「待接入发货管理」提醒。**
27. **[独立·简洁/可维护] `throttle.ts` CAS 逻辑重复（简洁度体检 2026-06-15）**：`throttleFail` 与 `throttleHit` 是两段结构完全相同的 CAS 重试循环（读→无记录 add→窗口翻滚→算→带 `_.exists(false)` 兜底的 `where(旧值).update`→`stats.updated` 判定→重试），只差字段名（fails/windowStart vs hits/hitWindowStart）+ 达阈「锁定」vs「返回超限」。throttle.ts 191 行＝次大 kit 文件近 4 倍。**抽内部 `bumpWindowed(key, 计数字段, 窗口字段, windowMs, reshape?)` 单点收口**，两公开函数变薄包装（191→~95 行）——精微并发逻辑只剩一处可审可测＝**简洁+安全单点**（重复=两处都得改对、改一处漏一处更脆）。纯重构不改行为；安全网已就位（throttleConcurrency/moneychainAlert/adminThrottle/userRateLimit 四套），押后独立 `/refactor-batch`。#21 引入、当时优先把正确性做对未回头收口。
28. **[独立·安全/可维护] 云集合名无单一来源（数据散落排查②·2026-06-15）**：17 个集合名全裸字面量散落（`'orders'`×17 / `'afterSales'`×11 / `'activations'`×9…），无 `COLLECTIONS` 常量。风险非改名而是**打错字**——写错集合名 → 静默建/查一个新集合，而库权限按 16 集合逐一锁、打错的不在锁定名单＝**无保护的洞**（安全相关）。改：收口集合名常量 + 可配守卫「`.collection()` 只收已知名」。改动面大（40+ 调用点），押后批次。
29. **[独立·可维护] 错误码单源已建却被绕过（数据散落排查③·2026-06-15）**：`shared.ERR`/`ErrorCode` 已定义，但 `reply.ts` 的 `err(error: string)` 未用该类型约束，云函数几乎全用裸串 `err('NOT_FOUND')`（`ERR.*` 仅 2 引用，裸码散落 40+：`'NOT_FOUND'`×5 / `'BAD_ARGS'`×5 / `'NO_ID'`×3…）＝单源形同虚设、同码多处重复。改：`err()` 收紧成 `ErrorCode` 类型 + 裸码归并 ERR（先确认前端是否按码匹配再定 ErrorCode 全集）。改动面大，押后批次。
30. **[独立·低危] 零星数据散落三则（数据散落排查④·2026-06-15）**：① 环境 ID `cloudbase-d4gcssqbv06865479` 在 `miniapp/utils/cloud.js` + `admin/StepBatch.vue`（嵌 URL）两包各一份（`cloudbaserc` 部署配置合理独立）——宜收 shared 常量；② 「官方旗舰店」后缀 checkout/order 各写一遍（店名已单源、后缀漏网，同 brand 家族）；③ 前端订单/售后状态裸字面量 ~30 处（shared 有 `ORDER_STATUS`，前端 JS 多处直接比较）。均不紧迫，触碰相关文件时顺手收。

---

## 上线前占位（决策追踪）

> 状态：🔴 上线必做 · ✅ 已定 · 🗣️ 待再讨论 · ❓ 待确认。需求侧对应见 `需求清单.md`（R20 券/R23 店名/R25 视频/R26 域名/R27 协议）。

## 一、交易与履约

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 1 | 微信支付 | ✅ **已通**（2026-06-12 真机付款验证：订单 2026061216508470 真实交易号回写） | ✅ 已做 | 工作流通道（调试日志 J）；正册资产见工作日志；测试商品记得下架 |
| 2 | 售后 / 退款 | ✅ **已通**（2026-06-12 真机验证：¥2 真单申请→审核→原路退款→回调翻态全链路） | ✅ 已做 | 工作流通道（退款流 kbgzl_n8gojr3a）；正册资产见工作日志 |
| 3 | 物流轨迹 | **代码侧已做（2026-06-15）**：订单页「查看物流」微信端打开快递100插件轨迹页（`plugin-private://wx6885acbedba59c14`），其它端/未知快递/打开失败回退复制运单号 | ✅ 代码侧已做（余真机验） | **决策（关键决策§20）＝快递100「快递跟踪」免费插件 v`1.1.4`**（聚合商不直连快递公司，已添加成功）。manifest 声明插件 + `utils/express` 中文快递名→编码（顺丰=shunfeng 等）+ 守卫 `express-plugin-wired`。**余靠人**：① 真机验插件轨迹渲染（根因#8）② 隐私指引登记第三方插件（与㉒一起）。**依赖风险**：快递100 服务市场已下架，故做了复制兜底。**＋发货合规见 #26** |
| 4 | 配送方式 | 结算页硬编码「顺丰·包邮」 | ✅ 已定·占位 | 用户确认**明确是占位设计** |
| 5 | 搭配购小件 | 钩针组/棉线包硬编码、不在商品体系 | ✅ 已定·占位 | 用户确认占位，**小搭配产品另有计划** |

## 二、营销 / 会员

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 6 | 优惠券 ¥20 | 每单无条件减 20 | ✅ 已定·占位 | 用户先前确认=开发期占位，P4 做真实券系统 |
| 7 | 幸运积分 | ~~结算页「可用 320·暂不使用」硬编码~~ | ✅ 已撤 | 暂无会员系统 → 2026-06-11 撤除结算页积分占位行 |
| 8 | 订单备注 | ~~结算页占位行~~ | ✅ 已撤 | 不做 → 2026-06-11 撤除结算页备注占位行 |

## 三、客服 / 互动

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 9 | 人工客服 | **代码侧已做（2026-06-15）**：用户拍板①微信客服官方组件；四入口（详情坞/「我」页/售后页/播放页 ServicePanel）微信端 `<button open-type="contact">` 进原生客服会话，非微信端 view+toast 兜底；旧假 Toast「正在接入人工客服」全退役 | ✅ 已定（余接待人·外部） | 守卫 `customer-service-wired`（假 Toast 绝迹 + 四入口接 open-type=contact）。**余外部步骤（靠人）**：mp 后台「客服」配置真实微信号当**接待人**、真机验客服会话能进能收发（根因#8）。接待人 = mp 后台运营动作，代码侧与谁接待解耦 |
| 10 | 分享 | **已做（2026-06-15）**：详情页接微信转发（onShareAppMessage）+ 朋友圈（onShareTimeline）+ 分享按钮 open-type=share；分享卡由 `utils/share` 构造 | ✅ 已做 | 守卫 `detail-share-wired` + `share.test`（5 条）；其它端非发布目标保留 toast；**转发卡渲染/回跳真机验**（根因#8） |
| 11 | 收藏 | 详情「已收藏」假反馈、不保存 | ✅ 按 AI 初判 | 无会员体系下价值低，暂留占位，非阻塞（可随时翻案） |
| 12 | 播放页「更多」 | ~~Toast「更多（开发中）」~~ | ✅ 已撤 | 2026-06-11 删除播放页「更多」按钮（留等宽空位维持标题居中） |

## 四、内容与媒体（有管线，运营逐步填）

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 13 | 商品图片 | 大量灰占位，真实封面已能从控制台点亮 | ✅ 按 AI 初判 | 控制台传齐即可，运营逐步填 |
| 14 | 课程视频 | 多为占位/外链；小熊已有真视频，其余缺 | 🔴 已定·必做 | 核心卖点，上线前关键课要有真视频（AI 初判） |
| 15 | 首页买家秀 | 品牌样例文案（写死 3 条） | ✅ 按 AI 初判 | 可暂留品牌文案，或后续接真实评价 |
| 16 | 详情「为你推荐」 | 静态样例 | ✅ 已修 | 2026-06-11 排除当前商品自己（仍是静态样例，真推荐看后续需要） |
| 17 | 种子商品/课程 | catalog/seed 占位身份 + 占位课 course-duck | ❓ 已解释·待确认 | 见下「种子商品说明」；上线前确认占位假货已被真实商品覆盖/清除，不露给顾客 |

## 五、扫码 / 域名 / 印刷

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 18 | 扫码域名 | 测试用默认托管域名，扫码到落地页、不自动跳小程序 | 🔴 已定·必做 | 印刷投产前必须，自有域名+微信规则（进行中，AI 初判） |

## 六、品牌 / 清理

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 19 | 店铺名 | **已定名（2026-06-15）**：「Lucky Ducky 小棉鸭」；旧占位「易织™小棉鸭®」6 处硬编码（order/checkout/welcome/BrandIntro/GroupPanel/productDetail）+ 协议页「本店」自称全替，收口 `constants/brand.js` 单源 | ✅ 已定 | 用户拍板店名＝「Lucky Ducky 小棉鸭」；守卫 `brand-name-single-source`（旧占位绝迹 + 字面量只在 brand.js）；shop 行保留「官方旗舰店」descriptor。**真机抽查显示**（根因#8） |
| 20 | pending-pay 孤页 + `?status=` 演示路径 | 无入口样例页 | ✅ 按 AI 初判 | P4 清理（技术债 #8） |

## 七、账户 / 协议（2026-06-14 加，随登录页落地）

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 21 | 用户协议 / 隐私政策 | **正文已补（2026-06-15）**：`pages/agreement` 改为结构化条款（用户协议/隐私政策各 10 条「第N条」，含退货权法律节点 R4、第三方=微信支付/云开发、**不采集手机号**、用户权利），标注「初稿」 | 🔴 上线必做（余法务复核） | 守卫 `agreement-text-real` 防回退占位；店名暂以「本店」自称（待 R23/㉓ 定名替换）；**正式发布前须经法务复核定稿** |
| 22 | 微信「隐私保护指引」 | **代码侧已接（2026-06-15）**：manifest `__usePrivacyCheck__:true` + `usePrivacyGate`（接 onNeedPrivacyAuthorization）+ `PrivacySheet` 授权弹窗（agreePrivacyAuthorization 按钮 + openPrivacyContract）+ App 启动注册 + profile-edit 挂载（守卫 `privacy-authorize-wired`） | 🔴 上线必做（余 mp 后台+真机） | **余外部步骤**：① mp 后台登记《隐私保护指引》（与㉑一致）② 真机验授权弹窗（根因#8）。注：mp-weixin 现无涉隐私接口调用（登录静默 / 头像 chooseAvatar / 昵称 input 均不受隐私闸约束），弹窗为前瞻接入；纯状态逻辑有 `tests/privacyGate.test.js` 兜底。**挂载范围 < 闸触发范围债见 #25**（接第一个隐私接口前补） |

---

## 附：种子商品（seed）说明
「种子数据」是开发期为让系统开箱能跑而预灌的**样例假数据**，不是功能：
- 上架前数据库需先有商品否则首页空白，故写死 5 条占位商品（幸运小鸭礼盒 ¥198 等）+ 1 门占位课（course-duck，有样例章节名、无真视频），用 `seedProducts`/`seedCourses` 灌入。
- 名字/价格/课程内容全是编的。控制台上架的真实商品写**同一张 products 表**会覆盖/补充。
- seed 现仅剩兜底作用：① 网页端无云时本地回退展示 ② 库被清空时初始兜底。
- **上线前动作**：确认占位假货已被真实商品覆盖或清除，不让编造商品露给顾客。

## 待办收束（按状态）
- **🔴 必做**：⑭关键课真视频 ⑱扫码域名 ㉑协议（正文已补·待法务复核）㉒隐私授权（代码侧已接·待 mp 后台登记+真机）—— ①支付②退款已通；㉑㉒ 代码侧 2026-06-15 落地、余外部步骤；均在计划内，无意外硬骨头。
- **🗣️ 待再讨论（用户已批注）**：⑰种子商品（已解释，待确认覆盖/清除） —— 下次讨论逐条定。（③物流 2026-06-15 已定＝快递100 免费插件 + 发货合规手动录入#26；⑨客服已做＝微信客服 open-type=contact，余接待人 mp 后台配·外部）
- **✅ 已做（2026-06-11 本批）**：⑦积分撤、⑧备注撤、⑫播放页更多撤、⑯推荐排除自身。
- **✅ 已定**：③物流＝快递100 免费插件（余外部前置）+ 发货合规手动录入(#26 上线必做)；④⑤⑥占位保留；⑨客服代码侧已做（微信客服 open-type=contact，余接待人 mp 后台配·外部）；⑩分享已做、⑪暂留；⑬⑮运营填；⑲店名定名「Lucky Ducky 小棉鸭」(2026-06-15 收口 brand.js 单源)；⑳P4 清理。
