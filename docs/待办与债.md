# 待办与债（技术债 backlog + 上线前占位）

> 来源:2026-06-07 两轮审核(① 代码可维护性 ② 逻辑拓扑结构)+ v0.1 全量审核(原报告已归档至 `archive/审核报告-v0.1.md`,其 P2/P3 结论与本清单一一对应,无遗漏新增)。
> 这些**不是功能 bug**(主流程/测试/构建都稳),是**长期可维护性**的改进项。
> 分工:功能 bug → `调试日志.md`;技术债/重构 → 本文件;新系统设计 → `archive/设计规格-课程电商系统.md`。
>
> ⚠️ **关键判断**:项目即将接**微信云开发**(见设计规格)。**部分重构与云开发数据层改造重叠** ——
> 标 `[结合云开发]` 的建议届时一起做(现在改完会被重构覆盖,白做);标 `[独立]` 的可随时做。

## 🚀 v0.9.5 republish 清单（下一步发版·攒批·2026-06-25）
> 小程序 v0.9.4 已线上；下列前端改动已合 main·未发布，攒一批发 v0.9.5（云函数修复本会话已逐个部署生产·不在此）。
- **内容（明细见 `重构日志` 2026-06-24~25 各前端批）**：① 帮助视频（求助面板辅助视频接云·两级主题→小段·菜单精简）；② 播放器稳定性批（分段地址缓存预取/首次进课不黑屏/真机白屏修/返回落本课目录/导航不困死无视频课时/进度拖拽+小段跨课时/续播定位）；③ 反馈入口+错误上报·扫码激活显对课修·评价 cursor 分页·协议措辞·前端状态走 shared。
- 🔴 **前置闸（顺序+详情见 `现状与路线`§上线执行 runbook）**：**最易漏=微信后台「下载合法域名」加视频临时 URL 域名 `636c-…tcb.qcloud.la`**（不加→正式版课程/帮助视频全黑屏）→ 扫普通链接二维码规则(`www.luckyducky.cn/q/`)→ 隐私指引 mp 登记+真机验 → 协议法务定稿 → 控制台锁新集合(inventory/auditLog/feedback/rateLimit/kfState/kfIdentity) → `urlCheck`→true → `build:mp-weixin` 导入上传提审发布。
- **真机验收**：进课不黑屏/切段/返回落本课·帮助视频主题分段连播(先控制台传 1 条)·扫码激活显对课(非默认)·下单→支付→解锁→退款黄金路径·隐私授权弹窗。

## 早期 backlog #1-7（2026-06-10 全清零·已卷档）
> Demo 期可维护性 7 项（sanitizeCart 小数qty / HelpSheet 归位 / SAMPLE_ADDRESS 解耦 / section props / checkout 拆件 / 样式策略 / storage 常量）全完成 → `archive/待办与债-已关账-202606.md`

## 新增（2026-06-11 起）
> 已关账 #8 `pending-pay` 孤页（2026-06-12 删页）、#9 trackEvent 无频控（2026-06-15 限频+TTL 根治）卷档移除——详 git 史 / `重构日志`（CLAUDE 规则②③·腾预算）。
10. **[独立] admin 三个大文件偏大**(优雅性审核 2026-06-11 B-2,P3):`StepVideos.vue`(583)/`Showcase.vue`(576)/
    `StepCards.vue`(509)。均功能密集(三层树编辑+拖拽+视频直传 / 一比一手机预览+双向拖拽 / 双面 SVG 卡片设计器)。
    拆分(预览组件 / 拖拽逻辑抽出)属判断题、有回归风险,非机械整洁化。**待触碰该页时顺手拆**,不主动重构。
11. **[独立·备选] 小程序硬编码颜色 token 化**(优雅性审核 2026-06-11 C-1,P3):src 有 17 处硬编码颜色,多为
    刻意一次性用色(播放器深色琥珀 / 紫头渐变 / 微妙描边 tint),`uni.scss` 无对应 token。**非低风险项**
    (改了易视觉漂移)。仅当未来扩展 token 体系时再统一,否则保留。
13. ~~**[独立] 列表分页体系 + 评价汇总精确**~~ ✅ **全做完**（2026-06-18 订单/售后/评价列表 cursor 分页·守卫 `reviews-list-paged`；2026-06-24 评价汇总改 `count()`+`aggregate(sum)` 精确不封顶·守卫 `reviews-summary-exact`·详 `重构日志`）。
15. ~~**[独立] admin 口令 bootstrap 抢占窗口**(外部审核 2026-06-12,P3)~~ ✅ **已根治**(2026-06-15 确认):`checkKey` bootstrap 须 `ADMIN_BOOTSTRAP_KEY` 部署密钥匹配,未设该环境变量=禁 bootstrap——「空库谁先登录谁占管理员」窗口已关。
16. **[视频功能·2026-06-25 多轮审计跟进]** 视频改动串（提速/白屏修/返回固定目录/F1·F3/审计#1#2#3/#9#10#5）已落 main，详 `重构日志` 同日条。剩两类：**✅ 部署闭环**（2026-06-25）= getCourses #10 白名单已被并行批 `b66fc42` 当漂移一并部署、用户复跑 deploy-fns 显示 0 待部署=零漂移；#5 纯注释编译产物不变无需部署。**真机验收**（根因#8·dev/H5 测不出）= 进课不黑→首帧即播 / 切后台回来错误浮层不黏(F1) / 进度条首触即应(F3) / 返回固定落目录 / **原生 video 同层渲染·暂停键·进度条·求助·返回四件套灵不灵（最大真机风险·结构已规避降级·未上 cover-view·降级才上·低端安卓+旧基础库）** / 横屏旋转几何(#7 搁置) / 全面屏底部 env()(#8 搁置)。**主动搁置（防过度工程）**= #4 频控(撤·无免费视频) / #5 退款收观看权(有意设计·确认进课即失退货权·仅 admin 强批的人工灰色·不自动失效 activations) / #12 占位末段埋点重报(占位退场) / #13 mmss>1h(YAGNI·现全<1h)。（#11 trackEvent 口径已修=外部报告 P2-4·见 #17）
17. **[视频外部审核报告·2026-06-25]** 桌面《视频链路多agent审核报告》14 条评估归档（详 `重构日志` 同日「外部视频审核评估」条）：P1-3/P1-4 早修(#3/#1)、**P2-5 进度fail-open + P2-4 trackEvent enteredAt + P1-3尾 本批已修**（✅·`3187dd2`·**trackEvent 待重部署**）、P2-2/P2-3 deferred/moot。**🔴 待你定方向：P1-5 退货权按件粒度**（同款 qty=2 只确认1件→两件都退不了）——查实「**拆单件行不局部**」：confirmEnter 已只翻第一行 refundable，但 `applyRefund` 用 `.find(productId)` 取第一行→拆行后取到被翻那行仍 NOT_REFUNDABLE（同 bug 换地方）+「一单一品一售后」唯一键冲突 → 做对=createOrder 拆行+applyRefund 改找可退行+售后改按行+confirmEnter 一整套**退款模型改动**。**建议改方案 B `refundableQty`**（行形状不变·确认件数-1·applyRefund 判余量退1件·blast 小）。**属并行 agent 活跃退款域**，宜由其统一改或协调。**并行 agent 域（路由给它·非我改）**：admin 发布链 P1-1/P1-2/P2-1、帮助视频 P1-6/P2-6/P2-7/P3-1。

18. **[外部审核 R1-R4·2026-06-25]** 桌面《全项目深度审核 R1-R4》评估归档（/deep-audit 亲核 9 条 P1 全 + 5 条 P2·14/14 命中·**无 P0**·详 `重构日志` 同日条）。校准：P1.9 实为 P2（码 _id 仍唯一·只后台分组合并）、P2.12 安全性被高估（mp 鉴权用 WX 上下文 OPENID 非客户端 token）。**✅ A 簇便宜确凿四修已落（`40bca80`·各配守卫·反向自检过）**：P1.6 样例地址清空 / P2.11 客服卡片路径 / P2.19 运单字段 / P1.9 批次 ID——**🔴 待你重部署 kfCallback+genQrcodes**，前端 P1.6 随 v0.9.5 republish。**✅ B 簇上量真钱/数据正确性四修已落（本批·各配守卫·反向自检×4·守卫 75→79）**：P1.2 同意退款前复核进课(`ENTERED_NOT_REFUNDABLE`·不触发退款) / P1.8 库存绝对写改 CAS(`expectedUpdatedAt`→`STOCK_CONFLICT`·防覆盖并发预留超卖) / P1.4 客服认领区分基建错vs重复(firstSeen 三态+`CLAIM_FAILED`保留游标) / P1.5 客服回调限批 50+墙钟预算 15s(防超时吞消息)——**🔴 待你重部署 adminApi(P1.2/P1.8)+kfCallback(P1.4/P1.5)**，前端 Inventory.vue 随 admin 域重部署。**✅ C 簇 P1.1 订单行身份 lineId 已落（本批·分两批拍板·新守卫 `order-line-identity`·守卫 79→80·测试 609·反向自检过）**：createOrder 每行 `lineId=productId__spec` + 退款/评价/复核/前端按有效行键 `it.lineId||it.productId` 定位、售后/评价 `_id=orderId__lineId`（旧订单回退 productId 兼容）——修同商品多 SKU 退错/评错行。**🔴 待你重部署 createOrder/applyRefund/submitReview/adminApi/confirmEnter**，前端 review/aftersales 随 v0.9.5 republish。**✅ C 簇 P1.3 数量级退款已落（本批·进课件级+退剩余·用户拍板中批低风险方案·新守卫 `refund-remaining-qty-after-enter`·守卫 80→81·测试 614·反向自检过）**：行加 `enteredQty`·confirmEnter 进课按件递增（`refundable=enteredQty<qty` 非整行作废）·applyRefund 退 `refundableQty=qty−enteredQty` 件、金额按件摊、`afterSale.qty=退件数`·前端售后页显剩余可退件（旧订单 enteredQty 视 0 兼容）。**不支持「退1留1」**（一行一售后退剩余全部·剩余走人工·任意件数多次部分退＝P1.3+·真有需求再上 §7）。**🔴 待你重部署 createOrder/applyRefund/confirmEnter**。**C 簇 P1.1+P1.3 全毕**。**✅ 上线前安全/卫生扫尾已落（本批·守卫 81→82·测试 617·反向自检×2）**：**P1.7**（漏的 P1·PII）logout `$reset` 全用户态 store 防换账号泄露上一位地址（新守卫 `logout-resets-user-scoped`）/ **P2.13** createOrder 地址四字段云端长度上限 / **P2.14** throttle 写 updatedAt + cleanupEvents 清过期 `rateLimit`/`kfState seen:*`（扩 `events-cleanup-wired`）；**P2.18 经核 by-design 不改**（grep 无 transaction_id 入日志·告警里订单号是 §4 明许对账标识）。**🔴 待你重部署 createOrder/cleanupEvents·前端 user store 随 republish**。**外审 R1-R4 代码侧全清（P1 9/9）**；剩 P2 设计类未排期（P2.4 admin 认证模型 / P2.6 内容编辑 CAS / P2.1 支付冷却 / P2.5·P2.7·P2.8·P2.10·P2.15 等）+ **D 簇（够不到·靠人）** 控制台集合 ACL / 真支付退款真机 / WeCom idconvert 错配负例 / 隐私登记 / kfBind unionid 前端未接(P2.9)。

## 上量就绪（4 位数/天 · 2026-06-15 看码评估）
> 容量 ≠ 正确性。并发正确性已稳（确定性 id 防重扣 / 条件抢占防重激活 / fail-closed 闸 / 状态机）；下列是**上量前必清**，按风险排（背景见 `重构日志` v0.9.2 里程碑容量评估）。**从未压测**——以下是看码推断、非压测证明（根因#8 同理：单人能用 ≠ 千人同时）。
16. **[控制台·头号未知] 数据库套餐档位**：读写全经云函数→库（16 集合锁后库是总闸），峰值 QPS 上限取决于套餐档位、代码里看不到。上量前去控制台确认档位能扛峰值（4 位数/天 ≈ 峰值每秒几十、爆发到 ~100）。
17. **[结合 R21·头号成本] 视频 CDN/VOD**：R21「视频上 VOD/CDN」标 P5 搁置——现视频很可能仍裸云存储直吐，4 位数/天观看的带宽/流量费是压倒性大头，可能先顶爆存储带宽。上量前定方案（与 #9 events 同属规模成本）。**上传侧（性能复审 P2）**：视频上传主路径 COS 直传（方向对），直传失败回退 base64 分片、云端 `uploadFinish` 卡 `total<=200`（≈12MB 上限）——大视频回退路径慢/吃内存/吃请求体；上量/大视频前把 fallback 改显式降级提示、别当常态主路径。
18. ~~**[独立] 看板内存聚合 1000 条/表封顶**~~ ✅ **代码侧已根治（2026-06-15·"稳健够用"）**：`getDashboard` 计数改 `.count()`（精确不封顶）、钱链异常改定向 where（精确不漏老单）、最近单改 `orderBy desc limit 5`（取真最新）；GMV/热度/卡点仍近 `SAMPLE=1000` 样本但加 `approx` 标注、admin 看板显「近 N 估算」（静默错→标注近似）。守卫 `capacity-reads-bounded` + 测试 `dashboard`。**续已做（2026-06-15·#18续）**：精确 GMV ＝ DB `.aggregate().match(paid).group($.sum amount).end()`（已付营收口径·不封顶·精确），扩测试桩 aggregate，`approx.gmv` 恒 false。**余**：>1000 真规模仍待压测（归 #19）。
19. **[独立] 索引核查 + 压测**：`closeExpiredOrders` 等 `where({status,createdAt})` 在增长的 orders 上需复合索引（`_openid` 自动建）；上量前补索引核查（控制台动作）。**压测台 ✅ 真证落地（2026-06-16）**：`scripts/loadtest.mjs`（`npm run loadtest`·直连测试库 `luckyducky-test-d6fgztai8647df45` 验地基·内置拒生产闸·跑完清理；配套 `npm run deploy:test`）三块地基均对**真库**证过——`--mode cas` 真库 `where().update()` CAS 原子（`v=20·0 丢更新`）、`--mode capacity` `count=2000=aggregate gmv=2000·991ms` 破千不封顶、**`--mode throttle` 频控端到端**（20 并发命中 max=10 → 恰 10 放行·hits=20·无泄漏，复刻 throttle.ts bumpWindowed 首写 add 撞号竞争 + 窗内 CAS 计数）⇒ throttle CAS#21 / transition / 激活码 / 看板 #18 从「桩绿」升「真证」（根因#8 那条腿）。**频控「只桩绿」缺口闭合**（黑盒打真函数走不通——`withRateLimit` 在 `withOpenId` 内、tcb invoke 给不了 openid；故复刻其确切 DB 操作打真库，throttle.ts 逻辑本身已 throttleConcurrency 桩证）。真跑暴露并修「内存桩 wx-server-sdk 形状 ≠ node-sdk 真形状」三层假阴（详 `调试日志` P / `重构日志` 2026-06-16），`readV`/`updatedCount` 加单测锁死。真容量副得：**环境对突发 DB 有 QPS 限流**（200 并发即挡·20 安全）——峰值非无限。**残：索引核查（控制台）。** **＋安全核验（审核 P1·2026-06-24）**：生产验证 CloudBase HTTP 网关是否强制覆盖 `x-forwarded-for`——覆盖则 adminApi per-IP 频控可信（全局兜底是额外保险）；不覆盖则 per-IP 可被轮换伪造、仅靠全局兜底（已加 `admin-throttle-global-backstop`·20/10分），未证实前别把 per-IP 当强边界。
20. ~~**[独立·安全] 用户端写函数频控（P2，根因#13）**~~ ✅ **已做**（2026-06-15）：`trackEvent`/`createOrder`/`login`/`updateProfile` 经 `withRateLimit` 按 openid 限频（60/20/30/20 次/分），守卫 `user-writes-throttled` + 测试 userRateLimit。后台登录限流（P1）同批前置。
> 🔒 **部署动作**：新增 `rateLimit` 集合（throttle 动态建）须在控制台锁「仅管理端」（与其余 16 集合同档，见 `console-assets/02-库权限期望表`）。
21. ~~**[独立·安全] 频控计数非原子（并发窗口）**~~ ✅ **已根治**（2026-06-15，用户拍板"先打#2/#21提前"·从上量线提前做）：`throttleHit`/`throttleFail` 改 **CAS 重试（乐观并发）**——读旧值 → `where({_id,旧值}).update`，`stats.updated=0` 即并发抢先改过则重读重试（与 `transition()`/激活码抢占同范式·根因#1），窗口初始化用 `_.exists(false)` 前置；杜绝读-改-写在突发并发同 key 下读到同一旧值各自写回致计数偏小/锁定漏过。**守卫=行为测试**（验行为非验存在）`tests/cloud/throttleConcurrency.test.js`：`Promise.all` 灌并发，断言 throttleHit 无丢更新+恰 max 放行、throttleFail 突发爆破仍锁定——原非原子实现下必红、CAS 后绿（=反向自检）；扩测试桩 `command.exists`。诚实边界：真库严格限流仍待真压测（根因#8·归 #19）。`throttleFail`（登录爆破）同批一起治。
22. **[独立·性能] 后台读路径固定限量（性能复审 2026-06-15 P2）**：~~`batches.ts` 批次列表/码导出 `limit(1000)` 截断~~ ✅ **已根治（2026-06-15）**：`listBatches`/`listBatchCodes` 改 `fetchAll`（按页 skip 取尽·扩桩 skip），守卫 `capacity-reads-bounded` + 测试 `batchesPaging`。`courses.ts` uploadChunks 的 limit ＝**安全不改**（上游 `uploadFinish` 已卡 `total<=200`、永远不撞顶）。~~**余未做**：评价 `getReviews` 最近 200 + 请求时现算评分/标签＝院外债#13 后半~~ ✅ **已做（2026-06-24）**：评分/计数/星级分布走 count()+aggregate(精确不封顶)·approx 恒 false（详 #13·守卫 `reviews-summary-exact`）；标签仍近样本 top-5（模糊印象·保留近似）。
23. **[独立·运维] 线上监控/告警缺失（可观测性，对照项目流程 2026-06-15）**：**代码侧已做（2026-06-15·用户拍板"#23 代码侧"）**：钱链「语义级/静默失败」+ 爆破锁定经 `kit/observe` 的 `alert()` 打统一可告警标记 `[LD_ALERT] sev=money|security`——payCallback 金额不符/未知订单、refundCallback 与单不符/非成功/未知售后、pay 无预付单、notify 防伪命中、throttleFail 锁定；守卫 `moneychain-alert-wired` + 行为测试 `moneychainAlert`（5 条）。平台指标管「函数挂没挂」、本标记管「钱链对不对/被没被刷」。**前端半边已做（2026-06-24·运营钩子①）**：用户反馈 `submitFeedback`（feedback 域·过闸+限频·写 feedback 集合·守卫 `feedback-write-gated`）+「我」页意见反馈入口（`pkg-extra/feedback`）+ 前端错误自动上报 `utils/report.js`→events 通道（App.vue 全局兜底·守卫 `error-report-wired`）；**靠人·控制台**：锁 `feedback` 集合仅管理端。**余外部步骤（靠人·控制台侧，本债续开到落地）**：① ~~对 `[LD_ALERT]` 配云开发日志告警 + 企微推送~~ ✅ **企微推送已闭环（2026-06-27）**：钱链告警走**代码直推**路线（notifyAlert→pushBotAlert→企微群机器人·5 个钱链事件）而非云开发日志告警；用户建群机器人 webhook → curl 真发验通（errcode:0）→ admin「消息与通知」页保存到 `adminConfig/settings.alertWebhook`（**webhook key 是凭证·只存后台 DB·不入 git/记忆**）→ 状态「已开启推送」·上线后钱链异常手机实时收。② 平台原生指标（调用量/错误率/P95/超时）阈值告警【余·可选·腾讯云控制台配】 ③ 容量水位看板【余·与 #16/#17 容量配套】。——原始三件套规格：29 函数上生产但无实时监控——出错/超时/被刷只能事后翻日志、无主动告警（评估侧有 `/capacity-check`，实时侧空）。三件套：① **埋点对象**＝钱链函数（createOrder/paynotify/refundnotify/callFlow）调用量·错误率·P95 耗时 + throttle 锁定数（爆破信号）；② **告警阈值**＝错误率突增 / 支付回调失败 / 函数超时率 / DB·带宽逼近套餐档位（接 #16/#17 水位）；③ **容量水位看板**＝日订单·GMV·活跃 + 函数调用数 + DB 读写 QPS + 存储/CDN 流量，对照套餐余量。优先用云开发自带监控告警（控制台配·代码看不到）+ 关键项推送（如企业微信机器人）。上量前补，与 #16/#17 容量档位配套。
   > **↳ #23 落地前的过渡守望 loop（发布窗口用·配齐云开发原生告警即退役）**：发布当天起开只读 `/loop`（省略间隔自定速，在 `packages/cloud` 跑，先 `tcb login` + `tcb fn list` 验通路；CLI 只读得到**部署函数**日志，控制台工作流节点 paynotify/refundnotify 仍看控制台监控）。
   > - **每轮**（只读，绝不 `tcb fn invoke`/写命令）：钱链 `createOrder pay payCallback applyRefund refundCallback closeExpiredOrders adminApi` 各跑 `tcb fn log <fn> --error --json -l 30 --order desc`，按 reqId 比上轮、只报**新增** error；createOrder/pay/payCallback 加 `--orderBy duration --order desc` 看慢调用（超时代理，阈值 `tcb fn detail` 取）。
   > - **判级**：🔴 立即停+报人＝payCallback/refundCallback 任何新 error（钱回调失败零容忍）｜ createOrder/pay/applyRefund 新 error≥3 或翻倍 ｜ 任一超时 ｜ adminApi 锁定/失败口令激增（爆破）｜ tcb 连 2 轮自身失败（提示重 `tcb login`）。🟡 非钱链零星 error / 时长升高 → 压 270s 连看 3 轮。🟢 无新 error。
   > - **节拍**：峰值 270s（缓存热）/ 平静夜间 1200–1800s，**避 300s**。**出口**：连 6 轮全绿问是否收 ｜ 说「收」即停 ｜ 发布后 48h 自动总结收尾。
   > - **边界**：只覆盖错误/可用性半边；容量水位（DB QPS/带宽/GMV/调用量）看控制台监控——本 loop 是 #23 的人肉过渡版，原生告警 + 企微推送配齐即退役。

## 产品功能缺口（规模 / 业务节奏触发）
24. **[独立·产品] 搜索 + 商品分类浏览未开发**（页面版图核查 2026-06-15）：首页「搜索框」是装饰（`@tap` 仅滚到品牌介绍区、无结果页），`catalog` 是课程目录、无独立商品分类/筛选页——商品浏览全靠首页陈列。**精品店当前 SKU 少（seed 仅 5 占位）属合理轻量、不阻塞上线**；SKU 上百后补真搜索（商品/课程）+ 分类筛选。其余功能缺口已各有册：客服 #9 / 分享 #10 / 收藏 #11 / 优惠券 #6·R20 / 物流 #3，订阅消息见《运营与增长规划》上线前钩子。
25. ~~**[独立·安全] 隐私授权弹窗挂载范围 < 闸触发范围**~~ ✅ **已根治（2026-06-16·把靠人变机器守·根因#8）**：盲区＝`registerPrivacyGate` 全局注册、任意页触发隐私授权都翻 `privacySheetVisible`，但 `<PrivacySheet/>` 须挂在该页才渲得出（漏挂→点不了同意→`resolve` 永挂），而守卫 `privacy-authorize-wired` 原只校验「四件套存在」、不验挂载可达性。**改＝扩守卫补挂载可达性检查**（不靠人记「接第一个隐私接口前挂」）：扫 `pages/**/*.vue`、剥非 mp 条件编译块（`mpReachableText`）后命中涉隐私接口（`PRIVACY_INTERFACES` 登记册·`uni|wx.chooseImage/getLocation…`）的页 → 其根 `index.vue` 必挂 `<PrivacySheet/>`，否则红；宁过勿漏（脚本级未 gate 调用也计入）。`profile-edit` 的脚本级 `uni.chooseImage` 即被计入 → 已挂 sheet → 绿（守卫今日即咬·非空转）；反向自检删其挂载即红。**接第一个 mp 隐私接口时**：新页加 `uni.getLocation` 等即逼出红、强制挂 sheet。诚实边界：真机授权弹窗渲染仍待 mp 后台登记《指引》+ 真机验（㉒·根因#8）。
26. 🔴 **[上线必做·合规] 发货信息管理未接微信**（物流调研 2026-06-15）：微信要求**实物+微信支付**的小程序，支付后须上传发货信息（`upload_shipping_info`，用小程序自己 AppSecret，**与快递公司 API 无关**），否则**订单资金冻结/无法结算** + 后台反复弹「待接入订单发货管理」。本仓 `shipOrder` 只把公司+单号存进自己库、**没调微信发货上传接口**＝合规缺口（现真钱验收是测试单没事，真实顾客订单的钱会被锁）。**决策（关键决策§20）＝上线前先「后台手动录入」**（卖家在 mp 后台「交易/订单→发货信息录入」逐单填，零代码、不阻塞）；量起来再接 `upload_shipping_info` 自动上传（数据已齐：order.id=out_trade_no + 买家 `_openid`，shipOrder 顺手调）。配套「确认收货组件」可选（微信超时自动确认，本仓已有自家 confirmReceive）。**上线前务必去 mp 后台确认有没有在弹「待接入发货管理」提醒。**
   **↳ 代码侧自动化已落（2026-06-18·重构日志同日条）**：`shipOrder` 本地翻 shipped 后经 `kit/shipping.ts` `uploadShippingToWx` 走微信云开发「云调用」`cloud.openapi` 上传 `upload_shipping_info`（**无需 AppSecret**·token 云环境注入）；fail-soft——上传失败不反噬本地发货 + 留痕 `wxShipUploaded/wxShipError` + `[LD_ALERT] sev=money` 告警。守卫 `shipping-info-uploaded-to-wx`（接缝单点+shipOrder 须调）+ `shipping-upload-fail-soft`（行为）+ **`openapi-perm-declared`（云调用权限随产物声明·2026-06-26 加）**。**休眠态·配好即生效**：真发原待**靠人三步**——**① mp 后台开通「发货信息管理」能力 ✅（2026-06-26 用户确认已开通·截图见会话）**；**② 云函数部署产物 config.json 声明 openapi 权限 ✅（2026-06-26·权限串 `wxaSecOrder.uploadShippingInfo`·非旧注释的 REST 风格 `wxa.sec.order.*`——官方云调用规则：权限串 === JS 调用路径 `cloud.openapi.<串>`；已由 `build.mjs` `OPENAPI_PERMS` 检测产物自动产 config.json·守卫 `openapi-perm-declared` 锁登记不漏）**；**③ 部署 adminApi（带新 config.json）✅（2026-06-27·hash 0a01445c→d323cf7c·dry-run 0 漂移·**先修了 deploy-fns hash 盲区才识别 config.json 改动·见下↓**）**；**剩仅 ④ 真单真机验上传成功**（根因#8 拿到≠用通·当前无待发货订单·待真实顾客订单来了发货时验「mp 后台无『待接入发货管理』提醒 + wxShipUploaded 留痕」）。手动录入仍是兜底（fail-soft 失败留痕告警·不阻塞发货）。
   **↳ deploy-fns hash 盲区修补（2026-06-27·根因#8 部署≠生效·`16b7f3f`）**：首次部署显「待部署 0」——deploy-fns 漂移 hash 只算 `index.js`（config.json 新增不在 hash 内 + shipping.ts 仅改注释被 esbuild 剥掉=index.js 不变）→ adminApi 判未变跳过 → 权限没真上去。修＝hash 非对称纳入 config.json（无 config.json 的 32 函数不受扰·仅 adminApi 显待部署）+ 守卫 `openapi-perm-declared` 扩第③关「deploy-fns hash 须真喂 config.json」（反向自检逮到首版只查关键词被注释满足=假绿→收紧查真实代码模式）。
   **↳ 订单中心 Path 已登记 ✅（2026-06-26·靠人·应「待接入订单管理」提醒·两个互补入口别填反）**：① **商户平台→订单管理→订单信息录入**＝单笔订单详情，填 `pages/order/index?id=${商品订单号}`（**带占位**·微信用 `out_trade_no`=`order.id` 替换→落 `pages/order/index`·`onLoad` 按 `q.id`→`fetchById`·`getOrderById` 校验 `_openid`）；② **mp 后台→基础功能→小程序订单中心 Path 设置**＝我的订单列表，填 `pages/order-list/index`（**勿含参数**·`?tab=` 仅可选预选·无参也正常拉本人单）。**均零代码改动**——App 自身已用同形深链（`checkout`/`paysuccess` 跳 `/pages/order/index?id=`；「我」页九宫格进 `order-list`）。JSAPI description（商品详情）早合规（`pay.ts` 经支付工作流传商品名到 JSAPI）。**余靠人**：待微信侧关联生效后真单真机验「订单中心点回小程序订单页」（根因#8·拿到≠用通）。
27. ~~**[独立·简洁/可维护] `throttle.ts` CAS 逻辑重复**~~ ✅ **已根治（2026-06-15）**：抽内部 `bumpWindowed(key, 计数字段, 窗口字段, windowMs, onPatch?)` 收口 CAS 重试，`throttleFail`/`throttleHit` 变薄包装（达阈锁定/返回超限由 onPatch+调用方注入）。**精微并发逻辑只剩一处可审可测**（191→157 行；重点不是行数是单点）。纯重构不改行为——四套安全网测试（throttleConcurrency/adminThrottle/userRateLimit/moneychainAlert）全过＝行为等价证明。
28. ~~**[独立·安全/可维护] 云集合名无单一来源**~~ ✅ **已根治（2026-06-15·轻量版）**：风险＝打错集合名 → 建到 16 锁名单外的无保护集合。**未做 95 处机械改 `C.x` 大扫除**（churn 高·风险大·收益低）——改为 `kit/collections.ts` 的 `COLLECTIONS` 登记册（18 集合·单源）+ 守卫 `known-collections-only`（校验全库 `.collection()/createCollection/ensure` 字面量名都在册，打错即红，新集合须先登记）。零 churn、直击安全目标；反向自检（删 events→cleanupEvents 红→还原）。
29. ~~**[独立·可维护] 错误码单源已建却被绕过**~~ ✅ **已根治（2026-06-15·轻量版）**：先查证**前端按精确错误码字符串分支**（order/checkout/aftersales/welcome/review 多处 ===）→ 码是契约不可改名 + 有动态码 `err('BAD_STATUS:'+v)`，故**不强类型化 `err()`**（会被动态码+churn 卡）。改：`ERR` 扩成 31 码完整登记册（单源·`ErrorCode` 派生）+ 守卫 `known-error-codes`（校验全库 `err('字面量')` 码都在册，打错即红、新码须先登记；动态码不误伤）。同 #28 路子、零调用点 churn。反向自检（删 REVIEWED→submitReview 红→还原）。注：守卫被并行 `7d43bda` 提交卷入物流 commit、ERR 登记册随本条补上使 HEAD 一致。
30. **[独立·低危] 零星数据散落三则（数据散落排查④·2026-06-15）**：② ~~「官方旗舰店」后缀 checkout/order 各写一遍~~ ✅ **已收（2026-06-15）**：收口 brand.js 的 `SHOP_FULL_NAME`、两处引常量，`brand-name-single-source` 守卫扩锁「官方旗舰店」只在 brand.js。**① 脚本侧 env id ✅ 已收（2026-06-16·loop）**：另发现生产 env id 还散在 4 个部署/测试脚本（`deploy-fns` 部署目标 / `loadtest`·`deploy-test` 拒生产闸·改一漏一致安全闸不一致＝真病根#5 而非纯低收益）→ 收口 `scripts/lib/env.mjs` 的 `PROD_ENV`，3 脚本 import，守卫 `prod-env-single-source`（guard-deploy 的 id 仅文案·豁免）。**③ 前端订单/售后状态裸字面量 ✅ 已收（2026-06-24·P3 消费侧收口）**：7 前端文件（order/order-list/checkout/aftersales/me/store/data-orders）的状态字面量改引 shared `ORDER_STATUS`/`AFTERSALE_STATUS`（P3 生成物），新守卫 `order-status-frontend-via-shared`（位置感知·只咬比较/赋值/`status:`/`.includes` 逻辑位·UI 键/CSS 类同形异义不误伤）——账本#2「限调用方用常量」落地到消费侧·先红后绿+反向自检。详 `重构日志` 同日 P3 消费侧条。**余维持"顺手收"**：① 前端两包（miniapp/admin）env id（跨包 fiddly·收益低）。
31. ~~**[独立·根因#8] dashboard `orderBy('createdAt')` 真库会排除缺该字段的文档（质量回顾发现②）**~~ ✅ **已处理（2026-06-15·#18续）**：① GMV 已不再用 orderBy 样本（改 aggregate），orderBy 仅剩 recentOrders 取最新 5；② 确认 `createOrder` 必写 `createdAt`（createOrder.ts:141）→ **不变量成立**，orderBy 不漏单（已在 dashboard 注释标注依赖）。**余**：真库 orderBy 语义仍待真环境验（归 #19）。
32. ~~**[独立·产品] dashboard GMV 口径含未付单（质量回顾发现③）**~~ ✅ **已定（2026-06-15·#18续）**：GMV 口径定为**已付营收**＝aggregate `match(status ∈ {paid,shipped,done})` 求和；pending/closed 未付不计入「合计」。（GMV ＝毛营收·未减退款；退款另在售后链，net 营收若要另算再议。）
33. ~~**[独立·低危] throttle `_.exists(false)` 防御分支当前走不到（质量回顾发现⑥·2026-06-15）**~~ ✅ **已补测试覆盖（2026-06-16·保留防御非删·根因#8）**：`bumpWindowed` 窗口字段缺失时走 `exists(false)` CAS，但 throttleHit/throttleFail 用不同 key、各自窗口字段总与计数同设 → 该分支当前用法不可达、原未被真测。**改＝补测试逼出**（`tests/cloud/throttleDefenseBranch.test.js` 2 条）：构造混用场景（同 key 先建 hit 系列记录、再调 throttleFail；及反向）显式覆盖该分支，断言「本系列窗口字段被首设 + 计数落地 + 不踩另一系列字段」；逻辑本就正确故测试即绿，**反向自检**＝把 `_.exists(false)` 改 `_.exists(true)` → 2 测试均红（CAS 条件永不匹配缺失字段 → updated=0 重试耗尽 → 记录不写）→ 还原绿，证测试真咬。保留防御（不删），测试 326→328。

---

## 上线前占位（决策追踪）

> 状态：🔴 上线必做 · ✅ 已定 · 🗣️ 待再讨论 · ❓ 待确认。需求侧对应见 `需求清单.md`（R20 券/R23 店名/R25 视频/R26 域名/R27 协议）。

## 一、交易与履约

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 1 | 微信支付 | ✅ **已通**（2026-06-12 真机付款验证：订单 2026061216508470 真实交易号回写） | ✅ 已做 | 工作流通道（调试日志 J）；正册资产见工作日志；测试商品记得下架 |
| 2 | 售后 / 退款 | ✅ **已通**（2026-06-12 真机验证：¥2 真单申请→审核→原路退款→回调翻态全链路） | ✅ 已做 | 工作流通道（退款流 kbgzl_n8gojr3a）；正册资产见工作日志 |
| 3 | 物流轨迹 | **已验证可用（2026-06-15）**：订单页「查看物流」`plugin://kuaidi100/index?num=&appName=&com=` 打开快递100插件，**真机/工具验过——能打开 + 物流信息正确**（跳转写法 plugin-private→plugin://别名 修正·调试日志 O）；其它端/打开失败回退复制运单号 | ✅ 验证可用 | 插件 v`1.1.4`（别名 kuaidi100）+ `utils/express` com 选填映射 + 守卫 `express-plugin-wired`。**加载慢＝快递100 插件实时查件的固有延迟（第三方·咱不可控）**，传 com 可省插件自识别一步略快（映射表 13 家）。**余靠人**：隐私指引登记第三方插件（与㉒一起）。**依赖风险**：快递100 服务市场已下架，故复制兜底常驻。**＋发货合规见 #26**。**↳ 待权限·换官方组件（2026-06-27 用户拍板）**：微信官方「物流查询组件」(`logistics_detail_search`) 是快递100 的官方免费替代——小程序内查轨迹同款 UX（mp 后台示例同「查看物流→物流详情」流）、**无第三方**（消灭快递100 隐私债）、无需对接各家快递（可删 `express.js` 编码表）、自带「微信快递服务」订阅通知；后端复用已有 `kit/shipping.ts` `wxDeliveryCode`（微信物流编码）。**准入硬卡＝该小程序 90 天内有过支付行为**（未上线无支付记录→「去接入」按钮灰、现在接不了）+ 90 天无严重违规 + **类目白名单**（待用户核钩织材料包是否在表内，否则微信开放社区发帖申请）。**故上线只能先用快递100**；待上线后满 90 天有真实支付 → 接查询组件 + 拆快递100 + 反撤隐私第三方披露(㉑·守卫 `express-plugin-wired` 绑定·插件移除即不再要求披露·前向兼容)。排 **v0.9.6+** |
| 4 | 配送方式 | 结算页硬编码「顺丰·包邮」 | ✅ 已定·占位 | 用户确认**明确是占位设计** |
| 5 | 搭配购小件 | 钩针组/棉线包硬编码、不在商品体系 | ✅ 已定·占位 | 用户确认占位，**小搭配产品另有计划** |
| 5b | `refund_required` 单退款执行（结构审核#2） | 审核 P0 修：关单回补后晚到回调·库存售罄→进 `refund_required` 待退款态（钱已收·不超卖·`[LD_ALERT] PAID_BUT_OOS` 告警）。前端「待退款」+admin「待退款」chip 已可见 | 🟡 半边·待工具（2026-06-24 用户拍板延后·罕见边缘） | **超卖已堵死**（已闭环）；缺系统内一等退款闭环。**拿起时做全套**：① admin 专属 tab/告警入口（别只埋「全部」）② 明确管理动作 `markLatePaidRefunded` 或接退款工作流·记 `refundRequiredHandledAt/refundTransactionId` ③ 纳入 adminApi action registry + auditLog + 系统事实 + 测试 + 守卫。现靠 `[LD_ALERT]` + 商户后台凭 `transactionId` 手动退 |

## 二、营销 / 会员

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 6 | 优惠券 ¥20 | 每单无条件减 20 | ✅ 已定·占位 | 用户先前确认=开发期占位，P4 做真实券系统 |
| 7·8 | 幸运积分 / 订单备注 | ~~结算页占位行~~ | ✅ 已撤 | 2026-06-11 撤除结算页积分（暂无会员系统）+ 备注（不做）两处占位行 |

## 三、客服 / 互动

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 9 | 人工客服 | **已升级独立微信客服（2026-06-16·代码侧）**：用户给真 corpId/url，4 入口（详情坞/「我」/售后/播放页 ServicePanel）经 `utils/customerService.js` 的 `openCustomerService()`→`wx.openCustomerServiceChat({corpId,extInfo:{url}})`；展示组件 emit→父级调、页面直接调；非微信端/失败 toast 兜底；旧 `open-type=contact` 原生客服按钮 + 假 Toast 全退役 | ✅ 代码接通+真机验过·点客服直达微信客服（余后台接待配置·靠人） | 守卫 `customer-service-wired`（单源 corpId/url + openCustomerServiceChat + 4 调用点 + corpId 字面量单源 + 旧 contact 按钮绝迹）。**注**：单源文件落 `utils/`（非决策草拟的 `constants/`）——值的唯一消费者是 helper、co-locate 最内聚（守反冗余）。**真机验过（2026-06-16）**：点客服直达微信客服会话、人工消息正常流通（曾见「先进小程序客服→卡片中转企业微信」实为旧 `open-type=contact` 行为·新代码已无；排查耗几轮因真机误导入了 main 树旧产物·非 parallel-b 新构建）。**余靠人·纯后台配置非代码**：① **机器人/智能助手不响应**＝接待模式须设「优先机器人接待」(非「人工接待」否则机器人没机会)+机器人须绑此客服账号(kfid/kfcd…)+知识库有词条+部分功能需企业认证；② 会话内若仍弹「卡片→企业微信」＝后台欢迎语配置(删卡片即直达人工)；③ 小程序后台绑同主体企业 ID+配接待人。**费用**：基础免费；企业微信未认证有接待用户数上限，无限制需企业认证 ¥300/年 |
| 10 | 分享 | **已做（2026-06-15）**：详情页接微信转发（onShareAppMessage）+ 朋友圈（onShareTimeline）+ 分享按钮 open-type=share；分享卡由 `utils/share` 构造 | ✅ 已做 | 守卫 `detail-share-wired` + `share.test`（5 条）；其它端非发布目标保留 toast；**转发卡渲染/回跳真机验**（根因#8） |
| 11 | 收藏 | 详情「已收藏」假反馈、不保存 | ✅ 按 AI 初判 | 无会员体系下价值低，暂留占位，非阻塞（可随时翻案） |
| 12 | 播放页「更多」 | ~~Toast「更多（开发中）」~~ | ✅ 已撤 | 2026-06-11 删除播放页「更多」按钮（留等宽空位维持标题居中） |

## 四、内容与媒体（有管线，运营逐步填）

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 13 | 商品图片 | 大量灰占位，真实封面已能从控制台点亮 | ✅ 按 AI 初判 | 控制台传齐即可，运营逐步填 |
| 14 | 课程视频 | 多为占位/外链；小熊已有真视频，其余缺 | 🔴 已定·必做 | 核心卖点，上线前关键课要有真视频（AI 初判） |
| 15 | 首页买家秀 | 品牌样例文案（写死 3 条） | ✅ 按 AI 初判 | 可暂留品牌文案，或后续接真实评价 |
| 16 | 详情「为你推荐」 | 静态样例 | ✅ 已修 | 2026-06-11 排除当前商品自己（仍是静态样例，真推荐看后续需要） |
| 17 | 种子商品/课程 | catalog/seed 占位身份 + 占位课 course-duck | ❓ 已解释·待确认 | 见下「种子商品说明」；上线前确认占位假货已被真实商品覆盖/清除，不露给顾客 |

## 五、扫码 / 域名 / 印刷

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 18 | 扫码域名 | **✅ 已完成（2026-06-26 用户确认）**：自有域名 `www.luckyducky.cn/q/` 接 CloudBase + 微信「扫普通链接二维码」规则已配 + 真机扫码验过跳小程序激活 | ✅ 已做 | 自有域名+微信规则闭环（详 `现状与路线`§runbook #6）；余 `urlCheck` 随合法域名配齐+发版翻 |

## 六、品牌 / 清理

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 19 | 店铺名 | **已定名（2026-06-15）**：「Lucky Ducky 小棉鸭」；旧占位「易织™小棉鸭®」6 处硬编码（order/checkout/welcome/BrandIntro/GroupPanel/productDetail）+ 协议页「本店」自称全替，收口 `constants/brand.js` 单源 | ✅ 已定 | 用户拍板店名＝「Lucky Ducky 小棉鸭」；守卫 `brand-name-single-source`（旧占位绝迹 + 字面量只在 brand.js）；shop 行保留「官方旗舰店」descriptor。**真机抽查显示**（根因#8） |
| 20 | pending-pay 孤页 + `?status=` 演示路径 | 无入口样例页 | ✅ 按 AI 初判 | P4 清理（技术债 #8） |

## 七、账户 / 协议（2026-06-14 加，随登录页落地）

| # | 占位点 | 现状 | 状态 | 决策 / 备注 |
|---|---|---|---|---|
| 21 | 用户协议 / 隐私政策 | **正文已补（2026-06-15）**：`pages/agreement` 改为结构化条款（用户协议/隐私政策各 10 条「第N条」，含退货权法律节点 R4、第三方=微信支付/云开发、**不采集手机号**、用户权利），标注「初稿」 | ✅ 用户拍板通过（2026-06-27·无外部法务·经营者自担风险） | 守卫 `agreement-text-real` 防回退占位；店名暂以「本店」自称（待 R23/㉓ 定名替换）；**正式发布前须经法务复核定稿**。**✅ 措辞已修（2026-06-18）**：原 blanket「不采集手机号」与 `address-edit` 手填收货电话矛盾——隐私政策两处（intro + 第一条）改为「本小程序不通过微信授权获取你的手机号（收货联系电话由你自行填写、仅用于配送联系）」；守卫 `agreement-text-real` 必含承诺从「不采集手机号」收紧到「不通过微信授权获取」（先红后绿+反向自检）。**✅ 第三方披露事实对齐（2026-06-27·`601c4ce`）**：隐私政策第三条原漏「快递100」（物流轨迹经其插件共享快递单号·与隐私指引登记清单不一致）→ 如实补 + 守卫 `agreement-text-real` 扩断言政策须列快递100（事实对齐·非法律判断·与 express-plugin-wired 绑定·反向自检过）。**✅ 用户拍板通过上线（2026-06-27·无外部法务·经营者评估后自行接受法律风险）**：页面去「初稿·待法务复核」用户可见提示（改客服联系语）+ 脚本注释记决策；CC 边界＝只做事实对齐（说的=做的）、法律充分性归用户自担（[[cc-scope-mechanical-not-legal]]）。**㉑ 由🔴转✅·随 v0.9.5 republish 上线。** |
| 22 | 微信「隐私保护指引」 | **✅ 已完成（2026-06-26 用户确认）**：代码侧已接（2026-06-15·manifest `__usePrivacyCheck__:true` + `usePrivacyGate` + `PrivacySheet` 授权弹窗 + App 启动注册 + profile-edit 挂载·守卫 `privacy-authorize-wired`）+ **mp 后台已登记《隐私保护指引》+ 真机验过授权弹窗** | ✅ 已做 | mp 后台登记+真机闭环。注：mp-weixin 现无涉隐私接口调用（登录静默 / 头像 chooseAvatar / 昵称 input 均不受隐私闸约束），弹窗为前瞻接入；纯状态逻辑有 `tests/privacyGate.test.js` 兜底。**挂载范围 < 闸触发范围债见 #25**（接第一个隐私接口前补）。**2026-06-16 用户真机跑过**（未触发弹窗·因当前无隐私接口被调·符合预期）；**mp 后台《指引》登记仍未做**（用户曾不知此步），真机「没事」≠ 隐私闸验过（根因#8） |

---

## 附：种子商品（seed）说明
「种子数据」是开发期为让系统开箱能跑而预灌的**样例假数据**，不是功能：
- 上架前数据库需先有商品否则首页空白，故写死 5 条占位商品（小棉鸭礼盒 ¥198 等）+ 1 门占位课（course-duck，有样例章节名、无真视频），用 `seedProducts`/`seedCourses` 灌入。
- 名字/价格/课程内容全是编的。控制台上架的真实商品写**同一张 products 表**会覆盖/补充。
- seed 现仅剩兜底作用：① 网页端无云时本地回退展示 ② 库被清空时初始兜底。
- **上线前动作**：确认占位假货已被真实商品覆盖或清除，不让编造商品露给顾客。

## 附：隐私指引登记照抄清单（㉒·2026-06-16 扫码核实采集点）— 去 `mp.weixin.qq.com`→设置→服务内容声明→《用户隐私保护指引》照填（基于代码实采点·非凭空）

| 收集的信息 | 用途话术 | 给第三方 |
|---|---|---|
| 微信昵称、头像 | 完善个人资料、订单与评价展示 | 否 |
| 相册（选取图片） | 仅你主动更换头像时读取所选图片 | 否 |
| 收货人姓名 | 订单配送 | 否 |
| 手机号（手动填的收货电话） | 配送联系，**不通过微信授权获取** | 否 |
| 收货地址（省市区+详细） | 订单配送 | 否 |
| 快递单号 | 查询物流轨迹 | **是→插件「快递100」**（provider `wx6885acbedba59c14` v1.1.4） |

- **明确不采集（别勾）**：地理位置（`getLocation` 0 处）、麦克风、摄像头、通讯录、`getUserProfile/getUserInfo`（0 处）、微信授权手机号（`getPhoneNumber` 0 处）。
- 填完→提交微信审核（几天）→ 审过后真机隐私授权弹窗才真正生效；**未登记发布提审会被打回**（第三方插件栏：快递100 见上表）。

## 待办收束（按状态）
- **🔴 必做**：⑭关键课真视频 —— ①支付②退款已通；**⑱扫码域名 ✅（2026-06-26 微信扫码规则+真机闭环）、㉒隐私授权 ✅（2026-06-26 mp 后台登记+真机闭环）、㉑协议 ✅（2026-06-27 用户拍板通过·无外部法务·经营者自担风险）**；均在计划内，无意外硬骨头。
- **🗣️ 待再讨论（用户已批注）**：⑰种子商品（已解释，待确认覆盖/清除） —— 下次讨论逐条定。（③物流 2026-06-15 已定＝快递100 免费插件 + 发货合规手动录入#26；⑨客服已做＝微信客服 open-type=contact，余接待人 mp 后台配·外部）
- **✅ 已做（2026-06-11 本批）**：⑦积分撤、⑧备注撤、⑫播放页更多撤、⑯推荐排除自身。
- **✅ 已定**：③物流＝快递100 免费插件（余外部前置）+ 发货合规手动录入(#26 上线必做)；④⑤⑥占位保留；⑨客服代码侧已做（微信客服 open-type=contact，余接待人 mp 后台配·外部）；⑩分享已做、⑪暂留；⑬⑮运营填；⑲店名定名「Lucky Ducky 小棉鸭」(2026-06-15 收口 brand.js 单源)；⑳P4 清理。
