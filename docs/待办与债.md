# 待办与债（技术债 backlog + 上线前占位）

> fable 重构线自己的技术债/占位账本。分叉前（next 仓的完整技术债/发版清单/占位决策记录，含优惠券·配送方式·物流·种子商品·隐私登记清单等仍可能有效的产品占位细节）已归档：`docs/archive/待办与债-next基线-至20260703.md`——**里面的具体占位点决策没有失效，只是挪出活文档**；查某项占位当时怎么定的，去那份查。长期决策的权威源仍是 `docs/关键决策记录.md`。

## 冻结记账（重写期 · 2026-07-03 起 · 解冻条件 = M5 切换完成，见 `现状与路线.md §路线图`）
- **上线剩余项挂起**：v0.9.5 republish（不再发）· 关键课真视频 · 真机 smoke——随重写新版在 M5 一并上线（明细见 `archive/待办与债-next基线-至20260703.md`）。
- **功能线挂起**：客服360 M③ 内部 VIP · 进销存真单验收（SCM-Z）+ 期初盘点——重写后在新结构上做；进销存/M⑦/节点拍照经用户确认**未投产**（2026-07-03），按可重建资产对待。
- **线上止血通道**：紧急修复走 next 仓（回滚基线）；本仓 M5 前不对旧产物迭代。

## M2 未搬面（2026-07-04 收口清点·明细见 `rewrite/mp/README.md §已知未搬面`）
- **M5 前必补（合规）**：✅ 已补 2026-07-04（隐私授权闸+consent 页·守卫 rw-mp-privacy-gated）；剩用户侧＝mp 后台《隐私保护指引》登记对齐 + 真机验（M5 前置闸2/3 内）。
- **待补非阻塞**：课程目录页 · 播放页帮助视频抽屉 · 订单物流轨迹（复制/插件跳转）· 分享卡。
- **渲染引擎拍板**：M2 首发全 WebView（spike 增益未证实·防过度工程）；Skyline 留上线后按页灰度。

## M3 低频待办（不阻塞演练与切换）
- 激活卡双面设计器（印刷用·旧台有）· 二维码图片打印导出（qrcode+jszip·旧台有）· 视频直传失败的分片回落通道。明细散见 `rewrite/admin/README.md` 与重构日志 M3 各批。

## M3 UI 换皮·设计稿超出后端数据源的元素（不编数·记账待后端·2026-07-05 批2 起）
- **数据看板趋势%与迷你柱**（design S12：每卡 +8.2%/环比柱）：`getDashboard` 只给当前累计值、无历史序列——趋势需时间窗聚合 action（本页已省略趋势、只渲染真值卡）。
- **数据看板热点/卡点段位分析**（design S12：最多看完段/最多停留段 Top5）：需按视频段聚合学员观看记录的学习分析 action（当前无）——本页省略这两块面板。
- **工作台·待办中心的「最近动态」流**（design S15）：需跨订单/退款/激活的统一事件流 action（当前无）——落地 S15 时该块用真空态或省略，不编流水。
- **今日速览的「今日」口径**（design S15：今日订单/GMV/新增用户/课程激活）：现有 stats 为累计值——落地时按真实可得口径命名（累计/本周），不假标「今日」。
- **商品列表「6 步上新进度」圆点**（design S11：产品图片→商品信息→SKU→教学视频→二维码卡片→码批次 各步完成态）：需上新向导（S5-S9）产出的分步完成态；当前 DraftRowVM 只有封面/名/价/规格聚合值——落地向导批时补该列真值，当前列表省略该列（不按字段瞎推 6/6）。
- **订单/售后列表搜索仅过滤已载页**（design S8/S10 搜索框）：当前 listOrders/listRefunds 无 keyword 参数，UI 搜索是客户端过滤已加载行——跨全量搜索需服务端加 keyword 过滤 action；订单「改单号/订单详情面板」（design S8「改单号」+ VMlhp 详情面板）需改运单/查详情 action，当前无，UI 未加假按钮。
- **二维码批次跨课「全部」列表 + 课程下拉**（design S9）：后端 `listBatches` 强制 courseId、无列全量批次 action——UI 按课程范围查（先输 course-xxx）；跨课全量总览需后端加「列全部批次（可选按课程/状态筛选）」action。
- **批次码表单码激活态**（design S9 详情抽屉：每码「已激活/未用」chip）：`listBatchCodes` 只回码 `_id` 串、不带 status——UI 码表只列码 + 看整体激活率，不标单码状态；补齐需 listBatchCodes 随码返回 status 字段。
- **印刷包 ZIP 下载 / 二维码图导出 / 测试二维码**（design S9 抽屉按钮·旧台 qrcode+jszip）：既有 M3 低频待办，UI 未加假按钮（导出 CSV 已真实现·图/ZIP 待补）。
- **库存批量导入/盘点**（design S14 头部按钮）：saveStock 仅 per-SKU、无批量导入 action——UI 未加假钮；补齐需后端「批量盘点/CSV 导入」action。低库存阈值（design「低于阈值 10」）为前端显示常量，后端无 per-SKU 阈值字段——如需按 SKU 定制阈值须加字段。
- **订单批量发货面板 / 电子面单取号 / 订单详情面板**（design OLQG3/XqXZ5/VMlhp·note qlcwr）：需批量 shipOrder + 电子面单取号 + 订单详情 action（当前 shipOrder 仅单单、无取号/详情）——订单页批量操作留待后端补齐。
- **财务对账微信手续费/实际到账 + 月份筛选**（design S16：手续费≈0.6%卡/微信实际到账卡/月份下拉/导出 Excel）：手续费与结算实收需微信结算数据源，getReconciliation 只给应收/退款/净额——UI 只作真等式+账单勾对、不伪造手续费估算；月份筛选需后端 getReconciliation 加 month 参数（现为近 30 天固定窗）；导出改客户端 CSV（Excel 待后端或前端 xlsx 库）。
- **消息与通知 S17（JxJm7）暂未建**：企微告警 webhook 已有（getSettings/saveSettings）；但「推送事件开关」（新订单/待发货堆积/库存预警/交易异常/退款/日报 + 订阅消息 发货/物流/激活/完课/复购）需通知偏好后端字段——getSettings 未必含，不建无持久化假开关。落地前先确认 settings 是否有 notifyPrefs，或后端加字段；且与既有 Settings 页重叠，需定位（并入 Settings 或独立页）。
- 上述均为 UI 层已按设计视觉就位、数据接口缺口；补齐是后端 action / 上新向导工作（非当批），补齐前 UI 诚实省略/空态、不占位假数。

## 深审残余（2026-07-05 全局深审·三路并行·P0/P1=0·机器可修 F1-F3 已修，下列为待人/待定项）
**已拍板（2026-07-05·决策§26）**
- **客户端「一行一售后」定为刻意**：applyRefund 拒后同行永久锁死（`orders.ts:307` + 确定性 `_id`）＝有意——每行只申请一次退款，复议走客服端。**结案不改客户端。**
- **客服端管理员越规退款＝新增能力**（已落 2026-07-05·`overrideRefund` action·cap `refund:manage`）：管理员凭退货管理权限越过资格规则（一行一售后锁/拒后锁死/已进课不可退）主动发起退款；**保留钱守恒**（退款 ≤ 该行实付分摊 − 全单已退·`refundShareFen` 单源封顶，越额被 NOTHING_LEFT 挡）。净新增、超出旧线 92 parity（决策§26 显式授权）。**留口**：若将来需「善意补偿超分摊额」＝另立补偿动作，非本动作。

**mp 真机走查窗口期·记账不动行为面（根因#8·不搅用户即将走查的表面）**
- **my-courses 加载失败回退「去激活」空态**（`rewrite/mp/pages/my-courses/my-courses.ts:14`·P2）：有课用户遇网络抖动被误导以为课丢了；home 已做对（loadFailed 标志）。真机走查后按 home 范式补 loadFailed 分支。
- **order-list/reviews/aftersales 加载失败与空态不分**（P3·无重试入口）；**成功后 setTimeout(navigateBack) 无 onUnload 清理**（checkout/feedback/review/profile-edit·手动返回可多弹页·P3）。
- **订单号随机双击可建重单+双预留**（`orders.ts:40-47,177` + mp `checkout.ts:62` submitting 在拉起支付前即释放·P2）：限频 20/60s+超时关单兜底、错向不超卖；建议 submitting 贯穿到支付结束。mp 侧改，走查窗口期挂账。

**低危纵深·非阻塞（P3·可延后或刻意）**
- rewrite 线缺 transition-declared 前端消费侧守卫（`order-status-frontend-via-shared` 只扫冻结线 mp）——本批已补 cloud 侧 `rw-order-transitions-declared`；mp 侧状态字面量守卫待 M2 真机后补。
- kfSend 本体无会话归属自校（授权靠调用方 scopedLoad 前置·与旧线设计一致）；频控 best-effort（写失败静默不锁·错向安全侧）；agent pollThread 不自识 SESSION_LOST（10s 兜底）；Inventory/ScmPurchase 无前端 busy 闸（后端 CAS/两步确认兜底）；recallScan 并发双跑可重复推一次运营汇总（非钱链）。

## 挂起问题（用户拍板「最后一起处理」）
- **退款工作流平台侧失效（调试案 A·2026-07-04）**：真单联测退款触发失败，败点隔离至控制台资产 #3 `kbgzl_n8gojr3a`（6/14 后平台侧漂移·被删/未发布/平台改版三选一）；取证步骤已给用户（tcb.cloud.tencent.com → 工作流 → 状态+运行记录），修复按 `console-assets/01` 重建手册第 5 步；修好后**重审批**售后单 `2026070413019480__pmq8yfwvp__sku测试文案` 即原路退回 2 元（无需新单）。代码半边（callFlow 吞错）已修（M2 批9·守卫 `rw-flow-observable`）。解冻条件：真单联测二次跑通退款链 = 结案。
