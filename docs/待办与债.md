# 待办与债（技术债 backlog + 上线前占位）

> fable 重构线自己的技术债/占位账本。分叉前（next 仓的完整技术债/发版清单/占位决策记录，含优惠券·配送方式·物流·种子商品·隐私登记清单等仍可能有效的产品占位细节）已归档：史料「待办与债-next基线-至20260703.md」——**里面的具体占位点决策没有失效，只是挪出活文档**；查某项占位当时怎么定的，去那份查。长期决策的权威源仍是 `docs/关键决策记录.md`。

## 云函数拓扑收编提案·✅ 已拍板执行（2026-07-23·代码侧本批落地·云端切换步骤见 运维手册§1.6 收编 runbook）
- **提案 A（收编三函数·17→13 部署单元）**：`kfSend` / `kfMedia` / `genQrcodes` 并入 kit/域内直连——`kfSend`→`kit/wecom.ts` 助手 `kfSendText`、`kfMedia`→同文件 `kfFetchMedia`、`genQrcodes`→`adminApi/actions/batches.ts` 内部函数 `generateQrcodes`。**代码侧已落地**（本批：三个独立函数文件已删、唯一调用方 `agentDesk.ts`/`batches.ts` 改直连调用、测试挪靶、`npm run build:rw-cloud` 产 13 函数）。
- **提案 B（`seedProducts` 退役）**：**已删**（函数 + 种子源 `shared/seed/products.ts` 一并删除，`initDb`/`seedCourses` 保留）。
- **不推荐**：定时器六函数合并——触发器粒度/超时预算/故障隔离各自独立是刻意设计，合并省不了实际成本反丢隔离。
- **云端切换（人工·待执行）**：部署 `adminApi`/`adminApiV2` → 开通固定公网出口IP → 冒烟（发消息/发图/生成码批次）→ 控制台真删 `kfSend`/`kfMedia`/`genQrcodes`/`seedProducts` 四个云函数——完整步骤见 `docs/运维手册.md` §1.6。

## 瘦身拍板批新记债（2026-07-23）
- **窄债（getMediaUrl DOWNLOAD_FAILED 分支不可达·拓扑收编批施工发现·既有设计非收编引入）**：kit/wecom.ts `getKfMedia` 对任何 JSON 错误响应恒回 `expired:true`（其头注自认「错误码未细分」），致 agentDesk getMediaUrl 的 `MEDIA_DOWNLOAD_FAILED` 分支经真实链走不到、一切下载失败都报 EXPIRED——修法：getKfMedia 按 errcode 细分过期 vs 其他失败；对应测试已如实断言现行为并注明（app-admin6）。
- **窄债（saveHomeContent 写路径零测试）**：需求映射表迁活线时发现 admin 写 action `saveHomeContent`（content.ts）无任何测试覆盖，只有读侧 getContent 被测——补一条写路径测试（含 sanitize 面）。
- ~~flag（skills 文本旧线残留）~~ **✅ 已处置（2026-07-23 瘦身大作战）**：`/frontend-check` skill 退役删除（uni-app 专项坑随旧线无对象·新线真机走查手册=rewrite/mp/README.md）；browser-check/capacity-check 文内引用同步改指。

## mp↔cloud 响应契约哨兵覆盖面（2026-07-19·B10）
- **窄债（按热度逐批扩）**：契约哨兵第一批只锁钱链/学习链 5 action（createOrder/pay/getMyOrders/getOrderById/getPlaybackUrl·守卫 `rw-app-response-contract`），app 其余 action（getCourses/getMyCourses/getMyAfterSales/getReviews 等）响应形状仍无哨兵——按热度逐批扩、别一口吞（B10 拍板宁窄勿宽·activateCourse 已有整形断言不算缺口）。

## Orders.vue 发货无 twoStep 二次确认（2026-07-19·B9）
- **flag 拍板**：`shipOrder` 本身无 twoStep 二次确认（开抽屉填表单一次提交，闸门是公司/运单号必填校验，不是同一按钮点两下）——要不要照 Products.vue 上架的 twoStep 模式给发货也补一道二次确认，留用户拍板（本批只补测试地基、未擅自设计缺口）。

## npm audit 存量未清·report-mode 起步升级前置（2026-07-19·B8）
- **flag 待认领**：CI `npm audit --omit=dev --audit-level=high` 报告模式起步（`continue-on-error: true`）——13 条 high（0 critical，2026-07-19 快照，会随 advisory DB 更新漂移）未清；`ci-audit-step-present` 守卫只钉步骤在位，不判定闸门级别。
- **升级 blocking 前置**（二选一）：① 逐条核对 breaking change 面后清存量；② 显式接受风险记账后再摘 `continue-on-error`。
- 非 `--force` 的 `npm audit fix` 只能修 1 个 moderate（postcss），对 13 条 high 无帮助（已本地 dry-run 核实，全部需要 `--force` 且都是 breaking：`@dcloudio/uni-mp-weixin` 等）。

## code-gaps 战役批B5 强更+电商漏斗埋点+冷启动遗留（2026-07-19）
- **flag 部署提醒**：本批新增 `wx.showModal` 文案触发品牌字体分层子集扩容重建（本机源 OTF+pyftsubset 可用，已真实重建并提交 6 个 woff + 3 个 txt，v2 版本号不变），托管 `/fonts/` 尚未同步——下次部署窗口须 `tcb hosting deploy` 覆盖这 6 个 woff，否则真机字体子集与仓内产物不一致（新增字仍会掉系统字体，直到部署落地）。
- **flag 真机验证**：`wx.getUpdateManager` 三回调（devtools 模拟器行为与真机不一致、可能不触发）+ `events` 集合 7 个新 trackEvent type 落库核对，均需线上已发布至少两版本后真机验证，代码侧仅证明「接线正确」。

## code-gaps 战役批B4 mp 主包体积守卫遗留（2026-07-19）
- **flag 拍板**：`rewrite/mp/project.config.json` 不入库＝上传包边界开关（`ignoreUploadUnusedFiles` 等）无版本控制单源——是否挪出 `.gitignore` 待用户拍板（涉 CLAUDE §8 约定变更，本批不擅动）。

## 批B6 审计日志读出口遗留（2026-07-19）
- **flag 待走查（根因#8·web 端「check 绿≠能用」）**：新页 `rewrite/admin/src/pages/Audit.vue` 未过 `/browser-check` 浏览器实机走查——`npm run check` 全绿+反向自检过≠真机渲染/交互已验，留待下次 admin 演练窗口一并验（筛选表单/翻页/时间窗三控件重点）。

## 部署状态实况（2026-07-20 Phase3 战役部署后·下次部署前必读）
- **已上线**：本战役 16 个云函数产物 + `adminApiV2` 别名（后台前端真正调用方）+ `/admin`（139 文件·线上入口 hash 与本地构建逐字一致）+ `/agent`（3 文件）。探针：adminApiV2 错口令 401 `BAD_KEY`（闸活）、app 新 action `getProductDetail` 被识别（证跑新码）。
- ✅ **云端已拉齐 main（2026-07-20 15:07–15:59·用户令「把云端拉齐到 main」）**：按 main `cbbc8ca` 重新构建 17 产物并全量部署（18 次＝17 产物 + `adminApiV2` 别名·含另一线的 `billReconcile`），`tcb fn list` 逐个核验时间戳全落本次窗口、状态「部署完成」；`.deploy-manifest.json` 已重建（`--dry-run` 复核「变更待部署 0 个」＝云端与代码库正式对齐）；冒烟：adminApiV2 `ping` → 200 `{ok:true}`。**此后每次改动照常跑 `DEPLOY_ALLOWED=1 node scripts/deploy-fns.mjs` 即可增量部署**（批M 后自动展开 adminApiV2 别名）。
- **部署环境坑（实测·下次直接照做）**：① 托管上传必须绕开 HTTP 代理（`env -u http_proxy -u https_proxy tcb hosting deploy …`，否则 502 tunneling socket；云函数部署不受影响）；② COS 上传偶发 60s 超时与 `Bad Gateway`，重试即过（客户端超时后服务端有时其实已成功，重试前先 `tcb fn detail` 看时间戳，别盲目重传）；③ 单次部署耗时长，用小批前台跑比后台常驻稳（后台任务会被回收）。
- **云端仍跑 17 函数、拓扑收编切换待人工（2026-07-23）**：代码侧已收编（13 函数产物），云端尚未切换——待办：部署 `adminApi`×2（含别名）→ 冒烟 → 控制台删 `kfSend`/`kfMedia`/`genQrcodes`/`seedProducts` 四个云函数（步骤见运维手册§1.6）。

## Phase3 全局清零战役新增（2026-07-19·9 代码批已落分支 worktree-phase3-epic-clean·PR #46·详见 重构日志 战役总账条）
- **✅ 已按方案 A 落地（P2·钱链·refund_required 死信无在库退款路径·用户 2026-07-19 拍板·批K）**：`overrideRefund` 状态闸放行 `refund_required`（钱守恒 `refundShareFen` 行/单双口径封顶零改动·客户自助 `applyRefund` 不扩·`closed` 仍拒＝那是钱没收的正常关单）。**联动**：`kit/inspect.ts` B2 死信判据同批升级为「`refund_required` **且钱还没退清**」——退款成功后订单状态不变（refundCallback 只回补库存+写对账痕），不改判据的话退清的单会被永久报死信淹没真死信；已退额按售后单 `status==='refunded'` 的 `refundAmount` 求和（`_.in` 分块批量读·无 N+1·逐行 `toFen` 后整数分比较），查不到/查询失败/实付额缺失一律保守计死信（fail-closed）。**「退清」分母＝该单实际可退上限**（逐行 `refundShareFen` 分摊上限之和），不是 `order.amount`——多行订单分摊逐行四舍五入、行间不共享余数预算，「各行都退到顶」仍可能差几分且无入口能追回，用 `amount` 当分母会让这类单永久误报高危死信（review P1 回补）。详见 重构日志 批K 条。
- **flag（余量收窄·2026-07-23 瘦身批）**：packages/ 已删（用户拍板）；`cloudbaserc.json` functions 清单批L 已迁活线（拓扑收编批后 14）；**functionRoot 已订正**为 `rewrite/cloud/dist`（同批·2026-07-23 拓扑收编批顺手改，原「刻意悬空 packages/cloud/dist」状态解除）——**唯**下条「cloudbaserc.json 迁移」flag 描述的 `tcb fn detail` 抄网现值（timeout/memory 真值核对进 console-assets）尚未做，仍待办。
- **免修判例（记档防重报）**：① my-courses/order-list reload 失败 toast 无失焦复核（P3·`rw-mp-await-side-effect-unloaded-recheck` 守卫族刻意只覆盖支付/导航类·瞬态观感不建机制）；② Reconciliation stale-reload 文案拼接边角（P3·批E 评审 deferred·需极窄并发窗+仅影响提示观感）；③ 批B cover 豁免过宽与 seedProducts 游离点（刻意取舍·理由见重构日志批B 条）。
- **观察项**：`app-orders.test.ts` 既有时间戳撞键类 flaky（批D 评审全量跑撞见一次·单跑必过·未被本战役任何批触碰）——再犯就按 /systematic-debugging 立案根治。

## 盲区体检拍板清单（2026-07-18·批1-5 已修守卫扫描面/部署链/产物闸/钱链检测迁活线·详见 `重构日志` 同日五条·完整报告 artifact 链接在会话）
- **flag 拍板（上线阻塞·合规四件）**：①**注销账户**——隐私政策（`rewrite/mp/lib/mapPages.ts:211`）白纸黑字承诺「删除个人信息或注销账户」、全仓零实现（个保法 47 条可核实违规）：最小修=admin 手工删除/匿名化 action + 运维 SOP；**注销语义须拍板**（订单/支付记录依电商法留存 vs 个人身份字段匿名化的边界）。②内容站 footer 补 **ICP 备案号**公示+beian.gov.cn 链接（域名备案已办≠页面公示义务已履行）。③**营业执照/主体信息**公示（电商法 15 条·小程序「关于」页或官网 footer）。④**发票方案**——全仓零记录（连「客服人工代开」都没写=遗漏非决策），至少一句进 `需求清单`。
- **flag 拍板（评价后台）**：`reviews` 集合零管理面——用户带图评价商户**看不到也下不了架**（漏检辱骂/投诉只能控制台裸改库；同病的 `feedback` 已记债、reviews 此前无记录=不对等）。修法：adminApi 补 `listReviews`/`hideReview`（hide 需 mp `getReviews` 过滤联动）或先只读列表页。
- **flag 拍板（两处 UGC 零内容安全）**：客服会话用户文本（`cs/kfCallback` 全链零 `msgSecCheck`）与 `submitFeedback` 正文（仅长度裁剪）——对照评价/头像/诊断照片三处 fail-closed 覆盖是遗漏非取舍；但接入的 fail-closed 代价同「一键报障」flag（安全接口抖动=顾客消息/反馈交不上去），须拍板后接。
- **flag 拍板（值班表·人的事一页纸）**：客服值守时段（无人值守时「转人工」静默超时零告知——覆盖不了先把话术改成显式告知在线时间）/退款审批时效目标+超时提醒/发货责任人/对账频率（自动侧已由 `timers/billReconcile` 每日兜底·批B2 2026-07-18，值班表只定人工核账节奏）。技术侧配套归 inspect 升格批（下条）。
- **flag 另立批（inspect 升格=修法③·数据面长期项打包）**：B 类不变量扩册——库存守恒（`materials.stock`≡SUM(stockLedger)·scmStock 并发丢移动路径已亲核·全仓无库存对账）/VOD 转码卡住/账单差异计数（✅已由 2026-07-18 批B2 `timers/billReconcile` 每日自动对账覆盖——wxOnly/金额不符→anomalies+告警·本批不再重复做）/`ALLOW_MOCK_PAY` 生产哨兵（唯一「意外存在=灾难」方向开关·配置清单只查该填的填没填）/站点可达/证书到期倒计时；集合**保留期正册**（`conversations`+`cs-media` 无界增长·TTL 清理器四条删除扫描无索引=自噬循环·`cleanupEvents` 失败 catch 与无事可清同形改告警·索引期望补进 `console-assets/03`）。台账既有种子=观测批 A5「定时器心跳」。**病根#17（快照外无传感器/义务无建模）随本批立册**（本战役只立了 #16）。
- **flag 另立批（cloudbaserc.json 内存/超时真值核对·functionRoot+函数清单已随 2026-07-23 拓扑收编批订正为活线口径）**：`envId=生产`+`functionRoot=rewrite/cloud/dist`+活线 14 条清单（含 timeout 配置，2026-07-23 拓扑收编批已迁）——**唯各函数 timeout/memory 数值是否与现网真值一致仍未核对**（`console-assets` 正册暂缺这份逐函数现网快照，凭空改这两项会在下次 config 应用时降级生产配置）。**做前必须** `tcb fn detail` 逐函数抄回现网内存/超时真值进 `console-assets` 正册再改。批3 已让 deploy-fns/deploy-test 显式 `--dir` 不吃它的 functionRoot。
- ~~flag 另立批（25 条 reverseTest 仍指冻结旧线测试）~~ **✅ 已清账（2026-07-23 瘦身拍板批）**：25 条全额改指活线等价测试（transition→transition.test.ts / userWrites→ids.test.ts 等·guard-coverage ②b 复核在位）；`admin-actions-audited` 旧线守卫随线退役（活线审计面由 rw-cs-360-read-audited + admin-action-audit-logged（已改指活线测试）覆盖）。
- **✅ 已迁活线（preflight.mjs+lib/deploy-drift.mjs·盲区体检批5）**：MONEY_CHAIN 旧线 6 独立函数名改为活线 4 项（`app`/`payCallback`/`refundCallback`/`closeExpiredOrders`，`createOrder`/`pay`/`applyRefund` 等折叠进 `app` 单一产物）；preflight 钱链检测目标由 `packages/cloud/dist` 改指 `rewrite/cloud/dist`；顺手退役两条旧线专属断言（`build:cloud 产物≡cloudbaserc` 已由 `deploy-config-complete` 覆盖、`cloudbaserc functionRoot` 覆盖面移交本节「cloudbaserc.json 迁移」flag）；`rw-toolchain-no-oldline` 白名单 10→8。详见 `重构日志` 同日批5条。
- **✅ 窄债已清（CLAUDE.md 身份段云函数计数）**：原「15 云函数」旧述经 Phase3 批I 订正 16、2026-07-20 merge 并入 billReconcile 后随手订正 17（真值 `系统事实` §13·`function-count-synced` 焊死）。
- **窄债（hashFnDist 算法两处手动同步）**：`scripts/lib/deploy-drift.mjs` 的 `hashFnDist` 与 `scripts/deploy-fns.mjs` 内联 hash 循环各自独立实现同一算法（index.js+config.json+package.json sha256）——未抽共享函数，因 `openapi-perm-declared` 守卫用正则原文锚定 `deploy-fns.mjs` 自身源码，统一实现需连带改该守卫正则，超盲区体检批5范围；靠两处头注互指维持一致，改一处务必同步改另一处。待后批评估是否值得统一。
- **flag 顺手批（app 网关查表命中 Object.prototype）**：`ACTIONS[action]` 对 `constructor`/`toString` 等原型成员非空——「未知 action fail-closed」在原型链上不成立（P3·无鉴权实害·响应契约破口）。一行修：`Object.create(null)` 承载或 `hasOwnProperty` 判定 + golden 负向用例补 `action='constructor'`。
- **flag 待用户（工作区积压分诊）**：17 分支领先 main 未合并 + 26 个残留 worktree（批1 已让它们不再污染闸·但积压本身该分诊：合/关/标长期实验）+ **PR #29 非 draft** 挂着。
- **flag 部署提醒（批3 连带）**：`.deploy-manifest.json` 已删=诚实未知态——下次 `DEPLOY_ALLOWED=1 deploy-fns` 会报全部 16 个待部署（含 `app` 落后半月的积压正好一并上），部署仍是人工授权动作、过 guard-deploy 闸；上线前对照 `docs/人工配置清单.md`。

## 课程链路审计残余项·待用户拍板（2026-07-17·机械修复批已落 16 条·详见 `重构日志` 同日条）
- **flag 拍板（P1·钱链·confirmEnter 退货权撤销 CAS 耗尽无补偿）**：enteredAt 抢占成功后订单行 refundable 撤销若 3 次 CAS 全败只 alert 不回滚——用户已能看课但订单行仍显示可退，refunds.ts/orders.ts 退款判据只读订单行不查 activations，可批准「已交付课程+退款」。两修法待拍板：A 退款侧联查 activations.enteredAt 兜底拒（改钱链读路径·彻底）；B 后台补偿任务重试推进 enteredQty（不碰退款路径·最终一致）。本批未动（钱链审不动刀纪律）。
- **flag 拍板（P2·throttle CAS 耗尽 fail-open）**：并发争抢耗尽 20 次重试的失败计数等于没发生（爆破可借洪水稀释计数）。本批只加 `CAS_EXHAUSTED` 告警；改 fail-closed（耗尽即拒）是安全/可用性权衡——频控库故障时会连带拒掉 login/trackEvent 等用户写，与头注「best-effort 不阻断主流程」刻意取舍相抵，须用户拍板。
- **flag 拍板（P1·checkpoints 学员端零入口）**：节点诊断拍照上传后端（submitCheckpointPhoto·鉴权/频控/imgSecCheck 齐）+ 管理端（Checkpoints.vue）+ 客服360面板全建成，但 rewrite/mp 全仓零调用——学员实际用不了，功能形同不存在。拍板：播放页/目录页补拍照入口，或需求文档标「暂缓·仅坐席侧」防团队误以为已上线。
- **flag 拍板（一键报障设计·审计设计提案）**：播放页求助面板加第 4 卡「反馈问题」内嵌小表单，自动带 courseId/segmentId/播放秒/机型/网络（字段扩现有 feedback 集合·不建新集合不建工单系统）+ admin 新建「用户反馈」列表页（照抄 Anomalies.vue 骨架——当前 feedback 集合写了后台没人看得见）+ feedback.content 接 msgSecCheck 的 fail-closed 取舍（内容安全接口抖动时反馈会交不上去）。三项待拍板后另立批。
- **✅ 已拍板已落码（转码管线·播放丝滑第一杠杆·2026-07-18 决策§31 选 VOD）**：批0/1/2 已落地（播放前缀分流+防盗链签名 fail-closed·admin 直传 VOD+转码同步+发布闸·GC 分流·配置资产正册 `console-assets/04`）——未配置期间全链自动走云存储老路零影响。**剩余**：① 人工=控制台五步（04 重建手册）+ admin /config-checklist VOD 组四值，**前置=secureConfig 集合锁权限**（上方最高优先项）；② 真实视频全链 E2E（TC3 手签/防盗链签名只有真云证得了·根因#8·04 验证终点信号）；③ 批3=mp 拖动预览窗消费 vodSpriteVtt + poster 进播放器（依赖真实转码产物·另立批）；④ 存量小熊课 15 段（19.4MB·cloud://）不迁·自然混跑。
- **flag 留债（P2·直传成功未入档的孤儿视频/图片）**：视频直传 COS 成功但 saveCourseDraft 一直没成功（放弃编辑/网络反复失败）的 fileId 从未进任何文档，发布 GC 差集永远碰不到——需「待确认表」或存储前缀列举比对，超本批范围。**图片同类「传了未保存」孤儿同样存在**：`uploadImage` 传上去但 saveDraft 从未成功的商品图，以及卡片图（Cards.vue）/首页内容（HomeContentForm）/目录页（CatalogPlayerTab）上传后未保存的图，都从不写进任何文档的 cover/images 字段、字段差集 GC 永远碰不到——与视频同队待拍板（同需「待确认表」或前缀列举，均超「按字段差集安全删」的边界）。已修的是「档内引用变更」路径：视频＝弃传分片 TTL + 课程发布替换缓期；图片＝批B 商品换图/发布替换缓期 pendingGc + deleteDraft 立即删（cover 永豁免）。
- **flag 留债（P2·进度链三条语义边界）**：① segment_done 单点上报无补发（网络失败即永久漏记该段完成·除非整段重播）；② 多设备并发观看 progress.last 按到达序 LWW（可能把续播位置退回旧设备更早进度）；③ 强杀小程序时 onHide 后的 fire-and-forget watch_at 可能没送达且无本地补发队列。三条都是「可容忍有损 vs 加持久化队列/时间戳比对」的取舍，量上来或用户反馈「进度丢了」再做。
- **flag 留债（P2·getCourses 全量三层树不懒加载）**：每会话首次调用下发全平台课程完整章/课时/段树（当前一门课约 6-8KB 无感）；课程扩到几十门后应改「目录摘要 + 按课懒加载详情」。量上来再做（现有 limit(1000) 上界已守）。
- ✅ **丝滑提案两条已落地**（丝滑战役批3·E7 当场核实 peek/initial-time/Promise.all/prefetch 皆在场·过程账见 archive 重构日志 fable 分册）：① 切段 peek 跳 loading 态✅；② 续播到秒（initial-time 首挂载 + onVideoMeta 补 seek 双保险·clampResumeAt 单源钳制）✅ + 深链目录/取址并行✅。残余=真机验（黑帧/落点±2s/换段不串秒），清单见 `重构日志` 2026-07-18 条。

## mp 播放页段间提速批·有意最小修边界（2026-07-14）
- ✅ **catalog 预热已迁活线**（2026-07-18 丝滑战役批3）：cache 提成共享单例 `lib/playbackCache.ts`（当年设想的原方案），catalog onShow/me refresh 落续播目标即 prefetch——首开播放页取址往返命中缓存归零；守卫 `rw-mp-player-prefetch-cache` 扩三断言锁单例+接线不许退化。
- **flag（真机待验·iOS 停摆若残留）**：playSegment 里 setData 回调内的显式 `play()` 处于 iOS 用户手势栈外，WebKit autoplay 限制可能仍拒（onTapVideo 的 tap 是真手势·才是 iOS 可靠恢复路径）。真机若仍见首帧停摆，社区多源建议试 `<video custom-cache="{{false}}">` 绕微信定容缓存——本批未含（我方素材未验·投机）。参见 `重构日志` 同日条。

## 跨系统硬化批·待用户拍板项（2026-07-13）
> 跨系统一致性收敛批对抗核验中辨出的 needs_user_decision 项——逐条记录供用户后续拍板。**(a)(b) 用户 2026-07-13 已拍板取低风险方案并实现**（见下）；(c)–(g) 仍待拍板，本批不自行执行（各有权衡/涉安全或产品取舍）。
- ~~**(a) adminApi 全局失败计数器可被武器化锁死唯一超管**~~ **✅ 已修（用户拍板方案 A·收尾硬化批）**：per-IP 硬闸保留；全局锁定不再预拒——仍进 `checkKey`，口令对即放行、口令错才计数 + 发 `GLOBAL_BRUTE` 告警。轮换 IP 的错误口令再也锁不住持正确口令的真超管（`adminApi/index.ts throttleGate` + login/loginByWecomCode·测试 `security-dos-hardening.test.ts` + `app-admin1.test.ts` 更新旧断言）。
- ~~**(b) createOrder 下单即预留库存、无在途上限可锁死热销 SKU**~~ **✅ 已修（用户拍板方案 A·收尾硬化批）**：createOrder real 模式 `reserveStock` 前先数本人未过支付窗的 `pending` 单，`≥MAX_PENDING_ORDERS(5)` 即拒 `TOO_MANY_PENDING`（新错误码·parity 登记）。单账号可同时锁的 SKU 数封顶。**残留（诚实标注）**：单张大 qty 单（qty≤999）仍能锁一个 SKU 的现有库存至支付窗过——彻底消除需方案 B「支付时才扣库存」（改钱链热路径·用户此次未取·量真上来再评估）。
- ~~(c) rewrite/spike-skyline/ 用完即弃工程建议整目录删~~ **✅ 已删（2026-07-23 瘦身大作战·spike 结论早records M0 归档条）**：整目录删除+eslint 豁免撤销+待办#8 一并销。
- ~~(d) rewrite/shared/tests/parity.test.ts 建议退役~~ **✅ 已退役（2026-07-23 瘦身拍板批·随旧线删除）**：新线集合登记由 `rw-collection-perm-registered` 在守；错误码字典无谱源守卫仍是既有 P2 flag（见下方错误码条）非本退役新增缺口。同批退役同性质的 `status.test.ts`（新旧对拍）。
- **(e) app action kfBind 新 mp 零接线，建议接线或退役**：身份桥接据称已被 `login` 存 unionid + `kfCallback` batchget 取代，新 mp 从未调用 `kfBind`。**2026-07-23 瘦身大作战核出矛盾证据后刻意未动**：`cs/kfCallback/dispatch.ts:368` 注释明言「映射由 kfBind 在小程序侧建」且 dispatch 仍在读该映射表——若 kfCallback batchget 路径并未同样写映射，删 kfBind＝删掉唯一写入方。拍板前先核：映射表（外部用户号→openid）的写入方到底有几个。
- ~~(f) rewrite/shared 生成物范式拍板~~ **✅ 已收口（2026-07-23 瘦身大作战）**：核实原账目已 stale——`PURCHASE/OUTWORK_ORDER_TRANSITIONS` 实际早被 scmPurchase/scmOutwork 真实消费（非零 import）；真正零消费的三件（`CsSessionStatus`/`CS_SESSION_STATUS`/`QrcodeStatus`）已删；孤儿谱源 `scm.spec.ts`（自称单源、无人读、生成器已亡）整文件删除，`scm.ts` 转正为手改单源+`order-domain.generated.json` 对账守卫拦漂移。
- **(g) flip-demo A/B 对比页提审前处置提醒**：`pages/flip-demo`（`app.json` 注册·约 635 行）是两方案 A/B 对比页，待两方案终拍——胜者迁首页、删败者 + 摘 `app.json` 注册，**提审前必须完成**（不能带 A/B demo 页提审）。

## mp/admin 前端订单状态展示表零依赖 rewrite/shared（跨系统一致性审计 f13/f14·2026-07-13·本批未修）
- **flag（P3·刻意留债）**：跨系统一致性审计确认 mp（`lib/mapOrders.ts`/`pages/order/order.ts`/`pages/order-list/order-list.ts`）与 admin（`lib/format.ts`/`pages/Orders.vue`）各自手写 2-3 份订单状态展示映射表，都不 import `rewrite/shared` 的 `ORDER_STATUS_SPEC`——当前逐值核对一致、无实际冲突，纯属维护成本问题。**本批（跨系统一致性收敛批）只修了 P2 级的错误码/状态机单源缺口，这两条 P3 刻意未动**：mp 侧有documented 的开发者工具编译约束（不能 import `@ldrw/shared`，`checkoutConst.ts` 是既有的"手动同步副本+`rw-mp-checkout-consts-synced`守卫"解法），照此模式修 f13 需要新增一个同类守卫，值得单独一批做扎实；admin 侧没有该约束（可以直接加 `@ldrw/shared` workspace 依赖），但改动涉及包依赖/构建配置，未经真实构建验证不敢在多车道并行批次里顺手做，留作后续单独小批。
- **flag（P2·后续可选）**：客服域（`agentDesk.ts`/`customer360`/`csAccess.ts`）与进销存域（`scmAssembly.ts`/`scmStock.ts`/`inventory.ts`）本批已把散落错误码收口进 `rewrite/shared/src/errors.ts` 的 `ERR` 字典（call 点全部改引用），但**没有配套机器守卫**防止未来新增错误码时又绕开字典手写字面量——`rw-order-transitions-declared`/`rw-cs-transitions-declared` 那类"谱源扫描"手法对状态机好办，但错误码没有唯一"谱源正则"可循（散落在 `reply(4xx,{error:'XXX'})` 各种调用形状里），需要专门设计扫描规则（可能有较高误报率，比如合法的非错误码字符串常量）。若要把这条架构原则从"本批人工清过一次"升级成"机器长期守住"，值得单独一次 `/refactor-batch` 设计并落地。

## `secureConfig` 集合权限锁定待控制台执行 + 配置清单审查批改动待部署（2026-07-12 审查批）
- **flag 待用户控制台动作（凭证承重·最高优先）**：`secureConfig` 集合（企微 Secret/Token/AESKey + 微信支付商户 API 私钥）由 admin 首次「保存」动态建档，**动态建默认 private=真洞**（`rateLimit`/`wxBills` 前科）——首次保存后（或预建时）立即 `echo y | tcb permission set collection:secureConfig --level adminonly -e cloudbase-d4gcssqbv06865479`，并顺带 `permission get` 复查 `anomalies`/`inspectRuns`（同为新线动态建）。期望档正册已登记 `console-assets/02-库权限期望表.md`「重写线净新增集合」节，守卫 `rw-collection-perm-registered` 焊死后续新集合不再漏登记。
- **flag 待部署授权**：审查批代码改动（kfCallback 每事件 5 读→2 读合并、小程序卡片 `miniappAppId`/`thumbMediaId` 随迁入库、admin 页错误人话/保存锁）落 git 未部署——生效需重部署 `adminApiV2` + `kfCallback`（其余 kfSend/kfMedia/kfHealthProbe/wxbill 为读次收敛，不部署仅是省不到往返、无功能差）+ `/admin` 静态托管。部署为独立人工授权动作（CLAUDE §3 部署闸）。

## mp-7fixes 战役遗留（2026-07-13·人工项与真机走查）
- **flag 待另立批（PR#16→main 合流取舍·getThread 晚到同秒消息）**：合并 PR#16 时发现 `agentDesk.ts::getThread` 被 #16（深审·GRACE_MS 游标回退窗）与 main（PR#20 批B·tie-group 补查）各自独立重写、解同一「同秒消息分页丢失」根因但**契约互斥**（GRACE_MS 靠客户端 msgid 去重容忍服务端跨轮重取；tie-group 契约要求服务端零重复）。承重并发代码不硬拼——合流取 **main tie-group 版**为准（新、在目标分支、其两个 tie-group 测试原样通过），**移除了 #16 的 GRACE_MS 回退窗 + 对应测试**。**代价（待补）**：#16 修的「出站毫秒 at > 入站秒截断 at 的晚到同秒顾客消息被 `at>cursor` 永久跳过、坐席看漏」这一真实边界 bug 现回退到 main 现状（未修）——需另立批用**兼容 tie-group 的方式**重做（如复合游标 (at,_id) + 有界 grace 重查且服务端按 _id 去重，或根治：入站归档统一毫秒精度使 at 与到达序单调），并补回等价测试。参见 `重构日志` 同日合流条。
- **flag 待运营补录（首页无图的真正修复）**：admin 后台①给 5 条种子商品（prod-1~5）补封面（商品编辑器上传 cover——发布闸强制）或按正常上新流程重发布；②HomeContent 编辑器补首页视觉板块素材（hero 主图/feature 特写/reassure 条目图/reviews 买家秀图/closing 收尾图）。数据到位首页图即显示，代码无需改。
- **✅ 已落地 2026-07-18（tab 退出提醒·原「待用户拍板」flag 勾销）**：用户「tabbar页需要防止误触退出」后按决策§30 落地——撤 `armExitAlert` 死代码，三 tab 页移植隐形 `<page-container>`「再点一次退出」+ 整页套 `<scroll-view>`（home 连带：下拉刷新迁 refresher、4 处 `wx.pageScrollTo` 迁 scroll-into-view、onPageScroll 迁 bindscroll）。新守卫 `rw-mp-tabbar-exit-guard` 六断言焊死（含判废 API 防回潮），反向自检 7 式咬红。**剩真机验 7 项**（安卓返回键主路径/三页滚动惯性/home 横向 rail 嵌套手势/下拉刷新观感/三处锚点跳转/cart 结算栏安全区/iOS 边缘侧滑未证实）——详见 `重构日志` 同日条。平台上界不变：胶囊×/Home/上滑杀/鸿蒙永远拦不住。
- **flag 待真机走查（根因#8·本战役全部视觉/交互项）**：①tabbar 贴边通栏观感（覆盖 2026-07-06 悬浮药丸设计，不满意改回只动 custom-tab-bar/index.wxss 一个文件）；②两卡等高真实长短文案压测；③订单横滑换栏 vs 纵向滚动误触（安卓中低端机重点）；④品牌字体全局观感（font-weight 钉 500·对比历史是否偏粗）+ 冷启 FOUT；⑤「继续观看」兜底回顶。
- **flag 正册补记**：console-assets/ 未记录小程序后台「downloadFile 合法域名」清单（tcloudbaseapp.com——老线字体真机用通过，说明后台已配）——下批补进正册（先 repo 后控制台约定）。
## 全局清零bug战役残余项（2026-07-13·三轮 sweep（12→10→8 确认）+ 三轮修复共21批·详见 `重构日志` 同日三条）
- ✅ **Refunds.vue 无深链预填**（已补 2026-07-19·Phase3 清零战役批E）：Refunds.vue onMounted 读 `route.query.q` 预填搜索框并自动检索（同 Conversations.vue/Batches.vue 已有深链范式）；Dashboard.vue 告警行「去处理」按钮新增 `alertQuery(a)`——恰好 1 个 id（唯一目标才有「跳到具体记录」语义）且非金额不符单时带 `?q=<反解单号>`（afterSales 复合 id `id.split('__')[0]`，订单 id 不含下划线、切分可靠已核）；多 id/无 id/金额不符单（走 /orders，深链范围不含）仍跳纯列表、不带参。测试见 `rewrite/admin/tests/money-ui.test.ts`。原文保留供追溯：批W 把 Dashboard 退款类告警跳转从 /orders 纠正到 /refunds，但 Refunds.vue 尚未实现 route.query 深链预填搜索（不同于 Conversations.vue/Batches.vue/Cards.vue 已有的深链范式）——afterSales 复合 id 反解出真实 orderId（`id.split('__')[0]`，已确认订单 id 不含下划线、切分可靠）后可以预填自动检索，只是本批范围内未做，是"跳对页面"到"跳到具体记录"之间的增量，值得下批顺手补。
- **shipUploadLock 陈旧锁抢占会对微信侧重复外呼一次上传**：批Y 的陈旧锁过期兜底，如果陈旧场景其实是"上一次已经把微信侧上传做完、只是本地释放没写回"，抢占后会重复调用一次 `uploadShippingInfo`——判断可接受（微信接口本身对重复上报有幂等语义，且已有 fail-soft 告警兜底），记账观察，未做去重。
- ✅ **MeAboutTab 的 SAVE_OK_MESSAGE 与 usePageContent.ts 成功文案手抄同步**（已修 2026-07-19·Phase3 清零战役批E）：`SAVE_OK_MESSAGE` 单源化——`mapPageContent.ts` 唯一定义点，`usePageContent.ts` 与 `HomeContentForm.vue` 都改 import 复用，字面量手抄不再存在；测试 `rewrite/admin/tests/mapPageContent.test.ts` 钉防回潮（两处源码扫描：字面量消失 + import 接线在）。原文保留供追溯：批N 新增的 `combineSaveMessages` 用字符串比对判断"是否等于成功文案"，这个字符串常量是手抄自 usePageContent.ts 的字面量、非同一处导出，如果以后有人改了后者的成功文案忘记同步，会把新文案误判为失败（方向上不算吞错，是"明明成功却显示失败"的假警报）——无机器守卫钉住这条字符串一致性，记账观察。
- **scmStock.ts 的 applyStockMoves 非重复键失败区分依赖 SDK 错误特征**：批AA 的区分逻辑基于 tcb SDK 错误对象形状识别"确定性 _id 冲突"，如果未来 SDK 版本升级改变错误形状，这个判据可能失效退化回旧行为（不会比修复前更差，但值得未来 SDK 升级时复核这一处）。
- **check-structure.mjs 新增两条 SCM 守卫（rw-scm-transitions-declared/rw-scm-ledger-idempotent）为正则近似检测，非完整 AST 解析**：批S 治理自纠时已如实标注边界（如 `fromFor(变量)` 非字面量形式会逃出对账、同一作用域变量被重新赋值的场景不追踪最后一次赋值）——与本仓其它结构守卫的既定精度取舍一致（挡明显越界，非完整数据流分析），如果未来这两条守卫又被绕过一次，参照批S这次的"反向自检"方法论处理，不要来回擦补丁。
- **harness 教益（供下次跑同类 sweep 战役参考）**：Round3 verify 阶段因 API 瞬断把"票数不足"错误归类成"已推翻"，险些让一条 P1（content.ts `_v` CAS 回归）被漏掉——下次写这类双票复核聚合逻辑时，"未达票数"与"真被推翻"必须分成两个桶，前者要触发补复核而不是直接归零。

## 收尾判断力审查残余项（2026-07-13·10 路对抗审查+7 批修复战役·详见 `重构日志` 同日条）
本批已修复 P0+6 条 P1（退款↔履约同步/客服丢消息/SCM假单源/admin竞态/下单幂等）+ 5 项 P2 顺手改 + 防御代码小修。以下为审查阶段确认但判断"记账观察、非本批阻塞项"的残余，及本战役各批 concerns 字段里如实标注的遗留点：
- **flip-demo「留一个/都留/都不留」具体选择未定**（2026-07-13 用户拍板"留"＝维持现状不回收/不合并首页，2026-07-11 条记录的三选一仍待真机对比后再定）：`rewrite/mp/pages/flip-demo/`、`lib/flipLever.ts`、`app.json` 注册、`pages/player/player.ts` 多处"照抄 flip-demo 已验实现"引用——demo 页命运会连带影响这几处注释是否需要更新，处置时一并检查。
- **优惠券商家券方案待用户核实灰度状态**：`docs/优惠券对接微信小店-方案.md`——微信支付"商家券"产品当前是否已全量开放申请，代码/研究验不出，需用户亲自去商户平台核实；确认后再排 `/refactor-batch` 实现批次（方案文档已给分批颗粒度）。
- **关单复活路径 reserveStock 与 transition 非原子**（P2·payCallback.ts/kit/inventory.ts/closeExpiredOrders.ts）：进程崩溃在两步之间理论上会重复扣库存，概率低（需真实进程崩溃卡在极窄窗口），记账观察不做。
- **FEE_MISMATCH 告警在支付回调重复到达时无法幂等**（P3·payCallback.ts）：每次重试重新触发告警，最多刷屏，不影响资金正确性，无需处理。
- **SCM 采购单/外协单「新建草稿」后端无幂等键**（P2·scmPurchase.ts/scmOutwork.ts，仅靠前端 saving 锁防重复点击）：开发者原注释已自认是未偿债务，维持记账。
- **submitFeedback 无去重键**（P3）：网络重试产生重复反馈记录，纯低危害噪音数据，不值得加机制。
- **命名认知税三处记账观察**（P2/P3，均已确认成立但判断"真绊到人再改"，不建机制）：`spec` 字段在物料主档（对象）与商品/订单/组装单（字符串）里形状不兼容（`scmStock.ts`/`scmMaterials.ts`/`scmAssembly.ts`/`orders.ts`）；`session`/`sessionId` 在 adminApi 同目录下同指登录会话与客服会话两个概念（`lib.ts`/`agentDesk.ts`/`conversations.ts`/`csAccess.ts`）；`sessionKey` 在 `csat.ts`/`conversations.ts` 语义不同。
- **mp 侧本战役改动未走真机/`/frontend-check`**（根因#8·工具限制）：批A 的 refund_required 文案改动、批E 的结算幂等键改动，均只走了类型检查+单测，未做真机走查，如实标注待下次真机窗口一并核实。
- **批C 记录：SCM 域"零生成器"（order.ts/learning.ts）与"packages 风格生成器"（scm.ts/scm.spec.ts）两种声明范式并存未统一**——本战役任务范围内已让 scm.ts 横幅诚实、生成器真正覆盖 rewrite/shared，但未统一两种范式本身，是更大范围的重构决策，留待后续。
- **批D 记录：Courses.vue 的 serialSave key 用 courseId 组件挂载时取值固定**——同一实例内切换课程号（load() 换课）不改变槽位标签，理论上少了"切走再切回同一门新切换到的课程"这一双重边缘场景的跨实例保护，写入不会错位（每次保存带自己的课程 id），概率极低，记账观察不做进一步工程。

## `app` 云函数部署落后 git——`kfBind`/`getPublicFaq`/`getPageContent` 均未随批上线（2026-07-12 批 B10 追记）
- **flag 待另立授权批**：`app` 函数云端现网停留在 2026-07-04 版本（批11 部署时明确未授权，见 `重构日志` 10905f0 记账），此后至少两批改动落在其中未随部署——批 B8 的 `getPublicFaq`、CMS 战役的 `getPageContent`、以及批 B10 的 `kfBind`（`WXKF_CORPID`/`SECRET` 改走 `kit/secureConfig` 读库）。批 B10 部署时刻意不碰 `app`（重部署会把前两项此前被搁置的功能一并带上生产，超出当次「只谈 config-checklist 页」的用户授权范围）——`kfBind` 因 `getSecureConfig` 的 env 兜底继续按老路径工作，无功能回退，仅是暂未享受到「本页填写即生效」的好处。下次拿到「部署 `app`」的明确授权时，这三项一并核验再上线。

## 企微免登（M⑦ 旧线能力）未迁 rewrite/agent（2026-07-12 批 B9·人工配置清单预审发现）
- **flag 待另立批**：`docs/系统事实.md`/人工配置清单页如实标注——重写线坐席台（`rewrite/agent`）现走外包令牌登录（`issueSession`/口令+会话令牌），企业微信 OAuth 免登为旧线 M⑦ 能力（`kit/wecom.ts::getWecomOAuthUserId` + `adminApi/actions/wecomLogin.ts::loginByWecomCode` 后端接缝仍在，前端坐席台未接入免登入口/OAuth 可信域名配置）——本批（人工配置清单页 B9）预审核对配置项时发现该落差，未迁移工作量超出本批范围，另立批次处理；配置清单页对应条目状态恒 `check` 并携带该说明，避免用户误以为「企微免登」是可配置生效项。

## 质检抽检粒度/UI 形态两处占位（2026-07-12 批 B7·质检抽检）
- **flag（量级未到）**：质检粒度定在客户会话档级（`csSession` 文档·`sampleQc`/`saveQcMark`/`listQcSampled`），**轮次级质检不做**——`csSession` 跨轮复用重开（顾客二次点「找人工」原会话 closed→pending 重开，见 `cs/kfCallback/dispatch.ts enqueueSession`）、消息本身无 `sessionId` 字段，「轮次」在现有数据模型里不是一个可寻址的实体。要做轮次级质检须先立 session 轮次实体（每次 pending→closed 一段生命周期单独归档），当前咨询量级未到、不为不存在的需求先建机制（防过度工程判例同 S11）。
- ~~flag（已知限制·重开不重置质检标记）~~ **✅ 已根修（2026-07-12 批 B7 收口·主脑亲手）**：`dispatch.ts` closed→pending 重开 patch 已一并置空 `qc`/`qcSampledAt`——质检标记锚定「上一段生命周期」，回头客重开后可再入质检候选池、`listQcSampled` 不再把旧评分误标现役会话。配源码扫描守卫（`qc-sampling.test.ts`「重开清质检标记」·反向自检删清空行即红已验）。
- **flag（设计 vs 实现）**：设计稿「客服会话」页信息架构为三 tab（会话检索/质检报表/质检抽检切页显示），本批落地仍延续 `Conversations.vue` 现状——两张 Card 纵向堆叠 + 追加第三张 Card「质检抽检」（同布局语言，不做 tab 改造）。三页内容都在同一页面纵向可滚，非切页——信息架构重构（改 tab 形态）另立批次，需先确认是否影响其余深链预填（`Csat.vue`/`Refunds.vue` 差评/退款「查会话」跳转均预填 query 后自动检索，tab 化后须保证这条深链路径仍可达）。

## csat 落 agentId 维度（2026-07-12 批 B6·CSAT 钻取+坐席看板）
- **flag 待后续批**：`csat` 集合结构性无 `agentId`（`recordCsat` 从未写入，见 `cs/kfCallback/dispatch.ts`）——本批坐席表（`Agents.vue`）因此**不做「满意度」列**（恒 null 的列是摆设·不编数不摆设），`listAgents` 只扩「在线状态/接待中/今日结束」三个真有数据源的字段，整体均分并行调既有 `getCsatReport`（全局口径·非按坐席）。待写侧 `recordCsat` 反查 `csSession` 补上 `agentId` 维度后，再回来给坐席表补逐坐席满意度列 + `Agents.vue` KPI 行按坐席细分。

## 坐席发图后置（2026-07-12 批 B5·顾客发图坐席可见）
- **flag 待用户**：本批（`cs/kfMedia`+`getMediaUrl`+`Desk.vue` 图片气泡）按「回调不下载媒体、下载延迟到坐席首次点开」实现，`kf/media/get` 的成功/失败判定按响应 `content-type`（二进制=成功 / `application/json`=失败）二分、失败统一按 EXPIRED 语义处理——**未接真实 `WXKF_CORPID`/`WXKF_SECRET` 环境验证过**：真机需核对①真实图片下载后 `uploadFile` 落云存储的字节是否完整（真实尺寸样本·根因#8）；②media_id 3 天过期边界的真实 errcode 是否真如预期触发 EXPIRED 分支（当前未按 errcode 细分，一律归 EXPIRED，若真机出现「非过期但同样返回 JSON 错误」的场景——如 access_token 失效——会被误标 EXPIRED，届时按真实 errcode 表补细分）；③`Desk.vue` 大图查看走 `window.open` 临时 URL（浏览器可能拦截弹窗），真机走查确认体验是否需要改用同页大图预览层。

## 坐席台在接多会话新消息提醒（2026-07-12 批 B4·预审裁决后置）
- **flag 待用户**：页内提醒公式 `N = 队列新增会话数 + 失焦期间当前打开会话的新入站消息数`——「我在接」里除当前打开会话外、其它会话的新入站消息本轮不做（现行轮询只拉当前会话线程 `getThread`）。需 `listMyActive` 扩出摘要字段（未读计数/最新一条消息）后才能扩这块提醒面；扩前先确认后端加字段是否越出本批「零后端改动」预审裁决范围。
## 深度审核 2026-07-12 残留债（报告已并卷退役 → 史料「审核报告合卷-202606.md」·2026-07-23 瘦身大作战转正）
- **flag（守卫镜像·结构不适配暂搁）**：`cs-module-registered`/`logout-resets-user-scoped` 的 rw- 镜像未上——新架构无同款 providers registry、mp 非 Vue store，需按新架构另设检查形状（防错误守卫），待有真痛点再立。
- **窄债 P3 四件（latent 低值·留后续批）**：① `orders.ts` `__` 分隔符拼接注入面（上架/建 SKU 校验口）② `adminApi/lib.ts` sessions 非原子（自愈重登·改动风险>收益）③ `dispatch.ts` CSAT 无一次性闸（同顾客可反复评星）④ `checkpoint.ts` 云存储孤儿照片无 GC（并入「传了未保存孤儿」同队）。
- （原报告所记「rewrite 钱链部署漂移无检测」已由 2026-07-18 盲区批5 迁活线解决·不再转记。）
- **修复进展**：2 条 P1（退款/发货不联动·部署闸绕过）与大批 P2/P3 已随 PR #10 批1–4 修复（台账内逐条标 ✅）；剩真机/控制台够不到项待人，见台账收尾段。本条仅指针，明细不双写。

## 教学播放重设计战役·前端已留接口等后台（2026-07-11 批A·明细见 `需求清单.md` R35–R37，勿双写）
- **前端已留接口、等后台**：R35（关键动作节点标注·admin 编辑器+云端落库）／R36（段落视频雪碧图预生成）／R37（求助面板课时/段落维度 + KB 精选 FAQ 公开读接口）——R35/R36 于 2026-07-11 核实 `rewrite/cloud` 零现存实现；R37 的 **(b) FAQ 公开读接口已落**（批B8·2026-07-12·`getPublicFaq`），**(a) 课时/段落维度关联仍零现存实现**、基线 `getHelpVideos` 全局源已存在（详见 R37 行）；mp 端 `SegmentPub.marks` 已留宽松读字段（R35 前端半边），落地顺序/判定见需求清单对应行，本条仅指针、不重复明细（错题本 E6）。

## 冻结记账（已解冻 ✅ 2026-07-09 M5 切换完成·本节改记「切换后余项」）
- ✅ 原「随 M5 上线」项落位：重写新版已是云环境唯一版本（切换经过见 `重构日志` 同日条）；**mp 上传/提审/发布仍待微信后台人工**（发布后真机走查·关键课真视频照旧待录）。
- **功能线挂起照旧**：客服360 M③ 内部 VIP · 进销存真单验收（SCM-Z）+ 期初盘点——在新结构上按需做。
- ✅「线上止血走 next 仓」失效：本仓即生产源；next 仓封存为回滚基线（可重建旧线·不再迭代）。

## M2 未搬面（2026-07-04 收口清点·明细见 `rewrite/mp/README.md §已知未搬面`）
- **M5 前必补（合规）**：✅ 已补 2026-07-04（隐私授权闸+consent 页·守卫 rw-mp-privacy-gated）；剩用户侧＝mp 后台《隐私保护指引》登记对齐 + 真机验（M5 前置闸2/3 内）。
- **待补非阻塞**：
  - ✅ **课程目录页**：已补（2026-07-11 播放器重设计战役批E）——新页 `rewrite/mp/pages/catalog`（章>课时两层，段落层级只活播放页 segstrip），`my-courses.ts`/`welcome.ts` 均改跳目录页（`me.ts`「继续学习」跨课续播仍直达播放页，未过目录，见 `rewrite/mp/README.md` 走查表第 13/14 项）。✅ **播放页帮助视频抽屉已勾销**（2026-07-11 播放器重设计战役批D）：改形态为求助面板（P3·底部 sheet）卡2「帮助视频」子层，接真数据（`getHelpVideos` + `mapHelpVideos`），课时/段落维度组待 R37 后端就绪后再开。扫码激活后自动定位新课子行为**已补**（2026-07-08 批次C）：`welcome.ts` onEnter redirectTo 带 courseId、`my-courses.ts` onLoad 存 highlightId、卡片高亮描边。
  - ✅ **订单物流轨迹**：已接通（2026-07-08 批次C）——快递100 插件声明（app.json plugins）+ `lib/express.ts` 编码映射 + `order.ts` onViewLogistics 跳插件（失败回退复制运单号）。`pages/agreement/agreement.ts:33` 隐私政策承诺兑现，文实不符解除。
  - ✅ **分享卡（转发能力）**：已补（2026-07-12 工业级完善战役批3·决策§29）——公开页 home/detail 接 `onShareAppMessage`+`onShareTimeline`（detail 带 ?id= 回流），私有页（交易/学习/隐私）**刻意不加钩子＝默认不可转发**；搜一搜 sitemap 同批放开公开页；守卫 `rw-mp-share-wired`。余真机验分享卡样式（发布后走查）。
- ✅ **客服触点产品决策已拍**（2026-07-08）：me/售后页各加「联系客服」入口，触点 2→4（详情/播放页/我页/售后），守卫 `rw-mp-customer-service-wired` 表驱动同步扩面。首页「+」加购已改真加购（单规格直加/多规格跳详情），假「已收藏」反馈绝迹，守卫 `rw-mp-home-quick-add-real`，R29 状态同步。

## 播放页竖屏沉浸全屏重设计（2026-07-08·帮助入口批）
- ✅ **投屏已全线取缔（2026-07-12 用户拍板·决策§28）**：原「双保险现状/2026-07-11 四点拍板/DLNA·AirPlay 真机清单/casting 事件取值校验」待办**全部作废**——播放页投屏主备路径、入口与样式已整体拆除，R39/R40 退役（需求清单），防回潮守卫 `rw-mp-no-casting` 焊死 rewrite/mp 源内投屏 token 零出现；未合并的投屏终态分支 PR #7 去留待用户处置。
- **渲染引擎拍板**：M2 首发全 WebView（spike 增益未证实·防过度工程）；Skyline 留上线后按页灰度。

## 冒烟层（批次D·2026-07-09·rewrite/mp 运行时冒烟·根因#8 机器半边）
- **已建成并实跑取证**：`scripts/mp-smoke.cjs`（miniprogram-automator 已转 devDependencies 正式登记）+ `npm run smoke:mp`；app.ts 加 onError/onUnhandledRejection 探针；守卫 `mp-smoke-wired`（155）。服务端口本机已开，两条路径（connect 现成实例 / launch 冷启动）均实跑成功：21 页逐页 PASS，exit 0。
- **⚠️ 覆盖边界·真机走查仍不可省（本批实测发现）**：冒烟层只做「打开页面 + 等约 2s」，对**默认不渲染的条件节点**（`wx:if` 为 false 的抽屉/遮罩等）挂的事件 handler 缺失**测不出**——实测验证：删 `player.ts` 的 `noop()` 后单跑冒烟仍 21/21 PASS（`listOpen` 默认 false，抽屉遮罩未渲染，`catchtouchmove="noop"` 从未被派发）；额外用一次性诊断脚本手动 `setData({listOpen:true})` + 模拟 `touchmove` 才复现出 `does not have a method "noop"` 告警，证实探针/console 抓取机制本身工作正常，缺口具体在「不打开条件节点就摸不到它上面的 handler」。**批次D规格 §4 的反向自检字面结果与规格「player 页必须 FAIL」不符**（规格假设「删 noop 后必然 FAIL」，实测为 PASS）——按错题本 E5 记账不擅自改脚本行为（不会为单页悄悄塞交互步骤），留给下批决策：要否为高风险条件节点页（player 抽屉等）补交互步骤，或接受「冒烟只保基础可达性、条件分支仍靠人工真机走查」的边界（README 走查表已覆盖该项）。
- 一次性异常注入自证（同批完成，未落代码）：`onLoad` 首行临时插入调用不存在的全局函数 → 冒烟正确抓到 TypeError（探针 + FAIL + exit 1）→ 已还原，`git diff` 确认零残留——证明探针链路本身可靠，问题只在条件渲染节点的可达性。

## mp 首页重设计战役（2026-07-06·本会话·Claude Design 交接包 z-t.zip 落地·逐批详见 `重构日志` 同日各批）
> 把 Claude Design 交接包逐屏落到原生 mp；逻辑/守卫/隐私/钱链全程保真·浏览器亲核每屏·精确 git add 避开并行 admin 会话（`design/控制台.pen` 重画线）未提交改动。
- ✅ **前端 21 屏全对齐设计**：首页9板块重建 + TabBar + 详情/购物车(待选区)/我/结算/支付成功/激活/订单列表/订单详情/地址×2/售后/评价/资料/我的课程/播放器/全部评价 + 反馈·关于·协议·同意一致性。
- ✅ **首页内容 CMS 写路径**（cloud+admin·「接 CMS」写侧）：saveHomeContent 白名单扩品牌/特写/放心/买家秀/收尾/页脚 + hero 搜索/图·admin HomeContent 编辑器补六卡·mapContent 往返·守卫 `rw-admin-content-ui-golden`。
- ✅ **C 类竖切① 买家秀晒图**（cloud+mp·根因#3 fail-closed）：submitReview 收 photos 逐张 imgSecCheck·getReviews 换短时址·守卫 `ugc-imgsecchecked` 迁到重写线·mp 选图坞+图墙+隐私闸。
- ✅ **C 类竖切补全（2026-07-07 loop·5 批·守卫先行→红→绿→反向自检咬红·逐批详见 `重构日志` 同日）**：
  - **订单行封面图**：createOrder 下单快照 p.cover 进 item（历史快照·不回读 catalog·T3）+ mapOrders VM cover + 订单列表/详情 `<image>`（旧单回退灰底占位）·守卫 orders-map/app-orders。
  - **物流运单**：mapOrders 从 `o.shipping` 子对象读 trackingNo/company（修「读顶层 o.trackingNo→永不显示」路径 bug）+ 公司名+复制·守卫 orders-map。
  - **取消订单**：新 `cancelOrder` action（仅本人+仅 pending·复用已声明 pending→closed 边·回补预留绑 CAS 幂等）+ app 注册 + 两页 pending 取消入口·守卫 app-orders。app action 25→27（含 getRatingSummary）。
  - **课程进度**：mapMyCourses 加 progress 参·段粒度 join done map（交集防 stale 顶爆·与后台 360 同口径）+ my-courses 细进度条·守卫 learning-map。
  - **详情评分**：slim `getRatingSummary` action（热路径只 count+sum·不拉列表/图/标签）+ detail 旁挂接线（复用 mapSummary·count≤0→null 不渲染假评）·守卫 app-reviews。
  - **地区选择器**：address-edit 换微信内置 `<picker mode="region">`（全国省市区运行时自带·region 空格拼接真相源）·守卫 `rw-mp-address-region-picker`。
  - **资料手机号**：updateProfile 白名单加 phone（归一化纯数字·7–11 位·非法剔除不覆盖·隐私不进日志）+ profile-edit 字段·守卫 app-user + anomaly 剥离钉。
- ✅ **C 类自修循环闭环（2026-07-07·详见 `调试日志` 案 D）**：并行审计+对抗验证滤假阳·**收敛 iter1=5→iter2=1→iter3=0→iter4=0**（双连续零）·共 **6 确认逻辑 bug**（全 P2）全修——phone 清空语义/客服360 percent 交集/my-courses·order·order-list 回包乱序代次守卫（同类 3 实例）/地址双击/资料载入失败清空。2 机器守卫（app-user/app-admin5）+ 4 Page 级修（待真机·见案 D 走查清单）。
  - **详情销量**：需产品决策——「销量」语义（真实订单派生 vs 运营手填基数·哪些状态计入 paid/shipped/done·退款是否扣减）+ 正确机制（products 冗余 sold 计数·订单转 paid 幂等自增·触钱链 transition-atomic-idempotent 守卫，切忌每次进详情实时聚合 orders=无界查询·根因#7）。定后 1 竖切可落；评分半边已先行。
  - **物流实时轨迹**（快递到哪了·非仅运单号）：需第三方快递查询 API（快递100/微信快递插件·外部服务+密钥/插件授权）；现「运单号+公司+复制」已满足设计口径（不伪造轨迹）。
  - **手机号 getPhoneNumber 一键授权**（权威可回拨号·非手动输入）：需小程序企业/组织主体认证 + getPhoneNumber 计费/额度确认 + 真机验（根因#8）；openapi 接缝范式已在仓（shipping.ts wxaSecOrder），待商家确认后平滑加装为升级。
- **⚠️ 部署待人**：①② 已核销（app/adminApi 云函数随 2026-07-09 M5 同名替换 + 2026-07-12 重部署上线；admin 前端随托管清空重传上正式路径 `/admin`·admin2 已退役）。仍待人：③ mp（devtools 导 rewrite/mp→体验版）。**前置**：mp 后台**开通「内容安全」能力**，否则带图评价一律 fail-closed 被拒（纯文字评价不受影响·根因#8「配好才生效」·须真机验能拦黄暴恐）。

## mp 逻辑自修循环（2026-07-06·本会话·用户拍板「loop：mp 前端完整实现·自检自修至无逻辑 bug」·逐批详见 `重构日志` 批A–F）
> 并行审计工作流(逐页审计+对抗验证滤假阳)→确认逻辑 bug→守卫先行/测试先行自修→check→精确提交→再审计，直到干净。**收敛序列 9→6→2→0**（iter4 最多改动的 player/checkout/order-list/cart 四页确认 0）。共修 **17 条确认逻辑 bug**（0 P0·1 P1·8 P2·8 P3），其中 iter2 逮出**批D 自引入的 1 条 P1 回归**（印证 loop re-audit 价值）。
- ✅ **两条永久守卫**（都治「守卫指冻结老线、活跃重写线没守」缺口）：`rw-mp-navback-timer-cleaned`（延时返回坞必 onUnload+clearTimeout·病根#5）、`rw-mp-player-stale-guarded`（切段取址必复核 playToken·同老线 player-playurl-stale-guarded 迁 rewrite）·repoCheck 147→149。
- ✅ **修完类目**：提交→延时返回坞孤儿定时器+二次提交(4 页含 checkout 双下单) / 播放页切段乱序覆盖+视频失败无限重取+src 不清+error 无身份闸+watch_at 漏报 / me 续看丢 segmentId / detail 推荐叠栈 / order-list 状态筛选下推服务端(+其切 tab 竞态回归) / checkout 提交在途未锁副操作+refresh 缺闸 / cart 步进器丢增量。
- **⚠️ 待人·真机走查**（根因#8·工具端验不出的返回栈/竞态/滚动/后台切回）：① 各页提交后延时返回窗口内手动返回不多弹页、连点不二次提交；② 播放页快速连点切段不串画面/音、放不了的段落落播放失败态非黑屏死循环；③「继续观看」回到上次那一段；④ 订单各 tab 翻页只出该状态单、快速切 tab 不串单；⑤ 购物车连点加减不丢数；⑥ 支付中后台切回不被弹页。
- **⚠️ 部署待人**：order-list 的 `getMyOrders` 收 status（app 云函数）随评价晒图一起重部署（见上节部署清单）；mp 前端整体导 rewrite/mp→体验版。
- **跳过记档**：getTabBar() 返回值 null 校验（2 条 UNCERTAIN·防御性非确认 bug·getTabBar 在已注册 tab 页实返实例）——如后续真机遇 tab 高亮偶发 TypeError 再补 `&& this.getTabBar()`。

## cloud bug sweep Round1 承重批派生（2026-07-09·详见 `调试日志.md` 案 E / E-Round2）
- **P3·同类扫描待排（不阻塞·结构性提示）**：本批修复的三个模式——①「先查后写」竞态（agents.ts createAgent/setAgentWecomUserId）② 单一游标无 tiebreaker（`kit/paging.ts` 单一 kit 实现，全仓走它的调用方已统一受益，此项不再需要另排查其余出现点）③ 裸 `.get()` 命中服务端默认 100 条截断（lib.ts getTxAlerts）——①③在本仓其余走同款写法的域可能有同构实例，未逐个排查（本批仅修 spec 点名的 7 处）。留待下一轮 sweep 或 `/deep-audit` 顺手扫一遍「先查后写」「裸 `.get()` 无 limit」两个模式的其余出现点。**2026-07-12 批4 进展**：③的最热两点位 getProducts/getCourses 已修（显式 .limit(1000)·守卫 `rw-app-catalog-reads-bounded`）；深审台账（已并卷 史料「审核报告合卷-202606.md」）P2 另点名 afterSales 三处同款（orders.ts:333/refunds.ts:38/236·钱链）留对方线／下批处理。
- **P3·同类扫描待排新增（Round2 复核发现）**：「管理员越权/越规操作路径 与 客户自助操作路径 各用不相交确定性 _id、可跨函数 TOCTOU 双写同一资源」这一类问题（本次实例＝`overrideRefund` vs `applyRefund`，已修）——本仓是否还有其他「admin 越权改写 X」与「客户自助改写 X」成对存在、且各自走独立确定性 _id 的路径（未逐个排查）。留待下一轮 sweep。
- **P3·afterSales.outRefundNo 索引（沿用既有待办）**：overrideRefund 确定性 `_id`（首个记录 `orderId__lineId` 裸位、续位 `orderId__lineId__ovrN`）与既有 `outRefundNo` 索引待办（见上「退款链跟进」节）同一张表，无需另立——量上来前一并补。**2026-07-12 批4：已并入 `console-assets/03-复合索引期望表.md` P1 段**（索引期望自此有正册，照单建）。

## M3 低频待办（不阻塞演练与切换）
- 激活卡双面设计器（印刷用·旧台有）· 二维码图片打印导出（qrcode+jszip·旧台有）· 视频直传失败的分片回落通道。明细散见 `rewrite/admin/README.md` 与重构日志 M3 各批。

## 观测批（病根#14 立后拆解 · 2026-07-04 · 治「报了没人看见」·上线前宜做）
> **→ 代码侧已落大半**（2026-07-05·见下「运行期巡检机 + bug 收集器」子系统）：①botpush=批5✅ · ②探活雏形=批2 inspect✅（**A5「全绿也发心跳」=Phase3 批H✅**·经 `notifyHeartbeat`＋`sendDailyHeartbeat` 每日确定性 id 去重发心跳；余防噪判据待）· git 外①webhook 值随批5 部署面配。下列保留原始拆解口径。
- **代码侧×3（每件带守卫·/refactor-batch 形状）**：① `notifyAlert` 接通企微群 botpush 接缝（`rewrite/cloud/src/kit/observe.ts` 注释自留的口·签名不变调用方零改动·守卫照 `moneychain-alert-wired` 模子锁「真调推送」）；② 探活定时函数（每日合成探测关键外部接缝：config.pay 与正册比对＝案 A preflight 机器化 + kfHealthProbe 挂入，异常 notifyAlert·**全绿也发心跳报平安**——「该来的消息没来」本身成告警，终结「告警的告警」递归）；③ 防噪判据入探活（AA 判例「持续 FORGED>0 且 onEvent=0」才告警·同 code 窗口聚合·测试锁）。
- **git 外×2（console-assets 纪律）**：群机器人 webhook 值入正册（REDACTED）；控制台 `[LD_ALERT]` 日志告警配置核对入 drift-checklist。
- **靠人面（写清即止）**：「群里有人、每天扫一眼心跳」机器保证不了＝#14 靠人终点；不上 APM/监控平台（一人运营规模·防过度工程）。

## M3 UI 换皮·设计稿超出后端数据源的元素（不编数·记账待后端·2026-07-05 批2 起）
> ⚠️ 2026-07-05 六域审计订正：本节下列数项「当前无 / 后端未支持」系换皮**误判**，后端 `rewrite/cloud` 实际已就绪——批量发货 `shipOrders`(orders.ts:127)、订单详情 `getOrderDetail`(orders.ts:7)、看板 `hot/stuck/recentActivity`(dashboard.ts:133-135)、服务端搜索 `q`(listOrders/listRefunds)、告警 `alertEvents`(cards.ts·合并保存)——这些是「换皮漏接」非「后端缺口」，已移入下节「换皮回归 backlog」为权威。本节**仅保留真后端受阻项**：趋势%历史序列 / 今日口径 / 6步进度分步态 / 跨课全部批次列表 / 单码激活态 / 库存批量导入 · per-SKU阈值 / 推送订阅偏好。印刷包卡面SVG 已解阻退役（见「换皮回归 backlog·Batches」）。
- **数据看板趋势%与迷你柱**（design S12：每卡 +8.2%/环比柱）：`getDashboard` 只给当前累计值、无历史序列——趋势需时间窗聚合 action（本页已省略趋势、只渲染真值卡）。
- **今日速览的「今日」口径**（design S15：今日订单/GMV/新增用户/课程激活）：现有 stats 为累计值——落地时按真实可得口径命名（累计/本周），不假标「今日」。
- **商品列表「6 步上新进度」圆点**（design S11：产品图片→商品信息→SKU→教学视频→二维码卡片→码批次 各步完成态）：需上新向导（S5-S9）产出的分步完成态；当前 DraftRowVM 只有封面/名/价/规格聚合值——落地向导批时补该列真值，当前列表省略该列（不按字段瞎推 6/6）。
- **订单/售后列表搜索仅过滤已载页**（design S8/S10 搜索框）：当前 listOrders/listRefunds 无 keyword 参数，UI 搜索是客户端过滤已加载行——跨全量搜索需服务端加 keyword 过滤 action；订单「改单号/订单详情面板」（design S8「改单号」+ VMlhp 详情面板）需改运单/查详情 action，当前无，UI 未加假按钮。
- **二维码批次跨课「全部」列表 + 课程下拉**（design S9）：后端 `listBatches` 强制 courseId、无列全量批次 action——UI 按课程范围查（先输 course-xxx）；跨课全量总览需后端加「列全部批次（可选按课程/状态筛选）」action。
- **批次码表单码激活态**（design S9 详情抽屉：每码「已激活/未用」chip）：`listBatchCodes` 只回码 `_id` 串、不带 status——UI 码表只列码 + 看整体激活率，不标单码状态；补齐需 listBatchCodes 随码返回 status 字段。
- ✅ **印刷包 ZIP 下载 / 测试二维码**（design S9 抽屉按钮）：已实现（`downloadPack`，提交 999755d 2026-07-05；`makeTestQr` 更早已在）——本条**退役**。**2026-07-08 拍板注明**：二维码码图不由我方生成（印刷厂拿地址清单自行制码），`makeTestQr` 仅量产前自测扫验落地页用途保留，非交付物。
- **库存批量导入/盘点**（design S14 头部按钮）：saveStock 仅 per-SKU、无批量导入 action——UI 未加假钮；补齐需后端「批量盘点/CSV 导入」action。低库存阈值（design「低于阈值 10」）为前端显示常量，后端无 per-SKU 阈值字段——如需按 SKU 定制阈值须加字段。
- **订单批量发货面板 / 电子面单取号 / 订单详情面板**（design OLQG3/XqXZ5/VMlhp·note qlcwr）：需批量 shipOrder + 电子面单取号 + 订单详情 action（当前 shipOrder 仅单单、无取号/详情）——订单页批量操作留待后端补齐。
- **财务对账微信手续费/实际到账 + 月份筛选**（design S16：手续费≈0.6%卡/微信实际到账卡/月份下拉/导出 Excel）：手续费与结算实收需微信结算数据源，getReconciliation 只给应收/退款/净额——UI 只作真等式+账单勾对、不伪造手续费估算；月份筛选需后端 getReconciliation 加 month 参数（现为近 30 天固定窗）；导出改客户端 CSV（Excel 待后端或前端 xlsx 库）。
- **消息与通知 S17（JxJm7）暂未建**：企微告警 webhook 已有（getSettings/saveSettings）；但「推送事件开关」（新订单/待发货堆积/库存预警/交易异常/退款/日报 + 订阅消息 发货/物流/激活/完课/复购）需通知偏好后端字段——getSettings 未必含，不建无持久化假开关。落地前先确认 settings 是否有 notifyPrefs，或后端加字段；且与既有 Settings 页重叠，需定位（并入 Settings 或独立页）。
- 上述均为 UI 层已按设计视觉就位、数据接口缺口；补齐是后端 action / 上新向导工作（非当批），补齐前 UI 诚实省略/空态、不占位假数。

## M3 UI 换皮回归·逐页 1:1 全还原 backlog（2026-07-05 六域审计·用户拍板全还原·勾销回填本表）
> 换皮批 batch2-18「逻辑零改·check 全绿」实为丢交互/功能：`map*.ts` 单测只覆盖纯映射、不覆盖 .vue 模板交互与功能完整性（根因#8），故全绿仍漏。**绝大多数后端能力已在 `rewrite/cloud` 就绪，仅前端换皮丢**：[就绪]=接后端即可 · [前端]=纯前端交互 · [受阻]=需后端 action/字段（多与上节重叠）· [守卫]=补机器守卫。旧线逐页参照 `packages/admin/src/pages/*`。5 bug 详见 `调试日志.md·案B`，随所在页批次先红后绿修。
> **钱/交易**
> - ✅Orders（还原批1 完成 2026-07-05）：批量发货多选+批量栏[shipOrders]｜详情抽屉(时间线/交易号/微信发货合规/逐商品激活)[getOrderDetail]｜shipped 改单号·done 查看｜服务端跨状态搜索[q]｜物流公司预设下拉·手机号掩码 — 全数复原
> - ✅Fulfill 发货工作台整页（还原批2 完成 2026-07-05）：拣货备货汇总/打印内部标签QR(qrcode)/扫码发货(classifyScan 安全判序)·单页三段+路由/fulfill+侧栏入口 — 全数复原
> - ✅Refunds（还原批3 + **二轮批Money-2** 2026-07-05）：加载更多游标分页[pageQuery]｜服务端搜索[q]｜已退/已拒结果块(到账时间/拒绝原因) — 复原；**二轮补·买家身份**(批3 曾「跳过·afterSale 无地址」)：后端 listRefunds join 订单收货地址返 buyerName/buyerPhone·列表掩码+抽屉全号联系寄回·✅已部署 adminApiV2 上线(2026-07-06)；页脚分母改本栏计数(换皮恒 counts.all 误导)
> - ✅Reconciliation（还原批3 完成 2026-07-05）：内部异常明细块[修 bug B2·exceptions 对象结构化]｜自定义日期区间[from/to]｜横幅反映异常笔数 — 复原；整段批量拉[跳过·单日拉够用·非阻塞]
> - ✅Dashboard（还原批4 完成 2026-07-05）：待处理行动条 4 卡(待发货/待审退款/低库存/告警·点进对应页)｜热点·卡点段位[hot/stuck·B5]｜最近动态四类事件流[recentActivity] — 复原；上新未完成卡[跳过·需商品状态 join·归 Products 批]｜漏斗环比%·激活率进度条[前端·次要·略]
> **商品/橱窗/内容/课程/批次**
> - ✅Products（还原批5 完成 2026-07-05）：多图相册编辑+‹›排序[images]｜params/detailSections/kit 三表编辑(1:1 StepInfo shape)｜上架前缺项预检(missing 清单)｜删除下架 twoStep 两步确认 — 复原；6步进度列[受阻·分步态无源·略]
> - 卡片双面设计器(StepCards·getCard/saveCard)[受阻·api未暴露·连印刷包]——全域唯一整块无处安放
> - ✅Showcase（还原批6 完成 2026-07-05）：iPhone WYSIWYG 预览｜双向拖拽排序(替数字框)｜防抖自动保存 700ms+离页补存 — 复原(mapShowcaseRows 补 price/tag)
> - ✅HomeContent（还原批6 完成 2026-07-05）：FAQ/信任条/按课行删除按钮(治只增不删)｜背景图缩略预览 — 复原
> - ✅Courses（还原批7 + 尾批C/D 2026-07-05）：✅三级 ↑↓ 重排｜✅删除二次确认｜✅发布护栏｜✅商品→课程深链｜✅**课时级批量传视频**(尾批C·videoBatch 移植+planLessonBatch 串行灌入·守卫 rw-admin-videobatch-golden)｜✅**防抖自动保存**(尾批D·watch deep 900ms+离页补存+load-guard 防加载即存)｜✅**时长自动读取**(尾批D·readDuration 读元数据填 dur·未填才读)
> - ✅HelpVideos（还原批7 + 尾批D 2026-07-05）：✅主题/小段 ↑↓ 重排｜✅删除二次确认｜✅**防抖自动保存**(尾批D·同 Courses·不 reload 免打断)｜✅**时长自动读取**(尾批D)；**⏳延后**：分段上传进度条(单文件即时进度已有·此指多段合并进度·价值低)
> - ✅Batches（还原批8 + **印刷包 ZIP 批** 999755d 2026-07-05 + **导出 fail-closed** 批次C 2026-07-08）：✅网址列CSV(激活码,URL·urlPrefix)｜✅测试二维码(扫验落地页)｜✅印刷包ZIP+卡面SVG(downloadPack·getCard 取卡面·非受阻)｜✅导出 fail-closed(未配网址前缀挡 exportCsv/downloadPack)；**⏳留**：时间区间筛选+折叠分页[前端·次要]
> - ✅Checkpoints（还原批8 完成 2026-07-05）：↑↓ 重排+order 按位重算(修排序错乱)｜删除二次确认｜默认 course-duck 自动载入｜空标题过滤 — 复原
> - Inventory：手动刷新钮[前端]（threshold 见 B4·基本平迁+净升级）
> **客服(cs)**
> - ✅Customer360（还原批9 + **二轮批CS 真复原** 2026-07-05）：⚠️批9「数组明细逐行渲染」实为**假绿**（`mapPanels` 只处理顶层数组·真实 provider 全是「对象含嵌套数组」·塌成「N条」·根因#8）——二轮批CS 真复原：`PanelVM` 扩 rows+groups 逐行逐字段·全字段中文名 CS_LABELS·golden 换真实形状用例；身份头(getUser)/手机掩码/刷新/openid 全显 — 复原
> - ✅Kb（还原批9 + **二轮批CS** 2026-07-05·修 bug B3）：键输入框(=菜单叶子 id)+非空唯一校验｜载入预设FAQ(4条正确键)｜删除二次确认 — 复原·bot 路由断链已修；**二轮补**：保存前 trim key/question/answer(带尾随空格键绕过唯一校验入库却 bot 命不中·数据卫生)
> - ✅Conversations（还原批10 + **二轮批CS** 2026-07-05）：SLA阈值可调+报表刷新[slaMs]｜气泡渠道chip｜回车检索｜「已到最早」终点标识 — 复原；**二轮补**：超SLA/未答复标红(mapReport bad 标·批10 曾判「略」)｜非文本消息 kind 独立 chip(换皮吞进正文)｜首响口径脚注
> - ✅Csat/Agents（还原批10 完成 2026-07-05）：Csat 刷新+分布百分比+空态；Agents 建号后回显口令(超管转交)+停用/恢复成功反馈 — 复原
> **进销存 SCM**
> - 共性：ScmFlowTabs 顶部流程跳转条(1-7)[前端]｜confirmDialog/toast→内联(成功信息显红·顺带修)[前端]
> - 🟡ScmMaterials（还原批12 部分 2026-07-05）：✅修 bug B4 threshold 输入(后端就绪·只缺输入框)；**⏳SCM 顺手度批**：毛线 uom 强制count 守卫｜行内按料号查流水[就绪·listLedger materialId]｜供应商 note+编辑
> - ✅ScmPurchase（还原批12 + 尾批B/G/H 2026-07-05）：✅草稿编辑[purchaseId·editDraft 预填]｜✅状态筛选(尾批B)｜✅逐行小计+总价预览(尾批G)｜✅**planner 去开单预填**(尾批H·consumePurchaseHandoff onMounted 收备货缺口·新草稿带供应商+缺口行)
> - ✅ScmOutwork（还原批13 + 尾批H/I 2026-07-05）：✅草稿编辑[outworkId·editDraft]｜✅planner 去开单预填(尾批H)｜✅**收货引导抽屉**(尾批I·发 raw→收 knotted 同色预填/应付损耗实时预览/收≤发防呆前端拦+超发标红禁确认·后端 receiveOutwork 仍权威)
> - ✅ScmBom（还原批11 + 尾批F 2026-07-05）：✅修 bug B1 组装幂等(防双扣料双入库·assemblyId 页面持有成功才换新)｜✅**共用料限非毛线非成品**(commonCandidates 过滤·下拉+占位)｜✅**专属件下拉**(pkg/card 按类过滤·换皮裸文本框易打错料号)｜✅**差异位产品编辑+datalist**(点列表回填·datalist 既有 id·缺专属件标记)｜✅**pkg:/card: 自动预选**(命名契约·watch productId)｜✅**执行产品下拉**(限有差异位的产品)｜✅**组装记录用料展开**(expanded·点单号看 consumedLines 扣料快照)
> - ✅ScmPlanner（还原批13 + 尾批H 2026-07-05）：✅产销"现存库存"列(listInventory 三列并表)｜✅**去开单 handoff 跳转**(尾批H·外协缺口→发原团/采购缺口按供应商→setHandoff+router.push·目标页 consume 预填·lib/scmHandoff 内存单例读一次即清·golden 反向自检咬红)
> - **🟡SCM 顺手度批（尾批B/E/F/G 2026-07-05）**：✅ScmMaterials 行内查流水｜✅ScmPurchase 状态筛选｜✅**ScmFlowTabs 顶部流程跳转条**(尾批E·5 页共性·单源 lib/scmFlow.ts+组件·golden 焊死 path 集合===Shell SCM 组·防死链/漂移·反向自检咬红)｜✅**成功信息显红修**(尾批E·note()区分成功绿/失败红·4 页)｜✅**ScmBom 顺手度**(尾批F·共用料限非毛线非成品/专属件下拉/差异位编辑+datalist/pkg-card 自动预选/执行产品下拉/记录用料展开)｜✅**ScmMaterials 供应商编辑+note+毛线uom强制count**(尾批G·点列表回填/note字段/yarn 锁 count 禁选)｜✅**ScmPurchase 逐行小计+总价预览**(尾批G)｜✅**去开单 handoff**(尾批H·planner缺口→采购/外协预填·lib/scmHandoff 单例读一次即清·golden+反向自检)｜✅**ScmOutwork 收货引导抽屉**(尾批I·发→收同色预填/应付损耗实时预览/收≤发防呆)——**SCM 顺手度全清 ✅**
> **系统/导航**
> - ✅Settings（还原批14 完成 2026-07-05）：6 钱链告警事件开关(alertEvents·cards.ts 合并保存·B6 证伪)+推送状态chip+无webhook置灰 — 复原
> - ✅系统顺手度（尾批A + J 2026-07-05）：✅Login 坐席走错门指引｜✅Shell 侧栏折叠+localStorage持久+进组自动展开｜✅**External 3 云卡 envId 深链**(尾批J·用户「能做的都做了」拍板·envId 从 VITE_ADMIN_API 派生不硬编码·只给 3 个 env-scoped 腾讯云链预选本环境·「预选本环境」chip 透明告知·**诚实边界守住**：续费/用量仍只说「登录后查看」不伪造动态数字)
> - ✅治理守卫（尾批A 完成 2026-07-05）：新 `rw-admin-nav-route-synced`(Shell↔router nav↔route 同步·防死链/孤儿页·根因#5·repoCheck 146·反向自检咬红) — 新线失守已补

## 深审残余（2026-07-05 全局深审·三路并行·P0/P1=0·机器可修 F1-F3 已修，下列为待人/待定项）
**已拍板（2026-07-05·决策§26）**
- **客户端「一行一售后」定为刻意**：applyRefund 拒后同行永久锁死（`orders.ts:307` + 确定性 `_id`）＝有意——每行只申请一次退款，复议走客服端。**结案不改客户端。**
- **客服端管理员越规退款＝新增能力**（已落 2026-07-05·`overrideRefund` action·cap `refund:manage`）：管理员凭退货管理权限越过资格规则（一行一售后锁/拒后锁死/已进课不可退）主动发起退款；**保留钱守恒**（退款 ≤ 该行实付分摊 − 全单已退·`refundShareFen` 单源封顶，越额被 NOTHING_LEFT 挡）。净新增、超出旧线 92 parity（决策§26 显式授权）。**留口**：若将来需「善意补偿超分摊额」＝另立补偿动作，非本动作。

**mp 真机走查窗口期·记账不动行为面（根因#8·不搅用户即将走查的表面）**
- ~~my-courses 加载失败回退「去激活」空态~~ **✅ 已修（勾销 2026-07-19·Phase3 批J 现场 grep 复核）**：my-courses.ts 已有 loadFailed 分支（:12/:31-35·失败不覆盖已展示课程·守卫 `rw-mp-list-loadfailed-state` 在场）。
- ~~order-list/reviews/aftersales 加载失败与空态不分~~ **✅ 已修（勾销 2026-07-19·Phase3 批J 现场 grep 复核）**：三页均有 loadFailed 态+onRetryLoad 重试入口（grep 计数 order-list 4/reviews 5/aftersales 5·wxml 重试各 1）。（原并记的「成功后 setTimeout(navigateBack) 无 onUnload 清理」已修 ✅——四页均有 onUnload+clearTimeout，守卫 `rw-mp-navback-timer-cleaned` 钉住；2026-07-09 现场 grep 复核勾销。）
- **订单号随机双击可建重单+双预留**（`orders.ts:40-47,177`·P2→已收窄）：限频 20/60s+超时关单兜底、错向不超卖；原挂账的「mp submitting 贯穿到支付结束」已落实 ✅（mp 自修循环 2026-07-06·checkout.ts 成功路径保持锁定不复位，2026-07-09 现场复核勾销）。残余面仅剩云侧随机单号本身（双端并发建单理论可撞，限频+关单兜底，非双击可达），维持挂账观察。
- **privacyGate open() 覆盖 pending 的并发窗口**（UNCERTAIN·需真机双击「添加照片」复现·静态判不了）：不改代码，只记账（bug sweep R1）。

**低危纵深·非阻塞（P3·可延后或刻意）**
- rewrite 线缺 transition-declared 前端消费侧守卫（`order-status-frontend-via-shared` 只扫冻结线 mp）——本批已补 cloud 侧 `rw-order-transitions-declared`；mp 侧状态字面量守卫待 M2 真机后补。
- kfSend 本体无会话归属自校（授权靠调用方 scopedLoad 前置·与旧线设计一致）；频控 best-effort（写失败静默不锁·错向安全侧）；agent pollThread 不自识 SESSION_LOST（10s 兜底）；Inventory/ScmPurchase 无前端 busy 闸（后端 CAS/两步确认兜底；跨行编辑态被顶掉一面已由战役 II 批G/H 快照守卫收口）；recallScan 并发双跑可重复推一次运营汇总（非钱链）。

## 2026-07-10 评审遗留
1. ✅ 已收口（2026-07-12 批 B1，评审后二次修正）：SCM 5 列表（scmPurchase listPurchases / scmOutwork listOutworks / scmAssembly listAssemblies / scmMaterials listSuppliers / kit/scmStock listStockLedger）全部改走 `kit/paging.ts` pageQuery（cursor 分页·超上限旧单可续查）；`listMaterials` 刻意不收口（picker 全量语义+主档规模有界，picker 消费点无参调用不动）。listSuppliers 用 `_id` 兜底 cursorField（供应商表无自然排序字段）——此路径撞上 pageQuery 复合游标 tie/span 分支在 `cursorField===_id` 时坍缩为同一条件的既有缺陷：首版仅按 `_id` 去重兜底页内重复，评审指出剩余行数落在 (limit/2, limit] 危险区时 `hasMore`/`nextCursor` 本身被算错、会造成跨页重复交付（首版 250 条测试样本恰落安全区未测出，"不重不漏"系过头结论）；已改法收口——调用前把复合游标 `{v,id}` 拆成纯值 `id` 再传 pageQuery，令其走无 tie/span 拼接问题的纯值游标分支（`cursorField===_id` 时 v===id 恒成立，拆分不丢信息），去重兜底整段删除，新增 350 条危险区回归测试钉住跨页 `hasMore` 正确收口（kit/paging.ts 全程未改，铁律不动）。前端 4 页（ScmMaterials 供应商列表+流水列表两处/ScmPurchase/ScmOutwork/ScmBom 组装记录）照 Conversations.vue/Orders.vue 模式加「加载更多」。新守卫 `rewrite/cloud/tests/scm-paging-bounded.test.ts`（源码扫描型·5 处断言禁裸 `.limit()` 直取）。详见 `重构日志.md` 批 B1。
2. ~~createOrder 无客户端幂等 token~~ **✅ 已修（勾销 2026-07-19·Phase3 批J 现场 grep 复核）**：mp `lib/checkout.ts:24-40` 每草稿一次 idemKey，云端 `app/actions/orders.ts:223-226` 消费（orderIdempotency 确定性 claim·2026-07-13 收尾判断力批E 落地，账目其后未销）。
3. 命名认知税三处（评审同义词专项）：售后数据层 afterSales vs 动作/UI 层 refund；cs 域 csSession/conversations/thread 三词丛；createOrder 入参 sku→落库 spec。择机统一·动库慎重。
4. 三端品牌口径三源：admin tokens.css（单源）/ agent App.vue（2026-07-10 批3 已上 rw-agent-tokens-synced）/ site 裸 hex×24+品牌文案硬编码（site 独立部署·接法待拍板）。
5. shared 生成常量族（PURCHASE_ORDER_STATUS/TRANSITIONS 及 ORDER_STATUS 同款）全仓零 import、真消费方是派生 JSON——「续留公共契约面 or 生成器停吐」待拍板。
6. mp Page 编排层竞态家族（_seq/unloaded）零单测（P1 覆盖缺口·纯函数化或 harness 待设计）。
7. 旧线专属守卫群（packages/ 作用面 fileRules×5、moneychain-alert-wired 等）＝守冻结参照·随 packages/ 处置拍板一并退役。**据实改账（2026-07-18 盲区体检）**：原「rw 版已就位（2026-07-10 批1）」记述失实——那批只镜像 4/5、漏了 `dep-direction`（T4 在活线零执行 8 天，台账比代码注释乐观正好盖住洞·病根#16 ②）；批2 已补 `rw-dep-direction` + `guard-scan-liveness` 活性下限（fileRule 活线零命中即红），此后这类「声称就位实则空转」由机器逮。**合流补注（Phase3 批I·2026-07-19）**：两条线曾各自独立补 `rw-dep-direction` 活线镜像，合并时融合为一条严格强于两者的版本（盲区体检的 mp 分层表+cloud kit 面+tests 排除 × 批I 的 require/反引号探测+剥行尾注释+admin/agent 扫描面）。
8. ~~spike-skyline 删除触发条件未定~~ ✅ 已随 2026-07-23 瘦身大作战删除（M2 早已完成·复验台使命尽）。系统事实旧线残段已同批退役归档。

## 运行期巡检机 + bug 收集器（用户拍板 2026-07-05·防治静默 bug·**已从独立 worktree 合并回 main**）
= 上节「观测批（病根#14）」计划的**代码侧落地**（①botpush=批5✅·②探活雏形=批2 inspect✅），并扩成完整「巡检机+bug 账本+告警」子系统。范围四拍板：巡检两层（基建存活 A + 业务不变量 B）／收集四路（服务端异常·不变量违反·关键流程失败·客户端错误）／记录+高危告警／只读看护线上（共用库）。守卫 `rw-anomaly-record-golden`/`rw-inspect-golden`/`rw-bot-push-single-seam` 均已**归口病根#14**（+原 #1/#3/#8/#12/#13）。批1 地基✅（anomalies + recordAnomaly·代码随 loop 落 5fff588）。
- **批2 巡检机 ✅**：`kit/inspect.ts` 引擎 + `inspectRuns` + `timers/inspect` + 守卫 `rw-inspect-golden`；三查（A1 db-reachable / B1 钱守恒 / B2 卡单+refund_required 死信）→写 inspectRuns + 红→recordAnomaly 闭环·只读·fail-soft·有界扫描 cap 500。**未做**：A2/A3 连接器活体 · A4 并 kfHealthProbe · B8 回调落地。**A5 定时器心跳 ✅**（Phase3 批H·`kit/observe.notifyHeartbeat`＋`kit/inspect.sendDailyHeartbeat`：timers/inspect 每 2h 跑时以确定性日 id `hb:<YYYYMMDD>`（Asia/Shanghai）写 inspectRuns 去重·每日经既有 botpush 接缝发「巡检机存活」·全绿/红灯都发·红项另有 alert 通道·心跳档 startedAt:0 不污染体检读路径 getInspectStatus）。同批：money-conserved 由逐 doc get 改 `_.in` 分块批量读（N+1 消除）；notifyRecall 接通推送（见批5 条）。
- **批3 可视界面 ✅**（2026-07-05）：`adminApi/actions/ops.ts` 4 action（runInspect/getInspectStatus/listAnomalies/resolveAnomaly·仅超管·只读业务数据）+ `pages/Inspect.vue`（体检面板·A/B 红绿灯 + 立即巡检）+ `pages/Anomalies.vue`（异常账本·四路筛选 + 标记已处理）+ router/Shell「系统」组入口 + 守卫 `rw-ops-console-golden`。系统事实 adminApi 93→97。**部署面（下步·待人工）见批5 部署面**（inspect + adminApiV2 含 4 新 action + rewrite/admin 托管一起部署）。
- **批4 采集桥 ✅**：`notifyAlert`→`recordAnomaly` 桥·动作失败自动进账本零改造·①服务端异常+③关键流程失败经桥覆盖·②不变量违反批2 闭环。catch-records-anomaly 全量守卫不做（rw-flow-observable 已覆盖·避重复 retrofit·病根#5）。
  - **批4b 客户端 intake**（已闭环）：mp 半边 ✅ 已于 2026-07-12 工业级完善批6 落地——本地函数 `app.ts::reportClientError`→`trackEvent('client_error',...)`→`app` 云函数 `actions/learning.ts` 桥接进 `recordAnomaly('client-error',...)`（非独立 adminApi action，此前「mp 冻结」措辞已过期，据实改）。admin/agent 半边 ✅ 本批（2026-07-19 批 B7）落地——两端 `lib/errorReporter.ts` 同构副本（window.onerror+unhandledrejection+Vue errorHandler 三件套→会话内去重封顶 20）打新 adminApi action `reportClientError`（服务端 agentId 二级节流 20/10min·cap 复用 `agent:handle`·`shouldAudit` 排除）。**mp 侧遗留窄债**：`anomalyFingerprint()`（`rewrite/shared/src/observability.ts`）的 ASCII 白名单会把 `app.ts`/`learning.ts` 里中文报错消息整段剥空、导致不同中文报错坍缩成同一条指纹——本批新写的 admin/agent 代码用 hashSig 短哈希避开同一坑，但 mp 侧现有实现未修（超出 B7 范围），留用户/后批处置。
- **批5 告警触达 ✅**（=观测批①）：botpush 接缝移植 + `notifyAlert` 推企微（读 adminConfig.alertWebhook·alertEvents 静音）+ 守卫 `rw-bot-push-single-seam`。
  - **批5 部署面·后端已上 ✅**（2026-07-05·用户批准·**真打共用生产** cloudbase-d4gcssqbv06865479）：`inspect` 部署（净新增·只读）+ 定时触发器 `inspectTimer` 每 2 小时 + `anomalies`/`inspectRuns` 建库锁 adminonly（已核）。真机验证 inspect 跑通、逮到卡单；真机逼出 recordAnomaly P1 已修（案C）。
    - **⚠️ 运营待办·线上真发现**：inspect 逮到 **9 单「付款超 72h 未发货」**（最早 2026-06-10·anomalies 账本有 samples 单号）——是真客户待发货还是老测试残单需人工核；核完在异常账本标记已处理（控制台上线后）或 `tcb db` 查。
    - **仍待人工**：~~adminApiV2+admin 托管部署~~ ✅ 已核销（2026-07-05 部署·2026-07-09 M5 转正 `/admin`）；`saveSettings` 配 `alertWebhook`（企微群机器人·用户暂无·配了高危才推）；集合真值 37→**39** 回写 `系统事实`/`库权限表`（现 `collection-count-synced` weld 老线 37·随 `packages/` 处置拍板时并）。
  - ~~notifyRecall 推送待接（stub·小 follow-up）~~ ✅（Phase3 批H：镜像 notifyAlert 推送半边·经同一 botpush 唯一接缝以 sev='recall' 推运营群·不接 recordAnomaly·fail-soft·调用方 recallScan 零改动）。

## admin2 逻辑自修循环·待人项（2026-07-06/07·loop 20 批修 69 逻辑 bug·两大轮各闭环·详见 `重构日志` 迭代A–I）
> 用户拍板「loop：admin2 完整实现·自检自修至无逻辑 bug」。**迭代A–H**（8 批·35 bug·迭代H 零确认闭环）后用户续 loop，换新会话 fresh eyes 再扫，**迭代I 共 34 确认 bug·12 修复批·round6 零确认闭环**：先按逐页维度逮 16（+re-audit 逮 5 自引入回归），**再换正交 concern 维度**逮出逐页漏的**跨页类** 13（round2–5：6+3+2+2）。**核心教益**：concern 维度逮出「载入失败→arm destructive write」跨页类，含 **2 条 P1 不可逆删档**（HelpVideos 失败载入后一存即 GC 删光云端视频 / Kb 失败载入后一存即整册删光知识库·bot 答案单源）——逐页 eyes 只看单页看不出「失败态 + 后端全档覆盖/孤儿 GC」的合谋，两维互补才扫净。新守卫 3：`nextNodeId`/`duplicateNodeIds`(cs-ui)、`currentNavGroup`(nav)、byCourse 合并(content-ui)。
- ✅**越规退款前端入口已接**（批 B8·2026-07-12）：`rewrite/admin` 售后退款页工具区新增「越规退款」入口——查订单号→选商品行→必填原因(trim 非空)+勾选「知晓越过常规售后流程」→按钮门控(`canOverrideRefund`)→调 `overrideRefund`；金额/退货资格仍全由服务端裁决（前端不传金额参数，钱链后端零改动）。
- ✅ **`refund:manage` cap 现只对超管 `'*'` 可达——非超管中间角色若将来被授予该 cap，整个越规退款 UI 仍不可用**（批 B8 评审发现·2026-07-12·**已修 2026-07-18 Phase 3 批C·见 `重构日志.md` 同日条**）：`overrideRefund` 单立 cap `refund:manage`（注释明确写「未来可给中间角色单授而不放全量 admin:write」），但售后退款页最小只读面 `listRefunds`/`refundCounts`/`getRefundDetail`/`listOrders` 此前均**未登记 `ACTION_CAPS`**、按默认拒落到 `admin:write`——即便给中间角色单授 `refund:manage`，该角色在页面加载这一步就已 403。**当年现状影响面＝0**（仅 `ROLES.superadmin=['*']` 天然含此 cap，是潜伏落差非活跃 bug）。**修法（已落）**：把能力闸判定升级为支持「任一命中」cap 数组（`ACTION_CAPS` 值 `string | string[]`·单值语义不变），四个只读 action 登记为 `['admin:write','refund:manage']`——`admin:write` 持有者行为完全不变（防收紧误伤），`refund:manage` 独立可用（债目意图）。写类 `approveRefund`/`rejectRefund` 刻意仍按默认 `admin:write`（不属「只读加载面」·越规退款查找步骤不依赖）。改动落 `rewrite/cloud/src/functions/adminApi/index.ts`（Phase 3 批C 白名单含此文件）。**批C 收尾 P2 评审补丁（2026-07-18）**：`listOrders` 无过滤时是全店订单浏览面（逐单回全文·含 `_openid`/`address` 姓名电话详址/`items`/`transactionId`/`amount`），能力闸只按 action 名放行、看不到 `data` 有没有过滤——`refund:manage`-only 若可不带 `q` 翻页即等于批量导出全店客户 PII（同 `lib.ts` `ROLES` 注释「刻意去掉裸 `customer:view` 防批量导出」一类风险）。修：`listOrders` handler 内对「仅持 `refund:manage`（无 `admin:write`/`'*'`）」的调用方强制精确单号 `q`、拒无过滤/状态浏览（返 403 `ORDER_NO_REQUIRED`），退款专员仍能核对「越规退款这一单」；`admin:write`/`'*'` 全店浏览行为完全不变。落 `rewrite/cloud/src/functions/adminApi/actions/orders.ts`。
- ~~`faq-via-kb-single-source` 待扩面覆盖 `getPublicFaq`~~ **✅ 已落地（2026-07-12 批 B8 收口·主脑亲手）**：`scripts/check-structure.mjs` 该守卫 run() 已扩扫 `rewrite/cloud/src/functions/app/actions/faq.ts`（必读 kb 集合 + 禁 title/content 字符串字面量内联·文件缺失也红），反向自检（内联假 FAQ → 守卫红 → cp 还原逐字节一致 → 绿）已现场验。
- **⚠️ 真机走查（根因#8「构建过≠真能用」·工具/单测证不出的并发时序/交互）**：① 会话过期即导登录（6 页）；② SCM/库存/对账动作成功/失败提示不被 reload 吞、失败显红；③ 看板副请求失败不显绿「无待办✓」；④ Products 恢复销售二次确认 + 关闭编辑器不丢最后编辑；⑤ ScmBom 未存模板/改产品清缺料预演；⑥ Cards 定稿落库确认、尺寸清空不崩；⑦ Courses/HelpVideos/Checkpoints 重排后删不删错、HelpVideos 上传中重排不错位；⑧ 各页 autosave 慢网连编不丢/不乱序覆盖；⑨ 订单/退款/会话快切标签/搜索 + 翻页在途切换：不跨栏混排、不跳页、双击「更早」不重复。
- **⚠️ 真机走查·迭代I 新增高值场景**（根因#8·工具/单测证不出）：① Checkpoints 改课号框未点载入时「保存整课」仍写已载入课、不覆盖别课；② Batches 生成后不跳到框里别的课、刷新失败有提示；③ Wizard 步3 补齐 SKU 后顶部上架闸即解锁（不用切步强刷）；④ Customer360 取数失败后仍能「返回结果/刷新」不成死胡同；⑤ 向导侧栏当前步唯一高亮（非 6 步全亮）；⑥ ScmBom 切产品后规格框不残留旧值、不生成幻影成品；⑦ Fulfill 连扫下一箱内码不丢选择；⑧ Inspect/Anomalies/Kb 等动作失败显红不被刷新吞、刷新失败不被「已保存」盖。
- **⚠️ 真机走查·载入失败保护（迭代I concern 维度·断网/弱网真机验最直观）**：断网/弱网首屏打开 **HelpVideos / HomeContent / Kb / Settings / ScmBom** 五页，各显「加载失败」后**编辑并点保存被拒**（提示「未成功载入…请刷新重试」）、绝不把空表单存下去删档/覆盖真实配置；恢复网络刷新后保存正常；合法「清空后保存」（成功载入的空库/空模板）仍可存。
- ~~部署 admin2（loop 迭代A–I 共 20 批 + 批次C Batches.vue 导出闸）~~ ✅ 已核销：全部改动已合 main，随 2026-07-09 M5 托管清空换新上正式路径 `/admin` + 2026-07-12 清空重传上线（admin2 已退役）。

## 治理债（不阻塞里程碑）
- **site 的 .astro 模板不在类型闸内（2026-07-12 批2 评审 P3 留档）**：根 typecheck 已纳 `tsc -p rewrite/site --noEmit`，但 tsc 只见 .ts（schema.ts/config.ts/端点），`Base.astro` 等模板 props 流转零类型检查（本批人工核对过正确）。要全覆盖需加 `@astrojs/check`+`astro check` 依赖挂进 typecheck——依赖成本 vs 10 页站规模，暂记债不做（守卫 `rw-site-in-gates` desc 已如实限定「schema.ts 等 GEO 承重层」）。
- ✅ **/refactor-batch skill 仓名漂移**（2026-07-05 巡逻轮#1 发现·已修 57e8b35 巡逻轮#6：`SKILL.md` 铁律段/收尾步已按 fable 身份段改写——样板房/生产仓/禁部署旧文→fable 工作仓/冻结仓 next/部署过闸）：本条债目其后被 35f372f 误重复带回账本；批E（2026-07-09）现场核实发现第33行收尾步仍残留一处「样板房不走异步 PR」（57e8b35 只改了铁律段与收尾步11/12，未触及这行），本批顺手订正为「本仓不走异步 PR」，`grep -n 样板房 .claude/skills/refactor-batch/SKILL.md` 复核零命中——**本条勾销**。

## 挂起问题（用户拍板「最后一起处理」）
- ~~退款工作流平台侧失效（调试案 A·2026-07-04）~~ **✅ 已结案（本条曾 stale·2026-07-23 瘦身大作战核正）**：调试案 A 终局记载——「平台侧漂移」假设已被推翻，真根因＝内部 `_id` 含中文违反微信单号正则；代码修（`refundNoFor`+守卫）后已在生产完成端到端真实退款验证（2 元原路退回·refundCallback 确认到账）。案文见 史料「调试日志-fable-结案-至20260713.md」 案 A。
