# 进销存 ERP（供应链）

> **这是什么**：本文件夹 = 「进销存 ERP」子项目的**活文档家**（需求定稿/施工蓝图/进度）。用户 2026-07-02 从需求出发逐条拍板后确立本线；范围＝**进销存最小闭环**（采购/外协→原料账→按模板组装→成品账→发货核销），非全套 ERP（财务凭证/多仓/批次效期 明确不做·CLAUDE §7 防过度工程）。
> **不占 docs-budget**：`docs-budget` 只数 `docs/` 顶层 `.md`，本子文件夹不计入。需求登记：`../需求清单.md` R31。

## 一句话

把「备货」从拍脑袋变成两本账一张模板：**采购/外协进原料账 → 按全局唯一配方模板打包（原料账−·成品账+）→ 订单发货核销留痕**。成品账/防超卖/RBAC/审计/后台骨架全复用现有，新建的只有「原料侧」。

## 需求定稿（用户 2026-07-02 会话逐条拍板·provenance=会话记录，勿再猜）

- **物料与计量**：每料号建档锁死计量方式（件 或 克·**一律正整数**）。毛线＝**颜色×量级（三档克重 大/中/小）×形态（原团/带结）** 按团数记账，**仅最大团有带结形态**；填充棉按克；其余（外包装/二维码卡片/钩针/记号扣/缝针/钥匙扣/玩偶眼睛）按件。外包装与卡片印刷随产品变＝每产品专属料号。
- **外协（管到发料领用·三档里最完整档）**：织女每人一档；发**最大团原团**（按色）→ 收**带结团**（同色同量级入库）→ 损耗=发−收可见 → 计件工钱应付（收回数×单价）→ 结算销账。
- **配方（BOM）**：所有产品构成几乎一致、数量重量完全相同 → **全局一张模板** + 每产品三个填空（三个量级各选颜色 + 专属包装料号 + 专属卡片料号）。最大团用带结、中/小团用原团（模板里标）。组装单存**模板快照**、不回读（历史单不追新）。
- **核销**：沿用「下单即扣成品库存防超卖」现有机制（不改钱链热路径）；发货动作核对实发=订单数并写流水留痕——「如实核销」靠流水可查兑现。
- **激活码交叉核对（可选后置）**：卡片入库数 ↔ 该产品激活码池 unused 数对账（印错/漏印可见·复用现有 qrcodes 线）。

## 板块清单（构成·**状态只看下方进度表**·防双源漂移）

| 批 | 板块 | 自包含构成 |
|---|---|---|
| SCM-0 | 地基：主档 + 两本账接缝 | 7 新集合登记 + 接口五门（蓝图§3）+ `scm.spec.ts` 状态机声明 + 物料/供应商主档 CRUD + admin 物料页（含**期初盘点/调整**） |
| SCM-A | 采购线 | `purchaseOrders` 状态机 + 到货入库 + admin 采购页 |
| SCM-B | 外协线 | `outworkOrders` 状态机 + 发料/收料/结算 + admin 外协页 |
| SCM-C | 配方组装线 | 模板/`bomProfiles` 管理 + `assemblyOrders` 执行（扣原料·入成品）+ admin 配方/打包页 |
| SCM-D | 计划核销线 | 备货计算器（目标套数→采购/外协缺口清单）+ 发货核销流水 + 激活码对账（可选） |
| SCM-Z | 整合收尾 | master 登记面并集 + 部署 + 真单验收 |

## 进度（**本表＝状态单源**·其他文档只引不抄）

| 序 | 批次 | 前置 | 状态 |
|---|---|---|---|
| 1 | SCM-0 地基（master 串行·先） | 无 | ✅ 代码完成（2026-07-02·`feat/scm-0` 独立 worktree·避让主树 M⑦ CC）：7 集合登记+库权限期望表 · 门1 `kit/scmStock`（applyStockMoves 幂等流水+CAS·主档 uom 锁）· 门2 `scm.spec.ts` 两状态机+gen 扩域+对账守卫扩扫 scm\*.ts · 门3 `resolveBom` 纯函数 · 门5 `actions/scmMaterials` 6 action（默认拒仅超管·写类自动审计）· admin「供应链」组+物料/供应商页（期初盘点/调整必留因）· 守卫 +3（结构×2+test×1）反向自检×4 咬红 · 测试 885→911 · **门4 produceStock 纪律修正随车道 C**（蓝图§3）。**✅ 已整合上 main**（2026-07-02·rebase 到 M⑦ 车道B 之上取并集[adminApi import/系统事实计数]·check 全绿 122 守卫/919 测试·main `ba0509a`·tag `scm/0/merged-1`）。**剩部署三步（用户终端·CC 部署被权限层拦）**：① `deploy-fns`（树已含 M⑦ 免登代码·不会滚回）② invoke `initDb` 建 7 新集合 ③ 7 集合锁 ADMINONLY（CLI `tcb permission set` 或控制台·对照 `console-assets/02-库权限期望表.md`）；后加期初盘点录入（靠人） |
| 2 | 第一波并行：SCM-A 采购 ‖ SCM-B 外协 | SCM-0 | ⬜ |
| 3 | 第二波并行：SCM-C 配方组装 ‖ SCM-D 计划核销 | SCM-0（门3/门4 已在 0 定义） | ⬜ |
| 4 | SCM-Z 整合 + 部署 + 真单验收 | A–D | ⬜ |
| 靠人 | 控制台锁 7 新集合仅管理端 · **期初盘点录入**（不录=账实必偏）· 真单真机验（根因#8）· 模板数值/单价由用户在 admin 填 | 随批 | ⬜ |

## 指针（单源·不重抄）

- **领域模型/接口收束/守卫候选/车道拆分**：`施工蓝图.md`
- **多 CC 并行机制（通用·worktree 隔离/git tag 自动中继/整合纪律/CC 冷启动工单）**：`../后台360工作站/并行工作流.md`——本线只定车道内容（蓝图§4）、机制不重抄
- **需求正册**：`../需求清单.md` R31 · **接口/集合客观事实**：`../系统事实.md`
- **成品账现有实现（原料账的镜像先例）**：`packages/cloud/src/kit/inventory.ts`（守卫 stock-atomic-conditional）
