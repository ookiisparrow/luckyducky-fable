# 工作日志

> 按日期记录工作进展(最新在上)。配套:`调试日志.md`(bug)、`技术债与重构.md`(欠债)、历史审核报告见 `archive/`。

## 2026-06-10 · 沉浸页自动化视觉验证 → 调试日志 A/B 关账

新增 `scripts/visual-check.cjs`(miniprogram-automator 连微信开发者工具):四沉浸页
(detail / catalog / welcome / player)浮层按钮矩形 vs 胶囊/状态栏自动判定全 ✓(与胶囊中线差 0px)
+ 截图眼校通过,调试日志 A/B 关账。脚本留存复用于以后 UI 验证,两个工具坑已写进脚本注释:
`automator.launch` 握手偶发挂死(改 connect 优先);`callWxMethod` 调同步 API 不回包(改 `evaluate`)。

## 2026-06-10 · 代码优雅化批次（独立技术债清零）

按「符合项目目标的前提下尽量优雅」执行:只做标 `[独立]` 的债(不碰 `[结合云开发]` 的 #3/#4/#5,P2/P4 时捆绑做),一事一 PR(#5–#8),lint/test/双端 build 全程绿。

1. **PR #5 调试日志 A/B**:`systemBar.js` 加 `getSystemBarVars()`(CoNavBar 复用);四沉浸页浮层与胶囊同水平带 + `--gap` 让位;播放页只动浮层、视频铺满到顶(黑块成因消除)。A/B 转 🟡 待真机眼校。
2. **PR #6 技术债 #2**:HelpSheet(949 行)归位 `pages/player/components/HelpSheet/`,拆壳 + 5 面板 + data.js;toast 副作用上提;`mmss`/`parseDur` 收进 `utils/format.js` 补测试(16→18)。
3. **PR #7 技术债 #7 + 审核 #10**:`constants/storage.js`(STORAGE_KEYS)+ `composables/useTimers.js`(onUnmounted 自动清理),7 页 setTimeout 全收口。
4. **PR #8 技术债 #6**:双写实测仅 4 class 且 co-addr-tag 已漂移;取值收进 `styles/co-mixins.scss` 单源、两侧 @include(不破坏 scoped 边界,产物等价已验证)。

独立技术债至此**全部清零**(#1 v0.1 已修),剩余 #3/#4/#5 按计划等云开发阶段。审核 P3 #8(user→profile 默认值)同属云改造覆盖范围,#11(页面偏大)非硬伤,均不单独处理。

## 2026-06-10 · 根本需求复盘 → 设计规格 v2

用户完整重述根本需求,逐条对照现行设计,定下三个结构性决策并修订规格。

1. **三个决策落定**:① 退货权**按单件商品**(`refundable` 下沉订单条目级;任何人确认均生效;发货时码绑订单条目);② **小片段=独立视频文件**(courses 改三层:chapter 大章节 / lesson 中章节 / segment 小片段,目录只显两层);③ **放弃获取手机号**(免企业认证依赖 + 按次付费接口)。
2. **新明确的需求细节进规格**:扫码跳**该课程**的两页式欢迎页(第 2 页确认即失该件退货权);分步播放交互(顶部分段进度条=segments,segment 播完自动停 + 重复播放/下一段);重复扫码路径(本人→继续学习,他人→已被激活);指导窗口沿用 HelpSheet。
3. **余下待确认**收口进规格 §七(后台形态/视频存储/支付资质/二维码类型/客服形态/商品课程基数/H5 定位/播放细节),其中支付资质建议立即启动申请。
4. 同步 `项目现状.md` 下一步(P2 前先修调试日志 A/B、上云捆绑技术债 #3/#4/#5)。

## 2026-06-10 · 项目记录精简 + 状态单一来源

通读全部文档后做的一次「记录瘦身」,不动代码、不动既定决策。

1. **状态收口单一来源**:CLAUDE.md 去状态化(§1 状态段 / 下一阶段重点 / §15 已知缺口 删除,指向 `项目现状.md`),并新增「记录约定」——每个里程碑只更新 工作日志 + 项目现状 两处,CLAUDE.md 仅约定变化时改。
2. **归档**:`审核报告-v0.1.md` → `archive/`(结论已沉淀进 技术债清单,与前两轮外部审核同等处理)。
3. **精简**:`微信云开发文档梳理.md` 644 → ~200 行(删官方教程复述,留坑/清单/安全/tcb CLI/链接,完整版在 git 历史);`调试日志.md` 已结案 3 条压缩为「现象 + 教训 + 状态」短条目。
4. 顺手修两处失效引用(代码标准 §12 指向、 工作日志头部审核报告指向)。

效果:现行文档 1393 → ~1175 行;每里程碑需同步的文件从 3 处降到 2 处。

## 2026-06-07 · 商品数据上云(P0 续)

接着 v0.1,把「商品」作为第一条业务数据端到端上云,作为课程 / 订单的样板。

### 做了什么
1. **两个云函数**:`seedProducts`(幂等灌 5 个商品,用业务 id 作 `_id` + `doc().set` upsert)、`getProducts`(按 sort 返回列表)。
2. **数据层收口**:`utils/cloud.js` 加 `callCloud`(集中 `wx.cloud.callFunction`,`wx` 守卫 + 条件编译);`api/shop.js` 的 `getProducts` 接 `callCloud`,H5 / App 回退本地 `catalog`;新建 `store/products.js`(商品 store,不持久化,页面从它取)。
3. **页面接入**:首页 `FeatureProducts` 用 `store.featured`(映射回字符串价喂 `ProductCard`);详情头部 `await store.load()` → `getById` → 本地兜底;`App.vue` onLaunch 加 `load()`。删死代码 `data/products.js`。
4. **部署 + 验证**:补 `cloudbaserc.json`;`tcb fn deploy --all`(自动云端装依赖)+ `tcb fn invoke seedProducts`(写 5 条)+ invoke `getProducts`(读回 5 条,CLI 层面证明数据在云端)。
5. **安全边界**:直接改 live 库做「改价铁证」被 auto 模式安全策略拦(写生产库 / AI 自助加权限规则均拒);改用 Console 无回退警告 + CLI invoke 验证,改价由用户自行执行。

### 产出
- 新增:`cloudfunctions/seedProducts`、`cloudfunctions/getProducts`、`store/products.js`、`cloudbaserc.json`。
- 改:`utils/cloud.js`、`api/shop.js`、`App.vue`、`FeatureProducts.vue`、`detail/index.vue`;删:`data/products.js`。
- 健康:lint ✓ / test 16 ✓ / build 双端 ✓。

### 下一步
- 真机眼校:首页 / 详情商品来自云端(看 Console 无回退警告)。
- 课程 / 订单照此三件套模式上云。

## 2026-06-07 · v0.1 里程碑

一天从「回顾工作」开始,完成了从功能快赢到 **v0.1 上云**的完整一程。

### 做了什么(时间线)
1. **纯前端快赢**:接通详情「为你推荐」→跳详情、首页「全部买家秀」→跳评价页。
2. **非沉浸页避微信胶囊**:新建 `utils/systemBar.js`(动态状态栏+胶囊,取代 `env` 硬编码),修好 10 个导航页 + 购物车 + 「我」紫头(`CoNavBar` 一改修 10 页)。
3. **购物车脏数据排查 → 系统性调试/质量体系(四支柱)**:① `logger` 全局错误捕获;② `persist` sanitize 数据契约 + `validate`;③ `调试日志.md`;④ Vitest 回归(`npm run test`,16 用例)。
4. **外部审核对账**(可维护性 + 拓扑):核实分类,修 P2 购物车 qty 契约 + P1 结算空草稿守卫,其余记技术债。
5. **真机验证**(微信「预览」)指引;UI 定位问题经确认非系统问题,**暂挂**。
6. **需求沟通 → 设计规格**:明确「实物购买 → 收货扫二维码 → 激活课程绑账户 → 确认进入即失退货权 → 只看自己买的课」全栈业务;后端定 **微信云开发**;产出 `设计规格-课程电商系统.md`(数据模型 / 核心机制 / 分阶段 P0–P5)。
7. **代码标准**:优化桌面初稿 → `代码标准.md`(21 维 + P0–P3 + uni-app 专项 + red flags)。
8. **v0.1 审核**:按代码标准全量审核 → **无 P0/P1**,产出 `审核报告-v0.1.md`;修唯一真 bug(`sanitizeCart` 小数 qty,要求正整数 + 补测试)。
9. **上 GitHub**:私有库 `ookiisparrow/luckyducky-miniprogram` + `main` 全历史 + **`v0.1` tag** + `Git协作约定.md`。

### 产出
- **7 个 commit**(跳转 / 避胶囊 / 质量体系 / 审核修复 / #1 / v0.1 文档 / Git 约定),全部已推 GitHub。
- **`v0.1` tag**(主版本节点)。
- **新建文档**:设计规格 / 代码标准 / 审核报告-v0.1 / 技术债与重构 / 调试日志 / Git协作约定(+ 本工作日志)。
- **新基建**:`logger` / `validate` / `systemBar` / Vitest。

### 当前状态
- 健康:`test`(16/16) + `lint` + H5/小程序 `build` 全绿,无 P0/P1。
- 本地 = 远程(GitHub 私库);工作树仅剩 3 个旧审核 `.md` 未跟踪。

### 待办 / 下一步
- **待修**:调试日志 A(沉浸页浮按钮避胶囊)、B(播放页黑块);技术债 #2–#7(部分结合云开发做)。
- **待确认**(开 P0 前):云开发四决策(后台形态 / 视频存储 / 支付资质 / 二维码类型)、管理后台谁做、视频教程细化。
- **下一步**:P0 云开发(建议走 `feature/cloud-p0` 分支,需先开通云开发环境拿环境 ID)。
