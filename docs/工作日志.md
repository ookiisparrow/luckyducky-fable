# 工作日志

> 按日期记录工作进展(最新在上)。配套:`调试日志.md`(bug)、`技术债与重构.md`(欠债)、历史审核报告见 `archive/`。

## 2026-06-11 · 会话收官记录（管理控制台从设计到投产的一整天）

单日完成「管理控制台」全周期:规格三轮迭代(§六/§七/§八)+ 设计稿十屏(Pencil)+ 实现九批(PR #37–#45)+ 部署上线 + 体验打磨(PR #47–#50)。同日小程序侧:SKU 全链路/首页与详情内容全面接云/分段真视频播放/进度上云收尾。

**交接清单（新会话从这里接）**:
1. **待合并 PR**:#47(控制台部署配置)、#48(上架必填标注)——功能已在线上,差代码归档;合并后照例同步 main。
2. **待用户验收**:验收单 B/E/F/I/P/Q/R/S/T/U(攒一次走完约一小时);U 是当前最新(扫码落地页,微信扫控制台「测试二维码」即验)。
3. **外部依赖**:①商户号提交(用户称很快;到位后开工 P4 真实微信支付)②域名 luckyducky.cn 绑定+微信扫码规则(用户暂缓;印刷投产前完成,已生成的码届时无需重印)。
4. **长期提醒**:云环境续费 **2026-12-07** 前;域名按年续费;管理口令=店铺钥匙。
5. **关键入口**:线上控制台 https://cloudbase-d4gcssqbv06865479-1440904924.tcloudbaseapp.com;扫码落地页 同域名 /q/;adminApi 服务地址见 admin/.env.production。
6. **流程教训已制度化**:每批独立 PR、用户合并确认后再开下批(PR 合并竞态两次教训);文档补丁先验证锚点在当前分支存在。

## 2026-06-11 · 扫码链路默认域名测试模式（用户要求先测畅通性）

域名绑定暂缓,用静态托管默认域名先把扫码链路跑通(自动跳小程序那一跳需自有域名+微信规则,边界已向用户说明)。

1. **落地页** `/q/`(admin/public 随控制台一起托管部署):品牌化页面,读 `?code=` 显示激活码 + 复制按钮 + 三步指引 + 退货提醒;非微信扫码(普通相机)的兜底页,正式规则配好后微信扫码不再经过本页。
2. **URL 格式统一为 query 式** `<前缀>?code=<码>`:托管目录页与微信前缀规则两吃,welcome 的 parseActivationCode 本就两种都认;印刷包 CSV 与示例同步;网址前缀默认值改为托管默认域名(测试期),正式印刷前换 luckyducky.cn。
3. **控制台「📱 测试二维码」**(批次行新按钮):取该批次首码生成二维码弹层(qrcode 库本地渲染)——手机微信扫屏幕即验:扫码 → 落地页显示激活码与指引;已部署上线,落地页 200 + 内容验证 ✓。

## 2026-06-11 · 播放页按段播放真实视频（内容管线端到端闭环）

决策 §14 预留的配套改造落地——控制台第④步传的视频此前学员端播不出来（播放页仍占位等分),本批打通。lint / test(63)/ 双端 build 全绿。

1. **分段视频模式**:课时的**全部小段都有 videoFileId** 时自动启用——每段独立播放(src=该段 fileID,小程序 video 原生支持 cloud://),段末=视频自然结束(onEnded 报 segment_done + 弹「重复播放/继续」,继续=切下一段 src);分段进度条按段填充;拖动条 = 当前段内时间轴。
2. **回退保真**:任一小段缺视频则整课时维持原「占位视频按时长等分」行为(含段末自动停),样例课程零回归;H5 无云自动回退。
3. **进度语义**:文件模式 watch_at 的 at/dur 为段内秒数(继续学习卡显示段内位置),segment_done/进度折叠/目录角标不受影响。

## 2026-06-11 · 架构答疑存档（服务器/数据位置/CDN/后台入口/限制评估）

控制台收官后用户连续四问:有无云服务器→数据和视频存哪→CDN 怎么搞→后台面板在哪→功能是否受云开发限制。逐一解答并全部落档:

1. **梳理 §12 新增**「架构形态与边界」:Serverless 无实例模式解释、上海地域多副本、存储目录约定、CDN 自带、两个后台入口、已踩限制与解法对照、规模阈值清单(配额→聚合上限→文档上限→冷启动)、**续费 2026-12-07** ⚠️。
2. **现状新增「运维事实」段**:含 管理控制台尚未部署(待办:静态托管发布)、CMS 暂缓、限制评估结论(业务形态在舒适区,先碰费用墙非功能墙)。

## 2026-06-11 · 数据看板上线（控制台开发清单收官）

侧边栏最后一个「规划中」落地。lint / admin build 全绿;adminApi 已部署、路由探测通过。

1. **聚合云函数** `getDashboard`(只读,≤1000 条/表内存聚合):四张统计卡(用户数/订单数+GMV/激活码激活率/在学学员)+ **热点**(看完次数 Top 段)+ **卡点**(学员最后停留 Top 段——重讲与加引导的选题依据,埋点「一次两用」的兑现)+ 最近订单。
2. **Dashboard 页** + 侧边栏「数据看板」转正(去「规划中」标)。
3. **控制台开发至此收官**:六步上新流水线 + 小程序橱窗(商品/文案/FAQ)+ 双面卡片印刷包 + 数据看板,小程序可变内容与经营动作全部图形化。

## 2026-06-11 · 详情页图文与套装清单接真（详情页样例时代收官）

详情页最后两块样例接管。lint / test(63)/ admin + 小程序双端 build 全绿;adminApi 已重部署。

1. **三块内容入商品档案**(第②步「商品信息」新增编辑区):参数表 [[k,v]]≤8、图文段落 {lead,body}≤4(**配图自动取第①步其余图的第 N 张**——零额外上传)、套装清单 {icon,name,qty}≤8(图标从既有 svg 选);白名单+上限,publishProduct 透传。
2. **小程序详情页**:云端三块非空则覆盖,否则维持通用样例(老商品零回归);DetailParams 段落图位接 src。
3. 至此详情页可变内容全部可由控制台管理:画廊(①图)、价格规格(②③)、图文/参数/套装(②)、配套课程(④)。

## 2026-06-11 · 橱窗逐块接入②③：首页文案 / 信任条 / FAQ 上云可编辑

按规格 §八 路线推进。lint / test(63,+2)/ admin + 小程序双端 build 全绿;adminApi + 新函数 getContent 均已部署、探测通过。

1. **内容上云**:`content` 集合 doc `home`{hero/trust/faq};读侧新函数 `getContent`(只读,无记录返回 null);写侧 adminApi `getHomeContent/saveHomeContent`(白名单+条数上限)。
2. **小程序**:`api/content.js` + `store/content.js`(按「块」覆盖——云端空块仍用本地默认,**防误清空线上**);首页 Hero/TrustStrip/FAQ 改读 store(Hero 本就收 props,信任条/FAQ 是纯 props——技术债 #4 的红利)。
3. **橱窗编辑**:Showcase 面板新增 头图文案(主标题/口号,左侧手机实时预览联动)/信任条(3 短语)/FAQ(问答增删,≤8 条);防抖自动保存,小程序重开生效。

## 2026-06-11 · 详情页 SKU 全链路接真（选规格 → 购物车 → 云端定价 → 快照）

控制台配的规格此前未接到小程序——新品详情页仍显示样例规格,黄金路径真实缺口。本批全链路打通,lint / test(61,+4)/ 双端 build 全绿;**createOrder 部署待用户执行**(定价逻辑,权限闸)。

1. **详情页**:有真实 skus 的商品,「已选」行弹原生 ActionSheet 选规格(跨端),价格与已选名实时切换;样例商品保持原占位行为。加购/立即购买携带 sku,条目 tag 镜像规格名(购物车/结算行即见)。
2. **购物车双键**:条目增 `sku` 字段,合并/删改/选中/结算扣减一律按 **id+sku** 定位(同 id 不同规格=独立条目);persist 契约归一 sku;既有签名向后兼容(sku 缺省空串)。
3. **云端定价 SKU 感知**(安全底座延续):createOrder 条目可带 sku——云端必须在 products.skus 中找到该规格(价格以云端为准,找不到整单拒 UNKNOWN_SKU);规格名进订单快照 spec。本地回退镜像:spec 透传、价格用本地目录价(演示链路)。
4. **测试**:+4(同 id 异 sku 独立条目/定位不串台/契约归一/快照 spec 透传);persist 期望同步。

## 2026-06-11 · 反面信息定稿（五项 + 退货小字提醒）+ 排版保完整

用户定反面信息清单:主文案/副文案/二维码/微信扫码引导/**激活后无法退货(小字提醒)**,并要求排版保证完整显示。

1. **模型**:back.texts 增 `warning`(退货提醒,50 字上限,默认「提示:激活课程后,该商品将不再支持退货」——与确认进课失退货权的业务规则呼应);按清单移除三步说明;旧卡兼容迁移同步更新。
2. **排版**:SVG 反面自上而下按比例布点(横版 90×54 与竖版 105×148 均验算不挤压);预览镜像同步(弹性撑开、警示小字灰色置于品牌条上方);扫码引导加粗升级为主要行动指引。
3. 预览改竖排(用户上一条反馈):正面上/反面下,右侧面板保完整宽度。

## 2026-06-11 · 卡片改双面（插画面 + 二维码面）+ 设计系统补全

用户反馈:卡片分正反面,二维码一面、插画一面,设计系统完善一些。规格 §六 修订,当轮落地。

1. **数据模型 v2**:cards 改 `front{art,bg,showBrand}` + `back{bg,texts,steps×3,brandText}`,云端读时兼容迁移旧单面草稿。
2. **设计器升级**:正反两张卡并排实时预览(竖版自适应宽);正面=满版插画(取封面/上传/衬底色/品牌角标开关),反面=标题/副文案/扫码提示/**三步说明可编辑**/**品牌条文字可编辑**/独立底色;色板扩到 6 色。
3. **印刷包升级**:`卡面-正面-插画.svg` + `卡面-反面-二维码.svg` + 地址 CSV + 双面印刷说明。

## 2026-06-11 · 控制台 v1.5：第⑤步卡片设计器 + 第⑥步码批次与印刷包（六步全通）

橱窗(PR #40)合并后实现 v1.5,**上新六步至此全部可用**。lint / test(57)/ admin build 全绿;adminApi 已部署、新路由 invoke 在线。

1. **第⑤步 StepCards**:实时预览 + 尺寸预设/自定义 mm + 图案(取封面/上传插画)+ 文字 + 底色;防抖自动保存 `cards` 集合;「定稿并启用」闸第⑥步。
2. **第⑥步 StepBatch**:卡面定稿前置检查;网址前缀设置(adminConfig.settings,衔接决策 §13);批次列表(码数/已激活,服务端聚合);生成批次**服务端互调 genQrcodes**;**印刷包.zip 浏览器打包**(jszip)= 卡面 SVG + 二维码地址.csv(BOM)+ 说明.txt。
3. **完成度闭环**:product 增 cardStatus/batchCount,⑤⑥圆点真实判定;六步全绿 = 可投产。

## 2026-06-11 · 小程序橱窗第一期实现（一比一排序 + 上下架）

设计(S10/规格 §八)拍板后当日实现;手机框按用户要求 **iPhone 17 比例(393×852)**。lint / test(57)/ admin build 全绿;已部署。

1. **后端**:`listShowcase`(products + 封面临时 URL)/`saveShowcase`(批量 {id, sort, featured} 白名单更新)。
2. **管理端** Showcase 页 + 侧边栏入口:手机框渲染首页关键区块(与小程序同一份数据);排序/上下架面板;**双向联动拖拽**(紫色落点线),700ms 防抖保存;开关 = featured。
3. **小程序端零改动**:featured 过滤与 sort 排序语义既有,橱窗改动重开即生效。

## 2026-06-11 · 新方向设计：小程序橱窗（一比一所见即所得管理）

用户验收商品上架后提出:①排序如何对应?②能否做「很直观的一比一管理台」。立为正式方向,先设计后实现。

1. **决策 §16 + 规格 §八**:一比一所见即所得;信息全面可管理按「内容上云三件套 + 橱窗可编辑」逐块接入。
2. **排序机制对账**:首页顺序 = `products.sort` 升序,新上架默认末尾。
3. **设计稿 S10**:手机框一比一首页(拖拽中卡片+紫色落点线)+ 排序/上下架面板 + 逐块接入路线表。
4. **流程教训**:PR #39 合并时间早于本批推送,导致 §八/§16/本批工作日志一度缺失(连环失锚后于「双面卡」批次体检补回)——**长分支多批次推送 + 异步合并易竞态,后续每批独立 PR、合并后再开下批**。

## 2026-06-11 · 控制台 v1 第四批：商品上架小程序（真实素材贯通）

第③批（视频编排）同分支续作。lint / test(57)/ admin + 小程序双端 build 全绿;adminApi 已重部署,新路由 invoke 验证在线。

1. **后端**:`publishProduct`——商品草稿校验(封面图/名称价格/SKU 齐)→ 映射入 `products`(价格转数字、首发 featured+sort 自动排队、重复上架保留原 sort)→ 草稿标记在售;**修白名单漏字段 bug**(cleanProduct 漏 courseId/videoStats,第④步完成度会被云端默默丢掉)。
2. **小程序端**:`ProductCard`/`FeatureProducts` 增 img(封面图 fileID,<image> 原生支持 cloud://);详情页 `DetailGallery` 接真实图列表(封面+其余图,主图/缩略联动,空则灰占位不回归);H5 本地回退无图自动走占位。**控制台传的封面图从此直接点亮小程序首页与详情**。
3. **管理端**:向导顶栏「上架小程序」按钮(①②③齐可上架,④未配齐给确认提示;在售后变「更新上架信息」+ 在售徽标);商品列表状态徽标改 在售/筹备中。
4. **拖拽落点线**(用户反馈):三层拖拽补紫色落点指示线 + 被拖行半透明高亮(对齐设计稿)。

## 2026-06-11 · 控制台 v1 第三批：第④步视频编排（三层树 + 视频直传 + 草稿/发布）

云接线批次(PR #38)合并后实现向导第④步。lint / test(57)/ admin build 全绿;待重新部署 adminApi 后由用户在 UI 复验。

1. **后端**(`adminApi` 扩展):课程草稿三件套——`getCourseDraft`(无草稿时以已发布课程为底稿,兼容存量 course-duck)/`saveCourseDraft`(三层白名单 cleanCourse)/`publishCourse`(草稿整体覆盖 `courses`,小程序 getCourses 即刻可见,引用模型老学员自动生效);**视频直传凭证** `getVideoUploadMeta`(@cloudbase/manager-node 用函数运行时临时密钥签发 COS 上传元数据,浏览器 PUT 直传,≤10MB 秒级);分片回落通道扩容(total≤200、视频走 videos/ 前缀)。
2. **前端**(`StepVideos.vue`):三层树编辑器(章/课时/小段增删、名称即输即存、⠿ 同层拖拽排序);小段三态视频位(未传/上传中带进度/已传可替换)+ 试看开关 + 时长自动读取(video metadata);素材进度条;草稿 800ms 防抖自动保存并同步 `videoStats`(步骤④完成度判定);**发布更新**按钮(缺视频段数确认提示)。视频上传双保险:直传凭证失败自动回落分片(用户无感,只是慢)。
3. **商品-课程关联**:product 草稿增 `courseId`(首次进④生成 `course-<pid>`)/`videoStats`;商品发布到小程序 products(含 cover/images 喂详情页)为后续批次。
4. **用户已确认约束**:单视频 ≤10MB(超 15MB 前端直接拦)。

## 2026-06-11 · 控制台 v1 第二批：云接线（adminApi + 口令登录 + 云端草稿 + 云存储图片）

骨架批次合并(PR #37)后接云。lint / test(57)/ admin build 全绿;部署与 E2E 待用户跑两条命令后执行。

1. **云函数 `adminApi`**(HTTP 访问服务触发,网页 fetch 调用):CORS + POST JSON 路由(ping/login/listDrafts/saveDraft/deleteDraft/uploadImage);**管理口令鉴权**(sha256 存 adminConfig,首次 login 即设置口令——bootstrap);商品草稿存 `productsDraft`(白名单字段+长度上限);图片 base64 转存云存储 `products/<pid>/`,返回 fileID + 临时 URL;listDrafts 附全量 fileID→URL 映射供网页展示。
2. **网页端双模式**:`api/cloud.js` 重写——配 `VITE_ADMIN_API`(admin/.env.local)走云,留空走本地演示;store 改按商品粒度保存(自动保存即落云);组件经 `imgUrl()` 显示 fileID;登录页文案随模式切换;加载/保存失败有错误条。
3. **规格偏差记档**(§三):登录方案 v1 = HTTP 服务 + 管理口令(零控制台配置、CLI 可全程验证),Web SDK 账号体系留作升级,适配层已收口。
4. **部署 + E2E 全过**(2026-06-11):用户执行两条命令后,curl 全链路验证 11 项全过——ping 连通/无口令拒/短口令拒/错口令拒/首登 bootstrap/复登/saveDraft/listDrafts/uploadImage(fileID+URL 可访问 HTTP 200)/deleteDraft;测试口令验后清除(nosql DELETE),首登设口令的机会留给用户。`.env.local` 已配服务地址(gitignore),开发服已切云模式。
5. **tcb 新坑**:`db nosql execute` 的 --command 是 `[{TableName, CommandType, Command(字符串化 Mongo 命令)}]` 结构(帮助示例为准),不是裸 Mongo 语句——记入 微信云开发梳理 §10。

## 2026-06-11 · 控制台 v1 第一批：admin 工程 + 流水线骨架（①②③步可用）

设计定稿后开工实现。独立网页工程 `admin/`(Vue 3 + Vite + Pinia + Router,设计 token 与小程序 uni.scss 同源),本批交付**本机可演示**的流水线骨架,lint(0 问题)/ test(57)/ admin build 全绿。

1. **骨架**:登录壳(本地演示模式)→ 商品列表(六步完成度圆点/新建/删除/继续上新进第一个未完成步骤)→ 上新向导(六步进度条可点跳转 + 上一步/下一步 + 每步自动保存)。
2. **三步可用**:①产品图片(封面图+其余图分组、HTML5 拖拽排序、图片压缩为缩略 dataURL)②商品信息(即输即存)③SKU(行内编辑表格);④⑤⑥为 v1.5 占位说明页。
3. **云接线预留**:后端适配层收口 `admin/src/api/cloud.js`(同小程序 utils/cloud.js 思路)——当前 localStorage 本地模式,下一批换 CloudBase Web SDK(真实登录/云函数二次校验/云存储)只改这一个文件。
4. **工程**:根 eslint 纳入 admin 源码(忽略 admin/dist、admin/node_modules);CLAUDE.md 文档地图补 admin/;验收单 J(本机演示 7 步)。

## 2026-06-11 · 向导调序：图片为先（图片款式驱动）

用户拍板:第①步改为**上传产品图片**(分**封面图** 1 张 + **其余图**若干),第②步才是商品信息——「我们的产品驱动是图片款式驱动」,先传图定款式再补档案。

1. **设计稿**:S7/S8 角色对调(S8=第1步 产品图片:封面图/其余图分组、无上一步、下一步→商品信息;S7=第2步 商品信息:补上一步钮、下一步→SKU);四个向导屏步骤条、侧边栏直达编号(1·产品图片/2·商品信息)、商品列表副标题全部同步。
2. **规格 §七**:六步顺序定稿 + 调序理由入档;`products` 模型改 `cover`(封面图)+ `images`(其余图,有序)——封面图即小程序列表卡/详情头图。

## 2026-06-11 · 工作台定稿「向导式 + 双入口」；补商品列表页（用户连续三轮迭代）

用户看稿连提三问:①没看到商品列表/SKU 编辑页(上版只画了入口骨架);②提议全流程放进一个页面;③定为「上一步/下一步」向导式 + 左侧「视频上传」「二维码管理」双直达入口(中途切入)。

1. **S5 重构为上新向导**:单页,顶部六步进度条(可点)+ 当前步内容占满主区(示例画第 3 步 SKU 表格)+ 底部上一步/下一步;④/⑤ 步内容 = S2 编排页/S4 卡片编辑器整页嵌入——「大工具塞不进一页」的矛盾由向导分步天然化解。
2. **新增 S6 商品列表**:商品卡(封面/价格/SKU 数/六步完成度圆点/在售-筹备状态)+ 新建商品入口。
3. **侧边栏定稿**(用户:左侧入口对应六步逻辑):商品与上新 + 「按步骤直达」区(① 商品信息 / ② 产品图片 / ③ 商品 SKU / ④ 视频上传 / ⑤ 二维码卡片 / ⑥ 码批次与印刷包)+ 数据看板;各屏高亮对应入口。规格 §七 同步「向导式 + 六步直达」。
4. **补向导第 1 / 2 步页面**(用户:商品信息与产品图片两步没看到——此前向导只画了第 3 步示例):S7 商品信息(名称/现价/划线价/角标标签/一句话简介表单 + 「详情图文在后续步骤」注;第 1 步无上一步钮)、S8 产品图片(主图带角标与紫描边/四图可删/上传位/比例建议注 + 「卡片设计可取用这里的图」提示),步骤条与底部导航状态随步联动。
5. **补向导第 6 步页面**(S9 码批次与印刷包):前置状态条(卡面已定稿才可生成)、已有批次行(码数/已激活/重新下载印刷包)、生成新批次区(数量 + 生成并打包下载 + 「印刷包.zip = 卡面.svg + 二维码地址.csv」说明)、完成钮(返回商品列表);至此六步全部有页面——①S7 ②S8 ③S5 ④S2 ⑤S4 ⑥S9。S9 导出图因渲染队列延迟待补(结构快照已验)。
6. **设计稿落盘 + 渲染恢复**:用户 ⌘S 保存 `design/console.pen` ✓、重启后渲染恢复;全部屏幕重新导出入库(商品列表/上新向导/视频编排/卡片列表/卡片编辑器),S1 旧课程列表标废弃留参考。Pencil 两坑沉淀:新路径文件须先在应用内保存否则仅存内存;多节点 export 偶发报错,逐个导出绕过。

## 2026-06-11 · 控制台信息架构细化：商品上新流水线（规格 §七 + 设计稿 S5）

用户明确逻辑层级:创建商品 → 上传产品图片 → 填写 SKU → 上传编排教学视频 → 设计二维码卡片 → 批量生成二维码地址;卡片可自定义尺寸,设计完成打包下载 SVG 设计图 + 二维码批量地址。

1. **主轴改商品**:控制台从「按工具分模块」改为「一款商品一条六步流水线」;规格 §七 落档(页面结构/products 扩展 images+skus/打包下载 ZIP=卡面 SVG + 地址 CSV);决策 §15(商品建档进自建控制台,§11 CMS 管商品降级为兜底)。
2. **设计稿**:新增 S5 商品工作台(六步导航,每步状态摘要+入口,断点续走);S4 卡片编辑器补「卡片尺寸」节(预设 90×54 / 105×148 + 自定义宽高 mm)、导出按钮改「导出印刷包(SVG + 地址)」;标注便签同步。仍处渲染故障盲画状态——结构经布局快照逐项验证,出图待 Pencil 保存重启后补。
3. **附带效益**:产品图片上传 = 小程序商品页真实素材的正式入口(替换灰占位的路径打通)。

## 2026-06-11 · 控制台追加「二维码卡片设计板块」（规格 + 设计稿两屏）

用户追加需求:产品增多后包装激活卡需图案区分——卡面与产品二维码绑定管理、可编辑文字、上传产品图/插画。

1. **设计稿新增两屏**(同一 .pen 文件):S3 卡片列表(卡面缩略/绑定课程/批次统计/草稿定稿状态)、S4 卡片编辑器(左画布实时预览:图案区/标题/副文案/二维码占位/三步说明/品牌条;右属性面板:绑定下拉/图片上传/文字三段/底色色板);侧边栏「二维码批次」升级为「二维码卡片」(组件级改动,四屏同步)。
2. **规格**:`设计规格-视频上传控制台.md` 更名 `设计规格-管理控制台.md`,新增 §六(卡片板块:页面/流程/cards 数据模型/印刷文件导出待定项),分期补 v1.5;决策 §13/§14 交叉对账(印刷交付物升级为「卡面 + 一码一张」)。
3. **波折记录**:Pencil 本会话渲染管线中途故障(新增内容画布/导出全空白,S1/S2 靠缓存仍可出图;布局快照验证 S3/S4 数据结构完好)——S3/S4 导出图待用户保存文件、重启 Pencil 后补;设计稿源文件目前仅存于 Pencil 内存,**保存动作只能由用户在应用内完成**(⌘S → `design/console.pen`)。

## 2026-06-11 · 视频上传控制台：规格 + UI 设计稿（先规格后代码）

用户定方向(网页版/逻辑清晰/拖拽排序/方便命名;二维码测试暂缓),本批交付设计,不动业务代码。

1. **子规格** `设计规格-视频上传控制台.md`:定位(管理员网页端,一款材料包=一门课)、两页交互(列表 + 三层编排:层内拖拽/双击改名/段级三态上传/试看开关/草稿自动保存+发布分离)、技术路线(admin 独立工程 + 静态托管 + CloudBase 登录 + 云函数二次校验;`coursesDraft` 草稿集合,发布覆盖 `courses`,小程序端零改动)、分期与待确认。
2. **UI 设计稿** `design/console.pen`(Pencil,沿用幸运紫 token)+ 导出图两张:S1 课程列表(卡片含素材进度/发布状态)、S2 课程编排(核心页,五种小段状态:已传/改名中/拖拽中+落点线/上传中/未传,页底操作提示条);画布旁附交互标注。
3. **决策 §14**:借此落定主规格 §七 两个挂账项——课程编排形态=自建编排页(种子方案功成身退)、视频存储 v1=云存储 fileID(VOD 留 P5)。CLAUDE.md 文档地图补 `design/` 目录与子规格。

## 2026-06-11 · 进度上云部署收尾 + E2E 全过；域名确认

1. **部署收尾**:getMyProgress 用户单独部署 ✓,两函数闸门 invoke(NO_OPENID)均拒 ✓。
2. **进度链路 E2E PASS**(工具端真实 openid,`scripts/progress-verify.cjs` 留存复用):`trackEvent` segment_done + watch_at 写入 → progress 文档折叠(done 段表 + last 观看点)→ `getMyProgress` 读回全对;events/progress 集合「不存在自建」逻辑生效(未手动跑 initDb)。验收单 I 标注 AI 预验通过 + 测试进度写入说明。
3. **域名确认**:用户确认已有备案域名 → 印刷物料依赖①解除(决策 §13 对账);剩 后台规则配置(②)→ URL 清单(③)→ 真机扫码验证(④)。

## 2026-06-11 · 二维码类型拍板落地（普通链接二维码）+ 需求/进展对账

用户连发四条信息,逐一落账:

1. **决策 §13 二维码类型 = 普通链接二维码**(用户拍板:印刷厂按地址产码,一码一地址):URL 形如 `https://<域名>/q/<激活码>`;welcome 解析收口 `api/activation.js` 的 `parseActivationCode`(?code= / scene / **?q=** 三轨,带测试 4 用例);剩余依赖=已备案域名 + 小程序后台「扫普通链接二维码」规则 + 真机扫码验证。原小程序码方案弃(URL Link 因不支持永久有效同弃)。
2. **第二微信号答疑 + 测试码**:用途=验证「别人用已用码被拒」(一码绑一人);生成新批次 3 个未用码(`b-course-duck-202606110841`),连同「体验版启动参数模拟扫码、不需真印二维码」说明写进验收单 F。
3. **商户号**:用户确认即将提交申请(进行中),现状 #1 对账。
4. **新需求登记:视频上传控制台**(定制 UI + 上传逻辑,给 segment 灌真实视频)→ 现状 下一步 #6,按约定先规格后代码;关联视频存储方案与控制台形态两个待定项。
5. **部署对账**:trackEvent ✓(invoke NO_OPENID ✓);getMyProgress **未部署**——踩到新坑「`tcb fn deploy A B` 只部署第一个」,记入 微信云开发梳理 §10,待用户单独部署。

## 2026-06-11 · 观看进度上云 + 通用埋点（一次埋点两用）

补 P3 时预定未做的「segment 级进度记忆 + 运营埋点」(规格 §三-播放/§七-埋点,事件模型按规格)。lint / test(53,+7)/ 双端 build / 工具眼校(未部署时回退样例,无回归)全绿。

1. **云函数**:`trackEvent`(openid 闸门;`events` 通用流水 `{type,page,targetId,meta,createdAt}` + `segment_done`/`watch_at` 两类事件**同时折叠**进 `progress` 紧凑文档(每用户每课一条:done 段表 + last 观看点),目录/继续学习读折叠文档不扫流水;集合不存在自建,免 initDb 时序依赖);`getMyProgress`(只返本人);initDb 补 events/progress 两集合(对账用)。
2. **前端底座**:`utils/track.js`(埋点出口,同 logger 思路收口;fire-and-forget 不阻塞交互;H5 无云空操作)+ `api/progress.js` + `store/progress.js`(getter 把云端 segment 粒度折成页面 lesson 级 `{done}/{watched}` 同形;**无云回退样例、有云无记录给真空态**;`lastWatch` 喂继续学习卡)。
3. **播放页上报**:段末自动停 / 视频播完 → `segment_done`;onHide/onUnload → `watch_at`(最后位置);顺带修一个潜在重复触发(段末分支加 endedSeg 守卫,pause 生效前 timeupdate 重入会重复暂停+重报)。
4. **页面接真**:目录角标改读 progress store(onShow 强刷,看完回来立即点亮);「我」页继续学习卡按云端 lastWatch 定位课时/章节(`继续观看`跳真实那节,原硬编码 l3);ContinueVideo「还剩 4 分钟」写死文案改现算。
5. **待办**:云函数部署待用户执行 `tcb fn deploy trackEvent getMyProgress`(权限闸,同前两次);部署后走验收单 I。

## 2026-06-11 · 订单列表接真单（「我」页订单入口去样例）

验收反馈触发(用户:下单编号与详情一致,但待支付/待发货/待收货里找不到该单、且无全部订单页)——
原因即 项目现状 已记缺口「『我』页订单入口仍走样例展示路径」+ 列表页缺失。本批接真,lint / test(46,+1)/ 双端 build / 工具眼校(列表显真实云单、角标计数正确)全绿。

1. **新页 `order-list`**:五 tab(全部/待支付/待发货/待收货/已完成),读 `store/orders`(小程序端云端 `getMyOrders` 进页强刷,H5/App 回退单仅会话内);订单卡(时间/状态/商品行/件数/实付)→ 点卡进详情同一笔;空态注明「模拟支付阶段,新订单直接进待发货」——用户「待支付里找不到单」即此,属正常行为。
2. **「我」页接真**:九宫格与「全部订单」→ order-list(`?tab=` 预选);角标从写死 1/2 改为 store 真实计数(只标 待支付/待发货/待收货 三个可办理态);退款/售后仍样例(P4 接真)。
3. **状态展示单一来源**:`data/orders.js` 新增 `ORDER_STATUS`(pending/paid/shipped/done → 标签/banner/动作),列表与详情共用;详情 `cfgFromOrder` 从硬编码「待发货」改按真实 status 映射,pending 态显示下单时间 + 去支付占位(P4 接真)。
4. **范围注记**:`pending-pay` 样例页入口已摘(页面保留,P4 处理,记技术债 #8);详情 `?status=` 样例路径保留。
5. 修账两笔:恢复本日志 P3 条目标题行(前次编辑误删);补回 PR #31 合并时序错过的文档对账(调试日志 E 等,cherry-pick 进本批)。

## 2026-06-11 · 资料上云（头像 / 昵称微信能力 + 去手机号）

「完善资料 UI」收账:资料从纯本地样例升级为云端同步,手机号按规格 §一 正式从 UI 移除。lint / test(45,+6)/ 双端 build 全绿。

1. **云函数 `updateProfile`**:openid 闸门 + 白名单字段(nickname/avatar/bio,字符串校验 + 截断;avatar 只收 `cloud://` fileID 或空串),只改本人 users 记录;先查后改避免「内容未变 stats.updated=0 误判建档」。
2. **微信头像昵称能力**:资料编辑页小程序端用 `button open-type="chooseAvatar"`(CLAUDE.md 不用 button 的例外——平台能力,条件编译隔离 + 样式归零)+ `input type="nickname"`(键盘一键带微信昵称);其它端保留相册选图。
3. **store 收口**:`saveProfile`(本地即时生效 → 头像临时路径传云存储换 fileID → `api/user.js` 调云函数;上传失败不中断,昵称签名照常同步但整体报「同步失败」)+ `mergeCloudProfile`(登录后云端非空资料覆盖本地展示,跨设备一致);`utils/cloud.js` 加 `uploadCloudFile` 收口上传。
4. **去手机号**:编辑资料页手机号字段、「我」页头部手机号行、样例数据 `USER.phone` 全部移除(规格「手机号不获取」落地)。
5. **测试**:新增 `tests/user.test.js` 6 用例(云端回灌空字段不clobber、临时头像换 fileID、已是 fileID 不重传、上传失败不清云端旧头像、云函数失败本地不受影响)。
6. **部署 + 闸门验证**:云函数部署被会话权限闸拦下后由用户执行 `tcb fn deploy updateProfile` ✓;CLI `invoke` 无微信身份返回 `NO_OPENID`,闸门生效(验收单 G-7 ✓)。
7. **真机调试白屏处置**:判定为 2026-06-10「工具僵尸实例」坑复发(非业务代码,重启工具后模拟器 首页/我/资料编辑 三页眼校全过),记调试日志 E,真机重连待用户复验。

## 2026-06-10 · P3 二维码激活闭环（核心商业模式跑通）

规格先行(PR #27:products↔course 关联 + 确认闸门细化),PR #28 落地;lint / test(37)/ 双端 build / 部署全绿。

1. **四云函数**:`genQrcodes`(管理通道:CLI 放行、小程序须 users.isAdmin;code 作 `_id` 库级唯一;课程存在性校验);`activateCourse`(**条件更新抢占**保一码一用并发安全;state=activated/mine 区分未确认/已确认);`confirmEnter`(enteredAt 只写一次留证 + **启发失效**本人订单最早可退条目);`getMyCourses`(只返回已确认——确认是进课唯一闸门,防"激活后绕过退货确认")。
2. **C 端**:welcome 改两页式课程欢迎页(`?code=`/小程序码 scene 进入,激活中/课程引导+退货权确认/继续学习/错误四态分流;无码走原引导);catalog 未激活锁态卡;player 鉴权弹回;`api/activation` + `store/activation` 收口(H5 演示模式全解锁,mp 异常**宁锁勿漏**)。
3. **E2E 全过**(开发者工具真实 openid,`scripts/p3-verify.cjs` 留存复用):首扫 activated → 确认 enteredAt → getMyCourses 解锁 → 重扫 mine → 无效码拒绝 → 目录无锁卡 → 播放鉴权过;另验**启发失效**:下单 prod-1 → 扫第二码确认 → `revoked` 命中 + 条目 `refundable=false`。顺带:调试日志 D(工具端云调用 timeout)今日多轮云调用**未复现**。
4. **范围注记**:小程序码图片生成(`wxacode.getUnlimited`)按 §七 待确认(二维码类型)延后到印刷物料阶段;CODE_TAKEN 他人扫需第二微信号,归真机验收(验收单 F-7)。

## 2026-06-10 · 订单上云（模拟支付）· 调试日志 C 关账

照三件套模式 + 规格先行(PR #24 模型细化),PR #25 落地,lint / test(33)/ 双端 build / 部署全绿。

1. **云函数**:`createOrder`——openid 取 `getWXContext`(CLI 无身份被 `NO_OPENID` 拒,闸门 invoke 验证);前端只传 id/qty + 地址快照,**价格按云端 products 现算**;名称/规格快照进条目、条目级 `refundable`;**模拟支付**直接 paid(P4 拆 pending→回调);epoch 毫秒时间戳。`getMyOrders` 按 openid 倒序。搭配购小件暂用函数内价表(与 data/checkout.js 同步注释,不入 products 避免污染推荐位)。
2. **前端**:`api/order.js`(H5/App 及云端异常回退本地同规则生成——调试日志 C 的前端快照方案降级为回退)+ `store/orders.js`(create/load,远端按 id 合并不丢回退单)。
3. **链路贯通(调试日志 C 关账)**:checkout 真实下单(防双击)→ paysuccess 读同一笔订单号/金额 → order 详情 `?id=` 渲染真单(地址用下单快照);样例 status 路径保留给「我」页演示入口。
4. **验收**:`验收手册.md` 新增验收单 E(下单 → 控制台 orders 出现同号记录 + 云端定价安全验证),待用户真机执行。

## 2026-06-10 · 立验收机制：非技术验收手册

用户提出"不懂代码,无法理解和验证后端"。立 `docs/验收手册.md`:三个观察窗口(云开发控制台=货架/小程序本体=行为/中文文档)、黄金路径清单、已完成部分(商品/课程/登录/云函数)的可操作验收单、四个质量代理信号、打回标准。新约定进 CLAUDE.md:**每个里程碑收尾附非技术验收单**,照单操作全过才算完成。

## 2026-06-10 · 开发者工具「代码质量」扫描三项未通过 → 全部修复

1. **JS 压缩**:页面 JS 本就被 Vite 压缩,未通过是生成的 project.config.json `minified:false`(扫描看「上传时压缩」开关)→ manifest mp-weixin `setting.minified:true`。
2. **组件按需注入**:manifest 加 `lazyCodeLoading:"requiredComponents"`(组件均页面级显式注册,安全;附带启动性能收益)。
3. **图片≤200K**:`hero-full.png` 是 402×874 照片存成 PNG(189K,透明通道无用)→ 转 JPEG q82(42K,-78%),包内图片总量 520K→91K。
4. 验证:lint/test 27/build 双端 ✓;重启工具眼校四沉浸页全 ✓ + welcome 截图确认 JPEG 渲染正常。
**媒体预案**(沿用既定决策):真实素材阶段大媒体一律云存储 fileID + MediaSlot 注入,视频不入包;包内只留小图标(svg)与必要小图,照片用 JPEG/WebP 不用 PNG。

## 2026-06-10 · 决策修订：激活码不绑订单（备货封码）+ 退货验收扫码兜底

用户明确码的生成与备货逻辑(按产品配套课程批量生成 → 打印 → 备货封入包装并封装),与已定决策 1 的「发货时绑订单条目」冲突(封装后发货无从知道盒内是哪个码)。用户拍板**放弃发货绑定**;设计上以「退货验收扫码」补回执行力:confirmEnter 启发式失效(自购命中)+ 退货须寄回完整包装、验收扫码裁决(自购/送礼一致有效)。规格 §二/§三/§四/§五/§六/§七/§八 九处同步修订;`qrcodes` 去掉 orderId/orderItemRef;详见 `关键决策记录.md` §12。

## 2026-06-10 · 补充需求：商品款式持续新增 + 配套课程同步新增

用户补充(并澄清为「不断**新增**」非替换):上新是常态运营。记入规格 §四-6「持续上新」:成对上架流水线(product → course 编排 + 按段传视频 → courseId 关联 → 码批次 → 随货);引用模型(内容更新对已激活用户自动生效;segment 结构变更旧进度作废可接受)。数据模型/代码已支持(多课数组 + courseId);**主要影响**:CMS 决策里「courses 种子当准源」对策有了到期点——上新常态化前须拍板课程编排形态(拆集合入 CMS / 自建编排页,进 §七 待确认)。

## 2026-06-10 · 决策落定：管理后台 = CMS 管数据 + 自建管二维码/看板

规格 §七 第一项待确认拍板(用户确认走推荐方案)。要点:平铺集合用云开发 CMS;业务动作(码批次/发货绑定/退货)自建;敏感集合 CMS 只读;**courses 三层暂不入 CMS**(种子为准源)。收益/代价与对策详见 `关键决策记录.md` §11;规格 §七 已同步。

## 2026-06-10 · P2 部署关账 + 订单上云前置清债（并行批次）

接上条:tcb 重新登录(授权页选微信扫码)后部署关账;等待与眼校间隙并行清掉订单上云的两个捆绑债。

1. **课程上云部署关账**:`tcb fn deploy seedCourses getCourses` + invoke seed(`{ok:true, ids:["course-duck"]}`)+ invoke `getCourses` 读回完整三层结构,CLI 层面验证云端数据就位。
2. **PR #15 技术债 #3**:`SAMPLE_ADDRESS` 迁出 `data/checkout.js` → 新建 `data/address.js`,地址 store 不再依赖结算页数据。
3. **PR #16 技术债 #5**:checkout 拆出页内组件 `CheckoutAddonList`(纯展示发事件)+ `CheckoutSubmitDock`(守卫留页面);`.co-dock` 四件套被 7 页共用,取值照 #6 模式收进 `co-mixins.scss` 两侧 @include。**独立 + 捆绑技术债至此全部清零**。
4. **自动化眼校全 ✓**(四沉浸页与胶囊中线差 0px):中途踩两个工具坑(假性 component not found / 自动化 RPC 僵尸端口),已沉淀进 `调试日志.md` + visual-check 脚本注释与探活守卫。

## 2026-06-10 · P2 课程三层模型上云（代码完成，云端部署待登录 → 同日已关账,见上条）

按 项目现状 下一步 #3 执行,三个 PR(#11–#13),lint / test(27)/ 双端 build 全程绿。

1. **PR #11 课程数据三层化**:`data/course.js` 按规格 v2 §三改 chapter / lesson / segment(`COURSES` 多课数组;segment 含 `videoFileId` 占位、`free` 试看下沉段级,素材剪好前播放页按时长等分回退);**进度与内容分离**——done/watched 剥离为 `SAMPLE_PROGRESS`(用户态,将来云端按 segment 粒度记忆);catalog / player 消费新结构(播放页分段进度条改读 `lesson.segments`,去硬编码 KP);`tests/course.test.js` 六条契约锁形状。
2. **PR #12 课程上云三件套**(照商品样板):云函数 `seedCourses`(业务 id 作 `_id`、幂等 upsert,种子与 `data/course.js` 同形)+ `getCourses`(按 sort);`api/course.js`(`callCloud` + 轻量边界校验,云端失败 / 非小程序端回退本地);`store/courses.js` 收口(不持久化,load 前 getters 给安全空形状);catalog / player 改从 store 取数;`tests/courses-store.test.js` 补回退与 getters 用例。
3. **PR #13 技术债 #4**:`TrustStrip` / `Reassurance` / `Reviews` / `FAQ` 改纯展示组件收 props,数据首页收口(`FeatureProducts` 已在商品上云时接 store)。

⚠️ **未完账**:tcb CLI 登录过期,`tcb fn deploy seedCourses getCourses` + invoke seed / getCourses 验证待 `tcb login` 后执行;未部署前小程序端走 api 回退路径(Console 可见回退 warn),不影响使用。

## 2026-06-10 · 沉浸页自动化视觉验证 → 调试日志 A/B 关账

新增 `scripts/visual-check.cjs`(miniprogram-automator 连微信开发者工具):四沉浸页
(detail / catalog / welcome / player)浮层按钮矩形 vs 胶囊/状态栏自动判定全 ✓(与胶囊中线差 0px)
+ 截图眼校通过,调试日志 A/B 关账。脚本留存复用于以后 UI 验证,两个工具坑已写进脚本注释:
`automator.launch` 握手偶发挂死(改 connect 优先);`callWxMethod` 调同步 API 不回包(改 `evaluate`)。

## 2026-06-10 · 代码优雅化批次（独立技术债清零）

按「符合项目目标的前提下尽量优雅」执行:只做标 `[独立]` 的债(不碰 `[结合云开发]` 的 #3/#4/#5,P2/P4 时捆绑做),一事一 PR(#5–#8),lint/test/双端 build 全程绿。

1. **PR #5 调试日志 A/B**:`systemBar.js` 加 `getSystemBarVars()`(CoNavBar 复用);四沉浸页浮层与胶囊同水平带 + `--gap` 让位;播放页只动浮层、视频铺满到顶(黑块成因消除)。A/B 转 🟡 待真机眼校。
2. **PR #6 技术债 #2**:HelpSheet(949 行)归位 `pages/player/components/HelpSheet/`,拆壳 + 5 面板 + data.js;toast 副作用上提;`mmss`/`parseDur` 收进 `utils/format.js` 补测试(16→18)。
3. **PR #7 技术债 #7 + 审核 #10**:`constants/storage.js`(STORAGE_KEYS)+ `composables/useTimers.js`(onUnmounted 自动清理),7 页 setTimeout 全收口。
4. **PR #8 技术债 #6**:双写实测仅 4 class 且 co-addr-tag 已漂移;取值收进 `styles/co-mixins.scss` 单源、两侧 @include(不破坏 scoped 边界,产物等价已验证)。

独立技术债至此**全部清零**(#1 v0.1 已修),剩余 #3/#4/#5 按计划等云开发阶段。审核 P3 #8(user→profile 默认值)同属云改造覆盖范围,#11(页面偏大)非硬伤,均不单独处理。

## 2026-06-10 · 根本需求复盘 → 设计规格 v2

用户完整重述根本需求,逐条对照现行设计,定下三个结构性决策并修订规格。

1. **三个决策落定**:① 退货权**按单件商品**(`refundable` 下沉订单条目级;任何人确认均生效;发货时码绑订单条目);② **小片段=独立视频文件**(courses 改三层:chapter 大章节 / lesson 中章节 / segment 小片段,目录只显两层);③ **放弃获取手机号**(免企业认证依赖 + 按次付费接口)。
2. **新明确的需求细节进规格**:扫码跳**该课程**的两页式欢迎页(第 2 页确认即失该件退货权);分步播放交互(顶部分段进度条=segments,segment 播完自动停 + 重复播放/下一段);重复扫码路径(本人→继续学习,他人→已被激活);指导窗口沿用 HelpSheet。
3. **余下待确认**收口进规格 §七(后台形态/视频存储/支付资质/二维码类型/客服形态/商品课程基数/H5 定位/播放细节),其中支付资质建议立即启动申请。
4. 同步 `项目现状.md` 下一步(P2 前先修调试日志 A/B、上云捆绑技术债 #3/#4/#5)。

## 2026-06-10 · 项目记录精简 + 状态单一来源

通读全部文档后做的一次「记录瘦身」,不动代码、不动既定决策。

1. **状态收口单一来源**:CLAUDE.md 去状态化(§1 状态段 / 下一阶段重点 / §15 已知缺口 删除,指向 `项目现状.md`),并新增「记录约定」——每个里程碑只更新 工作日志 + 项目现状 两处,CLAUDE.md 仅约定变化时改。
2. **归档**:`审核报告-v0.1.md` → `archive/`(结论已沉淀进 技术债清单,与前两轮外部审核同等处理)。
3. **精简**:`微信云开发文档梳理.md` 644 → ~200 行(删官方教程复述,留坑/清单/安全/tcb CLI/链接,完整版在 git 历史);`调试日志.md` 已结案 3 条压缩为「现象 + 教训 + 状态」短条目。
4. 顺手修两处失效引用(代码标准 §12 指向、 工作日志头部审核报告指向)。

效果:现行文档 1393 → ~1175 行;每里程碑需同步的文件从 3 处降到 2 处。

## 2026-06-07 · 商品数据上云(P0 续)

接着 v0.1,把「商品」作为第一条业务数据端到端上云,作为课程 / 订单的样板。

### 做了什么
1. **两个云函数**:`seedProducts`(幂等灌 5 个商品,用业务 id 作 `_id` + `doc().set` upsert)、`getProducts`(按 sort 返回列表)。
2. **数据层收口**:`utils/cloud.js` 加 `callCloud`(集中 `wx.cloud.callFunction`,`wx` 守卫 + 条件编译);`api/shop.js` 的 `getProducts` 接 `callCloud`,H5 / App 回退本地 `catalog`;新建 `store/products.js`(商品 store,不持久化,页面从它取)。
3. **页面接入**:首页 `FeatureProducts` 用 `store.featured`(映射回字符串价喂 `ProductCard`);详情头部 `await store.load()` → `getById` → 本地兜底;`App.vue` onLaunch 加 `load()`。删死代码 `data/products.js`。
4. **部署 + 验证**:补 `cloudbaserc.json`;`tcb fn deploy --all`(自动云端装依赖)+ `tcb fn invoke seedProducts`(写 5 条)+ invoke `getProducts`(读回 5 条,CLI 层面证明数据在云端)。
5. **安全边界**:直接改 live 库做「改价铁证」被 auto 模式安全策略拦(写生产库 / AI 自助加权限规则均拒);改用 Console 无回退警告 + CLI invoke 验证,改价由用户自行执行。

### 产出
- 新增:`cloudfunctions/seedProducts`、`cloudfunctions/getProducts`、`store/products.js`、`cloudbaserc.json`。
- 改:`utils/cloud.js`、`api/shop.js`、`App.vue`、`FeatureProducts.vue`、`detail/index.vue`;删:`data/products.js`。
- 健康:lint ✓ / test 16 ✓ / build 双端 ✓。

### 下一步
- 真机眼校:首页 / 详情商品来自云端(看 Console 无回退警告)。
- 课程 / 订单照此三件套模式上云。

## 2026-06-07 · v0.1 里程碑

一天从「回顾工作」开始,完成了从功能快赢到 **v0.1 上云**的完整一程。

### 做了什么(时间线)
1. **纯前端快赢**:接通详情「为你推荐」→跳详情、首页「全部买家秀」→跳评价页。
2. **非沉浸页避微信胶囊**:新建 `utils/systemBar.js`(动态状态栏+胶囊,取代 `env` 硬编码),修好 10 个导航页 + 购物车 + 「我」紫头(`CoNavBar` 一改修 10 页)。
3. **购物车脏数据排查 → 系统性调试/质量体系(四支柱)**:① `logger` 全局错误捕获;② `persist` sanitize 数据契约 + `validate`;③ `调试日志.md`;④ Vitest 回归(`npm run test`,16 用例)。
4. **外部审核对账**(可维护性 + 拓扑):核实分类,修 P2 购物车 qty 契约 + P1 结算空草稿守卫,其余记技术债。
5. **真机验证**(微信「预览」)指引;UI 定位问题经确认非系统问题,**暂挂**。
6. **需求沟通 → 设计规格**:明确「实物购买 → 收货扫二维码 → 激活课程绑账户 → 确认进入即失退货权 → 只看自己买的课」全栈业务;后端定 **微信云开发**;产出 `设计规格-课程电商系统.md`(数据模型 / 核心机制 / 分阶段 P0–P5)。
7. **代码标准**:优化桌面初稿 → `代码标准.md`(21 维 + P0–P3 + uni-app 专项 + red flags)。
8. **v0.1 审核**:按代码标准全量审核 → **无 P0/P1**,产出 `审核报告-v0.1.md`;修唯一真 bug(`sanitizeCart` 小数 qty,要求正整数 + 补测试)。
9. **上 GitHub**:私有库 `ookiisparrow/luckyducky-miniprogram` + `main` 全历史 + **`v0.1` tag** + `Git协作约定.md`。

### 产出
- **7 个 commit**(跳转 / 避胶囊 / 质量体系 / 审核修复 / #1 / v0.1 文档 / Git 约定),全部已推 GitHub。
- **`v0.1` tag**(主版本节点)。
- **新建文档**:设计规格 / 代码标准 / 审核报告-v0.1 / 技术债与重构 / 调试日志 / Git协作约定(+ 本工作日志)。
- **新基建**:`logger` / `validate` / `systemBar` / Vitest。

### 当前状态
- 健康:`test`(16/16) + `lint` + H5/小程序 `build` 全绿,无 P0/P1。
- 本地 = 远程(GitHub 私库);工作树仅剩 3 个旧审核 `.md` 未跟踪。

### 待办 / 下一步
- **待修**:调试日志 A(沉浸页浮按钮避胶囊)、B(播放页黑块);技术债 #2–#7(部分结合云开发做)。
- **待确认**(开 P0 前):云开发四决策(后台形态 / 视频存储 / 支付资质 / 二维码类型)、管理后台谁做、视频教程细化。
- **下一步**:P0 云开发(建议走 `feature/cloud-p0` 分支,需先开通云开发环境拿环境 ID)。
