# 微信客服智能客服 配置手册（runbook）

> **用途**：从零把「微信客服 in-chat 智能客服」配通的完整流程 + 排障手册。2026-06-17 与用户真机逐步联调跑通后沉淀（曾踩 4 个大坑、花了不少时间，全记在 §6 排障）。
> **代码侧**：CS 域 `cs/kfCallback`（回调主函数）+ `cs/kfBind`（身份桥接）+ 接缝单源 `kit/wecom.ts`；功能逻辑在 `cs/kfCallback/dispatch.ts`。客观接口/集合事实在 `系统事实.md`，本文只管「怎么配 + 怎么排障」。
> **⚠️ 凭证配置面变更（2026-07-12 决策·本段权威·下文 §3/§4 的「云函数环境变量」教学为迁移期旧路径）**：Token/Secret/EncodingAESKey/CorpId/AgentId + 小程序卡片 appid/thumb_media_id 现**首选在 `/admin` 人工配置清单页填写**——写入 DB `secureConfig/wxkf`（仅管理端 ACL·零回显），云函数运行时读库优先、**填写即生效，无需再改环境变量+重新部署**；环境变量降级为迁移期兜底（DB 无值才读）。本文一律只写变量/字段名、不写值。**⚠️ 别用 `tcb fn detail` 查含密钥的 env（明文全打印·会泄露）——核对用长度/hash 比对、不打印值（见 memory `dont-log-secrets-via-tcb-detail`）；改 Secret 后仍需清 `kfState._id=token` 缓存（§6 坑③），此点不因入库改变。**

> **⚠️ 架构变更（2026-07-01·独立端 → 企业微信内·本段权威·下方 §0–§7 含独立端旧模型细节·冲突以本段为准）**
> 为解锁承面A/私域/转人工自动（客户联系能力·独立端给不了），微信客服从「**独立端** `kf.weixin.qq.com` 开发配置」迁到「**企业微信内**·经自建应用 **Lucky Ducky**（AgentId `1000002`）管理」。**是同一个微信客服**（open_kfid 全程不变 `wkXCYoNAAAytL3jmRHxxzb8sYLl2Kpcg`·external_userid 空间共享·查订单身份不错位），只是换了管理/凭证面。
>
> **凭证对照表（改任一处照此核·两边必对齐）**：
>
> | 项 | 独立端（旧·已弃） | 企业微信内（当前·权威） | 填到 |
> |---|---|---|---|
> | 调 API 的 Secret | 微信客服开发配置 Secret | **自建应用 Lucky Ducky 的 Secret** | `WXKF_SECRET`（kfCallback/kfBind/kfSend/login/kfHealthProbe 各函数 env 都要） |
> | 回调 Token/AESKey | 开发配置回调 | **Lucky Ducky → 接收消息（已启用API接收）** | `WXKF_TOKEN`/`WXKF_AESKEY`（kfCallback env） |
> | 回调 URL | `kf.luckyducky.cn/kfcallback` | 同·不变 | Lucky Ducky 接收消息 URL |
> | 可信IP | **不需要** | **必需**（自建应用 Secret 调 API 不配报 `60020`） | 企业微信「企业可信IP」填云函数**固定出口IP** |
> | 固定出口IP | 不需要 | **必需**（CloudBase 默认出口 IP 动态·给 kfCallback/kfSend/kfBind/login/kfHealthProbe 开「固定公网出口IP」·同账号同地域共享一 EIP·EIP 计费） | CloudBase 云函数网络配置 |
> | 客服账号管理 | 独立后台 | 「正通过自建应用管理此账号」 | — |
>
> **迁移血泪（2026-07-01·一天踩通·调试日志 AA）**：① 切面换凭证 → `FORGED_CALLBACK`（Token/AESKey 没对齐）；② 自建应用 Secret → `60020`（要固定出口IP + 可信IP）；③ 切换后 `kfState` 同步游标 stale → 验签过但 `sync msgs:0`、机器人不回 → 清 `cursor:<openKfId>` 那一条（**别删** `token`/`seen:`）。
> **变更纪律（治「静默故障」·根本预防）**：① **别再独立端↔企业微信内来回切**（切 = 换凭证模型 = 必断）；② 动任何微信后台配置后**必发一条测试消息验「一条一回」**、别改完就走；③ 改 `WXKF_SECRET` 后清 `kfState._id=token`；④ 顺序永远 **env 先于后台**（后台保存触 echostr 验·env 已匹配才过）；⑤ 活体探针 `kfHealthProbe`（30 分定时·守卫 `kf-health-probe-wired`）探令牌/可信IP 静默故障推企微告警——但 `FORGED` 那类纯自动测不了（需合成真消息）、靠②兜。

## 0. 一张图看清架构

```
顾客微信  →  微信客服会话框  →（消息事件回调）→  自有域名 kf.luckyducky.cn/kfcallback
                                                         ↓（HTTP 访问服务）
                                                   云函数 kfCallback
                                                         ↓
                              验签解密 → 拉消息 sync_msg → 关键词识别(dispatch)
                                                         ↓
                              回 msgmenu 气泡 / 文字 / 小程序卡片 / 转人工（send_msg）
```

**关键认知（2026-07-01 迁移后已翻转·见顶部架构变更段）**：**当前（企业微信内模型）**顾客消息回调到**企业微信「自建应用 Lucky Ducky → 接收消息（已启用API接收）」**配的那个回调（经 `kf.luckyducky.cn/kfcallback` → kfCallback）。~~独立端旧模型走「微信客服独立后台 → 企业内部接入」——已弃~~。**别再两个都配/来回切**（切换会换掉验签凭证 → FORGED）。

## 1. 前置（一次性·靠人·在各平台后台做）

1. **企业微信企业认证**（已完成）——企业 ID `wwda6861818cb50dd9`。
2. **微信客服已开通 + 建好客服账号**（`kf.weixin.qq.com` → 客服账号）。当前账号：`杭州市西湖区好好看文化艺术工作室客服`（open_kfid `wkXCYoNAAAytL3jmRHxxzb8sYLl2Kpcg`）。顾客进入入口（二维码/链接）在「客服账号」里拿。
3. **自有域名 + 备案 + SSL + CNAME**：回调 URL 必须是**已备案、且与企业主体同主体/有关联**的域名（微信客服强制校验，文档没明说但会拦）。当前用 `kf.luckyducky.cn`，CNAME 指向云开发 HTTP 访问服务网关，配 SSL 证书。
4. **小程序绑微信开放平台**（身份桥接·查订单需要）——✅ 2026-07-01 认证通过 + **真机验通**（同号登录小程序→客服点查订单→回"没查到你名下的订单"＝身份认出·桥接闭环）。链路：login 存 unionid（`login-kf-identity-bridge`）→ kfCallback 用微信客服 `kf/customer/batchget` 反查 unionid→openid 建 kfIdentity（**绕开客户联系 idconvert 的 48002**·平台原生）。**排错**：`unionid 不在 users`＝发消息的微信没用同号登录过小程序（最常见），或小程序与企业微信不同开放平台主体。

## 2. 四个配置位置（各管一摊）

| # | 位置 | 管什么 |
|---|---|---|
| ① | **代码** `cs/kfCallback/dispatch.ts` | 关键词、菜单话术、FAQ 答案、小程序跳转页——**写死在代码里，无可视化后台**，改完要重新部署 |
| ② | **微信客服后台** `kf.weixin.qq.com` | 客服账号、接待人员、**企业内部接入回调**（开发配置：URL/Token/EncodingAESKey）|
| ③ | **云开发控制台** → 云函数 → `kfCallback` → 环境变量 | 所有密钥与参数（见 §3）|
| ④ | **人工接待台**：企业微信 App → 工作台 →「微信客服」→「客户咨询」（或电脑端 `kf.weixin.qq.com` 扫码登录）| 转人工后接待人员在此回顾客；接待人员**在线状态**在此切（🟢正在接待／🟡暂时挂起［不分新客·在聊的可续］／⊖停止接待）——这就是「人工在线/离线」开关，企业微信原生自带，无需自建（调试日志 AB 拍板用它）。离线时找人工的会话无人回会**超时自动结束**。 |

## 3. 环境变量清单（云函数 kfCallback / kfBind）

| 变量 | 必填 | 说明 | 取值来源 |
|---|---|---|---|
| `WXKF_CORPID` | ✅ | 企业 ID | 微信客服后台 → 开发配置 → 企业ID（`wwda6861818cb50dd9`）|
| `WXKF_SECRET` | ✅ | **微信客服「开发配置」页的 Secret**（不是自建应用的 Secret！见 §6 坑③）| 微信客服后台 → 开发配置 → Secret |
| `WXKF_TOKEN` | ✅ | 回调验签 Token | 自己设，与企业内部接入回调表单填的一致 |
| `WXKF_AESKEY` | ✅ | 回调消息 EncodingAESKey（43 位）| 自己设，与企业内部接入回调表单填的一致 |
| `WXKF_SERVICER` | 转人工需要 | 转人工时转给的接待人员 userid | 微信客服后台 → 接待人员 |
| `WXKF_MINIAPP_APPID` | 卡片需要 | 小程序 appid | 小程序后台 |
| `WXKF_THUMB_MEDIA_ID` | 卡片需要 | 小程序卡片缩略图素材 id | 企业微信素材接口上传得 |

> **Token/AESKey 务必用无歧义字符**（避开大写 I、小写 l、数字 0/1、大写 O）——曾因 I/l 抄串导致 echostr 验不过（§6 坑②）。生成干净值：`node -e` 随机取安全字符集，见 §7。
> **改完 env 立即生效**（云函数下次冷启动读新值）；若改了 `WXKF_SECRET` 且 token 已缓存，需删 `kfState` 集合里 `_id="token"` 那条强制重取（§6 坑③）。

## 4. 数据库集合（必须先建，否则不回复）

云开发控制台 → 数据库 → 新建集合（缺了会导致每条消息被当「已读」跳过，§6 坑④）：

- `kfState`：存 access_token 缓存、sync 游标、消息去重痕（`seen:<msgid>`）。
- `kfIdentity`：存 external_userid→openid 身份映射（查订单用）。

建议两集合「数据权限」设**仅管理端可读写**（云函数自带管理权限不受限，但挡前端越权）。

## 5. 配置步骤（按顺序，env 必须先于点「完成」）

1. **建集合** `kfState`、`kfIdentity`（§4）。
2. **配云函数环境变量**（§3）——至少 CORPID/SECRET/TOKEN/AESKEY 四个，保存。
3. **部署 kfCallback / kfBind**（敏感函数·见 §7 部署命令）。
4. **绑回调路径**：云开发 HTTP 访问服务把 `kf.luckyducky.cn/kfcallback` 路由到 `kfCallback` 函数。
5. **填企业内部接入回调**：`kf.weixin.qq.com` → 开发配置 → 回调配置 → 修改：
   - 回调URL `https://kf.luckyducky.cn/kfcallback`
   - Token = `WXKF_TOKEN` 的值；EncodingAESKey = `WXKF_AESKEY` 的值
   - 点「完成」→ 微信发 echostr 验证 → kfCallback 验签回明文 → 通过即「已接入」。
   - ⚠️ 冷启动可能 4–7 秒、逼近微信 ~5 秒超时；若第一次失败别改东西、立刻**再点一次**（函数已热）。
6. **真机验**：非企业成员的顾客微信进客服会话，发「物流」→ 应弹高亮菜单气泡。

## 6. 排障手册（真机踩过的坑，按现象查）

**现象：点「完成」报「请填写带 http 或 https 协议头的合法 URL」**
→ URL 框开头/结尾混进了空格（开头空格会让它判定不是 https 开头）。清空手动重输、确认无空格。

**坑②｜现象：报「openapi回调地址请求不通过」（URL 格式已对）**
→ 微信真发了 echostr 但函数验不过。99% 是**表单里的 Token/AESKey 与 env 不是同一个字符**（大写 I vs 小写 l vs 0/O 抄串）。**定位法**：用 §7 的脚本本地构造一个真 echostr 请求打线上，看回的是不是明文——回空＝验签/解密对不上。**根治**：用无歧义字符重新生成两个值，表单 + env 从同一份复制粘贴。

**坑③｜现象：echostr 过了、发消息后日志 `[kf] sync errcode:95012`、无回复**
→ `95012 = 无权限调用接口`。`WXKF_SECRET` 填错了——用成了**企业微信自建应用的 Secret**，要用**微信客服「开发配置」页的 Secret**。两者 token 权限范围不同，自建应用 token 调不了 `kf/sync_msg`。改 env 后若旧 token 已缓存，删 `kfState` 的 `_id="token"` 记录强制重取。（实测：微信客服 secret 不卡可信IP，云函数任意出口 IP 可调；自建应用 secret 才要配可信IP，会报 60020。）

**坑④｜现象：sync 成功 `errcode:0` 但日志全是 `[kf] dup-skip`、不回复**
→ `kfState` 集合没建。去重靠往 `kfState` 写「已读痕」，集合不存在→写失败→每条消息被当「已处理过」跳过。建 `kfState`、`kfIdentity` 两集合即可。

**现象：一次冒出好几个气泡**
→ 多为**历史积压一次性回**（之前失败期间发的消息攒着，首次成功 sync 全回了）。去重生效后同一条不会再重复回，发一条新消息验证「一条一回」即可。

**转人工模型（2026-07-02 起·承面C 自建工作台版·调试日志 AC）**
→ 转人工**不再调平台 `service_state/trans`**（守卫 `agent-channel-stays-assistant` 焊死）：会话入自建 `csSession` 待接队列（`/agent` 坐席工作台认领/回复/结束）+ 发文字确认；**平台会话恒智能助手态(1)**——坐席回复走 `send_msg` API、仅 state=1 可发，曾转 3（原生接待台模式）致坐席发送全被 **95018** 拒。bot 不抢话由**我方 csSession 态**判（pending/active/escalated 静默·closed 恢复）。**企业微信原生接待台自此退出人工链路**（历史路 A/B 取舍与 48002 排障见 git 2026-07-02 前版本·AB 拍板 A 已被取代）。⚠️ 若在接待台手动接入过某会话（state 3），坐席对它发消息仍 95018——在接待台结束该会话即恢复。

**现象：进会话欢迎气泡发不出 `[LD_ALERT] SENDMSG_FAILED errcode:40058`**
→ `40058 = 不合法参数`。常见两因：① 进会话事件的 `external_userid` 在 `event` 子对象、取顶层取成空 touser（已在代码修，抽 `extUserId` 兜两种 wire 形状）；② 缺 `WXKF_MINIAPP_APPID`/`WXKF_THUMB_MEDIA_ID` 致小程序卡片参数不全。

**坑⑤｜现象：发消息 bot 不回（`[LD_ALERT] SENDMSG_FAILED errcode:95018`，或日志 `[kf] service-state {state:3}` + `assistant-gate {gate:'skip'}`）（调试日志 AB）**
→ **先分清 bot 是真坏还是会话在人工态**。微信客服 `send_msg` 由 API 发**只在 `service_state=1`（智能助手）允许**；顾客一旦点过「找人工」→会话转 state 3（人工接待·聊天里显示「已接入人工服务」），此后该会话所有消息都归人工，bot 发不出（旧代码硬发→95018）或正确避让（现代码 `skip`）。**最常见误判**：拿一条**被转过人工的会话**测 bot，看它不回就以为 bot 全坏——其实换条干净新会话 bot 正常（欢迎菜单/关键词都回）。
→ **定位法**：看日志 `[kf] service-state {state}`——`0`（新会话·未处理）/`1`（智能助手）＝bot 会回；`3`（人工）/`2`（排队）/`4`（已结束）＝bot 让位、这条得人工在接待台回或起新会话。**验 bot 是否正常：用一个从没找过人工的干净微信发消息**（→ state 0 → bot 回）。
→ **代码侧（已上）**：`kfCallback` 回复前经 `kit/wecom.ts` 的 `ensureSmartAssistant`（读 `service_state/get`→**只接管 0 新会话[trans→1]·1 直发·2/3/4 让位 skip**——平台不允许 2→1、4→1[4→1 报 95013]，只硬试新会话免告警噪声）。日志 `assistant-gate {gate:'proceed'}` 即接管成功。（转 state 1 **不需要** servicer_userid，转 3 才需。）
→ **运营**：确认「通过API管理微信客服账号」已把该账号托管给自建应用（企业微信后台 → 微信客服 → 企业内部开发·本项目 Lucky Ducky 已托管）；「找人工」转人工后须有人在**企业微信接待台**应答，否则顾客卡人工态无回复——无人力则考虑改「找人工」话术不真转人工（`dispatch.ts` 转人工分支）。

**通用：看链路日志**
`npx tcb fn log kfCallback -e cloudbase-d4gcssqbv06865479`，盯 `[kf]` 全链路（onEvent / cursor-read / sync{errcode,msgs,nc} / handle / **assistant-gate** / sent / dup-skip）+ `[LD_ALERT]` 告警码。

## 7. 实操工具

**部署 kfCallback**（敏感函数·guard-deploy 会二次确认）：
```bash
npm run build:rw-cloud
npx tcb fn deploy kfCallback --dir rewrite/cloud/dist/kfCallback --force -e cloudbase-d4gcssqbv06865479
```
（重写线产物在 `rewrite/cloud/dist/<函数名>`；`packages/cloud/dist` 是已冻结旧线，勿再用。逐函数部署与顺序纪律见 `docs/运维手册.md` ①。）

**生成无歧义 Token + EncodingAESKey**：
```bash
node -e 'const c=require("crypto");const BAD=new Set(["I","l","O","0","1","o","i"]);const S="ABCDEFGHJKLMNPQRSTUVWXYZabcdefghjkmnpqrstuvwxyz23456789".split("").filter(x=>!BAD.has(x));let t="";while(t.length<16)t+=S[c.randomBytes(1)[0]%S.length];let a;do{a="";while(a.length<43)a+=S[c.randomBytes(1)[0]%S.length]}while(Buffer.from(a+"=","base64").length!==32);console.log("Token =",t,"\nAESKey=",a)'
```

**本地构造真 echostr 验证请求打线上**（定位坑②/坑③，传 Token、AESKey）：思路＝按 WXBizMsgCrypt 用 AESKey 加密一段明文得 echostr、用 Token 算 msg_signature、GET 打 `kfcallback`，看 body 是否回明文。完整脚本见 2026-06-17 会话记录（`kftest`）。

## 8. 功能清单（机器人能干啥）

逻辑全在 `cs/kfCallback/dispatch.ts`：
- **进会话欢迎**：顾客打开对话 → 弹 5 大入口菜单。
- **关键词识别**：4 大类（物流 / 激活码·课程·视频 / 退换·售后 / 钩织教程）→ 弹对应高亮 msgmenu 气泡。
- **菜单点选分流**：下级菜单 / FAQ 文字答 / 小程序卡片 / 查订单 / 转人工。
- **FAQ 文字答**（写死 4 条）：多久发货、改地址、激活码怎么用、退换政策。
- **查我的订单**（实时·零幻觉·✅2026-07-01 真机验通）：经身份桥接查最近 3 单状态+运单（小程序已绑开放平台→login 存 unionid→kfCallback `batchget` 反查建 kfIdentity→`resolveOpenid` 查单）。**前提：顾客用同一个微信登录过小程序**（否则回"还没识别到你的小程序账号，请先登录"）；认出但无单回"没查到你名下的订单"。
- **小程序卡片**：申请售后页、我的课程页（需 §3 卡片相关 env）。
- **转人工**：点「人工」或涉承诺/纠纷 → **先回「已转接人工，请稍候」文字** + 尽力调转接 API（自动分配接口 48002 无权限·见 §6，当前靠接待人员在接待台手动接入；权限打通后自动生效）。
- **去重 / 防伪**：同一条只回一次；回调验签解密 fail-closed。

**改话术/关键词/菜单/FAQ**：改 `dispatch.ts` → `npm run check` → 重新部署 kfCallback。目前没有可视化后台改话术，若需要是另一块要单独做的活。
