# 外包坐席工作台环境变量（复制为 .env.local 使用）
#
# 车道 B 当前对 mock 建：不配 VITE_ADMIN_API 时全走内存 mock（api/mock.js），
# 可离线开发/演示全套 UI。master 整合车道 A 真接口时，配上服务地址即切真：
#
#   VITE_ADMIN_API=https://<env>.service.tcloudbase.com/adminapi
#
# 坐席台 8 个 action（listQueue/claim/release/send/getThread/setAgentStatus/escalate/close）
# 走同一 adminApi 网关（cap agent:handle·见 shared/csAgentDesk.ts）；360 侧栏走 getCustomer360，
# 快捷回复走 listKb。切真接口只改这个变量，组件零改（单点接缝 api/agentApi.js）。
#
# 强制 mock（即使配了 API 也用 mock·联调期方便）：
#   VITE_AGENT_MOCK=1
