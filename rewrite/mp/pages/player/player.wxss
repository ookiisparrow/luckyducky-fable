/* 课程播放页·竖屏沉浸全屏重设计（播放器重设计战役 批B：P1 常态结构重排 + P4 段落播完不自动切换；
   批C：P2 自绘 seek 条 + 阻尼震感 + 关键节点磁吸 + 拖动预览）。
   整页纯黑铺满；视频 1:1.68（宽:高）·padding-top 百分比法（避 aspect-ratio·旧 WebView 不支持），
   垂直居中；上下 UI 是黑条覆盖层（绝对定位 + z-index 盖在 video 上·同层渲染官方已允许，不必 cover-view）。
   <video> 与全部既有回归事件绑定语义不变，本文件重排结构与视觉 + 段落进度条浮层 + 播完通栏重播。
   设计稿（播放器重设计.html）画布宽 402px，本页 rpx 换算比例 750/402≈1.866（px × 1.866 ≈ rpx）。 */
.ld-player {
  position: relative;
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  background: #000000; /* 整页纯黑（设计定案：全屏沉浸） */
}

/* —— 顶条（覆盖层·避让状态栏·标题居中·右侧空 spacer 与返回钮等宽·右上刻意留白给原生投屏按钮） ——
   右侧安全缓冲＝spacer(64rpx) + 本行右 padding(56rpx) = 120rpx，与本页改版前的既有留白量（原 `right:120rpx`
   整体偏移）对齐（评审复核：改版初版把右 padding 收到 16rpx、缓冲缩到约 80rpx，未经真机核实即收窄，
   已按原留白量补回；spacer 仍与返回钮等宽以保标题视觉居中，真机走查后如需再调此处一并改）。 */
.lp-topbar {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  /* z-index 高于求助面板遮罩（.lp-help-mask 20·position:fixed）：.ld-player 无 transform，fixed 元素
     以视口为包含块，仅靠 z-index 定层级——若不高于遮罩，面板打开时自绘返回箭头会被遮罩完全盖住、
     点不到，onBack() 里的求助面板逐层收口分支（helpSrc/helpPanel 四级链，见 player.ts）将永远
     打不到（评审 finding 复核·2026-07-11 批D 补丁）。仍低于加桌引导遮罩（.lp-dg-mask 30）——
     该弹窗不需要物理返回箭头参与（自身两个按钮已闭环，见 player.ts onBack 第二次按返回的判定）。 */
  z-index: 21;
  display: flex;
  align-items: center;
  gap: 14rpx;
  padding: 12rpx 56rpx 20rpx 8rpx;
  background: linear-gradient(180deg, rgba(0, 0, 0, 0.55) 0%, rgba(0, 0, 0, 0) 100%);
  box-sizing: content-box;
}
.lp-back {
  flex: 0 0 auto;
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-back-ico {
  width: 32rpx;
  height: 32rpx;
  transform: rotate(180deg); /* chevron-right 转向作返回箭头（复用现有图标资产，不新增素材） */
  filter: brightness(0) invert(1); /* 图标颜色写死在 svg 内·此处仅播放器局部反白，不入 tokens */
}
.lp-topbar-title {
  flex: 1;
  min-width: 0;
  text-align: center;
}
.lp-title-main {
  font-size: 28rpx;
  font-weight: 600;
  color: #ffffff;
  text-shadow: 0 2rpx 12rpx rgba(0, 0, 0, 0.4);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.lp-title-sub {
  margin-top: 4rpx;
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.6);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.lp-topbar-spacer {
  flex: 0 0 auto;
  width: 64rpx;
  height: 64rpx;
}

/* 投屏钮（T1 终态·标题组与 spacer 之间）+ 首次气泡（弱化胶囊·CAST_TIP_KEY 仅展示一次） */
.lp-cast {
  flex: 0 0 auto;
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-cast-ico {
  width: 34rpx;
  height: 34rpx;
}
.lp-cast-tip {
  position: absolute;
  top: 96rpx;
  right: 56rpx;
  z-index: 22;
  padding: 12rpx 22rpx;
  border-radius: var(--ld-radius-pill);
  background: var(--ld-duck);
  color: var(--ld-purple-ink);
  font-size: 22rpx;
  font-weight: 600;
  white-space: nowrap;
  box-shadow: 0 8rpx 26rpx rgba(0, 0, 0, 0.3);
}

/* —— 视频舞台 —— */
.lp-stage {
  position: absolute;
  inset: 0;
  z-index: 1;
  display: flex;
  align-items: center;
  width: 100%;
  height: 100%;
}
.lp-video-box {
  position: relative;
  width: 100%;
  padding-top: 168%; /* 1:1.68（宽:高）·padding-top 百分比法 */
}
.lp-video {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  /* object-fit 用默认 contain：旧 16:9 素材自动上下留黑居中，不裁切（设计定案，不显式覆盖） */
}

/* 全屏单击点击层（透明·playing 才挂） */
.lp-tapzone {
  position: absolute;
  inset: 0;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: center;
}
/* 暂停指示（非按钮·整屏单击即恢复·仅视觉提示·点它也走冒泡命中父层 tapzone） */
.lp-pause-badge {
  width: 112rpx;
  height: 112rpx;
  border-radius: 50%;
  background: rgba(0, 0, 0, 0.36);
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-pause-ico {
  width: 60rpx;
  height: 60rpx;
  filter: brightness(0) invert(1);
}

/* 单击提示条（设计 .tap-hint·402px 画布 ×1.866 换算 rpx）：playing 且未被首次 tap 关闭过 */
.lp-tap-hint {
  position: absolute;
  top: 26rpx;
  left: 50%;
  transform: translateX(-50%);
  z-index: 3;
  padding: 9rpx 26rpx;
  border-radius: 26rpx;
  background: rgba(0, 0, 0, 0.45);
  border: 1rpx solid rgba(255, 255, 255, 0.24);
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.6);
  white-space: nowrap;
}

/* 分段进度条浮层（设计 .segstrip：不用 backdrop-filter——CLAUDE §4 css-compat 避雷，
   直接把底色不透明度提到 .72 替代毛玻璃）。段落层级只活在这里，目录页只到课时层。 */
.lp-segstrip {
  position: absolute;
  left: 37rpx;
  right: 37rpx;
  top: 24rpx;
  z-index: 3;
  display: flex;
  flex-direction: column;
  gap: 17rpx;
  background: rgba(20, 15, 30, 0.72);
  border: 1rpx solid rgba(255, 255, 255, 0.24);
  border-radius: 30rpx;
  padding: 24rpx 28rpx 20rpx;
}
.lp-segs {
  display: flex;
  gap: 8rpx;
}
.lp-seg-cell {
  flex: 1;
  height: 11rpx;
  border-radius: 6rpx;
  background: rgba(255, 255, 255, 0.24); /* todo（默认·防脏值兜底同 todo 语义） */
}
.lp-seg-cell.done {
  background: var(--ld-purple);
}
.lp-seg-cell.cur {
  background: var(--ld-duck);
}
.lp-segcap {
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.lp-segcap-text {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.6);
}
.lp-legend {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  gap: 11rpx;
}
.lp-legend-dot {
  font-size: 18rpx;
  line-height: 1;
  color: var(--ld-duck);
}
.lp-legend-text {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.6);
}

/* —— T2 控制器态（casting==='connected'）：盖满舞台的电视遥控面板 —— */
.lp-castctl {
  position: absolute;
  inset: 0;
  z-index: 5;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 30rpx;
  padding: 0 40rpx;
  background: rgba(10, 8, 14, 0.92);
}
.lp-castctl-card {
  width: 100%;
  background: #1c1626;
  border-radius: 20rpx;
  padding: 40rpx 34rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
}
.lp-castctl-tv-sub {
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.56);
}
.lp-castctl-tv-name {
  font-size: 36rpx;
  font-weight: 700;
  color: #ffffff;
}
.lp-castctl-tv-dot {
  display: flex;
  align-items: center;
  gap: 8rpx;
  font-size: 22rpx;
  color: #6bd88a;
  margin-top: 4rpx;
}
.lp-castctl-tv-dot-mark {
  width: 12rpx;
  height: 12rpx;
  border-radius: 50%;
  background: #6bd88a;
}
.lp-castctl-tv-row {
  display: flex;
  gap: 20rpx;
  margin-top: 24rpx;
  width: 100%;
}
.lp-castctl-pill {
  flex: 1;
  height: 76rpx;
  border-radius: var(--ld-radius-pill);
  background: var(--ld-duck);
  color: #4a3408;
  font-size: 26rpx;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-castctl-pill.ghost {
  background: transparent;
  color: var(--ld-duck);
  border: 2rpx solid var(--ld-duck);
}
.lp-castctl-seg {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 14rpx;
}
.lp-castctl-seg-text {
  font-size: 26rpx;
  font-weight: 600;
  color: #ffffff;
  text-align: center;
}
.lp-castctl-chip {
  padding: 8rpx 22rpx;
  border-radius: var(--ld-radius-pill);
  background: var(--ld-duck);
  color: var(--ld-purple-ink);
  font-size: 20rpx;
  font-weight: 700;
}
.lp-castctl-hint {
  font-size: 20rpx;
  color: rgba(255, 255, 255, 0.44);
  text-align: center;
}
/* 遥控行播放/暂停大钮内的自绘暂停条（paused 时改显 play-circle.svg，见 wxml） */
.lp-castctl-pausebars {
  display: flex;
  gap: 10rpx;
}
.lp-castctl-pausebar {
  width: 9rpx;
  height: 34rpx;
  border-radius: 4rpx;
  background: #ffffff;
}
/* 快进 15 钮：复用 rotate-ccw 图标水平镜像（回看 15 用原方向），不新增素材 */
.lp-chev-fwd {
  transform: scaleX(-1);
}

/* 首帧前骨架（loading·首帧到达前不裂播放器） */
.lp-skeleton {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
  background: #0a0a0d;
}
.lp-skeleton-spin {
  width: 56rpx;
  height: 56rpx;
  border-radius: 50%;
  border: 5rpx solid rgba(255, 255, 255, 0.18);
  border-top-color: var(--ld-duck);
  animation: lp-spin 0.9s linear infinite;
}
@keyframes lp-spin {
  to {
    transform: rotate(360deg);
  }
}
.lp-skeleton-text {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.62);
}

/* 非播放态占位（empty/denied/missing/error·全屏黑底居中·顶条返回始终可用） */
.lp-state {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 16rpx;
  padding: 0 40rpx;
}
.lp-state-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #ffffff;
  text-align: center;
}
.lp-state-hint {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.56);
  text-align: center;
}
.lp-state-btn {
  margin-top: 10rpx;
  padding: 16rpx 46rpx;
  border-radius: var(--ld-radius-pill);
  background: var(--ld-duck);
  color: #4a3408; /* 深棕·鸭黄按钮文字（局部对比色） */
  font-size: 26rpx;
  font-weight: 600;
}
.lp-state-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 10rpx;
}
.lp-state-actions .lp-state-btn {
  margin-top: 0;
}
.lp-state-btn.ghost {
  background: rgba(255, 255, 255, 0.14);
  color: #ffffff;
  border: 2rpx solid rgba(255, 255, 255, 0.3);
}

/* —— 底条（覆盖层·仅播放中显示） —— */
.lp-bottombar {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 4;
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  padding: 40rpx 30rpx calc(28rpx + env(safe-area-inset-bottom));
  padding-bottom: calc(28rpx + constant(safe-area-inset-bottom)); /* 旧内核双写兜底 */
  padding-bottom: calc(28rpx + env(safe-area-inset-bottom));
  background: linear-gradient(0deg, rgba(0, 0, 0, 0.62) 0%, rgba(0, 0, 0, 0) 100%);
}

/* 通栏重播长条（设计 .replay：仅段落播完 segDone 时显示） */
.lp-replay {
  width: 100%;
  height: 108rpx;
  border-radius: 54rpx;
  background: #ffffff;
  color: #241733; /* 紫墨字 --purple-ink */
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 19rpx;
  font-size: 32rpx;
  font-weight: 700;
  box-shadow: 0 15rpx 52rpx rgba(0, 0, 0, 0.4);
}
.lp-replay-ico {
  width: 36rpx;
  height: 36rpx;
}

/* 进度行：时间 + slider + 时间 */
.lp-progress-row {
  display: flex;
  align-items: center;
  gap: 14rpx;
}
.lp-time {
  flex: 0 0 auto;
  font-size: 21rpx;
  color: rgba(255, 255, 255, 0.8);
  min-width: 64rpx;
  text-align: center;
}
.lp-time.cur-strong {
  color: #ffffff;
  font-weight: 700;
}

/* 自绘 seek 条（设计 .seek·画布 402px×1.866 换算，节点/触摸热区数值按批C 规格显式给定）：
   .lp-seek 是拖动触摸容器（≥72rpx 热区，扩大真机可点中率），内部视觉轨道保持设计原细身。 */
.lp-seek {
  position: relative;
  flex: 1;
  height: 72rpx;
  display: flex;
  align-items: center;
}
.lp-seek-track {
  position: absolute;
  left: 0;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  height: 8rpx;
  border-radius: 4rpx;
  background: rgba(255, 255, 255, 0.24);
}
.lp-seek-fill {
  position: absolute;
  left: 0;
  top: 50%;
  transform: translateY(-50%);
  height: 8rpx;
  border-radius: 4rpx;
  background: var(--ld-purple);
}
.lp-seek-node {
  position: absolute;
  top: 50%;
  width: 14rpx;
  height: 14rpx;
  transform: translate(-50%, -50%) rotate(45deg);
  background: var(--ld-duck);
}
.lp-seek-handle {
  position: absolute;
  top: 50%;
  width: 26rpx;
  height: 26rpx;
  transform: translate(-50%, -50%);
  border-radius: 50%;
  background: #ffffff;
  border: 4rpx solid var(--ld-purple);
}
.lp-seek-handle.big {
  width: 38rpx;
  height: 38rpx;
}

/* 拖动浮层（seeking 时显示·seek 条上方 absolute）：时间预览胶囊 + 命中关键动作节点再叠气泡。
   left 值不用 WXSS clamp()（本仓无先例·真机支持度未经验证）——player.ts previewLeftPx() 在 JS 侧算好
   夹在 [PREVIEW_EDGE_RPX, 轨道宽-PREVIEW_EDGE_RPX] 内的 px 落点，wxml 直接内联 px（MARK_SNAP_SEC
   磁吸窗口同批·真机再调参）。 */
.lp-seek-preview {
  position: absolute;
  bottom: 100%;
  margin-bottom: 14rpx;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 10rpx;
  pointer-events: none;
}
.lp-seek-preview-time {
  padding: 10rpx 24rpx;
  border-radius: var(--ld-radius-pill);
  background: rgba(20, 15, 30, 0.88);
  border: 1rpx solid rgba(255, 255, 255, 0.24);
  font-size: 30rpx;
  font-weight: 700;
  color: #ffffff;
  white-space: nowrap;
}
.lp-seek-preview-bubble {
  padding: 15rpx 26rpx;
  border-radius: 34rpx;
  background: var(--ld-duck);
  color: var(--ld-purple-ink);
  font-size: 22rpx;
  font-weight: 700;
  white-space: nowrap;
  box-shadow: 0 7rpx 26rpx rgba(242, 172, 60, 0.4);
}

/* 控制行：上一段 chevron · 中央求助大圆钮 · 下一段 chevron（设计 .crow） */
.lp-controls {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 75rpx;
}
.lp-chev {
  flex: 0 0 auto;
  width: 64rpx;
  height: 64rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-chev.dim {
  opacity: 0.36;
}
.lp-chev-ico {
  width: 40rpx;
  height: 40rpx;
  filter: brightness(0) invert(1);
}
.lp-chev-prev {
  transform: rotate(180deg);
}
.lp-helpbtn {
  flex: 0 0 auto;
  width: 108rpx;
  height: 108rpx;
  border-radius: 50%;
  background: var(--ld-purple);
  box-shadow: 0 11rpx 37rpx rgba(163, 113, 234, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-helpbtn-ico {
  width: 48rpx;
  height: 48rpx;
}

/* —— 求助面板（P3·播放器重设计战役批D·设计稿画布 402px×1.866 换算）—— */
.lp-help-mask {
  position: fixed;
  inset: 0;
  z-index: 20;
  background: rgba(0, 0, 0, 0.65);
  display: flex;
  align-items: flex-end;
}
.lp-help-sheet {
  width: 100%;
  height: 75vh; /* 约 75% 屏高（批D 规格） */
  background: #ffffff;
  color: var(--ld-ink);
  border-radius: 45rpx 45rpx 0 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  padding-bottom: env(safe-area-inset-bottom);
}
.lp-help-grab {
  flex: 0 0 auto;
  width: 82rpx;
  height: 9rpx;
  border-radius: 6rpx;
  background: var(--ld-line);
  margin: 16rpx auto 8rpx;
}
.lp-help-head {
  flex: 0 0 auto;
  padding: 0 37rpx 20rpx;
}
.lp-help-title {
  font-size: 41rpx;
  font-weight: 700;
  color: var(--ld-ink);
}
.lp-help-sub {
  margin-top: 11rpx;
  font-size: 26rpx;
  color: var(--ld-content-2);
}
.lp-help-card {
  flex: 0 0 auto;
  margin: 0 37rpx 20rpx;
  border: 1rpx solid var(--ld-line);
  border-radius: 34rpx;
  padding: 37rpx;
  display: flex;
  align-items: center;
  gap: 26rpx;
}
.lp-help-card-pic {
  flex: 0 0 auto;
  width: 97rpx;
  height: 97rpx;
  border-radius: 49rpx;
  background: var(--ld-purple);
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-help-card-ico {
  width: 45rpx;
  height: 45rpx;
}
.lp-help-card-tx {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 7rpx;
}
.lp-help-card-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--ld-ink);
}
.lp-help-card-sub {
  font-size: 24rpx;
  color: var(--ld-content-2);
}
.lp-help-card-chev {
  flex: 0 0 auto;
  width: 28rpx;
  height: 28rpx;
}
/* 子层（videos/faq）顶部导航：左返回箭头 + 居中标题（内嵌播放视图复用同一条导航条） */
.lp-help-nav {
  flex: 0 0 auto;
  display: flex;
  align-items: center;
  gap: 22rpx;
  padding: 19rpx 30rpx 30rpx;
}
.lp-help-back {
  flex: 0 0 auto;
  width: 67rpx;
  height: 67rpx;
  border-radius: 34rpx;
  background: #f2ebfc;
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-help-back-ico {
  width: 32rpx;
  height: 32rpx;
  transform: rotate(180deg); /* chevron-right 转向作返回箭头（复用现有图标资产） */
}
.lp-help-nav-title {
  flex: 1;
  min-width: 0;
  text-align: center;
  padding-right: 67rpx; /* 与左侧返回钮等宽，标题视觉居中 */
  font-size: 32rpx;
  font-weight: 700;
  color: var(--ld-ink);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

/* 帮助视频子层：主题列表自滚（sheet 壳体本身已 catch:touchmove 锁背景，此处内部单独可滚） */
.lp-help-scroll {
  flex: 1;
  min-height: 0;
  overflow: hidden;
  padding: 0 37rpx 45rpx;
}
.lp-help-group-title {
  font-size: 26rpx;
  font-weight: 700;
  color: var(--ld-ink);
  padding: 13rpx 4rpx 15rpx;
}
.lp-help-topic {
  display: flex;
  flex-direction: column;
  gap: 15rpx;
  margin-bottom: 37rpx;
}
.lp-help-topic-title {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--ld-ink);
}
.lp-help-topic-sub {
  margin-top: -7rpx;
  font-size: 24rpx;
  color: var(--ld-content-2);
}
.lp-help-seg {
  display: flex;
  align-items: center;
  gap: 26rpx;
  background: #ffffff;
  border: 1rpx solid var(--ld-line);
  border-radius: 30rpx;
  padding: 22rpx;
}
.lp-help-seg.dim {
  opacity: 0.5; /* url:null（未剪辑段）视觉置灰，tap 处理器内也短路不可点（双保险） */
}
.lp-help-seg-thumb {
  flex: 0 0 auto;
  width: 134rpx;
  height: 97rpx;
  border-radius: 19rpx;
  background: linear-gradient(150deg, #4a3b68, #2c2242);
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-help-seg-ico {
  width: 37rpx;
  height: 37rpx;
  filter: brightness(0) invert(1); /* 深色缩略位上反白，同页既有反白手法 */
}
.lp-help-seg-tx {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  gap: 7rpx;
}
.lp-help-seg-name {
  font-size: 26rpx;
  font-weight: 500;
  color: var(--ld-ink);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}
.lp-help-seg-dur {
  font-size: 22rpx;
  color: var(--ld-content-2);
}

/* 帮助视频/常见问题空态（loading/empty 共用容器，文案由 wxml 分支） */
.lp-help-empty {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 20rpx;
  padding: 60rpx 40rpx;
  text-align: center;
  color: var(--ld-content-2);
  font-size: 26rpx;
}
.lp-help-empty-btn {
  margin-top: 10rpx;
  padding: 16rpx 46rpx;
  border-radius: var(--ld-radius-pill);
  background: var(--ld-duck);
  color: #4a3408;
  font-size: 26rpx;
  font-weight: 600;
}

/* 内嵌播放视图（P3b-play 简化决策：原生 <video controls>，不做自绘控制条——辅助内容非主路径） */
.lp-help-play {
  flex: 1;
  min-height: 0;
  background: #000000;
  display: flex;
  align-items: center;
}
.lp-help-play-video {
  width: 100%;
  height: 100%;
}

/* —— 首次成功退出加桌引导（P5b·仅展示一次） —— */
.lp-dg-mask {
  position: fixed;
  inset: 0;
  z-index: 30;
  background: rgba(0, 0, 0, 0.65);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 58rpx;
}
.lp-dg-dlg {
  width: 100%;
  max-width: 620rpx;
  background: #ffffff;
  color: var(--ld-ink);
  border-radius: 45rpx;
  padding: 48rpx 41rpx 37rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 26rpx;
}
.lp-dg-title {
  font-size: 35rpx;
  font-weight: 700;
  text-align: center;
}
/* 矢量循环动画降级为静态示意区+文案（简化决策·真机动画开销与辅助内容优先级不匹配，记账见重构日志） */
.lp-dg-guide {
  width: 100%;
  background: #f2ebfc;
  border-radius: 30rpx;
  padding: 26rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 15rpx;
}
.lp-dg-guide-pic {
  width: 80rpx;
  height: 80rpx;
  border-radius: 40rpx;
  background: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
}
.lp-dg-guide-ico {
  width: 40rpx;
  height: 40rpx;
}
.lp-dg-guide-text {
  font-size: 24rpx;
  color: var(--ld-content);
  text-align: center;
  line-height: 1.6;
}
.lp-dg-stay {
  width: 100%;
  height: 93rpx;
  border-radius: 47rpx;
  background: var(--ld-purple-ink);
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 30rpx;
  font-weight: 700;
}
.lp-dg-quit {
  font-size: 26rpx;
  color: var(--ld-content-2);
  padding: 11rpx 0 4rpx;
}
