/* 设计 token 单一来源（M2 打样载体·承接旧线 uni.scss 幸运紫色票——UI 重设计拍板后只改这里）。
   CSS 变量在双引擎均已真机验证（M0 spike T1 ✅）。 */
page {
  /* 幸运紫 */
  --ld-purple: #a371ea; /* 强调、已验证 */
  --ld-purple-tab: #7b5caf; /* Tab 选中 */
  --ld-purple-meta: #7e6e96; /* 次要图标标签 */
  --ld-purple-line: #c9bbe0; /* 卡片描边 */
  --ld-purple-ink: #241733; /* 深紫黑：主 CTA */
  /* 中性 */
  --ld-ink: #222222;
  --ld-content: #3d3d42; /* 正文墨（重设计中性文字·非暖棕） */
  --ld-content-2: #6e6e76; /* 次要文字：划线价、用户名、说明 */
  --ld-bg: #ffffff;
  --ld-bg-lilac: #fcfaff;
  --ld-bg-sage: #edeee8; /* 媒体/占位底 */
  --ld-bg-grey: #f3f3f3; /* 买家秀条带底 */
  --ld-bg-faint: #fafafa;
  --ld-line: #e7e7e7; /* 中性发丝线：折叠组/分隔 */
  --ld-line-soft: #eeeeee;
  --ld-surface-cream: #e8e2d6; /* 商品卡暖描边 */
  /* 鸭黄暖点（评分/意象） */
  --ld-duck: #f9bf63;
  --ld-duck-deep: #f2ac3c; /* 买家秀星标 */
  /* 几何 */
  --ld-radius: 24rpx;
  --ld-radius-sm: 10rpx; /* 商品/买家秀卡（设计 5px）*/
  --ld-radius-md: 18rpx; /* 折叠组/特写图（设计 9px）*/
  --ld-radius-pill: 999rpx;
  /* 悬浮胶囊 TabBar 让位带（单源·安全区感知）：屏底 → 胶囊顶边 的预留高度。
     = env(安全区) + 18rpx 浮起间距 + 102rpx 胶囊高（.ld-tabbar padding 12×2 + .ld-tab padding 20×2 + icon 38）。
     几何随 custom-tab-bar/index.wxss 同步。凡「贴在悬浮 TabBar 上方」的元素统一 calc(var(--ld-tabbar-space) + 视觉间距)，
     不再各页拍魔法数——安全区变化时（有无小黑条）胶囊上浮，元素与它的间距恒定。 */
  --ld-tabbar-space: calc(env(safe-area-inset-bottom) + 120rpx);
  /* 品牌字体（文源圆体 WenYuan Rounded SC）：子集 WebFont 远程加载（loadBrandFonts·utils/brandFont.ts）
     到达前/失败时回退系统圆体栈（FOUT 可接受）。回退栈照抄旧线 uni.scss $font-display（逐项核对）。
     应用范围＝全局 page（战役 flag·主脑拍板，非老线「仅标题」先例）。 */
  --ld-font-brand: 'WenYuan Rounded SC', 'PingFang SC', 'SF Pro Rounded', ui-rounded, 'Hiragino Sans GB',
    system-ui, sans-serif;
}
