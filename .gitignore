# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
*.local

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 微信开发者工具在项目根生成的本地配置（不入库）
project.config.json
project.private.config.json

# format-hook 测试的临时格式化目录（afterEach 自删，防意外残留入库）
.fmt-test-*

# build:cloud 并发锁（原子 mkdir，构建完即删；崩溃残留由下次构建 2min 破除）
.build-cloud.lock