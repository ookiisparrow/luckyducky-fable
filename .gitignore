# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
*.local

# Editor directories and files
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 微信开发者工具在项目根生成的本地配置（不入库）
project.config.json
project.private.config.json
# 例外：Skyline spike 是手写的独立原生工程，其项目定义必须入库（导入即用）

# format-hook 测试的临时格式化目录（afterEach 自删，防意外残留入库）
.fmt-test-*

# build:cloud 并发锁（原子 mkdir，构建完即删；崩溃残留由下次构建 2min 破除）
.build-cloud.lock

# 体检面板产物（npm run report 生成·守卫 check-report-in-gates 核不入库）
reports/

# vitest --coverage 产物（B8·仅本地/CI 度量用，不入库）
coverage/